var GUI =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// install a JSONP callback for chunk loading
/******/ 	function webpackJsonpCallback(data) {
/******/ 		var chunkIds = data[0];
/******/ 		var moreModules = data[1];
/******/ 		var executeModules = data[2];
/******/
/******/ 		// add "moreModules" to the modules object,
/******/ 		// then flag all "chunkIds" as loaded and fire callback
/******/ 		var moduleId, chunkId, i = 0, resolves = [];
/******/ 		for(;i < chunkIds.length; i++) {
/******/ 			chunkId = chunkIds[i];
/******/ 			if(Object.prototype.hasOwnProperty.call(installedChunks, chunkId) && installedChunks[chunkId]) {
/******/ 				resolves.push(installedChunks[chunkId][0]);
/******/ 			}
/******/ 			installedChunks[chunkId] = 0;
/******/ 		}
/******/ 		for(moduleId in moreModules) {
/******/ 			if(Object.prototype.hasOwnProperty.call(moreModules, moduleId)) {
/******/ 				modules[moduleId] = moreModules[moduleId];
/******/ 			}
/******/ 		}
/******/ 		if(parentJsonpFunction) parentJsonpFunction(data);
/******/
/******/ 		while(resolves.length) {
/******/ 			resolves.shift()();
/******/ 		}
/******/
/******/ 		// add entry modules from loaded chunk to deferred list
/******/ 		deferredModules.push.apply(deferredModules, executeModules || []);
/******/
/******/ 		// run deferred modules when all chunks ready
/******/ 		return checkDeferredModules();
/******/ 	};
/******/ 	function checkDeferredModules() {
/******/ 		var result;
/******/ 		for(var i = 0; i < deferredModules.length; i++) {
/******/ 			var deferredModule = deferredModules[i];
/******/ 			var fulfilled = true;
/******/ 			for(var j = 1; j < deferredModule.length; j++) {
/******/ 				var depId = deferredModule[j];
/******/ 				if(installedChunks[depId] !== 0) fulfilled = false;
/******/ 			}
/******/ 			if(fulfilled) {
/******/ 				deferredModules.splice(i--, 1);
/******/ 				result = __webpack_require__(__webpack_require__.s = deferredModule[0]);
/******/ 			}
/******/ 		}
/******/
/******/ 		return result;
/******/ 	}
/******/
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// object to store loaded and loading chunks
/******/ 	// undefined = chunk not loaded, null = chunk preloaded/prefetched
/******/ 	// Promise = chunk loading, 0 = chunk loaded
/******/ 	var installedChunks = {
/******/ 		"vm-live": 0
/******/ 	};
/******/
/******/ 	var deferredModules = [];
/******/
/******/ 	// script path function
/******/ 	function jsonpScriptSrc(chunkId) {
/******/ 		return __webpack_require__.p + "js/" + ({"iframe-extension-worker":"iframe-extension-worker"}[chunkId]||chunkId) + ".js"
/******/ 	}
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/ 	// This file contains only the entry chunk.
/******/ 	// The chunk loading function for additional chunks
/******/ 	__webpack_require__.e = function requireEnsure(chunkId) {
/******/ 		var promises = [];
/******/
/******/
/******/ 		// JSONP chunk loading for javascript
/******/
/******/ 		var installedChunkData = installedChunks[chunkId];
/******/ 		if(installedChunkData !== 0) { // 0 means "already installed".
/******/
/******/ 			// a Promise means "currently loading".
/******/ 			if(installedChunkData) {
/******/ 				promises.push(installedChunkData[2]);
/******/ 			} else {
/******/ 				// setup Promise in chunk cache
/******/ 				var promise = new Promise(function(resolve, reject) {
/******/ 					installedChunkData = installedChunks[chunkId] = [resolve, reject];
/******/ 				});
/******/ 				promises.push(installedChunkData[2] = promise);
/******/
/******/ 				// start chunk loading
/******/ 				var script = document.createElement('script');
/******/ 				var onScriptComplete;
/******/
/******/ 				script.charset = 'utf-8';
/******/ 				script.timeout = 120;
/******/ 				if (__webpack_require__.nc) {
/******/ 					script.setAttribute("nonce", __webpack_require__.nc);
/******/ 				}
/******/ 				script.src = jsonpScriptSrc(chunkId);
/******/
/******/ 				// create error before stack unwound to get useful stacktrace later
/******/ 				var error = new Error();
/******/ 				onScriptComplete = function (event) {
/******/ 					// avoid mem leaks in IE.
/******/ 					script.onerror = script.onload = null;
/******/ 					clearTimeout(timeout);
/******/ 					var chunk = installedChunks[chunkId];
/******/ 					if(chunk !== 0) {
/******/ 						if(chunk) {
/******/ 							var errorType = event && (event.type === 'load' ? 'missing' : event.type);
/******/ 							var realSrc = event && event.target && event.target.src;
/******/ 							error.message = 'Loading chunk ' + chunkId + ' failed.\n(' + errorType + ': ' + realSrc + ')';
/******/ 							error.name = 'ChunkLoadError';
/******/ 							error.type = errorType;
/******/ 							error.request = realSrc;
/******/ 							chunk[1](error);
/******/ 						}
/******/ 						installedChunks[chunkId] = undefined;
/******/ 					}
/******/ 				};
/******/ 				var timeout = setTimeout(function(){
/******/ 					onScriptComplete({ type: 'timeout', target: script });
/******/ 				}, 120000);
/******/ 				script.onerror = script.onload = onScriptComplete;
/******/ 				document.head.appendChild(script);
/******/ 			}
/******/ 		}
/******/ 		return Promise.all(promises);
/******/ 	};
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// on error function for async loading
/******/ 	__webpack_require__.oe = function(err) { console.error(err); throw err; };
/******/
/******/ 	var jsonpArray = window["webpackJsonpGUI"] = window["webpackJsonpGUI"] || [];
/******/ 	var oldJsonpFunction = jsonpArray.push.bind(jsonpArray);
/******/ 	jsonpArray.push = webpackJsonpCallback;
/******/ 	jsonpArray = jsonpArray.slice();
/******/ 	for(var i = 0; i < jsonpArray.length; i++) webpackJsonpCallback(jsonpArray[i]);
/******/ 	var parentJsonpFunction = oldJsonpFunction;
/******/
/******/
/******/ 	// add entry module to deferred list
/******/ 	deferredModules.push(["./src/playground/vm-live.jsx","vendors~addon-settings~credits~editor~embed~fullscreen~player~vm-live","vendors~editor~embed~fullscreen~player~vm-live"]);
/******/ 	// run deferred modules when ready
/******/ 	return checkDeferredModules();
/******/ })
/************************************************************************/
/******/ ({

/***/ "../vm/node_modules/worker-loader/dist/cjs.js?name=js/extension-worker/extension-worker.[hash].js!../vm/src/extension-support/extension-worker.js":
/*!********************************************************************************************************************************************************!*\
  !*** ../vm/node_modules/worker-loader/dist/cjs.js?name=js/extension-worker/extension-worker.[hash].js!../vm/src/extension-support/extension-worker.js ***!
  \********************************************************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = function() {
  return new Worker(__webpack_require__.p + "js/extension-worker/extension-worker.cc1a6369f0a4d61c2a70.js");
};

/***/ }),

/***/ "../vm/src/blocks/scratch3_control.js":
/*!********************************************!*\
  !*** ../vm/src/blocks/scratch3_control.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Scratch3ControlBlocks = /*#__PURE__*/function () {
  function Scratch3ControlBlocks(runtime) {
    _classCallCheck(this, Scratch3ControlBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * The "counter" block value. For compatibility with 2.0.
     * @type {number}
     */
    this._counter = 0; // used by compiler

    this.runtime.on('RUNTIME_DISPOSED', this.clearCounter.bind(this));
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3ControlBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        control_repeat: this.repeat,
        control_repeat_until: this.repeatUntil,
        control_while: this.repeatWhile,
        control_for_each: this.forEach,
        control_forever: this.forever,
        control_wait: this.wait,
        control_wait_until: this.waitUntil,
        control_if: this["if"],
        control_if_else: this.ifElse,
        control_stop: this.stop,
        control_create_clone_of: this.createClone,
        control_delete_this_clone: this.deleteClone,
        control_get_counter: this.getCounter,
        control_incr_counter: this.incrCounter,
        control_clear_counter: this.clearCounter,
        control_all_at_once: this.allAtOnce
      };
    }
  }, {
    key: "getHats",
    value: function getHats() {
      return {
        control_start_as_clone: {
          restartExistingThreads: false
        }
      };
    }
  }, {
    key: "repeat",
    value: function repeat(args, util) {
      var times = Math.round(Cast.toNumber(args.TIMES));
      // Initialize loop
      if (typeof util.stackFrame.loopCounter === 'undefined') {
        util.stackFrame.loopCounter = times;
      }
      // Only execute once per frame.
      // When the branch finishes, `repeat` will be executed again and
      // the second branch will be taken, yielding for the rest of the frame.
      // Decrease counter
      util.stackFrame.loopCounter--;
      // If we still have some left, start the branch.
      if (util.stackFrame.loopCounter >= 0) {
        util.startBranch(1, true);
      }
    }
  }, {
    key: "repeatUntil",
    value: function repeatUntil(args, util) {
      var condition = Cast.toBoolean(args.CONDITION);
      // If the condition is false (repeat UNTIL), start the branch.
      if (!condition) {
        util.startBranch(1, true);
      }
    }
  }, {
    key: "repeatWhile",
    value: function repeatWhile(args, util) {
      var condition = Cast.toBoolean(args.CONDITION);
      // If the condition is true (repeat WHILE), start the branch.
      if (condition) {
        util.startBranch(1, true);
      }
    }
  }, {
    key: "forEach",
    value: function forEach(args, util) {
      var variable = util.target.lookupOrCreateVariable(args.VARIABLE.id, args.VARIABLE.name);
      if (typeof util.stackFrame.index === 'undefined') {
        util.stackFrame.index = 0;
      }
      if (util.stackFrame.index < Number(args.VALUE)) {
        util.stackFrame.index++;
        variable.value = util.stackFrame.index;
        util.startBranch(1, true);
      }
    }
  }, {
    key: "waitUntil",
    value: function waitUntil(args, util) {
      var condition = Cast.toBoolean(args.CONDITION);
      if (!condition) {
        util["yield"]();
      }
    }
  }, {
    key: "forever",
    value: function forever(args, util) {
      util.startBranch(1, true);
    }
  }, {
    key: "wait",
    value: function wait(args, util) {
      if (util.stackTimerNeedsInit()) {
        var duration = Math.max(0, 1000 * Cast.toNumber(args.DURATION));
        util.startStackTimer(duration);
        this.runtime.requestRedraw();
        util["yield"]();
      } else if (!util.stackTimerFinished()) {
        util["yield"]();
      }
    }
  }, {
    key: "if",
    value: function _if(args, util) {
      var condition = Cast.toBoolean(args.CONDITION);
      if (condition) {
        util.startBranch(1, false);
      }
    }
  }, {
    key: "ifElse",
    value: function ifElse(args, util) {
      var condition = Cast.toBoolean(args.CONDITION);
      if (condition) {
        util.startBranch(1, false);
      } else {
        util.startBranch(2, false);
      }
    }
  }, {
    key: "stop",
    value: function stop(args, util) {
      var option = args.STOP_OPTION;
      if (option === 'all') {
        util.stopAll();
      } else if (option === 'other scripts in sprite' || option === 'other scripts in stage') {
        util.stopOtherTargetThreads();
      } else if (option === 'this script') {
        util.stopThisScript();
      }
    }
  }, {
    key: "createClone",
    value: function createClone(args, util) {
      this._createClone(Cast.toString(args.CLONE_OPTION), util.target);
    }
  }, {
    key: "_createClone",
    value: function _createClone(cloneOption, target) {
      // used by compiler
      // Set clone target
      var cloneTarget;
      if (cloneOption === '_myself_') {
        cloneTarget = target;
      } else {
        cloneTarget = this.runtime.getSpriteTargetByName(cloneOption);
      }

      // If clone target is not found, return
      if (!cloneTarget) return;

      // Create clone
      var newClone = cloneTarget.makeClone();
      if (newClone) {
        this.runtime.addTarget(newClone);

        // Place behind the original target.
        newClone.goBehindOther(cloneTarget);
      }
    }
  }, {
    key: "deleteClone",
    value: function deleteClone(args, util) {
      if (util.target.isOriginal) return;
      this.runtime.disposeTarget(util.target);
      this.runtime.stopForTarget(util.target);
    }
  }, {
    key: "getCounter",
    value: function getCounter() {
      return this._counter;
    }
  }, {
    key: "clearCounter",
    value: function clearCounter() {
      this._counter = 0;
    }
  }, {
    key: "incrCounter",
    value: function incrCounter() {
      this._counter++;
    }
  }, {
    key: "allAtOnce",
    value: function allAtOnce(args, util) {
      // Since the "all at once" block is implemented for compatiblity with
      // Scratch 2.0 projects, it behaves the same way it did in 2.0, which
      // is to simply run the contained script (like "if 1 = 1").
      // (In early versions of Scratch 2.0, it would work the same way as
      // "run without screen refresh" custom blocks do now, but this was
      // removed before the release of 2.0.)
      util.startBranch(1, false);
    }
  }]);
}();
module.exports = Scratch3ControlBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_core_example.js":
/*!*************************************************!*\
  !*** ../vm/src/blocks/scratch3_core_example.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var BlockType = __webpack_require__(/*! ../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var ArgumentType = __webpack_require__(/*! ../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");

/* eslint-disable-next-line max-len */
var blockIconURI = 'data:image/svg+xml,%3Csvg id="rotate-counter-clockwise" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"%3E%3Cdefs%3E%3Cstyle%3E.cls-1%7Bfill:%233d79cc;%7D.cls-2%7Bfill:%23fff;%7D%3C/style%3E%3C/defs%3E%3Ctitle%3Erotate-counter-clockwise%3C/title%3E%3Cpath class="cls-1" d="M22.68,12.2a1.6,1.6,0,0,1-1.27.63H13.72a1.59,1.59,0,0,1-1.16-2.58l1.12-1.41a4.82,4.82,0,0,0-3.14-.77,4.31,4.31,0,0,0-2,.8,4.25,4.25,0,0,0-1.34,1.73,5.06,5.06,0,0,0,.54,4.62A5.58,5.58,0,0,0,12,17.74h0a2.26,2.26,0,0,1-.16,4.52A10.25,10.25,0,0,1,3.74,18,10.14,10.14,0,0,1,2.25,8.78,9.7,9.7,0,0,1,5.08,4.64,9.92,9.92,0,0,1,9.66,2.5a10.66,10.66,0,0,1,7.72,1.68l1.08-1.35a1.57,1.57,0,0,1,1.24-.6,1.6,1.6,0,0,1,1.54,1.21l1.7,7.37A1.57,1.57,0,0,1,22.68,12.2Z"/%3E%3Cpath class="cls-2" d="M21.38,11.83H13.77a.59.59,0,0,1-.43-1l1.75-2.19a5.9,5.9,0,0,0-4.7-1.58,5.07,5.07,0,0,0-4.11,3.17A6,6,0,0,0,7,15.77a6.51,6.51,0,0,0,5,2.92,1.31,1.31,0,0,1-.08,2.62,9.3,9.3,0,0,1-7.35-3.82A9.16,9.16,0,0,1,3.17,9.12,8.51,8.51,0,0,1,5.71,5.4,8.76,8.76,0,0,1,9.82,3.48a9.71,9.71,0,0,1,7.75,2.07l1.67-2.1a.59.59,0,0,1,1,.21L22,11.08A.59.59,0,0,1,21.38,11.83Z"/%3E%3C/svg%3E';

/**
 * An example core block implemented using the extension spec.
 * This is not loaded as part of the core blocks in the VM but it is provided
 * and used as part of tests.
 */
var Scratch3CoreExample = /*#__PURE__*/function () {
  function Scratch3CoreExample(runtime) {
    _classCallCheck(this, Scratch3CoreExample);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(Scratch3CoreExample, [{
    key: "getInfo",
    value: function getInfo() {
      return {
        id: 'coreExample',
        name: 'CoreEx',
        // This string does not need to be translated as this extension is only used as an example.
        blocks: [{
          func: 'MAKE_A_VARIABLE',
          blockType: BlockType.BUTTON,
          text: 'make a variable (CoreEx)'
        }, {
          opcode: 'exampleOpcode',
          blockType: BlockType.REPORTER,
          text: 'example block'
        }, {
          opcode: 'exampleWithInlineImage',
          blockType: BlockType.COMMAND,
          text: 'block with image [CLOCKWISE] inline',
          arguments: {
            CLOCKWISE: {
              type: ArgumentType.IMAGE,
              dataURI: blockIconURI
            }
          }
        }]
      };
    }

    /**
     * Example opcode just returns the name of the stage target.
     * @returns {string} The name of the first target in the project.
     */
  }, {
    key: "exampleOpcode",
    value: function exampleOpcode() {
      var stage = this.runtime.getTargetForStage();
      return stage ? stage.getName() : 'no stage yet';
    }
  }, {
    key: "exampleWithInlineImage",
    value: function exampleWithInlineImage() {
      return;
    }
  }]);
}();
module.exports = Scratch3CoreExample;

/***/ }),

/***/ "../vm/src/blocks/scratch3_data.js":
/*!*****************************************!*\
  !*** ../vm/src/blocks/scratch3_data.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Scratch3DataBlocks = /*#__PURE__*/function () {
  function Scratch3DataBlocks(runtime) {
    _classCallCheck(this, Scratch3DataBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3DataBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        data_variable: this.getVariable,
        data_setvariableto: this.setVariableTo,
        data_changevariableby: this.changeVariableBy,
        data_hidevariable: this.hideVariable,
        data_showvariable: this.showVariable,
        data_listcontents: this.getListContents,
        data_addtolist: this.addToList,
        data_deleteoflist: this.deleteOfList,
        data_deletealloflist: this.deleteAllOfList,
        data_insertatlist: this.insertAtList,
        data_replaceitemoflist: this.replaceItemOfList,
        data_itemoflist: this.getItemOfList,
        data_itemnumoflist: this.getItemNumOfList,
        data_lengthoflist: this.lengthOfList,
        data_listcontainsitem: this.listContainsItem,
        data_hidelist: this.hideList,
        data_showlist: this.showList
      };
    }
  }, {
    key: "getVariable",
    value: function getVariable(args, util) {
      var variable = util.target.lookupOrCreateVariable(args.VARIABLE.id, args.VARIABLE.name);
      return variable.value;
    }
  }, {
    key: "setVariableTo",
    value: function setVariableTo(args, util) {
      var variable = util.target.lookupOrCreateVariable(args.VARIABLE.id, args.VARIABLE.name);
      variable.value = args.VALUE;
      if (variable.isCloud) {
        util.ioQuery('cloud', 'requestUpdateVariable', [variable.name, args.VALUE]);
      }
    }
  }, {
    key: "changeVariableBy",
    value: function changeVariableBy(args, util) {
      var variable = util.target.lookupOrCreateVariable(args.VARIABLE.id, args.VARIABLE.name);
      var castedValue = Cast.toNumber(variable.value);
      var dValue = Cast.toNumber(args.VALUE);
      var newValue = castedValue + dValue;
      variable.value = newValue;
      if (variable.isCloud) {
        util.ioQuery('cloud', 'requestUpdateVariable', [variable.name, newValue]);
      }
    }
  }, {
    key: "changeMonitorVisibility",
    value: function changeMonitorVisibility(id, visible) {
      // Send the monitor blocks an event like the flyout checkbox event.
      // This both updates the monitor state and changes the isMonitored block flag.
      this.runtime.monitorBlocks.changeBlock({
        id: id,
        // Monitor blocks for variables are the variable ID.
        element: 'checkbox',
        // Mimic checkbox event from flyout.
        value: visible
      }, this.runtime);
    }
  }, {
    key: "showVariable",
    value: function showVariable(args) {
      this.changeMonitorVisibility(args.VARIABLE.id, true);
    }
  }, {
    key: "hideVariable",
    value: function hideVariable(args) {
      this.changeMonitorVisibility(args.VARIABLE.id, false);
    }
  }, {
    key: "showList",
    value: function showList(args) {
      this.changeMonitorVisibility(args.LIST.id, true);
    }
  }, {
    key: "hideList",
    value: function hideList(args) {
      this.changeMonitorVisibility(args.LIST.id, false);
    }
  }, {
    key: "getListContents",
    value: function getListContents(args, util) {
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);

      // If block is running for monitors, return copy of list as an array if changed.
      if (util.thread.updateMonitor) {
        // Return original list value if up-to-date, which doesn't trigger monitor update.
        if (list._monitorUpToDate) return list.value;
        // If value changed, reset the flag and return a copy to trigger monitor update.
        // MonitorState only detects updates when the object changes.
        list._monitorUpToDate = true;
        return list.value.slice();
      }

      // Determine if the list is all single letters.
      // If it is, report contents joined together with no separator.
      // If it's not, report contents joined together with a space.
      var allSingleLetters = true;
      for (var i = 0; i < list.value.length; i++) {
        var listItem = list.value[i];
        if (!(typeof listItem === 'string' && listItem.length === 1)) {
          allSingleLetters = false;
          break;
        }
      }
      if (allSingleLetters) {
        return list.value.join('');
      }
      return list.value.join(' ');
    }
  }, {
    key: "addToList",
    value: function addToList(args, util) {
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      list.value.push(args.ITEM);
      list._monitorUpToDate = false;
    }
  }, {
    key: "deleteOfList",
    value: function deleteOfList(args, util) {
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      var index = Cast.toListIndex(args.INDEX, list.value.length, true);
      if (index === Cast.LIST_INVALID) {
        return;
      } else if (index === Cast.LIST_ALL) {
        list.value = [];
        return;
      }
      list.value.splice(index - 1, 1);
      list._monitorUpToDate = false;
    }
  }, {
    key: "deleteAllOfList",
    value: function deleteAllOfList(args, util) {
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      list.value = [];
      return;
    }
  }, {
    key: "insertAtList",
    value: function insertAtList(args, util) {
      var item = args.ITEM;
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      var index = Cast.toListIndex(args.INDEX, list.value.length + 1, false);
      if (index === Cast.LIST_INVALID) {
        return;
      }
      list.value.splice(index - 1, 0, item);
      list._monitorUpToDate = false;
    }
  }, {
    key: "replaceItemOfList",
    value: function replaceItemOfList(args, util) {
      var item = args.ITEM;
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      var index = Cast.toListIndex(args.INDEX, list.value.length, false);
      if (index === Cast.LIST_INVALID) {
        return;
      }
      list.value[index - 1] = item;
      list._monitorUpToDate = false;
    }
  }, {
    key: "getItemOfList",
    value: function getItemOfList(args, util) {
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      var index = Cast.toListIndex(args.INDEX, list.value.length, false);
      if (index === Cast.LIST_INVALID) {
        return '';
      }
      return list.value[index - 1];
    }
  }, {
    key: "getItemNumOfList",
    value: function getItemNumOfList(args, util) {
      var item = args.ITEM;
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);

      // Go through the list items one-by-one using Cast.compare. This is for
      // cases like checking if 123 is contained in a list [4, 7, '123'] --
      // Scratch considers 123 and '123' to be equal.
      for (var i = 0; i < list.value.length; i++) {
        if (Cast.compare(list.value[i], item) === 0) {
          return i + 1;
        }
      }

      // We don't bother using .indexOf() at all, because it would end up with
      // edge cases such as the index of '123' in [4, 7, 123, '123', 9].
      // If we use indexOf(), this block would return 4 instead of 3, because
      // indexOf() sees the first occurence of the string 123 as the fourth
      // item in the list. With Scratch, this would be confusing -- after all,
      // '123' and 123 look the same, so one would expect the block to say
      // that the first occurrence of '123' (or 123) to be the third item.

      // Default to 0 if there's no match. Since Scratch lists are 1-indexed,
      // we don't have to worry about this conflicting with the "this item is
      // the first value" number (in JS that is 0, but in Scratch it's 1).
      return 0;
    }
  }, {
    key: "lengthOfList",
    value: function lengthOfList(args, util) {
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      return list.value.length;
    }
  }, {
    key: "listContainsItem",
    value: function listContainsItem(args, util) {
      var item = args.ITEM;
      var list = util.target.lookupOrCreateList(args.LIST.id, args.LIST.name);
      if (list.value.indexOf(item) >= 0) {
        return true;
      }
      // Try using Scratch comparison operator on each item.
      // (Scratch considers the string '123' equal to the number 123).
      for (var i = 0; i < list.value.length; i++) {
        if (Cast.compare(list.value[i], item) === 0) {
          return true;
        }
      }
      return false;
    }
  }]);
}();
module.exports = Scratch3DataBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_event.js":
/*!******************************************!*\
  !*** ../vm/src/blocks/scratch3_event.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Scratch3EventBlocks = /*#__PURE__*/function () {
  function Scratch3EventBlocks(runtime) {
    var _this = this;
    _classCallCheck(this, Scratch3EventBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
    this.runtime.on('KEY_PRESSED', function (key) {
      _this.runtime.startHats('event_whenkeypressed', {
        KEY_OPTION: key
      });
      _this.runtime.startHats('event_whenkeypressed', {
        KEY_OPTION: 'any'
      });
    });
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3EventBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        event_whentouchingobject: this.touchingObject,
        event_broadcast: this.broadcast,
        event_broadcastandwait: this.broadcastAndWait,
        event_whengreaterthan: this.hatGreaterThanPredicate
      };
    }
  }, {
    key: "getHats",
    value: function getHats() {
      return {
        event_whenflagclicked: {
          restartExistingThreads: true
        },
        event_whenkeypressed: {
          restartExistingThreads: false
        },
        event_whenthisspriteclicked: {
          restartExistingThreads: true
        },
        event_whentouchingobject: {
          restartExistingThreads: false,
          edgeActivated: true
        },
        event_whenstageclicked: {
          restartExistingThreads: true
        },
        event_whenbackdropswitchesto: {
          restartExistingThreads: true
        },
        event_whengreaterthan: {
          restartExistingThreads: false,
          edgeActivated: true
        },
        event_whenbroadcastreceived: {
          restartExistingThreads: true
        }
      };
    }
  }, {
    key: "touchingObject",
    value: function touchingObject(args, util) {
      return util.target.isTouchingObject(args.TOUCHINGOBJECTMENU);
    }
  }, {
    key: "hatGreaterThanPredicate",
    value: function hatGreaterThanPredicate(args, util) {
      var option = Cast.toString(args.WHENGREATERTHANMENU).toLowerCase();
      var value = Cast.toNumber(args.VALUE);
      switch (option) {
        case 'timer':
          return util.ioQuery('clock', 'projectTimer') > value;
        case 'loudness':
          return this.runtime.audioEngine && this.runtime.audioEngine.getLoudness() > value;
      }
      return false;
    }
  }, {
    key: "broadcast",
    value: function broadcast(args, util) {
      var broadcastVar = util.runtime.getTargetForStage().lookupBroadcastMsg(args.BROADCAST_OPTION.id, args.BROADCAST_OPTION.name);
      if (broadcastVar) {
        var broadcastOption = broadcastVar.name;
        util.startHats('event_whenbroadcastreceived', {
          BROADCAST_OPTION: broadcastOption
        });
      }
    }
  }, {
    key: "broadcastAndWait",
    value: function broadcastAndWait(args, util) {
      if (!util.stackFrame.broadcastVar) {
        util.stackFrame.broadcastVar = util.runtime.getTargetForStage().lookupBroadcastMsg(args.BROADCAST_OPTION.id, args.BROADCAST_OPTION.name);
      }
      if (util.stackFrame.broadcastVar) {
        var broadcastOption = util.stackFrame.broadcastVar.name;
        // Have we run before, starting threads?
        if (!util.stackFrame.startedThreads) {
          // No - start hats for this broadcast.
          util.stackFrame.startedThreads = util.startHats('event_whenbroadcastreceived', {
            BROADCAST_OPTION: broadcastOption
          });
          if (util.stackFrame.startedThreads.length === 0) {
            // Nothing was started.
            return;
          }
        }
        // We've run before; check if the wait is still going on.
        var instance = this;
        // Scratch 2 considers threads to be waiting if they are still in
        // runtime.threads. Threads that have run all their blocks, or are
        // marked done but still in runtime.threads are still considered to
        // be waiting.
        var waiting = util.stackFrame.startedThreads.some(function (thread) {
          return instance.runtime.threads.indexOf(thread) !== -1;
        });
        if (waiting) {
          // If all threads are waiting for the next tick or later yield
          // for a tick as well. Otherwise yield until the next loop of
          // the threads.
          if (util.stackFrame.startedThreads.every(function (thread) {
            return instance.runtime.isWaitingThread(thread);
          })) {
            util.yieldTick();
          } else {
            util["yield"]();
          }
        }
      }
    }
  }]);
}();
module.exports = Scratch3EventBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_looks.js":
/*!******************************************!*\
  !*** ../vm/src/blocks/scratch3_looks.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Clone = __webpack_require__(/*! ../util/clone */ "../vm/src/util/clone.js");
var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var StageLayering = __webpack_require__(/*! ../engine/stage-layering */ "../vm/src/engine/stage-layering.js");
var getMonitorIdForBlockWithArgs = __webpack_require__(/*! ../util/get-monitor-id */ "../vm/src/util/get-monitor-id.js");
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");

/**
 * @typedef {object} BubbleState - the bubble state associated with a particular target.
 * @property {Boolean} onSpriteRight - tracks whether the bubble is right or left of the sprite.
 * @property {?int} drawableId - the ID of the associated bubble Drawable, null if none.
 * @property {string} text - the text of the bubble.
 * @property {string} type - the type of the bubble, "say" or "think"
 * @property {?string} usageId - ID indicating the most recent usage of the say/think bubble.
 *      Used for comparison when determining whether to clear a say/think bubble.
 */
var Scratch3LooksBlocks = /*#__PURE__*/function () {
  function Scratch3LooksBlocks(runtime) {
    _classCallCheck(this, Scratch3LooksBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
    this._onTargetChanged = this._onTargetChanged.bind(this);
    this._onResetBubbles = this._onResetBubbles.bind(this);
    this._onTargetWillExit = this._onTargetWillExit.bind(this);
    this._updateBubble = this._updateBubble.bind(this);

    // Reset all bubbles on start/stop
    this.runtime.on('PROJECT_STOP_ALL', this._onResetBubbles);
    this.runtime.on('targetWasRemoved', this._onTargetWillExit);

    // Enable other blocks to use bubbles like ask/answer
    this.runtime.on(Scratch3LooksBlocks.SAY_OR_THINK, this._updateBubble);
  }

  /**
   * The default bubble state, to be used when a target has no existing bubble state.
   * @type {BubbleState}
   */
  return _createClass(Scratch3LooksBlocks, [{
    key: "_getBubbleState",
    value:
    /**
     * @param {Target} target - collect bubble state for this target. Probably, but not necessarily, a RenderedTarget.
     * @returns {BubbleState} the mutable bubble state associated with that target. This will be created if necessary.
     * @private
     */
    function _getBubbleState(target) {
      var bubbleState = target.getCustomState(Scratch3LooksBlocks.STATE_KEY);
      if (!bubbleState) {
        bubbleState = Clone.simple(Scratch3LooksBlocks.DEFAULT_BUBBLE_STATE);
        target.setCustomState(Scratch3LooksBlocks.STATE_KEY, bubbleState);
      }
      return bubbleState;
    }

    /**
     * Handle a target which has moved.
     * @param {RenderedTarget} target - the target which has moved.
     * @private
     */
  }, {
    key: "_onTargetChanged",
    value: function _onTargetChanged(target) {
      var bubbleState = this._getBubbleState(target);
      if (bubbleState.drawableId) {
        this._positionBubble(target);
      }
    }

    /**
     * Handle a target which is exiting.
     * @param {RenderedTarget} target - the target.
     * @private
     */
  }, {
    key: "_onTargetWillExit",
    value: function _onTargetWillExit(target) {
      var bubbleState = this._getBubbleState(target);
      if (bubbleState.drawableId && bubbleState.skinId) {
        this.runtime.renderer.destroyDrawable(bubbleState.drawableId, StageLayering.SPRITE_LAYER);
        this.runtime.renderer.destroySkin(bubbleState.skinId);
        bubbleState.drawableId = null;
        bubbleState.skinId = null;
        this.runtime.requestRedraw();
      }
      target.onTargetVisualChange = null;
    }

    /**
     * Handle project start/stop by clearing all visible bubbles.
     * @private
     */
  }, {
    key: "_onResetBubbles",
    value: function _onResetBubbles() {
      for (var n = 0; n < this.runtime.targets.length; n++) {
        var bubbleState = this._getBubbleState(this.runtime.targets[n]);
        bubbleState.text = '';
        this._onTargetWillExit(this.runtime.targets[n]);
      }
      clearTimeout(this._bubbleTimeout);
    }

    /**
     * Position the bubble of a target. If it doesn't fit on the specified side, flip and rerender.
     * @param {!Target} target Target whose bubble needs positioning.
     * @private
     */
  }, {
    key: "_positionBubble",
    value: function _positionBubble(target) {
      if (!target.visible) return;
      var bubbleState = this._getBubbleState(target);
      var _this$runtime$rendere = this.runtime.renderer.getCurrentSkinSize(bubbleState.drawableId),
        _this$runtime$rendere2 = _slicedToArray(_this$runtime$rendere, 2),
        bubbleWidth = _this$runtime$rendere2[0],
        bubbleHeight = _this$runtime$rendere2[1];
      var targetBounds;
      try {
        targetBounds = target.getBoundsForBubble();
      } catch (error_) {
        // Bounds calculation could fail (e.g. on empty costumes), in that case
        // use the x/y position of the target.
        targetBounds = {
          left: target.x,
          right: target.x,
          top: target.y,
          bottom: target.y
        };
      }
      var stageSize = this.runtime.renderer.getNativeSize();
      var stageBounds = {
        left: -stageSize[0] / 2,
        right: stageSize[0] / 2,
        top: stageSize[1] / 2,
        bottom: -stageSize[1] / 2
      };
      if (bubbleState.onSpriteRight && bubbleWidth + targetBounds.right > stageBounds.right && targetBounds.left - bubbleWidth > stageBounds.left) {
        // Only flip if it would fit
        bubbleState.onSpriteRight = false;
        this._renderBubble(target);
      } else if (!bubbleState.onSpriteRight && targetBounds.left - bubbleWidth < stageBounds.left && bubbleWidth + targetBounds.right < stageBounds.right) {
        // Only flip if it would fit
        bubbleState.onSpriteRight = true;
        this._renderBubble(target);
      } else {
        this.runtime.renderer.updateDrawablePosition(bubbleState.drawableId, [bubbleState.onSpriteRight ? Math.max(stageBounds.left,
        // Bubble should not extend past left edge of stage
        Math.min(stageBounds.right - bubbleWidth, targetBounds.right)) : Math.min(stageBounds.right - bubbleWidth,
        // Bubble should not extend past right edge of stage
        Math.max(stageBounds.left, targetBounds.left - bubbleWidth)),
        // Bubble should not extend past the top of the stage
        Math.min(stageBounds.top, targetBounds.bottom + bubbleHeight)]);
        this.runtime.requestRedraw();
      }
    }

    /**
     * Create a visible bubble for a target. If a bubble exists for the target,
     * just set it to visible and update the type/text. Otherwise create a new
     * bubble and update the relevant custom state.
     * @param {!Target} target Target who needs a bubble.
     * @return {undefined} Early return if text is empty string.
     * @private
     */
  }, {
    key: "_renderBubble",
    value: function _renderBubble(target) {
      // used by compiler
      if (!this.runtime.renderer) return;
      var bubbleState = this._getBubbleState(target);
      var type = bubbleState.type,
        text = bubbleState.text,
        onSpriteRight = bubbleState.onSpriteRight;

      // Remove the bubble if target is not visible, or text is being set to blank.
      if (!target.visible || text === '') {
        this._onTargetWillExit(target);
        return;
      }
      if (bubbleState.skinId) {
        this.runtime.renderer.updateTextSkin(bubbleState.skinId, type, text, onSpriteRight, [0, 0]);
      } else {
        target.onTargetVisualChange = this._onTargetChanged;
        bubbleState.drawableId = this.runtime.renderer.createDrawable(StageLayering.SPRITE_LAYER);
        bubbleState.skinId = this.runtime.renderer.createTextSkin(type, text, bubbleState.onSpriteRight, [0, 0]);
        this.runtime.renderer.updateDrawableSkinId(bubbleState.drawableId, bubbleState.skinId);
      }
      this._positionBubble(target);
    }

    /**
     * Properly format text for a text bubble.
     * @param {string} text The text to be formatted
     * @return {string} The formatted text
     * @private
     */
  }, {
    key: "_formatBubbleText",
    value: function _formatBubbleText(text) {
      if (text === '') return text;

      // Non-integers should be rounded to 2 decimal places (no more, no less), unless they're small enough that
      // rounding would display them as 0.00. This matches 2.0's behavior:
      // https://github.com/scratchfoundation/scratch-flash/blob/2e4a402ceb205a042887f54b26eebe1c2e6da6c0/src/scratch/ScratchSprite.as#L579-L585
      if (typeof text === 'number' && Math.abs(text) >= 0.01 && text % 1 !== 0) {
        text = text.toFixed(2);
      }

      // Limit the length of the string.
      text = String(text).substr(0, Scratch3LooksBlocks.SAY_BUBBLE_LIMIT);
      return text;
    }

    /**
     * The entry point for say/think blocks. Clears existing bubble if the text is empty.
     * Set the bubble custom state and then call _renderBubble.
     * @param {!Target} target Target that say/think blocks are being called on.
     * @param {!string} type Either "say" or "think"
     * @param {!string} text The text for the bubble, empty string clears the bubble.
     * @private
     */
  }, {
    key: "_updateBubble",
    value: function _updateBubble(target, type, text) {
      var bubbleState = this._getBubbleState(target);
      bubbleState.type = type;
      bubbleState.text = this._formatBubbleText(text);
      bubbleState.usageId = uid();
      this._renderBubble(target);
    }

    /**
     * Retrieve the block primitives implemented by this package.
     * @return {object.<string, Function>} Mapping of opcode to Function.
     */
  }, {
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        looks_say: this.say,
        looks_sayforsecs: this.sayforsecs,
        looks_think: this.think,
        looks_thinkforsecs: this.thinkforsecs,
        looks_show: this.show,
        looks_hide: this.hide,
        looks_hideallsprites: function looks_hideallsprites() {},
        // legacy no-op block
        looks_switchcostumeto: this.switchCostume,
        looks_switchbackdropto: this.switchBackdrop,
        looks_switchbackdroptoandwait: this.switchBackdropAndWait,
        looks_nextcostume: this.nextCostume,
        looks_nextbackdrop: this.nextBackdrop,
        looks_changeeffectby: this.changeEffect,
        looks_seteffectto: this.setEffect,
        looks_cleargraphiceffects: this.clearEffects,
        looks_changesizeby: this.changeSize,
        looks_setsizeto: this.setSize,
        looks_changestretchby: function looks_changestretchby() {},
        // legacy no-op blocks
        looks_setstretchto: function looks_setstretchto() {},
        looks_gotofrontback: this.goToFrontBack,
        looks_goforwardbackwardlayers: this.goForwardBackwardLayers,
        looks_size: this.getSize,
        looks_costumenumbername: this.getCostumeNumberName,
        looks_backdropnumbername: this.getBackdropNumberName
      };
    }
  }, {
    key: "getMonitored",
    value: function getMonitored() {
      return {
        looks_size: {
          isSpriteSpecific: true,
          getId: function getId(targetId) {
            return "".concat(targetId, "_size");
          }
        },
        looks_costumenumbername: {
          isSpriteSpecific: true,
          getId: function getId(targetId, fields) {
            return getMonitorIdForBlockWithArgs("".concat(targetId, "_costumenumbername"), fields);
          }
        },
        looks_backdropnumbername: {
          getId: function getId(_, fields) {
            return getMonitorIdForBlockWithArgs('backdropnumbername', fields);
          }
        }
      };
    }
  }, {
    key: "say",
    value: function say(args, util) {
      // @TODO in 2.0 calling say/think resets the right/left bias of the bubble
      var message = args.MESSAGE;
      this._say(message, util.target);
    }
  }, {
    key: "_say",
    value: function _say(message, target) {
      // used by compiler
      this.runtime.emit(Scratch3LooksBlocks.SAY_OR_THINK, target, 'say', message);
    }
  }, {
    key: "sayforsecs",
    value: function sayforsecs(args, util) {
      var _this = this;
      this.say(args, util);
      var target = util.target;
      var usageId = this._getBubbleState(target).usageId;
      return new Promise(function (resolve) {
        _this._bubbleTimeout = setTimeout(function () {
          _this._bubbleTimeout = null;
          // Clear say bubble if it hasn't been changed and proceed.
          if (_this._getBubbleState(target).usageId === usageId) {
            _this._updateBubble(target, 'say', '');
          }
          resolve();
        }, 1000 * args.SECS);
      });
    }
  }, {
    key: "think",
    value: function think(args, util) {
      this._think(args.MESSAGE, util.target);
    }
  }, {
    key: "_think",
    value: function _think(message, target) {
      // used by compiler
      this.runtime.emit(Scratch3LooksBlocks.SAY_OR_THINK, target, 'think', message);
    }
  }, {
    key: "thinkforsecs",
    value: function thinkforsecs(args, util) {
      var _this2 = this;
      this.think(args, util);
      var target = util.target;
      var usageId = this._getBubbleState(target).usageId;
      return new Promise(function (resolve) {
        _this2._bubbleTimeout = setTimeout(function () {
          _this2._bubbleTimeout = null;
          // Clear think bubble if it hasn't been changed and proceed.
          if (_this2._getBubbleState(target).usageId === usageId) {
            _this2._updateBubble(target, 'think', '');
          }
          resolve();
        }, 1000 * args.SECS);
      });
    }
  }, {
    key: "show",
    value: function show(args, util) {
      util.target.setVisible(true);
      this._renderBubble(util.target);
    }
  }, {
    key: "hide",
    value: function hide(args, util) {
      util.target.setVisible(false);
      this._renderBubble(util.target);
    }

    /**
     * Utility function to set the costume of a target.
     * Matches the behavior of Scratch 2.0 for different types of arguments.
     * @param {!Target} target Target to set costume to.
     * @param {Any} requestedCostume Costume requested, e.g., 0, 'name', etc.
     * @param {boolean=} optZeroIndex Set to zero-index the requestedCostume.
     * @return {Array.<!Thread>} Any threads started by this switch.
     */
  }, {
    key: "_setCostume",
    value: function _setCostume(target, requestedCostume, optZeroIndex) {
      // used by compiler
      if (typeof requestedCostume === 'number') {
        // Numbers should be treated as costume indices, always
        target.setCostume(optZeroIndex ? requestedCostume : requestedCostume - 1);
      } else {
        // Strings should be treated as costume names, where possible
        var costumeIndex = target.getCostumeIndexByName(requestedCostume.toString());
        if (costumeIndex !== -1) {
          target.setCostume(costumeIndex);
        } else if (requestedCostume === 'next costume') {
          target.setCostume(target.currentCostume + 1);
        } else if (requestedCostume === 'previous costume') {
          target.setCostume(target.currentCostume - 1);
          // Try to cast the string to a number (and treat it as a costume index)
          // Pure whitespace should not be treated as a number
          // Note: isNaN will cast the string to a number before checking if it's NaN
        } else if (!(isNaN(requestedCostume) || Cast.isWhiteSpace(requestedCostume))) {
          target.setCostume(optZeroIndex ? Number(requestedCostume) : Number(requestedCostume) - 1);
        }
      }

      // Per 2.0, 'switch costume' can't start threads even in the Stage.
      return [];
    }

    /**
     * Utility function to set the backdrop of a target.
     * Matches the behavior of Scratch 2.0 for different types of arguments.
     * @param {!Target} stage Target to set backdrop to.
     * @param {Any} requestedBackdrop Backdrop requested, e.g., 0, 'name', etc.
     * @param {boolean=} optZeroIndex Set to zero-index the requestedBackdrop.
     * @return {Array.<!Thread>} Any threads started by this switch.
     */
  }, {
    key: "_setBackdrop",
    value: function _setBackdrop(stage, requestedBackdrop, optZeroIndex) {
      // used by compiler
      if (typeof requestedBackdrop === 'number') {
        // Numbers should be treated as backdrop indices, always
        stage.setCostume(optZeroIndex ? requestedBackdrop : requestedBackdrop - 1);
      } else {
        // Strings should be treated as backdrop names where possible
        var costumeIndex = stage.getCostumeIndexByName(requestedBackdrop.toString());
        if (costumeIndex !== -1) {
          stage.setCostume(costumeIndex);
        } else if (requestedBackdrop === 'next backdrop') {
          stage.setCostume(stage.currentCostume + 1);
        } else if (requestedBackdrop === 'previous backdrop') {
          stage.setCostume(stage.currentCostume - 1);
        } else if (requestedBackdrop === 'random backdrop') {
          var numCostumes = stage.getCostumes().length;
          if (numCostumes > 1) {
            // Don't pick the current backdrop, so that the block
            // will always have an observable effect.
            var lowerBound = 0;
            var upperBound = numCostumes - 1;
            var costumeToExclude = stage.currentCostume;
            var nextCostume = MathUtil.inclusiveRandIntWithout(lowerBound, upperBound, costumeToExclude);
            stage.setCostume(nextCostume);
          }
          // Try to cast the string to a number (and treat it as a costume index)
          // Pure whitespace should not be treated as a number
          // Note: isNaN will cast the string to a number before checking if it's NaN
        } else if (!(isNaN(requestedBackdrop) || Cast.isWhiteSpace(requestedBackdrop))) {
          stage.setCostume(optZeroIndex ? Number(requestedBackdrop) : Number(requestedBackdrop) - 1);
        }
      }
      var newName = stage.getCostumes()[stage.currentCostume].name;
      return this.runtime.startHats('event_whenbackdropswitchesto', {
        BACKDROP: newName
      });
    }
  }, {
    key: "switchCostume",
    value: function switchCostume(args, util) {
      this._setCostume(util.target, args.COSTUME); // used by compiler
    }
  }, {
    key: "nextCostume",
    value: function nextCostume(args, util) {
      this._setCostume(util.target, util.target.currentCostume + 1, true);
    }
  }, {
    key: "switchBackdrop",
    value: function switchBackdrop(args) {
      this._setBackdrop(this.runtime.getTargetForStage(), args.BACKDROP);
    }
  }, {
    key: "switchBackdropAndWait",
    value: function switchBackdropAndWait(args, util) {
      // Have we run before, starting threads?
      if (!util.stackFrame.startedThreads) {
        // No - switch the backdrop.
        util.stackFrame.startedThreads = this._setBackdrop(this.runtime.getTargetForStage(), args.BACKDROP);
        if (util.stackFrame.startedThreads.length === 0) {
          // Nothing was started.
          return;
        }
      }
      // We've run before; check if the wait is still going on.
      var instance = this;
      // Scratch 2 considers threads to be waiting if they are still in
      // runtime.threads. Threads that have run all their blocks, or are
      // marked done but still in runtime.threads are still considered to
      // be waiting.
      var waiting = util.stackFrame.startedThreads.some(function (thread) {
        return instance.runtime.threads.indexOf(thread) !== -1;
      });
      if (waiting) {
        // If all threads are waiting for the next tick or later yield
        // for a tick as well. Otherwise yield until the next loop of
        // the threads.
        if (util.stackFrame.startedThreads.every(function (thread) {
          return instance.runtime.isWaitingThread(thread);
        })) {
          util.yieldTick();
        } else {
          util["yield"]();
        }
      }
    }
  }, {
    key: "nextBackdrop",
    value: function nextBackdrop() {
      var stage = this.runtime.getTargetForStage();
      this._setBackdrop(stage, stage.currentCostume + 1, true);
    }
  }, {
    key: "clampEffect",
    value: function clampEffect(effect, value) {
      // used by compiler
      var clampedValue = value;
      switch (effect) {
        case 'ghost':
          clampedValue = MathUtil.clamp(value, Scratch3LooksBlocks.EFFECT_GHOST_LIMIT.min, Scratch3LooksBlocks.EFFECT_GHOST_LIMIT.max);
          break;
        case 'brightness':
          clampedValue = MathUtil.clamp(value, Scratch3LooksBlocks.EFFECT_BRIGHTNESS_LIMIT.min, Scratch3LooksBlocks.EFFECT_BRIGHTNESS_LIMIT.max);
          break;
      }
      return clampedValue;
    }
  }, {
    key: "changeEffect",
    value: function changeEffect(args, util) {
      var effect = Cast.toString(args.EFFECT).toLowerCase();
      var change = Cast.toNumber(args.CHANGE);
      if (!Object.prototype.hasOwnProperty.call(util.target.effects, effect)) return;
      var newValue = change + util.target.effects[effect];
      newValue = this.clampEffect(effect, newValue);
      util.target.setEffect(effect, newValue);
    }
  }, {
    key: "setEffect",
    value: function setEffect(args, util) {
      var effect = Cast.toString(args.EFFECT).toLowerCase();
      var value = Cast.toNumber(args.VALUE);
      value = this.clampEffect(effect, value);
      util.target.setEffect(effect, value);
    }
  }, {
    key: "clearEffects",
    value: function clearEffects(args, util) {
      util.target.clearEffects();
    }
  }, {
    key: "changeSize",
    value: function changeSize(args, util) {
      var change = Cast.toNumber(args.CHANGE);
      util.target.setSize(util.target.size + change);
    }
  }, {
    key: "setSize",
    value: function setSize(args, util) {
      var size = Cast.toNumber(args.SIZE);
      util.target.setSize(size);
    }
  }, {
    key: "goToFrontBack",
    value: function goToFrontBack(args, util) {
      if (!util.target.isStage) {
        if (args.FRONT_BACK === 'front') {
          util.target.goToFront();
        } else {
          util.target.goToBack();
        }
      }
    }
  }, {
    key: "goForwardBackwardLayers",
    value: function goForwardBackwardLayers(args, util) {
      if (!util.target.isStage) {
        if (args.FORWARD_BACKWARD === 'forward') {
          util.target.goForwardLayers(Cast.toNumber(args.NUM));
        } else {
          util.target.goBackwardLayers(Cast.toNumber(args.NUM));
        }
      }
    }
  }, {
    key: "getSize",
    value: function getSize(args, util) {
      return Math.round(util.target.size);
    }
  }, {
    key: "getBackdropNumberName",
    value: function getBackdropNumberName(args) {
      var stage = this.runtime.getTargetForStage();
      if (args.NUMBER_NAME === 'number') {
        return stage.currentCostume + 1;
      }
      // Else return name
      return stage.getCostumes()[stage.currentCostume].name;
    }
  }, {
    key: "getCostumeNumberName",
    value: function getCostumeNumberName(args, util) {
      if (args.NUMBER_NAME === 'number') {
        return util.target.currentCostume + 1;
      }
      // Else return name
      return util.target.getCostumes()[util.target.currentCostume].name;
    }
  }], [{
    key: "DEFAULT_BUBBLE_STATE",
    get: function get() {
      return {
        drawableId: null,
        onSpriteRight: true,
        skinId: null,
        text: '',
        type: 'say',
        usageId: null
      };
    }

    /**
     * The key to load & store a target's bubble-related state.
     * @type {string}
     */
  }, {
    key: "STATE_KEY",
    get: function get() {
      return 'Scratch.looks';
    }

    /**
     * Event name for a text bubble being created or updated.
     * @const {string}
     */
  }, {
    key: "SAY_OR_THINK",
    get: function get() {
      // There are currently many places in the codebase which explicitly refer to this event by the string 'SAY',
      // so keep this as the string 'SAY' for now rather than changing it to 'SAY_OR_THINK' and breaking things.
      return 'SAY';
    }

    /**
     * Limit for say bubble string.
     * @const {string}
     */
  }, {
    key: "SAY_BUBBLE_LIMIT",
    get: function get() {
      return 330;
    }

    /**
     * Limit for ghost effect
     * @const {object}
     */
  }, {
    key: "EFFECT_GHOST_LIMIT",
    get: function get() {
      return {
        min: 0,
        max: 100
      };
    }

    /**
     * Limit for brightness effect
     * @const {object}
     */
  }, {
    key: "EFFECT_BRIGHTNESS_LIMIT",
    get: function get() {
      return {
        min: -100,
        max: 100
      };
    }
  }]);
}();
module.exports = Scratch3LooksBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_motion.js":
/*!*******************************************!*\
  !*** ../vm/src/blocks/scratch3_motion.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");
var Timer = __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js");
var Scratch3MotionBlocks = /*#__PURE__*/function () {
  function Scratch3MotionBlocks(runtime) {
    _classCallCheck(this, Scratch3MotionBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3MotionBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        motion_movesteps: this.moveSteps,
        motion_gotoxy: this.goToXY,
        motion_goto: this.goTo,
        motion_turnright: this.turnRight,
        motion_turnleft: this.turnLeft,
        motion_pointindirection: this.pointInDirection,
        motion_pointtowards: this.pointTowards,
        motion_glidesecstoxy: this.glide,
        motion_glideto: this.glideTo,
        motion_ifonedgebounce: this.ifOnEdgeBounce,
        motion_setrotationstyle: this.setRotationStyle,
        motion_changexby: this.changeX,
        motion_setx: this.setX,
        motion_changeyby: this.changeY,
        motion_sety: this.setY,
        motion_xposition: this.getX,
        motion_yposition: this.getY,
        motion_direction: this.getDirection,
        // Legacy no-op blocks:
        motion_scroll_right: function motion_scroll_right() {},
        motion_scroll_up: function motion_scroll_up() {},
        motion_align_scene: function motion_align_scene() {},
        motion_xscroll: function motion_xscroll() {},
        motion_yscroll: function motion_yscroll() {}
      };
    }
  }, {
    key: "getMonitored",
    value: function getMonitored() {
      return {
        motion_xposition: {
          isSpriteSpecific: true,
          getId: function getId(targetId) {
            return "".concat(targetId, "_xposition");
          }
        },
        motion_yposition: {
          isSpriteSpecific: true,
          getId: function getId(targetId) {
            return "".concat(targetId, "_yposition");
          }
        },
        motion_direction: {
          isSpriteSpecific: true,
          getId: function getId(targetId) {
            return "".concat(targetId, "_direction");
          }
        }
      };
    }
  }, {
    key: "moveSteps",
    value: function moveSteps(args, util) {
      var steps = Cast.toNumber(args.STEPS);
      this._moveSteps(steps, util.target);
    }
  }, {
    key: "_moveSteps",
    value: function _moveSteps(steps, target) {
      // used by compiler
      var radians = MathUtil.degToRad(90 - target.direction);
      var dx = steps * Math.cos(radians);
      var dy = steps * Math.sin(radians);
      target.setXY(target.x + dx, target.y + dy);
    }
  }, {
    key: "goToXY",
    value: function goToXY(args, util) {
      var x = Cast.toNumber(args.X);
      var y = Cast.toNumber(args.Y);
      util.target.setXY(x, y);
    }
  }, {
    key: "getTargetXY",
    value: function getTargetXY(targetName, util) {
      var targetX = 0;
      var targetY = 0;
      if (targetName === '_mouse_') {
        targetX = util.ioQuery('mouse', 'getScratchX');
        targetY = util.ioQuery('mouse', 'getScratchY');
      } else if (targetName === '_random_') {
        var stageWidth = this.runtime.stageWidth;
        var stageHeight = this.runtime.stageHeight;
        targetX = Math.round(stageWidth * (Math.random() - 0.5));
        targetY = Math.round(stageHeight * (Math.random() - 0.5));
      } else {
        targetName = Cast.toString(targetName);
        var goToTarget = this.runtime.getSpriteTargetByName(targetName);
        if (!goToTarget) return;
        targetX = goToTarget.x;
        targetY = goToTarget.y;
      }
      return [targetX, targetY];
    }
  }, {
    key: "goTo",
    value: function goTo(args, util) {
      var targetXY = this.getTargetXY(args.TO, util);
      if (targetXY) {
        util.target.setXY(targetXY[0], targetXY[1]);
      }
    }
  }, {
    key: "turnRight",
    value: function turnRight(args, util) {
      var degrees = Cast.toNumber(args.DEGREES);
      util.target.setDirection(util.target.direction + degrees);
    }
  }, {
    key: "turnLeft",
    value: function turnLeft(args, util) {
      var degrees = Cast.toNumber(args.DEGREES);
      util.target.setDirection(util.target.direction - degrees);
    }
  }, {
    key: "pointInDirection",
    value: function pointInDirection(args, util) {
      var direction = Cast.toNumber(args.DIRECTION);
      util.target.setDirection(direction);
    }
  }, {
    key: "pointTowards",
    value: function pointTowards(args, util) {
      var targetX = 0;
      var targetY = 0;
      if (args.TOWARDS === '_mouse_') {
        targetX = util.ioQuery('mouse', 'getScratchX');
        targetY = util.ioQuery('mouse', 'getScratchY');
      } else if (args.TOWARDS === '_random_') {
        util.target.setDirection(Math.round(Math.random() * 360) - 180);
        return;
      } else {
        args.TOWARDS = Cast.toString(args.TOWARDS);
        var pointTarget = this.runtime.getSpriteTargetByName(args.TOWARDS);
        if (!pointTarget) return;
        targetX = pointTarget.x;
        targetY = pointTarget.y;
      }
      var dx = targetX - util.target.x;
      var dy = targetY - util.target.y;
      var direction = 90 - MathUtil.radToDeg(Math.atan2(dy, dx));
      util.target.setDirection(direction);
    }
  }, {
    key: "glide",
    value: function glide(args, util) {
      if (util.stackFrame.timer) {
        var timeElapsed = util.stackFrame.timer.timeElapsed();
        if (timeElapsed < util.stackFrame.duration * 1000) {
          // In progress: move to intermediate position.
          var frac = timeElapsed / (util.stackFrame.duration * 1000);
          var dx = frac * (util.stackFrame.endX - util.stackFrame.startX);
          var dy = frac * (util.stackFrame.endY - util.stackFrame.startY);
          util.target.setXY(util.stackFrame.startX + dx, util.stackFrame.startY + dy);
          util["yield"]();
        } else {
          // Finished: move to final position.
          util.target.setXY(util.stackFrame.endX, util.stackFrame.endY);
        }
      } else {
        // First time: save data for future use.
        util.stackFrame.timer = new Timer();
        util.stackFrame.timer.start();
        util.stackFrame.duration = Cast.toNumber(args.SECS);
        util.stackFrame.startX = util.target.x;
        util.stackFrame.startY = util.target.y;
        util.stackFrame.endX = Cast.toNumber(args.X);
        util.stackFrame.endY = Cast.toNumber(args.Y);
        if (util.stackFrame.duration <= 0) {
          // Duration too short to glide.
          util.target.setXY(util.stackFrame.endX, util.stackFrame.endY);
          return;
        }
        util["yield"]();
      }
    }
  }, {
    key: "glideTo",
    value: function glideTo(args, util) {
      var targetXY = this.getTargetXY(args.TO, util);
      if (targetXY) {
        this.glide({
          SECS: args.SECS,
          X: targetXY[0],
          Y: targetXY[1]
        }, util);
      }
    }
  }, {
    key: "ifOnEdgeBounce",
    value: function ifOnEdgeBounce(args, util) {
      this._ifOnEdgeBounce(util.target);
    }
  }, {
    key: "_ifOnEdgeBounce",
    value: function _ifOnEdgeBounce(target) {
      // used by compiler
      var bounds = target.getBounds();
      if (!bounds) {
        return;
      }
      // Measure distance to edges.
      // Values are positive when the sprite is far away,
      // and clamped to zero when the sprite is beyond.
      var stageWidth = this.runtime.stageWidth;
      var stageHeight = this.runtime.stageHeight;
      var distLeft = Math.max(0, stageWidth / 2 + bounds.left);
      var distTop = Math.max(0, stageHeight / 2 - bounds.top);
      var distRight = Math.max(0, stageWidth / 2 - bounds.right);
      var distBottom = Math.max(0, stageHeight / 2 + bounds.bottom);
      // Find the nearest edge.
      var nearestEdge = '';
      var minDist = Infinity;
      if (distLeft < minDist) {
        minDist = distLeft;
        nearestEdge = 'left';
      }
      if (distTop < minDist) {
        minDist = distTop;
        nearestEdge = 'top';
      }
      if (distRight < minDist) {
        minDist = distRight;
        nearestEdge = 'right';
      }
      if (distBottom < minDist) {
        minDist = distBottom;
        nearestEdge = 'bottom';
      }
      if (minDist > 0) {
        return; // Not touching any edge.
      }
      // Point away from the nearest edge.
      var radians = MathUtil.degToRad(90 - target.direction);
      var dx = Math.cos(radians);
      var dy = -Math.sin(radians);
      if (nearestEdge === 'left') {
        dx = Math.max(0.2, Math.abs(dx));
      } else if (nearestEdge === 'top') {
        dy = Math.max(0.2, Math.abs(dy));
      } else if (nearestEdge === 'right') {
        dx = 0 - Math.max(0.2, Math.abs(dx));
      } else if (nearestEdge === 'bottom') {
        dy = 0 - Math.max(0.2, Math.abs(dy));
      }
      var newDirection = MathUtil.radToDeg(Math.atan2(dy, dx)) + 90;
      target.setDirection(newDirection);
      // Keep within the stage.
      var fencedPosition = target.keepInFence(target.x, target.y);
      target.setXY(fencedPosition[0], fencedPosition[1]);
    }
  }, {
    key: "setRotationStyle",
    value: function setRotationStyle(args, util) {
      util.target.setRotationStyle(args.STYLE);
    }
  }, {
    key: "changeX",
    value: function changeX(args, util) {
      var dx = Cast.toNumber(args.DX);
      util.target.setXY(util.target.x + dx, util.target.y);
    }
  }, {
    key: "setX",
    value: function setX(args, util) {
      var x = Cast.toNumber(args.X);
      util.target.setXY(x, util.target.y);
    }
  }, {
    key: "changeY",
    value: function changeY(args, util) {
      var dy = Cast.toNumber(args.DY);
      util.target.setXY(util.target.x, util.target.y + dy);
    }
  }, {
    key: "setY",
    value: function setY(args, util) {
      var y = Cast.toNumber(args.Y);
      util.target.setXY(util.target.x, y);
    }
  }, {
    key: "getX",
    value: function getX(args, util) {
      return this.limitPrecision(util.target.x);
    }
  }, {
    key: "getY",
    value: function getY(args, util) {
      return this.limitPrecision(util.target.y);
    }
  }, {
    key: "getDirection",
    value: function getDirection(args, util) {
      return util.target.direction;
    }

    // This corresponds to snapToInteger in Scratch 2
  }, {
    key: "limitPrecision",
    value: function limitPrecision(coordinate) {
      var rounded = Math.round(coordinate);
      var delta = coordinate - rounded;
      var limitedCoord = Math.abs(delta) < 1e-9 ? rounded : coordinate;
      return limitedCoord;
    }
  }]);
}();
module.exports = Scratch3MotionBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_operators.js":
/*!**********************************************!*\
  !*** ../vm/src/blocks/scratch3_operators.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast.js */ "../vm/src/util/cast.js");
var MathUtil = __webpack_require__(/*! ../util/math-util.js */ "../vm/src/util/math-util.js");
var Scratch3OperatorsBlocks = /*#__PURE__*/function () {
  function Scratch3OperatorsBlocks(runtime) {
    _classCallCheck(this, Scratch3OperatorsBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3OperatorsBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        operator_add: this.add,
        operator_subtract: this.subtract,
        operator_multiply: this.multiply,
        operator_divide: this.divide,
        operator_lt: this.lt,
        operator_equals: this.equals,
        operator_gt: this.gt,
        operator_and: this.and,
        operator_or: this.or,
        operator_not: this.not,
        operator_random: this.random,
        operator_join: this.join,
        operator_letter_of: this.letterOf,
        operator_length: this.length,
        operator_contains: this.contains,
        operator_mod: this.mod,
        operator_round: this.round,
        operator_mathop: this.mathop
      };
    }
  }, {
    key: "add",
    value: function add(args) {
      return Cast.toNumber(args.NUM1) + Cast.toNumber(args.NUM2);
    }
  }, {
    key: "subtract",
    value: function subtract(args) {
      return Cast.toNumber(args.NUM1) - Cast.toNumber(args.NUM2);
    }
  }, {
    key: "multiply",
    value: function multiply(args) {
      return Cast.toNumber(args.NUM1) * Cast.toNumber(args.NUM2);
    }
  }, {
    key: "divide",
    value: function divide(args) {
      return Cast.toNumber(args.NUM1) / Cast.toNumber(args.NUM2);
    }
  }, {
    key: "lt",
    value: function lt(args) {
      return Cast.compare(args.OPERAND1, args.OPERAND2) < 0;
    }
  }, {
    key: "equals",
    value: function equals(args) {
      return Cast.compare(args.OPERAND1, args.OPERAND2) === 0;
    }
  }, {
    key: "gt",
    value: function gt(args) {
      return Cast.compare(args.OPERAND1, args.OPERAND2) > 0;
    }
  }, {
    key: "and",
    value: function and(args) {
      return Cast.toBoolean(args.OPERAND1) && Cast.toBoolean(args.OPERAND2);
    }
  }, {
    key: "or",
    value: function or(args) {
      return Cast.toBoolean(args.OPERAND1) || Cast.toBoolean(args.OPERAND2);
    }
  }, {
    key: "not",
    value: function not(args) {
      return !Cast.toBoolean(args.OPERAND);
    }
  }, {
    key: "random",
    value: function random(args) {
      return this._random(args.FROM, args.TO);
    }
  }, {
    key: "_random",
    value: function _random(from, to) {
      // used by compiler
      var nFrom = Cast.toNumber(from);
      var nTo = Cast.toNumber(to);
      var low = nFrom <= nTo ? nFrom : nTo;
      var high = nFrom <= nTo ? nTo : nFrom;
      if (low === high) return low;
      // If both arguments are ints, truncate the result to an int.
      if (Cast.isInt(from) && Cast.isInt(to)) {
        return low + Math.floor(Math.random() * (high + 1 - low));
      }
      return Math.random() * (high - low) + low;
    }
  }, {
    key: "join",
    value: function join(args) {
      return Cast.toString(args.STRING1) + Cast.toString(args.STRING2);
    }
  }, {
    key: "letterOf",
    value: function letterOf(args) {
      var index = Cast.toNumber(args.LETTER) - 1;
      var str = Cast.toString(args.STRING);
      // Out of bounds?
      if (index < 0 || index >= str.length) {
        return '';
      }
      return str.charAt(index);
    }
  }, {
    key: "length",
    value: function length(args) {
      return Cast.toString(args.STRING).length;
    }
  }, {
    key: "contains",
    value: function contains(args) {
      var format = function format(string) {
        return Cast.toString(string).toLowerCase();
      };
      return format(args.STRING1).includes(format(args.STRING2));
    }
  }, {
    key: "mod",
    value: function mod(args) {
      var n = Cast.toNumber(args.NUM1);
      var modulus = Cast.toNumber(args.NUM2);
      var result = n % modulus;
      // Scratch mod uses floored division instead of truncated division.
      if (result / modulus < 0) result += modulus;
      return result;
    }
  }, {
    key: "round",
    value: function round(args) {
      return Math.round(Cast.toNumber(args.NUM));
    }
  }, {
    key: "mathop",
    value: function mathop(args) {
      var operator = Cast.toString(args.OPERATOR).toLowerCase();
      var n = Cast.toNumber(args.NUM);
      switch (operator) {
        case 'abs':
          return Math.abs(n);
        case 'floor':
          return Math.floor(n);
        case 'ceiling':
          return Math.ceil(n);
        case 'sqrt':
          return Math.sqrt(n);
        case 'sin':
          return Math.round(Math.sin(Math.PI * n / 180) * 1e10) / 1e10;
        case 'cos':
          return Math.round(Math.cos(Math.PI * n / 180) * 1e10) / 1e10;
        case 'tan':
          return MathUtil.tan(n);
        case 'asin':
          return Math.asin(n) * 180 / Math.PI;
        case 'acos':
          return Math.acos(n) * 180 / Math.PI;
        case 'atan':
          return Math.atan(n) * 180 / Math.PI;
        case 'ln':
          return Math.log(n);
        case 'log':
          return Math.log(n) / Math.LN10;
        case 'e ^':
          return Math.exp(n);
        case '10 ^':
          return Math.pow(10, n);
      }
      return 0;
    }
  }]);
}();
module.exports = Scratch3OperatorsBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_procedures.js":
/*!***********************************************!*\
  !*** ../vm/src/blocks/scratch3_procedures.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Scratch3ProcedureBlocks = /*#__PURE__*/function () {
  function Scratch3ProcedureBlocks(runtime) {
    _classCallCheck(this, Scratch3ProcedureBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3ProcedureBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        procedures_definition: this.definition,
        procedures_call: this.call,
        procedures_return: this["return"],
        argument_reporter_string_number: this.argumentReporterStringNumber,
        argument_reporter_boolean: this.argumentReporterBoolean
      };
    }
  }, {
    key: "definition",
    value: function definition() {
      // No-op: execute the blocks.
    }
  }, {
    key: "call",
    value: function call(args, util) {
      var stackFrame = util.stackFrame;
      var isReporter = !!args.mutation["return"];
      if (stackFrame.executed) {
        if (isReporter) {
          var returnValue = stackFrame.returnValue;
          // This stackframe will be reused for other reporters in this block, so clean it up for them.
          // Can't use reset() because that will reset too much.
          var threadStackFrame = util.thread.peekStackFrame();
          threadStackFrame.params = null;
          delete stackFrame.returnValue;
          delete stackFrame.executed;
          return returnValue;
        }
        return;
      }
      var procedureCode = args.mutation.proccode;
      var paramNamesIdsAndDefaults = util.getProcedureParamNamesIdsAndDefaults(procedureCode);

      // If null, procedure could not be found, which can happen if custom
      // block is dragged between sprites without the definition.
      // Match Scratch 2.0 behavior and noop.
      if (paramNamesIdsAndDefaults === null) {
        if (isReporter) {
          return '';
        }
        return;
      }
      var _paramNamesIdsAndDefa = _slicedToArray(paramNamesIdsAndDefaults, 3),
        paramNames = _paramNamesIdsAndDefa[0],
        paramIds = _paramNamesIdsAndDefa[1],
        paramDefaults = _paramNamesIdsAndDefa[2];

      // Initialize params for the current stackFrame to {}, even if the procedure does
      // not take any arguments. This is so that `getParam` down the line does not look
      // at earlier stack frames for the values of a given parameter (#1729)
      util.initParams();
      for (var i = 0; i < paramIds.length; i++) {
        if (Object.prototype.hasOwnProperty.call(args, paramIds[i])) {
          util.pushParam(paramNames[i], args[paramIds[i]]);
        } else {
          util.pushParam(paramNames[i], paramDefaults[i]);
        }
      }
      var addonBlock = util.runtime.getAddonBlock(procedureCode);
      if (addonBlock) {
        var result = addonBlock.callback(util.thread.getAllparams(), util);
        if (util.thread.status === 1 /* STATUS_PROMISE_WAIT */) {
          // If the addon block is using STATUS_PROMISE_WAIT to force us to sleep,
          // make sure to not re-run this block when we resume.
          stackFrame.executed = true;
        }
        return result;
      }
      stackFrame.executed = true;
      if (isReporter) {
        util.thread.peekStackFrame().waitingReporter = true;
        // Default return value
        stackFrame.returnValue = '';
      }
      util.startProcedure(procedureCode);
    }
  }, {
    key: "return",
    value: function _return(args, util) {
      util.stopThisScript();
      // If used outside of a custom block, there may be no stackframe.
      if (util.thread.peekStackFrame()) {
        util.stackFrame.returnValue = args.VALUE;
      }
    }
  }, {
    key: "argumentReporterStringNumber",
    value: function argumentReporterStringNumber(args, util) {
      var value = util.getParam(args.VALUE);
      if (value === null) {
        // tw: support legacy block
        if (String(args.VALUE).toLowerCase() === 'last key pressed') {
          return util.ioQuery('keyboard', 'getLastKeyPressed');
        }
        // When the parameter is not found in the most recent procedure
        // call, the default is always 0.
        return 0;
      }
      return value;
    }
  }, {
    key: "argumentReporterBoolean",
    value: function argumentReporterBoolean(args, util) {
      var value = util.getParam(args.VALUE);
      if (value === null) {
        // tw: implement is compiled? and is turbowarp?
        var lowercaseValue = String(args.VALUE).toLowerCase();
        if (util.target.runtime.compilerOptions.enabled && lowercaseValue === 'is compiled?') {
          return true;
        }
        if (lowercaseValue === 'is turbowarp?') {
          return true;
        }
        // When the parameter is not found in the most recent procedure
        // call, the default is always 0.
        return 0;
      }
      return value;
    }
  }]);
}();
module.exports = Scratch3ProcedureBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_sensing.js":
/*!********************************************!*\
  !*** ../vm/src/blocks/scratch3_sensing.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Timer = __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js");
var getMonitorIdForBlockWithArgs = __webpack_require__(/*! ../util/get-monitor-id */ "../vm/src/util/get-monitor-id.js");
var Scratch3SensingBlocks = /*#__PURE__*/function () {
  function Scratch3SensingBlocks(runtime) {
    _classCallCheck(this, Scratch3SensingBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * The "answer" block value.
     * @type {string}
     */
    this._answer = ''; // used by compiler

    /**
     * The timer utility.
     * @type {Timer}
     */
    this._timer = new Timer();

    /**
     * The stored microphone loudness measurement.
     * @type {number}
     */
    this._cachedLoudness = -1;

    /**
     * The time of the most recent microphone loudness measurement.
     * @type {number}
     */
    this._cachedLoudnessTimestamp = 0;

    /**
     * The list of queued questions and respective `resolve` callbacks.
     * @type {!Array}
     */
    this._questionList = [];
    this.runtime.on('ANSWER', this._onAnswer.bind(this));
    this.runtime.on('PROJECT_START', this._resetAnswer.bind(this));
    this.runtime.on('PROJECT_STOP_ALL', this._clearAllQuestions.bind(this));
    this.runtime.on('STOP_FOR_TARGET', this._clearTargetQuestions.bind(this));
    this.runtime.on('RUNTIME_DISPOSED', this._resetAnswer.bind(this));
  }

  /**
   * Retrieve the block primitives implemented by this package.
   * @return {object.<string, Function>} Mapping of opcode to Function.
   */
  return _createClass(Scratch3SensingBlocks, [{
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        sensing_touchingobject: this.touchingObject,
        sensing_touchingcolor: this.touchingColor,
        sensing_coloristouchingcolor: this.colorTouchingColor,
        sensing_distanceto: this.distanceTo,
        sensing_timer: this.getTimer,
        sensing_resettimer: this.resetTimer,
        sensing_of: this.getAttributeOf,
        sensing_mousex: this.getMouseX,
        sensing_mousey: this.getMouseY,
        sensing_setdragmode: this.setDragMode,
        sensing_mousedown: this.getMouseDown,
        sensing_keypressed: this.getKeyPressed,
        sensing_current: this.current,
        sensing_dayssince2000: this.daysSince2000,
        sensing_loudness: this.getLoudness,
        sensing_loud: this.isLoud,
        sensing_askandwait: this.askAndWait,
        sensing_answer: this.getAnswer,
        sensing_username: this.getUsername,
        sensing_userid: function sensing_userid() {},
        // legacy no-op block
        sensing_online: this.isOnline
      };
    }
  }, {
    key: "getMonitored",
    value: function getMonitored() {
      return {
        sensing_answer: {
          getId: function getId() {
            return 'answer';
          }
        },
        sensing_mousedown: {
          getId: function getId() {
            return 'mousedown';
          }
        },
        sensing_mousex: {
          getId: function getId() {
            return 'mousex';
          }
        },
        sensing_mousey: {
          getId: function getId() {
            return 'mousey';
          }
        },
        sensing_loudness: {
          getId: function getId() {
            return 'loudness';
          }
        },
        sensing_timer: {
          getId: function getId() {
            return 'timer';
          }
        },
        sensing_dayssince2000: {
          getId: function getId() {
            return 'dayssince2000';
          }
        },
        sensing_current: {
          // This is different from the default toolbox xml id in order to support
          // importing multiple monitors from the same opcode from sb2 files,
          // something that is not currently supported in scratch 3.
          getId: function getId(_, fields) {
            return getMonitorIdForBlockWithArgs('current', fields);
          } // _${param}`
        },
        sensing_online: {
          getId: function getId() {
            return 'online';
          }
        }
      };
    }
  }, {
    key: "_onAnswer",
    value: function _onAnswer(answer) {
      this._answer = answer;
      var questionObj = this._questionList.shift();
      if (questionObj) {
        var _questionObj = _slicedToArray(questionObj, 5),
          _question = _questionObj[0],
          resolve = _questionObj[1],
          target = _questionObj[2],
          wasVisible = _questionObj[3],
          wasStage = _questionObj[4];
        // If the target was visible when asked, hide the say bubble unless the target was the stage.
        if (wasVisible && !wasStage) {
          this.runtime.emit('SAY', target, 'say', '');
        }
        resolve();
        this._askNextQuestion();
      }
    }
  }, {
    key: "_resetAnswer",
    value: function _resetAnswer() {
      this._answer = '';
    }
  }, {
    key: "_enqueueAsk",
    value: function _enqueueAsk(question, resolve, target, wasVisible, wasStage) {
      this._questionList.push([question, resolve, target, wasVisible, wasStage]);
    }
  }, {
    key: "_askNextQuestion",
    value: function _askNextQuestion() {
      if (this._questionList.length > 0) {
        var _this$_questionList$ = _slicedToArray(this._questionList[0], 5),
          question = _this$_questionList$[0],
          _resolve = _this$_questionList$[1],
          target = _this$_questionList$[2],
          wasVisible = _this$_questionList$[3],
          wasStage = _this$_questionList$[4];
        // If the target is visible, emit a blank question and use the
        // say event to trigger a bubble unless the target was the stage.
        if (wasVisible && !wasStage) {
          this.runtime.emit('SAY', target, 'say', question);
          this.runtime.emit('QUESTION', '');
        } else {
          this.runtime.emit('QUESTION', question);
        }
      }
    }
  }, {
    key: "_clearAllQuestions",
    value: function _clearAllQuestions() {
      this._questionList = [];
      this.runtime.emit('QUESTION', null);
    }
  }, {
    key: "_clearTargetQuestions",
    value: function _clearTargetQuestions(stopTarget) {
      var currentlyAsking = this._questionList.length > 0 && this._questionList[0][2] === stopTarget;
      this._questionList = this._questionList.filter(function (question) {
        return question[2] !== stopTarget;
      });
      if (currentlyAsking) {
        this.runtime.emit('SAY', stopTarget, 'say', '');
        if (this._questionList.length > 0) {
          this._askNextQuestion();
        } else {
          this.runtime.emit('QUESTION', null);
        }
      }
    }
  }, {
    key: "askAndWait",
    value: function askAndWait(args, util) {
      var _this = this;
      var _target = util.target;
      return new Promise(function (resolve) {
        var isQuestionAsked = _this._questionList.length > 0;
        _this._enqueueAsk(String(args.QUESTION), resolve, _target, _target.visible, _target.isStage);
        if (!isQuestionAsked) {
          _this._askNextQuestion();
        }
      });
    }
  }, {
    key: "getAnswer",
    value: function getAnswer() {
      return this._answer;
    }
  }, {
    key: "touchingObject",
    value: function touchingObject(args, util) {
      return util.target.isTouchingObject(args.TOUCHINGOBJECTMENU);
    }
  }, {
    key: "touchingColor",
    value: function touchingColor(args, util) {
      var color = Cast.toRgbColorList(args.COLOR);
      return util.target.isTouchingColor(color);
    }
  }, {
    key: "colorTouchingColor",
    value: function colorTouchingColor(args, util) {
      var maskColor = Cast.toRgbColorList(args.COLOR);
      var targetColor = Cast.toRgbColorList(args.COLOR2);
      return util.target.colorIsTouchingColor(targetColor, maskColor);
    }
  }, {
    key: "distanceTo",
    value: function distanceTo(args, util) {
      if (util.target.isStage) return 10000;
      var targetX = 0;
      var targetY = 0;
      if (args.DISTANCETOMENU === '_mouse_') {
        targetX = util.ioQuery('mouse', 'getScratchX');
        targetY = util.ioQuery('mouse', 'getScratchY');
      } else {
        args.DISTANCETOMENU = Cast.toString(args.DISTANCETOMENU);
        var distTarget = this.runtime.getSpriteTargetByName(args.DISTANCETOMENU);
        if (!distTarget) return 10000;
        targetX = distTarget.x;
        targetY = distTarget.y;
      }
      var dx = util.target.x - targetX;
      var dy = util.target.y - targetY;
      return Math.sqrt(dx * dx + dy * dy);
    }
  }, {
    key: "setDragMode",
    value: function setDragMode(args, util) {
      util.target.setDraggable(args.DRAG_MODE === 'draggable');
    }
  }, {
    key: "getTimer",
    value: function getTimer(args, util) {
      return util.ioQuery('clock', 'projectTimer');
    }
  }, {
    key: "resetTimer",
    value: function resetTimer(args, util) {
      util.ioQuery('clock', 'resetProjectTimer');
    }
  }, {
    key: "getMouseX",
    value: function getMouseX(args, util) {
      return util.ioQuery('mouse', 'getScratchX');
    }
  }, {
    key: "getMouseY",
    value: function getMouseY(args, util) {
      return util.ioQuery('mouse', 'getScratchY');
    }
  }, {
    key: "getMouseDown",
    value: function getMouseDown(args, util) {
      return util.ioQuery('mouse', 'getIsDown');
    }
  }, {
    key: "current",
    value: function current(args) {
      var menuOption = Cast.toString(args.CURRENTMENU).toLowerCase();
      var date = new Date();
      switch (menuOption) {
        case 'year':
          return date.getFullYear();
        case 'month':
          return date.getMonth() + 1;
        // getMonth is zero-based
        case 'date':
          return date.getDate();
        case 'dayofweek':
          return date.getDay() + 1;
        // getDay is zero-based, Sun=0
        case 'hour':
          return date.getHours();
        case 'minute':
          return date.getMinutes();
        case 'second':
          return date.getSeconds();
      }
      return 0;
    }
  }, {
    key: "getKeyPressed",
    value: function getKeyPressed(args, util) {
      return util.ioQuery('keyboard', 'getKeyIsDown', [args.KEY_OPTION]);
    }
  }, {
    key: "daysSince2000",
    value: function daysSince2000() {
      var msPerDay = 24 * 60 * 60 * 1000;
      var start = new Date(2000, 0, 1); // Months are 0-indexed.
      var today = new Date();
      var dstAdjust = today.getTimezoneOffset() - start.getTimezoneOffset();
      var mSecsSinceStart = today.valueOf() - start.valueOf();
      mSecsSinceStart += (today.getTimezoneOffset() - dstAdjust) * 60 * 1000;
      return mSecsSinceStart / msPerDay;
    }
  }, {
    key: "getLoudness",
    value: function getLoudness() {
      if (typeof this.runtime.audioEngine === 'undefined') return -1;
      if (this.runtime.currentStepTime === null) return -1;

      // Only measure loudness once per step
      var timeSinceLoudness = this._timer.time() - this._cachedLoudnessTimestamp;
      if (timeSinceLoudness < this.runtime.currentStepTime) {
        return this._cachedLoudness;
      }
      this._cachedLoudnessTimestamp = this._timer.time();
      this._cachedLoudness = this.runtime.audioEngine.getLoudness();
      return this._cachedLoudness;
    }
  }, {
    key: "isLoud",
    value: function isLoud() {
      return this.getLoudness() > 10;
    }
  }, {
    key: "getAttributeOf",
    value: function getAttributeOf(args) {
      var attrTarget;
      if (args.OBJECT === '_stage_') {
        attrTarget = this.runtime.getTargetForStage();
      } else {
        args.OBJECT = Cast.toString(args.OBJECT);
        attrTarget = this.runtime.getSpriteTargetByName(args.OBJECT);
      }

      // attrTarget can be undefined if the target does not exist
      // (e.g. single sprite uploaded from larger project referencing
      // another sprite that wasn't uploaded)
      if (!attrTarget) return 0;

      // Generic attributes
      if (attrTarget.isStage) {
        switch (args.PROPERTY) {
          // Scratch 1.4 support
          case 'background #':
            return attrTarget.currentCostume + 1;
          case 'backdrop #':
            return attrTarget.currentCostume + 1;
          case 'backdrop name':
            return attrTarget.getCostumes()[attrTarget.currentCostume].name;
          case 'volume':
            return attrTarget.volume;
        }
      } else {
        switch (args.PROPERTY) {
          case 'x position':
            return attrTarget.x;
          case 'y position':
            return attrTarget.y;
          case 'direction':
            return attrTarget.direction;
          case 'costume #':
            return attrTarget.currentCostume + 1;
          case 'costume name':
            return attrTarget.getCostumes()[attrTarget.currentCostume].name;
          case 'size':
            return attrTarget.size;
          case 'volume':
            return attrTarget.volume;
        }
      }

      // Target variables.
      var varName = args.PROPERTY;
      var variable = attrTarget.lookupVariableByNameAndType(varName, '', true);
      if (variable) {
        return variable.value;
      }

      // Otherwise, 0
      return 0;
    }
  }, {
    key: "getUsername",
    value: function getUsername(args, util) {
      return util.ioQuery('userData', 'getUsername');
    }
  }, {
    key: "isOnline",
    value: function isOnline() {
      // Modern Node.js has a navigator object but does .onLine === undefined
      if ((typeof navigator === "undefined" ? "undefined" : _typeof(navigator)) === 'object' && typeof navigator.onLine === 'boolean') {
        return navigator.onLine;
      }
      // We're running in some non-browser environment. We probably have internet.
      return true;
    }
  }]);
}();
module.exports = Scratch3SensingBlocks;

/***/ }),

/***/ "../vm/src/blocks/scratch3_sound.js":
/*!******************************************!*\
  !*** ../vm/src/blocks/scratch3_sound.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Clone = __webpack_require__(/*! ../util/clone */ "../vm/src/util/clone.js");

/**
 * Occluded boolean value to make its use more understandable.
 * @const {boolean}
 */
var STORE_WAITING = true;
var Scratch3SoundBlocks = /*#__PURE__*/function () {
  function Scratch3SoundBlocks(runtime) {
    _classCallCheck(this, Scratch3SoundBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
    this.waitingSounds = {};

    // Clear sound effects on green flag and stop button events.
    this.stopAllSounds = this.stopAllSounds.bind(this);
    this._stopWaitingSoundsForTarget = this._stopWaitingSoundsForTarget.bind(this);
    this._clearEffectsForAllTargets = this._clearEffectsForAllTargets.bind(this);
    if (this.runtime) {
      this.runtime.on('PROJECT_STOP_ALL', this.stopAllSounds);
      this.runtime.on('PROJECT_STOP_ALL', this._clearEffectsForAllTargets);
      this.runtime.on('STOP_FOR_TARGET', this._stopWaitingSoundsForTarget);
      this.runtime.on('PROJECT_START', this._clearEffectsForAllTargets);
    }
    this._onTargetCreated = this._onTargetCreated.bind(this);
    if (this.runtime) {
      runtime.on('targetWasCreated', this._onTargetCreated);
    }
  }

  /**
   * The key to load & store a target's sound-related state.
   * @type {string}
   */
  return _createClass(Scratch3SoundBlocks, [{
    key: "_getSoundState",
    value:
    /**
     * @param {Target} target - collect sound state for this target.
     * @returns {SoundState} the mutable sound state associated with that target. This will be created if necessary.
     * @private
     */
    function _getSoundState(target) {
      var soundState = target.getCustomState(Scratch3SoundBlocks.STATE_KEY);
      if (!soundState) {
        soundState = Clone.simple(Scratch3SoundBlocks.DEFAULT_SOUND_STATE);
        target.setCustomState(Scratch3SoundBlocks.STATE_KEY, soundState);
        target.soundEffects = soundState.effects;
      }
      return soundState;
    }

    /**
     * When a Target is cloned, clone the sound state.
     * @param {Target} newTarget - the newly created target.
     * @param {Target} [sourceTarget] - the target used as a source for the new clone, if any.
     * @listens Runtime#event:targetWasCreated
     * @private
     */
  }, {
    key: "_onTargetCreated",
    value: function _onTargetCreated(newTarget, sourceTarget) {
      if (sourceTarget) {
        var soundState = sourceTarget.getCustomState(Scratch3SoundBlocks.STATE_KEY);
        if (soundState && newTarget) {
          newTarget.setCustomState(Scratch3SoundBlocks.STATE_KEY, Clone.simple(soundState));
          this._syncEffectsForTarget(newTarget);
        }
      }
    }

    /**
     * Retrieve the block primitives implemented by this package.
     * @return {object.<string, Function>} Mapping of opcode to Function.
     */
  }, {
    key: "getPrimitives",
    value: function getPrimitives() {
      return {
        sound_play: this.playSound,
        sound_playuntildone: this.playSoundAndWait,
        sound_stopallsounds: this.stopAllSounds,
        sound_seteffectto: this.setEffect,
        sound_changeeffectby: this.changeEffect,
        sound_cleareffects: this.clearEffects,
        sound_sounds_menu: this.soundsMenu,
        sound_beats_menu: this.beatsMenu,
        sound_effects_menu: this.effectsMenu,
        sound_setvolumeto: this.setVolume,
        sound_changevolumeby: this.changeVolume,
        sound_volume: this.getVolume
      };
    }
  }, {
    key: "getMonitored",
    value: function getMonitored() {
      return {
        sound_volume: {
          isSpriteSpecific: true,
          getId: function getId(targetId) {
            return "".concat(targetId, "_volume");
          }
        }
      };
    }
  }, {
    key: "playSound",
    value: function playSound(args, util) {
      // Don't return the promise, it's the only difference for AndWait
      this._playSound(args, util);
    }
  }, {
    key: "playSoundAndWait",
    value: function playSoundAndWait(args, util) {
      return this._playSound(args, util, STORE_WAITING);
    }
  }, {
    key: "_playSound",
    value: function _playSound(args, util, storeWaiting) {
      var index = this._getSoundIndex(args.SOUND_MENU, util);
      if (index >= 0) {
        var target = util.target;
        var sprite = target.sprite;
        var soundId = sprite.sounds[index].soundId;
        if (sprite.soundBank) {
          if (storeWaiting === STORE_WAITING) {
            this._addWaitingSound(target.id, soundId);
          } else {
            this._removeWaitingSound(target.id, soundId);
          }
          return sprite.soundBank.playSound(target, soundId);
        }
      }
    }
  }, {
    key: "_addWaitingSound",
    value: function _addWaitingSound(targetId, soundId) {
      if (!this.waitingSounds[targetId]) {
        this.waitingSounds[targetId] = new Set();
      }
      this.waitingSounds[targetId].add(soundId);
    }
  }, {
    key: "_removeWaitingSound",
    value: function _removeWaitingSound(targetId, soundId) {
      if (!this.waitingSounds[targetId]) {
        return;
      }
      this.waitingSounds[targetId]["delete"](soundId);
    }
  }, {
    key: "_getSoundIndex",
    value: function _getSoundIndex(soundName, util) {
      // if the sprite has no sounds, return -1
      var len = util.target.sprite.sounds.length;
      if (len === 0) {
        return -1;
      }

      // look up by name first
      var index = this.getSoundIndexByName(soundName, util);
      if (index !== -1) {
        return index;
      }

      // then try using the sound name as a 1-indexed index
      var oneIndexedIndex = parseInt(soundName, 10);
      if (!isNaN(oneIndexedIndex)) {
        return MathUtil.wrapClamp(oneIndexedIndex - 1, 0, len - 1);
      }

      // could not be found as a name or converted to index, return -1
      return -1;
    }
  }, {
    key: "getSoundIndexByName",
    value: function getSoundIndexByName(soundName, util) {
      var sounds = util.target.sprite.sounds;
      for (var i = 0; i < sounds.length; i++) {
        if (sounds[i].name === soundName) {
          return i;
        }
      }
      // if there is no sound by that name, return -1
      return -1;
    }
  }, {
    key: "stopAllSounds",
    value: function stopAllSounds() {
      if (this.runtime.targets === null) return;
      var allTargets = this.runtime.targets;
      for (var i = 0; i < allTargets.length; i++) {
        this._stopAllSoundsForTarget(allTargets[i]);
      }
    }
  }, {
    key: "_stopAllSoundsForTarget",
    value: function _stopAllSoundsForTarget(target) {
      if (target.sprite.soundBank) {
        target.sprite.soundBank.stopAllSounds(target);
        if (this.waitingSounds[target.id]) {
          this.waitingSounds[target.id].clear();
        }
      }
    }
  }, {
    key: "_stopWaitingSoundsForTarget",
    value: function _stopWaitingSoundsForTarget(target) {
      if (target.sprite.soundBank) {
        if (this.waitingSounds[target.id]) {
          var _iterator = _createForOfIteratorHelper(this.waitingSounds[target.id].values()),
            _step;
          try {
            for (_iterator.s(); !(_step = _iterator.n()).done;) {
              var soundId = _step.value;
              target.sprite.soundBank.stop(target, soundId);
            }
          } catch (err) {
            _iterator.e(err);
          } finally {
            _iterator.f();
          }
          this.waitingSounds[target.id].clear();
        }
      }
    }
  }, {
    key: "setEffect",
    value: function setEffect(args, util) {
      return this._updateEffect(args, util, false);
    }
  }, {
    key: "changeEffect",
    value: function changeEffect(args, util) {
      return this._updateEffect(args, util, true);
    }
  }, {
    key: "_updateEffect",
    value: function _updateEffect(args, util, change) {
      var effect = Cast.toString(args.EFFECT).toLowerCase();
      var value = Cast.toNumber(args.VALUE);
      var soundState = this._getSoundState(util.target);
      if (!Object.prototype.hasOwnProperty.call(soundState.effects, effect)) return;
      if (change) {
        soundState.effects[effect] += value;
      } else {
        soundState.effects[effect] = value;
      }
      var miscLimits = this.runtime.runtimeOptions.miscLimits;
      var _ref = miscLimits ? Scratch3SoundBlocks.EFFECT_RANGE[effect] : Scratch3SoundBlocks.LARGER_EFFECT_RANGE[effect],
        min = _ref.min,
        max = _ref.max;
      soundState.effects[effect] = MathUtil.clamp(soundState.effects[effect], min, max);
      this._syncEffectsForTarget(util.target);
      if (miscLimits) {
        // Yield until the next tick.
        return Promise.resolve();
      }

      // Requesting a redraw makes sure that "forever: change pitch by 1" still work but without
      // yielding unnecessarily in other cases
      this.runtime.requestRedraw();
    }
  }, {
    key: "_syncEffectsForTarget",
    value: function _syncEffectsForTarget(target) {
      if (!target || !target.sprite.soundBank) return;
      target.soundEffects = this._getSoundState(target).effects;
      target.sprite.soundBank.setEffects(target);
    }
  }, {
    key: "clearEffects",
    value: function clearEffects(args, util) {
      this._clearEffectsForTarget(util.target);
    }
  }, {
    key: "_clearEffectsForTarget",
    value: function _clearEffectsForTarget(target) {
      var soundState = this._getSoundState(target);
      for (var effect in soundState.effects) {
        if (!Object.prototype.hasOwnProperty.call(soundState.effects, effect)) continue;
        soundState.effects[effect] = 0;
      }
      this._syncEffectsForTarget(target);
    }
  }, {
    key: "_clearEffectsForAllTargets",
    value: function _clearEffectsForAllTargets() {
      if (this.runtime.targets === null) return;
      var allTargets = this.runtime.targets;
      for (var i = 0; i < allTargets.length; i++) {
        this._clearEffectsForTarget(allTargets[i]);
      }
    }
  }, {
    key: "setVolume",
    value: function setVolume(args, util) {
      var volume = Cast.toNumber(args.VOLUME);
      return this._updateVolume(volume, util);
    }
  }, {
    key: "changeVolume",
    value: function changeVolume(args, util) {
      var volume = Cast.toNumber(args.VOLUME) + util.target.volume;
      return this._updateVolume(volume, util);
    }
  }, {
    key: "_updateVolume",
    value: function _updateVolume(volume, util) {
      volume = MathUtil.clamp(volume, 0, 100);
      util.target.volume = volume;
      this._syncEffectsForTarget(util.target);
      if (this.runtime.runtimeOptions.miscLimits) {
        // Yield until the next tick.
        return Promise.resolve();
      }
      this.runtime.requestRedraw();
    }
  }, {
    key: "getVolume",
    value: function getVolume(args, util) {
      return util.target.volume;
    }
  }, {
    key: "soundsMenu",
    value: function soundsMenu(args) {
      return args.SOUND_MENU;
    }
  }, {
    key: "beatsMenu",
    value: function beatsMenu(args) {
      return args.BEATS;
    }
  }, {
    key: "effectsMenu",
    value: function effectsMenu(args) {
      return args.EFFECT;
    }
  }], [{
    key: "STATE_KEY",
    get: function get() {
      return 'Scratch.sound';
    }

    /**
     * The default sound-related state, to be used when a target has no existing sound state.
     * @type {SoundState}
     */
  }, {
    key: "DEFAULT_SOUND_STATE",
    get: function get() {
      return {
        effects: {
          pitch: 0,
          pan: 0
        }
      };
    }

    /**
     * The minimum and maximum MIDI note numbers, for clamping the input to play note.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "MIDI_NOTE_RANGE",
    get: function get() {
      return {
        min: 36,
        max: 96
      }; // C2 to C7
    }

    /**
     * The minimum and maximum beat values, for clamping the duration of play note, play drum and rest.
     * 100 beats at the default tempo of 60bpm is 100 seconds.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "BEAT_RANGE",
    get: function get() {
      return {
        min: 0,
        max: 100
      };
    }

    /** The minimum and maximum tempo values, in bpm.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "TEMPO_RANGE",
    get: function get() {
      return {
        min: 20,
        max: 500
      };
    }

    /** The minimum and maximum values for each sound effect.
     * @type {{effect:{min: number, max: number}}}
     */
  }, {
    key: "EFFECT_RANGE",
    get: function get() {
      return {
        pitch: {
          min: -360,
          max: 360
        },
        // -3 to 3 octaves
        pan: {
          min: -100,
          max: 100
        } // 100% left to 100% right
      };
    }

    /** The minimum and maximum values for sound effects when miscellaneous limits are removed. */
  }, {
    key: "LARGER_EFFECT_RANGE",
    get: function get() {
      return {
        // scratch-audio throws if pitch is too big because some math results in Infinity
        pitch: {
          min: -1000,
          max: 1000
        },
        // No reason for these to go beyond 100
        pan: {
          min: -100,
          max: 100
        }
      };
    }
  }]);
}();
module.exports = Scratch3SoundBlocks;

/***/ }),

/***/ "../vm/src/compiler/compat-block-utility.js":
/*!**************************************************!*\
  !*** ../vm/src/compiler/compat-block-utility.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
// @ts-check

var BlockUtility = __webpack_require__(/*! ../engine/block-utility */ "../vm/src/engine/block-utility.js");
var CompatibilityLayerBlockUtility = /*#__PURE__*/function (_BlockUtility) {
  function CompatibilityLayerBlockUtility() {
    var _this;
    _classCallCheck(this, CompatibilityLayerBlockUtility);
    _this = _callSuper(this, CompatibilityLayerBlockUtility);
    _this._stackFrame = {};
    _this._startedBranch = null;
    return _this;
  }
  _inherits(CompatibilityLayerBlockUtility, _BlockUtility);
  return _createClass(CompatibilityLayerBlockUtility, [{
    key: "stackFrame",
    get: function get() {
      var _this$thread;
      return (_this$thread = this.thread) === null || _this$thread === void 0 ? void 0 : _this$thread.compatibilityStackFrame;
    }
  }, {
    key: "startBranch",
    value: function startBranch(branchNumber, isLoop) {
      this._startedBranch = [branchNumber, isLoop];
    }
  }, {
    key: "startProcedure",
    value: function startProcedure() {
      throw new Error('startProcedure is not supported by this BlockUtility');
    }

    // Parameters are not used by compiled scripts.
  }, {
    key: "initParams",
    value: function initParams() {
      throw new Error('initParams is not supported by this BlockUtility');
    }
  }, {
    key: "pushParam",
    value: function pushParam() {
      throw new Error('pushParam is not supported by this BlockUtility');
    }
  }, {
    key: "getParam",
    value: function getParam() {
      throw new Error('getParam is not supported by this BlockUtility');
    }
  }, {
    key: "init",
    value: function init(thread, fakeBlockId, stackFrame) {
      this.thread = thread;
      this.sequencer = thread.target.runtime.sequencer;
      this._startedBranch = null;
      thread.stack[0] = fakeBlockId;
      thread.compatibilityStackFrame = stackFrame;
    }
  }]);
}(BlockUtility); // Export a single instance to be reused.
module.exports = new CompatibilityLayerBlockUtility();

/***/ }),

/***/ "../vm/src/compiler/compat-blocks.js":
/*!*******************************************!*\
  !*** ../vm/src/compiler/compat-blocks.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// @ts-check

/**
 * @fileoverview List of blocks to be supported in the compiler compatibility layer.
 * This is only for native blocks. Extensions should not be listed here.
 */

// Please keep these lists alphabetical.

var stacked = ['looks_changestretchby', 'looks_hideallsprites', 'looks_sayforsecs', 'looks_setstretchto', 'looks_switchbackdroptoandwait', 'looks_thinkforsecs', 'motion_align_scene', 'motion_glidesecstoxy', 'motion_glideto', 'motion_goto', 'motion_pointtowards', 'motion_scroll_right', 'motion_scroll_up', 'sensing_askandwait', 'sensing_setdragmode', 'sound_changeeffectby', 'sound_changevolumeby', 'sound_cleareffects', 'sound_play', 'sound_playuntildone', 'sound_seteffectto', 'sound_setvolumeto', 'sound_stopallsounds'];
var inputs = ['motion_xscroll', 'motion_yscroll', 'sensing_loud', 'sensing_loudness', 'sensing_online', 'sensing_userid', 'sound_volume'];
module.exports = {
  stacked: stacked,
  inputs: inputs
};

/***/ }),

/***/ "../vm/src/compiler/compile.js":
/*!*************************************!*\
  !*** ../vm/src/compiler/compile.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// @ts-check

var _require = __webpack_require__(/*! ./irgen */ "../vm/src/compiler/irgen.js"),
  IRGenerator = _require.IRGenerator;
var _require2 = __webpack_require__(/*! ./iroptimizer */ "../vm/src/compiler/iroptimizer.js"),
  IROptimizer = _require2.IROptimizer;
var _require3 = __webpack_require__(/*! ./hotloop-analyzer */ "../vm/src/compiler/hotloop-analyzer.js"),
  HotLoopAnalyzer = _require3.HotLoopAnalyzer;
var _require4 = __webpack_require__(/*! ./offload-planner */ "../vm/src/compiler/offload-planner.js"),
  OffloadPlanner = _require4.OffloadPlanner;
var JSGenerator = __webpack_require__(/*! ./jsgen */ "../vm/src/compiler/jsgen.js");
var compile = function compile(/** @type {import("../engine/thread")} */thread) {
  var irGenerator = new IRGenerator(thread);
  var ir = irGenerator.generate();
  var irOptimizer = new IROptimizer(ir);
  irOptimizer.optimize();
  var hotLoopAnalyzer = new HotLoopAnalyzer(ir);
  var hotLoops = hotLoopAnalyzer.analyze();
  var offloadPlanner = new OffloadPlanner(ir, hotLoops, {
    offload: thread.target.runtime.compilerOptions.offload,
    backends: {
      wasm: false,
      webgl: false
    }
  });
  var plans = offloadPlanner.plan();
  var procedures = {};
  var target = thread.target;
  var compileScript = function compileScript(/** @type {import("./intermediate").IntermediateScript} */script) {
    if (script.cachedCompileResult) {
      return script.cachedCompileResult;
    }
    var compiler = new JSGenerator(script, ir, target, plans);
    var result = compiler.compile();
    script.cachedCompileResult = result;
    return result;
  };
  var entry = compileScript(ir.entry);
  for (var _i = 0, _Object$keys = Object.keys(ir.procedures); _i < _Object$keys.length; _i++) {
    var procedureVariant = _Object$keys[_i];
    var procedureData = ir.procedures[procedureVariant];
    var procedureTree = compileScript(procedureData);
    procedures[procedureVariant] = procedureTree;
  }
  return {
    startingFunction: entry,
    procedures: procedures,
    executableHat: ir.entry.executableHat,
    offloadPlans: plans
  };
};
module.exports = compile;

/***/ }),

/***/ "../vm/src/compiler/enums.js":
/*!***********************************!*\
  !*** ../vm/src/compiler/enums.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

// @ts-check

/**
 * @fileoverview Common enums shared amongst parts of the compiler.
 */

/**
 * Enum for the type of the value that is returned by reporter blocks and stored in constants.
 *
 * At compile time, often we don't know exactly type a value will be but we can tell it must be one of a
 * set of types. For this reason, the number value of each type represents a possibility space, where set
 * bits indicate that their corropoding type *could* be encountered at runtime.
 * For example, a type of InputType.NUMBER | InputType.STRING means the value will be either a number or
 * a string at runtime, the compiler can't tell which, but we do know that it's not a boolean or NaN as
 * those bits are not set.
 *
 * @readonly
 * @enum {number}
 */
var InputType = {
  /** The value Infinity */
  NUMBER_POS_INF: 0x001,
  /** Any natural number */
  NUMBER_POS_INT: 0x002,
  /** Any positive fractional number, excluding integers. */
  NUMBER_POS_FRACT: 0x004,
  /** Any positive number excluding 0 and Infinity. Equal to NUMBER_POS_INT | NUMBER_POS_FRACT */
  NUMBER_POS_REAL: 0x006,
  /** The value 0 */
  NUMBER_ZERO: 0x008,
  /** The value -0 */
  NUMBER_NEG_ZERO: 0x010,
  /** Any negitive integer excluding -0 */
  NUMBER_NEG_INT: 0x020,
  /** Any negitive fractional number, excluding integers. */
  NUMBER_NEG_FRACT: 0x040,
  /** Any negitive number excluding -0 and -Infinity. Equal to NUMBER_NEG_INT | NUMBER_NEG_FRACT */
  NUMBER_NEG_REAL: 0x060,
  /** The value -Infinity */
  NUMBER_NEG_INF: 0x080,
  /** The value NaN */
  NUMBER_NAN: 0x100,
  /** Either 0 or -0. Equal to NUMBER_ZERO | NUMBER_NEG_ZERO */
  NUMBER_ANY_ZERO: 0x018,
  /** Either Infinity or -Infinity. Equal to NUMBER_POS_INF | NUMBER_NEG_INF */
  NUMBER_INF: 0x081,
  /** Any positive number, excluding 0. Equal to NUMBER_POS_REAL | NUMBER_POS_INF */
  NUMBER_POS: 0x007,
  /** Any negitive number, excluding -0. Equal to NUMBER_NEG_REAL | NUMBER_NEG_INF */
  NUMBER_NEG: 0x0E0,
  /** Any whole number. Equal to NUMBER_POS_INT | NUMBER_ZERO */
  NUMBER_WHOLE: 0x00A,
  /** Any integer. Equal to NUMBER_POS_INT | NUMBER_ANY_ZERO | NUMBER_NEG_INT */
  NUMBER_INT: 0x03A,
  /** Any number that works as an array index. Equal to NUMBER_INT | NUMBER_INF | NUMBER_NAN */
  NUMBER_INDEX: 0x1BB,
  /** Any fractional non-integer numbers. Equal to NUMBER_POS_FRACT | NUMBER_NEG_FRACT */
  NUMBER_FRACT: 0x44,
  /** Any real number. Equal to NUMBER_POS_REAL | NUMBER_ANY_ZERO | NUMBER_NEG_REAL */
  NUMBER_REAL: 0x07E,
  /** Any number, excluding NaN. Equal to NUMBER_REAL | NUMBER_INF */
  NUMBER: 0x0FF,
  /** Any number, including NaN. Equal to NUMBER | NUMBER_NAN */
  NUMBER_OR_NAN: 0x1FF,
  /** Anything that can be interperated as a number. Equal to NUMBER | STRING_NUM | BOOLEAN */
  NUMBER_INTERPRETABLE: 0x12FF,
  /** Any string which as a non-NaN neumeric interpretation, excluding ''.  */
  STRING_NUM: 0x200,
  /** Any string which has no non-NaN neumeric interpretation, including ''. */
  STRING_NAN: 0x400,
  /** Either of the strings 'true' or 'false'. */
  STRING_BOOLEAN: 0x800,
  /** Any string. Equal to STRING_NUM | STRING_NAN | STRING_BOOLEAN */
  STRING: 0xE00,
  /** Any boolean. */
  BOOLEAN: 0x1000,
  /** Any input that can be interperated as a boolean. Equal to BOOLEAN | STRING_BOOLEAN */
  BOOLEAN_INTERPRETABLE: 0x1800,
  /** Any value type (a type a scratch variable can hold). Equal to NUMBER_OR_NAN | STRING | BOOLEAN */
  ANY: 0x1FFF,
  /** An array of values in the form [R, G, B] */
  COLOR: 0x2000
};

/**
 * Enum for the opcodes of the stackable blocks used in the IR AST.
 * @readonly
 * @enum {string}
 */
var StackOpcode = {
  NOP: 'noop',
  ADDON_CALL: 'addons.call',
  DEBUGGER: 'tw.debugger',
  VISUAL_REPORT: 'visualReport',
  COMPATIBILITY_LAYER: 'compat',
  OLD_COMPILER_COMPATIBILITY_LAYER: 'oldCompiler',
  HAT_EDGE: 'hat.edge',
  HAT_PREDICATE: 'hat.predicate',
  CONTROL_IF_ELSE: 'control.if',
  CONTROL_CLONE_CREATE: 'control.createClone',
  CONTROL_CLONE_DELETE: 'control.deleteClone',
  CONTROL_WHILE: 'control.while',
  CONTROL_FOR: 'control.for',
  CONTROL_REPEAT: 'control.repeat',
  CONTROL_STOP_ALL: 'control.stopAll',
  CONTROL_STOP_OTHERS: 'control.stopOthers',
  CONTROL_STOP_SCRIPT: 'control.stopScript',
  CONTROL_WAIT: 'control.wait',
  CONTROL_WAIT_UNTIL: 'control.waitUntil',
  CONTROL_CLEAR_COUNTER: 'control.counterClear',
  CONTORL_INCR_COUNTER: 'control.counterIncr',
  LIST_ADD: 'list.add',
  LIST_INSERT: 'list.instert',
  LIST_REPLACE: 'list.replace',
  LIST_DELETE_ALL: 'list.deleteAll',
  LIST_DELETE: 'list.delete',
  LIST_SHOW: 'list.show',
  LIST_HIDE: 'list.hide',
  VAR_SET: 'var.set',
  VAR_SHOW: 'var.show',
  VAR_HIDE: 'var.hide',
  EVENT_BROADCAST: 'event.broadcast',
  EVENT_BROADCAST_AND_WAIT: 'event.broadcastAndWait',
  LOOKS_EFFECT_SET: 'looks.setEffect',
  LOOKS_EFFECT_CHANGE: 'looks.changeEffect',
  LOOKS_EFFECT_CLEAR: 'looks.clearEffects',
  LOOKS_SIZE_CHANGE: 'looks.changeSize',
  LOOKS_SIZE_SET: 'looks.setSize',
  LOOKS_LAYER_FORWARD: 'looks.forwardLayers',
  LOOKS_LAYER_BACKWARD: 'looks.backwardLayers',
  LOOKS_LAYER_FRONT: 'looks.goToFront',
  LOOKS_LAYER_BACK: 'looks.goToBack',
  LOOKS_HIDE: 'looks.hide',
  LOOKS_SHOW: 'looks.show',
  LOOKS_BACKDROP_NEXT: 'looks.nextBackdrop',
  LOOKS_BACKDROP_SET: 'looks.switchBackdrop',
  LOOKS_COSTUME_NEXT: 'looks.nextCostume',
  LOOKS_COSTUME_SET: 'looks.switchCostume',
  LOOKS_SAY: 'looks.say',
  LOOKS_THINK: 'looks.think',
  MOTION_X_SET: 'motion.setX',
  MOTION_X_CHANGE: 'motion.changeX',
  MOTION_Y_SET: 'motion.setY',
  MOTION_Y_CHANGE: 'motion.changeY',
  MOTION_XY_SET: 'motion.setXY',
  MOTION_IF_ON_EDGE_BOUNCE: 'motion.ifOnEdgeBounce',
  MOTION_STEP: 'motion.step',
  MOTION_ROTATION_STYLE_SET: 'motion.setRotationStyle',
  MOTION_DIRECTION_SET: 'motion.setDirection',
  PEN_UP: 'pen.up',
  PEN_DOWN: 'pen.down',
  PEN_CLEAR: 'pen.clear',
  PEN_COLOR_PARAM_SET: 'pen.setParam',
  PEN_COLOR_PARAM_CHANGE: 'pen.changeParam',
  PEN_COLOR_HUE_CHANGE_LEGACY: 'pen.legacyChangeHue',
  PEN_COLOR_HUE_SET_LEGACY: 'pen_setPenHueToNumber',
  PEN_COLOR_SHADE_CHANGE_LEGACY: 'pen.legacyChangeShade',
  PEN_COLOR_SHADE_SET_LEGACY: 'pen.legacySetShade',
  PEN_COLOR_SET: 'pen.setColor',
  PEN_SIZE_SET: 'pen.setSize',
  PEN_SIZE_CHANGE: 'pen.changeSize',
  PEN_STAMP: 'pen.stamp',
  SENSING_TIMER_RESET: 'timer.reset',
  PROCEDURE_RETURN: 'procedures.return',
  PROCEDURE_CALL: 'procedures.call'
};

/**
 * Enum for the opcodes of the reporter blocks used in the IR AST.
 * @readonly
 * @enum {string}
 */
var InputOpcode = {
  NOP: 'noop',
  ADDON_CALL: 'addons.call',
  CONSTANT: 'constant',
  CAST_NUMBER: 'cast.toNumber',
  CAST_NUMBER_INDEX: 'cast.toInteger',
  CAST_NUMBER_OR_NAN: 'cast.toNumberOrNaN',
  CAST_STRING: 'cast.toString',
  CAST_BOOLEAN: 'cast.toBoolean',
  CAST_COLOR: 'cast.toColor',
  COMPATIBILITY_LAYER: 'compat',
  OLD_COMPILER_COMPATIBILITY_LAYER: 'oldCompiler',
  LOOKS_BACKDROP_NUMBER: 'looks.backdropNumber',
  LOOKS_BACKDROP_NAME: 'looks.backdropName',
  LOOKS_COSTUME_NUMBER: 'looks.costumeNumber',
  LOOKS_COSTUME_NAME: 'looks.costumeName',
  LOOKS_SIZE_GET: 'looks.size',
  VAR_GET: 'var.get',
  LIST_GET: 'list.get',
  LIST_LENGTH: 'list.length',
  LIST_CONTAINS: 'list.contains',
  LIST_INDEX_OF: 'list.indexOf',
  LIST_CONTENTS: 'list.contents',
  MOTION_X_GET: 'motion.x',
  MOTION_Y_GET: 'motion.y',
  MOTION_DIRECTION_GET: 'motion.direction',
  OP_ADD: 'op.add',
  OP_AND: 'op.and',
  OP_CONTAINS: 'op.contains',
  OP_DIVIDE: 'op.divide',
  OP_EQUALS: 'op.equals',
  OP_GREATER: 'op.greater',
  OP_LESS: 'op.less',
  OP_JOIN: 'op.join',
  OP_LENGTH: 'op.length',
  OP_LETTER_OF: 'op.letterOf',
  OP_ABS: 'op.abs',
  OP_FLOOR: 'op.floor',
  OP_CEILING: 'op.ceiling',
  OP_SQRT: 'op.sqrt',
  OP_SIN: 'op.sin',
  OP_COS: 'op.cos',
  OP_TAN: 'op.tan',
  OP_ASIN: 'op.asin',
  OP_ACOS: 'op.acos',
  OP_ATAN: 'op.atan',
  OP_LOG_E: 'op.ln',
  OP_LOG_10: 'op.log',
  OP_POW_E: 'op.e^',
  OP_POW_10: 'op.10^',
  OP_MOD: 'op.mod',
  OP_MULTIPLY: 'op.multiply',
  OP_NOT: 'op.not',
  OP_OR: 'op.or',
  OP_RANDOM: 'op.random',
  OP_ROUND: 'op.round',
  OP_SUBTRACT: 'op.subtract',
  SENSING_ANSWER: 'sensing.answer',
  SENSING_COLOR_TOUCHING_COLOR: 'sensing.colorTouchingColor',
  SENSING_TIME_YEAR: 'sensing.year',
  SENSING_TIME_MONTH: 'sensing.month',
  SENSING_TIME_DATE: 'sensing.date',
  SENSING_TIME_WEEKDAY: 'sensing.dayofweek',
  SENSING_TIME_HOUR: 'sensing.hour',
  SENSING_TIME_MINUTE: 'sensing.minute',
  SENSING_TIME_SECOND: 'sensing.second',
  SENSING_TIME_DAYS_SINCE_2000: 'sensing.daysSince2000',
  SENSING_DISTANCE: 'sensing.distance',
  SENSING_KEY_DOWN: 'keyboard.pressed',
  SENSING_MOUSE_DOWN: 'mouse.down',
  SENSING_MOUSE_X: 'mouse.x',
  SENSING_MOUSE_Y: 'mouse.y',
  SENSING_OF: 'sensing.of',
  SENSING_OF_BACKDROP_NAME: 'sensing.of.backdropName',
  SENSING_OF_BACKDROP_NUMBER: 'sensing.of.backdropNumber',
  SENSING_OF_COSTUME_NAME: 'sensing.of.costumeName',
  SENSING_OF_COSTUME_NUMBER: 'sensing.of.costumeNumber',
  SENSING_OF_VOLUME: 'sensing.of.volume',
  SENSING_OF_POS_X: 'sensing.of.x',
  SENSING_OF_POS_Y: 'sensing.of.y',
  SENSING_OF_DIRECTION: 'sensing.of.direction',
  SENSING_OF_SIZE: 'sensing.of.size',
  SENSING_OF_VAR: 'sensing.of.var',
  SENSING_TIMER_GET: 'timer.get',
  SENSING_TOUCHING_COLOR: 'sensing.touchingColor',
  SENSING_TOUCHING_OBJECT: 'sensing.touching',
  SENSING_USERNAME: 'sensing.username',
  PROCEDURE_CALL: 'procedures.call',
  PROCEDURE_ARGUMENT: 'procedures.argument',
  CONTROL_COUNTER: 'control.counter',
  TW_KEY_LAST_PRESSED: 'tw.lastKeyPressed'
};
module.exports = {
  StackOpcode: StackOpcode,
  InputOpcode: InputOpcode,
  InputType: InputType
};

/***/ }),

/***/ "../vm/src/compiler/environment.js":
/*!*****************************************!*\
  !*** ../vm/src/compiler/environment.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// @ts-check
/* eslint-disable no-eval */

/**
 * @returns {boolean} true if the nullish coalescing operator (x ?? y) is supported.
 * See https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing_operator
 */
var supportsNullishCoalescing = function supportsNullishCoalescing() {
  try {
    // eslint-disable-next-line no-unused-vars
    var fn = new Function('undefined ?? 3');
    // if function construction succeeds, the browser understood the syntax.
    return true;
  } catch (e) {
    return false;
  }
};
module.exports = {
  supportsNullishCoalescing: supportsNullishCoalescing()
};

/***/ }),

/***/ "../vm/src/compiler/hotloop-analyzer.js":
/*!**********************************************!*\
  !*** ../vm/src/compiler/hotloop-analyzer.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// @ts-check

var _require = __webpack_require__(/*! ./enums.js */ "../vm/src/compiler/enums.js"),
  StackOpcode = _require.StackOpcode,
  InputOpcode = _require.InputOpcode,
  InputType = _require.InputType;
var LOOP_OPCODES = new Set([StackOpcode.CONTROL_REPEAT, StackOpcode.CONTROL_FOR, StackOpcode.CONTROL_WHILE]);
var SAFE_STACK_OPCODES = new Set([StackOpcode.NOP, StackOpcode.VAR_SET, StackOpcode.CONTROL_IF_ELSE]);
var PURE_INPUT_OPCODES = new Set([InputOpcode.NOP, InputOpcode.CONSTANT, InputOpcode.VAR_GET, InputOpcode.PROCEDURE_ARGUMENT, InputOpcode.CAST_BOOLEAN, InputOpcode.CAST_NUMBER, InputOpcode.CAST_NUMBER_INDEX, InputOpcode.CAST_NUMBER_OR_NAN, InputOpcode.OP_ADD, InputOpcode.OP_SUBTRACT, InputOpcode.OP_MULTIPLY, InputOpcode.OP_DIVIDE, InputOpcode.OP_MOD, InputOpcode.OP_ABS, InputOpcode.OP_FLOOR, InputOpcode.OP_CEILING, InputOpcode.OP_SQRT, InputOpcode.OP_SIN, InputOpcode.OP_COS, InputOpcode.OP_TAN, InputOpcode.OP_ASIN, InputOpcode.OP_ACOS, InputOpcode.OP_ATAN, InputOpcode.OP_LOG_E, InputOpcode.OP_LOG_10, InputOpcode.OP_POW_E, InputOpcode.OP_POW_10, InputOpcode.OP_ROUND, InputOpcode.OP_EQUALS, InputOpcode.OP_GREATER, InputOpcode.OP_LESS, InputOpcode.OP_NOT, InputOpcode.OP_AND, InputOpcode.OP_OR]);
var ARITHMETIC_INPUT_OPCODES = new Set([InputOpcode.OP_ADD, InputOpcode.OP_SUBTRACT, InputOpcode.OP_MULTIPLY, InputOpcode.OP_DIVIDE, InputOpcode.OP_MOD, InputOpcode.OP_ABS, InputOpcode.OP_FLOOR, InputOpcode.OP_CEILING, InputOpcode.OP_SQRT, InputOpcode.OP_SIN, InputOpcode.OP_COS, InputOpcode.OP_TAN, InputOpcode.OP_ASIN, InputOpcode.OP_ACOS, InputOpcode.OP_ATAN, InputOpcode.OP_LOG_E, InputOpcode.OP_LOG_10, InputOpcode.OP_POW_E, InputOpcode.OP_POW_10, InputOpcode.OP_ROUND]);
var TIMER_INPUT_OPCODES = new Set([InputOpcode.SENSING_TIMER_GET, InputOpcode.SENSING_TIME_YEAR, InputOpcode.SENSING_TIME_MONTH, InputOpcode.SENSING_TIME_DATE, InputOpcode.SENSING_TIME_WEEKDAY, InputOpcode.SENSING_TIME_HOUR, InputOpcode.SENSING_TIME_MINUTE, InputOpcode.SENSING_TIME_SECOND, InputOpcode.SENSING_TIME_DAYS_SINCE_2000]);
var IO_INPUT_OPCODES = new Set([InputOpcode.SENSING_ANSWER, InputOpcode.SENSING_COLOR_TOUCHING_COLOR, InputOpcode.SENSING_DISTANCE, InputOpcode.SENSING_KEY_DOWN, InputOpcode.SENSING_MOUSE_DOWN, InputOpcode.SENSING_MOUSE_X, InputOpcode.SENSING_MOUSE_Y, InputOpcode.SENSING_OF, InputOpcode.SENSING_OF_BACKDROP_NAME, InputOpcode.SENSING_OF_BACKDROP_NUMBER, InputOpcode.SENSING_OF_COSTUME_NAME, InputOpcode.SENSING_OF_COSTUME_NUMBER, InputOpcode.SENSING_OF_DIRECTION, InputOpcode.SENSING_OF_POS_X, InputOpcode.SENSING_OF_POS_Y, InputOpcode.SENSING_OF_SIZE, InputOpcode.SENSING_OF_VAR, InputOpcode.SENSING_OF_VOLUME, InputOpcode.SENSING_TOUCHING_COLOR, InputOpcode.SENSING_TOUCHING_OBJECT, InputOpcode.SENSING_USERNAME, InputOpcode.TW_KEY_LAST_PRESSED, InputOpcode.MOTION_X_GET, InputOpcode.MOTION_Y_GET, InputOpcode.MOTION_DIRECTION_GET, InputOpcode.LOOKS_BACKDROP_NAME, InputOpcode.LOOKS_BACKDROP_NUMBER, InputOpcode.LOOKS_COSTUME_NAME, InputOpcode.LOOKS_COSTUME_NUMBER, InputOpcode.LOOKS_SIZE_GET, InputOpcode.CONTROL_COUNTER]);
var LIST_INPUT_OPCODES = new Set([InputOpcode.LIST_GET, InputOpcode.LIST_LENGTH, InputOpcode.LIST_CONTAINS, InputOpcode.LIST_INDEX_OF, InputOpcode.LIST_CONTENTS]);
var JS_ONLY_STACK_PREFIXES = ['looks.', 'motion.', 'pen.'];
var isNumericLikeType = function isNumericLikeType(type) {
  return (type & InputType.NUMBER_OR_NAN) === type;
};
var isFloat32Constant = function isFloat32Constant(value) {
  if (typeof value !== 'number') return false;
  if (!Number.isFinite(value)) return false;
  return Math.fround(value) === value;
};
var HotLoopAnalyzer = /*#__PURE__*/function () {
  /**
   * @param {import('./intermediate').IntermediateRepresentation} ir
   */
  function HotLoopAnalyzer(ir) {
    _classCallCheck(this, HotLoopAnalyzer);
    this.ir = ir;
    this.nextLoopId = 0;
  }
  return _createClass(HotLoopAnalyzer, [{
    key: "analyze",
    value: function analyze() {
      var hotLoops = [];
      this._analyzeScript(this.ir.entry, 'entry', hotLoops);
      for (var _i = 0, _Object$keys = Object.keys(this.ir.procedures); _i < _Object$keys.length; _i++) {
        var procedureVariant = _Object$keys[_i];
        this._analyzeScript(this.ir.procedures[procedureVariant], procedureVariant, hotLoops);
      }
      this.ir.hotLoops = hotLoops;
      return hotLoops;
    }

    /**
     * @param {import('./intermediate').IntermediateScript} script
     * @param {string} scriptId
     * @param {object[]} hotLoops
     * @private
     */
  }, {
    key: "_analyzeScript",
    value: function _analyzeScript(script, scriptId, hotLoops) {
      script.loopSummaries = [];
      if (!script.stack) {
        return;
      }
      this._scanStack(script.stack, script, scriptId, hotLoops);
    }

    /**
     * @param {import('./intermediate').IntermediateStack} stack
     * @param {import('./intermediate').IntermediateScript} script
     * @param {string} scriptId
     * @param {object[]} hotLoops
     * @private
     */
  }, {
    key: "_scanStack",
    value: function _scanStack(stack, script, scriptId, hotLoops) {
      var _iterator = _createForOfIteratorHelper(stack.blocks),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var block = _step.value;
          if (LOOP_OPCODES.has(block.opcode)) {
            var summary = this._analyzeLoop(block, script, scriptId);
            block.loopSummary = summary;
            script.loopSummaries.push(summary);
            hotLoops.push(summary);
          }
          this._scanChildStacks(block, script, scriptId, hotLoops);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * @param {import('./intermediate').IntermediateStackBlock} block
     * @param {import('./intermediate').IntermediateScript} script
     * @param {string} scriptId
     * @returns {object}
     * @private
     */
  }, {
    key: "_analyzeLoop",
    value: function _analyzeLoop(block, script, scriptId) {
      var state = {
        totalStackBlocks: 0,
        arithmeticOps: 0,
        branchCount: 0,
        hasNestedLoop: false,
        effectProfile: {
          hasYieldingBlock: false,
          hasCompatibilityLayer: false,
          hasProcedureCall: false,
          hasBroadcast: false,
          hasTimer: false,
          hasRandom: false,
          hasIO: false,
          hasListMutation: false,
          hasVariableMutation: false,
          hasAddonCall: false,
          hasUnsupportedStack: false,
          hasUnsupportedInput: false,
          requiresJSRuntime: false
        },
        numericDomain: {
          pureNumeric: true,
          hasListAccess: false,
          hasMixedTypeRisk: false,
          hasStringRisk: false,
          hasNonFiniteRisk: false,
          hasUnknownRange: false,
          fp32Safe: true
        }
      };
      this._scanLoopHeader(block, state);
      this._scanStackBody(block.inputs["do"], state, false);
      var iterationCount = this._estimateIterationCount(block);
      if (state.numericDomain.hasStringRisk || state.numericDomain.hasMixedTypeRisk) {
        state.numericDomain.pureNumeric = false;
      }
      if (state.numericDomain.hasNonFiniteRisk || state.numericDomain.hasUnknownRange) {
        state.numericDomain.fp32Safe = false;
      }
      if (state.effectProfile.requiresJSRuntime || state.hasNestedLoop) {
        state.numericDomain.pureNumeric = false;
        state.numericDomain.fp32Safe = false;
      }
      var estimatedIterations = Number.isFinite(iterationCount.exact) ? iterationCount.exact : Number.isFinite(iterationCount.maximum) ? iterationCount.maximum : null;
      var hotness = estimatedIterations === null ? null : estimatedIterations * Math.max(state.arithmeticOps, 1);
      var isWarp = script.isWarp && !block.inputs.warpTimer;
      return {
        loopId: "".concat(scriptId, ":loop").concat(this.nextLoopId++),
        opcode: block.opcode,
        scriptId: scriptId,
        isWarp: isWarp,
        usesWarpTimer: Boolean(block.inputs.warpTimer),
        hasNestedLoop: state.hasNestedLoop,
        totalStackBlocks: state.totalStackBlocks,
        arithmeticOps: state.arithmeticOps,
        branchCount: state.branchCount,
        iterationCount: iterationCount,
        estimatedIterations: estimatedIterations,
        hotness: hotness,
        effectProfile: state.effectProfile,
        numericDomain: state.numericDomain
      };
    }

    /**
     * @param {import('./intermediate').IntermediateStackBlock} block
     * @param {object} state
     * @private
     */
  }, {
    key: "_scanLoopHeader",
    value: function _scanLoopHeader(block, state) {
      if (block.opcode === StackOpcode.CONTROL_REPEAT) {
        this._scanInput(block.inputs.times, state);
      } else if (block.opcode === StackOpcode.CONTROL_FOR) {
        this._scanInput(block.inputs.count, state);
      } else if (block.opcode === StackOpcode.CONTROL_WHILE) {
        this._scanInput(block.inputs.condition, state);
      }
    }

    /**
     * @param {import('./intermediate').IntermediateStack|null} stack
     * @param {object} state
     * @param {boolean} nested
     * @private
     */
  }, {
    key: "_scanStackBody",
    value: function _scanStackBody(stack, state, nested) {
      if (!stack) {
        return;
      }
      var _iterator2 = _createForOfIteratorHelper(stack.blocks),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var block = _step2.value;
          this._scanBlock(block, state, nested);
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }

    /**
     * @param {import('./intermediate').IntermediateStackBlock} block
     * @param {object} state
     * @param {boolean} nested
     * @private
     */
  }, {
    key: "_scanBlock",
    value: function _scanBlock(block, state, nested) {
      state.totalStackBlocks++;
      if (block.yields) {
        state.effectProfile.hasYieldingBlock = true;
        state.effectProfile.requiresJSRuntime = true;
      }
      if (LOOP_OPCODES.has(block.opcode)) {
        state.hasNestedLoop = true;
        this._scanLoopHeader(block, state);
        this._scanStackBody(block.inputs["do"], state, true);
        return;
      }
      if (nested) {
        state.hasNestedLoop = true;
      }
      switch (block.opcode) {
        case StackOpcode.NOP:
          break;
        case StackOpcode.VAR_SET:
          state.effectProfile.hasVariableMutation = true;
          this._scanInput(block.inputs.value, state);
          if (!isNumericLikeType(block.inputs.value.type)) {
            state.numericDomain.pureNumeric = false;
            state.numericDomain.fp32Safe = false;
          }
          break;
        case StackOpcode.CONTROL_IF_ELSE:
          state.branchCount++;
          this._scanInput(block.inputs.condition, state);
          this._scanStackBody(block.inputs.whenTrue, state, nested);
          this._scanStackBody(block.inputs.whenFalse, state, nested);
          break;
        case StackOpcode.LIST_ADD:
        case StackOpcode.LIST_INSERT:
        case StackOpcode.LIST_REPLACE:
        case StackOpcode.LIST_DELETE:
        case StackOpcode.LIST_DELETE_ALL:
          state.effectProfile.hasListMutation = true;
          state.numericDomain.hasListAccess = true;
          state.numericDomain.hasMixedTypeRisk = true;
          this._scanInputsObject(block.inputs, state);
          break;
        case StackOpcode.COMPATIBILITY_LAYER:
        case StackOpcode.OLD_COMPILER_COMPATIBILITY_LAYER:
          state.effectProfile.hasCompatibilityLayer = true;
          state.effectProfile.requiresJSRuntime = true;
          this._scanCompatibilityBlock(block, state, nested);
          break;
        case StackOpcode.PROCEDURE_CALL:
          state.effectProfile.hasProcedureCall = true;
          state.effectProfile.requiresJSRuntime = true;
          this._scanInputsObject(block.inputs, state);
          break;
        case StackOpcode.EVENT_BROADCAST:
        case StackOpcode.EVENT_BROADCAST_AND_WAIT:
          state.effectProfile.hasBroadcast = true;
          state.effectProfile.requiresJSRuntime = true;
          this._scanInputsObject(block.inputs, state);
          break;
        case StackOpcode.CONTROL_WAIT:
        case StackOpcode.CONTROL_WAIT_UNTIL:
        case StackOpcode.CONTROL_STOP_ALL:
        case StackOpcode.CONTROL_STOP_OTHERS:
        case StackOpcode.CONTROL_STOP_SCRIPT:
        case StackOpcode.CONTROL_CLONE_CREATE:
        case StackOpcode.CONTROL_CLONE_DELETE:
        case StackOpcode.CONTROL_CLEAR_COUNTER:
        case StackOpcode.CONTORL_INCR_COUNTER:
        case StackOpcode.SENSING_TIMER_RESET:
        case StackOpcode.LIST_SHOW:
        case StackOpcode.LIST_HIDE:
        case StackOpcode.VAR_SHOW:
        case StackOpcode.VAR_HIDE:
          state.effectProfile.requiresJSRuntime = true;
          this._scanInputsObject(block.inputs, state);
          break;
        default:
          if (SAFE_STACK_OPCODES.has(block.opcode)) {
            this._scanInputsObject(block.inputs, state);
          } else if (JS_ONLY_STACK_PREFIXES.some(function (prefix) {
            return block.opcode.startsWith(prefix);
          })) {
            state.effectProfile.requiresJSRuntime = true;
            this._scanInputsObject(block.inputs, state);
          } else {
            state.effectProfile.hasUnsupportedStack = true;
            state.effectProfile.requiresJSRuntime = true;
            this._scanInputsObject(block.inputs, state);
          }
          break;
      }
    }

    /**
     * @param {import('./intermediate').IntermediateStackBlock} block
     * @param {object} state
     * @param {boolean} nested
     * @private
     */
  }, {
    key: "_scanCompatibilityBlock",
    value: function _scanCompatibilityBlock(block, state, nested) {
      this._scanInputsObject(block.inputs, state);
      if (block.inputs.substacks) {
        for (var _i2 = 0, _Object$keys2 = Object.keys(block.inputs.substacks); _i2 < _Object$keys2.length; _i2++) {
          var substackName = _Object$keys2[_i2];
          this._scanStackBody(block.inputs.substacks[substackName], state, nested);
        }
      }
    }

    /**
     * @param {object} inputs
     * @param {object} state
     * @private
     */
  }, {
    key: "_scanInputsObject",
    value: function _scanInputsObject(inputs, state) {
      for (var _i3 = 0, _Object$keys3 = Object.keys(inputs); _i3 < _Object$keys3.length; _i3++) {
        var inputName = _Object$keys3[_i3];
        var input = inputs[inputName];
        if (input && input.inputs && typeof input.opcode === 'string') {
          this._scanInput(input, state);
        } else if (input && _typeof(input) === 'object' && !input.blocks) {
          this._scanInputsObject(input, state);
        }
      }
    }

    /**
     * @param {import('./intermediate').IntermediateInput} input
     * @param {object} state
     * @private
     */
  }, {
    key: "_scanInput",
    value: function _scanInput(input, state) {
      if (input.yields) {
        state.effectProfile.hasYieldingBlock = true;
        state.effectProfile.requiresJSRuntime = true;
      }
      if (ARITHMETIC_INPUT_OPCODES.has(input.opcode)) {
        state.arithmeticOps++;
      }
      if (input.isSometimesType(InputType.STRING)) {
        state.numericDomain.hasStringRisk = true;
      }
      if (input.isSometimesType(InputType.NUMBER_NAN | InputType.NUMBER_INF)) {
        state.numericDomain.hasNonFiniteRisk = true;
      }
      switch (input.opcode) {
        case InputOpcode.CONSTANT:
          if (!isFloat32Constant(input.inputs.value)) {
            state.numericDomain.fp32Safe = false;
          }
          return;
        case InputOpcode.VAR_GET:
        case InputOpcode.PROCEDURE_ARGUMENT:
          state.numericDomain.hasUnknownRange = true;
          return;
        case InputOpcode.OP_RANDOM:
          state.effectProfile.hasRandom = true;
          state.effectProfile.requiresJSRuntime = true;
          state.numericDomain.pureNumeric = false;
          break;
        case InputOpcode.ADDON_CALL:
          state.effectProfile.hasAddonCall = true;
          state.effectProfile.requiresJSRuntime = true;
          state.numericDomain.pureNumeric = false;
          break;
        case InputOpcode.COMPATIBILITY_LAYER:
        case InputOpcode.OLD_COMPILER_COMPATIBILITY_LAYER:
        case InputOpcode.PROCEDURE_CALL:
          state.effectProfile.hasCompatibilityLayer = true;
          state.effectProfile.requiresJSRuntime = true;
          state.numericDomain.pureNumeric = false;
          break;
        default:
          if (TIMER_INPUT_OPCODES.has(input.opcode)) {
            state.effectProfile.hasTimer = true;
            state.effectProfile.requiresJSRuntime = true;
            state.numericDomain.pureNumeric = false;
          } else if (IO_INPUT_OPCODES.has(input.opcode)) {
            state.effectProfile.hasIO = true;
            state.effectProfile.requiresJSRuntime = true;
            state.numericDomain.pureNumeric = false;
          } else if (LIST_INPUT_OPCODES.has(input.opcode)) {
            state.numericDomain.hasListAccess = true;
            if (input.opcode !== InputOpcode.LIST_LENGTH && input.opcode !== InputOpcode.LIST_INDEX_OF) {
              state.numericDomain.hasMixedTypeRisk = true;
            }
          } else if (!PURE_INPUT_OPCODES.has(input.opcode)) {
            state.effectProfile.hasUnsupportedInput = true;
            state.effectProfile.requiresJSRuntime = true;
            state.numericDomain.pureNumeric = false;
          }
          break;
      }
      this._scanInputsObject(input.inputs, state);
    }

    /**
     * @param {import('./intermediate').IntermediateStackBlock} block
     * @returns {{kind: string, exact: number|null, minimum: number|null, maximum: number|null}}
     * @private
     */
  }, {
    key: "_estimateIterationCount",
    value: function _estimateIterationCount(block) {
      if (block.opcode === StackOpcode.CONTROL_REPEAT) {
        var exact = this._readConstantNumber(block.inputs.times);
        if (exact !== null) {
          var count = !Number.isFinite(exact) ? Infinity : Math.max(0, Math.floor(exact + 0.5));
          return {
            kind: 'constant',
            exact: count,
            minimum: count,
            maximum: count
          };
        }
        return {
          kind: 'unknown',
          exact: null,
          minimum: 0,
          maximum: null
        };
      }
      if (block.opcode === StackOpcode.CONTROL_FOR) {
        var _exact = this._readConstantNumber(block.inputs.count);
        if (_exact !== null) {
          var _count = !Number.isFinite(_exact) ? Infinity : Math.max(0, Math.ceil(_exact));
          return {
            kind: 'constant',
            exact: _count,
            minimum: _count,
            maximum: _count
          };
        }
        return {
          kind: 'range',
          exact: null,
          minimum: 0,
          maximum: null
        };
      }
      return {
        kind: 'unknown',
        exact: null,
        minimum: 0,
        maximum: null
      };
    }

    /**
     * @param {import('./intermediate').IntermediateInput} input
     * @returns {number|null}
     * @private
     */
  }, {
    key: "_readConstantNumber",
    value: function _readConstantNumber(input) {
      if (input.opcode !== InputOpcode.CONSTANT) {
        return null;
      }
      var value = input.inputs.value;
      if (typeof value !== 'number') {
        return null;
      }
      return value;
    }

    /**
     * @param {import('./intermediate').IntermediateStackBlock} block
     * @param {import('./intermediate').IntermediateScript} script
     * @param {string} scriptId
     * @param {object[]} hotLoops
     * @private
     */
  }, {
    key: "_scanChildStacks",
    value: function _scanChildStacks(block, script, scriptId, hotLoops) {
      if (block.inputs && block.inputs["do"] && block.inputs["do"].blocks) {
        this._scanStack(block.inputs["do"], script, scriptId, hotLoops);
      }
      if (block.opcode === StackOpcode.CONTROL_IF_ELSE) {
        this._scanStack(block.inputs.whenTrue, script, scriptId, hotLoops);
        this._scanStack(block.inputs.whenFalse, script, scriptId, hotLoops);
      } else if (block.opcode === StackOpcode.COMPATIBILITY_LAYER && block.inputs.substacks) {
        for (var _i4 = 0, _Object$keys4 = Object.keys(block.inputs.substacks); _i4 < _Object$keys4.length; _i4++) {
          var substackName = _Object$keys4[_i4];
          this._scanStack(block.inputs.substacks[substackName], script, scriptId, hotLoops);
        }
      }
    }
  }]);
}();
module.exports = {
  HotLoopAnalyzer: HotLoopAnalyzer
};

/***/ }),

/***/ "../vm/src/compiler/intermediate.js":
/*!******************************************!*\
  !*** ../vm/src/compiler/intermediate.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
// @ts-check

var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var _require = __webpack_require__(/*! ./enums.js */ "../vm/src/compiler/enums.js"),
  InputOpcode = _require.InputOpcode,
  InputType = _require.InputType;
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/**
 * @fileoverview Common intermediates shared amongst parts of the compiler.
 */

/**
 * Describes a 'stackable' block (eg. show)
 */
var IntermediateStackBlock = /*#__PURE__*/_createClass(
/**
 * @param {import("./enums").StackOpcode} opcode
 * @param {Object} inputs
 * @param {boolean} yields
 */
function IntermediateStackBlock(opcode) {
  var inputs = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var yields = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  _classCallCheck(this, IntermediateStackBlock);
  /**
   * The type of the stackable block.
   * @type {import("./enums").StackOpcode}
   */
  this.opcode = opcode;

  /**
   * The inputs of this block.
   * @type {Object}
   */
  this.inputs = inputs;

  /**
   * Does this block cause a yield
   * @type {boolean}
   */
  this.yields = yields;

  /**
   * Should state changes made by this stack block be ignored? Used for testing.
   * @type {boolean}
   */
  this.ignoreState = false;

  /**
   * @type {import("./iroptimizer").TypeState?}
   */
  this.entryState = null;

  /**
   * @type {import("./iroptimizer").TypeState?}
   */
  this.exitState = null;

  /**
   * Summary generated by HotLoopAnalyzer for loop-shaped blocks.
   * @type {object|null}
   */
  this.loopSummary = null;

  /**
   * Backend plan generated by OffloadPlanner for loop-shaped blocks.
   * @type {object|null}
   */
  this.kernelPlan = null;
});
/**
 * Describes an input to a block.
 * This could be a constant, variable or math operation.
 */
var IntermediateInput = /*#__PURE__*/function () {
  /**
   * @param {InputOpcode} opcode
   * @param {InputType} type
   * @param {Object} inputs
   * @param {boolean} yields
   */
  function IntermediateInput(opcode, type) {
    var inputs = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    var yields = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
    _classCallCheck(this, IntermediateInput);
    /**
     * @type {InputOpcode}
     */
    this.opcode = opcode;

    /**
     * @type {InputType}
     */
    this.type = type;

    /**
     * @type {Object}
     */
    this.inputs = inputs;

    /**
     * @type {boolean}
     */
    this.yields = yields;
  }

  /**
   * Is this input a constant whos value equals value.
   * @param {*} value The value
   * @returns {boolean}
   */
  return _createClass(IntermediateInput, [{
    key: "isConstant",
    value: function isConstant(value) {
      if (this.opcode !== InputOpcode.CONSTANT) return false;
      var equal = this.inputs.value === value;
      if (!equal && typeof value === 'number') equal = +this.inputs.value === value;
      return equal;
    }

    /**
     * Is the type of this input guaranteed to always be the type at runtime.
     * @param {InputType} type
     * @returns {boolean}
     */
  }, {
    key: "isAlwaysType",
    value: function isAlwaysType(type) {
      return (this.type & type) === this.type;
    }

    /**
     * Is it possible for this input to be the type at runtime.
     * @param {InputType} type
     * @returns
     */
  }, {
    key: "isSometimesType",
    value: function isSometimesType(type) {
      return (this.type & type) !== 0;
    }

    /**
     * Converts this input to a target type.
     * If this input is a constant the conversion is performed now, at compile time.
     * If the input changes, the conversion is performed at runtime.
     * @param {InputType} targetType
     * @returns {IntermediateInput} An input with the new type.
     */
  }, {
    key: "toType",
    value: function toType(targetType) {
      var castOpcode;
      switch (targetType) {
        case InputType.BOOLEAN:
          castOpcode = InputOpcode.CAST_BOOLEAN;
          break;
        case InputType.NUMBER:
          castOpcode = InputOpcode.CAST_NUMBER;
          break;
        case InputType.NUMBER_INDEX:
          castOpcode = InputOpcode.CAST_NUMBER_INDEX;
          break;
        case InputType.NUMBER_OR_NAN:
          castOpcode = InputOpcode.CAST_NUMBER_OR_NAN;
          break;
        case InputType.STRING:
          castOpcode = InputOpcode.CAST_STRING;
          break;
        case InputType.COLOR:
          castOpcode = InputOpcode.CAST_COLOR;
          break;
        default:
          log.warn("Cannot cast to type: ".concat(targetType), this);
          throw new Error("Cannot cast to type: ".concat(targetType));
      }
      if (this.isAlwaysType(targetType)) return this;
      if (this.opcode === InputOpcode.CONSTANT) {
        // If we are a constant, we can do the cast here at compile time
        switch (castOpcode) {
          case InputOpcode.CAST_BOOLEAN:
            this.inputs.value = Cast.toBoolean(this.inputs.value);
            this.type = InputType.BOOLEAN;
            break;
          case InputOpcode.CAST_NUMBER:
          case InputOpcode.CAST_NUMBER_INDEX:
          case InputOpcode.CAST_NUMBER_OR_NAN:
            {
              if (this.isAlwaysType(InputType.BOOLEAN_INTERPRETABLE)) {
                this.type = InputType.NUMBER;
                this.inputs.value = +Cast.toBoolean(this.inputs.value);
              }
              var numberValue = +this.inputs.value;
              if (numberValue) {
                this.inputs.value = numberValue;
              } else /* numberValue is one of 0, -0, or NaN */if (Object.is(numberValue, -0)) {
                  this.inputs.value = -0;
                } else {
                  this.inputs.value = 0; // Convert NaN to 0
                }
              if (castOpcode === InputOpcode.CAST_NUMBER_INDEX) {
                // Round numberValue to an integer
                this.inputs.value |= 0;
              }
              this.type = IntermediateInput.getNumberInputType(this.inputs.value);
              break;
            }
          case InputOpcode.CAST_STRING:
            this.inputs.value += '';
            this.type = InputType.STRING;
            break;
          case InputOpcode.CAST_COLOR:
            this.inputs.value = Cast.toRgbColorList(this.inputs.value);
            this.type = InputType.COLOR;
            break;
        }
        return this;
      }
      return new IntermediateInput(castOpcode, targetType, {
        target: this
      });
    }
  }], [{
    key: "getNumberInputType",
    value: function getNumberInputType(number) {
      if (typeof number !== 'number') throw new Error('Expected a number.');
      if (number === Infinity) return InputType.NUMBER_POS_INF;
      if (number === -Infinity) return InputType.NUMBER_NEG_INF;
      if (number < 0) return Number.isInteger(number) ? InputType.NUMBER_NEG_INT : InputType.NUMBER_NEG_FRACT;
      if (number > 0) return Number.isInteger(number) ? InputType.NUMBER_POS_INT : InputType.NUMBER_POS_FRACT;
      if (Number.isNaN(number)) return InputType.NUMBER_NAN;
      if (Object.is(number, -0)) return InputType.NUMBER_NEG_ZERO;
      return InputType.NUMBER_ZERO;
    }
  }]);
}();
/**
 * @param {InputType} type
 * @returns {string}
 */
var stringifyType = function stringifyType(type) {
  var formatFlags = [];
  var _loop = function _loop() {
    var testFormat = InputType[enumValue];
    if ((testFormat & type) === testFormat) {
      for (var _i = 0, _formatFlags = formatFlags; _i < _formatFlags.length; _i++) {
        var existingFormat = _formatFlags[_i];
        if ((testFormat & InputType[existingFormat]) === testFormat) {
          continue;
        }
      }
      formatFlags = formatFlags.filter(function (value) {
        return (InputType[value] & testFormat) !== InputType[value];
      });
      formatFlags.push(enumValue);
    }
  };
  for (var enumValue in InputType) {
    _loop();
  }
  var str = null;
  var _iterator = _createForOfIteratorHelper(formatFlags),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var formatFlag = _step.value;
      if (str === null) {
        str = formatFlag;
      } else {
        str = "".concat(str, " | ").concat(formatFlag);
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  if (str === null) {
    return 'INVALID';
  }
  return str;
};

/**
 * A 'stack' of blocks, like the contents of a script or the inside
 * of a C block.
 */
var IntermediateStack = /*#__PURE__*/_createClass(
/**
 * @param {IntermediateStackBlock[]} [blocks]
 */
function IntermediateStack(blocks) {
  _classCallCheck(this, IntermediateStack);
  /** @type {IntermediateStackBlock[]} */
  this.blocks = blocks !== null && blocks !== void 0 ? blocks : [];
});
/**
 * An IntermediateScript describes a single script.
 * Scripts do not necessarily have hats.
 */
var IntermediateScript = /*#__PURE__*/_createClass(function IntermediateScript() {
  _classCallCheck(this, IntermediateScript);
  /**
   * The ID of the top block of this script.
   * @type {string?}
   */
  this.topBlockId = null;

  /**
   * List of nodes that make up this script.
   * @type {IntermediateStack?}
   */
  this.stack = null;

  /**
   * Whether this script is a procedure.
   * @type {boolean}
   */
  this.isProcedure = false;

  /**
   * This procedure's variant, if any.
   * @type {string}
   */
  this.procedureVariant = '';

  /**
   * This procedure's code, if any.
   * @type {string}
   */
  this.procedureCode = '';

  /**
   * List of names of arguments accepted by this function, if it is a procedure.
   * @type {string[]}
   */
  this.arguments = [];

  /**
   * Whether this script should be run in warp mode.
   * @type {boolean}
   */
  this.isWarp = false;

  /**
   * Whether this script can `yield`
   * If false, this script will be compiled as a regular JavaScript function (function)
   * If true, this script will be compiled as a generator function (function*)
   * @type {boolean}
   */
  this.yields = true;

  /**
   * Whether this script should use the "warp timer"
   * @type {boolean}
   */
  this.warpTimer = false;

  /**
   * List of procedure IDs that this script needs.
   * @readonly
   */
  this.dependedProcedures = [];

  /**
   * Cached result of compiling this script.
   * @type {Function|null}
   */
  this.cachedCompileResult = null;

  /**
   * Cached result of analysing this script.
   * @type {import("./iroptimizer").TypeState|null}
   */
  this.cachedAnalysisEndState = null;

  /**
   * Whether the top block of this script is an executable hat.
   * @type {boolean}
   */
  this.executableHat = false;

  /**
   * Loop summaries generated for this script.
   * @type {object[]}
   */
  this.loopSummaries = [];

  /**
   * Backend plans generated for this script.
   * @type {object[]}
   */
  this.offloadPlans = [];
});
/**
 * An IntermediateRepresentation contains scripts.
 */
var IntermediateRepresentation = /*#__PURE__*/function () {
  /**
   *
   * @param {IntermediateScript} entry
   * @param {Object.<string, IntermediateScript>} procedures
   */
  function IntermediateRepresentation(entry, procedures) {
    _classCallCheck(this, IntermediateRepresentation);
    /**
     * The entry point of this IR.
     * @type {IntermediateScript}
     */
    this.entry = entry;

    /**
     * Maps procedure variants to their intermediate script.
     * @type {Object.<string, IntermediateScript>}
     */
    this.procedures = procedures;

    /**
     * Flattened list of loop summaries for every script in this IR.
     * @type {object[]}
     */
    this.hotLoops = [];

    /**
     * Flattened list of backend plans for every script in this IR.
     * @type {object[]}
     */
    this.offloadPlans = [];
  }

  /**
   * Gets the first procedure with the given proccode.
   * @param {string} proccode
   * @returns {IntermediateScript | undefined}
   */
  return _createClass(IntermediateRepresentation, [{
    key: "getProcedure",
    value: function getProcedure(proccode) {
      return Object.values(this.procedures).find(function (procedure) {
        return procedure.procedureCode === proccode;
      });
    }
  }]);
}();
module.exports = {
  IntermediateStackBlock: IntermediateStackBlock,
  IntermediateInput: IntermediateInput,
  IntermediateStack: IntermediateStack,
  IntermediateScript: IntermediateScript,
  IntermediateRepresentation: IntermediateRepresentation,
  stringifyType: stringifyType
};

/***/ }),

/***/ "../vm/src/compiler/irgen.js":
/*!***********************************!*\
  !*** ../vm/src/compiler/irgen.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// @ts-check

var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var BlockType = __webpack_require__(/*! ../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Variable = __webpack_require__(/*! ../engine/variable */ "../vm/src/engine/variable.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var compatBlocks = __webpack_require__(/*! ./compat-blocks */ "../vm/src/compiler/compat-blocks.js");
var _require = __webpack_require__(/*! ./enums.js */ "../vm/src/compiler/enums.js"),
  StackOpcode = _require.StackOpcode,
  InputOpcode = _require.InputOpcode,
  InputType = _require.InputType;
var _require2 = __webpack_require__(/*! ./intermediate */ "../vm/src/compiler/intermediate.js"),
  IntermediateStackBlock = _require2.IntermediateStackBlock,
  IntermediateInput = _require2.IntermediateInput,
  IntermediateStack = _require2.IntermediateStack,
  IntermediateScript = _require2.IntermediateScript,
  IntermediateRepresentation = _require2.IntermediateRepresentation;
var oldCompilerCompatiblity = __webpack_require__(/*! ./old-compiler-compatibility.js */ "../vm/src/compiler/old-compiler-compatibility.js");

/**
 * @fileoverview Generate intermediate representations from Scratch blocks.
 */

/* eslint-disable max-len */

var SCALAR_TYPE = '';
var LIST_TYPE = 'list';

/**
 * @typedef DescendedVariable
 * @property {'target'|'stage'} scope
 * @property {string | null} id
 * @property {string} name
 * @property {boolean} isCloud
 */

/**
 * @param {string} code
 * @param {boolean} warp
 * @returns {string}
 */
var generateProcedureVariant = function generateProcedureVariant(code, warp) {
  if (warp) {
    return "W".concat(code);
  }
  return "Z".concat(code);
};

/**
 * @param {string} variant Variant generated by generateProcedureVariant()
 * @returns {string} original procedure code
 */
var parseProcedureCode = function parseProcedureCode(variant) {
  return variant.substring(1);
};

/**
 * @param {string} variant Variant generated by generateProcedureVariant()
 * @returns {boolean} true if warp enabled
 */
var parseIsWarp = function parseIsWarp(variant) {
  return variant.charAt(0) === 'W';
};
var ScriptTreeGenerator = /*#__PURE__*/function () {
  function ScriptTreeGenerator(thread) {
    _classCallCheck(this, ScriptTreeGenerator);
    /** @private */
    this.thread = thread;
    /** @private */
    this.target = thread.target;
    /** @private */
    this.blocks = thread.blockContainer;
    /** @private */
    this.runtime = this.target.runtime;
    /** @private */
    this.stage = this.runtime.getTargetForStage();

    /**
     * This script's intermediate representation.
     */
    this.script = new IntermediateScript();
    this.script.warpTimer = this.target.runtime.compilerOptions.warpTimer;

    /**
     * Cache of variable ID to variable data object.
     * @type {Object.<string, object>}
     * @private
     */
    this.variableCache = {};
    this.usesTimer = false;
    this.namesOfCostumesAndSounds = new Set();
    var _iterator = _createForOfIteratorHelper(this.runtime.targets),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var target = _step.value;
        if (target.isOriginal) {
          var sprite = target.sprite;
          var _iterator2 = _createForOfIteratorHelper(sprite.costumes),
            _step2;
          try {
            for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
              var costume = _step2.value;
              this.namesOfCostumesAndSounds.add(costume.name);
            }
          } catch (err) {
            _iterator2.e(err);
          } finally {
            _iterator2.f();
          }
          var _iterator3 = _createForOfIteratorHelper(sprite.sounds),
            _step3;
          try {
            for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
              var sound = _step3.value;
              this.namesOfCostumesAndSounds.add(sound.name);
            }
          } catch (err) {
            _iterator3.e(err);
          } finally {
            _iterator3.f();
          }
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    this.oldCompilerStub = oldCompilerCompatiblity.enabled ? new oldCompilerCompatiblity.ScriptTreeGeneratorStub(this) : null;
  }
  return _createClass(ScriptTreeGenerator, [{
    key: "setProcedureVariant",
    value: function setProcedureVariant(procedureVariant) {
      var procedureCode = parseProcedureCode(procedureVariant);
      this.script.procedureVariant = procedureVariant;
      this.script.procedureCode = procedureCode;
      this.script.isProcedure = true;
      this.script.yields = false;
      var paramNamesIdsAndDefaults = this.blocks.getProcedureParamNamesIdsAndDefaults(procedureCode);
      if (paramNamesIdsAndDefaults === null) {
        throw new Error("IR: cannot find procedure: ".concat(procedureVariant));
      }
      var _paramNamesIdsAndDefa = _slicedToArray(paramNamesIdsAndDefaults, 3),
        paramNames = _paramNamesIdsAndDefa[0],
        _paramIds = _paramNamesIdsAndDefa[1],
        _paramDefaults = _paramNamesIdsAndDefa[2];
      this.script.arguments = paramNames;
    }
  }, {
    key: "enableWarp",
    value: function enableWarp() {
      this.script.isWarp = true;
    }
  }, {
    key: "getBlockById",
    value: function getBlockById(blockId) {
      // Flyout blocks are stored in a special container.
      return this.blocks.getBlock(blockId) || this.blocks.runtime.flyoutBlocks.getBlock(blockId);
    }
  }, {
    key: "getBlockInfo",
    value: function getBlockInfo(fullOpcode) {
      var _StringUtil$splitFirs = StringUtil.splitFirst(fullOpcode, '_'),
        _StringUtil$splitFirs2 = _slicedToArray(_StringUtil$splitFirs, 2),
        category = _StringUtil$splitFirs2[0],
        opcode = _StringUtil$splitFirs2[1];
      if (!category || !opcode) {
        return null;
      }
      var categoryInfo = this.runtime._blockInfo.find(function (ci) {
        return ci.id === category;
      });
      if (!categoryInfo) {
        return null;
      }
      var blockInfo = categoryInfo.blocks.find(function (b) {
        return b.info.opcode === opcode;
      });
      if (!blockInfo) {
        return null;
      }
      return blockInfo;
    }
  }, {
    key: "createConstantInput",
    value: function createConstantInput(constant) {
      var preserveStrings = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
      if (constant === null) throw new Error('IR: Constant cannot have a null value.');
      constant += '';
      var numConstant = +constant;
      var preserve = preserveStrings && this.namesOfCostumesAndSounds.has(constant);
      if (!Number.isNaN(numConstant) && (constant.trim() !== '' || constant.includes('\t'))) {
        if (!preserve && numConstant.toString() === constant) {
          return new IntermediateInput(InputOpcode.CONSTANT, IntermediateInput.getNumberInputType(numConstant), {
            value: numConstant
          });
        }
        return new IntermediateInput(InputOpcode.CONSTANT, InputType.STRING_NUM, {
          value: constant
        });
      }
      if (!preserve) {
        if (constant === 'true') {
          return new IntermediateInput(InputOpcode.CONSTANT, InputType.STRING_BOOLEAN, {
            value: constant
          });
        } else if (constant === 'false') {
          return new IntermediateInput(InputOpcode.CONSTANT, InputType.STRING_BOOLEAN, {
            value: constant
          });
        }
      }
      return new IntermediateInput(InputOpcode.CONSTANT, InputType.STRING_NAN, {
        value: constant
      });
    }

    /**
     * Descend into a child input of a block. (eg. the input STRING of "length of ( )")
     * @param {*} parentBlock The parent Scratch block that contains the input.
     * @param {string} inputName The name of the input to descend into.
     * @param {boolean} preserveStrings Should this input keep the names of costumes and sounds at strings.
     * @private
     * @returns {IntermediateInput} Compiled input node for this input.
     */
  }, {
    key: "descendInputOfBlock",
    value: function descendInputOfBlock(parentBlock, inputName) {
      var preserveStrings = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
      var input = parentBlock.inputs[inputName];
      if (!input) {
        log.warn("IR: ".concat(parentBlock.opcode, ": missing input ").concat(inputName), parentBlock);
        return this.createConstantInput(0);
      }
      var inputId = input.block;
      var block = this.getBlockById(inputId);
      if (!block) {
        log.warn("IR: ".concat(parentBlock.opcode, ": could not find input ").concat(inputName, " with ID ").concat(inputId));
        return this.createConstantInput(0);
      }
      var intermediate = this.descendInput(block, preserveStrings);
      this.script.yields = this.script.yields || intermediate.yields;
      return intermediate;
    }

    /**
     * Descend into an input. (eg. "length of ( )")
     * @param {*} block The parent Scratch block input.
     * @param {boolean} preserveStrings Should this input keep the names of costumes and sounds at strings.
     * @private
     * @returns {IntermediateInput} Compiled input node for this input.
     */
  }, {
    key: "descendInput",
    value: function descendInput(block) {
      var preserveStrings = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
      if (this.oldCompilerStub) {
        var oldCompilerResult = this.oldCompilerStub.descendInputFromNewCompiler(block);
        if (oldCompilerResult) {
          return oldCompilerResult;
        }
      }
      switch (block.opcode) {
        case 'colour_picker':
          return this.createConstantInput(block.fields.COLOUR.value, true);
        case 'math_angle':
        case 'math_integer':
        case 'math_number':
        case 'math_positive_number':
        case 'math_whole_number':
          return this.createConstantInput(block.fields.NUM.value, preserveStrings);
        case 'text':
          return this.createConstantInput(block.fields.TEXT.value, preserveStrings);
        case 'argument_reporter_string_number':
          {
            var name = block.fields.VALUE.value;
            // lastIndexOf because multiple parameters with the same name will use the value of the last definition
            var index = this.script.arguments.lastIndexOf(name);
            if (index === -1) {
              // Legacy support
              if (name.toLowerCase() === 'last key pressed') {
                return new IntermediateInput(InputOpcode.TW_KEY_LAST_PRESSED, InputType.STRING);
              }
            }
            if (index === -1) {
              return this.createConstantInput(0);
            }
            return new IntermediateInput(InputOpcode.PROCEDURE_ARGUMENT, InputType.ANY, {
              index: index
            });
          }
        case 'argument_reporter_boolean':
          {
            // see argument_reporter_string_number above
            var _name = block.fields.VALUE.value;
            var _index = this.script.arguments.lastIndexOf(_name);
            if (_index === -1) {
              if (_name.toLowerCase() === 'is compiled?' || _name.toLowerCase() === 'is turbowarp?') {
                return this.createConstantInput(true).toType(InputType.BOOLEAN);
              }
              return this.createConstantInput(0);
            }
            return new IntermediateInput(InputOpcode.PROCEDURE_ARGUMENT, InputType.ANY, {
              index: _index
            });
          }
        case 'data_variable':
          return new IntermediateInput(InputOpcode.VAR_GET, InputType.ANY, {
            variable: this.descendVariable(block, 'VARIABLE', SCALAR_TYPE)
          });
        case 'data_itemoflist':
          return new IntermediateInput(InputOpcode.LIST_GET, InputType.ANY, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE),
            index: this.descendInputOfBlock(block, 'INDEX')
          });
        case 'data_lengthoflist':
          return new IntermediateInput(InputOpcode.LIST_LENGTH, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE)
          });
        case 'data_listcontainsitem':
          return new IntermediateInput(InputOpcode.LIST_CONTAINS, InputType.BOOLEAN, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE),
            item: this.descendInputOfBlock(block, 'ITEM')
          });
        case 'data_itemnumoflist':
          return new IntermediateInput(InputOpcode.LIST_INDEX_OF, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE),
            item: this.descendInputOfBlock(block, 'ITEM')
          });
        case 'data_listcontents':
          return new IntermediateInput(InputOpcode.LIST_CONTENTS, InputType.STRING, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE)
          });
        case 'event_broadcast_menu':
          {
            var broadcastOption = block.fields.BROADCAST_OPTION;
            var broadcastVariable = this.target.lookupBroadcastMsg(broadcastOption.id, broadcastOption.value);
            // TODO: empty string probably isn't the correct fallback
            var broadcastName = broadcastVariable ? broadcastVariable.name : '';
            return this.createConstantInput(broadcastName);
          }
        case 'looks_backdropnumbername':
          if (block.fields.NUMBER_NAME.value === 'number') {
            return new IntermediateInput(InputOpcode.LOOKS_BACKDROP_NUMBER, InputType.NUMBER_POS_INT);
          }
          return new IntermediateInput(InputOpcode.LOOKS_BACKDROP_NAME, InputType.STRING);
        case 'looks_costumenumbername':
          if (block.fields.NUMBER_NAME.value === 'number') {
            return new IntermediateInput(InputOpcode.LOOKS_COSTUME_NUMBER, InputType.NUMBER_POS_INT);
          }
          return new IntermediateInput(InputOpcode.LOOKS_COSTUME_NAME, InputType.STRING);
        case 'looks_size':
          return new IntermediateInput(InputOpcode.LOOKS_SIZE_GET, InputType.NUMBER_POS | InputType.NUMBER_ZERO);
        case 'motion_direction':
          return new IntermediateInput(InputOpcode.MOTION_DIRECTION_GET, InputType.NUMBER_REAL);
        case 'motion_xposition':
          return new IntermediateInput(InputOpcode.MOTION_X_GET, InputType.NUMBER);
        case 'motion_yposition':
          return new IntermediateInput(InputOpcode.MOTION_Y_GET, InputType.NUMBER);
        case 'operator_add':
          return new IntermediateInput(InputOpcode.OP_ADD, InputType.NUMBER_OR_NAN, {
            left: this.descendInputOfBlock(block, 'NUM1').toType(InputType.NUMBER),
            right: this.descendInputOfBlock(block, 'NUM2').toType(InputType.NUMBER)
          });
        case 'operator_and':
          return new IntermediateInput(InputOpcode.OP_AND, InputType.BOOLEAN, {
            left: this.descendInputOfBlock(block, 'OPERAND1').toType(InputType.BOOLEAN),
            right: this.descendInputOfBlock(block, 'OPERAND2').toType(InputType.BOOLEAN)
          });
        case 'operator_contains':
          return new IntermediateInput(InputOpcode.OP_CONTAINS, InputType.BOOLEAN, {
            string: this.descendInputOfBlock(block, 'STRING1').toType(InputType.STRING),
            contains: this.descendInputOfBlock(block, 'STRING2').toType(InputType.STRING)
          });
        case 'operator_divide':
          return new IntermediateInput(InputOpcode.OP_DIVIDE, InputType.NUMBER_OR_NAN, {
            left: this.descendInputOfBlock(block, 'NUM1').toType(InputType.NUMBER),
            right: this.descendInputOfBlock(block, 'NUM2').toType(InputType.NUMBER)
          });
        case 'operator_equals':
          return new IntermediateInput(InputOpcode.OP_EQUALS, InputType.BOOLEAN, {
            left: this.descendInputOfBlock(block, 'OPERAND1'),
            right: this.descendInputOfBlock(block, 'OPERAND2')
          });
        case 'operator_gt':
          return new IntermediateInput(InputOpcode.OP_GREATER, InputType.BOOLEAN, {
            left: this.descendInputOfBlock(block, 'OPERAND1'),
            right: this.descendInputOfBlock(block, 'OPERAND2')
          });
        case 'operator_join':
          return new IntermediateInput(InputOpcode.OP_JOIN, InputType.STRING, {
            left: this.descendInputOfBlock(block, 'STRING1').toType(InputType.STRING),
            right: this.descendInputOfBlock(block, 'STRING2').toType(InputType.STRING)
          });
        case 'operator_length':
          return new IntermediateInput(InputOpcode.OP_LENGTH, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO, {
            string: this.descendInputOfBlock(block, 'STRING').toType(InputType.STRING)
          });
        case 'operator_letter_of':
          return new IntermediateInput(InputOpcode.OP_LETTER_OF, InputType.STRING, {
            letter: this.descendInputOfBlock(block, 'LETTER').toType(InputType.NUMBER_INDEX),
            string: this.descendInputOfBlock(block, 'STRING').toType(InputType.STRING)
          });
        case 'operator_lt':
          return new IntermediateInput(InputOpcode.OP_LESS, InputType.BOOLEAN, {
            left: this.descendInputOfBlock(block, 'OPERAND1'),
            right: this.descendInputOfBlock(block, 'OPERAND2')
          });
        case 'operator_mathop':
          {
            var value = this.descendInputOfBlock(block, 'NUM').toType(InputType.NUMBER);
            var operator = block.fields.OPERATOR.value.toLowerCase();
            switch (operator) {
              case 'abs':
                return new IntermediateInput(InputOpcode.OP_ABS, InputType.NUMBER_POS | InputType.NUMBER_ZERO, {
                  value: value
                });
              case 'floor':
                return new IntermediateInput(InputOpcode.OP_FLOOR, InputType.NUMBER_INT | InputType.NUMBER_INF, {
                  value: value
                });
              case 'ceiling':
                return new IntermediateInput(InputOpcode.OP_CEILING, InputType.NUMBER_INT | InputType.NUMBER_INF, {
                  value: value
                });
              case 'sqrt':
                return new IntermediateInput(InputOpcode.OP_SQRT, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'sin':
                return new IntermediateInput(InputOpcode.OP_SIN, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'cos':
                return new IntermediateInput(InputOpcode.OP_COS, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'tan':
                return new IntermediateInput(InputOpcode.OP_TAN, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'asin':
                return new IntermediateInput(InputOpcode.OP_ASIN, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'acos':
                return new IntermediateInput(InputOpcode.OP_ACOS, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'atan':
                return new IntermediateInput(InputOpcode.OP_ATAN, InputType.NUMBER, {
                  value: value
                });
              case 'ln':
                return new IntermediateInput(InputOpcode.OP_LOG_E, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'log':
                return new IntermediateInput(InputOpcode.OP_LOG_10, InputType.NUMBER_OR_NAN, {
                  value: value
                });
              case 'e ^':
                return new IntermediateInput(InputOpcode.OP_POW_E, InputType.NUMBER, {
                  value: value
                });
              case '10 ^':
                return new IntermediateInput(InputOpcode.OP_POW_10, InputType.NUMBER, {
                  value: value
                });
              default:
                return this.createConstantInput(0);
            }
          }
        case 'operator_mod':
          return new IntermediateInput(InputOpcode.OP_MOD, InputType.NUMBER_OR_NAN, {
            left: this.descendInputOfBlock(block, 'NUM1').toType(InputType.NUMBER),
            right: this.descendInputOfBlock(block, 'NUM2').toType(InputType.NUMBER)
          });
        case 'operator_multiply':
          return new IntermediateInput(InputOpcode.OP_MULTIPLY, InputType.NUMBER_OR_NAN, {
            left: this.descendInputOfBlock(block, 'NUM1').toType(InputType.NUMBER),
            right: this.descendInputOfBlock(block, 'NUM2').toType(InputType.NUMBER)
          });
        case 'operator_not':
          return new IntermediateInput(InputOpcode.OP_NOT, InputType.BOOLEAN, {
            operand: this.descendInputOfBlock(block, 'OPERAND').toType(InputType.BOOLEAN)
          });
        case 'operator_or':
          return new IntermediateInput(InputOpcode.OP_OR, InputType.BOOLEAN, {
            left: this.descendInputOfBlock(block, 'OPERAND1').toType(InputType.BOOLEAN),
            right: this.descendInputOfBlock(block, 'OPERAND2').toType(InputType.BOOLEAN)
          });
        case 'operator_random':
          {
            var from = this.descendInputOfBlock(block, 'FROM');
            var to = this.descendInputOfBlock(block, 'TO');
            // If both values are known at compile time, we can do some optimizations.
            // TODO: move optimizations to jsgen?
            if (from.opcode === InputOpcode.CONSTANT && to.opcode === InputOpcode.CONSTANT) {
              var sFrom = from.inputs.value;
              var sTo = to.inputs.value;
              var nFrom = Cast.toNumber(sFrom);
              var nTo = Cast.toNumber(sTo);
              // If both numbers are the same, random is unnecessary.
              // todo: this probably never happens so consider removing
              if (nFrom === nTo) {
                return this.createConstantInput(nFrom);
              }
              // If both are ints, hint this to the compiler
              if (Cast.isInt(sFrom) && Cast.isInt(sTo)) {
                // Both inputs are ints, so we know neither are NaN
                return new IntermediateInput(InputOpcode.OP_RANDOM, InputType.NUMBER, {
                  low: (nFrom <= nTo ? from : to).toType(InputType.NUMBER),
                  high: (nFrom <= nTo ? to : from).toType(InputType.NUMBER),
                  useInts: true,
                  useFloats: false
                });
              }
              // Otherwise hint that these are floats
              return new IntermediateInput(InputOpcode.OP_RANDOM, InputType.NUMBER_OR_NAN, {
                low: (nFrom <= nTo ? from : to).toType(InputType.NUMBER),
                high: (nFrom <= nTo ? to : from).toType(InputType.NUMBER),
                useInts: false,
                useFloats: true
              });
            } else if (from.opcode === InputOpcode.CONSTANT) {
              // If only one value is known at compile-time, we can still attempt some optimizations.
              if (!Cast.isInt(Cast.toNumber(from.inputs.value))) {
                return new IntermediateInput(InputOpcode.OP_RANDOM, InputType.NUMBER_OR_NAN, {
                  low: from.toType(InputType.NUMBER),
                  high: to.toType(InputType.NUMBER),
                  useInts: false,
                  useFloats: true
                });
              }
            } else if (to.opcode === InputOpcode.CONSTANT) {
              if (!Cast.isInt(Cast.toNumber(from.inputs.value))) {
                return new IntermediateInput(InputOpcode.OP_RANDOM, InputType.NUMBER_OR_NAN, {
                  low: from.toType(InputType.NUMBER),
                  high: to.toType(InputType.NUMBER),
                  useInts: false,
                  useFloats: true
                });
              }
            }
            // No optimizations possible
            return new IntermediateInput(InputOpcode.OP_RANDOM, InputType.NUMBER_OR_NAN, {
              low: from,
              high: to,
              useInts: false,
              useFloats: false
            });
          }
        case 'operator_round':
          return new IntermediateInput(InputOpcode.OP_ROUND, InputType.NUMBER_INT | InputType.NUMBER_INF, {
            value: this.descendInputOfBlock(block, 'NUM').toType(InputType.NUMBER)
          });
        case 'operator_subtract':
          return new IntermediateInput(InputOpcode.OP_SUBTRACT, InputType.NUMBER_OR_NAN, {
            left: this.descendInputOfBlock(block, 'NUM1').toType(InputType.NUMBER),
            right: this.descendInputOfBlock(block, 'NUM2').toType(InputType.NUMBER)
          });
        case 'procedures_call':
          {
            var procedureInfo = this.getProcedureInfo(block);
            return new IntermediateInput(procedureInfo.opcode, InputType.ANY, procedureInfo.inputs, procedureInfo.yields);
          }
        case 'sensing_answer':
          return new IntermediateInput(InputOpcode.SENSING_ANSWER, InputType.STRING);
        case 'sensing_coloristouchingcolor':
          return new IntermediateInput(InputOpcode.SENSING_COLOR_TOUCHING_COLOR, InputType.BOOLEAN, {
            target: this.descendInputOfBlock(block, 'COLOR2').toType(InputType.COLOR),
            mask: this.descendInputOfBlock(block, 'COLOR').toType(InputType.COLOR)
          });
        case 'sensing_current':
          switch (block.fields.CURRENTMENU.value.toLowerCase()) {
            case 'year':
              return new IntermediateInput(InputOpcode.SENSING_TIME_YEAR, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO);
            case 'month':
              return new IntermediateInput(InputOpcode.SENSING_TIME_MONTH, InputType.NUMBER_POS_INT);
            case 'date':
              return new IntermediateInput(InputOpcode.SENSING_TIME_DATE, InputType.NUMBER_POS_INT);
            case 'dayofweek':
              return new IntermediateInput(InputOpcode.SENSING_TIME_WEEKDAY, InputType.NUMBER_POS_INT);
            case 'hour':
              return new IntermediateInput(InputOpcode.SENSING_TIME_HOUR, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO);
            case 'minute':
              return new IntermediateInput(InputOpcode.SENSING_TIME_MINUTE, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO);
            case 'second':
              return new IntermediateInput(InputOpcode.SENSING_TIME_SECOND, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO);
            default:
              return this.createConstantInput(0);
          }
        case 'sensing_dayssince2000':
          return new IntermediateInput(InputOpcode.SENSING_TIME_DAYS_SINCE_2000, InputType.NUMBER);
        case 'sensing_distanceto':
          return new IntermediateInput(InputOpcode.SENSING_DISTANCE, InputType.NUMBER_POS | InputType.NUMBER_ZERO, {
            target: this.descendInputOfBlock(block, 'DISTANCETOMENU').toType(InputType.STRING)
          });
        case 'sensing_keypressed':
          return new IntermediateInput(InputOpcode.SENSING_KEY_DOWN, InputType.BOOLEAN, {
            key: this.descendInputOfBlock(block, 'KEY_OPTION', true)
          });
        case 'sensing_mousedown':
          return new IntermediateInput(InputOpcode.SENSING_MOUSE_DOWN, InputType.BOOLEAN);
        case 'sensing_mousex':
          return new IntermediateInput(InputOpcode.SENSING_MOUSE_X, InputType.NUMBER);
        case 'sensing_mousey':
          return new IntermediateInput(InputOpcode.SENSING_MOUSE_Y, InputType.NUMBER);
        case 'sensing_of':
          {
            var property = block.fields.PROPERTY.value;
            var object = this.descendInputOfBlock(block, 'OBJECT').toType(InputType.STRING);
            if (object.opcode !== InputOpcode.CONSTANT) {
              return new IntermediateInput(InputOpcode.SENSING_OF, InputType.ANY, {
                object: object,
                property: property
              });
            }
            if (property === 'volume') {
              return new IntermediateInput(InputOpcode.SENSING_OF_VOLUME, InputType.NUMBER_POS_REAL | InputType.NUMBER_ZERO, {
                object: object,
                property: property
              });
            }
            if (object.isConstant('_stage_')) {
              // We assume that the stage always exists, so these don't need to be able to return 0.
              switch (property) {
                case 'background #': // fallthrough for scratch 1.0 compatibility
                case 'backdrop #':
                  return new IntermediateInput(InputOpcode.SENSING_OF_BACKDROP_NUMBER, InputType.NUMBER_POS_INT);
                case 'backdrop name':
                  return new IntermediateInput(InputOpcode.SENSING_OF_BACKDROP_NAME, InputType.STRING);
              }
            } else {
              // If the target sprite does not exist, these may all return 0, even the costume name one.
              switch (property) {
                case 'x position':
                  return new IntermediateInput(InputOpcode.SENSING_OF_POS_X, InputType.NUMBER, {
                    object: object
                  });
                case 'y position':
                  return new IntermediateInput(InputOpcode.SENSING_OF_POS_Y, InputType.NUMBER, {
                    object: object
                  });
                case 'direction':
                  return new IntermediateInput(InputOpcode.SENSING_OF_DIRECTION, InputType.NUMBER_REAL, {
                    object: object
                  });
                case 'costume #':
                  return new IntermediateInput(InputOpcode.SENSING_OF_COSTUME_NUMBER, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO, {
                    object: object
                  });
                case 'costume name':
                  return new IntermediateInput(InputOpcode.SENSING_OF_COSTUME_NAME, InputType.STRING | InputType.NUMBER_ZERO, {
                    object: object
                  });
                case 'size':
                  return new IntermediateInput(InputOpcode.SENSING_OF_SIZE, InputType.NUMBER_POS | InputType.NUMBER_ZERO, {
                    object: object
                  });
              }
            }
            return new IntermediateInput(InputOpcode.SENSING_OF_VAR, InputType.ANY, {
              object: object,
              property: property
            });
          }
        case 'sensing_timer':
          this.usesTimer = true;
          return new IntermediateInput(InputOpcode.SENSING_TIMER_GET, InputType.NUMBER_POS_REAL | InputType.NUMBER_ZERO);
        case 'sensing_touchingcolor':
          return new IntermediateInput(InputOpcode.SENSING_TOUCHING_COLOR, InputType.BOOLEAN, {
            color: this.descendInputOfBlock(block, 'COLOR').toType(InputType.COLOR)
          });
        case 'sensing_touchingobject':
          return new IntermediateInput(InputOpcode.SENSING_TOUCHING_OBJECT, InputType.BOOLEAN, {
            object: this.descendInputOfBlock(block, 'TOUCHINGOBJECTMENU')
          });
        case 'sensing_username':
          return new IntermediateInput(InputOpcode.SENSING_USERNAME, InputType.STRING);
        case 'sound_sounds_menu':
          // This menu is special compared to other menus -- it actually has an opcode function.
          return this.createConstantInput(block.fields.SOUND_MENU.value, true);
        case 'control_get_counter':
          return new IntermediateInput(InputOpcode.CONTROL_COUNTER, InputType.NUMBER_POS_INT | InputType.NUMBER_ZERO);
        case 'tw_getLastKeyPressed':
          return new IntermediateInput(InputOpcode.TW_KEY_LAST_PRESSED, InputType.STRING);
        default:
          {
            var opcodeFunction = this.runtime.getOpcodeFunction(block.opcode);
            if (opcodeFunction) {
              // It might be a non-compiled primitive from a standard category
              if (compatBlocks.inputs.includes(block.opcode)) {
                return this.descendCompatLayerInput(block);
              }
              // It might be an extension block.
              var blockInfo = this.getBlockInfo(block.opcode);
              if (blockInfo) {
                var type = blockInfo.info.blockType;
                if (type === BlockType.REPORTER || type === BlockType.BOOLEAN) {
                  return this.descendCompatLayerInput(block);
                }
              }
            }

            // It might be a menu.
            var inputs = Object.keys(block.inputs);
            var fields = Object.keys(block.fields);
            if (inputs.length === 0 && fields.length === 1) {
              return this.createConstantInput(block.fields[fields[0]].value, preserveStrings);
            }
            log.warn("IR: Unknown input: ".concat(block.opcode), block);
            throw new Error("IR: Unknown input: ".concat(block.opcode));
          }
      }
    }

    /**
     * Descend into a stacked block. (eg. "move ( ) steps")
     * @param {*} block The Scratch block to parse.
     * @private
     * @returns {IntermediateStackBlock} Compiled node for this block.
     */
  }, {
    key: "descendStackedBlock",
    value: function descendStackedBlock(block) {
      if (this.oldCompilerStub) {
        var oldCompilerResult = this.oldCompilerStub.descendStackedBlockFromNewCompiler(block);
        if (oldCompilerResult) {
          return oldCompilerResult;
        }
      }
      switch (block.opcode) {
        case 'control_all_at_once':
          // In Scratch 3, this block behaves like "if 1 = 1"
          return new IntermediateStackBlock(StackOpcode.CONTROL_IF_ELSE, {
            condition: this.createConstantInput(true).toType(InputType.BOOLEAN),
            whenTrue: this.descendSubstack(block, 'SUBSTACK'),
            whenFalse: new IntermediateStack()
          });
        case 'control_create_clone_of':
          return new IntermediateStackBlock(StackOpcode.CONTROL_CLONE_CREATE, {
            target: this.descendInputOfBlock(block, 'CLONE_OPTION').toType(InputType.STRING)
          });
        case 'control_delete_this_clone':
          return new IntermediateStackBlock(StackOpcode.CONTROL_CLONE_DELETE, {}, true);
        case 'control_forever':
          return new IntermediateStackBlock(StackOpcode.CONTROL_WHILE, {
            condition: this.createConstantInput(true).toType(InputType.BOOLEAN),
            "do": this.descendSubstack(block, 'SUBSTACK')
          }, this.analyzeLoop());
        case 'control_for_each':
          return new IntermediateStackBlock(StackOpcode.CONTROL_FOR, {
            variable: this.descendVariable(block, 'VARIABLE', SCALAR_TYPE),
            count: this.descendInputOfBlock(block, 'VALUE').toType(InputType.NUMBER),
            "do": this.descendSubstack(block, 'SUBSTACK')
          }, this.analyzeLoop());
        case 'control_if':
          return new IntermediateStackBlock(StackOpcode.CONTROL_IF_ELSE, {
            condition: this.descendInputOfBlock(block, 'CONDITION').toType(InputType.BOOLEAN),
            whenTrue: this.descendSubstack(block, 'SUBSTACK'),
            whenFalse: new IntermediateStack()
          });
        case 'control_if_else':
          return new IntermediateStackBlock(StackOpcode.CONTROL_IF_ELSE, {
            condition: this.descendInputOfBlock(block, 'CONDITION').toType(InputType.BOOLEAN),
            whenTrue: this.descendSubstack(block, 'SUBSTACK'),
            whenFalse: this.descendSubstack(block, 'SUBSTACK2')
          });
        case 'control_repeat':
          return new IntermediateStackBlock(StackOpcode.CONTROL_REPEAT, {
            times: this.descendInputOfBlock(block, 'TIMES').toType(InputType.NUMBER),
            "do": this.descendSubstack(block, 'SUBSTACK')
          }, this.analyzeLoop());
        case 'control_repeat_until':
          {
            // Dirty hack: automatically enable warp timer for this block if it uses timer
            // This fixes project that do things like "repeat until timer > 0.5"
            this.usesTimer = false;
            var condition = this.descendInputOfBlock(block, 'CONDITION').toType(InputType.BOOLEAN);
            var needsWarpTimer = this.usesTimer;
            return new IntermediateStackBlock(StackOpcode.CONTROL_WHILE, {
              condition: new IntermediateInput(InputOpcode.OP_NOT, InputType.BOOLEAN, {
                operand: condition
              }),
              "do": this.descendSubstack(block, 'SUBSTACK'),
              warpTimer: needsWarpTimer
            }, this.analyzeLoop() || needsWarpTimer);
          }
        case 'control_stop':
          {
            var level = block.fields.STOP_OPTION.value;
            if (level === 'all') {
              return new IntermediateStackBlock(StackOpcode.CONTROL_STOP_ALL, {}, true);
            } else if (level === 'other scripts in sprite' || level === 'other scripts in stage') {
              return new IntermediateStackBlock(StackOpcode.CONTROL_STOP_OTHERS);
            } else if (level === 'this script') {
              return new IntermediateStackBlock(StackOpcode.CONTROL_STOP_SCRIPT);
            }
            return new IntermediateStackBlock(StackOpcode.NOP);
          }
        case 'control_wait':
          return new IntermediateStackBlock(StackOpcode.CONTROL_WAIT, {
            seconds: this.descendInputOfBlock(block, 'DURATION').toType(InputType.NUMBER)
          }, true);
        case 'control_wait_until':
          return new IntermediateStackBlock(StackOpcode.CONTROL_WAIT_UNTIL, {
            condition: this.descendInputOfBlock(block, 'CONDITION').toType(InputType.BOOLEAN)
          }, true);
        case 'control_while':
          return new IntermediateStackBlock(StackOpcode.CONTROL_WHILE, {
            condition: this.descendInputOfBlock(block, 'CONDITION').toType(InputType.BOOLEAN),
            "do": this.descendSubstack(block, 'SUBSTACK'),
            // We should consider analyzing this like we do for control_repeat_until
            warpTimer: false
          }, this.analyzeLoop());
        case 'control_clear_counter':
          return new IntermediateStackBlock(StackOpcode.CONTROL_CLEAR_COUNTER);
        case 'control_incr_counter':
          return new IntermediateStackBlock(StackOpcode.CONTORL_INCR_COUNTER);
        case 'data_addtolist':
          return new IntermediateStackBlock(StackOpcode.LIST_ADD, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE),
            item: this.descendInputOfBlock(block, 'ITEM', true)
          });
        case 'data_changevariableby':
          {
            var variable = this.descendVariable(block, 'VARIABLE', SCALAR_TYPE);
            return new IntermediateStackBlock(StackOpcode.VAR_SET, {
              variable: variable,
              value: new IntermediateInput(InputOpcode.OP_ADD, InputType.NUMBER_OR_NAN, {
                left: new IntermediateInput(InputOpcode.VAR_GET, InputType.ANY, {
                  variable: variable
                }).toType(InputType.NUMBER),
                right: this.descendInputOfBlock(block, 'VALUE').toType(InputType.NUMBER)
              })
            });
          }
        case 'data_deletealloflist':
          return new IntermediateStackBlock(StackOpcode.LIST_DELETE_ALL, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE)
          });
        case 'data_deleteoflist':
          {
            var index = this.descendInputOfBlock(block, 'INDEX');
            if (index.isConstant('all')) {
              return new IntermediateStackBlock(StackOpcode.LIST_DELETE_ALL, {
                list: this.descendVariable(block, 'LIST', LIST_TYPE)
              });
            }
            return new IntermediateStackBlock(StackOpcode.LIST_DELETE, {
              list: this.descendVariable(block, 'LIST', LIST_TYPE),
              index: index
            });
          }
        case 'data_hidelist':
          return new IntermediateStackBlock(StackOpcode.LIST_HIDE, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE)
          });
        case 'data_hidevariable':
          return new IntermediateStackBlock(StackOpcode.VAR_HIDE, {
            variable: this.descendVariable(block, 'VARIABLE', SCALAR_TYPE)
          });
        case 'data_insertatlist':
          return new IntermediateStackBlock(StackOpcode.LIST_INSERT, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE),
            index: this.descendInputOfBlock(block, 'INDEX'),
            item: this.descendInputOfBlock(block, 'ITEM', true)
          });
        case 'data_replaceitemoflist':
          return new IntermediateStackBlock(StackOpcode.LIST_REPLACE, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE),
            index: this.descendInputOfBlock(block, 'INDEX'),
            item: this.descendInputOfBlock(block, 'ITEM', true)
          });
        case 'data_setvariableto':
          return new IntermediateStackBlock(StackOpcode.VAR_SET, {
            variable: this.descendVariable(block, 'VARIABLE', SCALAR_TYPE),
            value: this.descendInputOfBlock(block, 'VALUE', true)
          });
        case 'data_showlist':
          return new IntermediateStackBlock(StackOpcode.LIST_SHOW, {
            list: this.descendVariable(block, 'LIST', LIST_TYPE)
          });
        case 'data_showvariable':
          return new IntermediateStackBlock(StackOpcode.VAR_SHOW, {
            variable: this.descendVariable(block, 'VARIABLE', SCALAR_TYPE)
          });
        case 'event_broadcast':
          return new IntermediateStackBlock(StackOpcode.EVENT_BROADCAST, {
            broadcast: this.descendInputOfBlock(block, 'BROADCAST_INPUT').toType(InputType.STRING)
          });
        case 'event_broadcastandwait':
          return new IntermediateStackBlock(StackOpcode.EVENT_BROADCAST_AND_WAIT, {
            broadcast: this.descendInputOfBlock(block, 'BROADCAST_INPUT').toType(InputType.STRING)
          }, true);
        case 'looks_changeeffectby':
          return new IntermediateStackBlock(StackOpcode.LOOKS_EFFECT_CHANGE, {
            effect: block.fields.EFFECT.value.toLowerCase(),
            value: this.descendInputOfBlock(block, 'CHANGE').toType(InputType.NUMBER)
          });
        case 'looks_changesizeby':
          return new IntermediateStackBlock(StackOpcode.LOOKS_SIZE_CHANGE, {
            size: this.descendInputOfBlock(block, 'CHANGE').toType(InputType.NUMBER)
          });
        case 'looks_cleargraphiceffects':
          return new IntermediateStackBlock(StackOpcode.LOOKS_EFFECT_CLEAR);
        case 'looks_goforwardbackwardlayers':
          if (block.fields.FORWARD_BACKWARD.value === 'forward') {
            return new IntermediateStackBlock(StackOpcode.LOOKS_LAYER_FORWARD, {
              layers: this.descendInputOfBlock(block, 'NUM').toType(InputType.NUMBER)
            });
          }
          return new IntermediateStackBlock(StackOpcode.LOOKS_LAYER_BACKWARD, {
            layers: this.descendInputOfBlock(block, 'NUM').toType(InputType.NUMBER)
          });
        case 'looks_gotofrontback':
          if (block.fields.FRONT_BACK.value === 'front') {
            return new IntermediateStackBlock(StackOpcode.LOOKS_LAYER_FRONT);
          }
          return new IntermediateStackBlock(StackOpcode.LOOKS_LAYER_BACK);
        case 'looks_hide':
          return new IntermediateStackBlock(StackOpcode.LOOKS_HIDE);
        case 'looks_nextbackdrop':
          return new IntermediateStackBlock(StackOpcode.LOOKS_BACKDROP_NEXT);
        case 'looks_nextcostume':
          return new IntermediateStackBlock(StackOpcode.LOOKS_COSTUME_NEXT);
        case 'looks_say':
          return new IntermediateStackBlock(StackOpcode.LOOKS_SAY, {
            message: this.descendInputOfBlock(block, 'MESSAGE')
          });
        case 'looks_seteffectto':
          return new IntermediateStackBlock(StackOpcode.LOOKS_EFFECT_SET, {
            effect: block.fields.EFFECT.value.toLowerCase(),
            value: this.descendInputOfBlock(block, 'VALUE').toType(InputType.NUMBER)
          });
        case 'looks_setsizeto':
          return new IntermediateStackBlock(StackOpcode.LOOKS_SIZE_SET, {
            size: this.descendInputOfBlock(block, 'SIZE').toType(InputType.NUMBER)
          });
        case 'looks_show':
          return new IntermediateStackBlock(StackOpcode.LOOKS_SHOW);
        case 'looks_switchbackdropto':
          return new IntermediateStackBlock(StackOpcode.LOOKS_BACKDROP_SET, {
            backdrop: this.descendInputOfBlock(block, 'BACKDROP', true)
          });
        case 'looks_switchcostumeto':
          return new IntermediateStackBlock(StackOpcode.LOOKS_COSTUME_SET, {
            costume: this.descendInputOfBlock(block, 'COSTUME', true)
          });
        case 'looks_think':
          return new IntermediateStackBlock(StackOpcode.LOOKS_THINK, {
            message: this.descendInputOfBlock(block, 'MESSAGE')
          });
        case 'motion_changexby':
          return new IntermediateStackBlock(StackOpcode.MOTION_X_CHANGE, {
            dx: this.descendInputOfBlock(block, 'DX').toType(InputType.NUMBER)
          });
        case 'motion_changeyby':
          return new IntermediateStackBlock(StackOpcode.MOTION_Y_CHANGE, {
            dy: this.descendInputOfBlock(block, 'DY').toType(InputType.NUMBER)
          });
        case 'motion_gotoxy':
          return new IntermediateStackBlock(StackOpcode.MOTION_XY_SET, {
            x: this.descendInputOfBlock(block, 'X').toType(InputType.NUMBER),
            y: this.descendInputOfBlock(block, 'Y').toType(InputType.NUMBER)
          });
        case 'motion_ifonedgebounce':
          return new IntermediateStackBlock(StackOpcode.MOTION_IF_ON_EDGE_BOUNCE);
        case 'motion_movesteps':
          return new IntermediateStackBlock(StackOpcode.MOTION_STEP, {
            steps: this.descendInputOfBlock(block, 'STEPS').toType(InputType.NUMBER)
          });
        case 'motion_pointindirection':
          return new IntermediateStackBlock(StackOpcode.MOTION_DIRECTION_SET, {
            direction: this.descendInputOfBlock(block, 'DIRECTION').toType(InputType.NUMBER)
          });
        case 'motion_setrotationstyle':
          return new IntermediateStackBlock(StackOpcode.MOTION_ROTATION_STYLE_SET, {
            style: block.fields.STYLE.value
          });
        case 'motion_setx':
          return new IntermediateStackBlock(StackOpcode.MOTION_X_SET, {
            x: this.descendInputOfBlock(block, 'X').toType(InputType.NUMBER)
          });
        case 'motion_sety':
          return new IntermediateStackBlock(StackOpcode.MOTION_Y_SET, {
            y: this.descendInputOfBlock(block, 'Y').toType(InputType.NUMBER)
          });
        case 'motion_turnleft':
          return new IntermediateStackBlock(StackOpcode.MOTION_DIRECTION_SET, {
            direction: new IntermediateInput(InputOpcode.OP_SUBTRACT, InputType.NUMBER, {
              left: new IntermediateInput(InputOpcode.MOTION_DIRECTION_GET, InputType.NUMBER),
              right: this.descendInputOfBlock(block, 'DEGREES').toType(InputType.NUMBER)
            })
          });
        case 'motion_turnright':
          return new IntermediateStackBlock(StackOpcode.MOTION_DIRECTION_SET, {
            direction: new IntermediateInput(InputOpcode.OP_ADD, InputType.NUMBER, {
              left: new IntermediateInput(InputOpcode.MOTION_DIRECTION_GET, InputType.NUMBER),
              right: this.descendInputOfBlock(block, 'DEGREES').toType(InputType.NUMBER)
            })
          });
        case 'pen_clear':
          return new IntermediateStackBlock(StackOpcode.PEN_CLEAR);
        case 'pen_changePenColorParamBy':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_PARAM_CHANGE, {
            param: this.descendInputOfBlock(block, 'COLOR_PARAM').toType(InputType.STRING),
            value: this.descendInputOfBlock(block, 'VALUE').toType(InputType.NUMBER)
          });
        case 'pen_changePenHueBy':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_HUE_CHANGE_LEGACY, {
            hue: this.descendInputOfBlock(block, 'HUE').toType(InputType.NUMBER)
          });
        case 'pen_changePenShadeBy':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_SHADE_CHANGE_LEGACY, {
            shade: this.descendInputOfBlock(block, 'SHADE').toType(InputType.NUMBER)
          });
        case 'pen_penDown':
          return new IntermediateStackBlock(StackOpcode.PEN_DOWN);
        case 'pen_penUp':
          return new IntermediateStackBlock(StackOpcode.PEN_UP);
        case 'pen_setPenColorParamTo':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_PARAM_SET, {
            param: this.descendInputOfBlock(block, 'COLOR_PARAM').toType(InputType.STRING),
            value: this.descendInputOfBlock(block, 'VALUE').toType(InputType.NUMBER)
          });
        case 'pen_setPenColorToColor':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_SET, {
            color: this.descendInputOfBlock(block, 'COLOR')
          });
        case 'pen_setPenHueToNumber':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_HUE_SET_LEGACY, {
            hue: this.descendInputOfBlock(block, 'HUE').toType(InputType.NUMBER)
          });
        case 'pen_setPenShadeToNumber':
          return new IntermediateStackBlock(StackOpcode.PEN_COLOR_SHADE_SET_LEGACY, {
            shade: this.descendInputOfBlock(block, 'SHADE').toType(InputType.NUMBER)
          });
        case 'pen_setPenSizeTo':
          return new IntermediateStackBlock(StackOpcode.PEN_SIZE_SET, {
            size: this.descendInputOfBlock(block, 'SIZE').toType(InputType.NUMBER)
          });
        case 'pen_changePenSizeBy':
          return new IntermediateStackBlock(StackOpcode.PEN_SIZE_CHANGE, {
            size: this.descendInputOfBlock(block, 'SIZE').toType(InputType.NUMBER)
          });
        case 'pen_stamp':
          return new IntermediateStackBlock(StackOpcode.PEN_STAMP);
        case 'procedures_call':
          {
            var procedureCode = block.mutation.proccode;
            if (block.mutation["return"]) {
              var visualReport = this.descendVisualReport(block);
              if (visualReport) {
                return visualReport;
              }
            }
            if (procedureCode === 'tw:debugger;') {
              return new IntermediateStackBlock(StackOpcode.DEBUGGER);
            }
            var procedure = this.getProcedureInfo(block);
            return new IntermediateStackBlock(procedure.opcode, procedure.inputs, procedure.yields);
          }
        case 'procedures_return':
          return new IntermediateStackBlock(StackOpcode.PROCEDURE_RETURN, {
            value: this.descendInputOfBlock(block, 'VALUE')
          });
        case 'sensing_resettimer':
          return new IntermediateStackBlock(StackOpcode.SENSING_TIMER_RESET);
        default:
          {
            var opcodeFunction = this.runtime.getOpcodeFunction(block.opcode);
            if (opcodeFunction) {
              // It might be a non-compiled primitive from a standard category
              if (compatBlocks.stacked.includes(block.opcode)) {
                return this.descendCompatLayerStack(block);
              }
              // It might be an extension block.
              var blockInfo = this.getBlockInfo(block.opcode);
              if (blockInfo) {
                var type = blockInfo.info.blockType;
                if (type === BlockType.COMMAND || type === BlockType.CONDITIONAL || type === BlockType.LOOP) {
                  return this.descendCompatLayerStack(block);
                }
              }
            }
            var asVisualReport = this.descendVisualReport(block);
            if (asVisualReport) {
              return asVisualReport;
            }
            log.warn("IR: Unknown stacked block: ".concat(block.opcode), block);
            throw new Error("IR: Unknown stacked block: ".concat(block.opcode));
          }
      }
    }

    /**
     * Descend into a stack of blocks (eg. the blocks contained within an "if" block)
     * @param {*} parentBlock The parent Scratch block that contains the stack to parse.
     * @param {string} substackName The name of the stack to descend into.
     * @private
     * @returns {IntermediateStack} Stacked blocks.
     */
  }, {
    key: "descendSubstack",
    value: function descendSubstack(parentBlock, substackName) {
      var input = parentBlock.inputs[substackName];
      if (!input) {
        return new IntermediateStack();
      }
      var stackId = input.block;
      return this.walkStack(stackId);
    }

    /**
     * Descend into and walk the siblings of a stack.
     * @param {string} startingBlockId The ID of the first block of a stack.
     * @private
     * @returns {IntermediateStack} List of stacked block nodes.
     */
  }, {
    key: "walkStack",
    value: function walkStack(startingBlockId) {
      var result = new IntermediateStack();
      var blockId = startingBlockId;
      while (blockId !== null) {
        var block = this.getBlockById(blockId);
        if (!block) {
          break;
        }
        var node = this.descendStackedBlock(block);
        this.script.yields = this.script.yields || node.yields;
        result.blocks.push(node);
        blockId = block.next;
      }
      return result;
    }

    /**
     * @param {*} block
     * @returns {{
     *  opcode: StackOpcode & InputOpcode,
     *  inputs?: *,
     *  yields: boolean
     * }}
     */
  }, {
    key: "getProcedureInfo",
    value: function getProcedureInfo(block) {
      var procedureCode = block.mutation.proccode;
      var paramNamesIdsAndDefaults = this.blocks.getProcedureParamNamesIdsAndDefaults(procedureCode);
      if (paramNamesIdsAndDefaults === null) {
        return {
          opcode: StackOpcode.NOP,
          yields: false
        };
      }
      var _paramNamesIdsAndDefa2 = _slicedToArray(paramNamesIdsAndDefaults, 3),
        paramNames = _paramNamesIdsAndDefa2[0],
        paramIds = _paramNamesIdsAndDefa2[1],
        paramDefaults = _paramNamesIdsAndDefa2[2];
      var addonBlock = this.runtime.getAddonBlock(procedureCode);
      if (addonBlock) {
        var _args = {};
        for (var i = 0; i < paramIds.length; i++) {
          var value = void 0;
          if (block.inputs[paramIds[i]] && block.inputs[paramIds[i]].block) {
            value = this.descendInputOfBlock(block, paramIds[i], true);
          } else {
            value = this.createConstantInput(paramDefaults[i], true);
          }
          _args[paramNames[i]] = value;
        }
        return {
          opcode: StackOpcode.ADDON_CALL,
          inputs: {
            code: procedureCode,
            arguments: _args,
            blockId: block.id
          },
          yields: true
        };
      }
      var definitionId = this.blocks.getProcedureDefinition(procedureCode);
      var definitionBlock = this.blocks.getBlock(definitionId);
      if (!definitionBlock) {
        return {
          opcode: StackOpcode.NOP,
          yields: false
        };
      }
      var innerDefinition = this.blocks.getBlock(definitionBlock.inputs.custom_block.block);
      var isWarp = this.script.isWarp;
      if (!isWarp) {
        if (innerDefinition && innerDefinition.mutation) {
          var warp = innerDefinition.mutation.warp;
          if (typeof warp === 'boolean') {
            isWarp = warp;
          } else if (typeof warp === 'string') {
            isWarp = JSON.parse(warp);
          }
        }
      }
      var variant = generateProcedureVariant(procedureCode, isWarp);
      if (!this.script.dependedProcedures.includes(variant)) {
        this.script.dependedProcedures.push(variant);
      }
      var args = [];
      for (var _i = 0; _i < paramIds.length; _i++) {
        var _value = void 0;
        if (block.inputs[paramIds[_i]] && block.inputs[paramIds[_i]].block) {
          _value = this.descendInputOfBlock(block, paramIds[_i], true);
        } else {
          _value = this.createConstantInput(paramDefaults[_i], true);
        }
        args.push(_value);
      }
      return {
        opcode: StackOpcode.PROCEDURE_CALL,
        inputs: {
          code: procedureCode,
          variant: variant,
          arguments: args
        },
        yields: !this.script.isWarp && procedureCode === this.script.procedureCode
      };
    }

    /**
     * @param {*} block
     * @returns {IntermediateStackBlock | null}
     */
  }, {
    key: "descendVisualReport",
    value: function descendVisualReport(block) {
      if (!this.thread.stackClick || block.next) {
        return null;
      }
      try {
        return new IntermediateStackBlock(StackOpcode.VISUAL_REPORT, {
          input: this.descendInput(block)
        });
      } catch (e) {
        return null;
      }
    }

    /**
     * Descend into a variable.
     * @param {*} block The block that has the variable.
     * @param {string} fieldName The name of the field that the variable is stored in.
     * @param {''|'list'} type Variable type, '' for scalar and 'list' for list.
     * @private
     * @returns {*} A parsed variable object.
     */
  }, {
    key: "descendVariable",
    value: function descendVariable(block, fieldName, type) {
      var variable = block.fields[fieldName];
      var id = variable.id;
      if (id && Object.prototype.hasOwnProperty.call(this.variableCache, id)) {
        return this.variableCache[id];
      }
      var data = this._descendVariable(id, variable.value, type);
      // If variable ID was null, this might do some unnecessary updates, but that is a rare
      // edge case and it won't have any adverse effects anyways.
      this.variableCache[String(data.id)] = data;
      return data;
    }

    /**
     * @param {string|null} id The ID of the variable.
     * @param {string} name The name of the variable.
     * @param {''|'list'} type The variable type.
     * @private
     * @returns {DescendedVariable} A parsed variable object.
     */
  }, {
    key: "_descendVariable",
    value: function _descendVariable(id, name, type) {
      var target = this.target;
      var stage = this.stage;

      // Look for by ID in target...
      if (Object.prototype.hasOwnProperty.call(target.variables, id)) {
        var currVar = target.variables[String(id)];
        return {
          scope: 'target',
          id: currVar.id,
          name: currVar.name,
          isCloud: currVar.isCloud
        };
      }

      // Look for by ID in stage...
      if (!target.isStage) {
        if (stage && Object.prototype.hasOwnProperty.call(stage.variables, id)) {
          var _currVar = stage.variables[String(id)];
          return {
            scope: 'stage',
            id: _currVar.id,
            name: _currVar.name,
            isCloud: _currVar.isCloud
          };
        }
      }

      // Look for by name and type in target...
      for (var varId in target.variables) {
        if (Object.prototype.hasOwnProperty.call(target.variables, varId)) {
          var _currVar2 = target.variables[varId];
          if (_currVar2.name === name && _currVar2.type === type) {
            return {
              scope: 'target',
              id: _currVar2.id,
              name: _currVar2.name,
              isCloud: _currVar2.isCloud
            };
          }
        }
      }

      // Look for by name and type in stage...
      if (!target.isStage && stage) {
        for (var _varId in stage.variables) {
          if (Object.prototype.hasOwnProperty.call(stage.variables, _varId)) {
            var _currVar3 = stage.variables[_varId];
            if (_currVar3.name === name && _currVar3.type === type) {
              return {
                scope: 'stage',
                id: _currVar3.id,
                name: _currVar3.name,
                isCloud: _currVar3.isCloud
              };
            }
          }
        }
      }

      // Create it locally...
      var newVariable = new Variable(id, name, type, false);

      // Intentionally not using newVariable.id so that this matches vanilla Scratch quirks regarding
      // handling of null variable IDs.
      target.variables[String(id)] = newVariable;
      if (target.sprite) {
        // Create the variable in all instances of this sprite.
        // This is necessary because the script cache is shared between clones.
        // sprite.clones has all instances of this sprite including the original and all clones
        var _iterator4 = _createForOfIteratorHelper(target.sprite.clones),
          _step4;
        try {
          for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
            var clone = _step4.value;
            if (!Object.prototype.hasOwnProperty.call(clone.variables, id)) {
              clone.variables[String(id)] = new Variable(id, name, type, false);
            }
          }
        } catch (err) {
          _iterator4.e(err);
        } finally {
          _iterator4.f();
        }
      }
      return {
        scope: 'target',
        // If the given ID was null, this won't match the .id property of the Variable object.
        // This is intentional to match vanilla Scratch quirks.
        id: id,
        name: newVariable.name,
        isCloud: newVariable.isCloud
      };
    }

    /**
     * Descend into an input block that uses the compatibility layer.
     * @param {*} block The block to use the compatibility layer for.
     * @private
     * @returns {IntermediateInput} The parsed node.
     */
  }, {
    key: "descendCompatLayerInput",
    value: function descendCompatLayerInput(block) {
      var inputs = {};
      var fields = {};
      for (var _i2 = 0, _Object$keys = Object.keys(block.inputs); _i2 < _Object$keys.length; _i2++) {
        var name = _Object$keys[_i2];
        inputs[name] = this.descendInputOfBlock(block, name, true);
      }
      for (var _i3 = 0, _Object$keys2 = Object.keys(block.fields); _i3 < _Object$keys2.length; _i3++) {
        var _name2 = _Object$keys2[_i3];
        fields[_name2] = block.fields[_name2].value;
      }
      return new IntermediateInput(InputOpcode.COMPATIBILITY_LAYER, InputType.ANY, {
        opcode: block.opcode,
        id: block.id,
        inputs: inputs,
        fields: fields
      }, true);
    }

    /**
     * Descend into a stack block that uses the compatibility layer.
     * @param {*} block The block to use the compatibility layer for.
     * @private
     * @returns {IntermediateStackBlock} The parsed node.
     */
  }, {
    key: "descendCompatLayerStack",
    value: function descendCompatLayerStack(block) {
      var inputs = {};
      for (var _i4 = 0, _Object$keys3 = Object.keys(block.inputs); _i4 < _Object$keys3.length; _i4++) {
        var name = _Object$keys3[_i4];
        if (!name.startsWith('SUBSTACK')) {
          inputs[name] = this.descendInputOfBlock(block, name, true);
        }
      }
      var fields = {};
      for (var _i5 = 0, _Object$keys4 = Object.keys(block.fields); _i5 < _Object$keys4.length; _i5++) {
        var _name3 = _Object$keys4[_i5];
        fields[_name3] = block.fields[_name3].value;
      }
      var blockInfo = this.getBlockInfo(block.opcode);
      var blockType = blockInfo && blockInfo.info && blockInfo.info.blockType || BlockType.COMMAND;
      var substacks = {};
      if (blockType === BlockType.CONDITIONAL || blockType === BlockType.LOOP) {
        for (var inputName in block.inputs) {
          if (!inputName.startsWith('SUBSTACK')) continue;
          var branchNum = inputName === 'SUBSTACK' ? 1 : +inputName.substring('SUBSTACK'.length);
          if (!isNaN(branchNum)) {
            substacks[branchNum] = this.descendSubstack(block, inputName);
          }
        }
      }
      return new IntermediateStackBlock(StackOpcode.COMPATIBILITY_LAYER, {
        opcode: block.opcode,
        id: block.id,
        blockType: blockType,
        inputs: inputs,
        fields: fields,
        substacks: substacks
      }, true);
    }
  }, {
    key: "analyzeLoop",
    value: function analyzeLoop() {
      return !this.script.isWarp || this.script.warpTimer;
    }
  }, {
    key: "readTopBlockComment",
    value: function readTopBlockComment(commentId) {
      var comment = this.target.comments[commentId];
      if (!comment) {
        // can't find the comment
        // this is safe to ignore
        return;
      }
      var text = comment.text;
      var _iterator5 = _createForOfIteratorHelper(text.split('\n')),
        _step5;
      try {
        for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
          var line = _step5.value;
          if (!/^tw\b/.test(line)) {
            continue;
          }
          var flags = line.split(' ');
          var _iterator6 = _createForOfIteratorHelper(flags),
            _step6;
          try {
            for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
              var flag = _step6.value;
              switch (flag) {
                case 'nocompile':
                  throw new Error('Script explicitly disables compilation');
                case 'stuck':
                  this.script.warpTimer = true;
                  break;
              }
            }

            // Only the first 'tw' line is parsed.
          } catch (err) {
            _iterator6.e(err);
          } finally {
            _iterator6.f();
          }
          break;
        }
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }
    }

    /**
     * @param {*} hatBlock
     * @returns {IntermediateStack}
     */
  }, {
    key: "walkHat",
    value: function walkHat(hatBlock) {
      var nextBlock = hatBlock.next;
      var opcode = hatBlock.opcode;
      var hatInfo = this.runtime._hats[opcode];
      if (this.thread.stackClick) {
        // We still need to treat the hat as a normal block (so executableHat should be false) for
        // interpreter parity, but the reuslt is ignored.
        var _opcodeFunction = this.runtime.getOpcodeFunction(opcode);
        if (_opcodeFunction) {
          return new IntermediateStack([this.descendCompatLayerStack(hatBlock)].concat(_toConsumableArray(this.walkStack(nextBlock).blocks)));
        }
        return this.walkStack(nextBlock);
      }
      if (hatInfo.edgeActivated) {
        // Edge-activated HAT
        this.script.yields = true;
        this.script.executableHat = true;
        return new IntermediateStack([new IntermediateStackBlock(StackOpcode.HAT_EDGE, {
          id: hatBlock.id,
          condition: this.descendCompatLayerInput(hatBlock).toType(InputType.BOOLEAN)
        })].concat(_toConsumableArray(this.walkStack(nextBlock).blocks)));
      }
      var opcodeFunction = this.runtime.getOpcodeFunction(opcode);
      if (opcodeFunction) {
        // Predicate-based HAT
        this.script.yields = true;
        this.script.executableHat = true;
        return new IntermediateStack([new IntermediateStackBlock(StackOpcode.HAT_PREDICATE, {
          condition: this.descendCompatLayerInput(hatBlock).toType(InputType.BOOLEAN)
        })].concat(_toConsumableArray(this.walkStack(nextBlock).blocks)));
      }
      return this.walkStack(nextBlock);
    }

    /**
     * @param {string} topBlockId The ID of the top block of the script.
     * @returns {IntermediateScript}
     */
  }, {
    key: "generate",
    value: function generate(topBlockId) {
      this.blocks.populateProcedureCache();
      this.script.topBlockId = topBlockId;
      var topBlock = this.getBlockById(topBlockId);
      if (!topBlock) {
        if (this.script.isProcedure) {
          // Empty procedure
          return this.script;
        }
        throw new Error('Cannot find top block');
      }
      if (topBlock.comment) {
        this.readTopBlockComment(topBlock.comment);
      }

      // We do need to evaluate empty hats
      var hatInfo = this.runtime._hats[topBlock.opcode];
      var isHat = !!hatInfo;
      if (isHat) {
        this.script.stack = this.walkHat(topBlock);
      } else {
        // We don't evaluate the procedures_definition top block as it never does anything
        // We also don't want it to be treated like a hat block
        var entryBlock;
        if (topBlock.opcode === 'procedures_definition') {
          entryBlock = topBlock.next;
        } else {
          entryBlock = topBlockId;
        }
        if (entryBlock) {
          this.script.stack = this.walkStack(entryBlock);
        }
      }
      return this.script;
    }
  }]);
}();
var IRGenerator = /*#__PURE__*/function () {
  function IRGenerator(thread) {
    _classCallCheck(this, IRGenerator);
    this.thread = thread;
    this.blocks = thread.blockContainer;
    this.proceduresToCompile = new Map();
    this.compilingProcedures = new Map();
    /** @type {Object.<string, IntermediateScript>} */
    this.procedures = {};
    this.analyzedProcedures = [];
  }
  return _createClass(IRGenerator, [{
    key: "addProcedureDependencies",
    value: function addProcedureDependencies(dependencies) {
      var _iterator7 = _createForOfIteratorHelper(dependencies),
        _step7;
      try {
        for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
          var procedureVariant = _step7.value;
          if (Object.prototype.hasOwnProperty.call(this.procedures, procedureVariant)) {
            continue;
          }
          if (this.compilingProcedures.has(procedureVariant)) {
            continue;
          }
          if (this.proceduresToCompile.has(procedureVariant)) {
            continue;
          }
          var procedureCode = parseProcedureCode(procedureVariant);
          var definition = this.blocks.getProcedureDefinition(procedureCode);
          this.proceduresToCompile.set(procedureVariant, definition);
        }
      } catch (err) {
        _iterator7.e(err);
      } finally {
        _iterator7.f();
      }
    }

    /**
     * @param {ScriptTreeGenerator} generator The generator to run.
     * @param {string} topBlockId The ID of the top block in the stack.
     * @returns {IntermediateScript} Intermediate script.
     */
  }, {
    key: "generateScriptTree",
    value: function generateScriptTree(generator, topBlockId) {
      var result = generator.generate(topBlockId);
      this.addProcedureDependencies(result.dependedProcedures);
      return result;
    }

    /**
     * Recursively analyze a script and its dependencies.
     * @param {IntermediateScript} script Intermediate script.
     */
  }, {
    key: "analyzeScript",
    value: function analyzeScript(script) {
      var madeChanges = false;
      var _iterator8 = _createForOfIteratorHelper(script.dependedProcedures),
        _step8;
      try {
        for (_iterator8.s(); !(_step8 = _iterator8.n()).done;) {
          var procedureCode = _step8.value;
          var procedureData = this.procedures[procedureCode];

          // Analyze newly found procedures.
          if (!this.analyzedProcedures.includes(procedureCode)) {
            this.analyzedProcedures.push(procedureCode);
            if (this.analyzeScript(procedureData)) {
              madeChanges = true;
            }
            this.analyzedProcedures.pop();
          }

          // If a procedure used by a script may yield, the script itself may yield.
          if (procedureData.yields && !script.yields) {
            script.yields = true;
            madeChanges = true;
          }
        }
      } catch (err) {
        _iterator8.e(err);
      } finally {
        _iterator8.f();
      }
      return madeChanges;
    }

    /**
     * @returns {IntermediateRepresentation} Intermediate representation.
     */
  }, {
    key: "generate",
    value: function generate() {
      var entry = this.generateScriptTree(new ScriptTreeGenerator(this.thread), this.thread.topBlock);

      // Compile any required procedures.
      // As procedures can depend on other procedures, this process may take several iterations.
      var procedureTreeCache = this.blocks._cache.compiledProcedures;
      while (this.proceduresToCompile.size > 0) {
        this.compilingProcedures = this.proceduresToCompile;
        this.proceduresToCompile = new Map();
        var _iterator9 = _createForOfIteratorHelper(this.compilingProcedures.entries()),
          _step9;
        try {
          for (_iterator9.s(); !(_step9 = _iterator9.n()).done;) {
            var _step9$value = _slicedToArray(_step9.value, 2),
              procedureVariant = _step9$value[0],
              definitionId = _step9$value[1];
            if (procedureTreeCache[procedureVariant]) {
              var result = procedureTreeCache[procedureVariant];
              this.procedures[procedureVariant] = result;
              this.addProcedureDependencies(result.dependedProcedures);
            } else {
              var isWarp = parseIsWarp(procedureVariant);
              var generator = new ScriptTreeGenerator(this.thread);
              generator.setProcedureVariant(procedureVariant);
              if (isWarp) generator.enableWarp();
              var compiledProcedure = this.generateScriptTree(generator, definitionId);
              this.procedures[procedureVariant] = compiledProcedure;
              procedureTreeCache[procedureVariant] = compiledProcedure;
            }
          }
        } catch (err) {
          _iterator9.e(err);
        } finally {
          _iterator9.f();
        }
      }

      // Analyze scripts until no changes are made.
      while (this.analyzeScript(entry));
      return new IntermediateRepresentation(entry, this.procedures);
    }
  }]);
}();
module.exports = {
  ScriptTreeGenerator: ScriptTreeGenerator,
  IRGenerator: IRGenerator
};

/***/ }),

/***/ "../vm/src/compiler/iroptimizer.js":
/*!*****************************************!*\
  !*** ../vm/src/compiler/iroptimizer.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _readOnlyError(r) { throw new TypeError('"' + r + '" is read-only'); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// @ts-check

var _require = __webpack_require__(/*! ./enums.js */ "../vm/src/compiler/enums.js"),
  StackOpcode = _require.StackOpcode,
  InputOpcode = _require.InputOpcode,
  InputType = _require.InputType;
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

// These imports are used by jsdoc comments but eslint doesn't know that
/* eslint-disable no-unused-vars */
var _require2 = __webpack_require__(/*! ./intermediate */ "../vm/src/compiler/intermediate.js"),
  IntermediateStack = _require2.IntermediateStack,
  IntermediateInput = _require2.IntermediateInput,
  IntermediateScript = _require2.IntermediateScript,
  IntermediateRepresentation = _require2.IntermediateRepresentation,
  IntermediateStackBlock = _require2.IntermediateStackBlock;
/* eslint-enable no-unused-vars */
var TypeState = /*#__PURE__*/function () {
  function TypeState() {
    _classCallCheck(this, TypeState);
    /** @type {Object.<string, InputType | 0>}*/
    this.variables = Object.create(null);
  }

  /**
   * @returns {boolean}
   */
  return _createClass(TypeState, [{
    key: "clear",
    value: function clear() {
      var modified = false;
      for (var varId in this.variables) {
        if (this.variables[varId] !== InputType.ANY) {
          modified = true;
          break;
        }
      }
      this.variables = Object.create(null);
      return modified;
    }

    /**
     * @returns {TypeState}
     */
  }, {
    key: "clone",
    value: function clone() {
      var clone = new TypeState();
      for (var varId in this.variables) {
        clone.variables[varId] = this.variables[varId];
      }
      return clone;
    }

    /**
     * @param {TypeState} other
     * @param {(varId: string) => InputType | 0} stateMutator
     * @returns {boolean}
     * @private
     */
  }, {
    key: "mutate",
    value: function mutate(other, stateMutator) {
      var modified = false;
      for (var varId in other.variables) {
        var newValue = stateMutator(varId);
        if (newValue !== this.variables[varId]) {
          this.variables[varId] = newValue;
          modified = modified || true;
        }
      }
      for (var _varId in this.variables) {
        if (!other.variables[_varId]) {
          var _newValue = stateMutator(_varId);
          if (_newValue !== this.variables[_varId]) {
            this.variables[_varId] = _newValue;
            modified = modified || true;
          }
        }
      }
      return modified;
    }

    /**
     * @param {TypeState} other
     * @returns {boolean}
     */
  }, {
    key: "or",
    value: function or(other) {
      var _this = this;
      return this.mutate(other, function (varId) {
        var _this$variables$varId, _other$variables$varI;
        var thisType = (_this$variables$varId = _this.variables[varId]) !== null && _this$variables$varId !== void 0 ? _this$variables$varId : InputType.ANY;
        var otherType = (_other$variables$varI = other.variables[varId]) !== null && _other$variables$varI !== void 0 ? _other$variables$varI : InputType.ANY;
        return thisType | otherType;
      });
    }

    /**
     * @param {TypeState} other
     * @returns {boolean}
     */
  }, {
    key: "after",
    value: function after(other) {
      var _this2 = this;
      return this.mutate(other, function (varId) {
        var _this2$variables$varI;
        var otherType = other.variables[varId];
        if (otherType) return otherType;
        return (_this2$variables$varI = _this2.variables[varId]) !== null && _this2$variables$varI !== void 0 ? _this2$variables$varI : InputType.ANY;
      });
    }

    /**
     * @param {TypeState} other
     * @returns {boolean}
     */
  }, {
    key: "overwrite",
    value: function overwrite(other) {
      return this.mutate(other, function (varId) {
        var _other$variables$varI2;
        return (_other$variables$varI2 = other.variables[varId]) !== null && _other$variables$varI2 !== void 0 ? _other$variables$varI2 : InputType.ANY;
      });
    }

    /**
     * @param {*} variable A variable codegen object.
     * @param {InputType} type The type to set this variable to
     * @returns {boolean}
     */
  }, {
    key: "setVariableType",
    value: function setVariableType(variable, type) {
      if (this.variables[variable.id] === type) return false;
      this.variables[variable.id] = type;
      return true;
    }

    /**
     *
     * @param {*} variable A variable codegen object.
     * @returns {InputType}
     */
  }, {
    key: "getVariableType",
    value: function getVariableType(variable) {
      var _this$variables$varia;
      return (_this$variables$varia = this.variables[variable.id]) !== null && _this$variables$varia !== void 0 ? _this$variables$varia : InputType.ANY;
    }
  }]);
}();
var IROptimizer = /*#__PURE__*/function () {
  /**
   * @param {IntermediateRepresentation} ir
   */
  function IROptimizer(ir) {
    _classCallCheck(this, IROptimizer);
    /** @type {IntermediateRepresentation} */
    this.ir = ir;
    /** @type {boolean} Used for testing */
    this.ignoreYields = false;

    /** @private @type {TypeState | null} The state the analyzed script could exit in */
    this.exitState = null;
  }

  /**
   * @param {IntermediateInput} inputBlock
   * @param {TypeState} state
   * @returns {InputType}
   */
  return _createClass(IROptimizer, [{
    key: "getInputType",
    value: function getInputType(inputBlock, state) {
      var inputs = inputBlock.inputs;
      switch (inputBlock.opcode) {
        case InputOpcode.VAR_GET:
          return state.getVariableType(inputs.variable);
        case InputOpcode.ADDON_CALL:
          break;
        case InputOpcode.CAST_BOOLEAN:
          {
            var innerType = inputs.target.type;
            if (innerType & InputType.BOOLEAN) return innerType;
            return InputType.BOOLEAN;
          }
        case InputOpcode.CAST_NUMBER:
          {
            var _innerType = inputs.target.type;
            if (_innerType & InputType.NUMBER) return _innerType;
            return InputType.NUMBER;
          }
        case InputOpcode.CAST_NUMBER_INDEX:
          {
            var _innerType2 = inputs.target.type;
            if (_innerType2 & InputType.NUMBER_INDEX) return _innerType2;
            return InputType.NUMBER_INDEX;
          }
        case InputOpcode.CAST_NUMBER_OR_NAN:
          {
            var _innerType3 = inputs.target.type;
            if (_innerType3 & InputType.NUMBER_OR_NAN) return _innerType3;
            return InputType.NUMBER_OR_NAN;
          }
        case InputOpcode.CAST_STRING:
          {
            var _innerType4 = inputs.target.type;
            if (_innerType4 & InputType.STRING) return _innerType4;
            return InputType.STRING;
          }
        case InputOpcode.OP_ADD:
          {
            var leftType = inputs.left.type;
            var rightType = inputs.right.type;
            var resultType = 0;
            var canBeNaN = function canBeNaN() {
              // Infinity + (-Infinity) = NaN
              if (leftType & InputType.NUMBER_POS_INF && rightType & InputType.NUMBER_NEG_INF) return true;
              // (-Infinity) + Infinity = NaN
              if (leftType & InputType.NUMBER_NEG_INF && rightType & InputType.NUMBER_POS_INF) return true;
            };
            if (canBeNaN()) resultType |= InputType.NUMBER_NAN;
            var canBeFractional = function canBeFractional() {
              // For the plus operation to return a non-whole number one of it's
              //  inputs has to be a non-whole number
              if (leftType & InputType.NUMBER_FRACT) return true;
              if (rightType & InputType.NUMBER_FRACT) return true;
            };
            var canBeFract = canBeFractional();
            var canBePos = function canBePos() {
              if (leftType & InputType.NUMBER_POS) return true; // POS + ANY ~= POS
              if (rightType & InputType.NUMBER_POS) return true; // ANY + POS ~= POS
            };
            if (canBePos()) {
              resultType |= InputType.NUMBER_POS_INT | InputType.NUMBER_POS_INF;
              if (canBeFract) resultType |= InputType.NUMBER_POS_FRACT;
            }
            var canBeNeg = function canBeNeg() {
              if (leftType & InputType.NUMBER_NEG) return true; // NEG + ANY ~= NEG
              if (rightType & InputType.NUMBER_NEG) return true; // ANY + NEG ~= NEG
            };
            if (canBeNeg()) {
              resultType |= InputType.NUMBER_NEG_INT | InputType.NUMBER_NEG_INF;
              if (canBeFract) resultType |= InputType.NUMBER_NEG_FRACT;
            }
            var canBeZero = function canBeZero() {
              // POS_REAL + NEG_REAL ~= 0
              if (leftType & InputType.NUMBER_POS_REAL && rightType & InputType.NUMBER_NEG_REAL) return true;
              // NEG_REAL + POS_REAL ~= 0
              if (leftType & InputType.NUMBER_NEG_REAL && rightType & InputType.NUMBER_POS_REAL) return true;
              // 0 + 0 = 0
              if (leftType & InputType.NUMBER_ZERO && rightType & InputType.NUMBER_ZERO) return true;
              // 0 + -0 = 0
              if (leftType & InputType.NUMBER_ZERO && rightType & InputType.NUMBER_NEG_ZERO) return true;
              // -0 + 0 = 0
              if (leftType & InputType.NUMBER_NEG_ZERO && rightType & InputType.NUMBER_ZERO) return true;
            };
            if (canBeZero()) resultType |= InputType.NUMBER_ZERO;
            var canBeNegZero = function canBeNegZero() {
              // -0 + -0 = -0
              if (leftType & InputType.NUMBER_NEG_ZERO && rightType & InputType.NUMBER_NEG_ZERO) return true;
            };
            if (canBeNegZero()) resultType |= InputType.NUMBER_NEG_ZERO;
            return resultType;
          }
        case InputOpcode.OP_SUBTRACT:
          {
            var _leftType = inputs.left.type;
            var _rightType = inputs.right.type;
            var _resultType = 0;
            var _canBeNaN = function _canBeNaN() {
              // Infinity - Infinity = NaN
              if (_leftType & InputType.NUMBER_POS_INF && _rightType & InputType.NUMBER_POS_INF) return true;
              // (-Infinity) - (-Infinity) = NaN
              if (_leftType & InputType.NUMBER_NEG_INF && _rightType & InputType.NUMBER_NEG_INF) return true;
            };
            if (_canBeNaN()) _resultType |= InputType.NUMBER_NAN;
            var _canBeFractional = function _canBeFractional() {
              // For the subtract operation to return a non-whole number one of it's
              //  inputs has to be a non-whole number
              if (_leftType & InputType.NUMBER_FRACT) return true;
              if (_rightType & InputType.NUMBER_FRACT) return true;
            };
            var _canBeFract = _canBeFractional();
            var _canBePos = function _canBePos() {
              if (_leftType & InputType.NUMBER_POS) return true; // POS - ANY ~= POS
              if (_rightType & InputType.NUMBER_NEG) return true; // ANY - NEG ~= POS
            };
            if (_canBePos()) {
              _resultType |= InputType.NUMBER_POS_INT | InputType.NUMBER_POS_INF;
              if (_canBeFract) _resultType |= InputType.NUMBER_POS_FRACT;
            }
            var _canBeNeg = function _canBeNeg() {
              if (_leftType & InputType.NUMBER_NEG) return true; // NEG - ANY ~= NEG
              if (_rightType & InputType.NUMBER_POS) return true; // ANY - POS ~= NEG
            };
            if (_canBeNeg()) {
              _resultType |= InputType.NUMBER_NEG_INT | InputType.NUMBER_NEG_INF;
              if (_canBeFract) _resultType |= InputType.NUMBER_NEG_FRACT;
            }
            var _canBeZero = function _canBeZero() {
              // POS_REAL - POS_REAL ~= 0
              if (_leftType & InputType.NUMBER_POS_REAL && _rightType & InputType.NUMBER_POS_REAL) return true;
              // NEG_REAL - NEG_REAL ~= 0
              if (_leftType & InputType.NUMBER_NEG_REAL && _rightType & InputType.NUMBER_NEG_REAL) return true;
              // 0 - 0 = 0
              if (_leftType & InputType.NUMBER_ZERO && _rightType & InputType.NUMBER_ZERO) return true;
              // 0 - (-0) = 0
              if (_leftType & InputType.NUMBER_ZERO && _rightType & InputType.NUMBER_NEG_ZERO) return true;
              // (-0) - (-0) = 0
              if (_leftType & InputType.NUMBER_NEG_ZERO && _rightType & InputType.NUMBER_NEG_ZERO) return true;
            };
            if (_canBeZero()) _resultType |= InputType.NUMBER_ZERO;
            var _canBeNegZero = function _canBeNegZero() {
              // (-0) - 0 = -0
              if (_leftType & InputType.NUMBER_NEG_ZERO && _rightType & InputType.NUMBER_ZERO) return true;
            };
            if (_canBeNegZero()) _resultType |= InputType.NUMBER_NEG_ZERO;
            return _resultType;
          }
        case InputOpcode.OP_MULTIPLY:
          {
            var _leftType2 = inputs.left.type;
            var _rightType2 = inputs.right.type;
            var _resultType2 = 0;
            var _canBeNaN2 = function _canBeNaN2() {
              // (-)Infinity * 0 = NaN
              if (_leftType2 & InputType.NUMBER_INF && _rightType2 & InputType.NUMBER_ANY_ZERO) return true;
              // 0 * (-)Infinity = NaN
              if (_leftType2 & InputType.NUMBER_ANY_ZERO && _rightType2 & InputType.NUMBER_INF) return true;
            };
            if (_canBeNaN2()) _resultType2 |= InputType.NUMBER_NAN;
            var _canBeFractional2 = function _canBeFractional2() {
              // For the subtract operation to return a non-whole number one of it's
              //  inputs has to be a non-whole number
              if (_leftType2 & InputType.NUMBER_FRACT) return true;
              if (_rightType2 & InputType.NUMBER_FRACT) return true;
            };
            var _canBeFract2 = _canBeFractional2();
            var _canBePos2 = function _canBePos2() {
              // POS * POS = POS
              if (_leftType2 & InputType.NUMBER_POS && _rightType2 & InputType.NUMBER_POS) return true;
              // NEG * NEG = POS
              if (_leftType2 & InputType.NUMBER_NEG && _rightType2 & InputType.NUMBER_NEG) return true;
            };
            if (_canBePos2()) {
              _resultType2 |= InputType.NUMBER_POS_INT | InputType.NUMBER_POS_INF;
              if (_canBeFract2) _resultType2 |= InputType.NUMBER_POS_FRACT;
            }
            var _canBeNeg2 = function _canBeNeg2() {
              // POS * NEG = NEG
              if (_leftType2 & InputType.NUMBER_POS && _rightType2 & InputType.NUMBER_NEG) return true;
              // NEG * POS = NEG
              if (_leftType2 & InputType.NUMBER_NEG && _rightType2 & InputType.NUMBER_POS) return true;
            };
            if (_canBeNeg2()) {
              _resultType2 |= InputType.NUMBER_NEG_INT | InputType.NUMBER_NEG_INF;
              if (_canBeFract2) _resultType2 |= InputType.NUMBER_NEG_FRACT;
            }
            var _canBeZero2 = function _canBeZero2() {
              // 0 * 0 = 0
              if (_leftType2 & InputType.NUMBER_ZERO && _rightType2 & InputType.NUMBER_ZERO) return true;
              // -0 * -0 = 0
              if (_leftType2 & InputType.NUMBER_NEG_ZERO && _rightType2 & InputType.NUMBER_NEG_ZERO) return true;
              // 0 * POS_REAL = 0
              if (_leftType2 & InputType.NUMBER_ZERO && _rightType2 & InputType.NUMBER_POS_REAL) return true;
              // -0 * NEG_REAL = 0
              if (_leftType2 & InputType.NUMBER_NEG_ZERO && _rightType2 & InputType.NUMBER_NEG_REAL) return true;
              // POS_REAL * 0 = 0
              if (_leftType2 & InputType.NUMBER_POS_REAL && _rightType2 & InputType.NUMBER_ZERO) return true;
              // NEG_REAL * -0 = 0
              if (_leftType2 & InputType.NUMBER_NEG_REAL && _rightType2 & InputType.NUMBER_NEG_ZERO) return true;
              // Rounding errors like 1e-323 * 0.1 = 0
              if (_leftType2 & InputType.NUMBER_FRACT && _rightType2 & InputType.NUMBER_FRACT) return true;
            };
            if (_canBeZero2()) _resultType2 |= InputType.NUMBER_ZERO;
            var _canBeNegZero2 = function _canBeNegZero2() {
              // 0 * -0 = 0
              if (_leftType2 & InputType.NUMBER_ZERO && _rightType2 & InputType.NUMBER_NEG_ZERO) return true;
              // -0 * 0 = 0
              if (_leftType2 & InputType.NUMBER_NEG_ZERO && _rightType2 & InputType.NUMBER_ZERO) return true;
              // -0 * POS_REAL = -0
              if (_leftType2 & InputType.NUMBER_NEG_ZERO && _rightType2 & InputType.NUMBER_POS_REAL) return true;
              // 0 * NEG_REAL = -0
              if (_leftType2 & InputType.NUMBER_ZERO && _rightType2 & InputType.NUMBER_NEG_REAL) return true;
              // POS_REAL * -0 = -0
              if (_leftType2 & InputType.NUMBER_POS_REAL && _rightType2 & InputType.NUMBER_NEG_ZERO) return true;
              // NEG_REAL * 0 = -0
              if (_leftType2 & InputType.NUMBER_NEG_REAL && _rightType2 & InputType.NUMBER_ZERO) return true;
              // Rounding errors like -1e-323 / 10 = -0
              if (_leftType2 & InputType.NUMBER_NEG_REAL && _rightType2 & InputType.NUMBER_POS_REAL) return true;
              // Rounding errors like 1e-323 / -10 = -0
              if (_leftType2 & InputType.NUMBER_POS_REAL && _rightType2 & InputType.NUMBER_NEG_REAL) return true;
            };
            if (_canBeNegZero2()) _resultType2 |= InputType.NUMBER_NEG_ZERO;
            return _resultType2;
          }
        case InputOpcode.OP_DIVIDE:
          {
            var _leftType3 = inputs.left.type;
            var _rightType3 = inputs.right.type;
            var _resultType3 = 0;
            var _canBeNaN3 = function _canBeNaN3() {
              // (-)0 / (-)0 = NaN
              if (_leftType3 & InputType.NUMBER_ANY_ZERO && _rightType3 & InputType.NUMBER_ANY_ZERO) return true;
              // (-)Infinity / (-)Infinity = NaN
              if (_leftType3 & InputType.NUMBER_INF && _rightType3 & InputType.NUMBER_INF) return true;
              // (-)0 / NaN = NaN
              if (_leftType3 & InputType.NUMBER_ANY_ZERO && _rightType3 & InputType.NUMBER_NAN) return true;
            };
            if (_canBeNaN3()) _resultType3 |= InputType.NUMBER_NAN;
            var _canBePos3 = function _canBePos3() {
              // POS / POS = POS
              if (_leftType3 & InputType.NUMBER_POS && _rightType3 & InputType.NUMBER_POS) return true;
              // NEG / NEG = POS
              if (_leftType3 & InputType.NUMBER_NEG && _rightType3 & InputType.NUMBER_NEG) return true;
            };
            if (_canBePos3()) _resultType3 |= InputType.NUMBER_POS;
            var canBeNegInfinity = function canBeNegInfinity() {
              // NEG / 0 = -Infinity
              if (_leftType3 & InputType.NUMBER_NEG && _rightType3 & InputType.NUMBER_ZERO) return true;
              // POS / -0 = -Infinity
              if (_leftType3 & InputType.NUMBER_POS && _rightType3 & InputType.NUMBER_NEG_ZERO) return true;
              // NEG_REAL / POS_REAL ~= -Infinity
              if (_leftType3 & InputType.NUMBER_NEG_REAL && _rightType3 & InputType.NUMBER_POS_REAL) return true;
              // POS_REAL / NEG_REAL ~= -Infinity
              if (_leftType3 & InputType.NUMBER_POS_REAL && _rightType3 & InputType.NUMBER_NEG_REAL) return true;
            };
            if (canBeNegInfinity()) _resultType3 |= InputType.NUMBER_NEG_INF;
            var canBeInfinity = function canBeInfinity() {
              // POS / 0 = Infinity
              if (_leftType3 & InputType.NUMBER_POS && _rightType3 & InputType.NUMBER_ZERO) return true;
              // NEG / -0 = Infinity
              if (_leftType3 & InputType.NUMBER_NEG && _rightType3 & InputType.NUMBER_NEG_ZERO) return true;
              // POS_REAL / POS_REAL ~= Infinity
              if (_leftType3 & InputType.NUMBER_POS_REAL && _rightType3 & InputType.NUMBER_POS_REAL) return true;
              // NEG_REAL / NEG_REAL ~= Infinity
              if (_leftType3 & InputType.NUMBER_NEG_REAL && _rightType3 & InputType.NUMBER_NEG_REAL) return true;
            };
            if (canBeInfinity()) _resultType3 |= InputType.NUMBER_POS_INF;
            var _canBeNeg3 = function _canBeNeg3() {
              // POS / NEG = NEG
              if (_leftType3 & InputType.NUMBER_POS && _rightType3 & InputType.NUMBER_NEG) return true;
              // NEG / POS = NEG
              if (_leftType3 & InputType.NUMBER_NEG && _rightType3 & InputType.NUMBER_POS) return true;
            };
            if (_canBeNeg3()) _resultType3 |= InputType.NUMBER_NEG;
            var _canBeZero3 = function _canBeZero3() {
              // 0 / POS = 0
              if (_leftType3 & InputType.NUMBER_ZERO && _rightType3 & InputType.NUMBER_POS) return true;
              // -0 / NEG = 0
              if (_leftType3 & InputType.NUMBER_NEG_ZERO && _rightType3 & InputType.NUMBER_NEG) return true;
              // Rounding errors like 1e-323 / 10 = 0
              if (_leftType3 & InputType.NUMBER_POS_REAL && _rightType3 & InputType.NUMBER_POS_REAL) return true;
              // Rounding errors like -1e-323 / -10 = 0
              if (_leftType3 & InputType.NUMBER_NEG_REAL && _rightType3 & InputType.NUMBER_NEG_REAL) return true;
              // NUMBER_POS / Infinity = 0
              if (_leftType3 & InputType.NUMBER_POS && _rightType3 & InputType.NUMBER_POS_INF) return true;
              // NUMBER_NEG / -Infinity = 0
              if (_leftType3 & InputType.NUMBER_NEG && _rightType3 & InputType.NUMBER_NEG_INF) return true;
            };
            if (_canBeZero3()) _resultType3 |= InputType.NUMBER_ZERO;
            var _canBeNegZero3 = function _canBeNegZero3() {
              // -0 / POS = -0
              if (_leftType3 & InputType.NUMBER_NEG_ZERO && _rightType3 & InputType.NUMBER_POS) return true;
              // 0 / NEG = -0
              if (_leftType3 & InputType.NUMBER_ZERO && _rightType3 & InputType.NUMBER_NEG) return true;
              // Rounding errors like -1e-323 / 10 = -0
              if (_leftType3 & InputType.NUMBER_NEG_REAL && _rightType3 & InputType.NUMBER_POS_REAL) return true;
              // Rounding errors like 1e-323 / -10 = -0
              if (_leftType3 & InputType.NUMBER_POS_REAL && _rightType3 & InputType.NUMBER_NEG_REAL) return true;
              // NUMBER_POS / -Infinity = -0
              if (_leftType3 & InputType.NUMBER_POS && _rightType3 & InputType.NUMBER_NEG_INF) return true;
              // NUMBER_NEG / Infinity = -0
              if (_leftType3 & InputType.NUMBER_NEG && _rightType3 & InputType.NUMBER_POS_INF) return true;
            };
            if (_canBeNegZero3()) _resultType3 |= InputType.NUMBER_NEG_ZERO;
            return _resultType3;
          }
      }
      return inputBlock.type;
    }

    /**
     * @param {IntermediateInput} inputBlock
     * @param {TypeState} state
     * @returns {boolean}
     * @private
     */
  }, {
    key: "analyzeInputBlock",
    value: function analyzeInputBlock(inputBlock, state) {
      var inputs = inputBlock.inputs;
      var modified = this.analyzeInputs(inputs, state);
      var newType = this.getInputType(inputBlock, state);
      modified = modified || newType !== inputBlock.type;
      inputBlock.type = newType;
      switch (inputBlock.opcode) {
        case InputOpcode.ADDON_CALL:
          modified = state.clear() || modified;
          break;
        case InputOpcode.PROCEDURE_CALL:
          {
            modified = this.analyzeInputs(inputs.inputs, state) || modified;
            var script = this.ir.procedures[inputs.variant];
            if (!script || !script.cachedAnalysisEndState) {
              modified = state.clear() || modified;
            } else if (script.yields) {
              modified = state.overwrite(script.cachedAnalysisEndState) || modified;
            } else {
              modified = state.after(script.cachedAnalysisEndState) || modified;
            }
            break;
          }
      }
      return modified;
    }

    /**
     * @param {Object<any, any>} inputs
     * @param {TypeState} state
     * @returns {boolean} modified
     */
  }, {
    key: "analyzeInputs",
    value: function analyzeInputs(inputs, state) {
      var modified = false;
      for (var inputName in inputs) {
        var input = inputs[inputName];
        if (input instanceof IntermediateInput) {
          modified = this.analyzeInputBlock(input, state) || modified;
        }
      }
      return modified;
    }

    /**
     * @param {TypeState} state
     */
  }, {
    key: "addPossibleExitState",
    value: function addPossibleExitState(state) {
      if (this.exitState === null) {
        this.exitState = state.clone();
        return;
      }
      this.exitState.or(state);
    }

    /**
     * @param {IntermediateStackBlock} stackBlock
     * @param {TypeState} state
     * @returns {boolean}
     * @private
     */
  }, {
    key: "analyzeStackBlock",
    value: function analyzeStackBlock(stackBlock, state) {
      var inputs = stackBlock.inputs;
      var modified = false;
      if (stackBlock.ignoreState) {
        state = state.clone();
      }
      switch (stackBlock.opcode) {
        case StackOpcode.VAR_SET:
          modified = this.analyzeInputs(inputs, state) || modified;
          modified = state.setVariableType(inputs.variable, inputs.value.type) || modified;
          break;
        case StackOpcode.CONTROL_WHILE:
        case StackOpcode.CONTROL_FOR:
          modified = this.analyzeInputs(inputs, state) || modified;
          modified = this.analyzeLoopedStack(inputs["do"], state, stackBlock, true) || modified;
          break;
        case StackOpcode.CONTROL_REPEAT:
          modified = this.analyzeInputs(inputs, state) || modified;
          modified = this.analyzeLoopedStack(inputs["do"], state, stackBlock, false) || modified;
          break;
        case StackOpcode.CONTROL_IF_ELSE:
          {
            modified = this.analyzeInputs(inputs, state) || modified;
            var trueState = state.clone();
            modified = this.analyzeStack(inputs.whenTrue, trueState) || modified;
            modified = this.analyzeStack(inputs.whenFalse, state) || modified;
            modified = state.or(trueState) || modified;
            break;
          }
        case StackOpcode.CONTROL_STOP_SCRIPT:
          {
            modified = this.analyzeInputs(inputs, state) || modified;
            this.addPossibleExitState(state);
            break;
          }
        case StackOpcode.CONTROL_WAIT_UNTIL:
          {
            modified = state.clear() || modified;
            modified = this.analyzeInputs(inputs, state) || modified;
            break;
          }
        case StackOpcode.PROCEDURE_CALL:
          {
            modified = this.analyzeInputs(inputs, state) || modified;
            modified = this.analyzeInputs(inputs.inputs, state) || modified;
            var script = this.ir.procedures[inputs.variant];
            if (!script || !script.cachedAnalysisEndState) {
              modified = state.clear() || modified;
            } else if (script.yields) {
              modified = state.overwrite(script.cachedAnalysisEndState) || modified;
            } else {
              modified = state.after(script.cachedAnalysisEndState) || modified;
            }
            break;
          }
        case StackOpcode.COMPATIBILITY_LAYER:
          {
            modified = this.analyzeInputs(inputs, state) || modified;
            this.analyzeInputs(inputs.inputs, state);
            for (var substackName in inputs.substacks) {
              var newState = state.clone();
              modified = this.analyzeStack(inputs.substacks[substackName], newState) || modified;
              modified = state.or(newState) || modified;
            }
            break;
          }
        default:
          modified = this.analyzeInputs(inputs, state) || modified;
          break;
      }
      return modified;
    }

    /**
     * @param {IntermediateStack?} stack
     * @param {TypeState} state
     * @returns {boolean}
     * @private
     */
  }, {
    key: "analyzeStack",
    value: function analyzeStack(stack, state) {
      if (!stack) return false;
      var modified = false;
      var _iterator = _createForOfIteratorHelper(stack.blocks),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var stackBlock = _step.value;
          var stateChanged = this.analyzeStackBlock(stackBlock, state);
          if (!stackBlock.ignoreState) {
            if (stackBlock.yields && !this.ignoreYields) stateChanged = state.clear() || stateChanged;
            if (stateChanged) {
              if (stackBlock.exitState) stackBlock.exitState.or(state);else stackBlock.exitState = state.clone();
              modified = true;
            }
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      return modified;
    }

    /**
     * @param {IntermediateStack} stack
     * @param {TypeState} state
     * @param {IntermediateStackBlock} block
     * @param {boolean} willReevaluateInputs
     * @returns {boolean}
     * @private
     */
  }, {
    key: "analyzeLoopedStack",
    value: function analyzeLoopedStack(stack, state, block, willReevaluateInputs) {
      var modified = false;
      if (block.yields && !this.ignoreYields) {
        modified = state.clear();
        if (willReevaluateInputs) {
          modified = this.analyzeInputs(block.inputs, state) || modified;
        }
        block.entryState = state.clone();
        block.exitState = state.clone();
        return this.analyzeStack(stack, state) || modified;
      }
      var iterations = 0;
      var keepLooping;
      do {
        // If we are stuck in an apparent infinite loop, give up and assume the worst.
        if (iterations > 10000) {
          log.error('analyzeLoopedStack stuck in likely infinite loop; quitting', block, state);
          modified = state.clear();
          block.entryState = state.clone();
          block.exitState = state.clone();
          modified = this.analyzeInputs(block.inputs, state) || modified;
          return this.analyzeStack(stack, state) || modified;
        }
        iterations++;
        var newState = state.clone();
        modified = this.analyzeStack(stack, newState) || modified;
        modified = (keepLooping = state.or(newState)) || modified;
        if (willReevaluateInputs) {
          modified = this.analyzeInputs(block.inputs, state) || modified;
        }
      } while (keepLooping);
      block.entryState = state.clone();
      return modified;
    }

    /**
     * @param {IntermediateInput} input
     * @param {TypeState} state
     * @returns {IntermediateInput}
     * @private
     */
  }, {
    key: "optimizeInput",
    value: function optimizeInput(input, state) {
      for (var inputKey in input.inputs) {
        var inputInput = input.inputs[inputKey];
        if (inputInput instanceof IntermediateInput) {
          input.inputs[inputKey] = this.optimizeInput(inputInput, state);
        }
      }
      switch (input.opcode) {
        case InputOpcode.CAST_BOOLEAN:
          {
            var targetType = input.inputs.target.type;
            if ((targetType & InputType.BOOLEAN) === targetType) {
              return input.inputs.target;
            }
            return input;
          }
        case InputOpcode.CAST_NUMBER:
          {
            var _targetType = input.inputs.target.type;
            if ((_targetType & InputType.NUMBER) === _targetType) {
              return input.inputs.target;
            }
            return input;
          }
        case InputOpcode.CAST_NUMBER_INDEX:
          {
            var _targetType2 = input.inputs.target.type;
            if ((_targetType2 & InputType.NUMBER_INDEX) === _targetType2) {
              return input.inputs.target;
            }
            return input;
          }
        case InputOpcode.CAST_NUMBER_OR_NAN:
          {
            var _targetType3 = input.inputs.target.type;
            if ((_targetType3 & InputType.NUMBER_OR_NAN) === _targetType3) {
              return input.inputs.target;
            }
            return input;
          }
        case InputOpcode.CAST_STRING:
          {
            var _targetType4 = input.inputs.target.type;
            if ((_targetType4 & InputType.STRING) === _targetType4) {
              return input.inputs.target;
            }
            return input;
          }
      }
      return input;
    }

    /**
     * @param {IntermediateStack?} stack
     * @param {TypeState} state The state of the project before this stack is run.
     * @private
     */
  }, {
    key: "optimizeStack",
    value: function optimizeStack(stack, state) {
      if (!stack) return;
      var _iterator2 = _createForOfIteratorHelper(stack.blocks),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var stackBlock = _step2.value;
          if (stackBlock.entryState) state = stackBlock.entryState;
          for (var inputKey in stackBlock.inputs) {
            var input = stackBlock.inputs[inputKey];
            if (input instanceof IntermediateInput) {
              stackBlock.inputs[inputKey] = this.optimizeInput(input, state);
            } else if (input instanceof IntermediateStack) {
              this.optimizeStack(input, state);
            }
          }
          if (stackBlock.exitState) {
            state = stackBlock.exitState;
          }
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }

    /**
     * @param {IntermediateScript} script
     * @param {Set<string>} alreadyOptimized
     * @private
     */
  }, {
    key: "optimizeScript",
    value: function optimizeScript(script, alreadyOptimized) {
      if (script.isProcedure) {
        if (alreadyOptimized.has(script.procedureCode)) {
          return;
        }
        alreadyOptimized.add(script.procedureCode);
      }
      var _iterator3 = _createForOfIteratorHelper(script.dependedProcedures),
        _step3;
      try {
        for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
          var procVariant = _step3.value;
          this.optimizeScript(this.ir.procedures[procVariant], alreadyOptimized);
        }
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
      this.exitState = null;
      var exitState = new TypeState();
      this.analyzeStack(script.stack, exitState);
      this.addPossibleExitState(exitState);
      script.cachedAnalysisEndState = this.exitState;
      this.optimizeStack(script.stack, new TypeState());
    }
  }, {
    key: "optimize",
    value: function optimize() {
      this.optimizeScript(this.ir.entry, new Set());
    }
  }]);
}();
module.exports = {
  IROptimizer: IROptimizer,
  TypeState: TypeState
};

/***/ }),

/***/ "../vm/src/compiler/jsexecute.js":
/*!***************************************!*\
  !*** ../vm/src/compiler/jsexecute.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// @ts-check
/**
 * @fileoverview Runtime for scripts generated by jsgen
 */

/* eslint-disable no-unused-vars */
/* eslint-disable prefer-template */
/* eslint-disable valid-jsdoc */
/* eslint-disable max-len */

var globalState = {
  Timer: __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js"),
  Cast: __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js"),
  log: __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js"),
  blockUtility: __webpack_require__(/*! ./compat-block-utility */ "../vm/src/compiler/compat-block-utility.js"),
  /** @type{import("../engine/thread")?} */
  thread: null
};
var baseRuntime = '';
var runtimeFunctions = {};

/**
 * Determine whether the current tick is likely stuck.
 * This implements similar functionality to the warp timer found in Scratch.
 * @returns {boolean} true if the current tick is likely stuck.
 */
baseRuntime += "let stuckCounter = 0;\nconst isStuck = () => {\n    // The real time is not checked on every call for performance.\n    stuckCounter++;\n    if (stuckCounter === 100) {\n        stuckCounter = 0;\n        return globalState.thread.target.runtime.sequencer.timer.timeElapsed() > 500;\n    }\n    return false;\n};";

/**
 * Start hats by opcode.
 * @param {string} requestedHat The opcode of the hat to start.
 * @param {*} optMatchFields Fields to match.
 * @returns {Array} A list of threads that were started.
 */
runtimeFunctions.startHats = "const startHats = (requestedHat, optMatchFields) => {\n    const thread = globalState.thread;\n    const threads = thread.target.runtime.startHats(requestedHat, optMatchFields);\n    return threads;\n}";

/**
 * Implements "thread waiting", where scripts are halted until all the scripts have finished executing.
 * @param {Array} threads The list of threads.
 */
runtimeFunctions.waitThreads = "const waitThreads = function*(threads) {\n    const thread = globalState.thread;\n    const runtime = thread.target.runtime;\n\n    while (true) {\n        // determine whether any threads are running\n        let anyRunning = false;\n        for (let i = 0; i < threads.length; i++) {\n            if (runtime.threads.indexOf(threads[i]) !== -1) {\n                anyRunning = true;\n                break;\n            }\n        }\n        if (!anyRunning) {\n            // all threads are finished, can resume\n            return;\n        }\n\n        let allWaiting = true;\n        for (let i = 0; i < threads.length; i++) {\n            if (!runtime.isWaitingThread(threads[i])) {\n                allWaiting = false;\n                break;\n            }\n        }\n        if (allWaiting) {\n            thread.status = 3; // STATUS_YIELD_TICK\n        }\n\n        yield;\n    }\n}";

/**
 * waitPromise: Wait until a Promise resolves or rejects before continuing.
 * @param {Promise} promise The promise to wait for.
 * @returns {*} the value that the promise resolves to, otherwise undefined if the promise rejects
 */

/**
 * isPromise: Determine if a value is Promise-like
 * @param {unknown} promise The value to check
 * @returns {promise is PromiseLike} True if the value is Promise-like (has a .then())
 */

/**
 * executeInCompatibilityLayer: Execute a scratch-vm primitive.
 * @param {*} inputs The inputs to pass to the block.
 * @param {function} blockFunction The primitive's function.
 * @param {boolean} useFlags Whether to set flags (hasResumedFromPromise)
 * @param {string} blockId Block ID to set on the emulated block utility.
 * @param {*|null} branchInfo Extra information object for CONDITIONAL and LOOP blocks. See createBranchInfo().
 * @returns {*} the value returned by the block, if any.
 */
runtimeFunctions.executeInCompatibilityLayer = "let hasResumedFromPromise = false;\nconst waitPromise = function*(promise) {\n    const thread = globalState.thread;\n    let returnValue;\n\n    // enter STATUS_PROMISE_WAIT and yield\n    // this will stop script execution until the promise handlers reset the thread status\n    // because promise handlers might execute immediately, configure thread.status here\n    thread.status = 1; // STATUS_PROMISE_WAIT\n\n    promise\n        .then(value => {\n            returnValue = value;\n            thread.status = 0; // STATUS_RUNNING\n        }, error => {\n            thread.status = 0; // STATUS_RUNNING\n            globalState.log.warn('Promise rejected in compiled script:', error);\n            returnValue = '' + error;\n            thread.status = 0; // STATUS_RUNNING\n        });\n\n    yield;\n\n    return returnValue;\n};\nconst isPromise = value => (\n    // see engine/execute.js\n    value !== null &&\n    typeof value === 'object' &&\n    typeof value.then === 'function'\n);\nconst executeInCompatibilityLayer = function*(inputs, blockFunction, isWarp, useFlags, blockId, branchInfo) {\n    const thread = globalState.thread;\n    const blockUtility = globalState.blockUtility;\n    const stackFrame = branchInfo ? branchInfo.stackFrame : {};\n\n    const finish = (returnValue) => {\n        if (branchInfo) {\n            if (typeof returnValue === 'undefined' && blockUtility._startedBranch) {\n                branchInfo.isLoop = blockUtility._startedBranch[1];\n                return blockUtility._startedBranch[0];\n            }\n            branchInfo.isLoop = branchInfo.defaultIsLoop;\n            return returnValue;\n        }\n        return returnValue;\n    };\n\n    const executeBlock = () => {\n        blockUtility.init(thread, blockId, stackFrame);\n        return blockFunction(inputs, blockUtility);\n    };\n\n    let returnValue = executeBlock();\n    if (isPromise(returnValue)) {\n        returnValue = finish(yield* waitPromise(returnValue));\n        if (useFlags) hasResumedFromPromise = true;\n        return returnValue;\n    }\n\n    if (thread.status === 1 /* STATUS_PROMISE_WAIT */ || thread.status === 4 /* STATUS_DONE */) {\n        // Something external is forcing us to stop\n        yield;\n        // Make up a return value because whatever is forcing us to stop can't specify one\n        return '';\n    }\n\n    while (thread.status === 2 /* STATUS_YIELD */ || thread.status === 3 /* STATUS_YIELD_TICK */) {\n        // Yielded threads will run next iteration.\n        if (thread.status === 2 /* STATUS_YIELD */) {\n            thread.status = 0; // STATUS_RUNNING\n            // Yield back to the event loop when stuck or not in warp mode.\n            if (!isWarp || isStuck()) {\n                yield;\n            }\n        } else {\n            // status is STATUS_YIELD_TICK, always yield to the event loop\n            yield;\n        }\n\n        returnValue = executeBlock();\n        if (isPromise(returnValue)) {\n            returnValue = finish(yield* waitPromise(returnValue));\n            if (useFlags) hasResumedFromPromise = true;\n            return returnValue;\n        }\n\n        if (thread.status === 1 /* STATUS_PROMISE_WAIT */ || thread.status === 4 /* STATUS_DONE */) {\n            yield;\n            return finish('');\n        }\n    }\n\n    // todo: do we have to do anything extra if status is STATUS_DONE?\n\n    return finish(returnValue);\n}";

/**
 * @param {boolean} isLoop True if the block is a LOOP by default (can be overridden by startBranch() call)
 * @returns {unknown} Branch info object for compatibility layer.
 */
runtimeFunctions.createBranchInfo = "const createBranchInfo = (isLoop) => ({\n    defaultIsLoop: isLoop,\n    isLoop: false,\n    branch: 0,\n    stackFrame: {}\n});";

/**
 * End the current script.
 */
runtimeFunctions.retire = "const retire = () => {\n    const thread = globalState.thread;\n    thread.target.runtime.sequencer.retireThread(thread);\n}";

/**
 * Converts NaN to zero. Used to match Scratch's string-to-number.
 * Unlike (x || 0), -0 stays as -0 and is not converted to 0.
 * This function needs to be written such that it's very easy for browsers to inline it.
 * @param {number} value A number. Might be NaN.
 * @returns {number} A number. Never NaN.
 */
runtimeFunctions.toNotNaN = "const toNotNaN = value => Number.isNaN(value) ? 0 : value";

/**
 * Scratch cast to boolean.
 * Similar to Cast.toBoolean()
 * @param {*} value The value to cast
 * @returns {boolean} The value cast to a boolean
 */
runtimeFunctions.toBoolean = "const toBoolean = value => {\n    if (typeof value === 'boolean') {\n        return value;\n    }\n    if (typeof value === 'string') {\n        if (value === '' || value === '0' || value.toLowerCase() === 'false') {\n            return false;\n        }\n        return true;\n    }\n    return !!value;\n}";

/**
 * If a number is very close to a whole number, round to that whole number.
 * @param {number} value Value to round
 * @returns {number} Rounded number or original number
 */
runtimeFunctions.limitPrecision = "const limitPrecision = value => {\n    const rounded = Math.round(value);\n    const delta = value - rounded;\n    return (Math.abs(delta) < 1e-9) ? rounded : value;\n}";

/**
 * Used internally by the compare family of function.
 * See similar method in cast.js.
 * @param {*} val A value that evaluates to 0 in JS string-to-number conversation such as empty string, 0, or tab.
 * @returns {boolean} True if the value should not be treated as the number zero.
 */
baseRuntime += "const isNotActuallyZero = val => {\n    if (typeof val !== 'string') return false;\n    for (let i = 0; i < val.length; i++) {\n        const code = val.charCodeAt(i);\n        if (code === 48 || code === 9) {\n            return false;\n        }\n    }\n    return true;\n};";

/**
 * Determine if two values are equal.
 * @param {*} v1 First value
 * @param {*} v2 Second value
 * @returns {boolean} true if v1 is equal to v2
 */
baseRuntime += "const compareEqualSlow = (v1, v2) => {\n    const n1 = +v1;\n    if (Number.isNaN(n1) || (n1 === 0 && isNotActuallyZero(v1))) return ('' + v1).toLowerCase() === ('' + v2).toLowerCase();\n    const n2 = +v2;\n    if (Number.isNaN(n2) || (n2 === 0 && isNotActuallyZero(v2))) return ('' + v1).toLowerCase() === ('' + v2).toLowerCase();\n    return n1 === n2;\n};\nconst compareEqual = (v1, v2) => (typeof v1 === 'number' && typeof v2 === 'number' && !Number.isNaN(v1) && !Number.isNaN(v2) || v1 === v2) ? v1 === v2 : compareEqualSlow(v1, v2);";

/**
 * Determine if one value is greater than another.
 * @param {*} v1 First value
 * @param {*} v2 Second value
 * @returns {boolean} true if v1 is greater than v2
 */
runtimeFunctions.compareGreaterThan = "const compareGreaterThanSlow = (v1, v2) => {\n    let n1 = +v1;\n    let n2 = +v2;\n    if (n1 === 0 && isNotActuallyZero(v1)) {\n        n1 = NaN;\n    } else if (n2 === 0 && isNotActuallyZero(v2)) {\n        n2 = NaN;\n    }\n    if (Number.isNaN(n1) || Number.isNaN(n2)) {\n        const s1 = ('' + v1).toLowerCase();\n        const s2 = ('' + v2).toLowerCase();\n        return s1 > s2;\n    }\n    return n1 > n2;\n};\nconst compareGreaterThan = (v1, v2) => typeof v1 === 'number' && typeof v2 === 'number' && !Number.isNaN(v1) ? v1 > v2 : compareGreaterThanSlow(v1, v2)";

/**
 * Determine if one value is less than another.
 * @param {*} v1 First value
 * @param {*} v2 Second value
 * @returns {boolean} true if v1 is less than v2
 */
runtimeFunctions.compareLessThan = "const compareLessThanSlow = (v1, v2) => {\n    let n1 = +v1;\n    let n2 = +v2;\n    if (n1 === 0 && isNotActuallyZero(v1)) {\n        n1 = NaN;\n    } else if (n2 === 0 && isNotActuallyZero(v2)) {\n        n2 = NaN;\n    }\n    if (Number.isNaN(n1) || Number.isNaN(n2)) {\n        const s1 = ('' + v1).toLowerCase();\n        const s2 = ('' + v2).toLowerCase();\n        return s1 < s2;\n    }\n    return n1 < n2;\n};\nconst compareLessThan = (v1, v2) => typeof v1 === 'number' && typeof v2 === 'number' && !Number.isNaN(v2) ? v1 < v2 : compareLessThanSlow(v1, v2)";

/**
 * Generate a random integer.
 * @param {number} low Lower bound
 * @param {number} high Upper bound
 * @returns {number} A random integer between low and high, inclusive.
 */
runtimeFunctions.randomInt = "const randomInt = (low, high) => low + Math.floor(Math.random() * ((high + 1) - low))";

/**
 * Generate a random float.
 * @param {number} low Lower bound
 * @param {number} high Upper bound
 * @returns {number} A random floating point number between low and high.
 */
runtimeFunctions.randomFloat = "const randomFloat = (low, high) => (Math.random() * (high - low)) + low";

/**
 * Create and start a timer.
 * @returns {Timer} A started timer
 */
runtimeFunctions.timer = "const timer = () => {\n    const t = new globalState.Timer({\n        now: () => globalState.thread.target.runtime.currentMSecs\n    });\n    t.start();\n    return t;\n}";

/**
 * Returns the amount of days since January 1st, 2000.
 * @returns {number} Days since 2000.
 */
// Date.UTC(2000, 0, 1) === 946684800000
// Hardcoding it is marginally faster
runtimeFunctions.daysSince2000 = "const daysSince2000 = () => (Date.now() - 946684800000) / (24 * 60 * 60 * 1000)";

/**
 * Determine distance to a sprite or point.
 * @param {string} menu The name of the sprite or location to find.
 * @returns {number} Distance to the point, or 10000 if it cannot be calculated.
 */
runtimeFunctions.distance = "const distance = menu => {\n    const thread = globalState.thread;\n    if (thread.target.isStage) return 10000;\n\n    let targetX = 0;\n    let targetY = 0;\n    if (menu === '_mouse_') {\n        targetX = thread.target.runtime.ioDevices.mouse.getScratchX();\n        targetY = thread.target.runtime.ioDevices.mouse.getScratchY();\n    } else {\n        const distTarget = thread.target.runtime.getSpriteTargetByName(menu);\n        if (!distTarget) return 10000;\n        targetX = distTarget.x;\n        targetY = distTarget.y;\n    }\n\n    const dx = thread.target.x - targetX;\n    const dy = thread.target.y - targetY;\n    return Math.sqrt((dx * dx) + (dy * dy));\n}";

/**
 * Convert a Scratch list index to a JavaScript list index.
 * "all" is not considered as a list index.
 * Similar to Cast.toListIndex()
 * @param {number} index Scratch list index.
 * @param {number} length Length of the list.
 * @returns {number} 0 based list index, or -1 if invalid.
 */
baseRuntime += "const listIndexSlow = (index, length) => {\n    if (index === 'last') {\n        return length - 1;\n    } else if (index === 'random' || index === 'any') {\n        if (length > 0) {\n            return (Math.random() * length) | 0;\n        }\n        return -1;\n    }\n    index = (+index || 0) | 0;\n    if (index < 1 || index > length) {\n        return -1;\n    }\n    return index - 1;\n};\nconst listIndex = (index, length) => {\n    if (typeof index !== 'number') {\n      return listIndexSlow(index, length);\n    }\n    index = index | 0;\n    return index < 1 || index > length ? -1 : index - 1;\n};";

/**
 * Get a value from a list.
 * @param {Array} list The list
 * @param {*} idx The 1-indexed index in the list.
 * @returns {*} The list item, otherwise empty string if it does not exist.
 */
runtimeFunctions.listGet = "const listGet = (list, idx) => {\n    const index = listIndex(idx, list.length);\n    if (index === -1) {\n        return '';\n    }\n    return list[index];\n}";

/**
 * Replace a value in a list.
 * @param {import('../engine/variable')} list The list
 * @param {*} idx List index, Scratch style.
 * @param {*} value The new value.
 */
runtimeFunctions.listReplace = "const listReplace = (list, idx, value) => {\n    const index = listIndex(idx, list.value.length);\n    if (index === -1) {\n        return;\n    }\n    list.value[index] = value;\n    list._monitorUpToDate = false;\n}";

/**
 * Insert a value in a list.
 * @param {import('../engine/variable')} list The list.
 * @param {*} idx The Scratch index in the list.
 * @param {*} value The value to insert.
 */
runtimeFunctions.listInsert = "const listInsert = (list, idx, value) => {\n    const index = listIndex(idx, list.value.length + 1);\n    if (index === -1) {\n        return;\n    }\n    list.value.splice(index, 0, value);\n    list._monitorUpToDate = false;\n}";

/**
 * Delete a value from a list.
 * @param {import('../engine/variable')} list The list.
 * @param {*} idx The Scratch index in the list.
 */
runtimeFunctions.listDelete = "const listDelete = (list, idx) => {\n    if (idx === 'all') {\n        list.value = [];\n        return;\n    }\n    const index = listIndex(idx, list.value.length);\n    if (index === -1) {\n        return;\n    }\n    list.value.splice(index, 1);\n    list._monitorUpToDate = false;\n}";

/**
 * Return whether a list contains a value.
 * @param {import('../engine/variable')} list The list.
 * @param {*} item The value to search for.
 * @returns {boolean} True if the list contains the item
 */
runtimeFunctions.listContains = "const listContains = (list, item) => {\n    // TODO: evaluate whether indexOf is worthwhile here\n    if (list.value.indexOf(item) !== -1) {\n        return true;\n    }\n    for (let i = 0; i < list.value.length; i++) {\n        if (compareEqual(list.value[i], item)) {\n            return true;\n        }\n    }\n    return false;\n}";

/**
 * Find the 1-indexed index of an item in a list.
 * @param {import('../engine/variable')} list The list.
 * @param {*} item The item to search for
 * @returns {number} The 1-indexed index of the item in the list, otherwise 0
 */
runtimeFunctions.listIndexOf = "const listIndexOf = (list, item) => {\n    for (let i = 0; i < list.value.length; i++) {\n        if (compareEqual(list.value[i], item)) {\n            return i + 1;\n        }\n    }\n    return 0;\n}";

/**
 * Get the stringified form of a list.
 * @param {import('../engine/variable')} list The list.
 * @returns {string} Stringified form of the list.
 */
runtimeFunctions.listContents = "const listContents = list => {\n    for (let i = 0; i < list.value.length; i++) {\n        const listItem = list.value[i];\n        // this is an intentional break from what scratch 3 does to address our automatic string -> number conversions\n        // it fixes more than it breaks\n        if ((listItem + '').length !== 1) {\n            return list.value.join(' ');\n        }\n    }\n    return list.value.join('');\n}";

/**
 * Convert a color to an RGB list
 * @param {*} color The color value to convert
 * @return {Array.<number>} [r,g,b], values between 0-255.
 */
runtimeFunctions.colorToList = "const colorToList = color => globalState.Cast.toRgbColorList(color)";

/**
 * Implements Scratch modulo (floored division instead of truncated division)
 * @param {number} n Number
 * @param {number} modulus Base
 * @returns {number} n % modulus (floored division)
 */
runtimeFunctions.mod = "const mod = (n, modulus) => {\n    let result = n % modulus;\n    if (result / modulus < 0) result += modulus;\n    return result;\n}";

/**
 * Implements Scratch tangent.
 * @param {number} angle Angle in degrees.
 * @returns {number} value of tangent or Infinity or -Infinity
 */
runtimeFunctions.tan = "const tan = (angle) => {\n    switch (angle % 360) {\n    case -270: case 90: return Infinity;\n    case -90: case 270: return -Infinity;\n    }\n    return Math.round(Math.tan((Math.PI * angle) / 180) * 1e10) / 1e10;\n}";

/**
 * @param {function} callback The function to run
 * @param {...unknown} args The arguments to pass to the function
 * @returns {unknown} A generator that will yield once then call the function and return its value.
 */
runtimeFunctions.yieldThenCall = "const yieldThenCall = function* (callback, ...args) {\n    yield;\n    return callback(...args);\n}";

/**
 * @param {function} callback The generator function to run
 * @param {...unknown} args The arguments to pass to the generator function
 * @returns {unknown} A generator that will yield once then delegate to the generator function and return its value.
 */
runtimeFunctions.yieldThenCallGenerator = "const yieldThenCallGenerator = function* (callback, ...args) {\n    yield;\n    return yield* callback(...args);\n}";

/**
 * Step a compiled thread.
 * @param {import("../engine/thread")} thread The thread to step.
 */
var execute = function execute(thread) {
  globalState.thread = thread;
  thread.generator.next();
};
var threadStack = [];
var saveGlobalState = function saveGlobalState() {
  threadStack.push(globalState.thread);
};
var restoreGlobalState = function restoreGlobalState() {
  globalState.thread = threadStack.pop();
};
var insertRuntime = function insertRuntime(source) {
  var result = baseRuntime;
  for (var _i = 0, _Object$keys = Object.keys(runtimeFunctions); _i < _Object$keys.length; _i++) {
    var functionName = _Object$keys[_i];
    if (source.includes(functionName)) {
      result += "".concat(runtimeFunctions[functionName], ";");
    }
  }
  result += "return ".concat(source);
  return result;
};

/**
 * Evaluate arbitrary JS in the context of the runtime.
 * @param {string} source The string to evaluate.
 * @returns {*} The result of evaluating the string.
 */
var scopedEval = function scopedEval(source) {
  var withRuntime = insertRuntime(source);
  try {
    return new Function('globalState', withRuntime)(globalState);
  } catch (e) {
    globalState.log.error('was unable to compile script', withRuntime);
    throw e;
  }
};
execute.scopedEval = scopedEval;
execute.runtimeFunctions = runtimeFunctions;
execute.saveGlobalState = saveGlobalState;
execute.restoreGlobalState = restoreGlobalState;
module.exports = execute;

/***/ }),

/***/ "../vm/src/compiler/jsgen.js":
/*!***********************************!*\
  !*** ../vm/src/compiler/jsgen.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
// @ts-check

var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var BlockType = __webpack_require__(/*! ../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var VariablePool = __webpack_require__(/*! ./variable-pool */ "../vm/src/compiler/variable-pool.js");
var jsexecute = __webpack_require__(/*! ./jsexecute */ "../vm/src/compiler/jsexecute.js");
var environment = __webpack_require__(/*! ./environment */ "../vm/src/compiler/environment.js");
var _require = __webpack_require__(/*! ./enums.js */ "../vm/src/compiler/enums.js"),
  StackOpcode = _require.StackOpcode,
  InputOpcode = _require.InputOpcode,
  InputType = _require.InputType;
var oldCompilerCompatibility = __webpack_require__(/*! ./old-compiler-compatibility.js */ "../vm/src/compiler/old-compiler-compatibility.js");

// These imports are used by jsdoc comments but eslint doesn't know that
/* eslint-disable no-unused-vars */
var _require2 = __webpack_require__(/*! ./intermediate */ "../vm/src/compiler/intermediate.js"),
  IntermediateStackBlock = _require2.IntermediateStackBlock,
  IntermediateInput = _require2.IntermediateInput,
  IntermediateStack = _require2.IntermediateStack,
  IntermediateScript = _require2.IntermediateScript,
  IntermediateRepresentation = _require2.IntermediateRepresentation;
/* eslint-enable no-unused-vars */

/**
 * @fileoverview Convert intermediate representations to JavaScript functions.
 */

/* eslint-disable max-len */
/* eslint-disable prefer-template */

var sanitize = function sanitize(string) {
  if (typeof string !== 'string') {
    log.warn("sanitize got unexpected type: ".concat(_typeof(string)));
    string = '' + string;
  }
  return JSON.stringify(string).slice(1, -1);
};

// Pen-related constants
var PEN_EXT = 'runtime.ext_pen';
var PEN_STATE = "".concat(PEN_EXT, "._getPenState(target)");

/**
 * Variable pool used for factory function names.
 */
var factoryNameVariablePool = new VariablePool('factory');

/**
 * Variable pool used for generated functions (non-generator)
 */
var functionNameVariablePool = new VariablePool('fun');

/**
 * Variable pool used for generated generator functions.
 */
var generatorNameVariablePool = new VariablePool('gen');
var isSafeInputForEqualsOptimization = function isSafeInputForEqualsOptimization(input, other) {
  // Only optimize constants
  if (input.opcode !== InputOpcode.CONSTANT) return false;
  // Only optimize when the constant can always be thought of as a number
  if (input.isAlwaysType(InputType.NUMBER) || input.isAlwaysType(InputType.STRING_NUM)) {
    if (other.isSometimesType(InputType.NUMBER_NAN | InputType.STRING_NAN | InputType.BOOLEAN_INTERPRETABLE)) {
      // Never optimize 0 if the other input can be NaN, '' or a boolean.
      // eg. if '< 0 = "" >' was optimized it would turn into `0 === +""`,
      //  which would be true even though Scratch would return false.
      return +input.inputs.value !== 0;
    }
    return true;
  }
  return false;
};

/**
 * A frame contains some information about the current substack being compiled.
 */
var Frame = /*#__PURE__*/_createClass(function Frame(isLoop) {
  _classCallCheck(this, Frame);
  /**
   * Whether the current stack runs in a loop (while, for)
   * @type {boolean}
   * @readonly
   */
  this.isLoop = isLoop;

  /**
   * Whether the current block is the last block in the stack.
   * @type {boolean}
   */
  this.isLastBlock = false;
});
var JSGenerator = /*#__PURE__*/function () {
  /**
   * @param {IntermediateScript} script
   * @param {IntermediateRepresentation} ir
   * @param {import("../sprites/rendered-target")} target
   * @param {object[]} [offloadPlans]
   */
  function JSGenerator(script, ir, target) {
    var offloadPlans = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
    _classCallCheck(this, JSGenerator);
    this.script = script;
    this.ir = ir;
    this.target = target;
    this.source = '';
    this.isWarp = script.isWarp;
    this.isProcedure = script.isProcedure;
    this.warpTimer = script.warpTimer;

    /**
     * Stack of frames, most recent is last item.
     * @type {Frame[]}
     */
    this.frames = [];

    /**
     * The current Frame.
     * @type {Frame?}
     */
    this.currentFrame = null;
    this.localVariables = new VariablePool('a');
    this._setupVariablesPool = new VariablePool('b');
    this._setupVariables = {};
    this.descendedIntoModulo = false;
    this.isInHat = false;
    this.offloadPlans = offloadPlans;
    this.debug = this.target.runtime.debug;
    this.oldCompilerStub = new oldCompilerCompatibility.JSGeneratorStub(this);
  }

  /**
   * Enter a new frame
   * @param {Frame} frame New frame.
   */
  return _createClass(JSGenerator, [{
    key: "pushFrame",
    value: function pushFrame(frame) {
      this.frames.push(frame);
      this.currentFrame = frame;
    }

    /**
     * Exit the current frame
     */
  }, {
    key: "popFrame",
    value: function popFrame() {
      this.frames.pop();
      this.currentFrame = this.frames[this.frames.length - 1];
    }

    /**
     * @returns {boolean} true if the current block is the last command of a loop
     */
  }, {
    key: "isLastBlockInLoop",
    value: function isLastBlockInLoop() {
      for (var i = this.frames.length - 1; i >= 0; i--) {
        var frame = this.frames[i];
        if (!frame.isLastBlock) {
          return false;
        }
        if (frame.isLoop) {
          return true;
        }
      }
      return false;
    }

    /**
     * @param {IntermediateInput} block Input node to compile.
     * @returns {string} Compiled input.
     */
  }, {
    key: "descendInput",
    value: function descendInput(block) {
      var node = block.inputs;
      switch (block.opcode) {
        case InputOpcode.NOP:
          return "\"\"";
        case InputOpcode.PROCEDURE_ARGUMENT:
          return "p".concat(node.index);
        case InputOpcode.ADDON_CALL:
          return "(".concat(this.descendAddonCall(node), ")");
        case InputOpcode.CAST_BOOLEAN:
          return "toBoolean(".concat(this.descendInput(node.target), ")");
        case InputOpcode.CAST_NUMBER:
          if (node.target.isAlwaysType(InputType.BOOLEAN_INTERPRETABLE)) {
            return "(+".concat(this.descendInput(node.target.toType(InputType.BOOLEAN)), ")");
          }
          if (node.target.isAlwaysType(InputType.NUMBER_OR_NAN)) {
            return "toNotNaN(".concat(this.descendInput(node.target), ")");
          }
          return "toNotNaN(+".concat(this.descendInput(node.target), ")");
        case InputOpcode.CAST_NUMBER_OR_NAN:
          return "(+".concat(this.descendInput(node.target), ")");
        case InputOpcode.CAST_NUMBER_INDEX:
          return "(".concat(this.descendInput(node.target.toType(InputType.NUMBER_OR_NAN)), " | 0)");
        case InputOpcode.CAST_STRING:
          return "(\"\" + ".concat(this.descendInput(node.target), ")");
        case InputOpcode.CAST_COLOR:
          return "colorToList(".concat(this.descendInput(node.target), ")");
        case InputOpcode.COMPATIBILITY_LAYER:
          // Compatibility layer inputs never use flags.
          return "(".concat(this.generateCompatibilityLayerCall(node, false), ")");
        case InputOpcode.OLD_COMPILER_COMPATIBILITY_LAYER:
          return this.oldCompilerStub.descendInputFromNewCompiler(block);
        case InputOpcode.CONSTANT:
          if (block.isAlwaysType(InputType.NUMBER)) {
            if (typeof node.value !== 'number') throw new Error("JS: '".concat(block.type, "' type constant had ").concat(_typeof(node.value), " type value. Expected number."));
            if (Object.is(node.value, -0)) return '-0';
            return node.value.toString();
          } else if (block.isAlwaysType(InputType.BOOLEAN)) {
            if (typeof node.value !== 'boolean') throw new Error("JS: '".concat(block.type, "' type constant had ").concat(_typeof(node.value), " type value. Expected boolean."));
            return node.value.toString();
          } else if (block.isAlwaysType(InputType.COLOR)) {
            if (!Array.isArray(node.value)) throw new Error("JS: '".concat(block.type, "' type constant was not an array."));
            if (node.value.length !== 3) throw new Error("JS: '".concat(block.type, "' type constant had an array of length '").concat(node.value.length, "'. Expected 3."));
            for (var i = 0; i < 3; i++) {
              if (typeof node.value[i] !== 'number') {
                throw new Error("JS: '".concat(block.type, "' type constant element ").concat(i, " had a value of type '").concat(node.value[i], "'. Expected number."));
              }
            }
            return "[".concat(node.value[0], ",").concat(node.value[1], ",").concat(node.value[2], "]");
          } else if (block.isSometimesType(InputType.STRING)) {
            return "\"".concat(sanitize(node.value.toString()), "\"");
          }
          throw new Error("JS: Unknown constant input type '".concat(block.type, "'."));
        case InputOpcode.SENSING_KEY_DOWN:
          return "runtime.ioDevices.keyboard.getKeyIsDown(".concat(this.descendInput(node.key), ")");
        case InputOpcode.LIST_CONTAINS:
          return "listContains(".concat(this.referenceVariable(node.list), ", ").concat(this.descendInput(node.item), ")");
        case InputOpcode.LIST_CONTENTS:
          return "listContents(".concat(this.referenceVariable(node.list), ")");
        case InputOpcode.LIST_GET:
          {
            if (environment.supportsNullishCoalescing) {
              if (node.index.isAlwaysType(InputType.NUMBER_INTERPRETABLE | InputType.NUMBER_NAN)) {
                return "(".concat(this.referenceVariable(node.list), ".value[").concat(this.descendInput(node.index.toType(InputType.NUMBER_INDEX)), " - 1] ?? \"\")");
              }
              if (node.index.isConstant('last')) {
                return "(".concat(this.referenceVariable(node.list), ".value[").concat(this.referenceVariable(node.list), ".value.length - 1] ?? \"\")");
              }
            }
            return "listGet(".concat(this.referenceVariable(node.list), ".value, ").concat(this.descendInput(node.index), ")");
          }
        case InputOpcode.LIST_INDEX_OF:
          return "listIndexOf(".concat(this.referenceVariable(node.list), ", ").concat(this.descendInput(node.item), ")");
        case InputOpcode.LIST_LENGTH:
          return "".concat(this.referenceVariable(node.list), ".value.length");
        case InputOpcode.LOOKS_SIZE_GET:
          return 'Math.round(target.size)';
        case InputOpcode.LOOKS_BACKDROP_NAME:
          return 'stage.getCostumes()[stage.currentCostume].name';
        case InputOpcode.LOOKS_BACKDROP_NUMBER:
          return '(stage.currentCostume + 1)';
        case InputOpcode.LOOKS_COSTUME_NAME:
          return 'target.getCostumes()[target.currentCostume].name';
        case InputOpcode.LOOKS_COSTUME_NUMBER:
          return '(target.currentCostume + 1)';
        case InputOpcode.MOTION_DIRECTION_GET:
          return 'target.direction';
        case InputOpcode.MOTION_X_GET:
          return 'limitPrecision(target.x)';
        case InputOpcode.MOTION_Y_GET:
          return 'limitPrecision(target.y)';
        case InputOpcode.SENSING_MOUSE_DOWN:
          return 'runtime.ioDevices.mouse.getIsDown()';
        case InputOpcode.SENSING_MOUSE_X:
          return 'runtime.ioDevices.mouse.getScratchX()';
        case InputOpcode.SENSING_MOUSE_Y:
          return 'runtime.ioDevices.mouse.getScratchY()';
        case InputOpcode.OP_ABS:
          return "Math.abs(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_ACOS:
          return "((Math.acos(".concat(this.descendInput(node.value), ") * 180) / Math.PI)");
        case InputOpcode.OP_ADD:
          return "(".concat(this.descendInput(node.left), " + ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_AND:
          return "(".concat(this.descendInput(node.left), " && ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_ASIN:
          return "((Math.asin(".concat(this.descendInput(node.value), ") * 180) / Math.PI)");
        case InputOpcode.OP_ATAN:
          return "((Math.atan(".concat(this.descendInput(node.value), ") * 180) / Math.PI)");
        case InputOpcode.OP_CEILING:
          return "Math.ceil(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_CONTAINS:
          return "(".concat(this.descendInput(node.string), ".toLowerCase().indexOf(").concat(this.descendInput(node.contains), ".toLowerCase()) !== -1)");
        case InputOpcode.OP_COS:
          return "(Math.round(Math.cos((Math.PI * ".concat(this.descendInput(node.value), ") / 180) * 1e10) / 1e10)");
        case InputOpcode.OP_DIVIDE:
          return "(".concat(this.descendInput(node.left), " / ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_EQUALS:
          {
            var left = node.left;
            var right = node.right;

            // When either operand is known to never be a number, only use string comparison to avoid all number parsing.
            if (!left.isSometimesType(InputType.NUMBER_INTERPRETABLE) || !right.isSometimesType(InputType.NUMBER_INTERPRETABLE)) {
              return "(".concat(this.descendInput(left.toType(InputType.STRING)), ".toLowerCase() === ").concat(this.descendInput(right.toType(InputType.STRING)), ".toLowerCase())");
            }
            // When both operands are known to be numbers, we can use ===
            if (left.isAlwaysType(InputType.NUMBER_INTERPRETABLE) && right.isAlwaysType(InputType.NUMBER_INTERPRETABLE)) {
              return "(".concat(this.descendInput(left.toType(InputType.NUMBER)), " === ").concat(this.descendInput(right.toType(InputType.NUMBER)), ")");
            }
            // In certain conditions, we can use === when one of the operands is known to be a safe number.
            if (isSafeInputForEqualsOptimization(left, right) || isSafeInputForEqualsOptimization(right, left)) {
              return "(".concat(this.descendInput(left.toType(InputType.NUMBER)), " === ").concat(this.descendInput(right.toType(InputType.NUMBER)), ")");
            }
            // No compile-time optimizations possible - use fallback method.
            return "compareEqual(".concat(this.descendInput(left), ", ").concat(this.descendInput(right), ")");
          }
        case InputOpcode.OP_POW_E:
          return "Math.exp(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_FLOOR:
          return "Math.floor(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_GREATER:
          {
            var _left = node.left;
            var _right = node.right;
            // When the left operand is a number and the right operand is a number or NaN, we can use >
            if (_left.isAlwaysType(InputType.NUMBER_INTERPRETABLE) && _right.isAlwaysType(InputType.NUMBER_INTERPRETABLE | InputType.NUMBER_NAN)) {
              return "(".concat(this.descendInput(_left.toType(InputType.NUMBER)), " > ").concat(this.descendInput(_right.toType(InputType.NUMBER_OR_NAN)), ")");
            }
            // When the left operand is a number or NaN and the right operand is a number, we can negate <=
            if (_left.isAlwaysType(InputType.NUMBER_INTERPRETABLE | InputType.NUMBER_NAN) && _right.isAlwaysType(InputType.NUMBER_INTERPRETABLE)) {
              return "!(".concat(this.descendInput(_left.toType(InputType.NUMBER_OR_NAN)), " <= ").concat(this.descendInput(_right.toType(InputType.NUMBER)), ")");
            }
            // When either operand is known to never be a number, avoid all number parsing.
            if (!_left.isSometimesType(InputType.NUMBER_INTERPRETABLE) || !_right.isSometimesType(InputType.NUMBER_INTERPRETABLE)) {
              return "(".concat(this.descendInput(_left.toType(InputType.STRING)), ".toLowerCase() > ").concat(this.descendInput(_right.toType(InputType.STRING)), ".toLowerCase())");
            }
            // No compile-time optimizations possible - use fallback method.
            return "compareGreaterThan(".concat(this.descendInput(_left), ", ").concat(this.descendInput(_right), ")");
          }
        case InputOpcode.OP_JOIN:
          return "(".concat(this.descendInput(node.left), " + ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_LENGTH:
          return "".concat(this.descendInput(node.string), ".length");
        case InputOpcode.OP_LESS:
          {
            var _left2 = node.left;
            var _right2 = node.right;
            // When the left operand is a number or NaN and the right operand is a number, we can use <
            if (_left2.isAlwaysType(InputType.NUMBER_INTERPRETABLE | InputType.NUMBER_NAN) && _right2.isAlwaysType(InputType.NUMBER_INTERPRETABLE)) {
              return "(".concat(this.descendInput(_left2.toType(InputType.NUMBER_OR_NAN)), " < ").concat(this.descendInput(_right2.toType(InputType.NUMBER)), ")");
            }
            // When the left operand is a number and the right operand is a number or NaN, we can negate >=
            if (_left2.isAlwaysType(InputType.NUMBER_INTERPRETABLE) && _right2.isAlwaysType(InputType.NUMBER_INTERPRETABLE | InputType.NUMBER_NAN)) {
              return "!(".concat(this.descendInput(_left2.toType(InputType.NUMBER)), " >= ").concat(this.descendInput(_right2.toType(InputType.NUMBER_OR_NAN)), ")");
            }
            // When either operand is known to never be a number, avoid all number parsing.
            if (!_left2.isSometimesType(InputType.NUMBER_INTERPRETABLE) || !_right2.isSometimesType(InputType.NUMBER_INTERPRETABLE)) {
              return "(".concat(this.descendInput(_left2.toType(InputType.STRING)), ".toLowerCase() < ").concat(this.descendInput(_right2.toType(InputType.STRING)), ".toLowerCase())");
            }
            // No compile-time optimizations possible - use fallback method.
            return "compareLessThan(".concat(this.descendInput(_left2), ", ").concat(this.descendInput(_right2), ")");
          }
        case InputOpcode.OP_LETTER_OF:
          return "((".concat(this.descendInput(node.string), ")[").concat(this.descendInput(node.letter), " - 1] || \"\")");
        case InputOpcode.OP_LOG_E:
          return "Math.log(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_LOG_10:
          return "(Math.log(".concat(this.descendInput(node.value), ") / Math.LN10)");
        case InputOpcode.OP_MOD:
          this.descendedIntoModulo = true;
          return "mod(".concat(this.descendInput(node.left), ", ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_MULTIPLY:
          return "(".concat(this.descendInput(node.left), " * ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_NOT:
          return "!".concat(this.descendInput(node.operand));
        case InputOpcode.OP_OR:
          return "(".concat(this.descendInput(node.left), " || ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_RANDOM:
          if (node.useInts) {
            return "randomInt(".concat(this.descendInput(node.low), ", ").concat(this.descendInput(node.high), ")");
          }
          if (node.useFloats) {
            return "randomFloat(".concat(this.descendInput(node.low), ", ").concat(this.descendInput(node.high), ")");
          }
          return "runtime.ext_scratch3_operators._random(".concat(this.descendInput(node.low), ", ").concat(this.descendInput(node.high), ")");
        case InputOpcode.OP_ROUND:
          return "Math.round(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_SIN:
          return "(Math.round(Math.sin((Math.PI * ".concat(this.descendInput(node.value), ") / 180) * 1e10) / 1e10)");
        case InputOpcode.OP_SQRT:
          return "Math.sqrt(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_SUBTRACT:
          return "(".concat(this.descendInput(node.left), " - ").concat(this.descendInput(node.right), ")");
        case InputOpcode.OP_TAN:
          return "tan(".concat(this.descendInput(node.value), ")");
        case InputOpcode.OP_POW_10:
          return "(10 ** ".concat(this.descendInput(node.value), ")");
        case InputOpcode.PROCEDURE_CALL:
          {
            var procedureCode = node.code;
            var procedureVariant = node.variant;
            var procedureData = this.ir.procedures[procedureVariant];
            if (procedureData.stack === null) {
              // TODO still need to evaluate arguments for side effects
              return '""';
            }

            // Recursion makes this complicated because:
            //  - We need to yield *between* each call in the same command block
            //  - We need to evaluate arguments *before* that yield happens

            var procedureReference = "thread.procedures[\"".concat(sanitize(procedureVariant), "\"]");
            var args = [];
            var _iterator = _createForOfIteratorHelper(node.arguments),
              _step;
            try {
              for (_iterator.s(); !(_step = _iterator.n()).done;) {
                var input = _step.value;
                args.push(this.descendInput(input));
              }
            } catch (err) {
              _iterator.e(err);
            } finally {
              _iterator.f();
            }
            var joinedArgs = args.join(',');
            var yieldForRecursion = !this.isWarp && procedureCode === this.script.procedureCode;
            var yieldForHat = this.isInHat;
            if (yieldForRecursion || yieldForHat) {
              var runtimeFunction = procedureData.yields ? 'yieldThenCallGenerator' : 'yieldThenCall';
              return "(yield* ".concat(runtimeFunction, "(").concat(procedureReference, ", ").concat(joinedArgs, "))");
            }
            if (procedureData.yields) {
              return "(yield* ".concat(procedureReference, "(").concat(joinedArgs, "))");
            }
            return "".concat(procedureReference, "(").concat(joinedArgs, ")");
          }
        case InputOpcode.SENSING_ANSWER:
          return "runtime.ext_scratch3_sensing._answer";
        case InputOpcode.SENSING_COLOR_TOUCHING_COLOR:
          return "target.colorIsTouchingColor(".concat(this.descendInput(node.target), ", ").concat(this.descendInput(node.mask), ")");
        case InputOpcode.SENSING_TIME_DATE:
          return "(new Date().getDate())";
        case InputOpcode.SENSING_TIME_WEEKDAY:
          return "(new Date().getDay() + 1)";
        case InputOpcode.SENSING_TIME_DAYS_SINCE_2000:
          return 'daysSince2000()';
        case InputOpcode.SENSING_DISTANCE:
          // TODO: on stages, this can be computed at compile time
          return "distance(".concat(this.descendInput(node.target), ")");
        case InputOpcode.SENSING_TIME_HOUR:
          return "(new Date().getHours())";
        case InputOpcode.SENSING_TIME_MINUTE:
          return "(new Date().getMinutes())";
        case InputOpcode.SENSING_TIME_MONTH:
          return "(new Date().getMonth() + 1)";
        case InputOpcode.SENSING_OF:
          return "runtime.ext_scratch3_sensing.getAttributeOf({OBJECT: ".concat(this.descendInput(node.object), ", PROPERTY: \"").concat(sanitize(node.property), "\" })");
        case InputOpcode.SENSING_OF_VOLUME:
          {
            var targetRef = this.descendTargetReference(node.object);
            return "(".concat(targetRef, " ? ").concat(targetRef, ".volume : 0)");
          }
        case InputOpcode.SENSING_OF_BACKDROP_NUMBER:
          return "(stage.currentCostume + 1)";
        case InputOpcode.SENSING_OF_BACKDROP_NAME:
          return "stage.getCostumes()[stage.currentCostume].name";
        case InputOpcode.SENSING_OF_POS_X:
          {
            var _targetRef = this.descendTargetReference(node.object);
            return "(".concat(_targetRef, " ? ").concat(_targetRef, ".x : 0)");
          }
        case InputOpcode.SENSING_OF_POS_Y:
          {
            var _targetRef2 = this.descendTargetReference(node.object);
            return "(".concat(_targetRef2, " ? ").concat(_targetRef2, ".y : 0)");
          }
        case InputOpcode.SENSING_OF_DIRECTION:
          {
            var _targetRef3 = this.descendTargetReference(node.object);
            return "(".concat(_targetRef3, " ? ").concat(_targetRef3, ".direction : 0)");
          }
        case InputOpcode.SENSING_OF_COSTUME_NUMBER:
          {
            var _targetRef4 = this.descendTargetReference(node.object);
            return "(".concat(_targetRef4, " ? ").concat(_targetRef4, ".currentCostume + 1 : 0)");
          }
        case InputOpcode.SENSING_OF_COSTUME_NAME:
          {
            var _targetRef5 = this.descendTargetReference(node.object);
            return "(".concat(_targetRef5, " ? ").concat(_targetRef5, ".getCostumes()[").concat(_targetRef5, ".currentCostume].name : 0)");
          }
        case InputOpcode.SENSING_OF_SIZE:
          {
            var _targetRef6 = this.descendTargetReference(node.object);
            return "(".concat(_targetRef6, " ? ").concat(_targetRef6, ".size : 0)");
          }
        case InputOpcode.SENSING_OF_VAR:
          {
            var _targetRef7 = this.descendTargetReference(node.object);
            var varRef = this.evaluateOnce("".concat(_targetRef7, " && ").concat(_targetRef7, ".lookupVariableByNameAndType(\"").concat(sanitize(node.property), "\", \"\", true)"));
            return "(".concat(varRef, " ? ").concat(varRef, ".value : 0)");
          }
        case InputOpcode.SENSING_TIME_SECOND:
          return "(new Date().getSeconds())";
        case InputOpcode.SENSING_TOUCHING_OBJECT:
          return "target.isTouchingObject(".concat(this.descendInput(node.object), ")");
        case InputOpcode.SENSING_TOUCHING_COLOR:
          return "target.isTouchingColor(".concat(this.descendInput(node.color), ")");
        case InputOpcode.SENSING_USERNAME:
          return 'runtime.ioDevices.userData.getUsername()';
        case InputOpcode.SENSING_TIME_YEAR:
          return "(new Date().getFullYear())";
        case InputOpcode.SENSING_TIMER_GET:
          return 'runtime.ioDevices.clock.projectTimer()';
        case InputOpcode.CONTROL_COUNTER:
          return 'runtime.ext_scratch3_control._counter';
        case InputOpcode.TW_KEY_LAST_PRESSED:
          return 'runtime.ioDevices.keyboard.getLastKeyPressed()';
        case InputOpcode.VAR_GET:
          return "".concat(this.referenceVariable(node.variable), ".value");
        default:
          log.warn("JS: Unknown input: ".concat(block.opcode), node);
          throw new Error("JS: Unknown input: ".concat(block.opcode));
      }
    }

    /**
     * @param {IntermediateStackBlock} block Stacked block to compile.
     */
  }, {
    key: "descendStackedBlock",
    value: function descendStackedBlock(block) {
      var node = block.inputs;
      switch (block.opcode) {
        case StackOpcode.ADDON_CALL:
          {
            this.source += "".concat(this.descendAddonCall(node), ";\n");
            break;
          }
        case StackOpcode.COMPATIBILITY_LAYER:
          {
            // If the last command in a loop returns a promise, immediately continue to the next iteration.
            // If you don't do this, the loop effectively yields twice per iteration and will run at half-speed.
            var isLastInLoop = this.isLastBlockInLoop();
            var blockType = node.blockType;
            if (blockType === BlockType.COMMAND || blockType === BlockType.HAT) {
              this.source += "".concat(this.generateCompatibilityLayerCall(node, isLastInLoop), ";\n");
            } else if (blockType === BlockType.CONDITIONAL || blockType === BlockType.LOOP) {
              var branchVariable = this.localVariables.next();
              this.source += "const ".concat(branchVariable, " = createBranchInfo(").concat(blockType === BlockType.LOOP, ");\n");
              this.source += "while (".concat(branchVariable, ".branch = +(").concat(this.generateCompatibilityLayerCall(node, false, branchVariable), ")) {\n");
              this.source += "switch (".concat(branchVariable, ".branch) {\n");
              for (var index in node.substacks) {
                this.source += "case ".concat(+index, ": {\n");
                this.descendStack(node.substacks[index], new Frame(false));
                this.source += "break;\n";
                this.source += "}\n"; // close case
              }
              this.source += '}\n'; // close switch
              this.source += "if (!".concat(branchVariable, ".isLoop) break;\n");
              this.yieldLoop();
              this.source += '}\n'; // close while
            } else {
              throw new Error("Unknown block type: ".concat(blockType));
            }
            if (isLastInLoop) {
              this.source += 'if (hasResumedFromPromise) {hasResumedFromPromise = false;continue;}\n';
            }
            break;
          }
        case InputOpcode.OLD_COMPILER_COMPATIBILITY_LAYER:
          return this.oldCompilerStub.descendStackedBlockFromNewCompiler(block);
        case StackOpcode.HAT_EDGE:
          this.isInHat = true;
          this.source += '{\n';
          // For exact Scratch parity, evaluate the input before checking old edge state.
          // Can matter if the input is not instantly evaluated.
          this.source += "const resolvedValue = ".concat(this.descendInput(node.condition), ";\n");
          this.source += "const id = \"".concat(sanitize(node.id), "\";\n");
          this.source += 'const hasOldEdgeValue = target.hasEdgeActivatedValue(id);\n';
          this.source += "const oldEdgeValue = target.updateEdgeActivatedValue(id, resolvedValue);\n";
          this.source += "const edgeWasActivated = hasOldEdgeValue ? (!oldEdgeValue && resolvedValue) : resolvedValue;\n";
          this.source += "if (!edgeWasActivated) {\n";
          this.retire();
          this.source += '}\n';
          this.source += 'yield;\n';
          this.source += '}\n';
          this.isInHat = false;
          break;
        case StackOpcode.HAT_PREDICATE:
          this.isInHat = true;
          this.source += "if (!".concat(this.descendInput(node.condition), ") {\n");
          this.retire();
          this.source += '}\n';
          this.source += 'yield;\n';
          this.isInHat = false;
          break;
        case StackOpcode.CONTROL_CLONE_CREATE:
          this.source += "runtime.ext_scratch3_control._createClone(".concat(this.descendInput(node.target), ", target);\n");
          break;
        case StackOpcode.CONTROL_CLONE_DELETE:
          this.source += 'if (!target.isOriginal) {\n';
          this.source += '  runtime.disposeTarget(target);\n';
          this.source += '  runtime.stopForTarget(target);\n';
          this.retire();
          this.source += '}\n';
          break;
        case StackOpcode.CONTROL_FOR:
          {
            var _index = this.localVariables.next();
            this.source += "var ".concat(_index, " = 0; ");
            this.source += "while (".concat(_index, " < ").concat(this.descendInput(node.count), ") { ");
            this.source += "".concat(_index, "++; ");
            this.source += "".concat(this.referenceVariable(node.variable), ".value = ").concat(_index, ";\n");
            this.descendStack(node["do"], new Frame(true));
            this.yieldLoop();
            this.source += '}\n';
            break;
          }
        case StackOpcode.CONTROL_IF_ELSE:
          this.source += "if (".concat(this.descendInput(node.condition), ") {\n");
          this.descendStack(node.whenTrue, new Frame(false));
          // only add the else branch if it won't be empty
          // this makes scripts have a bit less useless noise in them
          if (node.whenFalse.blocks.length) {
            this.source += "} else {\n";
            this.descendStack(node.whenFalse, new Frame(false));
          }
          this.source += "}\n";
          break;
        case StackOpcode.CONTROL_REPEAT:
          {
            var i = this.localVariables.next();
            if (node.times.isAlwaysType(InputType.NUMBER_INT | InputType.NUMBER_INF)) {
              this.source += "for (var ".concat(i, " = ").concat(this.descendInput(node.times), "; ").concat(i, " > 0; ").concat(i, "--) {\n");
            } else {
              this.source += "for (var ".concat(i, " = ").concat(this.descendInput(node.times), "; ").concat(i, " >= 0.5; ").concat(i, "--) {\n");
            }
            this.descendStack(node["do"], new Frame(true));
            this.yieldLoop();
            this.source += "}\n";
            break;
          }
        case StackOpcode.CONTROL_STOP_ALL:
          this.source += 'runtime.stopAll();\n';
          this.retire();
          break;
        case StackOpcode.CONTROL_STOP_OTHERS:
          this.source += 'runtime.stopForTarget(target, thread);\n';
          break;
        case StackOpcode.CONTROL_STOP_SCRIPT:
          this.stopScript();
          break;
        case StackOpcode.CONTROL_WAIT:
          {
            var duration = this.localVariables.next();
            this.source += "thread.timer = timer();\n";
            this.source += "var ".concat(duration, " = Math.max(0, 1000 * ").concat(this.descendInput(node.seconds), ");\n");
            this.requestRedraw();
            // always yield at least once, even on 0 second durations
            this.yieldNotWarp();
            this.source += "while (thread.timer.timeElapsed() < ".concat(duration, ") {\n");
            this.yieldStuckOrNotWarp();
            this.source += '}\n';
            this.source += 'thread.timer = null;\n';
            break;
          }
        case StackOpcode.CONTROL_WAIT_UNTIL:
          {
            this.source += "while (!".concat(this.descendInput(node.condition), ") {\n");
            this.yieldStuckOrNotWarp();
            this.source += "}\n";
            break;
          }
        case StackOpcode.CONTROL_WHILE:
          this.source += "while (".concat(this.descendInput(node.condition), ") {\n");
          this.descendStack(node["do"], new Frame(true));
          if (node.warpTimer) {
            this.yieldStuckOrNotWarp();
          } else {
            this.yieldLoop();
          }
          this.source += "}\n";
          break;
        case StackOpcode.CONTROL_CLEAR_COUNTER:
          this.source += 'runtime.ext_scratch3_control._counter = 0;\n';
          break;
        case StackOpcode.CONTORL_INCR_COUNTER:
          this.source += 'runtime.ext_scratch3_control._counter++;\n';
          break;
        case StackOpcode.EVENT_BROADCAST:
          this.source += "startHats(\"event_whenbroadcastreceived\", { BROADCAST_OPTION: ".concat(this.descendInput(node.broadcast), " });\n");
          break;
        case StackOpcode.EVENT_BROADCAST_AND_WAIT:
          this.source += "yield* waitThreads(startHats(\"event_whenbroadcastreceived\", { BROADCAST_OPTION: ".concat(this.descendInput(node.broadcast), " }));\n");
          this.yielded();
          break;
        case StackOpcode.LIST_ADD:
          {
            var list = this.referenceVariable(node.list);
            this.source += "".concat(list, ".value.push(").concat(this.descendInput(node.item), ");\n");
            this.source += "".concat(list, "._monitorUpToDate = false;\n");
            break;
          }
        case StackOpcode.LIST_DELETE:
          {
            var _list = this.referenceVariable(node.list);
            if (node.index.isConstant('last')) {
              this.source += "".concat(_list, ".value.pop();\n");
              this.source += "".concat(_list, "._monitorUpToDate = false;\n");
              break;
            }
            if (node.index.isConstant(1)) {
              this.source += "".concat(_list, ".value.shift();\n");
              this.source += "".concat(_list, "._monitorUpToDate = false;\n");
              break;
            }
            // do not need a special case for all as that is handled in IR generation (list.deleteAll)
            this.source += "listDelete(".concat(_list, ", ").concat(this.descendInput(node.index), ");\n");
            break;
          }
        case StackOpcode.LIST_DELETE_ALL:
          this.source += "".concat(this.referenceVariable(node.list), ".value = [];\n");
          break;
        case StackOpcode.LIST_HIDE:
          this.source += "runtime.monitorBlocks.changeBlock({ id: \"".concat(sanitize(node.list.id), "\", element: \"checkbox\", value: false }, runtime);\n");
          break;
        case StackOpcode.LIST_INSERT:
          {
            var _list2 = this.referenceVariable(node.list);
            var item = this.descendInput(node.item);
            if (node.index.isConstant(1)) {
              this.source += "".concat(_list2, ".value.unshift(").concat(item, ");\n");
              this.source += "".concat(_list2, "._monitorUpToDate = false;\n");
              break;
            }
            this.source += "listInsert(".concat(_list2, ", ").concat(this.descendInput(node.index), ", ").concat(item, ");\n");
            break;
          }
        case StackOpcode.LIST_REPLACE:
          this.source += "listReplace(".concat(this.referenceVariable(node.list), ", ").concat(this.descendInput(node.index), ", ").concat(this.descendInput(node.item), ");\n");
          break;
        case StackOpcode.LIST_SHOW:
          this.source += "runtime.monitorBlocks.changeBlock({ id: \"".concat(sanitize(node.list.id), "\", element: \"checkbox\", value: true }, runtime);\n");
          break;
        case StackOpcode.LOOKS_LAYER_BACKWARD:
          if (!this.target.isStage) {
            this.source += "target.goBackwardLayers(".concat(this.descendInput(node.layers), ");\n");
          }
          break;
        case StackOpcode.LOOKS_EFFECT_CLEAR:
          this.source += 'target.clearEffects();\n';
          break;
        case StackOpcode.LOOKS_EFFECT_CHANGE:
          if (Object.prototype.hasOwnProperty.call(this.target.effects, node.effect)) {
            this.source += "target.setEffect(\"".concat(sanitize(node.effect), "\", runtime.ext_scratch3_looks.clampEffect(\"").concat(sanitize(node.effect), "\", ").concat(this.descendInput(node.value), " + target.effects[\"").concat(sanitize(node.effect), "\"]));\n");
          }
          break;
        case StackOpcode.LOOKS_SIZE_CHANGE:
          this.source += "target.setSize(target.size + ".concat(this.descendInput(node.size), ");\n");
          break;
        case StackOpcode.LOOKS_LAYER_FORWARD:
          if (!this.target.isStage) {
            this.source += "target.goForwardLayers(".concat(this.descendInput(node.layers), ");\n");
          }
          break;
        case StackOpcode.LOOKS_LAYER_BACK:
          if (!this.target.isStage) {
            this.source += 'target.goToBack();\n';
          }
          break;
        case StackOpcode.LOOKS_LAYER_FRONT:
          if (!this.target.isStage) {
            this.source += 'target.goToFront();\n';
          }
          break;
        case StackOpcode.LOOKS_HIDE:
          this.source += 'target.setVisible(false);\n';
          this.source += 'runtime.ext_scratch3_looks._renderBubble(target);\n';
          break;
        case StackOpcode.LOOKS_BACKDROP_NEXT:
          this.source += 'runtime.ext_scratch3_looks._setBackdrop(stage, stage.currentCostume + 1, true);\n';
          break;
        case StackOpcode.LOOKS_COSTUME_NEXT:
          this.source += 'target.setCostume(target.currentCostume + 1);\n';
          break;
        case StackOpcode.LOOKS_EFFECT_SET:
          if (Object.prototype.hasOwnProperty.call(this.target.effects, node.effect)) {
            this.source += "target.setEffect(\"".concat(sanitize(node.effect), "\", runtime.ext_scratch3_looks.clampEffect(\"").concat(sanitize(node.effect), "\", ").concat(this.descendInput(node.value), "));\n");
          }
          break;
        case StackOpcode.LOOKS_SIZE_SET:
          this.source += "target.setSize(".concat(this.descendInput(node.size), ");\n");
          break;
        case StackOpcode.LOOKS_SHOW:
          this.source += 'target.setVisible(true);\n';
          this.source += 'runtime.ext_scratch3_looks._renderBubble(target);\n';
          break;
        case StackOpcode.LOOKS_BACKDROP_SET:
          this.source += "runtime.ext_scratch3_looks._setBackdrop(stage, ".concat(this.descendInput(node.backdrop), ");\n");
          break;
        case StackOpcode.LOOKS_COSTUME_SET:
          this.source += "runtime.ext_scratch3_looks._setCostume(target, ".concat(this.descendInput(node.costume), ");\n");
          break;
        case StackOpcode.LOOKS_SAY:
          this.source += "runtime.ext_scratch3_looks._say(".concat(this.descendInput(node.message), ", target);\n");
          break;
        case StackOpcode.LOOKS_THINK:
          this.source += "runtime.ext_scratch3_looks._think(".concat(this.descendInput(node.message), ", target);\n");
          break;
        case StackOpcode.MOTION_X_CHANGE:
          this.source += "target.setXY(target.x + ".concat(this.descendInput(node.dx), ", target.y);\n");
          break;
        case StackOpcode.MOTION_Y_CHANGE:
          this.source += "target.setXY(target.x, target.y + ".concat(this.descendInput(node.dy), ");\n");
          break;
        case StackOpcode.MOTION_IF_ON_EDGE_BOUNCE:
          this.source += "runtime.ext_scratch3_motion._ifOnEdgeBounce(target);\n";
          break;
        case StackOpcode.MOTION_DIRECTION_SET:
          this.source += "target.setDirection(".concat(this.descendInput(node.direction), ");\n");
          break;
        case StackOpcode.MOTION_ROTATION_STYLE_SET:
          this.source += "target.setRotationStyle(\"".concat(sanitize(node.style), "\");\n");
          break;
        case StackOpcode.MOTION_X_SET: // fallthrough
        case StackOpcode.MOTION_Y_SET: // fallthrough
        case StackOpcode.MOTION_XY_SET:
          {
            this.descendedIntoModulo = false;
            var x = 'x' in node ? this.descendInput(node.x) : 'target.x';
            var y = 'y' in node ? this.descendInput(node.y) : 'target.y';
            this.source += "target.setXY(".concat(x, ", ").concat(y, ");\n");
            if (this.descendedIntoModulo) {
              this.source += "if (target.interpolationData) target.interpolationData = null;\n";
            }
            break;
          }
        case StackOpcode.MOTION_STEP:
          this.source += "runtime.ext_scratch3_motion._moveSteps(".concat(this.descendInput(node.steps), ", target);\n");
          break;
        case StackOpcode.NOP:
          break;
        case StackOpcode.PEN_CLEAR:
          this.source += "".concat(PEN_EXT, ".clear();\n");
          break;
        case StackOpcode.PEN_DOWN:
          this.source += "".concat(PEN_EXT, "._penDown(target);\n");
          break;
        case StackOpcode.PEN_COLOR_PARAM_CHANGE:
          this.source += "".concat(PEN_EXT, "._setOrChangeColorParam(").concat(this.descendInput(node.param), ", ").concat(this.descendInput(node.value), ", ").concat(PEN_STATE, ", true);\n");
          break;
        case StackOpcode.PEN_SIZE_CHANGE:
          this.source += "".concat(PEN_EXT, "._changePenSizeBy(").concat(this.descendInput(node.size), ", target);\n");
          break;
        case StackOpcode.PEN_COLOR_HUE_CHANGE_LEGACY:
          this.source += "".concat(PEN_EXT, "._changePenHueBy(").concat(this.descendInput(node.hue), ", target);\n");
          break;
        case StackOpcode.PEN_COLOR_SHADE_CHANGE_LEGACY:
          this.source += "".concat(PEN_EXT, "._changePenShadeBy(").concat(this.descendInput(node.shade), ", target);\n");
          break;
        case StackOpcode.PEN_COLOR_HUE_SET_LEGACY:
          this.source += "".concat(PEN_EXT, "._setPenHueToNumber(").concat(this.descendInput(node.hue), ", target);\n");
          break;
        case StackOpcode.PEN_COLOR_SHADE_SET_LEGACY:
          this.source += "".concat(PEN_EXT, "._setPenShadeToNumber(").concat(this.descendInput(node.shade), ", target);\n");
          break;
        case StackOpcode.PEN_COLOR_SET:
          this.source += "".concat(PEN_EXT, "._setPenColorToColor(").concat(this.descendInput(node.color), ", target);\n");
          break;
        case StackOpcode.PEN_COLOR_PARAM_SET:
          this.source += "".concat(PEN_EXT, "._setOrChangeColorParam(").concat(this.descendInput(node.param), ", ").concat(this.descendInput(node.value), ", ").concat(PEN_STATE, ", false);\n");
          break;
        case StackOpcode.PEN_SIZE_SET:
          this.source += "".concat(PEN_EXT, "._setPenSizeTo(").concat(this.descendInput(node.size), ", target);\n");
          break;
        case StackOpcode.PEN_STAMP:
          this.source += "".concat(PEN_EXT, "._stamp(target);\n");
          break;
        case StackOpcode.PEN_UP:
          this.source += "".concat(PEN_EXT, "._penUp(target);\n");
          break;
        case StackOpcode.PROCEDURE_CALL:
          {
            var procedureCode = node.code;
            var procedureVariant = node.variant;
            var procedureData = this.ir.procedures[procedureVariant];
            if (procedureData.stack === null) {
              // TODO still need to evaluate arguments
              break;
            }
            var yieldForRecursion = !this.isWarp && procedureCode === this.script.procedureCode;
            if (yieldForRecursion) {
              // Direct yields.
              this.yieldNotWarp();
            }
            if (procedureData.yields) {
              this.source += 'yield* ';
              if (!this.script.yields) {
                throw new Error('Script uses yielding procedure but is not marked as yielding.');
              }
            }
            this.source += "thread.procedures[\"".concat(sanitize(procedureVariant), "\"](");
            var args = [];
            var _iterator2 = _createForOfIteratorHelper(node.arguments),
              _step2;
            try {
              for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
                var input = _step2.value;
                args.push(this.descendInput(input));
              }
            } catch (err) {
              _iterator2.e(err);
            } finally {
              _iterator2.f();
            }
            this.source += args.join(',');
            this.source += ");\n";
            break;
          }
        case StackOpcode.PROCEDURE_RETURN:
          this.stopScriptAndReturn(this.descendInput(node.value));
          break;
        case StackOpcode.SENSING_TIMER_RESET:
          this.source += 'runtime.ioDevices.clock.resetProjectTimer();\n';
          break;
        case StackOpcode.DEBUGGER:
          this.source += 'debugger;\n';
          break;
        case StackOpcode.VAR_HIDE:
          this.source += "runtime.monitorBlocks.changeBlock({ id: \"".concat(sanitize(node.variable.id), "\", element: \"checkbox\", value: false }, runtime);\n");
          break;
        case StackOpcode.VAR_SET:
          {
            var varReference = this.referenceVariable(node.variable);
            this.source += "".concat(varReference, ".value = ").concat(this.descendInput(node.value), ";\n");
            if (node.variable.isCloud) {
              this.source += "runtime.ioDevices.cloud.requestUpdateVariable(\"".concat(sanitize(node.variable.name), "\", ").concat(varReference, ".value);\n");
            }
            break;
          }
        case StackOpcode.VAR_SHOW:
          this.source += "runtime.monitorBlocks.changeBlock({ id: \"".concat(sanitize(node.variable.id), "\", element: \"checkbox\", value: true }, runtime);\n");
          break;
        case StackOpcode.VISUAL_REPORT:
          {
            var value = this.localVariables.next();
            this.source += "const ".concat(value, " = ").concat(this.descendInput(node.input), ";");
            // blocks like legacy no-ops can return a literal `undefined`
            this.source += "if (".concat(value, " !== undefined) runtime.visualReport(target, \"").concat(sanitize(this.script.topBlockId), "\", ").concat(value, ");\n");
            break;
          }
        default:
          log.warn("JS: Unknown stacked block: ".concat(block.opcode), node);
          throw new Error("JS: Unknown stacked block: ".concat(block.opcode));
      }
    }

    /**
     * Compiles a reference to a target.
     * @param {IntermediateInput} input The target reference. Must be a string.
     * @returns {string} The compiled target reference
     */
  }, {
    key: "descendTargetReference",
    value: function descendTargetReference(input) {
      if (!input.isAlwaysType(InputType.STRING)) {
        throw new Error("JS: Object references must be strings!");
      }
      if (input.isConstant('_stage_')) return 'stage';
      return this.evaluateOnce("runtime.getSpriteTargetByName(".concat(this.descendInput(input), ")"));
    }

    /**
     * Compile a Record of input objects into a safe JS string.
     * @param {Record<string, IntermediateInput>} inputs
     * @returns {string}
     */
  }, {
    key: "descendInputRecord",
    value: function descendInputRecord(inputs) {
      var result = '{';
      for (var _i = 0, _Object$keys = Object.keys(inputs); _i < _Object$keys.length; _i++) {
        var name = _Object$keys[_i];
        var node = inputs[name];
        result += "\"".concat(sanitize(name), "\":").concat(this.descendInput(node), ",");
      }
      result += '}';
      return result;
    }

    /**
     * @param {IntermediateStack} stack
     * @param {Frame} frame
     */
  }, {
    key: "descendStack",
    value: function descendStack(stack, frame) {
      // Entering a stack -- all bets are off.
      // TODO: allow if/else to inherit values
      this.pushFrame(frame);
      for (var i = 0; i < stack.blocks.length; i++) {
        frame.isLastBlock = i === stack.blocks.length - 1;
        this.descendStackedBlock(stack.blocks[i]);
      }

      // Leaving a stack -- any assumptions made in the current stack do not apply outside of it
      // TODO: in if/else this might create an extra unused object
      this.popFrame();
    }

    /**
     * @param {*} node
     * @returns {string}
     */
  }, {
    key: "descendAddonCall",
    value: function descendAddonCall(node) {
      var inputs = this.descendInputRecord(node.arguments);
      var blockFunction = "runtime.getAddonBlock(\"".concat(sanitize(node.code), "\").callback");
      var blockId = "\"".concat(sanitize(node.blockId), "\"");
      return "yield* executeInCompatibilityLayer(".concat(inputs, ", ").concat(blockFunction, ", ").concat(this.isWarp, ", false, ").concat(blockId, ")");
    }

    /**
     * @param {*} variable
     * @returns {string}
     */
  }, {
    key: "referenceVariable",
    value: function referenceVariable(variable) {
      if (variable.scope === 'target') {
        return this.evaluateOnce("target.variables[\"".concat(sanitize(variable.id), "\"]"));
      }
      return this.evaluateOnce("stage.variables[\"".concat(sanitize(variable.id), "\"]"));
    }

    /**
     * @param {string} source
     * @returns {string}
     */
  }, {
    key: "evaluateOnce",
    value: function evaluateOnce(source) {
      if (Object.prototype.hasOwnProperty.call(this._setupVariables, source)) {
        return this._setupVariables[source];
      }
      var variable = this._setupVariablesPool.next();
      this._setupVariables[source] = variable;
      return variable;
    }
  }, {
    key: "retire",
    value: function retire() {
      // After running retire() (sets thread status and cleans up some unused data), we need to return to the event loop.
      // When in a procedure, return will only send us back to the previous procedure, so instead we yield back to the sequencer.
      // Outside of a procedure, return will correctly bring us back to the sequencer.
      if (this.isProcedure) {
        this.source += 'retire(); yield;\n';
      } else {
        this.source += 'retire(); return;\n';
      }
    }
  }, {
    key: "yieldLoop",
    value: function yieldLoop() {
      if (this.warpTimer) {
        this.yieldStuckOrNotWarp();
      } else {
        this.yieldNotWarp();
      }
    }

    /**
     * Write JS to yield the current thread if warp mode is disabled.
     */
  }, {
    key: "yieldNotWarp",
    value: function yieldNotWarp() {
      if (!this.isWarp) {
        this.source += 'yield;\n';
        this.yielded();
      }
    }

    /**
     * Write JS to yield the current thread if warp mode is disabled or if the script seems to be stuck.
     */
  }, {
    key: "yieldStuckOrNotWarp",
    value: function yieldStuckOrNotWarp() {
      if (this.isWarp) {
        this.source += 'if (isStuck()) yield;\n';
      } else {
        this.source += 'yield;\n';
      }
      this.yielded();
    }
  }, {
    key: "yielded",
    value: function yielded() {
      if (!this.script.yields) {
        throw new Error('Script yielded but is not marked as yielding.');
      }
      // Control may have been yielded to another script -- all bets are off.
    }

    /**
     * Write JS to request a redraw.
     */
  }, {
    key: "requestRedraw",
    value: function requestRedraw() {
      this.source += 'runtime.requestRedraw();\n';
    }

    /**
     * Generate a call into the compatibility layer.
     * @param {*} node The node of the block to generate from.
     * @param {boolean} setFlags Whether flags should be set describing how this function was processed.
     * @param {string|null} [frameName] Name of the stack frame variable, if any
     * @returns {string} The JS of the call.
     */
  }, {
    key: "generateCompatibilityLayerCall",
    value: function generateCompatibilityLayerCall(node, setFlags) {
      var frameName = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;
      var opcode = node.opcode;
      var result = 'yield* executeInCompatibilityLayer({';
      for (var _i2 = 0, _Object$keys2 = Object.keys(node.inputs); _i2 < _Object$keys2.length; _i2++) {
        var inputName = _Object$keys2[_i2];
        var input = node.inputs[inputName];
        var compiledInput = this.descendInput(input);
        result += "\"".concat(sanitize(inputName), "\":").concat(compiledInput, ",");
      }
      for (var _i3 = 0, _Object$keys3 = Object.keys(node.fields); _i3 < _Object$keys3.length; _i3++) {
        var fieldName = _Object$keys3[_i3];
        var field = node.fields[fieldName];
        result += "\"".concat(sanitize(fieldName), "\":\"").concat(sanitize(field), "\",");
      }
      var opcodeFunction = this.evaluateOnce("runtime.getOpcodeFunction(\"".concat(sanitize(opcode), "\")"));
      result += "}, ".concat(opcodeFunction, ", ").concat(this.isWarp, ", ").concat(setFlags, ", \"").concat(sanitize(node.id), "\", ").concat(frameName, ")");
      this.yielded();
      return result;
    }
  }, {
    key: "getScriptFactoryName",
    value: function getScriptFactoryName() {
      return factoryNameVariablePool.next();
    }
  }, {
    key: "getScriptName",
    value: function getScriptName(yields) {
      var name = yields ? generatorNameVariablePool.next() : functionNameVariablePool.next();
      if (this.isProcedure) {
        var simplifiedProcedureCode = this.script.procedureCode.replace(/%[\w]/g, '') // remove arguments
        .replace(/[^a-zA-Z0-9]/g, '_') // remove unsafe
        .substring(0, 20); // keep length reasonable
        name += "_".concat(simplifiedProcedureCode);
      }
      return name;
    }
  }, {
    key: "stopScript",
    value: function stopScript() {
      if (this.isProcedure) {
        this.source += 'return "";\n';
      } else {
        this.retire();
      }
    }

    /**
     * @param {string} valueJS JS code of value to return.
     */
  }, {
    key: "stopScriptAndReturn",
    value: function stopScriptAndReturn(valueJS) {
      if (this.isProcedure) {
        this.source += "return ".concat(valueJS, ";\n");
      } else {
        this.retire();
      }
    }

    /**
     * Generate the JS to pass into eval() based on the current state of the compiler.
     * @returns {string} JS to pass into eval()
     */
  }, {
    key: "createScriptFactory",
    value: function createScriptFactory() {
      var script = '';

      // Setup the factory
      script += "(function ".concat(this.getScriptFactoryName(), "(thread) { ");
      script += 'const target = thread.target; ';
      script += 'const runtime = target.runtime; ';
      script += 'const stage = runtime.getTargetForStage();\n';
      for (var _i4 = 0, _Object$keys4 = Object.keys(this._setupVariables); _i4 < _Object$keys4.length; _i4++) {
        var varValue = _Object$keys4[_i4];
        var varName = this._setupVariables[varValue];
        script += "const ".concat(varName, " = ").concat(varValue, ";\n");
      }

      // Generated script
      script += 'return ';
      if (this.script.yields) {
        script += "function* ";
      } else {
        script += "function ";
      }
      script += this.getScriptName(this.script.yields);
      script += ' (';
      if (this.script.arguments.length) {
        var args = [];
        for (var i = 0; i < this.script.arguments.length; i++) {
          args.push("p".concat(i));
        }
        script += args.join(',');
      }
      script += ') {\n';
      script += this.source;
      script += '}; })';
      return script;
    }

    /**
     * Compile this script.
     * @returns {Function} The factory function for the script.
     */
  }, {
    key: "compile",
    value: function compile() {
      if (this.script.stack) {
        this.descendStack(this.script.stack, new Frame(false));
      }
      this.stopScript();
      var factory = this.createScriptFactory();
      var fn = jsexecute.scopedEval(factory);
      if (this.debug) {
        log.info("JS: ".concat(this.target.getName(), ": compiled ").concat(this.script.procedureCode || 'script'), factory);
      }
      if (JSGenerator.testingApparatus) {
        JSGenerator.testingApparatus.report(this, factory);
      }
      return fn;
    }
  }]);
}(); // For extensions.
JSGenerator.unstable_exports = {
  factoryNameVariablePool: factoryNameVariablePool,
  functionNameVariablePool: functionNameVariablePool,
  generatorNameVariablePool: generatorNameVariablePool,
  VariablePool: VariablePool,
  PEN_EXT: PEN_EXT,
  PEN_STATE: PEN_STATE,
  Frame: Frame,
  sanitize: sanitize
};

// Test hook used by automated snapshot testing.
JSGenerator.testingApparatus = null;
module.exports = JSGenerator;

/***/ }),

/***/ "../vm/src/compiler/offload-planner.js":
/*!*********************************************!*\
  !*** ../vm/src/compiler/offload-planner.js ***!
  \*********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// @ts-check
var OffloadPlanner = /*#__PURE__*/function () {
  /**
   * @param {import('./intermediate').IntermediateRepresentation} ir
   * @param {object[]} hotLoops
   * @param {object} runtimeCaps
   */
  function OffloadPlanner(ir, hotLoops) {
    var runtimeCaps = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    _classCallCheck(this, OffloadPlanner);
    this.ir = ir;
    this.hotLoops = hotLoops;
    this.runtimeCaps = runtimeCaps;
  }
  return _createClass(OffloadPlanner, [{
    key: "plan",
    value: function plan() {
      var plans = [];
      var _iterator = _createForOfIteratorHelper(this.hotLoops),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var summary = _step.value;
          var _plan = this._planLoop(summary);
          summary.kernelPlan = _plan;
          plans.push(_plan);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      this.ir.offloadPlans = plans;
      this._attachPlansToBlocks();
      this._attachPlansToScripts();
      return plans;
    }

    /**
     * @private
     */
  }, {
    key: "_attachPlansToBlocks",
    value: function _attachPlansToBlocks() {
      var planMap = new Map(this.ir.offloadPlans.map(function (plan) {
        return [plan.loopId, plan];
      }));
      var _attachInStack = function attachInStack(stack) {
        if (!stack) {
          return;
        }
        var _iterator2 = _createForOfIteratorHelper(stack.blocks),
          _step2;
        try {
          for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
            var block = _step2.value;
            if (block.loopSummary) {
              block.kernelPlan = planMap.get(block.loopSummary.loopId) || null;
            }
            if (block.inputs && block.inputs["do"] && block.inputs["do"].blocks) {
              _attachInStack(block.inputs["do"]);
            }
            if (block.inputs && block.inputs.whenTrue && block.inputs.whenTrue.blocks) {
              _attachInStack(block.inputs.whenTrue);
            }
            if (block.inputs && block.inputs.whenFalse && block.inputs.whenFalse.blocks) {
              _attachInStack(block.inputs.whenFalse);
            }
            if (block.inputs && block.inputs.substacks) {
              for (var _i = 0, _Object$keys = Object.keys(block.inputs.substacks); _i < _Object$keys.length; _i++) {
                var substackName = _Object$keys[_i];
                _attachInStack(block.inputs.substacks[substackName]);
              }
            }
          }
        } catch (err) {
          _iterator2.e(err);
        } finally {
          _iterator2.f();
        }
      };
      _attachInStack(this.ir.entry.stack);
      for (var _i2 = 0, _Object$keys2 = Object.keys(this.ir.procedures); _i2 < _Object$keys2.length; _i2++) {
        var procedureVariant = _Object$keys2[_i2];
        _attachInStack(this.ir.procedures[procedureVariant].stack);
      }
    }

    /**
     * @private
     */
  }, {
    key: "_attachPlansToScripts",
    value: function _attachPlansToScripts() {
      this.ir.entry.offloadPlans = [];
      for (var _i3 = 0, _Object$keys3 = Object.keys(this.ir.procedures); _i3 < _Object$keys3.length; _i3++) {
        var procedureVariant = _Object$keys3[_i3];
        this.ir.procedures[procedureVariant].offloadPlans = [];
      }
      var _iterator3 = _createForOfIteratorHelper(this.ir.offloadPlans),
        _step3;
      try {
        for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
          var plan = _step3.value;
          var script = plan.scriptId === 'entry' ? this.ir.entry : this.ir.procedures[plan.scriptId];
          if (script) {
            script.offloadPlans.push(plan);
          }
        }
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
    }

    /**
     * @param {object} summary
     * @returns {object}
     * @private
     */
  }, {
    key: "_planLoop",
    value: function _planLoop(summary) {
      var offload = Object.assign({
        enabled: false,
        wasm: true,
        webgl: true,
        minWasmIterations: 256,
        minWebGLElements: 4096,
        verifyGPU: 'conservative'
      }, this.runtimeCaps.offload || {});
      var backends = Object.assign({
        wasm: false,
        webgl: false
      }, this.runtimeCaps.backends || {});
      var reasons = [];
      var candidateBackends = ['js'];
      if (!offload.enabled) {
        reasons.push('offload-disabled');
      } else if (!summary.isWarp) {
        reasons.push('non-warp-loop');
      } else if (summary.usesWarpTimer) {
        reasons.push('warp-timer-loop');
      } else if (summary.hasNestedLoop) {
        reasons.push('nested-loop');
      } else if (summary.effectProfile.requiresJSRuntime) {
        reasons.push('js-runtime-effects');
      } else if (!summary.numericDomain.pureNumeric) {
        reasons.push('non-numeric-domain');
      } else {
        if (offload.wasm && this._meetsWasmThreshold(summary, offload)) {
          candidateBackends.push('wasm');
        }
        if (offload.webgl && this._meetsWebGLThreshold(summary, offload)) {
          candidateBackends.push('webgl');
        }
        if (candidateBackends.length === 1) {
          reasons.push('below-offload-threshold');
        }
      }
      var preferredBackend = candidateBackends.includes('webgl') ? 'webgl' : candidateBackends.includes('wasm') ? 'wasm' : 'js';
      var selectedBackend = preferredBackend;
      if (selectedBackend === 'wasm' && !backends.wasm) {
        selectedBackend = 'js';
        reasons.push('wasm-backend-unavailable');
      }
      if (selectedBackend === 'webgl' && !backends.webgl) {
        selectedBackend = candidateBackends.includes('wasm') && backends.wasm ? 'wasm' : 'js';
        reasons.push('webgl-backend-unavailable');
        if (selectedBackend === 'js' && candidateBackends.includes('wasm') && !backends.wasm) {
          reasons.push('wasm-backend-unavailable');
        }
      }
      return {
        loopId: summary.loopId,
        scriptId: summary.scriptId,
        opcode: summary.opcode,
        selectedBackend: selectedBackend,
        preferredBackend: preferredBackend,
        candidateBackends: candidateBackends,
        verifyGPU: offload.verifyGPU,
        reasons: reasons
      };
    }

    /**
     * @param {object} summary
     * @param {object} offload
     * @returns {boolean}
     * @private
     */
  }, {
    key: "_meetsWasmThreshold",
    value: function _meetsWasmThreshold(summary, offload) {
      if (summary.estimatedIterations === null || !Number.isFinite(summary.estimatedIterations)) {
        return false;
      }
      return summary.estimatedIterations >= offload.minWasmIterations;
    }

    /**
     * @param {object} summary
     * @param {object} offload
     * @returns {boolean}
     * @private
     */
  }, {
    key: "_meetsWebGLThreshold",
    value: function _meetsWebGLThreshold(summary, offload) {
      if (!summary.numericDomain.hasListAccess) {
        return false;
      }
      if (!summary.numericDomain.fp32Safe) {
        return false;
      }
      if (summary.estimatedIterations === null || !Number.isFinite(summary.estimatedIterations)) {
        return false;
      }
      if (summary.arithmeticOps < 20) {
        return false;
      }
      return summary.estimatedIterations >= offload.minWebGLElements;
    }
  }]);
}();
module.exports = {
  OffloadPlanner: OffloadPlanner
};

/***/ }),

/***/ "../vm/src/compiler/old-compiler-compatibility.js":
/*!********************************************************!*\
  !*** ../vm/src/compiler/old-compiler-compatibility.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
/**
 * @fileoverview
 * Reimplements a subset of the old (pre-September 2025) compiler
 * to maintain compatibility with extensions patching the old compiler.
 *
 * Safety and compatibility is more important than performance. There may be
 * unnecessary type casts and scripts may be marked as yielding even when they
 * don't actually yield. Additionally, anything running in this compatibility layer
 * is not expected to receive the performance benefits of the new compiler.
 *
 * These assumptions are made about extensions using this compatibility layer:
 *  - Extensions do not try to combine this compatibility layer with any APIs
 *    provided by the new compiler.
 *  - Extensions treat IR nodes received from descendSubstack and similar as
 *    opaque objects.
 *  - Extensions need to implement the JS generators for all AST node kinds
 *    they use. Can not rely on the default JS generator.
 */

var _require = __webpack_require__(/*! ./enums */ "../vm/src/compiler/enums.js"),
  InputOpcode = _require.InputOpcode,
  InputType = _require.InputType;
// eslint-disable-next-line no-unused-vars
var _require2 = __webpack_require__(/*! ./intermediate */ "../vm/src/compiler/intermediate.js"),
  IntermediateInput = _require2.IntermediateInput,
  IntermediateStackBlock = _require2.IntermediateStackBlock,
  IntermediateStack = _require2.IntermediateStack;
var IRGeneratorStub = /*#__PURE__*/_createClass(function IRGeneratorStub() {
  _classCallCheck(this, IRGeneratorStub);
});
var ScriptTreeGeneratorStub = /*#__PURE__*/function () {
  /**
   * @param {import("./irgen").ScriptTreeGenerator} real
   */
  function ScriptTreeGeneratorStub(real) {
    _classCallCheck(this, ScriptTreeGeneratorStub);
    /**
     * @type {import("./irgen").ScriptTreeGenerator}
     */
    this.real = real;
    this.fakeThis = {
      thread: real.thread,
      target: real.target,
      blocks: real.blocks,
      runtime: real.runtime,
      stage: real.stage,
      script: real.script,
      /**
       * @param parentBlock Parent VM block.
       * @param {string} inputName Name of input.
       * @returns opaque object
       */
      descendInputOfBlock: function descendInputOfBlock(parentBlock, inputName) {
        var node = real.descendInputOfBlock(parentBlock, inputName, true);
        return node;
      },
      /**
       * @param {*} parentBlock Parent VM block.
       * @param {*} substackName Name of substack.
       * @returns opaque object
       */
      descendSubstack: function descendSubstack(parentBlock, substackName) {
        var substack = real.descendSubstack(parentBlock, substackName);
        return substack;
      },
      analyzeLoop: function analyzeLoop() {
        // TODO: not always necessary
        real.script.yields = true;
      }
    };
  }

  /**
   * Intended for extensions to override.
   * Always call from `fakeThis` context.
   * @param {{opcode: string}} block VM block
   * @returns {{kind: string}} Node object from old compiler.
   */
  return _createClass(ScriptTreeGeneratorStub, [{
    key: "descendInput",
    value: function descendInput(block) {
      // eslint-disable-line no-unused-vars
      return null;
    }

    /**
     * Intended for extensions to override.
     * Always call from `fakeThis` context.
     * @param {{opcode: string}} block VM block
     * @returns {{kind: string}} Node object from old compiler.
     */
  }, {
    key: "descendStackedBlock",
    value: function descendStackedBlock(block) {
      // eslint-disable-line no-unused-vars
      return null;
    }

    /**
     * @param block VM block
     * @returns {IntermediateInput|null}
     */
  }, {
    key: "descendInputFromNewCompiler",
    value: function descendInputFromNewCompiler(block) {
      var node = this.descendInput.call(this.fakeThis, block);
      if (node) {
        return new IntermediateInput(InputOpcode.OLD_COMPILER_COMPATIBILITY_LAYER, InputType.ANY, {
          oldNode: node
        }, true);
      }
      return null;
    }

    /**
     * @param block VM block
     * @returns {IntermediateStackBlock|null}
     */
  }, {
    key: "descendStackedBlockFromNewCompiler",
    value: function descendStackedBlockFromNewCompiler(block) {
      var node = this.descendStackedBlock.call(this.fakeThis, block);
      if (node) {
        return new IntermediateStackBlock(InputOpcode.OLD_COMPILER_COMPATIBILITY_LAYER, {
          oldNode: node
        }, true);
      }
      return null;
    }
  }]);
}(); // These are part of the old compiler's API.
var TYPE_NUMBER = 1;
var TYPE_STRING = 2;
var TYPE_BOOLEAN = 3;
var TYPE_UNKNOWN = 4;
var TYPE_NUMBER_NAN = 5;

/**
 * Part of the old compiler's API.
 */
var TypedInput = /*#__PURE__*/function () {
  /**
   * @param {string} source JavaScript
   * @param {number|IntermediateInput} typeOrIntermediate
   */
  function TypedInput(source, typeOrIntermediate) {
    _classCallCheck(this, TypedInput);
    /**
     * JavaScript.
     * @type {string}
     */
    this.source = source;
    if (typeOrIntermediate instanceof IntermediateInput) {
      // Path used by the compatibility layer itself

      /**
       * @type {IntermediateInput}
       */
      this.intermediate = typeOrIntermediate;

      /**
       * @type {number} See TYPE_* constants above
       */
      this.type = TYPE_UNKNOWN;
    } else {
      // Path used by extensions
      this.intermediate = null;
      this.type = typeOrIntermediate;
    }
  }
  return _createClass(TypedInput, [{
    key: "asNumber",
    value: function asNumber() {
      return "(+".concat(this.source, " || 0)");
    }
  }, {
    key: "asNumberOrNaN",
    value: function asNumberOrNaN() {
      return "(+".concat(this.source, ")");
    }
  }, {
    key: "asString",
    value: function asString() {
      return "(\"\" + ".concat(this.source, ")");
    }
  }, {
    key: "asBoolean",
    value: function asBoolean() {
      return "toBoolean(".concat(this.source, ")");
    }
  }, {
    key: "asColor",
    value: function asColor() {
      return this.asUnknown();
    }
  }, {
    key: "asUnknown",
    value: function asUnknown() {
      return this.source;
    }
  }, {
    key: "asSafe",
    value: function asSafe() {
      return this.asUnknown();
    }
  }, {
    key: "isAlwaysNumber",
    value: function isAlwaysNumber() {
      // TODO
      return false;
    }
  }, {
    key: "isAlwaysNumberOrNaN",
    value: function isAlwaysNumberOrNaN() {
      // TODO
      return false;
    }
  }, {
    key: "isNeverNumber",
    value: function isNeverNumber() {
      // TODO
      return false;
    }
  }]);
}();
/**
 * Part of the old compiler's API.
 */
var VariablePool = /*#__PURE__*/function () {
  function VariablePool(prefix) {
    _classCallCheck(this, VariablePool);
    this.prefix = prefix;
    this.count = 0;
  }
  return _createClass(VariablePool, [{
    key: "next",
    value: function next() {
      return "".concat(this.prefix).concat(this.count++);
    }
  }]);
}();
/**
 * Part of the old compiler's API.
 */
var Frame = /*#__PURE__*/_createClass(function Frame(isLoop) {
  _classCallCheck(this, Frame);
  this.isLoop = isLoop;
  this.isLastBlock = false;
});
var JSGeneratorStub = /*#__PURE__*/function () {
  /**
   * @param {import("./jsgen")} real
   */
  function JSGeneratorStub(real) {
    _classCallCheck(this, JSGeneratorStub);
    /**
     * @type {import("./jsgen")}
     */
    this.real = real;
    this.fakeThis = {
      script: real.script,
      ir: real.ir,
      target: real.target,
      get frames() {
        return real.frames;
      },
      get currentFrame() {
        return real.currentFrame;
      },
      get source() {
        return real.source;
      },
      set source(newSource) {
        real.source = newSource;
      },
      localVariables: new VariablePool('oldCompilerLocal'),
      /**
       * @param {IntermediateInput} intermediate
       * @returns {void} output is concatenated in this.source
       */
      descendInput: function descendInput(intermediate) {
        var js = real.descendInput(intermediate);
        return new TypedInput(js, intermediate);
      },
      /**
       * @param {IntermediateStack} stack Stack of blocks.
       * @param {Frame} frame New frame
       */
      descendStack: function descendStack(stack, frame) {
        real.descendStack(stack, frame);
      },
      yieldLoop: function yieldLoop() {
        return real.yieldLoop();
      },
      yieldNotWarp: function yieldNotWarp() {
        return real.yieldNotWarp();
      },
      yieldStuckOrNotWarp: function yieldStuckOrNotWarp() {
        return real.yieldStuckOrNotWarp();
      },
      yielded: function yielded() {
        return real.yielded();
      },
      requestRedraw: function requestRedraw() {
        return real.requestRedraw();
      }
    };
  }

  /**
   * Intended for extensions to override.
   * Always call from `fakeThis` context.
   * @param {{kind: string}} node Old compiler AST node.
   * @returns {TypedInput} Old compiler TypedInput.
   */
  return _createClass(JSGeneratorStub, [{
    key: "descendInput",
    value: function descendInput(node) {
      throw new Error("Unknown input: ".concat(node.kind));
    }

    /**
     * Intended for extensions to override.
     * Always call from `fakeThis` context.
     * @param {{kind: string}} node Old compiler AST node.
     */
  }, {
    key: "descendStackedBlock",
    value: function descendStackedBlock(node) {
      throw new Error("Unknown stacked block: ".concat(node.kind));
    }

    /**
     * @param {IntermediateInput} intermediate
     * @returns {string} JavaScript
     */
  }, {
    key: "descendInputFromNewCompiler",
    value: function descendInputFromNewCompiler(intermediate) {
      var oldNode = intermediate.inputs.oldNode;
      var typedInput = this.descendInput.call(this.fakeThis, oldNode);
      return typedInput.asSafe();
    }

    /**
     * @param {IntermediateStackBlock} intermediate
     * @returns {void} source property on real JSGenerator is modified directly
     */
  }, {
    key: "descendStackedBlockFromNewCompiler",
    value: function descendStackedBlockFromNewCompiler(intermediate) {
      var oldNode = intermediate.inputs.oldNode;
      this.descendStackedBlock.call(this.fakeThis, oldNode);
    }
  }]);
}();
/**
 * Part of old compiler's API.
 */
JSGeneratorStub.unstable_exports = {
  TYPE_NUMBER: TYPE_NUMBER,
  TYPE_STRING: TYPE_STRING,
  TYPE_BOOLEAN: TYPE_BOOLEAN,
  TYPE_UNKNOWN: TYPE_UNKNOWN,
  TYPE_NUMBER_NAN: TYPE_NUMBER_NAN,
  VariablePool: VariablePool,
  TypedInput: TypedInput,
  Frame: Frame
};
var oldCompilerCompatibility = {
  enabled: false,
  IRGeneratorStub: IRGeneratorStub,
  ScriptTreeGeneratorStub: ScriptTreeGeneratorStub,
  TypedInput: TypedInput,
  JSGeneratorStub: JSGeneratorStub
};
module.exports = oldCompilerCompatibility;

/***/ }),

/***/ "../vm/src/compiler/variable-pool.js":
/*!*******************************************!*\
  !*** ../vm/src/compiler/variable-pool.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// @ts-check
var VariablePool = /*#__PURE__*/function () {
  /**
   * @param {string} prefix The prefix at the start of the variable name.
   */
  function VariablePool(prefix) {
    _classCallCheck(this, VariablePool);
    if (prefix.trim().length === 0) {
      throw new Error('prefix cannot be empty');
    }
    this.prefix = prefix;
    /**
     * @private
     */
    this.count = 0;
  }
  return _createClass(VariablePool, [{
    key: "next",
    value: function next() {
      return "".concat(this.prefix).concat(this.count++);
    }
  }]);
}();
module.exports = VariablePool;

/***/ }),

/***/ "../vm/src/dispatch/central-dispatch.js":
/*!**********************************************!*\
  !*** ../vm/src/dispatch/central-dispatch.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var SharedDispatch = __webpack_require__(/*! ./shared-dispatch */ "../vm/src/dispatch/shared-dispatch.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/**
 * This class serves as the central broker for message dispatch. It expects to operate on the main thread / Window and
 * it must be informed of any Worker threads which will participate in the messaging system. From any context in the
 * messaging system, the dispatcher's "call" method can call any method on any "service" provided in any participating
 * context. The dispatch system will forward function arguments and return values across worker boundaries as needed.
 * @see {WorkerDispatch}
 */
var CentralDispatch = /*#__PURE__*/function (_SharedDispatch) {
  function CentralDispatch() {
    var _this;
    _classCallCheck(this, CentralDispatch);
    _this = _callSuper(this, CentralDispatch);

    /**
     * Map of channel name to worker or local service provider.
     * If the entry is a Worker, the service is provided by an object on that worker.
     * Otherwise, the service is provided locally and methods on the service will be called directly.
     * @see {setService}
     * @type {object.<Worker|object>}
     */
    _this.services = {};

    /**
     * The constructor we will use to recognize workers.
     * @type {Function}
     */
    _this.workerClass = typeof Worker === 'undefined' ? null : Worker;

    /**
     * List of workers attached to this dispatcher.
     * @type {Array}
     */
    _this.workers = [];
    return _this;
  }

  /**
   * Synchronously call a particular method on a particular service provided locally.
   * Calling this function on a remote service will fail.
   * @param {string} service - the name of the service.
   * @param {string} method - the name of the method.
   * @param {*} [args] - the arguments to be copied to the method, if any.
   * @returns {*} - the return value of the service method.
   */
  _inherits(CentralDispatch, _SharedDispatch);
  return _createClass(CentralDispatch, [{
    key: "callSync",
    value: function callSync(service, method) {
      var _this$_getServiceProv = this._getServiceProvider(service),
        provider = _this$_getServiceProv.provider,
        isRemote = _this$_getServiceProv.isRemote;
      if (provider) {
        if (isRemote) {
          throw new Error("Cannot use 'callSync' on remote provider for service ".concat(service, "."));
        }

        // TODO: verify correct `this` after switching from apply to spread
        // eslint-disable-next-line prefer-spread
        for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          args[_key - 2] = arguments[_key];
        }
        return provider[method].apply(provider, args);
      }
      throw new Error("Provider not found for service: ".concat(service));
    }

    /**
     * Synchronously set a local object as the global provider of the specified service.
     * WARNING: Any method on the provider can be called from any worker within the dispatch system.
     * @param {string} service - a globally unique string identifying this service. Examples: 'vm', 'gui', 'extension9'.
     * @param {object} provider - a local object which provides this service.
     */
  }, {
    key: "setServiceSync",
    value: function setServiceSync(service, provider) {
      if (Object.prototype.hasOwnProperty.call(this.services, service)) {
        log.warn("Central dispatch replacing existing service provider for ".concat(service));
      }
      this.services[service] = provider;
    }

    /**
     * Set a local object as the global provider of the specified service.
     * WARNING: Any method on the provider can be called from any worker within the dispatch system.
     * @param {string} service - a globally unique string identifying this service. Examples: 'vm', 'gui', 'extension9'.
     * @param {object} provider - a local object which provides this service.
     * @returns {Promise} - a promise which will resolve once the service is registered.
     */
  }, {
    key: "setService",
    value: function setService(service, provider) {
      /** Return a promise for consistency with {@link WorkerDispatch#setService} */
      try {
        this.setServiceSync(service, provider);
        return Promise.resolve();
      } catch (e) {
        return Promise.reject(e);
      }
    }

    /**
     * Add a worker to the message dispatch system. The worker must implement a compatible message dispatch framework.
     * The dispatcher will immediately attempt to "handshake" with the worker.
     * @param {Worker} worker - the worker to add into the dispatch system.
     */
  }, {
    key: "addWorker",
    value: function addWorker(worker) {
      if (this.workers.indexOf(worker) === -1) {
        this.workers.push(worker);
        worker.onmessage = this._onMessage.bind(this, worker);
        this._remoteCall(worker, 'dispatch', 'handshake')["catch"](function (e) {
          log.error("Could not handshake with worker: ".concat(e));
        });
      } else {
        log.warn('Central dispatch ignoring attempt to add duplicate worker');
      }
    }

    /**
     * Fetch the service provider object for a particular service name.
     * @override
     * @param {string} service - the name of the service to look up
     * @returns {{provider:(object|Worker), isRemote:boolean}} - the means to contact the service, if found
     * @protected
     */
  }, {
    key: "_getServiceProvider",
    value: function _getServiceProvider(service) {
      var provider = this.services[service];
      return provider && {
        provider: provider,
        isRemote: Boolean(this.workerClass && provider instanceof this.workerClass || provider.isRemote)
      };
    }

    /**
     * Handle a call message sent to the dispatch service itself
     * @override
     * @param {Worker} worker - the worker which sent the message.
     * @param {DispatchCallMessage} message - the message to be handled.
     * @returns {Promise|undefined} - a promise for the results of this operation, if appropriate
     * @protected
     */
  }, {
    key: "_onDispatchMessage",
    value: function _onDispatchMessage(worker, message) {
      var promise;
      switch (message.method) {
        case 'setService':
          promise = this.setService(message.args[0], worker);
          break;
        default:
          log.error("Central dispatch received message for unknown method: ".concat(message.method));
      }
      return promise;
    }
  }]);
}(SharedDispatch);
module.exports = new CentralDispatch();

/***/ }),

/***/ "../vm/src/dispatch/shared-dispatch.js":
/*!*********************************************!*\
  !*** ../vm/src/dispatch/shared-dispatch.js ***!
  \*********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/**
 * @typedef {object} DispatchCallMessage - a message to the dispatch system representing a service method call
 * @property {*} responseId - send a response message with this response ID. See {@link DispatchResponseMessage}
 * @property {string} service - the name of the service to be called
 * @property {string} method - the name of the method to be called
 * @property {Array|undefined} args - the arguments to be passed to the method
 */

/**
 * @typedef {object} DispatchResponseMessage - a message to the dispatch system representing the results of a call
 * @property {*} responseId - a copy of the response ID from the call which generated this response
 * @property {*|undefined} error - if this is truthy, then it contains results from a failed call (such as an exception)
 * @property {*|undefined} result - if error is not truthy, then this contains the return value of the call (if any)
 */

/**
 * @typedef {DispatchCallMessage|DispatchResponseMessage} DispatchMessage
 * Any message to the dispatch system.
 */

/**
 * The SharedDispatch class is responsible for dispatch features shared by
 * {@link CentralDispatch} and {@link WorkerDispatch}.
 */
var SharedDispatch = /*#__PURE__*/function () {
  function SharedDispatch() {
    _classCallCheck(this, SharedDispatch);
    /**
     * List of callback registrations for promises waiting for a response from a call to a service on another
     * worker. A callback registration is an array of [resolve,reject] Promise functions.
     * Calls to local services don't enter this list.
     * @type {Array.<Function[]>}
     */
    this.callbacks = [];

    /**
     * The next response ID to be used.
     * @type {int}
     */
    this.nextResponseId = 0;
  }

  /**
   * Call a particular method on a particular service, regardless of whether that service is provided locally or on
   * a worker. If the service is provided by a worker, the `args` will be copied using the Structured Clone
   * algorithm, except for any items which are also in the `transfer` list. Ownership of those items will be
   * transferred to the worker, and they should not be used after this call.
   * @example
   *      dispatcher.call('vm', 'setData', 'cat', 42);
   *      // this finds the worker for the 'vm' service, then on that worker calls:
   *      vm.setData('cat', 42);
   * @param {string} service - the name of the service.
   * @param {string} method - the name of the method.
   * @param {*} [args] - the arguments to be copied to the method, if any.
   * @returns {Promise} - a promise for the return value of the service method.
   */
  return _createClass(SharedDispatch, [{
    key: "call",
    value: function call(service, method) {
      for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
        args[_key - 2] = arguments[_key];
      }
      return this.transferCall.apply(this, [service, method, null].concat(args));
    }

    /**
     * Call a particular method on a particular service, regardless of whether that service is provided locally or on
     * a worker. If the service is provided by a worker, the `args` will be copied using the Structured Clone
     * algorithm, except for any items which are also in the `transfer` list. Ownership of those items will be
     * transferred to the worker, and they should not be used after this call.
     * @example
     *      dispatcher.transferCall('vm', 'setData', [myArrayBuffer], 'cat', myArrayBuffer);
     *      // this finds the worker for the 'vm' service, transfers `myArrayBuffer` to it, then on that worker calls:
     *      vm.setData('cat', myArrayBuffer);
     * @param {string} service - the name of the service.
     * @param {string} method - the name of the method.
     * @param {Array} [transfer] - objects to be transferred instead of copied. Must be present in `args` to be useful.
     * @param {*} [args] - the arguments to be copied to the method, if any.
     * @returns {Promise} - a promise for the return value of the service method.
     */
  }, {
    key: "transferCall",
    value: function transferCall(service, method, transfer) {
      try {
        var _this$_getServiceProv = this._getServiceProvider(service),
          provider = _this$_getServiceProv.provider,
          isRemote = _this$_getServiceProv.isRemote;
        if (provider) {
          for (var _len2 = arguments.length, args = new Array(_len2 > 3 ? _len2 - 3 : 0), _key2 = 3; _key2 < _len2; _key2++) {
            args[_key2 - 3] = arguments[_key2];
          }
          if (isRemote) {
            return this._remoteTransferCall.apply(this, [provider, service, method, transfer].concat(args));
          }

          // TODO: verify correct `this` after switching from apply to spread
          // eslint-disable-next-line prefer-spread
          var result = provider[method].apply(provider, args);
          return Promise.resolve(result);
        }
        return Promise.reject(new Error("Service not found: ".concat(service)));
      } catch (e) {
        return Promise.reject(e);
      }
    }

    /**
     * Check if a particular service lives on another worker.
     * @param {string} service - the service to check.
     * @returns {boolean} - true if the service is remote (calls must cross a Worker boundary), false otherwise.
     * @private
     */
  }, {
    key: "_isRemoteService",
    value: function _isRemoteService(service) {
      return this._getServiceProvider(service).isRemote;
    }

    /**
     * Like {@link call}, but force the call to be posted through a particular communication channel.
     * @param {object} provider - send the call through this object's `postMessage` function.
     * @param {string} service - the name of the service.
     * @param {string} method - the name of the method.
     * @param {*} [args] - the arguments to be copied to the method, if any.
     * @returns {Promise} - a promise for the return value of the service method.
     */
  }, {
    key: "_remoteCall",
    value: function _remoteCall(provider, service, method) {
      for (var _len3 = arguments.length, args = new Array(_len3 > 3 ? _len3 - 3 : 0), _key3 = 3; _key3 < _len3; _key3++) {
        args[_key3 - 3] = arguments[_key3];
      }
      return this._remoteTransferCall.apply(this, [provider, service, method, null].concat(args));
    }

    /**
     * Like {@link transferCall}, but force the call to be posted through a particular communication channel.
     * @param {object} provider - send the call through this object's `postMessage` function.
     * @param {string} service - the name of the service.
     * @param {string} method - the name of the method.
     * @param {Array} [transfer] - objects to be transferred instead of copied. Must be present in `args` to be useful.
     * @param {*} [args] - the arguments to be copied to the method, if any.
     * @returns {Promise} - a promise for the return value of the service method.
     */
  }, {
    key: "_remoteTransferCall",
    value: function _remoteTransferCall(provider, service, method, transfer) {
      var _this = this;
      for (var _len4 = arguments.length, args = new Array(_len4 > 4 ? _len4 - 4 : 0), _key4 = 4; _key4 < _len4; _key4++) {
        args[_key4 - 4] = arguments[_key4];
      }
      return new Promise(function (resolve, reject) {
        var responseId = _this._storeCallbacks(resolve, reject);

        /** @TODO: remove this hack! this is just here so we don't try to send `util` to a worker */
        // tw: upstream's logic is broken
        // Args is actually a 3 length list of [args, util, real block info]
        // We only want to send args. The others will throw errors when they try to be cloned
        if (args.length > 0 && typeof args[args.length - 1].func === 'function') {
          args.pop();
          args.pop();
        }
        if (transfer) {
          provider.postMessage({
            service: service,
            method: method,
            responseId: responseId,
            args: args
          }, transfer);
        } else {
          provider.postMessage({
            service: service,
            method: method,
            responseId: responseId,
            args: args
          });
        }
      });
    }

    /**
     * Store callback functions pending a response message.
     * @param {Function} resolve - function to call if the service method returns.
     * @param {Function} reject - function to call if the service method throws.
     * @returns {*} - a unique response ID for this set of callbacks. See {@link _deliverResponse}.
     * @protected
     */
  }, {
    key: "_storeCallbacks",
    value: function _storeCallbacks(resolve, reject) {
      var responseId = this.nextResponseId++;
      this.callbacks[responseId] = [resolve, reject];
      return responseId;
    }

    /**
     * Deliver call response from a worker. This should only be called as the result of a message from a worker.
     * @param {int} responseId - the response ID of the callback set to call.
     * @param {DispatchResponseMessage} message - the message containing the response value(s).
     * @protected
     */
  }, {
    key: "_deliverResponse",
    value: function _deliverResponse(responseId, message) {
      try {
        var _this$callbacks$respo = _slicedToArray(this.callbacks[responseId], 2),
          resolve = _this$callbacks$respo[0],
          reject = _this$callbacks$respo[1];
        delete this.callbacks[responseId];
        if (message.error) {
          reject(message.error);
        } else {
          resolve(message.result);
        }
      } catch (e) {
        log.error("Dispatch callback failed: ".concat(e));
      }
    }

    /**
     * Handle a message event received from a connected worker.
     * @param {Worker} worker - the worker which sent the message, or the global object if running in a worker.
     * @param {MessageEvent} event - the message event to be handled.
     * @protected
     */
  }, {
    key: "_onMessage",
    value: function _onMessage(worker, event) {
      /** @type {DispatchMessage} */
      var message = event.data;
      message.args = message.args || [];
      var promise;
      if (message.service) {
        if (message.service === 'dispatch') {
          promise = this._onDispatchMessage(worker, message);
        } else {
          promise = this.call.apply(this, [message.service, message.method].concat(_toConsumableArray(message.args)));
        }
      } else if (typeof message.responseId === 'undefined') {
        log.error("Dispatch caught malformed message from a worker: ".concat(JSON.stringify(event)));
      } else {
        this._deliverResponse(message.responseId, message);
      }
      if (promise) {
        if (typeof message.responseId === 'undefined') {
          log.error("Dispatch message missing required response ID: ".concat(JSON.stringify(event)));
        } else {
          promise.then(function (result) {
            return worker.postMessage({
              responseId: message.responseId,
              result: result
            });
          }, function (error) {
            return worker.postMessage({
              responseId: message.responseId,
              error: "".concat(error)
            });
          });
        }
      }
    }

    /**
     * Fetch the service provider object for a particular service name.
     * @abstract
     * @param {string} service - the name of the service to look up
     * @returns {{provider:(object|Worker), isRemote:boolean}} - the means to contact the service, if found
     * @protected
     */
  }, {
    key: "_getServiceProvider",
    value: function _getServiceProvider(service) {
      throw new Error("Could not get provider for ".concat(service, ": _getServiceProvider not implemented"));
    }

    /**
     * Handle a call message sent to the dispatch service itself
     * @abstract
     * @param {Worker} worker - the worker which sent the message.
     * @param {DispatchCallMessage} message - the message to be handled.
     * @returns {Promise|undefined} - a promise for the results of this operation, if appropriate
     * @private
     */
  }, {
    key: "_onDispatchMessage",
    value: function _onDispatchMessage(worker, message) {
      throw new Error("Unimplemented dispatch message handler cannot handle ".concat(message.method, " method"));
    }
  }]);
}();
module.exports = SharedDispatch;

/***/ }),

/***/ "../vm/src/engine/adapter.js":
/*!***********************************!*\
  !*** ../vm/src/engine/adapter.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var mutationAdapter = __webpack_require__(/*! ./mutation-adapter */ "../vm/src/engine/mutation-adapter.js");
var html = __webpack_require__(/*! htmlparser2 */ "../vm/node_modules/htmlparser2/lib/index.js");
var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");

/**
 * Convert and an individual block DOM to the representation tree.
 * Based on Blockly's `domToBlockHeadless_`.
 * @param {Element} blockDOM DOM tree for an individual block.
 * @param {object} blocks Collection of blocks to add to.
 * @param {boolean} isTopBlock Whether blocks at this level are "top blocks."
 * @param {?string} parent Parent block ID.
 * @return {undefined}
 */
var _domToBlock = function domToBlock(blockDOM, blocks, isTopBlock, parent) {
  if (!blockDOM.attribs.id) {
    blockDOM.attribs.id = uid();
  }

  // Block skeleton.
  var block = {
    id: blockDOM.attribs.id,
    // Block ID
    opcode: blockDOM.attribs.type,
    // For execution, "event_whengreenflag".
    inputs: {},
    // Inputs to this block and the blocks they point to.
    fields: {},
    // Fields on this block and their values.
    next: null,
    // Next block in the stack, if one exists.
    topLevel: isTopBlock,
    // If this block starts a stack.
    parent: parent,
    // Parent block ID, if available.
    shadow: blockDOM.name === 'shadow',
    // If this represents a shadow/slot.
    x: blockDOM.attribs.x,
    // X position of script, if top-level.
    y: blockDOM.attribs.y // Y position of script, if top-level.
  };

  // Add the block to the representation tree.
  blocks[block.id] = block;

  // Process XML children and find enclosed blocks, fields, etc.
  for (var i = 0; i < blockDOM.children.length; i++) {
    var xmlChild = blockDOM.children[i];
    // Enclosed blocks and shadows
    var childBlockNode = null;
    var childShadowNode = null;
    for (var j = 0; j < xmlChild.children.length; j++) {
      var grandChildNode = xmlChild.children[j];
      if (!grandChildNode.name) {
        // Non-XML tag node.
        continue;
      }
      var grandChildNodeName = grandChildNode.name.toLowerCase();
      if (grandChildNodeName === 'block') {
        childBlockNode = grandChildNode;
      } else if (grandChildNodeName === 'shadow') {
        childShadowNode = grandChildNode;
      }
    }

    // Use shadow block only if there's no real block node.
    if (!childBlockNode && childShadowNode) {
      childBlockNode = childShadowNode;
    }

    // Not all Blockly-type blocks are handled here,
    // as we won't be using all of them for Scratch.
    switch (xmlChild.name.toLowerCase()) {
      case 'field':
        {
          // Add the field to this block.
          var fieldName = xmlChild.attribs.name;
          // Add id in case it is a variable field
          var fieldId = xmlChild.attribs.id;
          var fieldData = '';
          if (xmlChild.children.length > 0 && xmlChild.children[0].data) {
            fieldData = xmlChild.children[0].data;
          } else {
            // If the child of the field with a data property
            // doesn't exist, set the data to an empty string.
            fieldData = '';
          }
          block.fields[fieldName] = {
            name: fieldName,
            id: fieldId,
            value: fieldData
          };
          var fieldVarType = xmlChild.attribs.variabletype;
          if (typeof fieldVarType === 'string') {
            block.fields[fieldName].variableType = fieldVarType;
          }
          break;
        }
      case 'comment':
        {
          block.comment = xmlChild.attribs.id;
          break;
        }
      case 'value':
      case 'statement':
        {
          // Recursively generate block structure for input block.
          _domToBlock(childBlockNode, blocks, false, block.id);
          if (childShadowNode && childBlockNode !== childShadowNode) {
            // Also generate the shadow block.
            _domToBlock(childShadowNode, blocks, false, block.id);
          }
          // Link this block's input to the child block.
          var inputName = xmlChild.attribs.name;
          block.inputs[inputName] = {
            name: inputName,
            block: childBlockNode.attribs.id,
            shadow: childShadowNode ? childShadowNode.attribs.id : null
          };
          break;
        }
      case 'next':
        {
          if (!childBlockNode || !childBlockNode.attribs) {
            // Invalid child block.
            continue;
          }
          // Recursively generate block structure for next block.
          _domToBlock(childBlockNode, blocks, false, block.id);
          // Link next block to this block.
          block.next = childBlockNode.attribs.id;
          break;
        }
      case 'mutation':
        {
          block.mutation = mutationAdapter(xmlChild);
          break;
        }
    }
  }
};

/**
 * Convert outer blocks DOM from a Blockly CREATE event
 * to a usable form for the Scratch runtime.
 * This structure is based on Blockly xml.js:`domToWorkspace` and `domToBlock`.
 * @param {Element} blocksDOM DOM tree for this event.
 * @return {Array.<object>} Usable list of blocks from this CREATE event.
 */
var domToBlocks = function domToBlocks(blocksDOM) {
  // At this level, there could be multiple blocks adjacent in the DOM tree.
  var blocks = {};
  for (var i = 0; i < blocksDOM.length; i++) {
    var block = blocksDOM[i];
    if (!block.name || !block.attribs) {
      continue;
    }
    var tagName = block.name.toLowerCase();
    if (tagName === 'block' || tagName === 'shadow') {
      _domToBlock(block, blocks, true, null);
    }
  }
  // Flatten blocks object into a list.
  var blocksList = [];
  for (var b in blocks) {
    if (!Object.prototype.hasOwnProperty.call(blocks, b)) continue;
    blocksList.push(blocks[b]);
  }
  return blocksList;
};

/**
 * Adapter between block creation events and block representation which can be
 * used by the Scratch runtime.
 * @param {object} e `Blockly.events.create` or `Blockly.events.endDrag`
 * @return {Array.<object>} List of blocks from this CREATE event.
 */
var adapter = function adapter(e) {
  // Validate input
  if (_typeof(e) !== 'object') return;
  if (_typeof(e.xml) !== 'object') return;
  return domToBlocks(html.parseDOM(e.xml.outerHTML, {
    decodeEntities: true
  }));
};
module.exports = adapter;

/***/ }),

/***/ "../vm/src/engine/block-utility.js":
/*!*****************************************!*\
  !*** ../vm/src/engine/block-utility.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Thread = __webpack_require__(/*! ./thread */ "../vm/src/engine/thread.js");
var Timer = __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js");

/**
 * @fileoverview
 * Interface provided to block primitive functions for interacting with the
 * runtime, thread, target, and convenient methods.
 */
var BlockUtility = /*#__PURE__*/function () {
  function BlockUtility() {
    var _this = this;
    var sequencer = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
    var thread = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;
    _classCallCheck(this, BlockUtility);
    /**
     * A sequencer block primitives use to branch or start procedures with
     * @type {?Sequencer}
     */
    this.sequencer = sequencer;

    /**
     * The block primitives thread with the block's target, stackFrame and
     * modifiable status.
     * @type {?Thread}
     */
    this.thread = thread;
    this._nowObj = {
      now: function now() {
        return _this.sequencer.runtime.currentMSecs;
      }
    };
  }

  /**
   * The target the primitive is working on.
   * @type {Target}
   */
  return _createClass(BlockUtility, [{
    key: "target",
    get: function get() {
      return this.thread.target;
    }

    /**
     * The runtime the block primitive is running in.
     * @type {Runtime}
     */
  }, {
    key: "runtime",
    get: function get() {
      return this.sequencer.runtime;
    }

    /**
     * Use the runtime's currentMSecs value as a timestamp value for now
     * This is useful in some cases where we need compatibility with Scratch 2
     * @type {function}
     */
  }, {
    key: "nowObj",
    get: function get() {
      if (this.runtime) {
        return this._nowObj;
      }
      return null;
    }

    /**
     * The stack frame used by loop and other blocks to track internal state.
     * @type {object}
     */
  }, {
    key: "stackFrame",
    get: function get() {
      var frame = this.thread.peekStackFrame();
      if (frame.executionContext === null) {
        frame.executionContext = {};
      }
      return frame.executionContext;
    }

    /**
     * Check the stack timer and return a boolean based on whether it has finished or not.
     * @return {boolean} - true if the stack timer has finished.
     */
  }, {
    key: "stackTimerFinished",
    value: function stackTimerFinished() {
      var timeElapsed = this.stackFrame.timer.timeElapsed();
      if (timeElapsed < this.stackFrame.duration) {
        return false;
      }
      return true;
    }

    /**
     * Check if the stack timer needs initialization.
     * @return {boolean} - true if the stack timer needs to be initialized.
     */
  }, {
    key: "stackTimerNeedsInit",
    value: function stackTimerNeedsInit() {
      return !this.stackFrame.timer;
    }

    /**
     * Create and start a stack timer
     * @param {number} duration - a duration in milliseconds to set the timer for.
     */
  }, {
    key: "startStackTimer",
    value: function startStackTimer(duration) {
      if (this.nowObj) {
        this.stackFrame.timer = new Timer(this.nowObj);
      } else {
        this.stackFrame.timer = new Timer();
      }
      this.stackFrame.timer.start();
      this.stackFrame.duration = duration;
    }

    /**
     * Set the thread to yield.
     */
  }, {
    key: "yield",
    value: function _yield() {
      this.thread.status = Thread.STATUS_YIELD;
    }

    /**
     * Set the thread to yield until the next tick of the runtime.
     */
  }, {
    key: "yieldTick",
    value: function yieldTick() {
      this.thread.status = Thread.STATUS_YIELD_TICK;
    }

    /**
     * Start a branch in the current block.
     * @param {number} branchNum Which branch to step to (i.e., 1, 2).
     * @param {boolean} isLoop Whether this block is a loop.
     */
  }, {
    key: "startBranch",
    value: function startBranch(branchNum, isLoop) {
      this.sequencer.stepToBranch(this.thread, branchNum, isLoop);
    }

    /**
     * Stop all threads.
     */
  }, {
    key: "stopAll",
    value: function stopAll() {
      this.sequencer.runtime.stopAll();
    }

    /**
     * Stop threads other on this target other than the thread holding the
     * executed block.
     */
  }, {
    key: "stopOtherTargetThreads",
    value: function stopOtherTargetThreads() {
      this.sequencer.runtime.stopForTarget(this.thread.target, this.thread);
    }

    /**
     * Stop this thread.
     */
  }, {
    key: "stopThisScript",
    value: function stopThisScript() {
      this.thread.stopThisScript();
    }

    /**
     * Start a specified procedure on this thread.
     * @param {string} procedureCode Procedure code for procedure to start.
     */
  }, {
    key: "startProcedure",
    value: function startProcedure(procedureCode) {
      this.sequencer.stepToProcedure(this.thread, procedureCode);
    }

    /**
     * Get names and ids of parameters for the given procedure.
     * @param {string} procedureCode Procedure code for procedure to query.
     * @return {Array.<string>} List of param names for a procedure.
     */
  }, {
    key: "getProcedureParamNamesAndIds",
    value: function getProcedureParamNamesAndIds(procedureCode) {
      return this.thread.target.blocks.getProcedureParamNamesAndIds(procedureCode);
    }

    /**
     * Get names, ids, and defaults of parameters for the given procedure.
     * @param {string} procedureCode Procedure code for procedure to query.
     * @return {Array.<string>} List of param names for a procedure.
     */
  }, {
    key: "getProcedureParamNamesIdsAndDefaults",
    value: function getProcedureParamNamesIdsAndDefaults(procedureCode) {
      return this.thread.target.blocks.getProcedureParamNamesIdsAndDefaults(procedureCode);
    }

    /**
     * Initialize procedure parameters in the thread before pushing parameters.
     */
  }, {
    key: "initParams",
    value: function initParams() {
      this.thread.initParams();
    }

    /**
     * Store a procedure parameter value by its name.
     * @param {string} paramName The procedure's parameter name.
     * @param {*} paramValue The procedure's parameter value.
     */
  }, {
    key: "pushParam",
    value: function pushParam(paramName, paramValue) {
      this.thread.pushParam(paramName, paramValue);
    }

    /**
     * Retrieve the stored parameter value for a given parameter name.
     * @param {string} paramName The procedure's parameter name.
     * @return {*} The parameter's current stored value.
     */
  }, {
    key: "getParam",
    value: function getParam(paramName) {
      return this.thread.getParam(paramName);
    }

    /**
     * Start all relevant hats.
     * @param {!string} requestedHat Opcode of hats to start.
     * @param {object=} optMatchFields Optionally, fields to match on the hat.
     * @param {Target=} optTarget Optionally, a target to restrict to.
     * @return {Array.<Thread>} List of threads started by this function.
     */
  }, {
    key: "startHats",
    value: function startHats(requestedHat, optMatchFields, optTarget) {
      // Store thread and sequencer to ensure we can return to the calling block's context.
      // startHats may execute further blocks and dirty the BlockUtility's execution context
      // and confuse the calling block when we return to it.
      var callerThread = this.thread;
      var callerSequencer = this.sequencer;
      var result = this.sequencer.runtime.startHats(requestedHat, optMatchFields, optTarget);

      // Restore thread and sequencer to prior values before we return to the calling block.
      this.thread = callerThread;
      this.sequencer = callerSequencer;
      return result;
    }

    /**
     * Query a named IO device.
     * @param {string} device The name of like the device, like keyboard.
     * @param {string} func The name of the device's function to query.
     * @param {Array.<*>} args Arguments to pass to the device's function.
     * @return {*} The expected output for the device's function.
     */
  }, {
    key: "ioQuery",
    value: function ioQuery(device, func, args) {
      // Find the I/O device and execute the query/function call.
      if (this.sequencer.runtime.ioDevices[device] && this.sequencer.runtime.ioDevices[device][func]) {
        var devObject = this.sequencer.runtime.ioDevices[device];
        // TODO: verify correct `this` after switching from apply to spread
        // eslint-disable-next-line prefer-spread
        return devObject[func].apply(devObject, args);
      }
    }
  }]);
}();
module.exports = BlockUtility;

/***/ }),

/***/ "../vm/src/engine/blocks-execute-cache.js":
/*!************************************************!*\
  !*** ../vm/src/engine/blocks-execute-cache.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/**
 * @fileoverview
 * Access point for private method shared between blocks.js and execute.js for
 * caching execute information.
 */

/**
 * A private method shared with execute to build an object containing the block
 * information execute needs and that is reset when other cached Blocks info is
 * reset.
 * @param {Blocks} blocks Blocks containing the expected blockId
 * @param {string} blockId blockId for the desired execute cache
 */
exports.getCached = function () {
  throw new Error('blocks.js has not initialized BlocksExecuteCache');
};

// Call after the default throwing getCached is assigned for Blocks to replace.
__webpack_require__(/*! ./blocks */ "../vm/src/engine/blocks.js");

/***/ }),

/***/ "../vm/src/engine/blocks-runtime-cache.js":
/*!************************************************!*\
  !*** ../vm/src/engine/blocks-runtime-cache.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
/**
 * @fileoverview
 * The BlocksRuntimeCache caches data about the top block of scripts so that
 * Runtime can iterate a targeted opcode and iterate the returned set faster.
 * Many top blocks need to match fields as well as opcode, since that matching
 * compares strings in uppercase we can go ahead and uppercase the cached value
 * so we don't need to in the future.
 */
/**
 * A set of cached data about the top block of a script.
 * @param {Blocks} container - Container holding the block and related data
 * @param {string} blockId - Id for whose block data is cached in this instance
 */
var RuntimeScriptCache = /*#__PURE__*/_createClass(function RuntimeScriptCache(container, blockId) {
  _classCallCheck(this, RuntimeScriptCache);
  /**
   * Container with block data for blockId.
   * @type {Blocks}
   */
  this.container = container;

  /**
   * ID for block this instance caches.
   * @type {string}
   */
  this.blockId = blockId;
  var block = container.getBlock(blockId);
  var fields = container.getFields(block);

  /**
   * Formatted fields or fields of input blocks ready for comparison in
   * runtime.
   *
   * This is a clone of parts of the targeted blocks. Changes to these
   * clones are limited to copies under RuntimeScriptCache and will not
   * appear in the original blocks in their container. This copy is
   * modified changing the case of strings to uppercase. These uppercase
   * values will be compared later by the VM.
   * @type {object}
   */
  this.fieldsOfInputs = Object.assign({}, fields);
  if (Object.keys(fields).length === 0) {
    var inputs = container.getInputs(block);
    for (var input in inputs) {
      if (!Object.prototype.hasOwnProperty.call(inputs, input)) continue;
      var id = inputs[input].block;
      var inputBlock = container.getBlock(id);
      var inputFields = container.getFields(inputBlock);
      Object.assign(this.fieldsOfInputs, inputFields);
    }
  }
  for (var key in this.fieldsOfInputs) {
    var field = this.fieldsOfInputs[key] = Object.assign({}, this.fieldsOfInputs[key]);
    if (field.value.toUpperCase) {
      field.value = field.value.toUpperCase();
    }
  }
});
/**
 * Get an array of scripts from a block container prefiltered to match opcode.
 * @param {Blocks} container - Container of blocks
 * @param {string} opcode - Opcode to filter top blocks by
 */
exports.getScripts = function () {
  throw new Error('blocks.js has not initialized BlocksRuntimeCache');
};

/**
 * Exposed RuntimeScriptCache class used by integration in blocks.js.
 * @private
 */
exports._RuntimeScriptCache = RuntimeScriptCache;
__webpack_require__(/*! ./blocks */ "../vm/src/engine/blocks.js");

/***/ }),

/***/ "../vm/src/engine/blocks.js":
/*!**********************************!*\
  !*** ../vm/src/engine/blocks.js ***!
  \**********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var adapter = __webpack_require__(/*! ./adapter */ "../vm/src/engine/adapter.js");
var mutationAdapter = __webpack_require__(/*! ./mutation-adapter */ "../vm/src/engine/mutation-adapter.js");
var xmlEscape = __webpack_require__(/*! ../util/xml-escape */ "../vm/src/util/xml-escape.js");
var MonitorRecord = __webpack_require__(/*! ./monitor-record */ "../vm/src/engine/monitor-record.js");
var Clone = __webpack_require__(/*! ../util/clone */ "../vm/src/util/clone.js");
var BlocksExecuteCache = __webpack_require__(/*! ./blocks-execute-cache */ "../vm/src/engine/blocks-execute-cache.js");
var BlocksRuntimeCache = __webpack_require__(/*! ./blocks-runtime-cache */ "../vm/src/engine/blocks-runtime-cache.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var Variable = __webpack_require__(/*! ./variable */ "../vm/src/engine/variable.js");
var getMonitorIdForBlockWithArgs = __webpack_require__(/*! ../util/get-monitor-id */ "../vm/src/util/get-monitor-id.js");

/**
 * @fileoverview
 * Store and mutate the VM block representation,
 * and handle updates from Scratch Blocks events.
 */

/**
 * Create a block container.
 * @param {Runtime} runtime The runtime this block container operates within
 * @param {boolean} optNoGlow Optional flag to indicate that blocks in this container
 * should not request glows. This does not affect glows when clicking on a block to execute it.
 */
var Blocks = /*#__PURE__*/function () {
  function Blocks(runtime, optNoGlow) {
    _classCallCheck(this, Blocks);
    this.runtime = runtime;

    /**
     * All blocks in the workspace.
     * Keys are block IDs, values are metadata about the block.
     * @type {Object.<string, Object>}
     */
    this._blocks = {};

    /**
     * All top-level scripts in the workspace.
     * A list of block IDs that represent scripts (i.e., first block in script).
     * @type {Array.<String>}
     */
    this._scripts = [];

    /**
     * Runtime Cache
     * @type {{inputs: {}, procedureParamNames: {}, procedureDefinitions: {}}}
     * @private
     */
    Object.defineProperty(this, '_cache', {
      writable: true,
      enumerable: false
    });
    this._cache = {
      /**
       * Cache block inputs by block id
       * @type {object.<string, !Array.<object>>}
       */
      inputs: {},
      /**
       * Cache procedure Param Names by block id
       * @type {object.<string, ?Array.<string>>}
       */
      procedureParamNames: {},
      /**
       * Cache procedure definitions by block id
       * @type {object.<string, ?string>}
       */
      procedureDefinitions: {},
      /**
       * A cache for execute to use and store on. Only available to
       * execute.
       * @type {object.<string, object>}
       */
      _executeCached: {},
      /**
       * A cache of block IDs and targets to start threads on as they are
       * actively monitored.
       * @type {Array<{blockId: string, target: Target}>}
       */
      _monitored: null,
      /**
       * A cache of hat opcodes to collection of theads to execute.
       * @type {object.<string, object>}
       */
      scripts: {},
      /**
       * tw: A cache of top block (usually hat, but not always) opcodes to compiled scripts.
       * @type {object.<string, object>}
       */
      compiledScripts: {},
      /**
       * tw: A cache of procedure code opcodes to a parsed intermediate representation
       * @type {object.<string, object>}
       */
      compiledProcedures: {},
      /**
       * tw: Whether populateProcedureCache has been run
       */
      proceduresPopulated: false
    };

    /**
     * Flag which indicates that blocks in this container should not glow.
     * Blocks will still glow when clicked on, but this flag is used to control
     * whether the blocks in this container can request a glow as part of
     * a running stack. E.g. the flyout block container and the monitor block container
     * should not be able to request a glow, but blocks containers belonging to
     * sprites should.
     * @type {boolean}
     */
    this.forceNoGlow = optNoGlow || false;
  }

  /**
   * Get the cached compilation result of a block.
   * @param {string} blockId ID of the top block.
   * @returns {{success: boolean; value: any}|null} Cached success or error, or null if there is no cached value.
   */
  return _createClass(Blocks, [{
    key: "getCachedCompileResult",
    value: function getCachedCompileResult(blockId) {
      if (Object.prototype.hasOwnProperty.call(this._cache.compiledScripts, blockId)) {
        return this._cache.compiledScripts[blockId];
      }
      return null;
    }

    /**
     * Set the cached compilation result of a script.
     * @param {string} blockId ID of the top block.
     * @param {*} value The compilation result to store.
     */
  }, {
    key: "cacheCompileResult",
    value: function cacheCompileResult(blockId, value) {
      this._cache.compiledScripts[blockId] = {
        success: true,
        value: value
      };
    }

    /**
     * Set the cached error of a script.
     * @param {string} blockId ID of the top block.
     * @param {*} error The error to store.
     */
  }, {
    key: "cacheCompileError",
    value: function cacheCompileError(blockId, error) {
      this._cache.compiledScripts[blockId] = {
        success: false,
        value: error
      };
    }

    /**
     * Blockly inputs that represent statements/branch.
     * are prefixed with this string.
     * @const{string}
     */
  }, {
    key: "getBlock",
    value:
    /**
     * Provide an object with metadata for the requested block ID.
     * @param {!string} blockId ID of block we have stored.
     * @return {?object} Metadata about the block, if it exists.
     */
    function getBlock(blockId) {
      return this._blocks[blockId];
    }

    /**
     * Get all known top-level blocks that start scripts.
     * @return {Array.<string>} List of block IDs.
     */
  }, {
    key: "getScripts",
    value: function getScripts() {
      return this._scripts;
    }

    /**
      * Get the next block for a particular block
      * @param {?string} id ID of block to get the next block for
      * @return {?string} ID of next block in the sequence
      */
  }, {
    key: "getNextBlock",
    value: function getNextBlock(id) {
      var block = this._blocks[id];
      return typeof block === 'undefined' ? null : block.next;
    }

    /**
     * Get the branch for a particular C-shaped block.
     * @param {?string} id ID for block to get the branch for.
     * @param {?number} branchNum Which branch to select (e.g. for if-else).
     * @return {?string} ID of block in the branch.
     */
  }, {
    key: "getBranch",
    value: function getBranch(id, branchNum) {
      var block = this._blocks[id];
      if (typeof block === 'undefined') return null;
      if (!branchNum) branchNum = 1;
      var inputName = Blocks.BRANCH_INPUT_PREFIX;
      if (branchNum > 1) {
        inputName += branchNum;
      }

      // Empty C-block?
      var input = block.inputs[inputName];
      return typeof input === 'undefined' ? null : input.block;
    }

    /**
     * Get the opcode for a particular block
     * @param {?object} block The block to query
     * @return {?string} the opcode corresponding to that block
     */
  }, {
    key: "getOpcode",
    value: function getOpcode(block) {
      return typeof block === 'undefined' ? null : block.opcode;
    }

    /**
     * Get all fields and their values for a block.
     * @param {?object} block The block to query.
     * @return {?object} All fields and their values.
     */
  }, {
    key: "getFields",
    value: function getFields(block) {
      return typeof block === 'undefined' ? null : block.fields;
    }

    /**
     * Get all non-branch inputs for a block.
     * @param {?object} block the block to query.
     * @return {?Array.<object>} All non-branch inputs and their associated blocks.
     */
  }, {
    key: "getInputs",
    value: function getInputs(block) {
      if (typeof block === 'undefined') return null;
      var inputs = this._cache.inputs[block.id];
      if (typeof inputs !== 'undefined') {
        return inputs;
      }
      inputs = {};
      for (var input in block.inputs) {
        // Ignore blocks prefixed with branch prefix.
        if (input.substring(0, Blocks.BRANCH_INPUT_PREFIX.length) !== Blocks.BRANCH_INPUT_PREFIX) {
          inputs[input] = block.inputs[input];
        }
      }
      this._cache.inputs[block.id] = inputs;
      return inputs;
    }

    /**
     * Get mutation data for a block.
     * @param {?object} block The block to query.
     * @return {?object} Mutation for the block.
     */
  }, {
    key: "getMutation",
    value: function getMutation(block) {
      return typeof block === 'undefined' ? null : block.mutation;
    }

    /**
     * Get the top-level script for a given block.
     * @param {?string} id ID of block to query.
     * @return {?string} ID of top-level script block.
     */
  }, {
    key: "getTopLevelScript",
    value: function getTopLevelScript(id) {
      var block = this._blocks[id];
      if (typeof block === 'undefined') return null;
      while (block.parent !== null) {
        block = this._blocks[block.parent];
      }
      return block.id;
    }

    /**
     * Get the procedure definition for a given name.
     * @param {?string} name Name of procedure to query.
     * @return {?string} ID of procedure definition.
     */
  }, {
    key: "getProcedureDefinition",
    value: function getProcedureDefinition(name) {
      var blockID = this._cache.procedureDefinitions[name];
      if (typeof blockID !== 'undefined') {
        return blockID;
      }
      for (var id in this._blocks) {
        if (!Object.prototype.hasOwnProperty.call(this._blocks, id)) continue;
        var block = this._blocks[id];
        if (block.opcode === 'procedures_definition') {
          // tw: make sure that populateProcedureCache is kept up to date with this method
          var internal = this._getCustomBlockInternal(block);
          if (internal && internal.mutation.proccode === name) {
            this._cache.procedureDefinitions[name] = id; // The outer define block id
            return id;
          }
        }
      }
      this._cache.procedureDefinitions[name] = null;
      return null;
    }

    /**
     * Get names and ids of parameters for the given procedure.
     * @param {?string} name Name of procedure to query.
     * @return {?Array.<string>} List of param names for a procedure.
     */
  }, {
    key: "getProcedureParamNamesAndIds",
    value: function getProcedureParamNamesAndIds(name) {
      return this.getProcedureParamNamesIdsAndDefaults(name).slice(0, 2);
    }

    /**
     * Get names, ids, and defaults of parameters for the given procedure.
     * @param {?string} name Name of procedure to query.
     * @return {?Array.<string>} List of param names for a procedure.
     */
  }, {
    key: "getProcedureParamNamesIdsAndDefaults",
    value: function getProcedureParamNamesIdsAndDefaults(name) {
      var cachedNames = this._cache.procedureParamNames[name];
      if (typeof cachedNames !== 'undefined') {
        return cachedNames;
      }
      for (var id in this._blocks) {
        if (!Object.prototype.hasOwnProperty.call(this._blocks, id)) continue;
        var block = this._blocks[id];
        if (block.opcode === 'procedures_prototype' && block.mutation.proccode === name) {
          // tw: make sure that populateProcedureCache is kept up to date with this method
          var names = JSON.parse(block.mutation.argumentnames);
          var ids = JSON.parse(block.mutation.argumentids);
          var defaults = JSON.parse(block.mutation.argumentdefaults);
          this._cache.procedureParamNames[name] = [names, ids, defaults];
          return this._cache.procedureParamNames[name];
        }
      }
      var addonBlock = this.runtime.getAddonBlock(name);
      if (addonBlock) {
        this._cache.procedureParamNames[name] = addonBlock.namesIdsDefaults;
        return addonBlock.namesIdsDefaults;
      }
      this._cache.procedureParamNames[name] = null;
      return null;
    }

    /**
     * tw: Setup the procedureParamNames and procedureDefinitions caches all at once.
     * This makes subsequent calls to these methods faster.
     */
  }, {
    key: "populateProcedureCache",
    value: function populateProcedureCache() {
      if (this._cache.proceduresPopulated) {
        return;
      }
      for (var id in this._blocks) {
        if (!Object.prototype.hasOwnProperty.call(this._blocks, id)) continue;
        var block = this._blocks[id];
        if (block.opcode === 'procedures_prototype') {
          var name = block.mutation.proccode;
          if (!this._cache.procedureParamNames[name]) {
            var names = JSON.parse(block.mutation.argumentnames);
            var ids = JSON.parse(block.mutation.argumentids);
            var defaults = JSON.parse(block.mutation.argumentdefaults);
            this._cache.procedureParamNames[name] = [names, ids, defaults];
          }
          continue;
        }
        if (block.opcode === 'procedures_definition') {
          var internal = this._getCustomBlockInternal(block);
          if (internal) {
            var _name = internal.mutation.proccode;
            if (!this._cache.procedureDefinitions[_name]) {
              this._cache.procedureDefinitions[_name] = id;
            }
            continue;
          }
        }
      }
      this._cache.proceduresPopulated = true;
    }
  }, {
    key: "duplicate",
    value: function duplicate() {
      var newBlocks = new Blocks(this.runtime, this.forceNoGlow);
      newBlocks._blocks = Clone.simple(this._blocks);
      newBlocks._scripts = Clone.simple(this._scripts);
      return newBlocks;
    }
    // ---------------------------------------------------------------------

    /**
     * Create event listener for blocks, variables, and comments. Handles validation and
     * serves as a generic adapter between the blocks, variables, and the
     * runtime interface.
     * @param {object} e Blockly "block" or "variable" event
     */
  }, {
    key: "blocklyListen",
    value: function blocklyListen(e) {
      // Validate event
      if (_typeof(e) !== 'object') return;
      if (typeof e.blockId !== 'string' && typeof e.varId !== 'string' && typeof e.commentId !== 'string') {
        return;
      }
      var stage = this.runtime.getTargetForStage();
      var editingTarget = this.runtime.getEditingTarget();

      // UI event: clicked scripts toggle in the runtime.
      if (e.element === 'stackclick') {
        this.runtime.toggleScript(e.blockId, {
          stackClick: true
        });
        return;
      }

      // Block create/update/destroy
      switch (e.type) {
        case 'create':
          {
            var newBlocks = adapter(e);
            // A create event can create many blocks. Add them all.
            for (var i = 0; i < newBlocks.length; i++) {
              this.createBlock(newBlocks[i]);
            }
            break;
          }
        case 'change':
          this.changeBlock({
            id: e.blockId,
            element: e.element,
            name: e.name,
            value: e.newValue
          });
          break;
        case 'move':
          this.moveBlock({
            id: e.blockId,
            oldParent: e.oldParentId,
            oldInput: e.oldInputName,
            newParent: e.newParentId,
            newInput: e.newInputName,
            newCoordinate: e.newCoordinate
          });
          break;
        case 'dragOutside':
          this.runtime.emitBlockDragUpdate(e.isOutside);
          break;
        case 'endDrag':
          this.runtime.emitBlockDragUpdate(false /* areBlocksOverGui */);

          // Drag blocks onto another sprite
          if (e.isOutside) {
            var _newBlocks = adapter(e);
            this.runtime.emitBlockEndDrag(_newBlocks, e.blockId);
          }
          break;
        case 'delete':
          // Don't accept delete events for missing blocks,
          // or shadow blocks being obscured.
          if (!Object.prototype.hasOwnProperty.call(this._blocks, e.blockId) || this._blocks[e.blockId].shadow) {
            return;
          }
          // Inform any runtime to forget about glows on this script.
          if (this._blocks[e.blockId].topLevel) {
            this.runtime.quietGlow(e.blockId);
          }
          this.deleteBlock(e.blockId);
          break;
        case 'var_create':
          this.resetCache(); // tw: more aggressive cache resetting
          // Check if the variable being created is global or local
          // If local, create a local var on the current editing target, as long
          // as there are no conflicts, and the current target is actually a sprite
          // If global or if the editing target is not present or we somehow got
          // into a state where a local var was requested for the stage,
          // create a stage (global) var after checking for name conflicts
          // on all the sprites.
          if (e.isLocal && editingTarget && !editingTarget.isStage && !e.isCloud) {
            if (!editingTarget.lookupVariableById(e.varId)) {
              editingTarget.createVariable(e.varId, e.varName, e.varType);
              this.emitProjectChanged();
            }
          } else {
            if (stage.lookupVariableById(e.varId)) {
              // Do not re-create a variable if it already exists
              return;
            }
            // Check for name conflicts in all of the targets
            var allTargets = this.runtime.targets.filter(function (t) {
              return t.isOriginal;
            });
            var _iterator = _createForOfIteratorHelper(allTargets),
              _step;
            try {
              for (_iterator.s(); !(_step = _iterator.n()).done;) {
                var target = _step.value;
                if (target.lookupVariableByNameAndType(e.varName, e.varType, true)) {
                  return;
                }
              }
            } catch (err) {
              _iterator.e(err);
            } finally {
              _iterator.f();
            }
            stage.createVariable(e.varId, e.varName, e.varType, e.isCloud);
            this.emitProjectChanged();
          }
          break;
        case 'var_rename':
          if (editingTarget && Object.prototype.hasOwnProperty.call(editingTarget.variables, e.varId)) {
            // This is a local variable, rename on the current target
            editingTarget.renameVariable(e.varId, e.newName);
            // Update all the blocks on the current target that use
            // this variable
            editingTarget.blocks.updateBlocksAfterVarRename(e.varId, e.newName);
          } else {
            // This is a global variable
            stage.renameVariable(e.varId, e.newName);
            // Update all blocks on all targets that use the renamed variable
            var targets = this.runtime.targets;
            for (var _i = 0; _i < targets.length; _i++) {
              var currTarget = targets[_i];
              currTarget.blocks.updateBlocksAfterVarRename(e.varId, e.newName);
            }
          }
          this.emitProjectChanged();
          break;
        case 'var_delete':
          {
            this.resetCache(); // tw: more aggressive cache resetting
            var _target = editingTarget && Object.prototype.hasOwnProperty.call(editingTarget.variables, e.varId) ? editingTarget : stage;
            _target.deleteVariable(e.varId);
            this.emitProjectChanged();
            break;
          }
        case 'comment_create':
          this.resetCache(); // tw: comments can affect compilation
          if (this.runtime.getEditingTarget()) {
            var _currTarget = this.runtime.getEditingTarget();
            _currTarget.createComment(e.commentId, e.blockId, e.text, e.xy.x, e.xy.y, e.width, e.height, e.minimized);
            if (_currTarget.comments[e.commentId].x === null && _currTarget.comments[e.commentId].y === null) {
              // Block comments imported from 2.0 projects are imported with their
              // x and y coordinates set to null so that scratch-blocks can
              // auto-position them. If we are receiving a create event for these
              // comments, then the auto positioning should have taken place.
              // Update the x and y position of these comments to match the
              // one from the event.
              _currTarget.comments[e.commentId].x = e.xy.x;
              _currTarget.comments[e.commentId].y = e.xy.y;
            }
          }
          this.emitProjectChanged();
          break;
        case 'comment_change':
          this.resetCache(); // tw: comments can affect compilation
          if (this.runtime.getEditingTarget()) {
            var _currTarget2 = this.runtime.getEditingTarget();
            if (!Object.prototype.hasOwnProperty.call(_currTarget2.comments, e.commentId)) {
              log.warn("Cannot change comment with id ".concat(e.commentId, " because it does not exist."));
              return;
            }
            var comment = _currTarget2.comments[e.commentId];
            var change = e.newContents_;
            if (Object.prototype.hasOwnProperty.call(change, 'minimized')) {
              comment.minimized = change.minimized;
            }
            if (Object.prototype.hasOwnProperty.call(change, 'width') && Object.prototype.hasOwnProperty.call(change, 'height')) {
              comment.width = change.width;
              comment.height = change.height;
            }
            if (Object.prototype.hasOwnProperty.call(change, 'text')) {
              comment.text = change.text;
            }
            this.emitProjectChanged();
          }
          break;
        case 'comment_move':
          if (this.runtime.getEditingTarget()) {
            var _currTarget3 = this.runtime.getEditingTarget();
            if (_currTarget3 && !Object.prototype.hasOwnProperty.call(_currTarget3.comments, e.commentId)) {
              log.warn("Cannot change comment with id ".concat(e.commentId, " because it does not exist."));
              return;
            }
            var _comment = _currTarget3.comments[e.commentId];
            var newCoord = e.newCoordinate_;
            _comment.x = newCoord.x;
            _comment.y = newCoord.y;
            this.emitProjectChanged();
          }
          break;
        case 'comment_delete':
          this.resetCache(); // tw: comments can affect compilation
          if (this.runtime.getEditingTarget()) {
            var _currTarget4 = this.runtime.getEditingTarget();
            if (!Object.prototype.hasOwnProperty.call(_currTarget4.comments, e.commentId)) {
              // If we're in this state, we have probably received
              // a delete event from a workspace that we switched from
              // (e.g. a delete event for a comment on sprite a's workspace
              // when switching from sprite a to sprite b)
              return;
            }
            delete _currTarget4.comments[e.commentId];
            if (e.blockId) {
              var block = _currTarget4.blocks.getBlock(e.blockId);
              if (!block) {
                log.warn("Could not find block referenced by comment with id: ".concat(e.commentId));
                return;
              }
              delete block.comment;
            }
            this.emitProjectChanged();
          }
          break;
      }
    }

    // ---------------------------------------------------------------------

    /**
     * Reset all runtime caches.
     */
  }, {
    key: "resetCache",
    value: function resetCache() {
      this._cache.inputs = {};
      this._cache.procedureParamNames = {};
      this._cache.procedureDefinitions = {};
      this._cache._executeCached = {};
      this._cache._monitored = null;
      this._cache.scripts = {};
      this._cache.compiledScripts = {};
      this._cache.compiledProcedures = {};
      this._cache.proceduresPopulated = false;
    }

    /**
     * Emit a project changed event if this is a block container
     * that can affect the project state.
     */
  }, {
    key: "emitProjectChanged",
    value: function emitProjectChanged() {
      if (!this.forceNoGlow) {
        this.runtime.emitProjectChanged();
      }
    }

    /**
     * Block management: create blocks and scripts from a `create` event
     * @param {!object} block Blockly create event to be processed
     */
  }, {
    key: "createBlock",
    value: function createBlock(block) {
      // Does the block already exist?
      // Could happen, e.g., for an unobscured shadow.
      if (Object.prototype.hasOwnProperty.call(this._blocks, block.id)) {
        return;
      }
      // Create new block.
      this._blocks[block.id] = block;
      // Push block id to scripts array.
      // Blocks are added as a top-level stack if they are marked as a top-block
      // (if they were top-level XML in the event).
      if (block.topLevel) {
        this._addScript(block.id);
      }
      this.resetCache();

      // A new block was actually added to the block container,
      // emit a project changed event
      this.emitProjectChanged();
    }

    /**
     * Block management: change block field values
     * @param {!object} args Blockly change event to be processed
     */
  }, {
    key: "changeBlock",
    value: function changeBlock(args) {
      // Validate
      if (['field', 'mutation', 'checkbox'].indexOf(args.element) === -1) return;
      var block = this._blocks[args.id];
      if (typeof block === 'undefined') return;
      switch (args.element) {
        case 'field':
          // TODO when the field of a monitored block changes,
          // update the checkbox in the flyout based on whether
          // a monitor for that current combination of selected parameters exists
          // e.g.
          // 1. check (current [v year])
          // 2. switch dropdown in flyout block to (current [v minute])
          // 3. the checkbox should become unchecked if we're not already
          //    monitoring current minute

          // Update block value
          if (!block.fields[args.name]) return;
          if (args.name === 'VARIABLE' || args.name === 'LIST' || args.name === 'BROADCAST_OPTION') {
            // Get variable name using the id in args.value.
            var variable = this.runtime.getEditingTarget().lookupVariableById(args.value);
            if (variable) {
              block.fields[args.name].value = variable.name;
              block.fields[args.name].id = args.value;
            }
          } else {
            // Changing the value in a dropdown
            block.fields[args.name].value = args.value;

            // The selected item in the sensing of block menu needs to change based on the
            // selected target.  Set it to the first item in the menu list.
            // TODO: (#1787)
            if (block.opcode === 'sensing_of_object_menu') {
              if (block.fields.OBJECT.value === '_stage_') {
                this._blocks[block.parent].fields.PROPERTY.value = 'backdrop #';
              } else {
                this._blocks[block.parent].fields.PROPERTY.value = 'x position';
              }
              this.runtime.requestBlocksUpdate();
            }
            var flyoutBlock = block.shadow && block.parent ? this._blocks[block.parent] : block;
            if (flyoutBlock.isMonitored) {
              this.runtime.requestUpdateMonitor({
                id: flyoutBlock.id,
                params: this._getBlockParams(flyoutBlock)
              });
            }
          }
          break;
        case 'mutation':
          block.mutation = mutationAdapter(args.value);
          break;
        case 'checkbox':
          {
            // A checkbox usually has a one to one correspondence with the monitor
            // block but in the case of monitored reporters that have arguments,
            // map the old id to a new id, creating a new monitor block if necessary
            if (block.fields && Object.keys(block.fields).length > 0 && block.opcode !== 'data_variable' && block.opcode !== 'data_listcontents') {
              // This block has an argument which needs to get separated out into
              // multiple monitor blocks with ids based on the selected argument
              var newId = getMonitorIdForBlockWithArgs(block.id, block.fields);
              // Note: we're not just constantly creating a longer and longer id everytime we check
              // the checkbox because we're using the id of the block in the flyout as the base

              // check if a block with the new id already exists, otherwise create
              var newBlock = this.runtime.monitorBlocks.getBlock(newId);
              if (!newBlock) {
                newBlock = JSON.parse(JSON.stringify(block));
                newBlock.id = newId;
                this.runtime.monitorBlocks.createBlock(newBlock);
              }
              block = newBlock; // Carry on through the rest of this code with newBlock
            }
            var wasMonitored = block.isMonitored;
            block.isMonitored = args.value;

            // Variable blocks may be sprite specific depending on the owner of the variable
            var isSpriteLocalVariable = false;
            if (block.opcode === 'data_variable') {
              isSpriteLocalVariable = !this.runtime.getTargetForStage().variables[block.fields.VARIABLE.id];
            } else if (block.opcode === 'data_listcontents') {
              isSpriteLocalVariable = !this.runtime.getTargetForStage().variables[block.fields.LIST.id];
            }
            var isSpriteSpecific = isSpriteLocalVariable || Object.prototype.hasOwnProperty.call(this.runtime.monitorBlockInfo, block.opcode) && this.runtime.monitorBlockInfo[block.opcode].isSpriteSpecific;
            if (isSpriteSpecific) {
              // If creating a new sprite specific monitor, the only possible target is
              // the current editing one b/c you cannot dynamically create monitors.
              // Also, do not change the targetId if it has already been assigned
              block.targetId = block.targetId || this.runtime.getEditingTarget().id;
            } else {
              block.targetId = null;
            }
            if (wasMonitored && !block.isMonitored) {
              this.runtime.requestHideMonitor(block.id);
            } else if (!wasMonitored && block.isMonitored) {
              // Tries to show the monitor for specified block. If it doesn't exist, add the monitor.
              if (!this.runtime.requestShowMonitor(block.id)) {
                this.runtime.requestAddMonitor(new MonitorRecord({
                  id: block.id,
                  targetId: block.targetId,
                  spriteName: block.targetId ? this.runtime.getTargetById(block.targetId).getName() : null,
                  opcode: block.opcode,
                  params: this._getBlockParams(block),
                  // @todo(vm#565) for numerical values with decimals, some countries use comma
                  value: '',
                  mode: block.opcode === 'data_listcontents' ? 'list' : 'default'
                }));
              }
            }
            break;
          }
      }
      this.emitProjectChanged();
      this.resetCache();
    }

    /**
     * Block management: move blocks from parent to parent
     * @param {!object} e Blockly move event to be processed
     */
  }, {
    key: "moveBlock",
    value: function moveBlock(e) {
      if (!Object.prototype.hasOwnProperty.call(this._blocks, e.id)) {
        return;
      }
      var block = this._blocks[e.id];
      // Track whether a change actually occurred
      // ignoring changes like routine re-positioning
      // of a block when loading a workspace
      var didChange = false;

      // Move coordinate changes.
      if (e.newCoordinate) {
        didChange = block.x !== e.newCoordinate.x || block.y !== e.newCoordinate.y;
        block.x = e.newCoordinate.x;
        block.y = e.newCoordinate.y;
      }

      // Remove from any old parent.
      if (typeof e.oldParent !== 'undefined') {
        var oldParent = this._blocks[e.oldParent];
        if (typeof e.oldInput !== 'undefined' && oldParent.inputs[e.oldInput].block === e.id) {
          // This block was connected to the old parent's input.
          oldParent.inputs[e.oldInput].block = null;
        } else if (oldParent.next === e.id) {
          // This block was connected to the old parent's next connection.
          oldParent.next = null;
        }
        this._blocks[e.id].parent = null;
        didChange = true;
      }

      // Is this block a top-level block?
      if (typeof e.newParent === 'undefined') {
        this._addScript(e.id);
      } else {
        // Remove script, if one exists.
        this._deleteScript(e.id);
        // Otherwise, try to connect it in its new place.
        if (typeof e.newInput === 'undefined') {
          // Moved to the new parent's next connection.
          this._blocks[e.newParent].next = e.id;
        } else {
          // Moved to the new parent's input.
          // Don't obscure the shadow block.
          var oldShadow = null;
          if (Object.prototype.hasOwnProperty.call(this._blocks[e.newParent].inputs, e.newInput)) {
            oldShadow = this._blocks[e.newParent].inputs[e.newInput].shadow;
          }

          // If the block being attached is itself a shadow, make sure to set
          // both block and shadow to that blocks ID. This happens when adding
          // inputs to a custom procedure.
          if (this._blocks[e.id].shadow) oldShadow = e.id;
          this._blocks[e.newParent].inputs[e.newInput] = {
            name: e.newInput,
            block: e.id,
            shadow: oldShadow
          };
        }
        this._blocks[e.id].parent = e.newParent;
        didChange = true;
      }
      this.resetCache();
      if (didChange) this.emitProjectChanged();
    }

    /**
     * Block management: run all blocks.
     * @param {!object} runtime Runtime to run all blocks in.
     */
  }, {
    key: "runAllMonitored",
    value: function runAllMonitored(runtime) {
      var _this = this;
      if (this._cache._monitored === null) {
        this._cache._monitored = Object.keys(this._blocks).filter(function (blockId) {
          return _this.getBlock(blockId).isMonitored;
        }).map(function (blockId) {
          var targetId = _this.getBlock(blockId).targetId;
          return {
            blockId: blockId,
            target: targetId ? runtime.getTargetById(targetId) : null
          };
        });
      }
      var monitored = this._cache._monitored;
      for (var i = 0; i < monitored.length; i++) {
        var _monitored$i = monitored[i],
          blockId = _monitored$i.blockId,
          target = _monitored$i.target;
        runtime.addMonitorScript(blockId, target);
      }
    }

    /**
     * Block management: delete blocks and their associated scripts. Does nothing if a block
     * with the given ID does not exist.
     * @param {!string} blockId Id of block to delete
     */
  }, {
    key: "deleteBlock",
    value: function deleteBlock(blockId) {
      // @todo In runtime, stop threads running on this script.

      // Get block
      var block = this._blocks[blockId];
      if (!block) {
        // No block with the given ID exists
        return;
      }

      // Delete children
      if (block.next !== null) {
        this.deleteBlock(block.next);
      }

      // Delete inputs (including branches)
      for (var input in block.inputs) {
        // If it's null, the block in this input moved away.
        if (block.inputs[input].block !== null) {
          this.deleteBlock(block.inputs[input].block);
        }
        // Delete obscured shadow blocks.
        if (block.inputs[input].shadow !== null && block.inputs[input].shadow !== block.inputs[input].block) {
          this.deleteBlock(block.inputs[input].shadow);
        }
      }

      // Delete any script starting with this block.
      this._deleteScript(blockId);

      // Delete block itself.
      delete this._blocks[blockId];
      this.resetCache();
      this.emitProjectChanged();
    }

    /**
     * Delete all blocks and their associated scripts.
     */
  }, {
    key: "deleteAllBlocks",
    value: function deleteAllBlocks() {
      var _this2 = this;
      var blockIds = Object.keys(this._blocks);
      blockIds.forEach(function (blockId) {
        return _this2.deleteBlock(blockId);
      });
    }

    /**
     * Returns a map of all references to variables or lists from blocks
     * in this block container.
     * @param {Array<object>} optBlocks Optional list of blocks to constrain the search to.
     * This is useful for getting variable/list references for a stack of blocks instead
     * of all blocks on the workspace
     * @param {?boolean} optIncludeBroadcast Optional whether to include broadcast fields.
     * @return {object} A map of variable ID to a list of all variable references
     * for that ID. A variable reference contains the field referencing that variable
     * and also the type of the variable being referenced.
     */
  }, {
    key: "getAllVariableAndListReferences",
    value: function getAllVariableAndListReferences(optBlocks, optIncludeBroadcast) {
      var blocks = optBlocks ? optBlocks : this._blocks;
      var allReferences = Object.create(null);
      for (var blockId in blocks) {
        var varOrListField = null;
        var varType = null;
        if (blocks[blockId].fields.VARIABLE) {
          varOrListField = blocks[blockId].fields.VARIABLE;
          varType = Variable.SCALAR_TYPE;
        } else if (blocks[blockId].fields.LIST) {
          varOrListField = blocks[blockId].fields.LIST;
          varType = Variable.LIST_TYPE;
        } else if (optIncludeBroadcast && blocks[blockId].fields.BROADCAST_OPTION) {
          varOrListField = blocks[blockId].fields.BROADCAST_OPTION;
          varType = Variable.BROADCAST_MESSAGE_TYPE;
        }
        if (varOrListField) {
          var currVarId = varOrListField.id;
          if (allReferences[currVarId]) {
            allReferences[currVarId].push({
              referencingField: varOrListField,
              type: varType
            });
          } else {
            allReferences[currVarId] = [{
              referencingField: varOrListField,
              type: varType
            }];
          }
        }
      }
      return allReferences;
    }

    /**
     * Keep blocks up to date after a variable gets renamed.
     * @param {string} varId The id of the variable that was renamed
     * @param {string} newName The new name of the variable that was renamed
     */
  }, {
    key: "updateBlocksAfterVarRename",
    value: function updateBlocksAfterVarRename(varId, newName) {
      var blocks = this._blocks;
      for (var blockId in blocks) {
        var varOrListField = null;
        if (blocks[blockId].fields.VARIABLE) {
          varOrListField = blocks[blockId].fields.VARIABLE;
        } else if (blocks[blockId].fields.LIST) {
          varOrListField = blocks[blockId].fields.LIST;
        }
        if (varOrListField) {
          var currFieldId = varOrListField.id;
          if (varId === currFieldId) {
            varOrListField.value = newName;
          }
        }
      }
    }

    /**
     * Keep blocks up to date after they are shared between targets.
     * @param {boolean} isStage If the new target is a stage.
     */
  }, {
    key: "updateTargetSpecificBlocks",
    value: function updateTargetSpecificBlocks(isStage) {
      var blocks = this._blocks;
      for (var blockId in blocks) {
        if (isStage && blocks[blockId].opcode === 'event_whenthisspriteclicked') {
          blocks[blockId].opcode = 'event_whenstageclicked';
        } else if (!isStage && blocks[blockId].opcode === 'event_whenstageclicked') {
          blocks[blockId].opcode = 'event_whenthisspriteclicked';
        }
      }
    }

    /**
     * Update blocks after a sound, costume, or backdrop gets renamed.
     * Any block referring to the old name of the asset should get updated
     * to refer to the new name.
     * @param {string} oldName The old name of the asset that was renamed.
     * @param {string} newName The new name of the asset that was renamed.
     * @param {string} assetType String representation of the kind of asset
     * that was renamed. This can be one of 'sprite','costume', 'sound', or
     * 'backdrop'.
     */
  }, {
    key: "updateAssetName",
    value: function updateAssetName(oldName, newName, assetType) {
      var getAssetField;
      if (assetType === 'costume') {
        getAssetField = this._getCostumeField.bind(this);
      } else if (assetType === 'sound') {
        getAssetField = this._getSoundField.bind(this);
      } else if (assetType === 'backdrop') {
        getAssetField = this._getBackdropField.bind(this);
      } else if (assetType === 'sprite') {
        getAssetField = this._getSpriteField.bind(this);
      } else {
        return;
      }
      var blocks = this._blocks;
      for (var blockId in blocks) {
        var assetField = getAssetField(blockId);
        if (assetField && assetField.value === oldName) {
          assetField.value = newName;
        }
      }
      this.resetCache();
    }

    /**
     * Update sensing_of blocks after a variable gets renamed.
     * @param {string} oldName The old name of the variable that was renamed.
     * @param {string} newName The new name of the variable that was renamed.
     * @param {string} targetName The name of the target the variable belongs to.
     * @return {boolean} Returns true if any of the blocks were updated.
     */
  }, {
    key: "updateSensingOfReference",
    value: function updateSensingOfReference(oldName, newName, targetName) {
      var blocks = this._blocks;
      var blockUpdated = false;
      for (var blockId in blocks) {
        var block = blocks[blockId];
        if (block.opcode === 'sensing_of' && block.fields.PROPERTY.value === oldName &&
        // If block and shadow are different, it means a block is inserted to OBJECT, and should be ignored.
        block.inputs.OBJECT.block === block.inputs.OBJECT.shadow) {
          var inputBlock = this.getBlock(block.inputs.OBJECT.block);
          if (inputBlock.fields.OBJECT.value === targetName) {
            block.fields.PROPERTY.value = newName;
            blockUpdated = true;
          }
        }
      }
      if (blockUpdated) this.resetCache();
      return blockUpdated;
    }

    /**
     * Helper function to retrieve a costume menu field from a block given its id.
     * @param {string} blockId A unique identifier for a block
     * @return {?object} The costume menu field of the block with the given block id.
     * Null if either a block with the given id doesn't exist or if a costume menu field
     * does not exist on the block with the given id.
     */
  }, {
    key: "_getCostumeField",
    value: function _getCostumeField(blockId) {
      var block = this.getBlock(blockId);
      if (block && Object.prototype.hasOwnProperty.call(block.fields, 'COSTUME')) {
        return block.fields.COSTUME;
      }
      return null;
    }

    /**
     * Helper function to retrieve a sound menu field from a block given its id.
     * @param {string} blockId A unique identifier for a block
     * @return {?object} The sound menu field of the block with the given block id.
     * Null, if either a block with the given id doesn't exist or if a sound menu field
     * does not exist on the block with the given id.
     */
  }, {
    key: "_getSoundField",
    value: function _getSoundField(blockId) {
      var block = this.getBlock(blockId);
      if (block && Object.prototype.hasOwnProperty.call(block.fields, 'SOUND_MENU')) {
        return block.fields.SOUND_MENU;
      }
      return null;
    }

    /**
     * Helper function to retrieve a backdrop menu field from a block given its id.
     * @param {string} blockId A unique identifier for a block
     * @return {?object} The backdrop menu field of the block with the given block id.
     * Null, if either a block with the given id doesn't exist or if a backdrop menu field
     * does not exist on the block with the given id.
     */
  }, {
    key: "_getBackdropField",
    value: function _getBackdropField(blockId) {
      var block = this.getBlock(blockId);
      if (block && Object.prototype.hasOwnProperty.call(block.fields, 'BACKDROP')) {
        return block.fields.BACKDROP;
      }
      return null;
    }

    /**
     * Helper function to retrieve a sprite menu field from a block given its id.
     * @param {string} blockId A unique identifier for a block
     * @return {?object} The sprite menu field of the block with the given block id.
     * Null, if either a block with the given id doesn't exist or if a sprite menu field
     * does not exist on the block with the given id.
     */
  }, {
    key: "_getSpriteField",
    value: function _getSpriteField(blockId) {
      var block = this.getBlock(blockId);
      if (!block) {
        return null;
      }
      var spriteMenuNames = ['TOWARDS', 'TO', 'OBJECT', 'VIDEOONMENU2', 'DISTANCETOMENU', 'TOUCHINGOBJECTMENU', 'CLONE_OPTION'];
      for (var i = 0; i < spriteMenuNames.length; i++) {
        var menuName = spriteMenuNames[i];
        if (Object.prototype.hasOwnProperty.call(block.fields, menuName)) {
          return block.fields[menuName];
        }
      }
      return null;
    }

    // ---------------------------------------------------------------------

    /**
     * Encode all of `this._blocks` as an XML string usable
     * by a Blockly/scratch-blocks workspace.
     * @param {object<string, Comment>} comments Map of comments referenced by id
     * @return {string} String of XML representing this object's blocks.
     */
  }, {
    key: "toXML",
    value: function toXML(comments) {
      var _this3 = this;
      return this._scripts.map(function (script) {
        return _this3.blockToXML(script, comments);
      }).join();
    }

    /**
     * Recursively encode an individual block and its children
     * into a Blockly/scratch-blocks XML string.
     * @param {!string} blockId ID of block to encode.
     * @param {object<string, Comment>} comments Map of comments referenced by id
     * @return {string} String of XML representing this block and any children.
     */
  }, {
    key: "blockToXML",
    value: function blockToXML(blockId, comments) {
      var block = this._blocks[blockId];
      // block should exist, but currently some blocks' next property point
      // to a blockId for non-existent blocks. Until we track down that behavior,
      // this early exit allows the project to load.
      if (!block) return;
      // Encode properties of this block.
      var tagName = block.shadow ? 'shadow' : 'block';
      var xmlString = "<".concat(tagName, "\n                id=\"").concat(xmlEscape(block.id), "\"\n                type=\"").concat(xmlEscape(block.opcode), "\"\n                ").concat(block.topLevel ? "x=\"".concat(block.x, "\" y=\"").concat(block.y, "\"") : '', "\n            >");
      var commentId = block.comment;
      if (commentId) {
        if (comments) {
          if (Object.prototype.hasOwnProperty.call(comments, commentId)) {
            xmlString += comments[commentId].toXML();
          } else {
            log.warn("Could not find comment with id: ".concat(commentId, " in provided comment descriptions."));
          }
        } else {
          log.warn("Cannot serialize comment with id: ".concat(commentId, "; no comment descriptions provided."));
        }
      }
      // Add any mutation. Must come before inputs.
      if (block.mutation) {
        xmlString += this.mutationToXML(block.mutation);
      }
      // Add any inputs on this block.
      for (var input in block.inputs) {
        if (!Object.prototype.hasOwnProperty.call(block.inputs, input)) continue;
        var blockInput = block.inputs[input];
        // Only encode a value tag if the value input is occupied.
        if (blockInput.block || blockInput.shadow) {
          xmlString += "<value name=\"".concat(xmlEscape(blockInput.name), "\">");
          if (blockInput.block) {
            xmlString += this.blockToXML(blockInput.block, comments);
          }
          if (blockInput.shadow && blockInput.shadow !== blockInput.block) {
            // Obscured shadow.
            xmlString += this.blockToXML(blockInput.shadow, comments);
          }
          xmlString += '</value>';
        }
      }
      // Add any fields on this block.
      for (var field in block.fields) {
        if (!Object.prototype.hasOwnProperty.call(block.fields, field)) continue;
        var blockField = block.fields[field];
        xmlString += "<field name=\"".concat(xmlEscape(blockField.name), "\"");
        var fieldId = blockField.id;
        if (fieldId) {
          xmlString += " id=\"".concat(xmlEscape(fieldId), "\"");
        }
        var varType = blockField.variableType;
        if (typeof varType === 'string') {
          xmlString += " variabletype=\"".concat(xmlEscape(varType), "\"");
        }
        var value = blockField.value;
        if (typeof value === 'string') {
          value = xmlEscape(blockField.value);
        }
        xmlString += ">".concat(value, "</field>");
      }
      // Add blocks connected to the next connection.
      if (block.next) {
        xmlString += "<next>".concat(this.blockToXML(block.next, comments), "</next>");
      }
      xmlString += "</".concat(tagName, ">");
      return xmlString;
    }

    /**
     * Recursively encode a mutation object to XML.
     * @param {!object} mutation Object representing a mutation.
     * @return {string} XML string representing a mutation.
     */
  }, {
    key: "mutationToXML",
    value: function mutationToXML(mutation) {
      var mutationString = "<".concat(mutation.tagName);
      for (var prop in mutation) {
        if (prop === 'children' || prop === 'tagName') continue;
        var mutationValue = typeof mutation[prop] === 'string' ? xmlEscape(mutation[prop]) : mutation[prop];

        // Handle dynamic extension blocks
        if (prop === 'blockInfo') {
          mutationValue = xmlEscape(JSON.stringify(mutation[prop]));
        }
        mutationString += " ".concat(prop, "=\"").concat(mutationValue, "\"");
      }
      mutationString += '>';
      for (var i = 0; i < mutation.children.length; i++) {
        mutationString += this.mutationToXML(mutation.children[i]);
      }
      mutationString += "</".concat(mutation.tagName, ">");
      return mutationString;
    }

    // ---------------------------------------------------------------------
    /**
     * Helper to serialize block fields and input fields for reporting new monitors
     * @param {!object} block Block to be paramified.
     * @return {!object} object of param key/values.
     */
  }, {
    key: "_getBlockParams",
    value: function _getBlockParams(block) {
      var params = {};
      for (var key in block.fields) {
        params[key] = block.fields[key].value;
      }
      for (var inputKey in block.inputs) {
        var inputBlock = this._blocks[block.inputs[inputKey].block];
        for (var _key in inputBlock.fields) {
          params[_key] = inputBlock.fields[_key].value;
        }
      }
      return params;
    }

    /**
     * Helper to get the corresponding internal procedure definition block
     * @param {!object} defineBlock Outer define block.
     * @return {!object} internal definition block which has the mutation.
     */
  }, {
    key: "_getCustomBlockInternal",
    value: function _getCustomBlockInternal(defineBlock) {
      if (defineBlock.inputs && defineBlock.inputs.custom_block) {
        return this._blocks[defineBlock.inputs.custom_block.block];
      }
    }

    /**
     * Helper to add a stack to `this._scripts`.
     * @param {?string} topBlockId ID of block that starts the script.
     */
  }, {
    key: "_addScript",
    value: function _addScript(topBlockId) {
      var i = this._scripts.indexOf(topBlockId);
      if (i > -1) return; // Already in scripts.
      this._scripts.push(topBlockId);
      // Update `topLevel` property on the top block.
      this._blocks[topBlockId].topLevel = true;
    }

    /**
     * Helper to remove a script from `this._scripts`.
     * @param {?string} topBlockId ID of block that starts the script.
     */
  }, {
    key: "_deleteScript",
    value: function _deleteScript(topBlockId) {
      var i = this._scripts.indexOf(topBlockId);
      if (i > -1) this._scripts.splice(i, 1);
      // Update `topLevel` property on the top block.
      if (this._blocks[topBlockId]) this._blocks[topBlockId].topLevel = false;
    }
  }], [{
    key: "BRANCH_INPUT_PREFIX",
    get: function get() {
      return 'SUBSTACK';
    }
  }]);
}();
/**
 * A private method shared with execute to build an object containing the block
 * information execute needs and that is reset when other cached Blocks info is
 * reset.
 * @param {Blocks} blocks Blocks containing the expected blockId
 * @param {string} blockId blockId for the desired execute cache
 * @param {function} CacheType constructor for cached block information
 * @return {object} execute cache object
 */
BlocksExecuteCache.getCached = function (blocks, blockId, CacheType) {
  var cached = blocks._cache._executeCached[blockId];
  if (typeof cached !== 'undefined') {
    return cached;
  }
  var block = blocks.getBlock(blockId);
  if (typeof block === 'undefined') return null;
  if (typeof CacheType === 'undefined') {
    cached = {
      id: blockId,
      opcode: blocks.getOpcode(block),
      fields: blocks.getFields(block),
      inputs: blocks.getInputs(block),
      mutation: blocks.getMutation(block)
    };
  } else {
    cached = new CacheType(blocks, {
      id: blockId,
      opcode: blocks.getOpcode(block),
      fields: blocks.getFields(block),
      inputs: blocks.getInputs(block),
      mutation: blocks.getMutation(block)
    });
  }
  blocks._cache._executeCached[blockId] = cached;
  return cached;
};

/**
 * Cache class constructor for runtime. Used to consider what threads should
 * start based on hat data.
 * @type {function}
 */
var RuntimeScriptCache = BlocksRuntimeCache._RuntimeScriptCache;

/**
 * Get an array of scripts from a block container prefiltered to match opcode.
 * @param {Blocks} blocks - Container of blocks
 * @param {string} opcode - Opcode to filter top blocks by
 * @returns {Array.<RuntimeScriptCache>} - Array of RuntimeScriptCache cache
 *   objects
 */
BlocksRuntimeCache.getScripts = function (blocks, opcode) {
  var scripts = blocks._cache.scripts[opcode];
  if (!scripts) {
    scripts = blocks._cache.scripts[opcode] = [];
    var allScripts = blocks._scripts;
    for (var i = 0; i < allScripts.length; i++) {
      var topBlockId = allScripts[i];
      var block = blocks.getBlock(topBlockId);
      if (block.opcode === opcode) {
        scripts.push(new RuntimeScriptCache(blocks, topBlockId));
      }
    }
  }
  return scripts;
};
module.exports = Blocks;

/***/ }),

/***/ "../vm/src/engine/comment.js":
/*!***********************************!*\
  !*** ../vm/src/engine/comment.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @fileoverview
 * Object representing a Scratch Comment (block or workspace).
 */

var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var xmlEscape = __webpack_require__(/*! ../util/xml-escape */ "../vm/src/util/xml-escape.js");
var Comment = /*#__PURE__*/function () {
  /**
   * @param {string} id Id of the comment.
   * @param {string} text Text content of the comment.
   * @param {number} x X position of the comment on the workspace.
   * @param {number} y Y position of the comment on the workspace.
   * @param {number} width The width of the comment when it is full size.
   * @param {number} height The height of the comment when it is full size.
   * @param {boolean} minimized Whether the comment is minimized.
   * @constructor
   */
  function Comment(id, text, x, y, width, height, minimized) {
    _classCallCheck(this, Comment);
    this.id = id || uid();
    this.text = text;
    this.x = x;
    this.y = y;
    this.width = Math.max(Number(width), Comment.MIN_WIDTH);
    this.height = Math.max(Number(height), Comment.MIN_HEIGHT);
    this.minimized = minimized || false;
    this.blockId = null;
  }
  return _createClass(Comment, [{
    key: "toXML",
    value: function toXML() {
      return "<comment id=\"".concat(this.id, "\" x=\"").concat(this.x, "\" y=\"").concat(this.y, "\" w=\"").concat(this.width, "\" h=\"").concat(this.height, "\" pinned=\"").concat(this.blockId !== null, "\" minimized=\"").concat(this.minimized, "\">").concat(xmlEscape(this.text), "</comment>");
    }

    // TODO choose min and defaults for width and height
  }], [{
    key: "MIN_WIDTH",
    get: function get() {
      return 20;
    }
  }, {
    key: "MIN_HEIGHT",
    get: function get() {
      return 20;
    }
  }, {
    key: "DEFAULT_WIDTH",
    get: function get() {
      return 100;
    }
  }, {
    key: "DEFAULT_HEIGHT",
    get: function get() {
      return 100;
    }
  }]);
}();
module.exports = Comment;

/***/ }),

/***/ "../vm/src/engine/execute.js":
/*!***********************************!*\
  !*** ../vm/src/engine/execute.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var BlockUtility = __webpack_require__(/*! ./block-utility */ "../vm/src/engine/block-utility.js");
var BlocksExecuteCache = __webpack_require__(/*! ./blocks-execute-cache */ "../vm/src/engine/blocks-execute-cache.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var Thread = __webpack_require__(/*! ./thread */ "../vm/src/engine/thread.js");
var cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");

/**
 * Single BlockUtility instance reused by execute for every pritimive ran.
 * @const
 */
var blockUtility = new BlockUtility();

/**
 * Profiler frame name for block functions.
 * @const {string}
 */
var blockFunctionProfilerFrame = 'blockFunction';

/**
 * Profiler frame ID for 'blockFunction'.
 * @type {number}
 */
var blockFunctionProfilerId = -1;

/**
 * Utility function to determine if a value is a Promise.
 * @param {*} value Value to check for a Promise.
 * @return {boolean} True if the value appears to be a Promise.
 */
var isPromise = function isPromise(value) {
  return value !== null && _typeof(value) === 'object' && typeof value.then === 'function';
};

/**
 * Handle any reported value from the primitive, either directly returned
 * or after a promise resolves.
 * @param {*} resolvedValue Value eventually returned from the primitive.
 * @param {!Sequencer} sequencer Sequencer stepping the thread for the ran
 * primitive.
 * @param {!Thread} thread Thread containing the primitive.
 * @param {!string} currentBlockId Id of the block in its thread for value from
 * the primitive.
 * @param {!string} opcode opcode used to identify a block function primitive.
 * @param {!boolean} isHat Is the current block a hat?
 */
// @todo move this to callback attached to the thread when we have performance
// metrics (dd)
var handleReport = function handleReport(resolvedValue, sequencer, thread, blockCached, lastOperation) {
  var currentBlockId = blockCached.id;
  var opcode = blockCached.opcode;
  var isHat = blockCached._isHat;
  var isConditional = blockCached._isConditional;
  var isLoop = blockCached._isLoop;
  thread.pushReportedValue(resolvedValue);
  if (isHat) {
    // Hat predicate was evaluated.
    if (thread.stackClick) {
      thread.status = Thread.STATUS_RUNNING;
    } else if (sequencer.runtime.getIsEdgeActivatedHat(opcode)) {
      // If this is an edge-activated hat, only proceed if the value is
      // true and used to be false, or the stack was activated explicitly
      // via stack click
      var hasOldEdgeValue = thread.target.hasEdgeActivatedValue(currentBlockId);
      var oldEdgeValue = thread.target.updateEdgeActivatedValue(currentBlockId, resolvedValue);
      var edgeWasActivated = hasOldEdgeValue ? !oldEdgeValue && resolvedValue : resolvedValue;
      if (edgeWasActivated) {
        thread.status = Thread.STATUS_RUNNING;
      } else {
        sequencer.retireThread(thread);
      }
    } else if (resolvedValue) {
      // Predicate returned true: allow the script to run.
      thread.status = Thread.STATUS_RUNNING;
    } else {
      // Predicate returned false: do not allow script to run
      sequencer.retireThread(thread);
    }
  } else if ((isConditional || isLoop) && typeof resolvedValue !== 'undefined') {
    sequencer.stepToBranch(thread, cast.toNumber(resolvedValue), isLoop);
  } else {
    // In a non-hat, report the value visually if necessary if
    // at the top of the thread stack.
    if (lastOperation && typeof resolvedValue !== 'undefined' && thread.atStackTop()) {
      if (thread.stackClick) {
        sequencer.runtime.visualReport(thread.target, currentBlockId, resolvedValue);
      }
      if (thread.updateMonitor) {
        var targetId = sequencer.runtime.monitorBlocks.getBlock(currentBlockId).targetId;
        if (targetId && !sequencer.runtime.getTargetById(targetId)) {
          // Target no longer exists
          return;
        }
        sequencer.runtime.requestUpdateMonitor({
          id: currentBlockId,
          spriteName: targetId ? sequencer.runtime.getTargetById(targetId).getName() : null,
          value: resolvedValue
        });
      }
    }
    // Finished any yields.
    thread.status = Thread.STATUS_RUNNING;
  }
};
var handlePromiseResolution = function handlePromiseResolution(resolvedValue, sequencer, thread, blockCached, lastOperation) {
  handleReport(resolvedValue, sequencer, thread, blockCached, lastOperation);
  // If it's a command block or a top level reporter in a stackClick.
  // TW: Don't mangle the stack when we just finished executing a hat block.
  // Hat block is always the top and first block of the script. There are no loops to find.
  if (lastOperation && (!blockCached._isHat || thread.stackClick)) {
    var stackFrame;
    var nextBlockId;
    do {
      // In the case that the promise is the last block in the current thread stack
      // We need to pop out repeatedly until we find the next block.
      var popped = thread.popStack();
      if (popped === null) {
        return;
      }
      nextBlockId = thread.target.blocks.getNextBlock(popped);
      if (nextBlockId !== null) {
        // A next block exists so break out this loop
        break;
      }
      // Investigate the next block and if not in a loop,
      // then repeat and pop the next item off the stack frame
      stackFrame = thread.peekStackFrame();
    } while (stackFrame !== null && !stackFrame.isLoop);
    thread.pushStack(nextBlockId);
  }
};
var handlePromise = function handlePromise(primitiveReportedValue, sequencer, thread, blockCached, lastOperation) {
  if (thread.status === Thread.STATUS_RUNNING) {
    // Primitive returned a promise; automatically yield thread.
    thread.status = Thread.STATUS_PROMISE_WAIT;
  }
  // Promise handlers
  primitiveReportedValue.then(function (resolvedValue) {
    handlePromiseResolution(resolvedValue, sequencer, thread, blockCached, lastOperation);
  }, function (rejectionReason) {
    // Promise rejected: the primitive had some error.
    log.warn('Primitive rejected promise: ', rejectionReason);
    handlePromiseResolution("".concat(rejectionReason), sequencer, thread, blockCached, lastOperation);
  });
};

/**
 * A execute.js internal representation of a block to reduce the time spent in
 * execute as the same blocks are called the most.
 *
 * With the help of the Blocks class create a mutable copy of block
 * information. The members of BlockCached derived values of block information
 * that does not need to be reevaluated until a change in Blocks. Since Blocks
 * handles where the cache instance is stored, it drops all cache versions of a
 * block when any change happens to it. This way we can quickly execute blocks
 * and keep perform the right action according to the current block information
 * in the editor.
 *
 * @param {Blocks} blockContainer the related Blocks instance
 * @param {object} cached default set of cached values
 */
var BlockCached = /*#__PURE__*/_createClass(function BlockCached(blockContainer, cached) {
  _classCallCheck(this, BlockCached);
  /**
   * Block id in its parent set of blocks.
   * @type {string}
   */
  this.id = cached.id;

  /**
   * Block operation code for this block.
   * @type {string}
   */
  this.opcode = cached.opcode;

  /**
   * Original block object containing argument values for static fields.
   * @type {object}
   */
  this.fields = cached.fields;

  /**
   * Original block object containing argument values for executable inputs.
   * @type {object}
   */
  this.inputs = cached.inputs;

  /**
   * Procedure mutation.
   * @type {?object}
   */
  this.mutation = cached.mutation;

  /**
   * The profiler the block is configured with.
   * @type {?Profiler}
   */
  this._profiler = null;

  /**
   * Profiler information frame.
   * @type {?ProfilerFrame}
   */
  this._profilerFrame = null;

  /**
   * Is the opcode a hat (event responder) block.
   * @type {boolean}
   */
  this._isHat = false;

  /**
   * The block opcode's implementation function.
   * @type {?function}
   */
  this._blockFunction = null;

  /**
   * Is the block function defined for this opcode?
   * @type {boolean}
   */
  this._definedBlockFunction = false;

  /**
   * Is this block a block with no function but a static value to return.
   * @type {boolean}
   */
  this._isShadowBlock = false;

  /**
   * The static value of this block if it is a shadow block.
   * @type {?any}
   */
  this._shadowValue = null;

  /**
   * A copy of the block's fields that may be modified.
   * @type {object}
   */
  this._fields = Object.assign({}, this.fields);

  /**
   * A copy of the block's inputs that may be modified.
   * @type {object}
   */
  this._inputs = Object.assign({}, this.inputs);

  /**
   * An arguments object for block implementations. All executions of this
   * specific block will use this objecct.
   * @type {object}
   */
  this._argValues = {
    mutation: this.mutation
  };

  /**
   * The inputs key the parent refers to this BlockCached by.
   * @type {string}
   */
  this._parentKey = null;

  /**
   * The target object where the parent wants the resulting value stored
   * with _parentKey as the key.
   * @type {object}
   */
  this._parentValues = null;

  /**
   * A sequence of non-shadow operations that can must be performed. This
   * list recreates the order this block and its children are executed.
   * Since the order is always the same we can safely store that order
   * and iterate over the operations instead of dynamically walking the
   * tree every time.
   * @type {Array<BlockCached>}
   */
  this._ops = [];
  var runtime = blockUtility.sequencer.runtime;
  var opcode = this.opcode,
    fields = this.fields,
    inputs = this.inputs;

  // Assign opcode isHat and blockFunction data to avoid dynamic lookups.
  this._isHat = runtime.getIsHat(opcode);
  this._blockFunction = runtime.getOpcodeFunction(opcode);
  this._definedBlockFunction = typeof this._blockFunction !== 'undefined';
  var flowing = runtime._flowing[opcode];
  this._isConditional = !!(flowing && flowing.conditional);
  this._isLoop = !!(flowing && flowing.loop);

  // Store the current shadow value if there is a shadow value.
  var fieldKeys = Object.keys(fields);
  this._isShadowBlock = !this._definedBlockFunction && fieldKeys.length === 1 && Object.keys(inputs).length === 0;
  this._shadowValue = this._isShadowBlock && fields[fieldKeys[0]].value;

  // Store the static fields onto _argValues.
  for (var fieldName in fields) {
    if (fieldName === 'VARIABLE' || fieldName === 'LIST' || fieldName === 'BROADCAST_OPTION') {
      this._argValues[fieldName] = {
        id: fields[fieldName].id,
        name: fields[fieldName].value
      };
    } else {
      this._argValues[fieldName] = fields[fieldName].value;
    }
  }

  // Remove custom_block. It is not part of block execution.
  delete this._inputs.custom_block;
  if ('BROADCAST_INPUT' in this._inputs) {
    // BROADCAST_INPUT is called BROADCAST_OPTION in the args and is an
    // object with an unchanging shape.
    this._argValues.BROADCAST_OPTION = {
      id: null,
      name: null
    };

    // We can go ahead and compute BROADCAST_INPUT if it is a shadow
    // value.
    var broadcastInput = this._inputs.BROADCAST_INPUT;
    if (broadcastInput.block === broadcastInput.shadow) {
      // Shadow dropdown menu is being used.
      // Get the appropriate information out of it.
      var shadow = blockContainer.getBlock(broadcastInput.shadow);
      var broadcastField = shadow.fields.BROADCAST_OPTION;
      this._argValues.BROADCAST_OPTION.id = broadcastField.id;
      this._argValues.BROADCAST_OPTION.name = broadcastField.value;

      // Evaluating BROADCAST_INPUT here we do not need to do so
      // later.
      delete this._inputs.BROADCAST_INPUT;
    }
  }

  // Cache all input children blocks in the operation lists. The
  // operations can later be run in the order they appear in correctly
  // executing the operations quickly in a flat loop instead of needing to
  // recursivly iterate them.
  for (var inputName in this._inputs) {
    var input = this._inputs[inputName];
    if (input.block) {
      var _this$_ops;
      var inputCached = BlocksExecuteCache.getCached(blockContainer, input.block, BlockCached);
      if (inputCached._isHat) {
        continue;
      }
      (_this$_ops = this._ops).push.apply(_this$_ops, _toConsumableArray(inputCached._ops));
      inputCached._parentKey = inputName;
      inputCached._parentValues = this._argValues;

      // Shadow values are static and do not change, go ahead and
      // store their value on args.
      if (inputCached._isShadowBlock) {
        this._argValues[inputName] = inputCached._shadowValue;
      }
    }
  }

  // The final operation is this block itself. At the top most block is a
  // command block or a block that is being run as a monitor.
  if (this._definedBlockFunction) {
    this._ops.push(this);
  }
});
/**
 * Initialize a BlockCached instance so its command/hat
 * block and reporters can be profiled during execution.
 * @param {Profiler} profiler - The profiler that is currently enabled.
 * @param {BlockCached} blockCached - The blockCached instance to profile.
 */
var _prepareBlockProfiling = function _prepareBlockProfiling(profiler, blockCached) {
  blockCached._profiler = profiler;
  if (blockFunctionProfilerId === -1) {
    blockFunctionProfilerId = profiler.idByName(blockFunctionProfilerFrame);
  }
  var ops = blockCached._ops;
  for (var i = 0; i < ops.length; i++) {
    ops[i]._profilerFrame = profiler.frame(blockFunctionProfilerId, ops[i].opcode);
  }
};

/**
 * Execute a block.
 * @param {!Sequencer} sequencer Which sequencer is executing.
 * @param {!Thread} thread Thread which to read and execute.
 */
var execute = function execute(sequencer, thread) {
  var runtime = sequencer.runtime;

  // store sequencer and thread so block functions can access them through
  // convenience methods.
  blockUtility.sequencer = sequencer;
  blockUtility.thread = thread;

  // Current block to execute is the one on the top of the stack.
  var currentBlockId = thread.peekStack();
  var currentStackFrame = thread.peekStackFrame();
  var blockContainer = thread.blockContainer;
  var blockCached = BlocksExecuteCache.getCached(blockContainer, currentBlockId, BlockCached);
  if (blockCached === null) {
    blockContainer = runtime.flyoutBlocks;
    blockCached = BlocksExecuteCache.getCached(blockContainer, currentBlockId, BlockCached);
    // Stop if block or target no longer exists.
    if (blockCached === null) {
      // No block found: stop the thread; script no longer exists.
      sequencer.retireThread(thread);
      return;
    }
  }
  var ops = blockCached._ops;
  var length = ops.length;
  var i = 0;
  if (currentStackFrame.reported !== null) {
    var reported = currentStackFrame.reported;
    // Reinstate all the previous values.
    var _loop = function _loop() {
      var _reported$i = reported[i],
        oldOpCached = _reported$i.opCached,
        inputValue = _reported$i.inputValue;
      var opCached = ops.find(function (op) {
        return op.id === oldOpCached;
      });
      if (opCached) {
        var _inputName = opCached._parentKey;
        var _argValues = opCached._parentValues;
        if (_inputName === 'BROADCAST_INPUT') {
          // Something is plugged into the broadcast input.
          // Cast it to a string. We don't need an id here.
          _argValues.BROADCAST_OPTION.id = null;
          _argValues.BROADCAST_OPTION.name = cast.toString(inputValue);
        } else {
          _argValues[_inputName] = inputValue;
        }
      }
    };
    for (; i < reported.length; i++) {
      _loop();
    }

    // Find the last reported block that is still in the set of operations.
    // This way if the last operation was removed, we'll find the next
    // candidate. If an earlier block that was performed was removed then
    // we'll find the index where the last operation is now.
    if (reported.length > 0) {
      var lastExisting = reported.reverse().find(function (report) {
        return ops.find(function (op) {
          return op.id === report.opCached;
        });
      });
      if (lastExisting) {
        i = ops.findIndex(function (opCached) {
          return opCached.id === lastExisting.opCached;
        }) + 1;
      } else {
        i = 0;
      }
    }

    // The reporting block must exist and must be the next one in the sequence of operations.
    if (thread.justReported !== null && ops[i] && ops[i].id === currentStackFrame.reporting) {
      var opCached = ops[i];
      var inputValue = thread.justReported;
      thread.justReported = null;
      var inputName = opCached._parentKey;
      var argValues = opCached._parentValues;
      if (inputName === 'BROADCAST_INPUT') {
        // Something is plugged into the broadcast input.
        // Cast it to a string. We don't need an id here.
        argValues.BROADCAST_OPTION.id = null;
        argValues.BROADCAST_OPTION.name = cast.toString(inputValue);
      } else {
        argValues[inputName] = inputValue;
      }
      i += 1;
    }
    currentStackFrame.reporting = null;
    currentStackFrame.reported = null;
    currentStackFrame.waitingReporter = false;
  }
  var start = i;
  for (; i < length; i++) {
    var lastOperation = i === length - 1;
    var _opCached = ops[i];
    currentStackFrame.op = _opCached;
    var blockFunction = _opCached._blockFunction;

    // Update values for arguments (inputs).
    var _argValues2 = _opCached._argValues;

    // Fields are set during opCached initialization.

    // Blocks should glow when a script is starting,
    // not after it has finished (see #1404).
    // Only blocks in blockContainers that don't forceNoGlow
    // should request a glow.
    if (!blockContainer.forceNoGlow) {
      thread.requestScriptGlowInFrame = true;
    }

    // Inputs are set during previous steps in the loop.

    var primitiveReportedValue = blockFunction(_argValues2, blockUtility);
    var primitiveIsPromise = isPromise(primitiveReportedValue);
    if (primitiveIsPromise || currentStackFrame.waitingReporter) {
      if (primitiveIsPromise) {
        handlePromise(primitiveReportedValue, sequencer, thread, _opCached, lastOperation);
      }

      // Store the already reported values. They will be thawed into the
      // future versions of the same operations by block id. The reporting
      // operation if it is promise waiting will set its parent value at
      // that time.
      thread.justReported = null;
      currentStackFrame.reporting = ops[i].id;
      currentStackFrame.reported = ops.slice(0, i).map(function (reportedCached) {
        var inputName = reportedCached._parentKey;
        var reportedValues = reportedCached._parentValues;
        if (inputName === 'BROADCAST_INPUT') {
          return {
            opCached: reportedCached.id,
            inputValue: reportedValues[inputName].BROADCAST_OPTION.name
          };
        }
        return {
          opCached: reportedCached.id,
          inputValue: reportedValues[inputName]
        };
      });

      // We are waiting to be resumed later. Stop running this set of operations
      // and continue them later after thawing the reported values.
      break;
    } else if (thread.status === Thread.STATUS_RUNNING) {
      if (lastOperation) {
        handleReport(primitiveReportedValue, sequencer, thread, _opCached, lastOperation);
      } else {
        // By definition a block that is not last in the list has a
        // parent.
        var _inputName2 = _opCached._parentKey;
        var parentValues = _opCached._parentValues;
        if (_inputName2 === 'BROADCAST_INPUT') {
          // Something is plugged into the broadcast input.
          // Cast it to a string. We don't need an id here.
          parentValues.BROADCAST_OPTION.id = null;
          parentValues.BROADCAST_OPTION.name = cast.toString(primitiveReportedValue);
        } else {
          parentValues[_inputName2] = primitiveReportedValue;
        }
      }
    } else if (thread.status === Thread.STATUS_DONE) {
      // Nothing else to execute.
      break;
    }
  }
  if (runtime.profiler !== null) {
    if (blockCached._profiler !== runtime.profiler) {
      _prepareBlockProfiling(runtime.profiler, blockCached);
    }
    // Determine the index that is after the last executed block. `i` is
    // currently the block that was just executed. `i + 1` will be the block
    // after that. `length` with the min call makes sure we don't try to
    // reference an operation outside of the set of operations.
    var end = Math.min(i + 1, length);
    for (var p = start; p < end; p++) {
      ops[p]._profilerFrame.count += 1;
    }
  }
};
module.exports = execute;

/***/ }),

/***/ "../vm/src/engine/monitor-record.js":
/*!******************************************!*\
  !*** ../vm/src/engine/monitor-record.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @param {unknown} obj
 * @returns {boolean}
 */
var defined = function defined(obj) {
  return typeof obj !== 'undefined' && obj !== null;
};

/**
 * @typedef JSDelta Delta object using regular JS object.
 * @property {string|null} [id]
 * @property {string|null} [spriteName]
 * @property {string|null} [targetId]
 * @property {string|null} [opcode]
 * @property {unknown} [value]
 * @property {unknown} [params]
 * @property {string|null} [mode]
 * @property {number|null} [sliderMin]
 * @property {number|null} [sliderMax]
 * @property {boolean|null} [isDiscrete]
 * @property {number|null} [x]
 * @property {number|null} [y]
 * @property {number|null} [width]
 * @property {number|null} [height]
 * @property {boolean|null} [visible]
 */

/**
 * @typedef ImmutableJSDelta Delta object that is an immutable.js Map/OrderedMap.
 * @property {() => JSDelta} toJS
 */

/**
 * @typedef {JSDelta|ImmutableJSDelta|Map} ExternalDelta Delta object that might be JS object, immutable.js, or JS Map
 */

/**
 * @implements {JSDelta}
 */
var MonitorRecord = /*#__PURE__*/function () {
  /**
   * @param {JSDelta} delta
   */
  function MonitorRecord(delta) {
    var _delta$id, _delta$spriteName, _delta$targetId, _delta$opcode, _delta$value, _delta$params, _delta$mode, _delta$sliderMin, _delta$sliderMax, _delta$isDiscrete, _delta$x, _delta$y, _delta$width, _delta$height, _delta$visible;
    _classCallCheck(this, MonitorRecord);
    /**
     * Block ID
     */
    this.id = (_delta$id = delta.id) !== null && _delta$id !== void 0 ? _delta$id : null;
    /**
     * Present only if the monitor is sprite-specific, such as x position
     */
    this.spriteName = (_delta$spriteName = delta.spriteName) !== null && _delta$spriteName !== void 0 ? _delta$spriteName : null;
    /**
     * Present only if the monitor is sprite-specific, such as x position
     */
    this.targetId = (_delta$targetId = delta.targetId) !== null && _delta$targetId !== void 0 ? _delta$targetId : null;
    this.opcode = (_delta$opcode = delta.opcode) !== null && _delta$opcode !== void 0 ? _delta$opcode : null;
    this.value = (_delta$value = delta.value) !== null && _delta$value !== void 0 ? _delta$value : null;
    this.params = (_delta$params = delta.params) !== null && _delta$params !== void 0 ? _delta$params : null;
    this.mode = (_delta$mode = delta.mode) !== null && _delta$mode !== void 0 ? _delta$mode : 'default';
    this.sliderMin = (_delta$sliderMin = delta.sliderMin) !== null && _delta$sliderMin !== void 0 ? _delta$sliderMin : 0;
    this.sliderMax = (_delta$sliderMax = delta.sliderMax) !== null && _delta$sliderMax !== void 0 ? _delta$sliderMax : 100;
    this.isDiscrete = (_delta$isDiscrete = delta.isDiscrete) !== null && _delta$isDiscrete !== void 0 ? _delta$isDiscrete : true;
    /**
     * (x: null, y: null) Indicates that the monitor should be auto-positioned
     */
    this.x = (_delta$x = delta.x) !== null && _delta$x !== void 0 ? _delta$x : null;
    /**
     * (x: null, y: null) Indicates that the monitor should be auto-positioned
     */
    this.y = (_delta$y = delta.y) !== null && _delta$y !== void 0 ? _delta$y : null;
    this.width = (_delta$width = delta.width) !== null && _delta$width !== void 0 ? _delta$width : 0;
    this.height = (_delta$height = delta.height) !== null && _delta$height !== void 0 ? _delta$height : 0;
    this.visible = (_delta$visible = delta.visible) !== null && _delta$visible !== void 0 ? _delta$visible : true;
  }

  /**
   * Exists for compatibility with code expecting an immutable.js Map
   * @param {string} property
   */
  return _createClass(MonitorRecord, [{
    key: "get",
    value: function get(property) {
      switch (property) {
        case 'id':
          return this.id;
        case 'spriteName':
          return this.spriteName;
        case 'targetId':
          return this.targetId;
        case 'opcode':
          return this.opcode;
        case 'value':
          return this.value;
        case 'params':
          return this.params;
        case 'mode':
          return this.mode;
        case 'sliderMin':
          return this.sliderMin;
        case 'sliderMax':
          return this.sliderMax;
        case 'isDiscrete':
          return this.isDiscrete;
        case 'x':
          return this.x;
        case 'y':
          return this.y;
        case 'width':
          return this.width;
        case 'height':
          return this.height;
        case 'visible':
          return this.visible;
      }
      return null;
    }

    /**
     * @param {JSDelta} delta
     * @returns {boolean} true if modified
     */
  }, {
    key: "merge",
    value: function merge(delta) {
      var didChange = false;
      if (defined(delta.id) && !Object.is(this.id, delta.id)) {
        this.id = delta.id;
        didChange = true;
      }
      if (defined(delta.spriteName) && !Object.is(this.spriteName, delta.spriteName)) {
        this.spriteName = delta.spriteName;
        didChange = true;
      }
      if (defined(delta.targetId) && !Object.is(this.targetId, delta.targetId)) {
        this.targetId = delta.targetId;
        didChange = true;
      }
      if (defined(delta.opcode) && !Object.is(this.opcode, delta.opcode)) {
        this.opcode = delta.opcode;
        didChange = true;
      }
      if (defined(delta.value) && !Object.is(this.value, delta.value)) {
        this.value = delta.value;
        didChange = true;
      }
      if (defined(delta.params) && !Object.is(this.params, delta.params)) {
        this.params = delta.params;
        didChange = true;
      }
      if (defined(delta.mode) && !Object.is(this.mode, delta.mode)) {
        this.mode = delta.mode;
        didChange = true;
      }
      if (defined(delta.sliderMin) && !Object.is(this.sliderMin, delta.sliderMin)) {
        this.sliderMin = delta.sliderMin;
        didChange = true;
      }
      if (defined(delta.sliderMax) && !Object.is(this.sliderMax, delta.sliderMax)) {
        this.sliderMax = delta.sliderMax;
        didChange = true;
      }
      if (defined(delta.isDiscrete) && !Object.is(this.isDiscrete, delta.isDiscrete)) {
        this.isDiscrete = delta.isDiscrete;
        didChange = true;
      }
      if (defined(delta.x) && !Object.is(this.x, delta.x)) {
        this.x = delta.x;
        didChange = true;
      }
      if (defined(delta.y) && !Object.is(this.y, delta.y)) {
        this.y = delta.y;
        didChange = true;
      }
      if (defined(delta.width) && !Object.is(this.width, delta.width)) {
        this.width = delta.width;
        didChange = true;
      }
      if (defined(delta.height) && !Object.is(this.height, delta.height)) {
        this.height = delta.height;
        didChange = true;
      }
      if (defined(delta.visible) && !Object.is(this.visible, delta.visible)) {
        this.visible = delta.visible;
        didChange = true;
      }
      return didChange;
    }
  }]);
}();
/**
 * For compatibility, converts delta received from consumer to a plain JS delta for internal use.
 * @param {ExternalDelta} obj
 * @returns {JSDelta}
 */
MonitorRecord.externalDeltaToJS = function (obj) {
  if (typeof obj.toJS === 'function') {
    return obj.toJS();
  }
  // Nothing in Scratch would pass a JS map into this, but some weird extensions do.
  if (obj instanceof Map) {
    return Object.fromEntries(Array.from(obj.entries()));
  }
  return obj;
};
module.exports = MonitorRecord;

/***/ }),

/***/ "../vm/src/engine/mutation-adapter.js":
/*!********************************************!*\
  !*** ../vm/src/engine/mutation-adapter.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var html = __webpack_require__(/*! htmlparser2 */ "../vm/node_modules/htmlparser2/lib/index.js");
var decodeHtml = __webpack_require__(/*! decode-html */ "../vm/node_modules/decode-html/index.js");

/**
 * Convert a part of a mutation DOM to a mutation VM object, recursively.
 * @param {object} dom DOM object for mutation tag.
 * @return {object} Object representing useful parts of this mutation.
 */
var _mutatorTagToObject = function mutatorTagToObject(dom) {
  var obj = Object.create(null);
  obj.tagName = dom.name;
  obj.children = [];
  for (var prop in dom.attribs) {
    if (prop === 'xmlns') continue;
    obj[prop] = decodeHtml(dom.attribs[prop]);
    // Note: the capitalization of block info in the following lines is important.
    // The lowercase is read in from xml which normalizes case. The VM uses camel case everywhere else.
    if (prop === 'blockinfo') {
      obj.blockInfo = JSON.parse(obj.blockinfo);
      delete obj.blockinfo;
    }
  }
  for (var i = 0; i < dom.children.length; i++) {
    obj.children.push(_mutatorTagToObject(dom.children[i]));
  }
  return obj;
};

/**
 * Adapter between mutator XML or DOM and block representation which can be
 * used by the Scratch runtime.
 * @param {(object|string)} mutation Mutation XML string or DOM.
 * @return {object} Object representing the mutation.
 */
var mutationAdpater = function mutationAdpater(mutation) {
  var mutationParsed;
  // Check if the mutation is already parsed; if not, parse it.
  if (_typeof(mutation) === 'object') {
    mutationParsed = mutation;
  } else {
    mutationParsed = html.parseDOM(mutation)[0];
  }
  return _mutatorTagToObject(mutationParsed);
};
module.exports = mutationAdpater;

/***/ }),

/***/ "../vm/src/engine/profiler.js":
/*!************************************!*\
  !*** ../vm/src/engine/profiler.js ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
/**
 * @fileoverview
 * A way to profile Scratch internal performance. Like what blocks run during a
 * step? How much time do they take? How much time is spent inbetween blocks?
 *
 * Profiler aims for to spend as little time inside its functions while
 * recording. For this it has a simple internal record structure that records a
 * series of values for each START and STOP event in a single array. This lets
 * all the values be pushed in one call for the array. This simplicity allows
 * the contents of the start() and stop() calls to be inlined in areas that are
 * called frequently enough to want even greater performance from Profiler so
 * what is recorded better reflects on the profiled code and not Profiler
 * itself.
 */

/**
 * The next id returned for a new profile'd function.
 * @type {number}
 */
var nextId = 0;

/**
 * The mapping of names to ids.
 * @const {Object.<string, number>}
 */
var profilerNames = {};

/**
 * The START event identifier in Profiler records.
 * @const {number}
 */
var START = 0;

/**
 * The STOP event identifier in Profiler records.
 * @const {number}
 */
var STOP = 1;

/**
 * The number of cells used in the records array by a START event.
 * @const {number}
 */
var START_SIZE = 4;

/**
 * The number of cells used in the records array by a STOP event.
 * @const {number}
 */
var STOP_SIZE = 2;

/**
 * Stored reference to Performance instance provided by the Browser.
 * @const {Performance}
 */
var performance = (typeof window === "undefined" ? "undefined" : _typeof(window)) === 'object' && window.performance;

/**
 * Callback handle called by Profiler for each frame it decodes from its
 * records.
 * @callback FrameCallback
 * @param {ProfilerFrame} frame
 */

/**
 * A set of information about a frame of execution that was recorded.
 */
var ProfilerFrame = /*#__PURE__*/_createClass(
/**
 * @param {number} depth Depth of the frame in the recorded stack.
 */
function ProfilerFrame(depth) {
  _classCallCheck(this, ProfilerFrame);
  /**
   * The numeric id of a record symbol like Runtime._step or
   * blockFunction.
   * @type {number}
   */
  this.id = -1;

  /**
   * The amount of time spent inside the recorded frame and any deeper
   * frames.
   * @type {number}
   */
  this.totalTime = 0;

  /**
   * The amount of time spent only inside this record frame. Not
   * including time in any deeper frames.
   * @type {number}
   */
  this.selfTime = 0;

  /**
   * An arbitrary argument for the recorded frame. For example a block
   * function might record its opcode as an argument.
   * @type {*}
   */
  this.arg = null;

  /**
   * The depth of the recorded frame. This can help compare recursive
   * funtions that are recorded. Each level of recursion with have a
   * different depth value.
   * @type {number}
   */
  this.depth = depth;

  /**
   * A summarized count of the number of calls to this frame.
   * @type {number}
   */
  this.count = 0;
});
var Profiler = /*#__PURE__*/function () {
  /**
   * @param {FrameCallback} onFrame a handle called for each recorded frame.
   * The passed frame value may not be stored as it'll be updated with later
   * frame information. Any information that is further stored by the handler
   * should make copies or reduce the information.
   */
  function Profiler() {
    var onFrame = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : function () {};
    _classCallCheck(this, Profiler);
    /**
     * A series of START and STOP values followed by arguments. After
     * recording is complete the full set of records is reported back by
     * stepping through the series to connect the relative START and STOP
     * information.
     * @type {Array.<*>}
     */
    this.records = [];

    /**
     * An array of frames incremented on demand instead as part of start
     * and stop.
     * @type {Array.<ProfilerFrame>}
     */
    this.increments = [];

    /**
     * An array of profiler frames separated by counter argument. Generally
     * for Scratch these frames are separated by block function opcode.
     * This tracks each time an opcode is called.
     * @type {Array.<ProfilerFrame>}
     */
    this.counters = [];

    /**
     * A frame with no id or argument.
     * @type {ProfilerFrame}
     */
    this.nullFrame = new ProfilerFrame(-1);

    /**
     * A cache of ProfilerFrames to reuse when reporting the recorded
     * frames in records.
     * @type {Array.<ProfilerFrame>}
     */
    this._stack = [new ProfilerFrame(0)];

    /**
     * A callback handle called with each decoded frame when reporting back
     * all the recorded times.
     * @type {FrameCallback}
     */
    this.onFrame = onFrame;

    /**
     * A reference to the START record id constant.
     * @const {number}
     */
    this.START = START;

    /**
     * A reference to the STOP record id constant.
     * @const {number}
     */
    this.STOP = STOP;
  }

  /**
   * Start recording a frame of time for an id and optional argument.
   * @param {number} id The id returned by idByName for a name symbol like
   * Runtime._step.
   * @param {?*} arg An arbitrary argument value to store with the frame.
   */
  return _createClass(Profiler, [{
    key: "start",
    value: function start(id, arg) {
      this.records.push(START, id, arg, performance.now());
    }

    /**
     * Stop the current frame.
     */
  }, {
    key: "stop",
    value: function stop() {
      this.records.push(STOP, performance.now());
    }

    /**
     * Increment the number of times this symbol is called.
     * @param {number} id The id returned by idByName for a name symbol.
     */
  }, {
    key: "increment",
    value: function increment(id) {
      if (!this.increments[id]) {
        this.increments[id] = new ProfilerFrame(-1);
        this.increments[id].id = id;
      }
      this.increments[id].count += 1;
    }

    /**
     * Find or create a ProfilerFrame-like object whose counter can be
     * incremented outside of the Profiler.
     * @param {number} id The id returned by idByName for a name symbol.
     * @param {*} arg The argument for a frame that identifies it in addition
     *   to the id.
     * @return {{count: number}} A ProfilerFrame-like whose count should be
     *   incremented for each call.
     */
  }, {
    key: "frame",
    value: function frame(id, arg) {
      for (var i = 0; i < this.counters.length; i++) {
        if (this.counters[i].id === id && this.counters[i].arg === arg) {
          return this.counters[i];
        }
      }
      var newCounter = new ProfilerFrame(-1);
      newCounter.id = id;
      newCounter.arg = arg;
      this.counters.push(newCounter);
      return newCounter;
    }

    /**
     * Decode records and report all frames to `this.onFrame`.
     */
  }, {
    key: "reportFrames",
    value: function reportFrames() {
      var stack = this._stack;
      var depth = 1;

      // Step through the records and initialize Frame instances from the
      // START and STOP events. START and STOP events are separated by events
      // for deeper frames run by higher frames. Frames are stored on a stack
      // and reinitialized for each START event. When a stop event is reach
      // the Frame for the current depth has its final values stored and its
      // passed to the current onFrame callback. This way Frames are "pushed"
      // for each START event and "popped" for each STOP and handed to an
      // outside handle to any desired reduction of the collected data.
      for (var i = 0; i < this.records.length;) {
        if (this.records[i] === START) {
          if (depth >= stack.length) {
            stack.push(new ProfilerFrame(depth));
          }

          // Store id, arg, totalTime, and initialize selfTime.
          var frame = stack[depth++];
          frame.id = this.records[i + 1];
          frame.arg = this.records[i + 2];
          // totalTime is first set as the time recorded by this START
          // event. Once the STOP event is reached the stored start time
          // is subtracted from the recorded stop time. The resulting
          // difference is the actual totalTime, and replaces the start
          // time in frame.totalTime.
          //
          // totalTime is used this way as a convenient member to store a
          // value between the two events without needing additional
          // members on the Frame or in a shadow map.
          frame.totalTime = this.records[i + 3];
          // selfTime is decremented until we reach the STOP event for
          // this frame. totalTime will be added to it then to get the
          // time difference.
          frame.selfTime = 0;
          i += START_SIZE;
        } else if (this.records[i] === STOP) {
          var now = this.records[i + 1];
          var _frame = stack[--depth];
          // totalTime is the difference between the start event time
          // stored in totalTime and the stop event time pulled from this
          // record.
          _frame.totalTime = now - _frame.totalTime;
          // selfTime is the difference of this frame's totalTime and the
          // sum of totalTime of deeper frames.
          _frame.selfTime += _frame.totalTime;

          // Remove this frames totalTime from the parent's selfTime.
          stack[depth - 1].selfTime -= _frame.totalTime;

          // This frame occured once.
          _frame.count = 1;
          this.onFrame(_frame);
          i += STOP_SIZE;
        } else {
          this.records.length = 0;
          throw new Error('Unable to decode Profiler records.');
        }
      }
      for (var j = 0; j < this.increments.length; j++) {
        if (this.increments[j] && this.increments[j].count > 0) {
          this.onFrame(this.increments[j]);
          this.increments[j].count = 0;
        }
      }
      for (var k = 0; k < this.counters.length; k++) {
        if (this.counters[k].count > 0) {
          this.onFrame(this.counters[k]);
          this.counters[k].count = 0;
        }
      }
      this.records.length = 0;
    }

    /**
     * Lookup or create an id for a frame name.
     * @param {string} name The name to return an id for.
     * @return {number} The id for the passed name.
     */
  }, {
    key: "idByName",
    value: function idByName(name) {
      return Profiler.idByName(name);
    }

    /**
     * Reverse lookup the name from a given frame id.
     * @param {number} id The id to search for.
     * @return {string} The name for the given id.
     */
  }, {
    key: "nameById",
    value: function nameById(id) {
      return Profiler.nameById(id);
    }

    /**
     * Lookup or create an id for a frame name.
     * @static
     * @param {string} name The name to return an id for.
     * @return {number} The id for the passed name.
     */
  }], [{
    key: "idByName",
    value: function idByName(name) {
      if (typeof profilerNames[name] !== 'number') {
        profilerNames[name] = nextId++;
      }
      return profilerNames[name];
    }

    /**
     * Reverse lookup the name from a given frame id.
     * @static
     * @param {number} id The id to search for.
     * @return {string} The name for the given id.
     */
  }, {
    key: "nameById",
    value: function nameById(id) {
      for (var name in profilerNames) {
        if (profilerNames[name] === id) {
          return name;
        }
      }
      return null;
    }

    /**
     * Profiler is only available on platforms with the Performance API.
     * @return {boolean} Can the Profiler run in this browser?
     */
  }, {
    key: "available",
    value: function available() {
      return (typeof window === "undefined" ? "undefined" : _typeof(window)) === 'object' && typeof window.performance !== 'undefined';
    }
  }]);
}();
/**
 * A reference to the START record id constant.
 * @const {number}
 */
Profiler.START = START;

/**
 * A reference to the STOP record id constant.
 * @const {number}
 */
Profiler.STOP = STOP;
module.exports = Profiler;

/***/ }),

/***/ "../vm/src/engine/runtime.js":
/*!***********************************!*\
  !*** ../vm/src/engine/runtime.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var EventEmitter = __webpack_require__(/*! events */ "./node_modules/events/events.js");
var ExtendedJSON = __webpack_require__(/*! @turbowarp/json */ "../vm/node_modules/@turbowarp/json/src/index.js");
var uuid = __webpack_require__(/*! uuid */ "../vm/node_modules/uuid/dist/esm-browser/index.js");
var ArgumentType = __webpack_require__(/*! ../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var Blocks = __webpack_require__(/*! ./blocks */ "../vm/src/engine/blocks.js");
var BlocksRuntimeCache = __webpack_require__(/*! ./blocks-runtime-cache */ "../vm/src/engine/blocks-runtime-cache.js");
var BlockType = __webpack_require__(/*! ../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Profiler = __webpack_require__(/*! ./profiler */ "../vm/src/engine/profiler.js");
var Sequencer = __webpack_require__(/*! ./sequencer */ "../vm/src/engine/sequencer.js");
var execute = __webpack_require__(/*! ./execute.js */ "../vm/src/engine/execute.js");
var compilerExecute = __webpack_require__(/*! ../compiler/jsexecute */ "../vm/src/compiler/jsexecute.js");
var ScratchBlocksConstants = __webpack_require__(/*! ./scratch-blocks-constants */ "../vm/src/engine/scratch-blocks-constants.js");
var TargetType = __webpack_require__(/*! ../extension-support/target-type */ "../vm/src/extension-support/target-type.js");
var Thread = __webpack_require__(/*! ./thread */ "../vm/src/engine/thread.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var maybeFormatMessage = __webpack_require__(/*! ../util/maybe-format-message */ "../vm/src/util/maybe-format-message.js");
var StageLayering = __webpack_require__(/*! ./stage-layering */ "../vm/src/engine/stage-layering.js");
var Variable = __webpack_require__(/*! ./variable */ "../vm/src/engine/variable.js");
var xmlEscape = __webpack_require__(/*! ../util/xml-escape */ "../vm/src/util/xml-escape.js");
var ScratchLinkWebSocket = __webpack_require__(/*! ../util/scratch-link-websocket */ "../vm/src/util/scratch-link-websocket.js");
var FontManager = __webpack_require__(/*! ./tw-font-manager */ "../vm/src/engine/tw-font-manager.js");
var fetchWithTimeout = __webpack_require__(/*! ../util/fetch-with-timeout */ "../vm/src/util/fetch-with-timeout.js");
var platform = __webpack_require__(/*! ./tw-platform.js */ "../vm/src/engine/tw-platform.js");
var safeStringify = __webpack_require__(/*! ../util/tw-safe-stringify.js */ "../vm/src/util/tw-safe-stringify.js");
var MonitorState = __webpack_require__(/*! ./tw-monitor-state.js */ "../vm/src/engine/tw-monitor-state.js");

// Virtual I/O devices.
var Clock = __webpack_require__(/*! ../io/clock */ "../vm/src/io/clock.js");
var Cloud = __webpack_require__(/*! ../io/cloud */ "../vm/src/io/cloud.js");
var Keyboard = __webpack_require__(/*! ../io/keyboard */ "../vm/src/io/keyboard.js");
var Mouse = __webpack_require__(/*! ../io/mouse */ "../vm/src/io/mouse.js");
var MouseWheel = __webpack_require__(/*! ../io/mouseWheel */ "../vm/src/io/mouseWheel.js");
var UserData = __webpack_require__(/*! ../io/userData */ "../vm/src/io/userData.js");
var Video = __webpack_require__(/*! ../io/video */ "../vm/src/io/video.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var defaultBlockPackages = {
  scratch3_control: __webpack_require__(/*! ../blocks/scratch3_control */ "../vm/src/blocks/scratch3_control.js"),
  scratch3_event: __webpack_require__(/*! ../blocks/scratch3_event */ "../vm/src/blocks/scratch3_event.js"),
  scratch3_looks: __webpack_require__(/*! ../blocks/scratch3_looks */ "../vm/src/blocks/scratch3_looks.js"),
  scratch3_motion: __webpack_require__(/*! ../blocks/scratch3_motion */ "../vm/src/blocks/scratch3_motion.js"),
  scratch3_operators: __webpack_require__(/*! ../blocks/scratch3_operators */ "../vm/src/blocks/scratch3_operators.js"),
  scratch3_sound: __webpack_require__(/*! ../blocks/scratch3_sound */ "../vm/src/blocks/scratch3_sound.js"),
  scratch3_sensing: __webpack_require__(/*! ../blocks/scratch3_sensing */ "../vm/src/blocks/scratch3_sensing.js"),
  scratch3_data: __webpack_require__(/*! ../blocks/scratch3_data */ "../vm/src/blocks/scratch3_data.js"),
  scratch3_procedures: __webpack_require__(/*! ../blocks/scratch3_procedures */ "../vm/src/blocks/scratch3_procedures.js")
};
var interpolate = __webpack_require__(/*! ./tw-interpolate */ "../vm/src/engine/tw-interpolate.js");
var FrameLoop = __webpack_require__(/*! ./tw-frame-loop */ "../vm/src/engine/tw-frame-loop.js");
var MonitorRecord = __webpack_require__(/*! ./monitor-record.js */ "../vm/src/engine/monitor-record.js");
var defaultExtensionColors = ['#0FBD8C', '#0DA57A', '#0B8E69'];
var COMMENT_CONFIG_MAGIC = ' // _twconfig_';

/**
 * Information used for converting Scratch argument types into scratch-blocks data.
 * @type {object.<ArgumentType, {shadowType: string, fieldType: string}>}
 */
var ArgumentTypeMap = function () {
  var map = {};
  map[ArgumentType.ANGLE] = {
    shadow: {
      type: 'math_angle',
      // We specify fieldNames here so that we can pick
      // create and populate a field with the defaultValue
      // specified in the extension.
      // When the `fieldName` property is not specified,
      // the <field></field> will be left out of the XML and
      // the scratch-blocks defaults for that field will be
      // used instead (e.g. default of 0 for number fields)
      fieldName: 'NUM'
    }
  };
  map[ArgumentType.COLOR] = {
    shadow: {
      type: 'colour_picker',
      fieldName: 'COLOUR'
    }
  };
  map[ArgumentType.NUMBER] = {
    shadow: {
      type: 'math_number',
      fieldName: 'NUM'
    }
  };
  map[ArgumentType.STRING] = {
    shadow: {
      type: 'text',
      fieldName: 'TEXT'
    }
  };
  map[ArgumentType.BOOLEAN] = {
    check: 'Boolean'
  };
  map[ArgumentType.MATRIX] = {
    shadow: {
      type: 'matrix',
      fieldName: 'MATRIX'
    }
  };
  map[ArgumentType.NOTE] = {
    shadow: {
      type: 'note',
      fieldName: 'NOTE'
    }
  };
  map[ArgumentType.IMAGE] = {
    // Inline images are weird because they're not actually "arguments".
    // They are more analagous to the label on a block.
    fieldType: 'field_image'
  };
  map[ArgumentType.COSTUME] = {
    shadow: {
      type: 'looks_costume',
      fieldName: 'COSTUME'
    }
  };
  map[ArgumentType.SOUND] = {
    shadow: {
      type: 'sound_sounds_menu',
      fieldName: 'SOUND_MENU'
    }
  };
  return map;
}();

/**
 * A pair of functions used to manage the cloud variable limit,
 * to be used when adding (or attempting to add) or removing a cloud variable.
 * @typedef {object} CloudDataManager
 * @property {function} canAddCloudVariable A function to call to check that
 * a cloud variable can be added.
 * @property {function} addCloudVariable A function to call to track a new
 * cloud variable on the runtime.
 * @property {function} removeCloudVariable A function to call when
 * removing an existing cloud variable.
 * @property {function} hasCloudVariables A function to call to check that
 * the runtime has any cloud variables.
 * @property {function} getNumberOfCloudVariables A function that returns the
 * number of cloud variables in the project.
 */

/**
 * Creates and manages cloud variable limit in a project,
 * and returns two functions to be used to add a new
 * cloud variable (while checking that it can be added)
 * and remove an existing cloud variable.
 * These are to be called whenever attempting to create or delete
 * a cloud variable.
 * @param {Object} cloudOptions
 * @param {number} cloudOptions.limit Maximum number of cloud variables
 * @return {CloudDataManager} The functions to be used when adding or removing a
 * cloud variable.
 */
var cloudDataManager = function cloudDataManager(cloudOptions) {
  var count = 0;
  var canAddCloudVariable = function canAddCloudVariable() {
    return count < cloudOptions.limit;
  };
  var addCloudVariable = function addCloudVariable() {
    count++;
  };
  var removeCloudVariable = function removeCloudVariable() {
    count--;
  };
  var hasCloudVariables = function hasCloudVariables() {
    return count > 0;
  };
  var getNumberOfCloudVariables = function getNumberOfCloudVariables() {
    return count;
  };
  return {
    canAddCloudVariable: canAddCloudVariable,
    addCloudVariable: addCloudVariable,
    removeCloudVariable: removeCloudVariable,
    hasCloudVariables: hasCloudVariables,
    getNumberOfCloudVariables: getNumberOfCloudVariables
  };
};

/**
 * Numeric ID for Runtime._step in Profiler instances.
 * @type {number}
 */
var stepProfilerId = -1;

/**
 * Numeric ID for Sequencer.stepThreads in Profiler instances.
 * @type {number}
 */
var stepThreadsProfilerId = -1;

/**
 * Numeric ID for RenderWebGL.draw in Profiler instances.
 * @type {number}
 */
var rendererDrawProfilerId = -1;

/**
 * Manages targets, scripts, and the sequencer.
 * @constructor
 */
var Runtime = /*#__PURE__*/function (_EventEmitter) {
  function Runtime() {
    var _this;
    _classCallCheck(this, Runtime);
    _this = _callSuper(this, Runtime);

    /**
     * Target management and storage.
     * @type {Array.<!Target>}
     */
    _this.targets = [];

    /**
     * Targets in reverse order of execution. Shares its order with drawables.
     * @type {Array.<!Target>}
     */
    _this.executableTargets = [];

    /**
     * A list of threads that are currently running in the VM.
     * Threads are added when execution starts and pruned when execution ends.
     * @type {Array.<Thread>}
     */
    _this.threads = [];
    _this.threadMap = new Map();

    /** @type {!Sequencer} */
    _this.sequencer = new Sequencer(_this);

    /**
     * Storage container for flyout blocks.
     * These will execute on `_editingTarget.`
     * @type {!Blocks}
     */
    _this.flyoutBlocks = new Blocks(_this, true /* force no glow */);

    /**
     * Storage container for monitor blocks.
     * These will execute on a target maybe
     * @type {!Blocks}
     */
    _this.monitorBlocks = new Blocks(_this, true /* force no glow */);

    /**
     * Currently known editing target for the VM.
     * @type {?Target}
     */
    _this._editingTarget = null;

    /**
     * Map to look up a block primitive's implementation function by its opcode.
     * This is a two-step lookup: package name first, then primitive name.
     * @type {Object.<string, Function>}
     */
    _this._primitives = {};

    /**
     * Map to look up all block information by extended opcode.
     * @type {Array.<CategoryInfo>}
     * @private
     */
    _this._blockInfo = [];

    /**
     * Map to look up hat blocks' metadata.
     * Keys are opcode for hat, values are metadata objects.
     * @type {Object.<string, Object>}
     */
    _this._hats = {};

    /**
     * Map of opcode to information about whether the block's return value should be interpreted
     * for control flow purposes.
     * @type {Record<string, {conditional: boolean}>}
     */
    _this._flowing = {};

    /**
     * A list of script block IDs that were glowing during the previous frame.
     * @type {!Array.<!string>}
     */
    _this._scriptGlowsPreviousFrame = [];

    /**
     * Number of non-monitor threads running during the previous frame.
     * @type {number}
     */
    _this._nonMonitorThreadCount = 0;

    /**
     * All threads that finished running and were removed from this.threads
     * by behaviour in Sequencer.stepThreads.
     * @type {Array<Thread>}
     */
    _this._lastStepDoneThreads = null;

    /**
     * Currently known number of clones, used to enforce clone limit.
     * @type {number}
     */
    _this._cloneCounter = 0;

    /**
     * Flag to emit a targets update at the end of a step. When target data
     * changes, this flag is set to true.
     * @type {boolean}
     */
    _this._refreshTargets = false;

    /**
     * Map to look up all monitor block information by opcode.
     * @type {object}
     * @private
     */
    _this.monitorBlockInfo = {};

    /**
     * Ordered map of all monitors, which are MonitorRecord objects.
     */
    _this._monitorState = new MonitorState();

    /**
     * Whether the project is in "turbo mode."
     * @type {Boolean}
     */
    _this.turboMode = false;

    /**
     * tw: Responsible for managing the VM's many timers.
     */
    _this.frameLoop = new FrameLoop(_this);

    /**
     * Current length of a step.
     * Changes as mode switches, and used by the sequencer to calculate
     * WORK_TIME.
     * @type {!number}
     */
    _this.currentStepTime = 1000 / 30;

    // Set an intial value for this.currentMSecs
    _this.updateCurrentMSecs();

    /**
     * Whether any primitive has requested a redraw.
     * Affects whether `Sequencer.stepThreads` will yield
     * after stepping each thread.
     * Reset on every frame.
     * @type {boolean}
     */
    _this.redrawRequested = false;

    // Register all given block packages.
    _this._registerBlockPackages();

    // Register and initialize "IO devices", containers for processing
    // I/O related data.
    /** @type {Object.<string, Object>} */
    _this.ioDevices = {
      clock: new Clock(_this),
      cloud: new Cloud(_this),
      keyboard: new Keyboard(_this),
      mouse: new Mouse(_this),
      mouseWheel: new MouseWheel(_this),
      userData: new UserData(),
      video: new Video(_this)
    };

    /**
     * A list of extensions, used to manage hardware connection.
     */
    _this.peripheralExtensions = {};

    /**
     * A runtime profiler that records timed events for later playback to
     * diagnose Scratch performance.
     * @type {Profiler}
     */
    _this.profiler = null;
    _this.cloudOptions = {
      limit: 10
    };
    var newCloudDataManager = cloudDataManager(_this.cloudOptions);

    /**
     * Check wether the runtime has any cloud data.
     * @type {function}
     * @return {boolean} Whether or not the runtime currently has any
     * cloud variables.
     */
    _this.hasCloudData = newCloudDataManager.hasCloudVariables;

    /**
     * A function which checks whether a new cloud variable can be added
     * to the runtime.
     * @type {function}
     * @return {boolean} Whether or not a new cloud variable can be added
     * to the runtime.
     */
    _this.canAddCloudVariable = newCloudDataManager.canAddCloudVariable;

    /**
     * A function which returns the number of cloud variables in the runtime.
     * @returns {number}
     */
    _this.getNumberOfCloudVariables = newCloudDataManager.getNumberOfCloudVariables;

    /**
     * A function that tracks a new cloud variable in the runtime,
     * updating the cloud variable limit. Calling this function will
     * emit a cloud data update event if this is the first cloud variable
     * being added.
     * @type {function}
     */
    _this.addCloudVariable = _this._initializeAddCloudVariable(newCloudDataManager);

    /**
     * A function which updates the runtime's cloud variable limit
     * when removing a cloud variable and emits a cloud update event
     * if the last of the cloud variables is being removed.
     * @type {function}
     */
    _this.removeCloudVariable = _this._initializeRemoveCloudVariable(newCloudDataManager);

    /**
     * A string representing the origin of the current project from outside of the
     * Scratch community, such as CSFirst.
     * @type {?string}
     */
    _this.origin = null;

    /**
     * Metadata about the platform this VM is part of.
     */
    _this.platform = Object.assign({}, platform);
    _this._initScratchLink();
    _this.resetRunId();
    _this._stageTarget = null;
    _this.addonBlocks = {};
    _this.stageWidth = Runtime.STAGE_WIDTH;
    _this.stageHeight = Runtime.STAGE_HEIGHT;
    _this.runtimeOptions = {
      maxClones: Runtime.MAX_CLONES,
      miscLimits: true,
      fencing: true
    };
    _this.compilerOptions = {
      enabled: true,
      warpTimer: false,
      offload: {
        enabled: false,
        wasm: true,
        webgl: true,
        minWasmIterations: 256,
        minWebGLElements: 4096,
        verifyGPU: 'conservative'
      }
    };
    _this.debug = false;
    _this._lastStepTime = Date.now();
    _this.interpolationEnabled = false;
    _this._defaultStoredSettings = _this._generateAllProjectOptions();

    /**
     * TW: We support a "packaged runtime" mode. This can be used when:
     *  - there will never be an editor attached such as scratch-gui or scratch-blocks
     *  - the project will never be exported with saveProjectSb3()
     *  - original costume and sound data is not needed
     * In this mode, the runtime is able to discard large amounts of data and avoid some processing
     * to make projects load faster and use less memory.
     * This is not designed to protect projects from copying as someone can still copy the data that
     * gets fed into the runtime in the first place.
     * This mode is used by the TurboWarp Packager.
     */
    _this.isPackaged = false;

    /**
     * Contains information about the external communication methods that the scripts inside the project
     * can use to send data from inside the project to an external server.
     * Do not update this directly. Use Runtime.setExternalCommunicationMethod() instead.
     */
    _this.externalCommunicationMethods = {
      cloudVariables: false,
      customExtensions: false
    };
    _this.on(Runtime.HAS_CLOUD_DATA_UPDATE, function (enabled) {
      _this.setExternalCommunicationMethod('cloudVariables', enabled);
    });

    /**
     * If set to true, features such as reading colors from the user's webcam will be disabled
     * when the project has access to any external communication method to protect user privacy.
     * Requires TurboWarp/scratch-render.
     * Do not update this directly. Use Runtime.setEnforcePrivacy() instead.
     */
    _this.enforcePrivacy = true;

    /**
     * If true, an external communication method exists and enforcePrivacy is enabled.
     * Do not update this directly. Must be changed via public functions that call Runtime.updatePrivacy().
     */
    _this.privacyRestrictionsActive = false;

    /**
     * Internal map of opaque identifiers to the callback to run that function.
     * @type {Map<string, function>}
     */
    _this.extensionButtons = new Map();

    /**
     * Responsible for managing custom fonts.
     */
    _this.fontManager = new FontManager(_this);

    /**
     * Maps extension ID to a JSON-serializable value.
     * @type {Object.<string, object>}
     */
    _this.extensionStorage = {};

    /**
     * Total number of scratch-storage load() requests since the runtime was created or cleared.
     */
    _this.totalAssetRequests = 0;

    /**
     * Total number of finished or errored scratch-storage load() requests since the runtime was created or cleared.
     */
    _this.finishedAssetRequests = 0;
    return _this;
  }

  /**
   * Width of the stage, in pixels.
   * @const {number}
   */
  _inherits(Runtime, _EventEmitter);
  return _createClass(Runtime, [{
    key: "_initializeAddCloudVariable",
    value:
    // -----------------------------------------------------------------------------
    // -----------------------------------------------------------------------------

    // Helper function for initializing the addCloudVariable function
    function _initializeAddCloudVariable(newCloudDataManager) {
      var _this2 = this;
      // The addCloudVariable function
      return function () {
        var hadCloudVarsBefore = _this2.hasCloudData();
        newCloudDataManager.addCloudVariable();
        if (!hadCloudVarsBefore && _this2.hasCloudData()) {
          _this2.emit(Runtime.HAS_CLOUD_DATA_UPDATE, true);
        }
      };
    }

    // Helper function for initializing the removeCloudVariable function
  }, {
    key: "_initializeRemoveCloudVariable",
    value: function _initializeRemoveCloudVariable(newCloudDataManager) {
      var _this3 = this;
      return function () {
        var hadCloudVarsBefore = _this3.hasCloudData();
        newCloudDataManager.removeCloudVariable();
        if (hadCloudVarsBefore && !_this3.hasCloudData()) {
          _this3.emit(Runtime.HAS_CLOUD_DATA_UPDATE, false);
        }
      };
    }

    /**
     * Register default block packages with this runtime.
     * @todo Prefix opcodes with package name.
     * @private
     */
  }, {
    key: "_registerBlockPackages",
    value: function _registerBlockPackages() {
      for (var packageName in defaultBlockPackages) {
        if (Object.prototype.hasOwnProperty.call(defaultBlockPackages, packageName)) {
          // @todo pass a different runtime depending on package privilege?
          var packageObject = new defaultBlockPackages[packageName](this);
          // Collect primitives from package.
          if (packageObject.getPrimitives) {
            var packagePrimitives = packageObject.getPrimitives();
            for (var op in packagePrimitives) {
              if (Object.prototype.hasOwnProperty.call(packagePrimitives, op)) {
                this._primitives[op] = packagePrimitives[op].bind(packageObject);
              }
            }
          }
          // Collect hat metadata from package.
          if (packageObject.getHats) {
            var packageHats = packageObject.getHats();
            for (var hatName in packageHats) {
              if (Object.prototype.hasOwnProperty.call(packageHats, hatName)) {
                this._hats[hatName] = packageHats[hatName];
              }
            }
          }
          // Collect monitored from package.
          if (packageObject.getMonitored) {
            this.monitorBlockInfo = Object.assign({}, this.monitorBlockInfo, packageObject.getMonitored());
          }
          this.compilerRegisterExtension(packageName, packageObject);
        }
      }
    }
  }, {
    key: "compilerRegisterExtension",
    value: function compilerRegisterExtension(name, extensionObject) {
      this["ext_".concat(name)] = extensionObject;
    }
  }, {
    key: "getMonitorState",
    value: function getMonitorState() {
      return this._monitorState;
    }

    /**
     * Generate an extension-specific menu ID.
     * @param {string} menuName - the name of the menu.
     * @param {string} extensionId - the ID of the extension hosting the menu.
     * @returns {string} - the constructed ID.
     * @private
     */
  }, {
    key: "_makeExtensionMenuId",
    value: function _makeExtensionMenuId(menuName, extensionId) {
      return "".concat(extensionId, "_menu_").concat(menuName);
    }

    /**
     * Create a context ("args") object for use with `formatMessage` on messages which might be target-specific.
     * @param {Target} [target] - the target to use as context. If a target is not provided, default to the current
     * editing target or the stage.
     */
  }, {
    key: "makeMessageContextForTarget",
    value: function makeMessageContextForTarget(target) {
      var context = {};
      target = target || this.getEditingTarget() || this.getTargetForStage();
      if (target) {
        context.targetType = target.isStage ? TargetType.STAGE : TargetType.SPRITE;
      }
    }

    /**
     * Register the primitives provided by an extension.
     * @param {ExtensionMetadata} extensionInfo - information about the extension (id, blocks, etc.)
     * @private
     */
  }, {
    key: "_registerExtensionPrimitives",
    value: function _registerExtensionPrimitives(extensionInfo) {
      var categoryInfo = {
        id: extensionInfo.id,
        name: maybeFormatMessage(extensionInfo.name),
        showStatusButton: extensionInfo.showStatusButton,
        blockIconURI: extensionInfo.blockIconURI,
        menuIconURI: extensionInfo.menuIconURI
      };
      if (extensionInfo.color1) {
        categoryInfo.color1 = extensionInfo.color1;
        categoryInfo.color2 = extensionInfo.color2;
        categoryInfo.color3 = extensionInfo.color3;
      } else {
        categoryInfo.color1 = defaultExtensionColors[0];
        categoryInfo.color2 = defaultExtensionColors[1];
        categoryInfo.color3 = defaultExtensionColors[2];
      }
      this._blockInfo.push(categoryInfo);
      this._fillExtensionCategory(categoryInfo, extensionInfo);
      for (var fieldTypeName in categoryInfo.customFieldTypes) {
        if (Object.prototype.hasOwnProperty.call(extensionInfo.customFieldTypes, fieldTypeName)) {
          var fieldTypeInfo = categoryInfo.customFieldTypes[fieldTypeName];

          // Emit events for custom field types from extension
          this.emit(Runtime.EXTENSION_FIELD_ADDED, {
            name: "field_".concat(fieldTypeInfo.extendedName),
            implementation: fieldTypeInfo.fieldImplementation
          });
        }
      }
      this.emit(Runtime.EXTENSION_ADDED, categoryInfo);
    }

    /**
     * Reregister the primitives for an extension
     * @param  {ExtensionMetadata} extensionInfo - new info (results of running getInfo) for an extension
     * @private
     */
  }, {
    key: "_refreshExtensionPrimitives",
    value: function _refreshExtensionPrimitives(extensionInfo) {
      var categoryInfo = this._blockInfo.find(function (info) {
        return info.id === extensionInfo.id;
      });
      if (categoryInfo) {
        categoryInfo.name = maybeFormatMessage(extensionInfo.name);
        this._fillExtensionCategory(categoryInfo, extensionInfo);
        this.emit(Runtime.BLOCKSINFO_UPDATE, categoryInfo);
      }
    }

    /**
     * Read extension information, convert menus, blocks and custom field types
     * and store the results in the provided category object.
     * @param {CategoryInfo} categoryInfo - the category to be filled
     * @param {ExtensionMetadata} extensionInfo - the extension metadata to read
     * @private
     */
  }, {
    key: "_fillExtensionCategory",
    value: function _fillExtensionCategory(categoryInfo, extensionInfo) {
      categoryInfo.blocks = [];
      categoryInfo.customFieldTypes = {};
      categoryInfo.menus = [];
      categoryInfo.menuInfo = {};
      for (var menuName in extensionInfo.menus) {
        if (Object.prototype.hasOwnProperty.call(extensionInfo.menus, menuName)) {
          var menuInfo = extensionInfo.menus[menuName];
          var convertedMenu = this._buildMenuForScratchBlocks(menuName, menuInfo, categoryInfo);
          categoryInfo.menus.push(convertedMenu);
          categoryInfo.menuInfo[menuName] = menuInfo;
        }
      }
      for (var fieldTypeName in extensionInfo.customFieldTypes) {
        if (Object.prototype.hasOwnProperty.call(extensionInfo.customFieldTypes, fieldTypeName)) {
          var fieldType = extensionInfo.customFieldTypes[fieldTypeName];
          var fieldTypeInfo = this._buildCustomFieldInfo(fieldTypeName, fieldType, extensionInfo.id, categoryInfo);
          categoryInfo.customFieldTypes[fieldTypeName] = fieldTypeInfo;
        }
      }
      if (extensionInfo.docsURI) {
        var xml = '<button ' + "text=\"".concat(xmlEscape(maybeFormatMessage({
          id: 'tw.blocks.openDocs',
          "default": 'Open Documentation',
          description: 'Button that opens site with more documentation about an extension'
        })), "\" ") + 'callbackKey="OPEN_EXTENSION_DOCS" ' + "callbackData=\"".concat(xmlEscape(extensionInfo.docsURI), "\"></button>");
        var block = {
          info: {},
          xml: xml
        };
        categoryInfo.blocks.push(block);
      }
      var _iterator = _createForOfIteratorHelper(extensionInfo.blocks),
        _step2;
      try {
        for (_iterator.s(); !(_step2 = _iterator.n()).done;) {
          var blockInfo = _step2.value;
          try {
            var convertedBlock = this._convertForScratchBlocks(blockInfo, categoryInfo);
            categoryInfo.blocks.push(convertedBlock);
            if (convertedBlock.json) {
              var opcode = convertedBlock.json.type;
              if (blockInfo.blockType !== BlockType.EVENT) {
                this._primitives[opcode] = convertedBlock.info.func;
              }
              if (blockInfo.blockType === BlockType.EVENT || blockInfo.blockType === BlockType.HAT) {
                this._hats[opcode] = {
                  edgeActivated: blockInfo.isEdgeActivated,
                  restartExistingThreads: blockInfo.shouldRestartExistingThreads
                };
              } else if (blockInfo.blockType === BlockType.CONDITIONAL) {
                this._flowing[opcode] = {
                  conditional: true,
                  loop: false
                };
              } else if (blockInfo.blockType === BlockType.LOOP) {
                this._flowing[opcode] = {
                  conditional: false,
                  loop: true
                };
              }
            }
          } catch (e) {
            log.error('Error parsing block: ', {
              block: blockInfo,
              error: e
            });
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * Convert the given extension menu items into the scratch-blocks style of list of pairs.
     * If the menu is dynamic (e.g. the passed in argument is a function), return the input unmodified.
     * @param {object} menuItems - an array of menu items or a function to retrieve such an array
     * @returns {object} - an array of 2 element arrays or the original input function
     * @private
     */
  }, {
    key: "_convertMenuItems",
    value: function _convertMenuItems(menuItems) {
      if (typeof menuItems !== 'function') {
        var extensionMessageContext = this.makeMessageContextForTarget();
        return menuItems.map(function (item) {
          var formattedItem = maybeFormatMessage(item, extensionMessageContext);
          switch (_typeof(formattedItem)) {
            case 'string':
              return [formattedItem, formattedItem];
            case 'object':
              return [maybeFormatMessage(item.text, extensionMessageContext), item.value];
            default:
              throw new Error("Can't interpret menu item: ".concat(JSON.stringify(item)));
          }
        });
      }
      return menuItems;
    }

    /**
     * Build the scratch-blocks JSON for a menu. Note that scratch-blocks treats menus as a special kind of block.
     * @param {string} menuName - the name of the menu
     * @param {object} menuInfo - a description of this menu and its items
     * @property {*} items - an array of menu items or a function to retrieve such an array
     * @property {boolean} [acceptReporters] - if true, allow dropping reporters onto this menu
     * @param {CategoryInfo} categoryInfo - the category for this block
     * @returns {object} - a JSON-esque object ready for scratch-blocks' consumption
     * @private
     */
  }, {
    key: "_buildMenuForScratchBlocks",
    value: function _buildMenuForScratchBlocks(menuName, menuInfo, categoryInfo) {
      var menuId = this._makeExtensionMenuId(menuName, categoryInfo.id);
      var menuItems = this._convertMenuItems(menuInfo.items);
      return {
        json: {
          message0: '%1',
          type: menuId,
          inputsInline: true,
          output: 'String',
          colour: categoryInfo.color1,
          colourSecondary: categoryInfo.color2,
          colourTertiary: categoryInfo.color3,
          outputShape: menuInfo.acceptReporters ? ScratchBlocksConstants.OUTPUT_SHAPE_ROUND : ScratchBlocksConstants.OUTPUT_SHAPE_SQUARE,
          args0: [{
            type: 'field_dropdown',
            name: menuName,
            options: menuItems
          }]
        }
      };
    }
  }, {
    key: "_buildCustomFieldInfo",
    value: function _buildCustomFieldInfo(fieldName, fieldInfo, extensionId, categoryInfo) {
      var extendedName = "".concat(extensionId, "_").concat(fieldName);
      return {
        fieldName: fieldName,
        extendedName: extendedName,
        argumentTypeInfo: {
          shadow: {
            type: extendedName,
            fieldName: "field_".concat(extendedName)
          }
        },
        scratchBlocksDefinition: this._buildCustomFieldTypeForScratchBlocks(extendedName, fieldInfo.output, fieldInfo.outputShape, categoryInfo),
        fieldImplementation: fieldInfo.implementation
      };
    }

    /**
     * Build the scratch-blocks JSON needed for a fieldType.
     * Custom field types need to be namespaced to the extension so that extensions can't interfere with each other
     * @param  {string} fieldName - The name of the field
     * @param {string} output - The output of the field
     * @param {number} outputShape - Shape of the field (from ScratchBlocksConstants)
     * @param {object} categoryInfo - The category the field belongs to (Used to set its colors)
     * @returns {object} - Object to be inserted into scratch-blocks
     */
  }, {
    key: "_buildCustomFieldTypeForScratchBlocks",
    value: function _buildCustomFieldTypeForScratchBlocks(fieldName, output, outputShape, categoryInfo) {
      return {
        json: {
          type: fieldName,
          message0: '%1',
          inputsInline: true,
          output: output,
          colour: categoryInfo.color1,
          colourSecondary: categoryInfo.color2,
          colourTertiary: categoryInfo.color3,
          outputShape: outputShape,
          args0: [{
            name: "field_".concat(fieldName),
            type: "field_".concat(fieldName)
          }]
        }
      };
    }

    /**
     * Convert ExtensionBlockMetadata into data ready for scratch-blocks.
     * @param {ExtensionBlockMetadata} blockInfo - the block info to convert
     * @param {CategoryInfo} categoryInfo - the category for this block
     * @returns {ConvertedBlockInfo} - the converted & original block information
     * @private
     */
  }, {
    key: "_convertForScratchBlocks",
    value: function _convertForScratchBlocks(blockInfo, categoryInfo) {
      if (blockInfo === '---') {
        return this._convertSeparatorForScratchBlocks(blockInfo);
      }
      if (blockInfo.blockType === BlockType.LABEL) {
        return this._convertLabelForScratchBlocks(blockInfo);
      }
      if (blockInfo.blockType === BlockType.BUTTON) {
        return this._convertButtonForScratchBlocks(blockInfo, categoryInfo);
      }
      if (blockInfo.blockType === BlockType.XML) {
        return this._convertXmlForScratchBlocks(blockInfo);
      }
      return this._convertBlockForScratchBlocks(blockInfo, categoryInfo);
    }

    /**
     * Convert ExtensionBlockMetadata into scratch-blocks JSON & XML, and generate a proxy function.
     * @param {ExtensionBlockMetadata} blockInfo - the block to convert
     * @param {CategoryInfo} categoryInfo - the category for this block
     * @returns {ConvertedBlockInfo} - the converted & original block information
     * @private
     */
  }, {
    key: "_convertBlockForScratchBlocks",
    value: function _convertBlockForScratchBlocks(blockInfo, categoryInfo) {
      var _blockInfo$color, _blockInfo$color2, _blockInfo$color3;
      var extendedOpcode = "".concat(categoryInfo.id, "_").concat(blockInfo.opcode);
      var blockJSON = {
        type: extendedOpcode,
        inputsInline: true,
        category: categoryInfo.name,
        extensions: [],
        colour: (_blockInfo$color = blockInfo.color1) !== null && _blockInfo$color !== void 0 ? _blockInfo$color : categoryInfo.color1,
        colourSecondary: (_blockInfo$color2 = blockInfo.color2) !== null && _blockInfo$color2 !== void 0 ? _blockInfo$color2 : categoryInfo.color2,
        colourTertiary: (_blockInfo$color3 = blockInfo.color3) !== null && _blockInfo$color3 !== void 0 ? _blockInfo$color3 : categoryInfo.color3
      };
      var context = {
        // TODO: store this somewhere so that we can map args appropriately after translation.
        // This maps an arg name to its relative position in the original (usually English) block text.
        // When displaying a block in another language we'll need to run a `replace` action similar to the one
        // below, but each `[ARG]` will need to be replaced with the number in this map.
        argsMap: {},
        blockJSON: blockJSON,
        categoryInfo: categoryInfo,
        blockInfo: blockInfo,
        inputList: []
      };

      // If an icon for the extension exists, prepend it to each block, with a vertical separator.
      // We can overspecify an icon for each block, but if no icon exists on a block, fall back to
      // the category block icon.
      var iconURI = blockInfo.blockIconURI || categoryInfo.blockIconURI;

      // All extension blocks have from_extension
      blockJSON.extensions.push('from_extension');

      // Allow easily detecting which blocks use default colors
      if (blockJSON.colour === defaultExtensionColors[0] && blockJSON.colourSecondary === defaultExtensionColors[1] && blockJSON.colourTertiary === defaultExtensionColors[2]) {
        blockJSON.extensions.push('default_extension_colors');
      }
      if (iconURI) {
        // scratch_extension is a misleading name - this is for fixing the icon rendering
        blockJSON.extensions.push('scratch_extension');
        blockJSON.message0 = '%1 %2';
        var iconJSON = {
          type: 'field_image',
          src: iconURI,
          width: 40,
          height: 40
        };
        var separatorJSON = {
          type: 'field_vertical_separator'
        };
        blockJSON.args0 = [iconJSON, separatorJSON];
      }
      switch (blockInfo.blockType) {
        case BlockType.COMMAND:
          blockJSON.outputShape = ScratchBlocksConstants.OUTPUT_SHAPE_SQUARE;
          blockJSON.previousStatement = null; // null = available connection; undefined = hat
          if (!blockInfo.isTerminal) {
            blockJSON.nextStatement = null; // null = available connection; undefined = terminal
          }
          break;
        case BlockType.REPORTER:
          blockJSON.output = blockInfo.allowDropAnywhere ? null : 'String'; // TODO: distinguish number & string here?
          blockJSON.outputShape = ScratchBlocksConstants.OUTPUT_SHAPE_ROUND;
          break;
        case BlockType.BOOLEAN:
          blockJSON.output = 'Boolean';
          blockJSON.outputShape = ScratchBlocksConstants.OUTPUT_SHAPE_HEXAGONAL;
          break;
        case BlockType.HAT:
        case BlockType.EVENT:
          if (!Object.prototype.hasOwnProperty.call(blockInfo, 'isEdgeActivated')) {
            // if absent, this property defaults to true
            blockInfo.isEdgeActivated = true;
          }
          blockJSON.outputShape = ScratchBlocksConstants.OUTPUT_SHAPE_SQUARE;
          blockJSON.nextStatement = null; // null = available connection; undefined = terminal
          break;
        case BlockType.CONDITIONAL:
        case BlockType.LOOP:
          blockInfo.branchCount = blockInfo.branchCount || 1;
          blockJSON.outputShape = ScratchBlocksConstants.OUTPUT_SHAPE_SQUARE;
          blockJSON.previousStatement = null; // null = available connection; undefined = hat
          if (!blockInfo.isTerminal) {
            blockJSON.nextStatement = null; // null = available connection; undefined = terminal
          }
          break;
      }

      // Allow extensiosn to override outputShape
      if (blockInfo.blockShape) {
        blockJSON.outputShape = blockInfo.blockShape;
      }
      var blockText = Array.isArray(blockInfo.text) ? blockInfo.text : [blockInfo.text];
      var inTextNum = 0; // text for the next block "arm" is blockText[inTextNum]
      var inBranchNum = 0; // how many branches have we placed into the JSON so far?
      var outLineNum = 0; // used for scratch-blocks `message${outLineNum}` and `args${outLineNum}`
      var convertPlaceholders = this._convertPlaceholders.bind(this, context);
      var extensionMessageContext = this.makeMessageContextForTarget();

      // alternate between a block "arm" with text on it and an open slot for a substack
      while (inTextNum < blockText.length || inBranchNum < blockInfo.branchCount) {
        if (inTextNum < blockText.length) {
          context.outLineNum = outLineNum;
          var lineText = maybeFormatMessage(blockText[inTextNum], extensionMessageContext);
          var convertedText = lineText.replace(/\[(.+?)]/g, convertPlaceholders);
          if (blockJSON["message".concat(outLineNum)]) {
            blockJSON["message".concat(outLineNum)] += convertedText;
          } else {
            blockJSON["message".concat(outLineNum)] = convertedText;
          }
          ++inTextNum;
          ++outLineNum;
        }
        if (inBranchNum < blockInfo.branchCount) {
          blockJSON["message".concat(outLineNum)] = '%1';
          blockJSON["args".concat(outLineNum)] = [{
            type: 'input_statement',
            name: "SUBSTACK".concat(inBranchNum > 0 ? inBranchNum + 1 : '')
          }];
          ++inBranchNum;
          ++outLineNum;
        }
      }
      if (blockInfo.blockType === BlockType.REPORTER || blockInfo.blockType === BlockType.BOOLEAN) {
        if (!blockInfo.disableMonitor && context.inputList.length === 0) {
          blockJSON.checkboxInFlyout = true;
        }
      } else if (blockInfo.branchIconURI || blockInfo.blockType === BlockType.LOOP && !Object.prototype.hasOwnProperty.call(blockInfo, 'branchIconURI')) {
        var _blockInfo$branchIcon;
        // Add icon to the bottom right of a loop block
        blockJSON["lastDummyAlign".concat(outLineNum)] = 'RIGHT';
        blockJSON["message".concat(outLineNum)] = '%1';
        blockJSON["args".concat(outLineNum)] = [{
          type: 'field_image',
          src: (_blockInfo$branchIcon = blockInfo.branchIconURI) !== null && _blockInfo$branchIcon !== void 0 ? _blockInfo$branchIcon : 'media://repeat.svg',
          width: 24,
          height: 24,
          alt: '*',
          // TODO remove this since we don't use collapsed blocks in scratch
          flip_rtl: true
        }];
        ++outLineNum;
      }
      var mutation = blockInfo.isDynamic ? "<mutation blockInfo=\"".concat(xmlEscape(JSON.stringify(blockInfo)), "\"/>") : '';
      var inputs = context.inputList.join('');
      var blockXML = "<block type=\"".concat(xmlEscape(extendedOpcode), "\">").concat(mutation).concat(inputs, "</block>");
      if (blockInfo.extensions) {
        var _iterator2 = _createForOfIteratorHelper(blockInfo.extensions),
          _step3;
        try {
          for (_iterator2.s(); !(_step3 = _iterator2.n()).done;) {
            var extension = _step3.value;
            if (!blockJSON.extensions.includes(extension)) {
              blockJSON.extensions.push(extension);
            }
          }
        } catch (err) {
          _iterator2.e(err);
        } finally {
          _iterator2.f();
        }
      }
      return {
        info: context.blockInfo,
        json: context.blockJSON,
        xml: blockXML
      };
    }

    /**
     * Generate a separator between blocks categories or sub-categories.
     * @param {ExtensionBlockMetadata} blockInfo - the block to convert
     * @param {CategoryInfo} categoryInfo - the category for this block
     * @returns {ConvertedBlockInfo} - the converted & original block information
     * @private
     */
  }, {
    key: "_convertSeparatorForScratchBlocks",
    value: function _convertSeparatorForScratchBlocks(blockInfo) {
      return {
        info: blockInfo,
        xml: '<sep gap="36"/>'
      };
    }

    /**
     * Generate a label between blocks categories or sub-categories.
     * @param {ExtensionBlockMetadata} blockInfo - the block to convert
     * @returns {ConvertedBlockInfo} - the converted & original block information
     * @private
     */
  }, {
    key: "_convertLabelForScratchBlocks",
    value: function _convertLabelForScratchBlocks(blockInfo) {
      return {
        info: blockInfo,
        xml: "<label text=\"".concat(xmlEscape(blockInfo.text), "\"></label>")
      };
    }

    /**
     * Convert a button for scratch-blocks. A button has no opcode but specifies a callback name in the `func` field.
     * @param {ExtensionBlockMetadata} buttonInfo - the button to convert
     * @property {string} func - the callback name
     * @param {CategoryInfo} categoryInfo - the category for this button
     * @returns {ConvertedBlockInfo} - the converted & original button information
     * @private
     */
  }, {
    key: "_convertButtonForScratchBlocks",
    value: function _convertButtonForScratchBlocks(buttonInfo, categoryInfo) {
      var extensionMessageContext = this.makeMessageContextForTarget();
      var buttonText = maybeFormatMessage(buttonInfo.text, extensionMessageContext);
      var nativeCallbackKeys = ['MAKE_A_LIST', 'MAKE_A_PROCEDURE', 'MAKE_A_VARIABLE'];
      if (nativeCallbackKeys.includes(buttonInfo.func)) {
        return {
          info: buttonInfo,
          xml: "<button text=\"".concat(xmlEscape(buttonText), "\" callbackKey=\"").concat(xmlEscape(buttonInfo.func), "\"></button>")
        };
      }
      // Callbacks with data will be forwarded from GUI
      var id = "".concat(categoryInfo.id, "_").concat(buttonInfo.func);
      // callFunc is set by extension manager
      this.extensionButtons.set(id, buttonInfo.callFunc);
      return {
        info: buttonInfo,
        xml: "<button text=\"".concat(xmlEscape(buttonText), "\"") + ' callbackKey="EXTENSION_CALLBACK"' + " callbackData=\"".concat(xmlEscape(id), "\"></button>")
      };
    }
  }, {
    key: "_convertXmlForScratchBlocks",
    value: function _convertXmlForScratchBlocks(xmlInfo) {
      return {
        info: xmlInfo,
        xml: xmlInfo.xml
      };
    }
  }, {
    key: "handleExtensionButtonPress",
    value: function handleExtensionButtonPress(buttonData) {
      var callback = this.extensionButtons.get(buttonData);
      callback();
    }

    /**
     * Helper for _convertPlaceholdes which handles inline images which are a specialized case of block "arguments".
     * @param {object} argInfo Metadata about the inline image as specified by the extension
     * @return {object} JSON blob for a scratch-blocks image field.
     * @private
     */
  }, {
    key: "_constructInlineImageJson",
    value: function _constructInlineImageJson(argInfo) {
      if (!argInfo.dataURI) {
        log.warn('Missing data URI in extension block with argument type IMAGE');
      }
      return {
        type: 'field_image',
        src: argInfo.dataURI || '',
        // TODO these probably shouldn't be hardcoded...?
        width: 24,
        height: 24,
        // Whether or not the inline image should be flipped horizontally
        // in RTL languages. Defaults to false, indicating that the
        // image will not be flipped.
        flip_rtl: argInfo.flipRTL || false
      };
    }

    /**
     * Helper for _convertForScratchBlocks which handles linearization of argument placeholders. Called as a callback
     * from string#replace. In addition to the return value the JSON and XML items in the context will be filled.
     * @param {object} context - information shared with _convertForScratchBlocks about the block, etc.
     * @param {string} match - the overall string matched by the placeholder regex, including brackets: '[FOO]'.
     * @param {string} placeholder - the name of the placeholder being matched: 'FOO'.
     * @return {string} scratch-blocks placeholder for the argument: '%1'.
     * @private
     */
  }, {
    key: "_convertPlaceholders",
    value: function _convertPlaceholders(context, match, placeholder) {
      // Determine whether the argument type is one of the known standard field types
      var argInfo = context.blockInfo.arguments[placeholder] || {};
      var argTypeInfo = ArgumentTypeMap[argInfo.type] || {};

      // Field type not a standard field type, see if extension has registered custom field type
      if (!ArgumentTypeMap[argInfo.type] && context.categoryInfo.customFieldTypes[argInfo.type]) {
        argTypeInfo = context.categoryInfo.customFieldTypes[argInfo.type].argumentTypeInfo;
      }

      // Start to construct the scratch-blocks style JSON defining how the block should be
      // laid out
      var argJSON;

      // Most field types are inputs (slots on the block that can have other blocks plugged into them)
      // check if this is not one of those cases. E.g. an inline image on a block.
      if (argTypeInfo.fieldType === 'field_image') {
        argJSON = this._constructInlineImageJson(argInfo);
      } else {
        // Construct input value

        // Layout a block argument (e.g. an input slot on the block)
        argJSON = {
          type: 'input_value',
          name: placeholder
        };
        var defaultValue = typeof argInfo.defaultValue === 'undefined' ? null : maybeFormatMessage(argInfo.defaultValue, this.makeMessageContextForTarget()).toString();
        if (argTypeInfo.check) {
          // Right now the only type of 'check' we have specifies that the
          // input slot on the block accepts Boolean reporters, so it should be
          // shaped like a hexagon
          argJSON.check = argTypeInfo.check;
        }
        var valueName;
        var shadowType;
        var fieldName;
        if (argInfo.menu) {
          var menuInfo = context.categoryInfo.menuInfo[argInfo.menu];
          if (menuInfo.acceptReporters) {
            valueName = placeholder;
            shadowType = this._makeExtensionMenuId(argInfo.menu, context.categoryInfo.id);
            fieldName = argInfo.menu;
          } else {
            argJSON.type = 'field_dropdown';
            argJSON.options = this._convertMenuItems(menuInfo.items);
            valueName = null;
            shadowType = null;
            fieldName = placeholder;
          }
        } else {
          valueName = placeholder;
          shadowType = argTypeInfo.shadow && argTypeInfo.shadow.type || null;
          fieldName = argTypeInfo.shadow && argTypeInfo.shadow.fieldName || null;
        }

        // <value> is the ScratchBlocks name for a block input.
        if (valueName) {
          context.inputList.push("<value name=\"".concat(xmlEscape(placeholder), "\">"));
        }

        // The <shadow> is a placeholder for a reporter and is visible when there's no reporter in this input.
        // Boolean inputs don't need to specify a shadow in the XML.
        if (shadowType) {
          context.inputList.push("<shadow type=\"".concat(xmlEscape(shadowType), "\">"));
        }

        // A <field> displays a dynamic value: a user-editable text field, a drop-down menu, etc.
        // Leave out the field if defaultValue or fieldName are not specified
        if (defaultValue !== null && fieldName) {
          context.inputList.push("<field name=\"".concat(xmlEscape(fieldName), "\">").concat(xmlEscape(defaultValue), "</field>"));
        }
        if (shadowType) {
          context.inputList.push('</shadow>');
        }
        if (valueName) {
          context.inputList.push('</value>');
        }
      }
      var argsName = "args".concat(context.outLineNum);
      var blockArgs = context.blockJSON[argsName] = context.blockJSON[argsName] || [];
      if (argJSON) blockArgs.push(argJSON);
      var argNum = blockArgs.length;
      context.argsMap[placeholder] = argNum;
      return "%".concat(argNum);
    }

    /**
     * @returns {Array.<object>} scratch-blocks XML for each category of extension blocks, in category order.
     * @param {?Target} [target] - the active editing target (optional)
     * @property {string} id - the category / extension ID
     * @property {string} xml - the XML text for this category, starting with `<category>` and ending with `</category>`
     */
  }, {
    key: "getBlocksXML",
    value: function getBlocksXML(target) {
      return this._blockInfo.map(function (categoryInfo) {
        var name = categoryInfo.name,
          color1 = categoryInfo.color1,
          color2 = categoryInfo.color2;
        // Filter out blocks that aren't supposed to be shown on this target, as determined by the block info's
        // `hideFromPalette` and `filter` properties.
        var paletteBlocks = categoryInfo.blocks.filter(function (block) {
          var blockFilterIncludesTarget = true;
          // If an editing target is not passed, include all blocks
          // If the block info doesn't include a `filter` property, always include it
          if (target && block.info.filter) {
            blockFilterIncludesTarget = block.info.filter.includes(target.isStage ? TargetType.STAGE : TargetType.SPRITE);
          }
          // If the block info's `hideFromPalette` is true, then filter out this block
          return blockFilterIncludesTarget && !block.info.hideFromPalette;
        });
        var colorXML = "colour=\"".concat(xmlEscape(color1), "\" secondaryColour=\"").concat(xmlEscape(color2), "\"");

        // Use a menu icon if there is one. Otherwise, use the block icon. If there's no icon,
        // the category menu will show its default colored circle.
        var menuIconURI = '';
        if (categoryInfo.menuIconURI) {
          menuIconURI = categoryInfo.menuIconURI;
        } else if (categoryInfo.blockIconURI) {
          menuIconURI = categoryInfo.blockIconURI;
        }
        var menuIconXML = menuIconURI ? "iconURI=\"".concat(xmlEscape(menuIconURI), "\"") : '';
        var statusButtonXML = '';
        if (categoryInfo.showStatusButton) {
          statusButtonXML = 'showStatusButton="true"';
        }
        var xml = "<category name=\"".concat(xmlEscape(name), "\"");
        xml += " id=\"".concat(xmlEscape(categoryInfo.id), "\"");
        xml += " ".concat(statusButtonXML);
        xml += " ".concat(colorXML);
        xml += " ".concat(menuIconXML, ">");
        xml += paletteBlocks.map(function (block) {
          return block.xml;
        }).join('');
        xml += '</category>';
        return {
          id: categoryInfo.id,
          xml: xml
        };
      });
    }

    /**
     * @returns {Array.<string>} - an array containing the scratch-blocks JSON information for each dynamic block.
     */
  }, {
    key: "getBlocksJSON",
    value: function getBlocksJSON() {
      return this._blockInfo.reduce(function (result, categoryInfo) {
        return result.concat(categoryInfo.blocks.map(function (blockInfo) {
          return blockInfo.json;
        }));
      }, []);
    }

    /**
     * One-time initialization for Scratch Link support.
     */
  }, {
    key: "_initScratchLink",
    value: function _initScratchLink() {
      // Check that we're actually in a real browser, not Node.js or JSDOM, and we have a valid-looking origin.
      // note that `if (self?....)` will throw if `self` is undefined, so check for that first!
      if (typeof self !== 'undefined' && typeof document !== 'undefined' && document.getElementById && self.origin && self.origin !== 'null' &&
      // note this is a string comparison, not a null check
      self.navigator && self.navigator.userAgent && !(self.navigator.userAgent.includes('Node.js') || self.navigator.userAgent.includes('jsdom'))) {
        // Create a script tag for the Scratch Link browser extension, unless one already exists
        var scriptElement = document.getElementById('scratch-link-extension-script');
        if (!scriptElement) {
          var script = document.createElement('script');
          script.id = 'scratch-link-extension-script';
          document.body.appendChild(script);

          // Tell the browser extension to inject its script.
          // If the extension isn't present or isn't active, this will do nothing.
          self.postMessage('inject-scratch-link-script', self.origin);
        }
      }
    }

    /**
     * Get a scratch link socket.
     * @param {string} type Either BLE or BT
     * @returns {ScratchLinkSocket} The scratch link socket.
     */
  }, {
    key: "getScratchLinkSocket",
    value: function getScratchLinkSocket(type) {
      var factory = this._linkSocketFactory || this._defaultScratchLinkSocketFactory;
      return factory(type);
    }

    /**
     * Configure how ScratchLink sockets are created. Factory must consume a "type" parameter
     * either BT or BLE.
     * @param {Function} factory The new factory for creating ScratchLink sockets.
     */
  }, {
    key: "configureScratchLinkSocketFactory",
    value: function configureScratchLinkSocketFactory(factory) {
      this._linkSocketFactory = factory;
    }

    /**
     * The default scratch link socket creator, using websockets to the installed device manager.
     * @param {string} type Either BLE or BT
     * @returns {ScratchLinkSocket} The new scratch link socket (a WebSocket object)
     */
  }, {
    key: "_defaultScratchLinkSocketFactory",
    value: function _defaultScratchLinkSocketFactory(type) {
      var Scratch = self.Scratch;
      var ScratchLinkSafariSocket = Scratch && Scratch.ScratchLinkSafariSocket;
      // detect this every time in case the user turns on the extension after loading the page
      var useSafariSocket = ScratchLinkSafariSocket && ScratchLinkSafariSocket.isSafariHelperCompatible();
      return useSafariSocket ? new ScratchLinkSafariSocket(type) : new ScratchLinkWebSocket(type);
    }

    /**
     * Register an extension that communications with a hardware peripheral by id,
     * to have access to it and its peripheral functions in the future.
     * @param {string} extensionId - the id of the extension.
     * @param {object} extension - the extension to register.
     */
  }, {
    key: "registerPeripheralExtension",
    value: function registerPeripheralExtension(extensionId, extension) {
      this.peripheralExtensions[extensionId] = extension;
    }

    /**
     * Tell the specified extension to scan for a peripheral.
     * @param {string} extensionId - the id of the extension.
     */
  }, {
    key: "scanForPeripheral",
    value: function scanForPeripheral(extensionId) {
      if (this.peripheralExtensions[extensionId]) {
        this.peripheralExtensions[extensionId].scan();
      }
    }

    /**
     * Connect to the extension's specified peripheral.
     * @param {string} extensionId - the id of the extension.
     * @param {number} peripheralId - the id of the peripheral.
     */
  }, {
    key: "connectPeripheral",
    value: function connectPeripheral(extensionId, peripheralId) {
      if (this.peripheralExtensions[extensionId]) {
        this.peripheralExtensions[extensionId].connect(peripheralId);
      }
    }

    /**
     * Disconnect from the extension's connected peripheral.
     * @param {string} extensionId - the id of the extension.
     */
  }, {
    key: "disconnectPeripheral",
    value: function disconnectPeripheral(extensionId) {
      if (this.peripheralExtensions[extensionId]) {
        this.peripheralExtensions[extensionId].disconnect();
      }
    }

    /**
     * Returns whether the extension has a currently connected peripheral.
     * @param {string} extensionId - the id of the extension.
     * @return {boolean} - whether the extension has a connected peripheral.
     */
  }, {
    key: "getPeripheralIsConnected",
    value: function getPeripheralIsConnected(extensionId) {
      var isConnected = false;
      if (this.peripheralExtensions[extensionId]) {
        isConnected = this.peripheralExtensions[extensionId].isConnected();
      }
      return isConnected;
    }

    /**
     * Emit an event to indicate that the microphone is being used to stream audio.
     * @param {boolean} listening - true if the microphone is currently listening.
     */
  }, {
    key: "emitMicListening",
    value: function emitMicListening(listening) {
      this.emit(Runtime.MIC_LISTENING, listening);
    }

    /**
     * Retrieve the function associated with the given opcode.
     * @param {!string} opcode The opcode to look up.
     * @return {Function} The function which implements the opcode.
     */
  }, {
    key: "getOpcodeFunction",
    value: function getOpcodeFunction(opcode) {
      return this._primitives[opcode];
    }

    /**
     * Return whether an opcode represents a hat block.
     * @param {!string} opcode The opcode to look up.
     * @return {boolean} True if the op is known to be a hat.
     */
  }, {
    key: "getIsHat",
    value: function getIsHat(opcode) {
      return Object.prototype.hasOwnProperty.call(this._hats, opcode);
    }

    /**
     * Return whether an opcode represents an edge-activated hat block.
     * @param {!string} opcode The opcode to look up.
     * @return {boolean} True if the op is known to be a edge-activated hat.
     */
  }, {
    key: "getIsEdgeActivatedHat",
    value: function getIsEdgeActivatedHat(opcode) {
      return Object.prototype.hasOwnProperty.call(this._hats, opcode) && this._hats[opcode].edgeActivated;
    }

    /**
     * Attach the audio engine
     * @param {!AudioEngine} audioEngine The audio engine to attach
     */
  }, {
    key: "attachAudioEngine",
    value: function attachAudioEngine(audioEngine) {
      this.audioEngine = audioEngine;
    }

    /**
     * Attach the renderer
     * @param {!RenderWebGL} renderer The renderer to attach
     */
  }, {
    key: "attachRenderer",
    value: function attachRenderer(renderer) {
      this.renderer = renderer;
      this.renderer.setLayerGroupOrdering(StageLayering.LAYER_GROUPS);
      this.renderer.offscreenTouching = !this.runtimeOptions.fencing;
      this.updatePrivacy();
    }

    /**
     * Set the bitmap adapter for the VM/runtime, which converts scratch 2
     * bitmaps to scratch 3 bitmaps. (Scratch 3 bitmaps are all bitmap resolution 2)
     * @param {!function} bitmapAdapter The adapter to attach
     */
  }, {
    key: "attachV2BitmapAdapter",
    value: function attachV2BitmapAdapter(bitmapAdapter) {
      this.v2BitmapAdapter = bitmapAdapter;
    }

    /**
     * Attach the storage module
     * @param {!ScratchStorage} storage The storage module to attach
     */
  }, {
    key: "attachStorage",
    value: function attachStorage(storage) {
      this.storage = storage;
      if (this.isPackaged) {
        // In packaged runtime mode, generating real asset IDs is a waste of time.
        // We do still want to preserve every asset having a unique ID.
        var originalCreateAsset = storage.createAsset;
        var assetIdCounter = 0;
        // eslint-disable-next-line no-unused-vars
        storage.createAsset = function packagedCreateAsset(assetType, dataFormat, data, assetId, generateId) {
          if (!assetId) {
            assetId = (++assetIdCounter).toString();
          }
          return originalCreateAsset.call(this, assetType, dataFormat, data, assetId,
          // Never generate real asset ID
          false);
        };
      }
      fetchWithTimeout.setFetch(storage.scratchFetch.scratchFetch);
      this.resetRunId();
    }

    // -----------------------------------------------------------------------------
    // -----------------------------------------------------------------------------

    /**
     * Create a thread and push it to the list of threads.
     * @param {!string} id ID of block that starts the stack.
     * @param {!Target} target Target to run thread on.
     * @param {?object} opts optional arguments
     * @param {?boolean} opts.stackClick true if the script was activated by clicking on the stack
     * @param {?boolean} opts.updateMonitor true if the script should update a monitor value
     * @return {!Thread} The newly created thread.
     */
  }, {
    key: "_pushThread",
    value: function _pushThread(id, target, opts) {
      var thread = new Thread(id);
      thread.target = target;
      thread.stackClick = Boolean(opts && opts.stackClick);
      thread.updateMonitor = Boolean(opts && opts.updateMonitor);
      thread.blockContainer = thread.updateMonitor ? this.monitorBlocks : target.blocks;
      thread.pushStack(id);
      this.threads.push(thread);
      if (!thread.stackClick && !thread.updateMonitor) {
        this.threadMap.set(thread.getId(), thread);
      }

      // tw: compile new threads. Do not attempt to compile monitor threads.
      if (!(opts && opts.updateMonitor) && this.compilerOptions.enabled) {
        thread.tryCompile();
      }
      return thread;
    }

    /**
     * Stop a thread: stop running it immediately, and remove it from the thread list later.
     * @param {!Thread} thread Thread object to remove from actives
     */
  }, {
    key: "_stopThread",
    value: function _stopThread(thread) {
      // Mark the thread for later removal
      thread.isKilled = true;
      // Inform sequencer to stop executing that thread.
      this.sequencer.retireThread(thread);
    }

    /**
     * Restart a thread in place, maintaining its position in the list of threads.
     * This is used by `startHats` to and is necessary to ensure 2.0-like execution order.
     * Test project: https://scratch.mit.edu/projects/130183108/
     * @param {!Thread} thread Thread object to restart.
     * @return {Thread} The restarted thread.
     */
  }, {
    key: "_restartThread",
    value: function _restartThread(thread) {
      var newThread = new Thread(thread.topBlock);
      newThread.target = thread.target;
      newThread.stackClick = thread.stackClick;
      newThread.updateMonitor = thread.updateMonitor;
      newThread.blockContainer = thread.blockContainer;
      newThread.pushStack(thread.topBlock);
      // tw: when a thread is restarted, we have to check whether the previous script was attempted to be compiled.
      if (thread.triedToCompile && this.compilerOptions.enabled) {
        newThread.tryCompile();
      }
      if (!newThread.stackClick && !newThread.updateMonitor) {
        this.threadMap.set(newThread.getId(), newThread);
      }
      var i = this.threads.indexOf(thread);
      if (i > -1) {
        this.threads[i] = newThread;
        return newThread;
      }
      this.threads.push(thread);
      return thread;
    }
  }, {
    key: "emitCompileError",
    value: function emitCompileError(target, error) {
      this.emit(Runtime.COMPILE_ERROR, target, error);
    }

    /**
     * Return whether a thread is currently active/running.
     * @param {?Thread} thread Thread object to check.
     * @return {boolean} True if the thread is active/running.
     */
  }, {
    key: "isActiveThread",
    value: function isActiveThread(thread) {
      return thread.stack.length > 0 && thread.status !== Thread.STATUS_DONE && this.threads.indexOf(thread) > -1;
    }

    /**
     * Return whether a thread is waiting for more information or done.
     * @param {?Thread} thread Thread object to check.
     * @return {boolean} True if the thread is waiting
     */
  }, {
    key: "isWaitingThread",
    value: function isWaitingThread(thread) {
      return thread.status === Thread.STATUS_PROMISE_WAIT || thread.status === Thread.STATUS_YIELD_TICK || !this.isActiveThread(thread);
    }

    /**
     * Toggle a script.
     * @param {!string} topBlockId ID of block that starts the script.
     * @param {?object} opts optional arguments to toggle script
     * @param {?string} opts.target target ID for target to run script on. If not supplied, uses editing target.
     * @param {?boolean} opts.stackClick true if the user activated the stack by clicking, false if not. This
     *     determines whether we show a visual report when turning on the script.
     */
  }, {
    key: "toggleScript",
    value: function toggleScript(topBlockId, opts) {
      opts = Object.assign({
        target: this._editingTarget,
        stackClick: false
      }, opts);
      // Remove any existing thread.
      for (var i = 0; i < this.threads.length; i++) {
        // Toggling a script that's already running turns it off
        if (this.threads[i].topBlock === topBlockId && this.threads[i].status !== Thread.STATUS_DONE) {
          var blockContainer = opts.target.blocks;
          var opcode = blockContainer.getOpcode(blockContainer.getBlock(topBlockId));
          if (this.getIsEdgeActivatedHat(opcode) && this.threads[i].stackClick !== opts.stackClick) {
            // Allow edge activated hat thread stack click to coexist with
            // edge activated hat thread that runs every frame
            continue;
          }
          this._stopThread(this.threads[i]);
          return;
        }
      }
      // Otherwise add it.
      this._pushThread(topBlockId, opts.target, opts);
    }

    /**
     * Enqueue a script that when finished will update the monitor for the block.
     * @param {!string} topBlockId ID of block that starts the script.
     * @param {?Target} optTarget target Target to run script on. If not supplied, uses editing target.
     */
  }, {
    key: "addMonitorScript",
    value: function addMonitorScript(topBlockId, optTarget) {
      if (!optTarget) optTarget = this._editingTarget;
      for (var i = 0; i < this.threads.length; i++) {
        // Don't re-add the script if it's already running
        if (this.threads[i].topBlock === topBlockId && this.threads[i].status !== Thread.STATUS_DONE && this.threads[i].updateMonitor) {
          return;
        }
      }
      // Otherwise add it.
      this._pushThread(topBlockId, optTarget, {
        updateMonitor: true
      });
    }

    /**
     * Run a function `f` for all scripts in a workspace.
     * `f` will be called with two parameters:
     *  - the top block ID of the script.
     *  - the target that owns the script.
     * @param {!Function} f Function to call for each script.
     * @param {Target=} optTarget Optionally, a target to restrict to.
     */
  }, {
    key: "allScriptsDo",
    value: function allScriptsDo(f, optTarget) {
      var targets = this.executableTargets;
      if (optTarget) {
        targets = [optTarget];
      }
      for (var t = targets.length - 1; t >= 0; t--) {
        var target = targets[t];
        var scripts = target.blocks.getScripts();
        for (var j = 0; j < scripts.length; j++) {
          var topBlockId = scripts[j];
          f(topBlockId, target);
        }
      }
    }
  }, {
    key: "allScriptsByOpcodeDo",
    value: function allScriptsByOpcodeDo(opcode, f, optTarget) {
      var targets = this.executableTargets;
      if (optTarget) {
        targets = [optTarget];
      }
      for (var t = targets.length - 1; t >= 0; t--) {
        var target = targets[t];
        var scripts = BlocksRuntimeCache.getScripts(target.blocks, opcode);
        for (var j = 0; j < scripts.length; j++) {
          f(scripts[j], target);
        }
      }
    }

    /**
     * Start all relevant hats.
     * @param {!string} requestedHatOpcode Opcode of hats to start.
     * @param {object=} optMatchFields Optionally, fields to match on the hat.
     * @param {Target=} optTarget Optionally, a target to restrict to.
     * @return {Array.<Thread>} List of threads started by this function.
     */
  }, {
    key: "startHats",
    value: function startHats(requestedHatOpcode, optMatchFields, optTarget) {
      var _this4 = this;
      if (!Object.prototype.hasOwnProperty.call(this._hats, requestedHatOpcode)) {
        // No known hat with this opcode.
        return;
      }
      var instance = this;
      var newThreads = [];
      // Look up metadata for the relevant hat.
      var hatMeta = instance._hats[requestedHatOpcode];
      for (var opts in optMatchFields) {
        if (!Object.prototype.hasOwnProperty.call(optMatchFields, opts)) continue;
        optMatchFields[opts] = optMatchFields[opts].toUpperCase();
      }

      // tw: By assuming that all new threads will not interfere with eachother, we can optimize the loops
      // inside the allScriptsByOpcodeDo callback below.
      var startingThreadListLength = this.threads.length;

      // Consider all scripts, looking for hats with opcode `requestedHatOpcode`.
      this.allScriptsByOpcodeDo(requestedHatOpcode, function (script, target) {
        var topBlockId = script.blockId,
          hatFields = script.fieldsOfInputs;

        // Match any requested fields.
        // For example: ensures that broadcasts match.
        // This needs to happen before the block is evaluated
        // (i.e., before the predicate can be run) because "broadcast and wait"
        // needs to have a precise collection of started threads.
        for (var matchField in optMatchFields) {
          if (hatFields[matchField].value !== optMatchFields[matchField]) {
            // Field mismatch.
            return;
          }
        }
        if (hatMeta.restartExistingThreads) {
          // If `restartExistingThreads` is true, we should stop
          // any existing threads starting with the top block.
          var existingThread = _this4.threadMap.get(Thread.getIdFromTargetAndBlock(target, topBlockId));
          if (existingThread) {
            newThreads.push(_this4._restartThread(existingThread));
            return;
          }
        } else {
          // If `restartExistingThreads` is false, we should
          // give up if any threads with the top block are running.
          for (var j = 0; j < startingThreadListLength; j++) {
            if (_this4.threads[j].target === target && _this4.threads[j].topBlock === topBlockId &&
            // stack click threads and hat threads can coexist
            !_this4.threads[j].stackClick && _this4.threads[j].status !== Thread.STATUS_DONE) {
              // Some thread is already running.
              return;
            }
          }
        }
        // Start the thread with this top block.
        newThreads.push(_this4._pushThread(topBlockId, target));
      }, optTarget);
      // For compatibility with Scratch 2, edge triggered hats need to be processed before
      // threads are stepped. See ScratchRuntime.as for original implementation
      newThreads.forEach(function (thread) {
        if (thread.isCompiled) {
          if (thread.executableHat) {
            // It is quite likely that we are currently executing a block, so make sure
            // that we leave the compiler's state intact at the end.
            compilerExecute.saveGlobalState();
            compilerExecute(thread);
            compilerExecute.restoreGlobalState();
          }
        } else {
          execute(_this4.sequencer, thread);
          thread.goToNextBlock();
        }
      });
      return newThreads;
    }

    /**
     * Dispose all targets. Return to clean state.
     */
  }, {
    key: "dispose",
    value: function dispose() {
      this.stopAll();
      // Deleting each target's variable's monitors.
      this.targets.forEach(function (target) {
        if (target.isOriginal) target.deleteMonitors();
      });
      this.targets.map(this.disposeTarget, this);
      this.extensionStorage = {};
      // tw: explicitly emit a MONITORS_UPDATE instead of relying on implicit behavior of _step()
      if (!this._monitorState.empty()) {
        this._monitorState = new MonitorState();
        this.emit(Runtime.MONITORS_UPDATE, this._monitorState.shallowClone());
      }
      this.emit(Runtime.RUNTIME_DISPOSED);
      this.ioDevices.clock.resetProjectTimer();
      this.fontManager.clear();
      // @todo clear out extensions? turboMode? etc.

      // *********** Cloud *******************

      // If the runtime currently has cloud data,
      // emit a has cloud data update event resetting
      // it to false
      if (this.hasCloudData()) {
        this.emit(Runtime.HAS_CLOUD_DATA_UPDATE, false);
      }
      this.ioDevices.cloud.clear();

      // Reset runtime cloud data info
      var newCloudDataManager = cloudDataManager(this.cloudOptions);
      this.hasCloudData = newCloudDataManager.hasCloudVariables;
      this.canAddCloudVariable = newCloudDataManager.canAddCloudVariable;
      this.getNumberOfCloudVariables = newCloudDataManager.getNumberOfCloudVariables;
      this.addCloudVariable = this._initializeAddCloudVariable(newCloudDataManager);
      this.removeCloudVariable = this._initializeRemoveCloudVariable(newCloudDataManager);
      this.resetProgress();
    }

    /**
     * Add a target to the runtime. This tracks the sprite pane
     * ordering of the target. The target still needs to be put
     * into the correct execution order after calling this function.
     * @param {Target} target target to add
     */
  }, {
    key: "addTarget",
    value: function addTarget(target) {
      this.targets.push(target);
      this.executableTargets.push(target);
      if (target.isStage && !this._stageTarget) {
        this._stageTarget = target;
      }
    }

    /**
     * Move a target in the execution order by a relative amount.
     *
     * A positve number will make the target execute earlier. A negative number
     * will make the target execute later in the order.
     *
     * @param {Target} executableTarget target to move
     * @param {number} delta number of positions to move target by
     * @returns {number} new position in execution order
     */
  }, {
    key: "moveExecutable",
    value: function moveExecutable(executableTarget, delta) {
      var oldIndex = this.executableTargets.indexOf(executableTarget);
      this.executableTargets.splice(oldIndex, 1);
      var newIndex = oldIndex + delta;
      if (newIndex > this.executableTargets.length) {
        newIndex = this.executableTargets.length;
      }
      if (newIndex <= 0) {
        if (this.executableTargets.length > 0 && this.executableTargets[0].isStage) {
          newIndex = 1;
        } else {
          newIndex = 0;
        }
      }
      this.executableTargets.splice(newIndex, 0, executableTarget);
      return newIndex;
    }

    /**
     * Set a target to execute at a specific position in the execution order.
     *
     * Infinity will set the target to execute first. 0 will set the target to
     * execute last (before the stage).
     *
     * @param {Target} executableTarget target to move
     * @param {number} newIndex position in execution order to place the target
     * @returns {number} new position in the execution order
     */
  }, {
    key: "setExecutablePosition",
    value: function setExecutablePosition(executableTarget, newIndex) {
      var oldIndex = this.executableTargets.indexOf(executableTarget);
      return this.moveExecutable(executableTarget, newIndex - oldIndex);
    }

    /**
     * Remove a target from the execution set.
     * @param {Target} executableTarget target to remove
     */
  }, {
    key: "removeExecutable",
    value: function removeExecutable(executableTarget) {
      var oldIndex = this.executableTargets.indexOf(executableTarget);
      if (oldIndex > -1) {
        this.executableTargets.splice(oldIndex, 1);
      }
    }

    /**
     * Dispose of a target.
     * @param {!Target} disposingTarget Target to dispose of.
     */
  }, {
    key: "disposeTarget",
    value: function disposeTarget(disposingTarget) {
      this.targets = this.targets.filter(function (target) {
        if (disposingTarget !== target) return true;
        // Allow target to do dispose actions.
        target.dispose();
        // Remove from list of targets.
        return false;
      });
      if (this._stageTarget === disposingTarget) {
        this._stageTarget = null;
      }
    }

    /**
     * Stop any threads acting on the target.
     * @param {!Target} target Target to stop threads for.
     * @param {Thread=} optThreadException Optional thread to skip.
     */
  }, {
    key: "stopForTarget",
    value: function stopForTarget(target, optThreadException) {
      // Emit stop event to allow blocks to clean up any state.
      this.emit(Runtime.STOP_FOR_TARGET, target, optThreadException);

      // Stop any threads on the target.
      for (var i = 0; i < this.threads.length; i++) {
        if (this.threads[i] === optThreadException) {
          continue;
        }
        if (this.threads[i].target === target) {
          this._stopThread(this.threads[i]);
        }
      }
    }

    /**
     * Reset the Run ID. Call this any time the project logically starts, stops, or changes identity.
     */
  }, {
    key: "resetRunId",
    value: function resetRunId() {
      if (!this.storage) {
        // see also: attachStorage
        return;
      }
      var newRunId = uuid.v1();
      this.storage.scratchFetch.setMetadata(this.storage.scratchFetch.RequestMetadata.RunId, newRunId);
    }

    /**
     * Start all threads that start with the green flag.
     */
  }, {
    key: "greenFlag",
    value: function greenFlag() {
      this.stopAll();
      this.emit(Runtime.PROJECT_START);
      this.updateCurrentMSecs();
      this.ioDevices.clock.resetProjectTimer();
      this.targets.forEach(function (target) {
        return target.clearEdgeActivatedValues();
      });
      // Inform all targets of the green flag.
      for (var i = 0; i < this.targets.length; i++) {
        this.targets[i].onGreenFlag();
      }
      this.startHats('event_whenflagclicked');
    }

    /**
     * Stop "everything."
     */
  }, {
    key: "stopAll",
    value: function stopAll() {
      // Emit stop event to allow blocks to clean up any state.
      this.emit(Runtime.PROJECT_STOP_ALL);

      // Dispose all clones.
      var newTargets = [];
      for (var i = 0; i < this.targets.length; i++) {
        this.targets[i].onStopAll();
        if (Object.prototype.hasOwnProperty.call(this.targets[i], 'isOriginal') && !this.targets[i].isOriginal) {
          this.targets[i].dispose();
        } else {
          newTargets.push(this.targets[i]);
        }
      }
      this.targets = newTargets;
      // Dispose of the active thread.
      if (this.sequencer.activeThread !== null) {
        this._stopThread(this.sequencer.activeThread);
      }
      // Remove all remaining threads from executing in the next tick.
      this.threads = [];
      this.threadMap.clear();
      this.resetRunId();
    }
  }, {
    key: "_renderInterpolatedPositions",
    value: function _renderInterpolatedPositions() {
      var frameStarted = this._lastStepTime;
      var now = Date.now();
      var timeSinceStart = now - frameStarted;
      var progressInFrame = Math.min(1, Math.max(0, timeSinceStart / this.currentStepTime));
      interpolate.interpolate(this, progressInFrame);
      if (this.renderer) {
        this.renderer.draw();
      }
    }
  }, {
    key: "updateThreadMap",
    value: function updateThreadMap() {
      this.threadMap.clear();
      var _iterator3 = _createForOfIteratorHelper(this.threads),
        _step4;
      try {
        for (_iterator3.s(); !(_step4 = _iterator3.n()).done;) {
          var thread = _step4.value;
          if (!thread.stackClick && !thread.updateMonitor) {
            this.threadMap.set(thread.getId(), thread);
          }
        }
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
    }

    /**
     * Repeatedly run `sequencer.stepThreads` and filter out
     * inactive threads after each iteration.
     */
  }, {
    key: "_step",
    value: function _step() {
      if (this.interpolationEnabled) {
        interpolate.setupInitialState(this);
      }
      if (this.profiler !== null) {
        if (stepProfilerId === -1) {
          stepProfilerId = this.profiler.idByName('Runtime._step');
        }
        this.profiler.start(stepProfilerId);
      }

      // Clean up threads that were told to stop during or since the last step
      this.threads = this.threads.filter(function (thread) {
        return !thread.isKilled;
      });
      this.updateThreadMap();

      // Find all edge-activated hats, and add them to threads to be evaluated.
      for (var hatType in this._hats) {
        if (!Object.prototype.hasOwnProperty.call(this._hats, hatType)) continue;
        var hat = this._hats[hatType];
        if (hat.edgeActivated) {
          this.startHats(hatType);
        }
      }
      this.redrawRequested = false;
      this._pushMonitors();
      if (this.profiler !== null) {
        if (stepThreadsProfilerId === -1) {
          stepThreadsProfilerId = this.profiler.idByName('Sequencer.stepThreads');
        }
        this.profiler.start(stepThreadsProfilerId);
      }
      this.emit(Runtime.BEFORE_EXECUTE);
      var doneThreads = this.sequencer.stepThreads();
      if (this.profiler !== null) {
        this.profiler.stop();
      }
      this.emit(Runtime.AFTER_EXECUTE);
      this._updateGlows(doneThreads);
      // Add done threads so that even if a thread finishes within 1 frame, the green
      // flag will still indicate that a script ran.
      this._emitProjectRunStatus(this.threads.length + doneThreads.length - this._getMonitorThreadCount([].concat(_toConsumableArray(this.threads), _toConsumableArray(doneThreads))));
      // Store threads that completed this iteration for testing and other
      // internal purposes.
      this._lastStepDoneThreads = doneThreads;
      if (this.renderer) {
        // @todo: Only render when this.redrawRequested or clones rendered.
        if (this.profiler !== null) {
          if (rendererDrawProfilerId === -1) {
            rendererDrawProfilerId = this.profiler.idByName('RenderWebGL.draw');
          }
          this.profiler.start(rendererDrawProfilerId);
        }
        // tw: do not draw if document is hidden or a rAF loop is running
        // Checking for the animation frame loop is more reliable than using
        // interpolationEnabled in some edge cases
        if (!document.hidden && !this.frameLoop._interpolationAnimation) {
          this.renderer.draw();
        }
        if (this.profiler !== null) {
          this.profiler.stop();
        }
      }
      if (this._refreshTargets) {
        this.emit(Runtime.TARGETS_UPDATE, false /* Don't emit project changed */);
        this._refreshTargets = false;
      }
      if (this._monitorState.dirty) {
        this.emit(Runtime.MONITORS_UPDATE, this._monitorState.shallowClone());
        this._monitorState.dirty = false;
      }
      if (this.profiler !== null) {
        this.profiler.stop();
        this.profiler.reportFrames();
      }
      if (this.interpolationEnabled) {
        this._lastStepTime = Date.now();
      }
    }

    /**
     * Get the number of threads in the given array that are monitor threads (threads
     * that update monitor values, and don't count as running a script).
     * @param {!Array.<Thread>} threads The set of threads to look through.
     * @return {number} The number of monitor threads in threads.
     */
  }, {
    key: "_getMonitorThreadCount",
    value: function _getMonitorThreadCount(threads) {
      var count = 0;
      threads.forEach(function (thread) {
        if (thread.updateMonitor) count++;
      });
      return count;
    }

    /**
     * Queue monitor blocks to sequencer to be run.
     */
  }, {
    key: "_pushMonitors",
    value: function _pushMonitors() {
      this.monitorBlocks.runAllMonitored(this);
    }

    /**
     * Set the current editing target known by the runtime.
     * @param {!Target} editingTarget New editing target.
     */
  }, {
    key: "setEditingTarget",
    value: function setEditingTarget(editingTarget) {
      var oldEditingTarget = this._editingTarget;
      this._editingTarget = editingTarget;
      // Script glows must be cleared.
      this._scriptGlowsPreviousFrame = [];
      this._updateGlows();
      if (oldEditingTarget !== this._editingTarget) {
        this.requestToolboxExtensionsUpdate();
      }
    }

    /**
     * Set whether we are in 30 TPS compatibility mode.
     * @param {boolean} compatibilityModeOn True iff in compatibility mode.
     */
  }, {
    key: "setCompatibilityMode",
    value: function setCompatibilityMode(compatibilityModeOn) {
      // tw: "compatibility mode" is replaced with a generic framerate setter,
      // but this method is kept for compatibility
      if (compatibilityModeOn) {
        this.setFramerate(30);
      } else {
        this.setFramerate(60);
      }
    }

    /**
     * tw: Change runtime target frames per second
     * @param {number} framerate Target frames per second
     */
  }, {
    key: "setFramerate",
    value: function setFramerate(framerate) {
      // Setting framerate to anything greater than this is unnecessary and can break the sequencer
      // Additionally, the JS spec says intervals can't run more than once every 4ms (250/s) anyways
      if (framerate > 250) framerate = 250;
      // Convert negative framerates to 1FPS
      // Note that 0 is a special value which means "matching device screen refresh rate"
      if (framerate < 0) framerate = 1;
      this.frameLoop.setFramerate(framerate);
      this.emit(Runtime.FRAMERATE_CHANGED, framerate);
    }

    /**
     * tw: Enable or disable interpolation.
     * @param {boolean} interpolationEnabled True if interpolation should be enabled.
     */
  }, {
    key: "setInterpolation",
    value: function setInterpolation(interpolationEnabled) {
      this.interpolationEnabled = interpolationEnabled;
      this.frameLoop.setInterpolation(this.interpolationEnabled);
      this.emit(Runtime.INTERPOLATION_CHANGED, interpolationEnabled);
    }

    /**
     * tw: Update runtime options
     * @param {*} runtimeOptions New options
     */
  }, {
    key: "setRuntimeOptions",
    value: function setRuntimeOptions(runtimeOptions) {
      this.runtimeOptions = Object.assign({}, this.runtimeOptions, runtimeOptions);
      this.emit(Runtime.RUNTIME_OPTIONS_CHANGED, this.runtimeOptions);
      if (this.renderer) {
        this.renderer.offscreenTouching = !this.runtimeOptions.fencing;
      }
    }

    /**
     * tw: Update compiler options
     * @param {*} compilerOptions New options
     */
  }, {
    key: "setCompilerOptions",
    value: function setCompilerOptions(compilerOptions) {
      var nextCompilerOptions = Object.assign({}, this.compilerOptions, compilerOptions);
      if (compilerOptions && compilerOptions.offload) {
        nextCompilerOptions.offload = Object.assign({}, this.compilerOptions.offload, compilerOptions.offload);
      }
      this.compilerOptions = nextCompilerOptions;
      this.resetAllCaches();
      this.emit(Runtime.COMPILER_OPTIONS_CHANGED, this.compilerOptions);
    }

    /**
     * Change width and height of stage. This will also inform the renderer of the new stage size.
     * @param {number} width New stage width
     * @param {number} height New stage height
     */
  }, {
    key: "setStageSize",
    value: function setStageSize(width, height) {
      width = Math.round(Math.max(1, width));
      height = Math.round(Math.max(1, height));
      if (this.stageWidth !== width || this.stageHeight !== height) {
        var deltaX = width - this.stageWidth;
        var deltaY = height - this.stageHeight;
        // Preserve monitor location relative to the center of the stage
        if (this._monitorState.size > 0) {
          var offsetX = deltaX / 2;
          var offsetY = deltaY / 2;
          var _iterator4 = _createForOfIteratorHelper(this._monitorState.valueSeq()),
            _step5;
          try {
            for (_iterator4.s(); !(_step5 = _iterator4.n()).done;) {
              var monitor = _step5.value;
              this.requestUpdateMonitor({
                id: monitor.id,
                x: monitor.get('x') + offsetX,
                y: monitor.get('y') + offsetY
              });
            }
          } catch (err) {
            _iterator4.e(err);
          } finally {
            _iterator4.f();
          }
          this.emit(Runtime.MONITORS_UPDATE, this._monitorState.shallowClone());
        }
        this.stageWidth = width;
        this.stageHeight = height;
        if (this.renderer) {
          this.renderer.setStageSize(-width / 2, width / 2, -height / 2, height / 2);
        }
        this.emit(Runtime.STAGE_SIZE_CHANGED, width, height);
      }
    }

    // eslint-disable-next-line no-unused-vars
  }, {
    key: "setInEditor",
    value: function setInEditor(inEditor) {
      // no-op
    }

    /**
     * TW: Enable "packaged runtime" mode. This is a one-way operation.
     */
  }, {
    key: "convertToPackagedRuntime",
    value: function convertToPackagedRuntime() {
      if (this.storage) {
        throw new Error('convertToPackagedRuntime must be called before attachStorage');
      }
      this.isPackaged = true;
    }

    /**
     * tw: Reset the cache of all block containers.
     */
  }, {
    key: "resetAllCaches",
    value: function resetAllCaches() {
      var _iterator5 = _createForOfIteratorHelper(this.targets),
        _step6;
      try {
        for (_iterator5.s(); !(_step6 = _iterator5.n()).done;) {
          var target = _step6.value;
          if (target.isOriginal) {
            target.blocks.resetCache();
          }
        }
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }
      this.flyoutBlocks.resetCache();
      this.monitorBlocks.resetCache();
    }

    /**
     * Add an "addon block"
     * @param {object} options Options object
     * @param {string} options.procedureCode The ID of the block
     * @param {function} options.callback The callback, called with (args, BlockUtility). May return a promise.
     * @param {string[]} [options.arguments] Names of the arguments accepted. Optional if no arguments.
     * @param {boolean} [options.hidden] True to not include this block in the block palette
     * @param {1|2} [options.return] 1 for round reporter, 2 for boolean reported, leave empty for statement.
     */
  }, {
    key: "addAddonBlock",
    value: function addAddonBlock(options) {
      var procedureCode = options.procedureCode;
      var argumentNames = options.arguments || [];
      var names = argumentNames;
      var ids = argumentNames.map(function (_, i) {
        return "arg".concat(i);
      });
      var defaults = argumentNames.map(function () {
        return '';
      });
      this.addonBlocks[procedureCode] = _objectSpread({
        namesIdsDefaults: [names, ids, defaults]
      }, options);
      if (!options.hidden) {
        var ID = 'a-b';
        var blockInfo = this._blockInfo.find(function (i) {
          return i.id === ID;
        });
        if (!blockInfo) {
          // eslint-disable-next-line max-len
          var ICON = '<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48"><path d="M14.92 1.053A13.835 13.835 0 0 0 1.052 14.919v18.162a13.835 13.835 0 0 0 13.866 13.866h18.162a13.835 13.835 0 0 0 13.866-13.866V14.919A13.835 13.835 0 0 0 33.081 1.053zm16.6 12.746L41.72 24 31.52 34.201l-3.276-3.275L35.17 24l-6.926-6.926Zm-15.116.073 3.278 3.278L12.83 24l6.926 6.926L16.48 34.2 6.28 24Z" style="fill:#29beb8;fill-opacity:1;stroke:none;stroke-width:1.51371;stroke-miterlimit:4;stroke-dasharray:none;stroke-opacity:1"/></svg>';
          blockInfo = {
            id: ID,
            name: maybeFormatMessage({
              id: 'tw.blocks.addons',
              "default": 'Addons',
              description: 'Name of the addon block category in the extension list'
            }),
            color1: '#29beb8',
            color2: '#3aa8a4',
            color3: '#3aa8a4',
            menuIconURI: "data:image/svg+xml;,".concat(encodeURIComponent(ICON)),
            blocks: [],
            customFieldTypes: {},
            menus: []
          };
          this._blockInfo.unshift(blockInfo);
        }
        blockInfo.blocks.push({
          info: {},
          xml: '<block type="procedures_call" gap="16"><mutation generateshadows="true" warp="false"' + " proccode=\"".concat(xmlEscape(procedureCode), "\"") + " argumentnames=\"".concat(xmlEscape(JSON.stringify(names)), "\"") + " argumentids=\"".concat(xmlEscape(JSON.stringify(ids)), "\"") + " argumentdefaults=\"".concat(xmlEscape(JSON.stringify(defaults)), "\"") + "".concat(options["return"] ? " return=\"".concat(xmlEscape(options["return"].toString()), "\"") : '') + '></mutation></block>'
        });
      }
      this.resetAllCaches();
    }
  }, {
    key: "getAddonBlock",
    value: function getAddonBlock(procedureCode) {
      if (Object.prototype.hasOwnProperty.call(this.addonBlocks, procedureCode)) {
        return this.addonBlocks[procedureCode];
      }
      return null;
    }
  }, {
    key: "findProjectOptionsComment",
    value: function findProjectOptionsComment() {
      var target = this.getTargetForStage();
      var comments = target.comments;
      for (var _i = 0, _Object$values = Object.values(comments); _i < _Object$values.length; _i++) {
        var comment = _Object$values[_i];
        if (comment.text.includes(COMMENT_CONFIG_MAGIC)) {
          return comment;
        }
      }
      return null;
    }
  }, {
    key: "parseProjectOptions",
    value: function parseProjectOptions() {
      var comment = this.findProjectOptionsComment();
      if (!comment) return;
      var lineWithMagic = comment.text.split('\n').find(function (i) {
        return i.endsWith(COMMENT_CONFIG_MAGIC);
      });
      if (!lineWithMagic) {
        log.warn('Config comment does not contain valid line');
        return;
      }
      var jsonText = lineWithMagic.substr(0, lineWithMagic.length - COMMENT_CONFIG_MAGIC.length);
      var parsed;
      try {
        parsed = ExtendedJSON.parse(jsonText);
        if (!parsed || _typeof(parsed) !== 'object') {
          throw new Error('Invalid object');
        }
      } catch (e) {
        log.warn('Config comment has invalid JSON', e);
        return;
      }
      if (typeof parsed.framerate === 'number') {
        this.setFramerate(parsed.framerate);
      }
      if (parsed.turbo) {
        this.turboMode = true;
        this.emit(Runtime.TURBO_MODE_ON);
      }
      if (parsed.interpolation) {
        this.setInterpolation(true);
      }
      if (parsed.runtimeOptions) {
        this.setRuntimeOptions(parsed.runtimeOptions);
      }
      if (parsed.hq && this.renderer) {
        this.renderer.setUseHighQualityRender(true);
      }
      var storedWidth = +parsed.width || this.stageWidth;
      var storedHeight = +parsed.height || this.stageHeight;
      if (storedWidth !== this.stageWidth || storedHeight !== this.stageHeight) {
        this.setStageSize(storedWidth, storedHeight);
      }
    }
  }, {
    key: "_generateAllProjectOptions",
    value: function _generateAllProjectOptions() {
      return {
        framerate: this.frameLoop.framerate,
        runtimeOptions: this.runtimeOptions,
        interpolation: this.interpolationEnabled,
        turbo: this.turboMode,
        hq: this.renderer ? this.renderer.useHighQualityRender : false,
        width: this.stageWidth,
        height: this.stageHeight
      };
    }
  }, {
    key: "generateDifferingProjectOptions",
    value: function generateDifferingProjectOptions() {
      var _difference = function difference(oldObject, newObject) {
        var result = {};
        for (var _i2 = 0, _Object$keys = Object.keys(newObject); _i2 < _Object$keys.length; _i2++) {
          var key = _Object$keys[_i2];
          var newValue = newObject[key];
          var oldValue = oldObject[key];
          if (_typeof(newValue) === 'object' && newValue) {
            var valueDiffering = _difference(oldValue, newValue);
            if (Object.keys(valueDiffering).length > 0) {
              result[key] = valueDiffering;
            }
          } else if (newValue !== oldValue) {
            result[key] = newValue;
          }
        }
        return result;
      };
      return _difference(this._defaultStoredSettings, this._generateAllProjectOptions());
    }
  }, {
    key: "storeProjectOptions",
    value: function storeProjectOptions() {
      var options = this.generateDifferingProjectOptions();
      // TODO: translate
      var text = "Configuration for https://turbowarp.org/\nYou can move, resize, and minimize this comment, but don't edit it by hand. This comment can be deleted to remove the stored settings.\n".concat(ExtendedJSON.stringify(options)).concat(COMMENT_CONFIG_MAGIC);
      var existingComment = this.findProjectOptionsComment();
      if (existingComment) {
        existingComment.text = text;
      } else {
        var target = this.getTargetForStage();
        // TODO: smarter position logic
        target.createComment(uid(), null, text, 50, 50, 350, 170, false);
      }
      this.emitProjectChanged();
    }

    /**
     * Eagerly (re)compile all scripts within this project.
     */
  }, {
    key: "precompile",
    value: function precompile() {
      var _this5 = this;
      this.allScriptsDo(function (topBlockId, target) {
        var topBlock = target.blocks.getBlock(topBlockId);
        if (_this5.getIsHat(topBlock.opcode)) {
          var thread = new Thread(topBlockId);
          thread.target = target;
          thread.blockContainer = target.blocks;
          thread.tryCompile();
        }
      });
    }
  }, {
    key: "enableDebug",
    value: function enableDebug() {
      this.resetAllCaches();
      this.debug = true;
    }

    /**
     * Emit glows/glow clears for scripts after a single tick.
     * Looks at `this.threads` and notices which have turned on/off new glows.
     * @param {Array.<Thread>=} optExtraThreads Optional list of inactive threads.
     */
  }, {
    key: "_updateGlows",
    value: function _updateGlows(optExtraThreads) {
      var searchThreads = [];
      searchThreads.push.apply(searchThreads, _toConsumableArray(this.threads));
      if (optExtraThreads) {
        searchThreads.push.apply(searchThreads, _toConsumableArray(optExtraThreads));
      }
      // Set of scripts that request a glow this frame.
      var requestedGlowsThisFrame = [];
      // Final set of scripts glowing during this frame.
      var finalScriptGlows = [];
      // Find all scripts that should be glowing.
      for (var i = 0; i < searchThreads.length; i++) {
        var thread = searchThreads[i];
        var target = thread.target;
        if (target === this._editingTarget) {
          var blockForThread = thread.blockGlowInFrame;
          if (thread.requestScriptGlowInFrame || thread.stackClick) {
            var script = target.blocks.getTopLevelScript(blockForThread);
            if (!script) {
              // Attempt to find in flyout blocks.
              script = this.flyoutBlocks.getTopLevelScript(blockForThread);
            }
            if (script) {
              requestedGlowsThisFrame.push(script);
            }
          }
        }
      }
      // Compare to previous frame.
      for (var j = 0; j < this._scriptGlowsPreviousFrame.length; j++) {
        var previousFrameGlow = this._scriptGlowsPreviousFrame[j];
        if (requestedGlowsThisFrame.indexOf(previousFrameGlow) < 0) {
          // Glow turned off.
          this.glowScript(previousFrameGlow, false);
        } else {
          // Still glowing.
          finalScriptGlows.push(previousFrameGlow);
        }
      }
      for (var k = 0; k < requestedGlowsThisFrame.length; k++) {
        var currentFrameGlow = requestedGlowsThisFrame[k];
        if (this._scriptGlowsPreviousFrame.indexOf(currentFrameGlow) < 0) {
          // Glow turned on.
          this.glowScript(currentFrameGlow, true);
          finalScriptGlows.push(currentFrameGlow);
        }
      }
      this._scriptGlowsPreviousFrame = finalScriptGlows;
    }

    /**
     * Emit run start/stop after each tick. Emits when `this.threads.length` goes
     * between non-zero and zero
     *
     * @param {number} nonMonitorThreadCount The new nonMonitorThreadCount
     */
  }, {
    key: "_emitProjectRunStatus",
    value: function _emitProjectRunStatus(nonMonitorThreadCount) {
      if (this._nonMonitorThreadCount === 0 && nonMonitorThreadCount > 0) {
        this.emit(Runtime.PROJECT_RUN_START);
      }
      if (this._nonMonitorThreadCount > 0 && nonMonitorThreadCount === 0) {
        this.emit(Runtime.PROJECT_RUN_STOP);
      }
      this._nonMonitorThreadCount = nonMonitorThreadCount;
    }

    /**
     * "Quiet" a script's glow: stop the VM from generating glow/unglow events
     * about that script. Use when a script has just been deleted, but we may
     * still be tracking glow data about it.
     * @param {!string} scriptBlockId Id of top-level block in script to quiet.
     */
  }, {
    key: "quietGlow",
    value: function quietGlow(scriptBlockId) {
      var index = this._scriptGlowsPreviousFrame.indexOf(scriptBlockId);
      if (index > -1) {
        this._scriptGlowsPreviousFrame.splice(index, 1);
      }
    }

    /**
     * Emit feedback for block glowing (used in the sequencer).
     * @param {?string} blockId ID for the block to update glow
     * @param {boolean} isGlowing True to turn on glow; false to turn off.
     */
  }, {
    key: "glowBlock",
    value: function glowBlock(blockId, isGlowing) {
      if (isGlowing) {
        this.emit(Runtime.BLOCK_GLOW_ON, {
          id: blockId
        });
      } else {
        this.emit(Runtime.BLOCK_GLOW_OFF, {
          id: blockId
        });
      }
    }

    /**
     * Emit feedback for script glowing.
     * @param {?string} topBlockId ID for the top block to update glow
     * @param {boolean} isGlowing True to turn on glow; false to turn off.
     */
  }, {
    key: "glowScript",
    value: function glowScript(topBlockId, isGlowing) {
      if (isGlowing) {
        this.emit(Runtime.SCRIPT_GLOW_ON, {
          id: topBlockId
        });
      } else {
        this.emit(Runtime.SCRIPT_GLOW_OFF, {
          id: topBlockId
        });
      }
    }

    /**
     * Emit whether blocks are being dragged over gui
     * @param {boolean} areBlocksOverGui True if blocks are dragged out of blocks workspace, false otherwise
     */
  }, {
    key: "emitBlockDragUpdate",
    value: function emitBlockDragUpdate(areBlocksOverGui) {
      this.emit(Runtime.BLOCK_DRAG_UPDATE, areBlocksOverGui);
    }

    /**
     * Emit event to indicate that the block drag has ended with the blocks outside the blocks workspace
     * @param {Array.<object>} blocks The set of blocks dragged to the GUI
     * @param {string} topBlockId The original id of the top block being dragged
     */
  }, {
    key: "emitBlockEndDrag",
    value: function emitBlockEndDrag(blocks, topBlockId) {
      this.emit(Runtime.BLOCK_DRAG_END, blocks, topBlockId);
    }

    /**
     * Emit value for reporter to show in the blocks.
     * @param {Target} target The target that the block was run in.
     * @param {string} blockId ID for the block.
     * @param {string} value Value to show associated with the block.
     */
  }, {
    key: "visualReport",
    value: function visualReport(target, blockId, value) {
      if (target === this.getEditingTarget()) {
        this.emit(Runtime.VISUAL_REPORT, {
          id: blockId,
          value: safeStringify(value)
        });
      }
    }

    /**
     * Add a monitor to the state. If the monitor already exists in the state,
     * updates those properties that are defined in the given monitor record.
     * @param {import('./monitor-record.js')} monitor Monitor to add.
     */
  }, {
    key: "requestAddMonitor",
    value: function requestAddMonitor(monitor) {
      if (!this.requestUpdateMonitor(monitor)) {
        // update monitor if it exists in the state
        // if the monitor did not exist in the state, add it
        this._monitorState.set(monitor.id, monitor);
      }
    }

    /**
     * Update a monitor in the state and report success/failure of update.
     * @param {import('./monitor-record.js').ExternalDelta} delta Monitor values to update. Values on the monitor will
     *     overwrite values on the old monitor with the same ID. If a value isn't defined on the new monitor,
     *     the old monitor will keep its old value.
     * @return {boolean} true if monitor exists in the state and was updated, false if it did not exist.
     */
  }, {
    key: "requestUpdateMonitor",
    value: function requestUpdateMonitor(delta) {
      delta = MonitorRecord.externalDeltaToJS(delta);
      var id = delta.id;
      if (this._monitorState.has(id)) {
        this._monitorState.set(id, delta);
        return true;
      }
      return false;
    }

    /**
     * Removes a monitor from the state. Does nothing if the monitor already does
     * not exist in the state.
     * @param {!string} monitorId ID of the monitor to remove.
     */
  }, {
    key: "requestRemoveMonitor",
    value: function requestRemoveMonitor(monitorId) {
      this._monitorState["delete"](monitorId);
    }

    /**
     * Hides a monitor and returns success/failure of action.
     * @param {!string} monitorId ID of the monitor to hide.
     * @return {boolean} true if monitor exists and was updated, false otherwise
     */
  }, {
    key: "requestHideMonitor",
    value: function requestHideMonitor(monitorId) {
      return this.requestUpdateMonitor({
        id: monitorId,
        visible: false
      });
    }

    /**
     * Shows a monitor and returns success/failure of action.
     * not exist in the state.
     * @param {!string} monitorId ID of the monitor to show.
     * @return {boolean} true if monitor exists and was updated, false otherwise
     */
  }, {
    key: "requestShowMonitor",
    value: function requestShowMonitor(monitorId) {
      return this.requestUpdateMonitor({
        id: monitorId,
        visible: true
      });
    }

    /**
     * Removes all monitors with the given target ID from the state. Does nothing if
     * the monitor already does not exist in the state.
     * @param {!string} targetId Remove all monitors with given target ID.
     */
  }, {
    key: "requestRemoveMonitorByTargetId",
    value: function requestRemoveMonitorByTargetId(targetId) {
      this._monitorState.filter(function (value) {
        return value.targetId !== targetId;
      });
    }

    /**
     * Get a target by its id.
     * @param {string} targetId Id of target to find.
     * @return {?Target} The target, if found.
     */
  }, {
    key: "getTargetById",
    value: function getTargetById(targetId) {
      for (var i = 0; i < this.targets.length; i++) {
        var target = this.targets[i];
        if (target.id === targetId) {
          return target;
        }
      }
    }

    /**
     * Get the first original (non-clone-block-created) sprite given a name.
     * @param {string} spriteName Name of sprite to look for.
     * @return {?Target} Target representing a sprite of the given name.
     */
  }, {
    key: "getSpriteTargetByName",
    value: function getSpriteTargetByName(spriteName) {
      for (var i = 0; i < this.targets.length; i++) {
        var target = this.targets[i];
        if (target.isStage) {
          continue;
        }
        if (target.sprite && target.sprite.name === spriteName) {
          return target;
        }
      }
    }

    /**
     * Get a target by its drawable id.
     * @param {number} drawableID drawable id of target to find
     * @return {?Target} The target, if found
     */
  }, {
    key: "getTargetByDrawableId",
    value: function getTargetByDrawableId(drawableID) {
      for (var i = 0; i < this.targets.length; i++) {
        var target = this.targets[i];
        if (target.drawableID === drawableID) return target;
      }
    }

    /**
     * Update the clone counter to track how many clones are created.
     * @param {number} changeAmount How many clones have been created/destroyed.
     */
  }, {
    key: "changeCloneCounter",
    value: function changeCloneCounter(changeAmount) {
      this._cloneCounter += changeAmount;
    }

    /**
     * Return whether there are clones available.
     * @return {boolean} True until the number of clones hits runtimeOptions.maxClones
     */
  }, {
    key: "clonesAvailable",
    value: function clonesAvailable() {
      return this._cloneCounter < this.runtimeOptions.maxClones;
    }

    /**
     * Handle that the project has loaded in the Virtual Machine.
     */
  }, {
    key: "handleProjectLoaded",
    value: function handleProjectLoaded() {
      this.emit(Runtime.PROJECT_LOADED);
      this.resetRunId();
    }

    /**
     * Report that the project has changed in a way that would affect serialization
     */
  }, {
    key: "emitProjectChanged",
    value: function emitProjectChanged() {
      this.emit(Runtime.PROJECT_CHANGED);
    }

    /**
     * Report that a new target has been created, possibly by cloning an existing target.
     * @param {Target} newTarget - the newly created target.
     * @param {Target} [sourceTarget] - the target used as a source for the new clone, if any.
     * @fires Runtime#targetWasCreated
     */
  }, {
    key: "fireTargetWasCreated",
    value: function fireTargetWasCreated(newTarget, sourceTarget) {
      this.emit('targetWasCreated', newTarget, sourceTarget);
    }

    /**
     * Report that a clone target is being removed.
     * @param {Target} target - the target being removed
     * @fires Runtime#targetWasRemoved
     */
  }, {
    key: "fireTargetWasRemoved",
    value: function fireTargetWasRemoved(target) {
      this.emit('targetWasRemoved', target);
    }

    /**
     * Get a target representing the Scratch stage, if one exists.
     * @return {?Target} The target, if found.
     */
  }, {
    key: "getTargetForStage",
    value: function getTargetForStage() {
      if (this._stageTarget) {
        return this._stageTarget;
      }
      for (var i = 0; i < this.targets.length; i++) {
        var target = this.targets[i];
        if (target.isStage) {
          this._stageTarget = target;
          return target;
        }
      }
    }

    /**
     * Get the editing target.
     * @return {?Target} The editing target.
     */
  }, {
    key: "getEditingTarget",
    value: function getEditingTarget() {
      return this._editingTarget;
    }
  }, {
    key: "getAllVarNamesOfType",
    value: function getAllVarNamesOfType(varType) {
      var varNames = [];
      var _iterator6 = _createForOfIteratorHelper(this.targets),
        _step7;
      try {
        for (_iterator6.s(); !(_step7 = _iterator6.n()).done;) {
          var target = _step7.value;
          var targetVarNames = target.getAllVariableNamesInScopeByType(varType, true);
          varNames = varNames.concat(targetVarNames);
        }
      } catch (err) {
        _iterator6.e(err);
      } finally {
        _iterator6.f();
      }
      return varNames;
    }

    /**
     * Get the label or label function for an opcode
     * @param {string} extendedOpcode - the opcode you want a label for
     * @return {object} - object with label and category
     * @property {string} category - the category for this opcode
     * @property {Function} [labelFn] - function to generate the label for this opcode
     * @property {string} [label] - the label for this opcode if `labelFn` is absent
     */
  }, {
    key: "getLabelForOpcode",
    value: function getLabelForOpcode(extendedOpcode) {
      var _StringUtil$splitFirs = StringUtil.splitFirst(extendedOpcode, '_'),
        _StringUtil$splitFirs2 = _slicedToArray(_StringUtil$splitFirs, 2),
        category = _StringUtil$splitFirs2[0],
        opcode = _StringUtil$splitFirs2[1];
      if (!(category && opcode)) return;
      var categoryInfo = this._blockInfo.find(function (ci) {
        return ci.id === category;
      });
      if (!categoryInfo) return;
      var block = categoryInfo.blocks.find(function (b) {
        return b.info.opcode === opcode;
      });
      if (!block) return;

      // TODO: we may want to format the label in a locale-specific way.
      return {
        category: 'extension',
        // This assumes that all extensions have the same monitor color.
        label: "".concat(categoryInfo.name, ": ").concat(block.info.text)
      };
    }

    /**
     * Create a new global variable avoiding conflicts with other variable names.
     * @param {string} variableName The desired variable name for the new global variable.
     * This can be turned into a fresh name as necessary.
     * @param {string} optVarId An optional ID to use for the variable. A new one will be generated
     * if a falsey value for this parameter is provided.
     * @param {string} optVarType The type of the variable to create. Defaults to Variable.SCALAR_TYPE.
     * @return {Variable} The new variable that was created.
     */
  }, {
    key: "createNewGlobalVariable",
    value: function createNewGlobalVariable(variableName, optVarId, optVarType) {
      var varType = typeof optVarType === 'string' ? optVarType : Variable.SCALAR_TYPE;
      var allVariableNames = this.getAllVarNamesOfType(varType);
      var newName = StringUtil.unusedName(variableName, allVariableNames);
      var variable = new Variable(optVarId || uid(), newName, varType);
      var stage = this.getTargetForStage();
      stage.variables[variable.id] = variable;
      return variable;
    }

    /**
     * Tell the runtime to request a redraw.
     * Use after a clone/sprite has completed some visible operation on the stage.
     */
  }, {
    key: "requestRedraw",
    value: function requestRedraw() {
      this.redrawRequested = true;
    }

    /**
     * Emit a targets update at the end of the step if the provided target is
     * the original sprite
     * @param {!Target} target Target requesting the targets update
     */
  }, {
    key: "requestTargetsUpdate",
    value: function requestTargetsUpdate(target) {
      if (!target.isOriginal) return;
      this._refreshTargets = true;
    }

    /**
     * Emit an event that indicates that the blocks on the workspace need updating.
     */
  }, {
    key: "requestBlocksUpdate",
    value: function requestBlocksUpdate() {
      this.emit(Runtime.BLOCKS_NEED_UPDATE);
    }

    /**
     * Emit an event that indicates that the toolbox extension blocks need updating.
     */
  }, {
    key: "requestToolboxExtensionsUpdate",
    value: function requestToolboxExtensionsUpdate() {
      this.emit(Runtime.TOOLBOX_EXTENSIONS_NEED_UPDATE);
    }

    /**
     * Set up timers to repeatedly step in a browser.
     */
  }, {
    key: "start",
    value: function start() {
      // Do not start if we are already running
      if (this.frameLoop.running) return;
      this.frameLoop.start();
      this.emit(Runtime.RUNTIME_STARTED);
    }

    /**
     * @deprecated Used by old versions of TurboWarp. Superceded by upstream's quit()
     */
  }, {
    key: "stop",
    value: function stop() {
      this.quit();
    }

    /**
     * Quit the Runtime, clearing any handles which might keep the process alive.
     * Do not use the runtime after calling this method. This method is meant for test shutdown.
     */
  }, {
    key: "quit",
    value: function quit() {
      if (!this.frameLoop.running) {
        return;
      }
      this.frameLoop.stop();
      this.emit(Runtime.RUNTIME_STOPPED);
    }

    /**
     * Turn on profiling.
     * @param {Profiler/FrameCallback} onFrame A callback handle passed a
     * profiling frame when the profiler reports its collected data.
     */
  }, {
    key: "enableProfiling",
    value: function enableProfiling(onFrame) {
      if (Profiler.available()) {
        this.profiler = new Profiler(onFrame);
      }
    }

    /**
     * Turn off profiling.
     */
  }, {
    key: "disableProfiling",
    value: function disableProfiling() {
      this.profiler = null;
    }

    /**
     * Update a millisecond timestamp value that is saved on the Runtime.
     * This value is helpful in certain instances for compatibility with Scratch 2,
     * which sometimes uses a `currentMSecs` timestamp value in Interpreter.as
     */
  }, {
    key: "updateCurrentMSecs",
    value: function updateCurrentMSecs() {
      this.currentMSecs = Date.now();
    }
  }, {
    key: "updatePrivacy",
    value: function updatePrivacy() {
      this.privacyRestrictionsActive = this.enforcePrivacy && Object.values(this.externalCommunicationMethods).some(function (i) {
        return i;
      });
      if (this.renderer && this.renderer.setPrivateSkinAccess) {
        this.renderer.setPrivateSkinAccess(!this.privacyRestrictionsActive);
      }
    }

    /**
     * @param {boolean} enabled True if restrictions should be enforced to protect user privacy.
     */
  }, {
    key: "setEnforcePrivacy",
    value: function setEnforcePrivacy(enabled) {
      this.enforcePrivacy = enabled;
      this.updatePrivacy();
    }

    /**
     * @param {string} method Name of the method in Runtime.externalCommunicationMethods
     * @param {boolean} enabled True if the feature is enabled.
     */
  }, {
    key: "setExternalCommunicationMethod",
    value: function setExternalCommunicationMethod(method, enabled) {
      if (!Object.prototype.hasOwnProperty.call(this.externalCommunicationMethods, method)) {
        throw new Error("Unknown method: ".concat(method));
      }
      this.externalCommunicationMethods[method] = enabled;
      this.updatePrivacy();
    }
  }, {
    key: "emitAssetProgress",
    value: function emitAssetProgress() {
      this.emit(Runtime.ASSET_PROGRESS, this.finishedAssetRequests, this.totalAssetRequests);
    }
  }, {
    key: "resetProgress",
    value: function resetProgress() {
      this.finishedAssetRequests = 0;
      this.totalAssetRequests = 0;
      this.emitAssetProgress();
    }

    /**
     * Wrap an asset loading promise with progress support.
     * @template T
     * @param {() => Promise<T>} callback
     * @returns {Promise<T>}
     */
  }, {
    key: "wrapAssetRequest",
    value: function wrapAssetRequest(callback) {
      var _this6 = this;
      this.totalAssetRequests++;
      this.emitAssetProgress();
      var onSuccess = function onSuccess(result) {
        _this6.finishedAssetRequests++;
        _this6.emitAssetProgress();
        return result;
      };
      var onError = function onError(error) {
        _this6.finishedAssetRequests++;
        _this6.emitAssetProgress();
        throw error;
      };
      return callback().then(onSuccess, onError);
    }
  }], [{
    key: "STAGE_WIDTH",
    get: function get() {
      // tw: stage size is set per-runtime, this is only the initial value
      return 480;
    }

    /**
     * Height of the stage, in pixels.
     * @const {number}
     */
  }, {
    key: "STAGE_HEIGHT",
    get: function get() {
      // tw: stage size is set per-runtime, this is only the initial value
      return 360;
    }

    /**
     * Event name for glowing a script.
     * @const {string}
     */
  }, {
    key: "SCRIPT_GLOW_ON",
    get: function get() {
      return 'SCRIPT_GLOW_ON';
    }

    /**
     * Event name for unglowing a script.
     * @const {string}
     */
  }, {
    key: "SCRIPT_GLOW_OFF",
    get: function get() {
      return 'SCRIPT_GLOW_OFF';
    }

    /**
     * Event name for glowing a block.
     * @const {string}
     */
  }, {
    key: "BLOCK_GLOW_ON",
    get: function get() {
      return 'BLOCK_GLOW_ON';
    }

    /**
     * Event name for unglowing a block.
     * @const {string}
     */
  }, {
    key: "BLOCK_GLOW_OFF",
    get: function get() {
      return 'BLOCK_GLOW_OFF';
    }

    /**
     * Event name for a cloud data update
     * to this project.
     * @const {string}
     */
  }, {
    key: "HAS_CLOUD_DATA_UPDATE",
    get: function get() {
      return 'HAS_CLOUD_DATA_UPDATE';
    }

    /**
     * Event name for turning on turbo mode.
     * @const {string}
     */
  }, {
    key: "TURBO_MODE_ON",
    get: function get() {
      return 'TURBO_MODE_ON';
    }

    /**
     * Event name for turning off turbo mode.
     * @const {string}
     */
  }, {
    key: "TURBO_MODE_OFF",
    get: function get() {
      return 'TURBO_MODE_OFF';
    }

    /**
     * Event name for runtime options changing.
     * @const {string}
     */
  }, {
    key: "RUNTIME_OPTIONS_CHANGED",
    get: function get() {
      return 'RUNTIME_OPTIONS_CHANGED';
    }

    /**
     * Event name for compiler options changing.
     * @const {string}
     */
  }, {
    key: "COMPILER_OPTIONS_CHANGED",
    get: function get() {
      return 'COMPILER_OPTIONS_CHANGED';
    }

    /**
     * Event name for framerate changing.
     * @const {string}
     */
  }, {
    key: "FRAMERATE_CHANGED",
    get: function get() {
      return 'FRAMERATE_CHANGED';
    }

    /**
     * Event name for interpolation changing.
     * @const {string}
     */
  }, {
    key: "INTERPOLATION_CHANGED",
    get: function get() {
      return 'INTERPOLATION_CHANGED';
    }

    /**
     * Event name for stage size changing.
     * @const {string}
     */
  }, {
    key: "STAGE_SIZE_CHANGED",
    get: function get() {
      return 'STAGE_SIZE_CHANGED';
    }

    /**
     * Event name for compiler errors.
     * @const {string}
     */
  }, {
    key: "COMPILE_ERROR",
    get: function get() {
      return 'COMPILE_ERROR';
    }

    /**
     * Event called before any block is executed.
     */
  }, {
    key: "BEFORE_EXECUTE",
    get: function get() {
      return 'BEFORE_EXECUTE';
    }

    /**
     * Event called after every block in the project has been executed.
     */
  }, {
    key: "AFTER_EXECUTE",
    get: function get() {
      return 'AFTER_EXECUTE';
    }

    /**
     * Event name for reporting asset download progress. Fired with finished, total
     * @const {string}
     */
  }, {
    key: "ASSET_PROGRESS",
    get: function get() {
      return 'ASSET_PROGRESS';
    }

    /**
     * Event name when the project is started (threads may not necessarily be
     * running).
     * @const {string}
     */
  }, {
    key: "PROJECT_START",
    get: function get() {
      return 'PROJECT_START';
    }

    /**
     * Event name when threads start running.
     * Used by the UI to indicate running status.
     * @const {string}
     */
  }, {
    key: "PROJECT_RUN_START",
    get: function get() {
      return 'PROJECT_RUN_START';
    }

    /**
     * Event name when threads stop running
     * Used by the UI to indicate not-running status.
     * @const {string}
     */
  }, {
    key: "PROJECT_RUN_STOP",
    get: function get() {
      return 'PROJECT_RUN_STOP';
    }

    /**
     * Event name for project being stopped or restarted by the user.
     * Used by blocks that need to reset state.
     * @const {string}
     */
  }, {
    key: "PROJECT_STOP_ALL",
    get: function get() {
      return 'PROJECT_STOP_ALL';
    }

    /**
     * Event name for target being stopped by a stop for target call.
     * Used by blocks that need to stop individual targets.
     * @const {string}
     */
  }, {
    key: "STOP_FOR_TARGET",
    get: function get() {
      return 'STOP_FOR_TARGET';
    }

    /**
     * Event name for visual value report.
     * @const {string}
     */
  }, {
    key: "VISUAL_REPORT",
    get: function get() {
      return 'VISUAL_REPORT';
    }

    /**
     * Event name for project loaded report.
     * @const {string}
     */
  }, {
    key: "PROJECT_LOADED",
    get: function get() {
      return 'PROJECT_LOADED';
    }

    /**
     * Event name for report that a change was made that can be saved
     * @const {string}
     */
  }, {
    key: "PROJECT_CHANGED",
    get: function get() {
      return 'PROJECT_CHANGED';
    }

    /**
     * Event name for report that a change was made to an extension in the toolbox.
     * @const {string}
     */
  }, {
    key: "TOOLBOX_EXTENSIONS_NEED_UPDATE",
    get: function get() {
      return 'TOOLBOX_EXTENSIONS_NEED_UPDATE';
    }

    /**
     * Event name for targets update report.
     * @const {string}
     */
  }, {
    key: "TARGETS_UPDATE",
    get: function get() {
      return 'TARGETS_UPDATE';
    }

    /**
     * Event name for monitors update.
     * @const {string}
     */
  }, {
    key: "MONITORS_UPDATE",
    get: function get() {
      return 'MONITORS_UPDATE';
    }

    /**
     * Event name for block drag update.
     * @const {string}
     */
  }, {
    key: "BLOCK_DRAG_UPDATE",
    get: function get() {
      return 'BLOCK_DRAG_UPDATE';
    }

    /**
     * Event name for block drag end.
     * @const {string}
     */
  }, {
    key: "BLOCK_DRAG_END",
    get: function get() {
      return 'BLOCK_DRAG_END';
    }

    /**
     * Event name for reporting that an extension was added.
     * @const {string}
     */
  }, {
    key: "EXTENSION_ADDED",
    get: function get() {
      return 'EXTENSION_ADDED';
    }

    /**
     * Event name for reporting that an extension as asked for a custom field to be added
     * @const {string}
     */
  }, {
    key: "EXTENSION_FIELD_ADDED",
    get: function get() {
      return 'EXTENSION_FIELD_ADDED';
    }

    /**
     * Event name for updating the available set of peripheral devices.
     * This causes the peripheral connection modal to update a list of
     * available peripherals.
     * @const {string}
     */
  }, {
    key: "PERIPHERAL_LIST_UPDATE",
    get: function get() {
      return 'PERIPHERAL_LIST_UPDATE';
    }

    /**
     * Event name for when the user picks a bluetooth device to connect to
     * via Companion Device Manager (CDM)
     * @const {string}
     */
  }, {
    key: "USER_PICKED_PERIPHERAL",
    get: function get() {
      return 'USER_PICKED_PERIPHERAL';
    }

    /**
     * Event name for reporting that a peripheral has connected.
     * This causes the status button in the blocks menu to indicate 'connected'.
     * @const {string}
     */
  }, {
    key: "PERIPHERAL_CONNECTED",
    get: function get() {
      return 'PERIPHERAL_CONNECTED';
    }

    /**
     * Event name for reporting that a peripheral has been intentionally disconnected.
     * This causes the status button in the blocks menu to indicate 'disconnected'.
     * @const {string}
     */
  }, {
    key: "PERIPHERAL_DISCONNECTED",
    get: function get() {
      return 'PERIPHERAL_DISCONNECTED';
    }

    /**
     * Event name for reporting that a peripheral has encountered a request error.
     * This causes the peripheral connection modal to switch to an error state.
     * @const {string}
     */
  }, {
    key: "PERIPHERAL_REQUEST_ERROR",
    get: function get() {
      return 'PERIPHERAL_REQUEST_ERROR';
    }

    /**
     * Event name for reporting that a peripheral connection has been lost.
     * This causes a 'peripheral connection lost' error alert to display.
     * @const {string}
     */
  }, {
    key: "PERIPHERAL_CONNECTION_LOST_ERROR",
    get: function get() {
      return 'PERIPHERAL_CONNECTION_LOST_ERROR';
    }

    /**
     * Event name for reporting that a peripheral has not been discovered.
     * This causes the peripheral connection modal to show a timeout state.
     * @const {string}
     */
  }, {
    key: "PERIPHERAL_SCAN_TIMEOUT",
    get: function get() {
      return 'PERIPHERAL_SCAN_TIMEOUT';
    }

    /**
     * Event name to indicate that the microphone is being used to stream audio.
     * @const {string}
     */
  }, {
    key: "MIC_LISTENING",
    get: function get() {
      return 'MIC_LISTENING';
    }

    /**
     * Event name for reporting that blocksInfo was updated.
     * @const {string}
     */
  }, {
    key: "BLOCKSINFO_UPDATE",
    get: function get() {
      return 'BLOCKSINFO_UPDATE';
    }

    /**
     * Event name when the runtime tick loop has been started.
     * @const {string}
     */
  }, {
    key: "RUNTIME_STARTED",
    get: function get() {
      return 'RUNTIME_STARTED';
    }

    /**
     * Event name when the runtime tick loop has been stopped.
     * @const {string}
     */
  }, {
    key: "RUNTIME_STOPPED",
    get: function get() {
      return 'RUNTIME_STOPPED';
    }

    /**
     * Event name when the runtime dispose has been called.
     * @const {string}
     */
  }, {
    key: "RUNTIME_DISPOSED",
    get: function get() {
      return 'RUNTIME_DISPOSED';
    }

    /**
     * Event name for reporting that a block was updated and needs to be rerendered.
     * @const {string}
     */
  }, {
    key: "BLOCKS_NEED_UPDATE",
    get: function get() {
      return 'BLOCKS_NEED_UPDATE';
    }

    /**
     * Event name when platform name inside a project does not match the runtime.
     */
  }, {
    key: "PLATFORM_MISMATCH",
    get: function get() {
      return 'PLATFORM_MISMATCH';
    }

    /**
     * How rapidly we try to step threads by default, in ms.
     */
  }, {
    key: "THREAD_STEP_INTERVAL",
    get: function get() {
      // tw: not used, only exists for compatibility
      return 1000 / 60;
    }

    /**
     * In compatibility mode, how rapidly we try to step threads, in ms.
     */
  }, {
    key: "THREAD_STEP_INTERVAL_COMPATIBILITY",
    get: function get() {
      // tw: not used, only exists for compatibility
      return 1000 / 30;
    }

    /**
     * How many clones can be created at a time.
     * @const {number}
     */
  }, {
    key: "MAX_CLONES",
    get: function get() {
      // tw: clone limit is set per-runtime in runtimeOptions, this is only the initial value
      return 300;
    }
  }]);
}(EventEmitter);
/**
 * Event fired after a new target has been created, possibly by cloning an existing target.
 *
 * @event Runtime#targetWasCreated
 * @param {Target} newTarget - the newly created target.
 * @param {Target} [sourceTarget] - the target used as a source for the new clone, if any.
 */
module.exports = Runtime;

/***/ }),

/***/ "../vm/src/engine/scratch-blocks-constants.js":
/*!****************************************************!*\
  !*** ../vm/src/engine/scratch-blocks-constants.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * These constants are copied from scratch-blocks/core/constants.js
 * @TODO find a way to require() these straight from scratch-blocks... maybe make a scratch-blocks/dist/constants.js?
 * @readonly
 * @enum {int}
 */
var ScratchBlocksConstants = {
  /**
   * ENUM for output shape: hexagonal (booleans/predicates).
   * @const
   */
  OUTPUT_SHAPE_HEXAGONAL: 1,
  /**
   * ENUM for output shape: rounded (numbers).
   * @const
   */
  OUTPUT_SHAPE_ROUND: 2,
  /**
   * ENUM for output shape: squared (any/all values; strings).
   * @const
   */
  OUTPUT_SHAPE_SQUARE: 3
};
module.exports = ScratchBlocksConstants;

/***/ }),

/***/ "../vm/src/engine/sequencer.js":
/*!*************************************!*\
  !*** ../vm/src/engine/sequencer.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Timer = __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js");
var Thread = __webpack_require__(/*! ./thread */ "../vm/src/engine/thread.js");
var execute = __webpack_require__(/*! ./execute.js */ "../vm/src/engine/execute.js");
var compilerExecute = __webpack_require__(/*! ../compiler/jsexecute */ "../vm/src/compiler/jsexecute.js");

/**
 * Profiler frame name for stepping a single thread.
 * @const {string}
 */
var stepThreadProfilerFrame = 'Sequencer.stepThread';

/**
 * Profiler frame name for the inner loop of stepThreads.
 * @const {string}
 */
var stepThreadsInnerProfilerFrame = 'Sequencer.stepThreads#inner';

/**
 * Profiler frame name for execute.
 * @const {string}
 */
var executeProfilerFrame = 'execute';

/**
 * Profiler frame ID for stepThreadProfilerFrame.
 * @type {number}
 */
var stepThreadProfilerId = -1;

/**
 * Profiler frame ID for stepThreadsInnerProfilerFrame.
 * @type {number}
 */
var stepThreadsInnerProfilerId = -1;

/**
 * Profiler frame ID for executeProfilerFrame.
 * @type {number}
 */
var executeProfilerId = -1;
var Sequencer = /*#__PURE__*/function () {
  function Sequencer(runtime) {
    _classCallCheck(this, Sequencer);
    /**
     * A utility timer for timing thread sequencing.
     * @type {!Timer}
     */
    this.timer = new Timer();

    /**
     * Reference to the runtime owning this sequencer.
     * @type {!Runtime}
     */
    this.runtime = runtime;
    this.activeThread = null;
  }

  /**
   * Time to run a warp-mode thread, in ms.
   * @type {number}
   */
  return _createClass(Sequencer, [{
    key: "stepThreads",
    value:
    /**
     * Step through all threads in `this.runtime.threads`, running them in order.
     * @return {Array.<!Thread>} List of inactive threads after stepping.
     */
    function stepThreads() {
      // Work time is 75% of the thread stepping interval.
      var WORK_TIME = 0.75 * this.runtime.currentStepTime;
      // For compatibility with Scatch 2, update the millisecond clock
      // on the Runtime once per step (see Interpreter.as in Scratch 2
      // for original use of `currentMSecs`)
      this.runtime.updateCurrentMSecs();
      // Start counting toward WORK_TIME.
      this.timer.start();
      // Count of active threads.
      var numActiveThreads = Infinity;
      // Whether `stepThreads` has run through a full single tick.
      var ranFirstTick = false;
      var doneThreads = [];
      // Conditions for continuing to stepping threads:
      // 1. We must have threads in the list, and some must be active.
      // 2. Time elapsed must be less than WORK_TIME.
      // 3. Either turbo mode, or no redraw has been requested by a primitive.
      while (this.runtime.threads.length > 0 && numActiveThreads > 0 && this.timer.timeElapsed() < WORK_TIME && (this.runtime.turboMode || !this.runtime.redrawRequested)) {
        if (this.runtime.profiler !== null) {
          if (stepThreadsInnerProfilerId === -1) {
            stepThreadsInnerProfilerId = this.runtime.profiler.idByName(stepThreadsInnerProfilerFrame);
          }
          this.runtime.profiler.start(stepThreadsInnerProfilerId);
        }
        numActiveThreads = 0;
        var stoppedThread = false;
        // Attempt to run each thread one time.
        var threads = this.runtime.threads;
        for (var i = 0; i < threads.length; i++) {
          var activeThread = this.activeThread = threads[i];
          // Check if the thread is done so it is not executed.
          if (activeThread.stack.length === 0 || activeThread.status === Thread.STATUS_DONE) {
            // Finished with this thread.
            stoppedThread = true;
            continue;
          }
          if (activeThread.status === Thread.STATUS_YIELD_TICK && !ranFirstTick) {
            // Clear single-tick yield from the last call of `stepThreads`.
            activeThread.status = Thread.STATUS_RUNNING;
          }
          if (activeThread.status === Thread.STATUS_RUNNING || activeThread.status === Thread.STATUS_YIELD) {
            // Normal-mode thread: step.
            if (this.runtime.profiler !== null) {
              if (stepThreadProfilerId === -1) {
                stepThreadProfilerId = this.runtime.profiler.idByName(stepThreadProfilerFrame);
              }

              // Increment the number of times stepThread is called.
              this.runtime.profiler.increment(stepThreadProfilerId);
            }
            this.stepThread(activeThread);
            activeThread.warpTimer = null;
          }
          if (activeThread.status === Thread.STATUS_RUNNING) {
            numActiveThreads++;
          }
          // Check if the thread completed while it just stepped to make
          // sure we remove it before the next iteration of all threads.
          if (activeThread.stack.length === 0 || activeThread.status === Thread.STATUS_DONE) {
            // Finished with this thread.
            stoppedThread = true;
          }
        }
        // We successfully ticked once. Prevents running STATUS_YIELD_TICK
        // threads on the next tick.
        ranFirstTick = true;
        if (this.runtime.profiler !== null) {
          this.runtime.profiler.stop();
        }

        // Filter inactive threads from `this.runtime.threads`.
        if (stoppedThread) {
          var nextActiveThread = 0;
          for (var _i = 0; _i < this.runtime.threads.length; _i++) {
            var thread = this.runtime.threads[_i];
            if (thread.stack.length !== 0 && thread.status !== Thread.STATUS_DONE) {
              this.runtime.threads[nextActiveThread] = thread;
              nextActiveThread++;
            } else {
              this.runtime.threadMap["delete"](thread.getId());
              doneThreads.push(thread);
            }
          }
          this.runtime.threads.length = nextActiveThread;
        }
      }
      this.activeThread = null;
      return doneThreads;
    }

    /**
     * Step the requested thread for as long as necessary.
     * @param {!Thread} thread Thread object to step.
     */
  }, {
    key: "stepThread",
    value: function stepThread(thread) {
      if (thread.isCompiled) {
        compilerExecute(thread);
        return;
      }
      var currentBlockId = thread.peekStack();
      if (!currentBlockId) {
        // A "null block" - empty branch.
        thread.popStack();

        // Did the null follow a hat block?
        if (thread.stack.length === 0) {
          thread.status = Thread.STATUS_DONE;
          return;
        }
      }
      // Save the current block ID to notice if we did control flow.
      while (currentBlockId = thread.peekStack()) {
        var initialStackSize = thread.stack.length;
        var isWarpMode = thread.peekStackFrame().warpMode;
        if (isWarpMode && !thread.warpTimer) {
          // Initialize warp-mode timer if it hasn't been already.
          // This will start counting the thread toward `Sequencer.WARP_TIME`.
          thread.warpTimer = new Timer();
          thread.warpTimer.start();
        }
        // Execute the current block.
        if (this.runtime.profiler !== null) {
          if (executeProfilerId === -1) {
            executeProfilerId = this.runtime.profiler.idByName(executeProfilerFrame);
          }

          // Increment the number of times execute is called.
          this.runtime.profiler.increment(executeProfilerId);
        }
        if (thread.target === null) {
          this.retireThread(thread);
        } else {
          execute(this, thread);
        }
        thread.blockGlowInFrame = currentBlockId;
        // If the thread has yielded or is waiting, yield to other threads.
        if (thread.status === Thread.STATUS_YIELD) {
          // Mark as running for next iteration.
          thread.status = Thread.STATUS_RUNNING;
          // In warp mode, yielded blocks are re-executed immediately.
          if (isWarpMode && thread.warpTimer.timeElapsed() <= Sequencer.WARP_TIME) {
            continue;
          }
          return;
        } else if (thread.status === Thread.STATUS_PROMISE_WAIT) {
          // A promise was returned by the primitive. Yield the thread
          // until the promise resolves. Promise resolution should reset
          // thread.status to Thread.STATUS_RUNNING.
          return;
        } else if (thread.status === Thread.STATUS_YIELD_TICK) {
          // stepThreads will reset the thread to Thread.STATUS_RUNNING
          return;
        } else if (thread.status === Thread.STATUS_DONE) {
          // Nothing more to execute.
          return;
        }
        // If no control flow has happened, switch to next block.
        if (thread.stack.length === initialStackSize && thread.peekStack() === currentBlockId && !thread.peekStackFrame().waitingReporter) {
          thread.goToNextBlock();
        }
        // If no next block has been found at this point, look on the stack.
        while (!thread.peekStack()) {
          thread.popStack();
          if (thread.stack.length === 0) {
            // No more stack to run!
            thread.status = Thread.STATUS_DONE;
            return;
          }
          var stackFrame = thread.peekStackFrame();
          isWarpMode = stackFrame.warpMode;
          if (stackFrame.isLoop) {
            // The current level of the stack is marked as a loop.
            // Return to yield for the frame/tick in general.
            // Unless we're in warp mode - then only return if the
            // warp timer is up.
            if (!isWarpMode || thread.warpTimer.timeElapsed() > Sequencer.WARP_TIME) {
              // Don't do anything to the stack, since loops need
              // to be re-executed.
              return;
            }
            // Don't go to the next block for this level of the stack,
            // since loops need to be re-executed.
            continue;
          } else if (stackFrame.waitingReporter) {
            // This level of the stack was waiting for a value.
            // This means a reporter has just returned - so don't go
            // to the next block for this level of the stack.
            continue;
          }
          // Get next block of existing block on the stack.
          thread.goToNextBlock();
        }
      }
    }

    /**
     * Step a thread into a block's branch.
     * @param {!Thread} thread Thread object to step to branch.
     * @param {number} branchNum Which branch to step to (i.e., 1, 2).
     * @param {boolean} isLoop Whether this block is a loop.
     */
  }, {
    key: "stepToBranch",
    value: function stepToBranch(thread, branchNum, isLoop) {
      if (!branchNum) {
        branchNum = 1;
      }
      var currentBlockId = thread.peekStack();
      var branchId = thread.target.blocks.getBranch(currentBlockId, branchNum);
      thread.peekStackFrame().isLoop = isLoop;
      if (branchId) {
        // Push branch ID to the thread's stack.
        thread.pushStack(branchId);
      } else {
        thread.pushStack(null);
      }
    }

    /**
     * Step a procedure.
     * @param {!Thread} thread Thread object to step to procedure.
     * @param {!string} procedureCode Procedure code of procedure to step to.
     */
  }, {
    key: "stepToProcedure",
    value: function stepToProcedure(thread, procedureCode) {
      var definition = thread.target.blocks.getProcedureDefinition(procedureCode);
      if (!definition) {
        return;
      }
      // Check if the call is recursive.
      // If so, set the thread to yield after pushing.
      var isRecursive = thread.isRecursiveCall(procedureCode);
      // To step to a procedure, we put its definition on the stack.
      // Execution for the thread will proceed through the definition hat
      // and on to the main definition of the procedure.
      // When that set of blocks finishes executing, it will be popped
      // from the stack by the sequencer, returning control to the caller.
      thread.pushStack(definition);
      // In known warp-mode threads, only yield when time is up.
      if (thread.peekStackFrame().warpMode && thread.warpTimer.timeElapsed() > Sequencer.WARP_TIME) {
        thread.status = Thread.STATUS_YIELD;
      } else {
        // Look for warp-mode flag on definition, and set the thread
        // to warp-mode if needed.
        var definitionBlock = thread.target.blocks.getBlock(definition);
        var innerBlock = thread.target.blocks.getBlock(definitionBlock.inputs.custom_block.block);
        var doWarp = false;
        if (innerBlock && innerBlock.mutation) {
          var warp = innerBlock.mutation.warp;
          if (typeof warp === 'boolean') {
            doWarp = warp;
          } else if (typeof warp === 'string') {
            doWarp = JSON.parse(warp);
          }
        }
        if (doWarp) {
          thread.peekStackFrame().warpMode = true;
        } else if (isRecursive) {
          // In normal-mode threads, yield any time we have a recursive call.
          thread.status = Thread.STATUS_YIELD;
        }
      }
    }

    /**
     * Retire a thread in the middle, without considering further blocks.
     * @param {!Thread} thread Thread object to retire.
     */
  }, {
    key: "retireThread",
    value: function retireThread(thread) {
      thread.stack = [];
      thread.stackFrame = [];
      thread.requestScriptGlowInFrame = false;
      thread.status = Thread.STATUS_DONE;
      if (thread.isCompiled) {
        thread.procedures = null;
        thread.generator = null;
      }
    }
  }], [{
    key: "WARP_TIME",
    get: function get() {
      return 500;
    }
  }]);
}();
module.exports = Sequencer;

/***/ }),

/***/ "../vm/src/engine/stage-layering.js":
/*!******************************************!*\
  !*** ../vm/src/engine/stage-layering.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var StageLayering = /*#__PURE__*/function () {
  function StageLayering() {
    _classCallCheck(this, StageLayering);
  }
  return _createClass(StageLayering, null, [{
    key: "BACKGROUND_LAYER",
    get: function get() {
      return 'background';
    }
  }, {
    key: "VIDEO_LAYER",
    get: function get() {
      return 'video';
    }
  }, {
    key: "PEN_LAYER",
    get: function get() {
      return 'pen';
    }
  }, {
    key: "SPRITE_LAYER",
    get: function get() {
      return 'sprite';
    }

    // Order of layer groups relative to each other,
  }, {
    key: "LAYER_GROUPS",
    get: function get() {
      return [StageLayering.BACKGROUND_LAYER, StageLayering.VIDEO_LAYER, StageLayering.PEN_LAYER, StageLayering.SPRITE_LAYER];
    }
  }]);
}();
module.exports = StageLayering;

/***/ }),

/***/ "../vm/src/engine/target.js":
/*!**********************************!*\
  !*** ../vm/src/engine/target.js ***!
  \**********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var EventEmitter = __webpack_require__(/*! events */ "./node_modules/events/events.js");
var Blocks = __webpack_require__(/*! ./blocks */ "../vm/src/engine/blocks.js");
var Variable = __webpack_require__(/*! ../engine/variable */ "../vm/src/engine/variable.js");
var Comment = __webpack_require__(/*! ../engine/comment */ "../vm/src/engine/comment.js");
var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var VariableUtil = __webpack_require__(/*! ../util/variable-util */ "../vm/src/util/variable-util.js");

/**
 * @fileoverview
 * A Target is an abstract "code-running" object for the Scratch VM.
 * Examples include sprites/clones or potentially physical-world devices.
 */
var Target = /*#__PURE__*/function (_EventEmitter) {
  /**
   * @param {Runtime} runtime Reference to the runtime.
   * @param {?Blocks} blocks Blocks instance for the blocks owned by this target.
   * @constructor
   */
  function Target(runtime, blocks) {
    var _this;
    _classCallCheck(this, Target);
    _this = _callSuper(this, Target);
    if (!blocks) {
      blocks = new Blocks(runtime);
    }

    /**
     * Reference to the runtime.
     * @type {Runtime}
     */
    _this.runtime = runtime;
    /**
     * A unique ID for this target.
     * @type {string}
     */
    _this.id = uid();
    /**
     * Blocks run as code for this target.
     * @type {!Blocks}
     */
    _this.blocks = blocks;
    /**
     * Dictionary of variables and their values for this target.
     * Key is the variable id.
     * @type {Object.<string,*>}
     */
    _this.variables = {};
    /**
     * Dictionary of comments for this target.
     * Key is the comment id.
     * @type {Object.<string,*>}
     */
    _this.comments = {};
    /**
     * Dictionary of custom state for this target.
     * This can be used to store target-specific custom state for blocks which need it.
     * TODO: do we want to persist this in SB3 files?
     * @type {Object.<string,*>}
     */
    _this._customState = {};

    /**
     * Currently known values for edge-activated hats.
     * Keys are block ID for the hat; values are the currently known values.
     * @type {Object.<string, *>}
     */
    _this._edgeActivatedHatValues = {};

    /**
     * Maps extension ID to a JSON-serializable value.
     * @type {Object.<string, object>}
     */
    _this.extensionStorage = {};
    return _this;
  }

  /**
   * Called when the project receives a "green flag."
   * @abstract
   */
  _inherits(Target, _EventEmitter);
  return _createClass(Target, [{
    key: "onGreenFlag",
    value: function onGreenFlag() {}

    /**
     * Return a human-readable name for this target.
     * Target implementations should override this.
     * @abstract
     * @returns {string} Human-readable name for the target.
     */
  }, {
    key: "getName",
    value: function getName() {
      return this.id;
    }

    /**
     * Update an edge-activated hat block value.
     * @param {!string} blockId ID of hat to store value for.
     * @param {*} newValue Value to store for edge-activated hat.
     * @return {*} The old value for the edge-activated hat.
     */
  }, {
    key: "updateEdgeActivatedValue",
    value: function updateEdgeActivatedValue(blockId, newValue) {
      var oldValue = this._edgeActivatedHatValues[blockId];
      this._edgeActivatedHatValues[blockId] = newValue;
      return oldValue;
    }
  }, {
    key: "hasEdgeActivatedValue",
    value: function hasEdgeActivatedValue(blockId) {
      return Object.prototype.hasOwnProperty.call(this._edgeActivatedHatValues, blockId);
    }

    /**
     * Clear all edge-activaed hat values.
     */
  }, {
    key: "clearEdgeActivatedValues",
    value: function clearEdgeActivatedValues() {
      this._edgeActivatedHatValues = {};
    }

    /**
     * Look up a variable object, first by id, and then by name if the id is not found.
     * Create a new variable if both lookups fail.
     * @param {string} id Id of the variable.
     * @param {string} name Name of the variable.
     * @return {!Variable} Variable object.
     */
  }, {
    key: "lookupOrCreateVariable",
    value: function lookupOrCreateVariable(id, name) {
      var variable = this.lookupVariableById(id);
      if (variable) return variable;
      variable = this.lookupVariableByNameAndType(name, Variable.SCALAR_TYPE);
      if (variable) return variable;

      // No variable with this name exists - create it locally.
      var newVariable = new Variable(id, name, Variable.SCALAR_TYPE, false);
      this.variables[id] = newVariable;
      return newVariable;
    }

    /**
     * Look up a broadcast message object with the given id and return it
     * if it exists.
     * @param {string} id Id of the variable.
     * @param {string} name Name of the variable.
     * @return {?Variable} Variable object.
     */
  }, {
    key: "lookupBroadcastMsg",
    value: function lookupBroadcastMsg(id, name) {
      var broadcastMsg;
      if (id) {
        broadcastMsg = this.lookupVariableById(id);
      } else if (name) {
        broadcastMsg = this.lookupBroadcastByInputValue(name);
      } else {
        log.error('Cannot find broadcast message if neither id nor name are provided.');
      }
      if (broadcastMsg) {
        if (name && broadcastMsg.name.toLowerCase() !== name.toLowerCase()) {
          log.error("Found broadcast message with id: ".concat(id, ", but") + "its name, ".concat(broadcastMsg.name, " did not match expected name ").concat(name, "."));
        }
        if (broadcastMsg.type !== Variable.BROADCAST_MESSAGE_TYPE) {
          log.error("Found variable with id: ".concat(id, ", but its type ").concat(broadcastMsg.type) + "did not match expected type ".concat(Variable.BROADCAST_MESSAGE_TYPE));
        }
        return broadcastMsg;
      }
    }

    /**
     * Look up a broadcast message with the given name and return the variable
     * if it exists. Does not create a new broadcast message variable if
     * it doesn't exist.
     * @param {string} name Name of the variable.
     * @return {?Variable} Variable object.
     */
  }, {
    key: "lookupBroadcastByInputValue",
    value: function lookupBroadcastByInputValue(name) {
      var vars = this.variables;
      for (var propName in vars) {
        if (vars[propName].type === Variable.BROADCAST_MESSAGE_TYPE && vars[propName].name.toLowerCase() === name.toLowerCase()) {
          return vars[propName];
        }
      }
    }

    /**
     * Look up a variable object.
     * Search begins for local variables; then look for globals.
     * @param {string} id Id of the variable.
     * @param {string} name Name of the variable.
     * @return {!Variable} Variable object.
     */
  }, {
    key: "lookupVariableById",
    value: function lookupVariableById(id) {
      // If we have a local copy, return it.
      if (Object.prototype.hasOwnProperty.call(this.variables, id)) {
        return this.variables[id];
      }
      // If the stage has a global copy, return it.
      if (this.runtime && !this.isStage) {
        var stage = this.runtime.getTargetForStage();
        if (stage && Object.prototype.hasOwnProperty.call(stage.variables, id)) {
          return stage.variables[id];
        }
      }
    }

    /**
     * Look up a variable object by its name and variable type.
     * Search begins with local variables; then global variables if a local one
     * was not found.
     * @param {string} name Name of the variable.
     * @param {string} type Type of the variable. Defaults to Variable.SCALAR_TYPE.
     * @param {?bool} skipStage Optional flag to skip checking the stage
     * @return {?Variable} Variable object if found, or null if not.
     */
  }, {
    key: "lookupVariableByNameAndType",
    value: function lookupVariableByNameAndType(name, type, skipStage) {
      if (typeof name !== 'string') return;
      if (typeof type !== 'string') type = Variable.SCALAR_TYPE;
      skipStage = skipStage || false;
      for (var varId in this.variables) {
        var currVar = this.variables[varId];
        if (currVar.name === name && currVar.type === type) {
          return currVar;
        }
      }
      if (!skipStage && this.runtime && !this.isStage) {
        var stage = this.runtime.getTargetForStage();
        if (stage) {
          for (var _varId in stage.variables) {
            var _currVar = stage.variables[_varId];
            if (_currVar.name === name && _currVar.type === type) {
              return _currVar;
            }
          }
        }
      }
      return null;
    }

    /**
    * Look up a list object for this target, and create it if one doesn't exist.
    * Search begins for local lists; then look for globals.
    * @param {!string} id Id of the list.
    * @param {!string} name Name of the list.
    * @return {!Varible} Variable object representing the found/created list.
     */
  }, {
    key: "lookupOrCreateList",
    value: function lookupOrCreateList(id, name) {
      var list = this.lookupVariableById(id);
      if (list) return list;
      list = this.lookupVariableByNameAndType(name, Variable.LIST_TYPE);
      if (list) return list;

      // No variable with this name exists - create it locally.
      var newList = new Variable(id, name, Variable.LIST_TYPE, false);
      this.variables[id] = newList;
      return newList;
    }

    /**
     * Creates a variable with the given id and name and adds it to the
     * dictionary of variables.
     * @param {string} id Id of variable
     * @param {string} name Name of variable.
     * @param {string} type Type of variable, '', 'broadcast_msg', or 'list'
     * @param {boolean} isCloud Whether the variable to create has the isCloud flag set.
     * Additional checks are made that the variable can be created as a cloud variable.
     */
  }, {
    key: "createVariable",
    value: function createVariable(id, name, type, isCloud) {
      if (!Object.prototype.hasOwnProperty.call(this.variables, id)) {
        var newVariable = new Variable(id, name, type, false);
        if (isCloud && this.isStage && this.runtime.canAddCloudVariable()) {
          newVariable.isCloud = true;
          this.runtime.addCloudVariable();
          this.runtime.ioDevices.cloud.requestCreateVariable(newVariable);
        }
        this.variables[id] = newVariable;
      }
    }

    /**
     * Creates a comment with the given properties.
     * @param {string} id Id of the comment.
     * @param {string} blockId Optional id of the block the comment is attached
     * to if it is a block comment.
     * @param {string} text The text the comment contains.
     * @param {number} x The x coordinate of the comment on the workspace.
     * @param {number} y The y coordinate of the comment on the workspace.
     * @param {number} width The width of the comment when it is full size
     * @param {number} height The height of the comment when it is full size
     * @param {boolean} minimized Whether the comment is minimized.
     */
  }, {
    key: "createComment",
    value: function createComment(id, blockId, text, x, y, width, height, minimized) {
      if (!Object.prototype.hasOwnProperty.call(this.comments, id)) {
        var newComment = new Comment(id, text, x, y, width, height, minimized);
        if (blockId) {
          newComment.blockId = blockId;
          var blockWithComment = this.blocks.getBlock(blockId);
          if (blockWithComment) {
            blockWithComment.comment = id;
          } else {
            log.warn("Could not find block with id ".concat(blockId, " associated with commentId: ").concat(id));
          }
        }
        this.comments[id] = newComment;
      }
    }

    /**
     * Renames the variable with the given id to newName.
     * @param {string} id Id of variable to rename.
     * @param {string} newName New name for the variable.
     */
  }, {
    key: "renameVariable",
    value: function renameVariable(id, newName) {
      var _this2 = this;
      if (Object.prototype.hasOwnProperty.call(this.variables, id)) {
        var variable = this.variables[id];
        if (variable.id === id) {
          var oldName = variable.name;
          variable.name = newName;
          if (this.runtime) {
            if (variable.isCloud && this.isStage) {
              this.runtime.ioDevices.cloud.requestRenameVariable(oldName, newName);
            }
            if (variable.type === Variable.SCALAR_TYPE) {
              // sensing__of may be referencing to this variable.
              // Change the reference.
              var blockUpdated = false;
              this.runtime.targets.forEach(function (t) {
                blockUpdated = t.blocks.updateSensingOfReference(oldName, newName, _this2.isStage ? '_stage_' : _this2.getName()) || blockUpdated;
              });
              // Request workspace change only if sensing_of blocks were actually updated.
              if (blockUpdated) this.runtime.requestBlocksUpdate();
            }
            var blocks = this.runtime.monitorBlocks;
            blocks.changeBlock({
              id: id,
              element: 'field',
              name: variable.type === Variable.LIST_TYPE ? 'LIST' : 'VARIABLE',
              value: id
            }, this.runtime);
            var monitorBlock = blocks.getBlock(variable.id);
            if (monitorBlock) {
              this.runtime.requestUpdateMonitor({
                id: id,
                params: blocks._getBlockParams(monitorBlock)
              });
            }
          }
        }
      }
    }

    /**
     * Removes the variable with the given id from the dictionary of variables.
     * @param {string} id Id of variable to delete.
     */
  }, {
    key: "deleteVariable",
    value: function deleteVariable(id) {
      if (Object.prototype.hasOwnProperty.call(this.variables, id)) {
        // Get info about the variable before deleting it
        var deletedVariableName = this.variables[id].name;
        var deletedVariableWasCloud = this.variables[id].isCloud;
        delete this.variables[id];
        if (this.runtime) {
          if (deletedVariableWasCloud && this.isStage) {
            this.runtime.ioDevices.cloud.requestDeleteVariable(deletedVariableName);
            this.runtime.removeCloudVariable();
          }
          this.runtime.monitorBlocks.deleteBlock(id);
          this.runtime.requestRemoveMonitor(id);
        }
      }
    }

    /**
     * Remove this target's monitors from the runtime state and remove the
     * target-specific monitored blocks (e.g. local variables, global variables for the stage, x-position).
     * NOTE: This does not delete any of the stage monitors like backdrop name.
     */
  }, {
    key: "deleteMonitors",
    value: function deleteMonitors() {
      var _this3 = this;
      this.runtime.requestRemoveMonitorByTargetId(this.id);
      var targetSpecificMonitorBlockIds;
      if (this.isStage) {
        // This only deletes global variables and not other stage monitors like backdrop number.
        targetSpecificMonitorBlockIds = Object.keys(this.variables);
      } else {
        targetSpecificMonitorBlockIds = Object.keys(this.runtime.monitorBlocks._blocks).filter(function (key) {
          return _this3.runtime.monitorBlocks._blocks[key].targetId === _this3.id;
        });
      }
      var _iterator = _createForOfIteratorHelper(targetSpecificMonitorBlockIds),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var blockId = _step.value;
          this.runtime.monitorBlocks.deleteBlock(blockId);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * Create a clone of the variable with the given id from the dictionary of
     * this target's variables.
     * @param {string} id Id of variable to duplicate.
     * @param {boolean=} optKeepOriginalId Optional flag to keep the original variable ID
     * for the duplicate variable. This is necessary when cloning a sprite, for example.
     * @return {?Variable} The duplicated variable, or null if
     * the original variable was not found.
     */
  }, {
    key: "duplicateVariable",
    value: function duplicateVariable(id, optKeepOriginalId) {
      if (Object.prototype.hasOwnProperty.call(this.variables, id)) {
        var originalVariable = this.variables[id];
        var newVariable = new Variable(optKeepOriginalId ? id : null,
        // conditionally keep original id or generate a new one
        originalVariable.name, originalVariable.type, originalVariable.isCloud);
        if (newVariable.type === Variable.LIST_TYPE) {
          newVariable.value = originalVariable.value.slice(0);
        } else {
          newVariable.value = originalVariable.value;
        }
        return newVariable;
      }
      return null;
    }

    /**
     * Duplicate the dictionary of this target's variables as part of duplicating.
     * this target or making a clone.
     * @param {object=} optBlocks Optional block container for the target being duplicated.
     * If provided, new variables will be generated with new UIDs and any variable references
     * in this blocks container will be updated to refer to the corresponding new IDs.
     * @return {object} The duplicated dictionary of variables
     */
  }, {
    key: "duplicateVariables",
    value: function duplicateVariables(optBlocks) {
      var _this4 = this;
      var allVarRefs;
      if (optBlocks) {
        allVarRefs = optBlocks.getAllVariableAndListReferences();
      }
      return Object.keys(this.variables).reduce(function (accum, varId) {
        var newVariable = _this4.duplicateVariable(varId, !optBlocks);
        accum[newVariable.id] = newVariable;
        if (optBlocks && allVarRefs) {
          var currVarRefs = allVarRefs[varId];
          if (currVarRefs) {
            _this4.mergeVariables(varId, newVariable.id, currVarRefs);
          }
        }
        return accum;
      }, {});
    }

    /**
     * Post/edit sprite info.
     * @param {object} data An object with sprite info data to set.
     * @abstract
     */
  }, {
    key: "postSpriteInfo",
    value: function postSpriteInfo() {}

    /**
     * Retrieve custom state associated with this target and the provided state ID.
     * @param {string} stateId - specify which piece of state to retrieve.
     * @returns {*} the associated state, if any was found.
     */
  }, {
    key: "getCustomState",
    value: function getCustomState(stateId) {
      return this._customState[stateId];
    }

    /**
     * Store custom state associated with this target and the provided state ID.
     * @param {string} stateId - specify which piece of state to store on this target.
     * @param {*} newValue - the state value to store.
     */
  }, {
    key: "setCustomState",
    value: function setCustomState(stateId, newValue) {
      this._customState[stateId] = newValue;
    }

    /**
     * Call to destroy a target.
     * @abstract
     */
  }, {
    key: "dispose",
    value: function dispose() {
      this._customState = {};
      if (this.runtime) {
        this.runtime.removeExecutable(this);
      }
    }

    // Variable Conflict Resolution Helpers

    /**
     * Get the names of all the variables of the given type that are in scope for this target.
     * For targets that are not the stage, this includes any target-specific
     * variables as well as any stage variables unless the skipStage flag is true.
     * For the stage, this is all stage variables.
     * @param {string} type The variable type to search for; defaults to Variable.SCALAR_TYPE
     * @param {?bool} skipStage Optional flag to skip the stage.
     * @return {Array<string>} A list of variable names
     */
  }, {
    key: "getAllVariableNamesInScopeByType",
    value: function getAllVariableNamesInScopeByType(type, skipStage) {
      if (typeof type !== 'string') type = Variable.SCALAR_TYPE;
      skipStage = skipStage || false;
      var targetVariables = Object.values(this.variables).filter(function (v) {
        return v.type === type;
      }).map(function (variable) {
        return variable.name;
      });
      if (skipStage || this.isStage || !this.runtime) {
        return targetVariables;
      }
      var stage = this.runtime.getTargetForStage();
      var stageVariables = stage.getAllVariableNamesInScopeByType(type);
      return targetVariables.concat(stageVariables);
    }

    /**
     * Merge variable references with another variable.
     * @param {string} idToBeMerged ID of the variable whose references need to be updated
     * @param {string} idToMergeWith ID of the variable that the old references should be replaced with
     * @param {?Array<Object>} optReferencesToUpdate Optional context of the change.
     * Defaults to all the blocks in this target.
     * @param {?string} optNewName New variable name to merge with. The old
     * variable name in the references being updated should be replaced with this new name.
     * If this parameter is not provided or is '', no name change occurs.
     */
  }, {
    key: "mergeVariables",
    value: function mergeVariables(idToBeMerged, idToMergeWith, optReferencesToUpdate, optNewName) {
      var referencesToChange = optReferencesToUpdate ||
      // TODO should there be a separate helper function that traverses the blocks
      // for all references for a given ID instead of doing the below..?
      this.blocks.getAllVariableAndListReferences()[idToBeMerged];
      VariableUtil.updateVariableIdentifiers(referencesToChange, idToMergeWith, optNewName);
    }

    /**
     * Share a local variable (and given references for that variable) to the stage.
     * @param {string} varId The ID of the variable to share.
     * @param {Array<object>} varRefs The list of variable references being shared,
     * that reference the given variable ID. The names and IDs of these variable
     * references will be updated to refer to the new (or pre-existing) global variable.
     */
  }, {
    key: "shareLocalVariableToStage",
    value: function shareLocalVariableToStage(varId, varRefs) {
      if (!this.runtime) return;
      var variable = this.variables[varId];
      if (!variable) {
        log.warn("Cannot share a local variable to the stage if it's not local.");
        return;
      }
      var stage = this.runtime.getTargetForStage();
      // If a local var is being shared with the stage,
      // sharing will make the variable global, resulting in a conflict
      // with the existing local variable. Preemptively Resolve this conflict
      // by renaming the new global variable.

      // First check if we've already done the local to global transition for this
      // variable. If we have, merge it with the global variable we've already created.
      var varIdForStage = "StageVarFromLocal_".concat(varId);
      var stageVar = stage.lookupVariableById(varIdForStage);
      // If a global var doesn't already exist, create a new one with a fresh name.
      // Use the ID we created above so that we can lookup this new variable in the
      // future if we decide to share this same variable again.
      if (!stageVar) {
        var varName = variable.name;
        var varType = variable.type;
        var newStageName = "Stage: ".concat(varName);
        stageVar = this.runtime.createNewGlobalVariable(newStageName, varIdForStage, varType);
      }
      // Update all variable references to use the new name and ID
      this.mergeVariables(varId, stageVar.id, varRefs, stageVar.name);
    }

    /**
     * Share a local variable with a sprite, merging with one of the same name and
     * type if it already exists on the sprite, or create a new one.
     * @param {string} varId Id of the variable to share
     * @param {Target} sprite The sprite to share the variable with
     * @param {Array<object>} varRefs A list of all the variable references currently being shared.
     */
  }, {
    key: "shareLocalVariableToSprite",
    value: function shareLocalVariableToSprite(varId, sprite, varRefs) {
      if (!this.runtime) return;
      if (this.isStage) return;
      var variable = this.variables[varId];
      if (!variable) {
        log.warn("Tried to call 'shareLocalVariableToSprite' with a non-local variable.");
        return;
      }
      var varName = variable.name;
      var varType = variable.type;
      // Check if the receiving sprite already has a variable of the same name and type
      // and use the existing variable, otherwise create a new one.
      var existingLocalVar = sprite.lookupVariableByNameAndType(varName, varType);
      var newVarId;
      if (existingLocalVar) {
        newVarId = existingLocalVar.id;
      } else {
        var newVar = new Variable(null, varName, varType);
        newVarId = newVar.id;
        sprite.variables[newVarId] = newVar;
      }

      // Merge with the local variable on the new sprite.
      this.mergeVariables(varId, newVarId, varRefs);
    }

    /**
     * Given a list of variable referencing fields, shares those variables with
     * the target with the provided id, resolving any variable conflicts that arise
     * using the following rules:
     *
     * If this target is the stage, exit. There are no conflicts that arise
     * from sharing variables from the stage to another sprite. The variables
     * already exist globally, so no further action is needed.
     *
     * If a variable being referenced is a global variable, do nothing. The
     * global variable already exists so no further action is needed.
     *
     * If a variable being referenced is local, and
     * 1) The receiving target is a sprite:
     * create a new local variable or merge with an existing local variable
     * of the same name and type. Update all the referencing fields
     * for the original variable to reference the new variable.
     * 2) The receiving target is the stage:
     * Create a new global variable with a fresh name and update all the referencing
     * fields to reference the new variable.
     *
     * @param {Array<object>} blocks The blocks containing
     * potential conflicting references to variables.
     * @param {Target} receivingTarget The target receiving the variables
     */
  }, {
    key: "resolveVariableSharingConflictsWithTarget",
    value: function resolveVariableSharingConflictsWithTarget(blocks, receivingTarget) {
      if (this.isStage) return;

      // Get all the variable references in the given list of blocks
      var allVarListRefs = this.blocks.getAllVariableAndListReferences(blocks);

      // For all the variables being referenced, check for which ones are local
      // to this target, and resolve conflicts based on whether the receiving target
      // is a sprite (with a conflicting local variable) or whether it is
      // the stage (which cannot have local variables)
      for (var varId in allVarListRefs) {
        var currVar = this.variables[varId];
        if (!currVar) continue; // The current variable is global, there shouldn't be any conflicts here, skip it.

        // Get the list of references for the current variable id
        var currVarListRefs = allVarListRefs[varId];
        if (receivingTarget.isStage) {
          this.shareLocalVariableToStage(varId, currVarListRefs);
        } else {
          this.shareLocalVariableToSprite(varId, receivingTarget, currVarListRefs);
        }
      }
    }

    /**
     * Fixes up variable references in this target avoiding conflicts with
     * pre-existing variables in the same scope.
     * This is used when uploading this target as a new sprite into an existing
     * project, where the new sprite may contain references
     * to variable names that already exist as global variables in the project
     * (and thus are in scope for variable references in the given sprite).
     *
     * If this target has a block that references an existing global variable and that
     * variable *does not* exist in this target (e.g. it was a global variable in the
     * project the sprite was originally exported from), merge the variables. This entails
     * fixing the variable references in this sprite to reference the id of the pre-existing global variable.
     *
     * If this target has a block that references an existing global variable and that
     * variable does exist in the target itself (e.g. it's a local variable in the sprite being uploaded),
     * then the local variable is renamed to distinguish itself from the pre-existing variable.
     * All blocks that reference the local variable will be updated to use the new name.
     */
    // TODO (#1360) This function is too long, add some helpers for the different chunks and cases...
  }, {
    key: "fixUpVariableReferences",
    value: function fixUpVariableReferences() {
      var _this5 = this;
      if (!this.runtime) return; // There's no runtime context to conflict with
      if (this.isStage) return; // Stage can't have variable conflicts with itself (and also can't be uploaded)
      var stage = this.runtime.getTargetForStage();
      if (!stage || !stage.variables) return;
      var renameConflictingLocalVar = function renameConflictingLocalVar(id, name, type) {
        var conflict = stage.lookupVariableByNameAndType(name, type);
        if (conflict) {
          var newName = StringUtil.unusedName("".concat(_this5.getName(), ": ").concat(name), _this5.getAllVariableNamesInScopeByType(type));
          _this5.renameVariable(id, newName);
          return newName;
        }
        return null;
      };
      var allReferences = this.blocks.getAllVariableAndListReferences();
      var unreferencedLocalVarIds = [];
      if (Object.keys(this.variables).length > 0) {
        for (var localVarId in this.variables) {
          if (!Object.prototype.hasOwnProperty.call(this.variables, localVarId)) continue;
          if (!allReferences[localVarId]) unreferencedLocalVarIds.push(localVarId);
        }
      }
      var conflictIdsToReplace = Object.create(null);
      var conflictNamesToReplace = Object.create(null);

      // Cache the list of all variable names by type so that we don't need to
      // re-calculate this in every iteration of the following loop.
      var varNamesByType = {};
      var allVarNames = function allVarNames(type) {
        var namesOfType = varNamesByType[type];
        if (namesOfType) return namesOfType;
        varNamesByType[type] = _this5.runtime.getAllVarNamesOfType(type);
        return varNamesByType[type];
      };
      var _loop = function _loop() {
        // We don't care about which var ref we get, they should all have the same var info
        var varRef = allReferences[varId][0];
        var varName = varRef.referencingField.value;
        var varType = varRef.type;
        if (_this5.lookupVariableById(varId)) {
          // Found a variable with the id in either the target or the stage,
          // figure out which one.
          if (Object.prototype.hasOwnProperty.call(_this5.variables, varId)) {
            // If the target has the variable, then check whether the stage
            // has one with the same name and type. If it does, then rename
            // this target specific variable so that there is a distinction.
            var newVarName = renameConflictingLocalVar(varId, varName, varType);
            if (newVarName) {
              // We are not calling this.blocks.updateBlocksAfterVarRename
              // here because it will search through all the blocks. We already
              // have access to all the references for this var id.
              allReferences[varId].map(function (ref) {
                ref.referencingField.value = newVarName;
                return ref;
              });
            }
          }
        } else {
          // We didn't find the referenced variable id anywhere,
          // Treat it as a reference to a global variable (from the original
          // project this sprite was exported from).
          // Check for whether a global variable of the same name and type exists,
          // and if so, track it to merge with the existing global in a second pass of the blocks.
          var existingVar = stage.lookupVariableByNameAndType(varName, varType);
          if (existingVar) {
            if (!conflictIdsToReplace[varId]) {
              conflictIdsToReplace[varId] = existingVar.id;
            }
          } else {
            // A global variable with the same name did not already exist,
            // create a new one such that it does not conflict with any
            // names of local variables of the same type.
            var allNames = allVarNames(varType);
            var freshName = StringUtil.unusedName(varName, allNames);
            stage.createVariable(varId, freshName, varType);
            if (!conflictNamesToReplace[varId]) {
              conflictNamesToReplace[varId] = freshName;
            }
          }
        }
      };
      for (var varId in allReferences) {
        _loop();
      }
      // Rename any local variables that were missed above because they aren't
      // referenced by any blocks
      for (var id in unreferencedLocalVarIds) {
        var _varId2 = unreferencedLocalVarIds[id];
        var name = this.variables[_varId2].name;
        var type = this.variables[_varId2].type;
        renameConflictingLocalVar(_varId2, name, type);
      }
      // Handle global var conflicts with existing global vars (e.g. a sprite is uploaded, and has
      // blocks referencing some variable that the sprite does not own, and this
      // variable conflicts with a global var)
      // In this case, we want to merge the new variable referenes with the
      // existing global variable
      for (var conflictId in conflictIdsToReplace) {
        var existingId = conflictIdsToReplace[conflictId];
        var referencesToUpdate = allReferences[conflictId];
        this.mergeVariables(conflictId, existingId, referencesToUpdate);
      }

      // Handle global var conflicts existing local vars (e.g a sprite is uploaded,
      // and has blocks referencing some variable that the sprite does not own, and this
      // variable conflcits with another sprite's local var).
      // In this case, we want to go through the variable references and update
      // the name of the variable in that reference.
      var _loop2 = function _loop2() {
        var newName = conflictNamesToReplace[_conflictId];
        var referencesToUpdate = allReferences[_conflictId];
        referencesToUpdate.map(function (ref) {
          ref.referencingField.value = newName;
          return ref;
        });
      };
      for (var _conflictId in conflictNamesToReplace) {
        _loop2();
      }
    }
  }]);
}(EventEmitter);
module.exports = Target;

/***/ }),

/***/ "../vm/src/engine/thread.js":
/*!**********************************!*\
  !*** ../vm/src/engine/thread.js ***!
  \**********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/**
 * Recycle bin for empty stackFrame objects
 * @type Array<_StackFrame>
 */
var _stackFrameFreeList = [];

/**
 * A frame used for each level of the stack. A general purpose
 * place to store a bunch of execution context and parameters
 * @param {boolean} warpMode Whether this level of the stack is warping
 * @constructor
 * @private
 */
var _StackFrame = /*#__PURE__*/function () {
  function _StackFrame(warpMode) {
    _classCallCheck(this, _StackFrame);
    /**
     * Whether this level of the stack is a loop.
     * @type {boolean}
     */
    this.isLoop = false;

    /**
     * Whether this level is in warp mode.  Is set by some legacy blocks and
     * "turbo mode"
     * @type {boolean}
     */
    this.warpMode = warpMode;

    /**
     * Reported value from just executed block.
     * @type {Any}
     */
    this.justReported = null;

    /**
     * The active block that is waiting on a promise.
     * @type {string}
     */
    this.reporting = '';

    /**
     * Persists reported inputs during async block.
     * @type {Object}
     */
    this.reported = null;

    /**
     * Name of waiting reporter.
     * @type {string}
     */
    this.waitingReporter = null;

    /**
     * Procedure parameters.
     * @type {Object}
     */
    this.params = null;

    /**
     * A context passed to block implementations.
     * @type {Object}
     */
    this.executionContext = null;

    /**
     * Internal block object being executed. This is *not* the same as the object found
     * in target.blocks.
     * @type {object}
     */
    this.op = null;
  }

  /**
   * Reset all properties of the frame to pristine null and false states.
   * Used to recycle.
   * @return {_StackFrame} this
   */
  return _createClass(_StackFrame, [{
    key: "reset",
    value: function reset() {
      this.isLoop = false;
      this.warpMode = false;
      this.justReported = null;
      this.reported = null;
      this.waitingReporter = null;
      this.params = null;
      this.executionContext = null;
      this.op = null;
      return this;
    }

    /**
     * Reuse an active stack frame in the stack.
     * @param {?boolean} warpMode defaults to current warpMode
     * @returns {_StackFrame} this
     */
  }, {
    key: "reuse",
    value: function reuse() {
      var warpMode = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.warpMode;
      this.reset();
      this.warpMode = Boolean(warpMode);
      return this;
    }

    /**
     * Create or recycle a stack frame object.
     * @param {boolean} warpMode Enable warpMode on this frame.
     * @returns {_StackFrame} The clean stack frame with correct warpMode setting.
     */
  }], [{
    key: "create",
    value: function create(warpMode) {
      var stackFrame = _stackFrameFreeList.pop();
      if (typeof stackFrame !== 'undefined') {
        stackFrame.warpMode = Boolean(warpMode);
        return stackFrame;
      }
      return new _StackFrame(warpMode);
    }

    /**
     * Put a stack frame object into the recycle bin for reuse.
     * @param {_StackFrame} stackFrame The frame to reset and recycle.
     */
  }, {
    key: "release",
    value: function release(stackFrame) {
      if (typeof stackFrame !== 'undefined') {
        _stackFrameFreeList.push(stackFrame.reset());
      }
    }
  }]);
}();
/**
 * A thread is a running stack context and all the metadata needed.
 * @param {?string} firstBlock First block to execute in the thread.
 * @constructor
 */
var Thread = /*#__PURE__*/function () {
  function Thread(firstBlock) {
    _classCallCheck(this, Thread);
    /**
     * ID of top block of the thread
     * @type {!string}
     */
    this.topBlock = firstBlock;

    /**
     * Stack for the thread. When the sequencer enters a control structure,
     * the block is pushed onto the stack so we know where to exit.
     * @type {Array.<string>}
     */
    this.stack = [];

    /**
     * Stack frames for the thread. Store metadata for the executing blocks.
     * @type {Array.<_StackFrame>}
     */
    this.stackFrames = [];

    /**
     * Status of the thread, one of three states (below)
     * @type {number}
     */
    this.status = 0; /* Thread.STATUS_RUNNING */

    /**
     * Whether the thread is killed in the middle of execution.
     * @type {boolean}
     */
    this.isKilled = false;

    /**
     * Target of this thread.
     * @type {?Target}
     */
    this.target = null;

    /**
     * The Blocks this thread will execute.
     * @type {Blocks}
     */
    this.blockContainer = null;

    /**
     * Whether the thread requests its script to glow during this frame.
     * @type {boolean}
     */
    this.requestScriptGlowInFrame = false;

    /**
     * Which block ID should glow during this frame, if any.
     * @type {?string}
     */
    this.blockGlowInFrame = null;

    /**
     * A timer for when the thread enters warp mode.
     * Substitutes the sequencer's count toward WORK_TIME on a per-thread basis.
     * @type {?Timer}
     */
    this.warpTimer = null;
    this.justReported = null;
    this.triedToCompile = false;
    this.isCompiled = false;

    // compiler data
    // these values only make sense if isCompiled == true
    this.timer = null;
    /**
     * The thread's generator.
     * @type {Generator}
     */
    this.generator = null;
    /**
     * @type {Object.<string, import('../compiler/compile').CompiledScript>}
     */
    this.procedures = null;
    this.executableHat = false;
    this.compatibilityStackFrame = null;
  }

  /**
   * Thread status for initialized or running thread.
   * This is the default state for a thread - execution should run normally,
   * stepping from block to block.
   * @const
   */
  return _createClass(Thread, [{
    key: "getId",
    value: function getId() {
      return Thread.getIdFromTargetAndBlock(this.target, this.topBlock);
    }

    /**
     * Push stack and update stack frames appropriately.
     * @param {string} blockId Block ID to push to stack.
     */
  }, {
    key: "pushStack",
    value: function pushStack(blockId) {
      this.stack.push(blockId);
      // Push an empty stack frame, if we need one.
      // Might not, if we just popped the stack.
      if (this.stack.length > this.stackFrames.length) {
        var parent = this.stackFrames[this.stackFrames.length - 1];
        this.stackFrames.push(_StackFrame.create(typeof parent !== 'undefined' && parent.warpMode));
      }
    }

    /**
     * Reset the stack frame for use by the next block.
     * (avoids popping and re-pushing a new stack frame - keeps the warpmode the same
     * @param {string} blockId Block ID to push to stack.
     */
  }, {
    key: "reuseStackForNextBlock",
    value: function reuseStackForNextBlock(blockId) {
      this.stack[this.stack.length - 1] = blockId;
      this.stackFrames[this.stackFrames.length - 1].reuse();
    }

    /**
     * Pop last block on the stack and its stack frame.
     * @return {string} Block ID popped from the stack.
     */
  }, {
    key: "popStack",
    value: function popStack() {
      _StackFrame.release(this.stackFrames.pop());
      return this.stack.pop();
    }

    /**
     * Pop back down the stack frame until we hit a procedure call or the stack frame is emptied
     */
  }, {
    key: "stopThisScript",
    value: function stopThisScript() {
      var blockID = this.peekStack();
      while (blockID !== null) {
        var block = this.target.blocks.getBlock(blockID);

        // Reporter form of procedures_call
        if (this.peekStackFrame().waitingReporter) {
          break;
        }

        // Command form of procedures_call
        if (typeof block !== 'undefined' && block.opcode === 'procedures_call') {
          // By definition, if we get here, the procedure is done, so skip ahead so
          // the arguments won't be re-evaluated and then discarded as frozen state
          // about which arguments have been evaluated is lost.
          // This fixes https://github.com/TurboWarp/scratch-vm/issues/201
          this.goToNextBlock();
          break;
        }
        this.popStack();
        blockID = this.peekStack();
      }
      if (this.stack.length === 0) {
        // Clean up!
        this.requestScriptGlowInFrame = false;
        this.status = Thread.STATUS_DONE;
      }
    }

    /**
     * Get top stack item.
     * @return {?string} Block ID on top of stack.
     */
  }, {
    key: "peekStack",
    value: function peekStack() {
      return this.stack.length > 0 ? this.stack[this.stack.length - 1] : null;
    }

    /**
     * Get top stack frame.
     * @return {?object} Last stack frame stored on this thread.
     */
  }, {
    key: "peekStackFrame",
    value: function peekStackFrame() {
      return this.stackFrames.length > 0 ? this.stackFrames[this.stackFrames.length - 1] : null;
    }

    /**
     * Get stack frame above the current top.
     * @return {?object} Second to last stack frame stored on this thread.
     */
  }, {
    key: "peekParentStackFrame",
    value: function peekParentStackFrame() {
      return this.stackFrames.length > 1 ? this.stackFrames[this.stackFrames.length - 2] : null;
    }

    /**
     * Push a reported value to the parent of the current stack frame.
     * @param {*} value Reported value to push.
     */
  }, {
    key: "pushReportedValue",
    value: function pushReportedValue(value) {
      this.justReported = typeof value === 'undefined' ? null : value;
    }

    /**
     * Initialize procedure parameters on this stack frame.
     */
  }, {
    key: "initParams",
    value: function initParams() {
      var stackFrame = this.peekStackFrame();
      if (stackFrame.params === null) {
        stackFrame.params = {};
      }
    }

    /**
     * Add a parameter to the stack frame.
     * Use when calling a procedure with parameter values.
     * @param {!string} paramName Name of parameter.
     * @param {*} value Value to set for parameter.
     */
  }, {
    key: "pushParam",
    value: function pushParam(paramName, value) {
      var stackFrame = this.peekStackFrame();
      stackFrame.params[paramName] = value;
    }

    /**
     * Get a parameter at the lowest possible level of the stack.
     * @param {!string} paramName Name of parameter.
     * @return {*} value Value for parameter.
     */
  }, {
    key: "getParam",
    value: function getParam(paramName) {
      for (var i = this.stackFrames.length - 1; i >= 0; i--) {
        var frame = this.stackFrames[i];
        if (frame.params === null) {
          continue;
        }
        if (Object.prototype.hasOwnProperty.call(frame.params, paramName)) {
          return frame.params[paramName];
        }
        return null;
      }
      return null;
    }
  }, {
    key: "getAllparams",
    value: function getAllparams() {
      var stackFrame = this.peekStackFrame();
      return stackFrame.params;
    }

    /**
     * Whether the current execution of a thread is at the top of the stack.
     * @return {boolean} True if execution is at top of the stack.
     */
  }, {
    key: "atStackTop",
    value: function atStackTop() {
      return this.peekStack() === this.topBlock;
    }

    /**
     * Switch the thread to the next block at the current level of the stack.
     * For example, this is used in a standard sequence of blocks,
     * where execution proceeds from one block to the next.
     */
  }, {
    key: "goToNextBlock",
    value: function goToNextBlock() {
      var nextBlockId = this.target.blocks.getNextBlock(this.peekStack());
      this.reuseStackForNextBlock(nextBlockId);
    }

    /**
     * Attempt to determine whether a procedure call is recursive,
     * by examining the stack.
     * @param {!string} procedureCode Procedure code of procedure being called.
     * @return {boolean} True if the call appears recursive.
     */
  }, {
    key: "isRecursiveCall",
    value: function isRecursiveCall(procedureCode) {
      var callCount = 5; // Max number of enclosing procedure calls to examine.
      var sp = this.stackFrames.length - 1;
      for (var i = sp - 1; i >= 0; i--) {
        var block = this.target.blocks.getBlock(this.stackFrames[i].op.id) || this.target.runtime.flyoutBlocks.getBlock(this.stackFrames[i].op.id);
        if (block.opcode === 'procedures_call' && block.mutation.proccode === procedureCode) {
          return true;
        }
        if (--callCount < 0) return false;
      }
      return false;
    }

    /**
     * Attempt to compile this thread.
     */
  }, {
    key: "tryCompile",
    value: function tryCompile() {
      if (!this.blockContainer) {
        return;
      }

      // importing the compiler here avoids circular dependency issues
      var compile = __webpack_require__(/*! ../compiler/compile */ "../vm/src/compiler/compile.js");
      this.triedToCompile = true;

      // stackClick === true disables hat block generation
      // It would be great to cache these separately, but for now it's easiest to just disable them to avoid
      // cached versions of scripts breaking projects.
      var canCache = !this.stackClick;
      var topBlock = this.topBlock;
      // Flyout blocks are stored in a special block container.
      var blocks = this.blockContainer.getBlock(topBlock) ? this.blockContainer : this.target.runtime.flyoutBlocks;
      var cachedResult = canCache && blocks.getCachedCompileResult(topBlock);
      // If there is a cached error, do not attempt to recompile.
      if (cachedResult && !cachedResult.success) {
        return;
      }
      var result;
      if (cachedResult) {
        result = cachedResult.value;
      } else {
        try {
          result = compile(this);
          if (canCache) {
            blocks.cacheCompileResult(topBlock, result);
          }
        } catch (error) {
          log.error('cannot compile script', this.target.getName(), error);
          if (canCache) {
            blocks.cacheCompileError(topBlock, error);
          }
          this.target.runtime.emitCompileError(this.target, error);
          return;
        }
      }
      this.procedures = {};
      for (var _i = 0, _Object$keys = Object.keys(result.procedures); _i < _Object$keys.length; _i++) {
        var procedureCode = _Object$keys[_i];
        this.procedures[procedureCode] = result.procedures[procedureCode](this);
      }
      this.generator = result.startingFunction(this)();
      this.executableHat = result.executableHat;
      if (!this.blockContainer.forceNoGlow) {
        this.blockGlowInFrame = this.topBlock;
        this.requestScriptGlowInFrame = true;
      }
      this.isCompiled = true;
    }
  }], [{
    key: "STATUS_RUNNING",
    get: function get() {
      return 0; // used by compiler
    }

    /**
     * Threads are in this state when a primitive is waiting on a promise;
     * execution is paused until the promise changes thread status.
     * @const
     */
  }, {
    key: "STATUS_PROMISE_WAIT",
    get: function get() {
      return 1; // used by compiler
    }

    /**
     * Thread status for yield.
     * @const
     */
  }, {
    key: "STATUS_YIELD",
    get: function get() {
      return 2; // used by compiler
    }

    /**
     * Thread status for a single-tick yield. This will be cleared when the
     * thread is resumed.
     * @const
     */
  }, {
    key: "STATUS_YIELD_TICK",
    get: function get() {
      return 3; // used by compiler
    }

    /**
     * Thread status for a finished/done thread.
     * Thread is in this state when there are no more blocks to execute.
     * @const
     */
  }, {
    key: "STATUS_DONE",
    get: function get() {
      return 4; // used by compiler
    }

    /**
     * @param {Target} target The target running the thread.
     * @param {string} topBlock ID of the thread's top block.
     * @returns {string} A unique ID for this target and thread.
     */
  }, {
    key: "getIdFromTargetAndBlock",
    value: function getIdFromTargetAndBlock(target, topBlock) {
      // & should never appear in any IDs, so we can use it as a separator
      return "".concat(target.id, "&").concat(topBlock);
    }
  }]);
}(); // for extensions
Thread._StackFrame = _StackFrame;
module.exports = Thread;

/***/ }),

/***/ "../vm/src/engine/tw-font-manager.js":
/*!*******************************************!*\
  !*** ../vm/src/engine/tw-font-manager.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var EventEmitter = __webpack_require__(/*! events */ "./node_modules/events/events.js");
var AssetUtil = __webpack_require__(/*! ../util/tw-asset-util */ "../vm/src/util/tw-asset-util.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/*
 * In general in this file, note that font names in browsers are case-insensitive
 * but are whitespace-sensitive.
 */

/**
 * @typedef InternalFont
 * @property {boolean} system True if the font is built in to the system
 * @property {string} family The font's name
 * @property {string} fallback Fallback font family list
 * @property {Asset} [asset] scratch-storage asset if system: false
 */

/**
 * @param {string} font
 * @returns {string}
 */
var removeInvalidCharacters = function removeInvalidCharacters(font) {
  return font.replace(/[^-\w ]/g, '');
};

/**
 * @param {InternalFont[]} fonts Modified in-place
 * @param {InternalFont} newFont
 * @returns {InternalFont|null}
 */
var addOrUpdateFont = function addOrUpdateFont(fonts, newFont) {
  var oldFont;
  var oldIndex = fonts.findIndex(function (i) {
    return i.family.toLowerCase() === newFont.family.toLowerCase();
  });
  if (oldIndex !== -1) {
    oldFont = fonts[oldIndex];
    fonts.splice(oldIndex, 1);
  }
  fonts.push(newFont);
  return oldFont;
};
var FontManager = /*#__PURE__*/function (_EventEmitter) {
  /**
   * @param {Runtime} runtime
   */
  function FontManager(runtime) {
    var _this;
    _classCallCheck(this, FontManager);
    _this = _callSuper(this, FontManager);

    /** @type {Runtime} */
    _this.runtime = runtime;

    /** @type {Array<InternalFont>} */
    _this.fonts = [];

    /**
     * All entries should be lowercase.
     * @type {Set<string>}
     */
    _this.restrictedFonts = new Set();
    return _this;
  }

  /**
   * Prevents a family from being overridden by a custom font. The project may still use it as a system font.
   * @param {string} family
   */
  _inherits(FontManager, _EventEmitter);
  return _createClass(FontManager, [{
    key: "restrictFont",
    value: function restrictFont(family) {
      var _this2 = this;
      if (!this.isValidSystemFont(family)) {
        throw new Error('Invalid font');
      }
      this.restrictedFonts.add(family.toLowerCase());
      var oldLength = this.fonts.length;
      this.fonts = this.fonts.filter(function (font) {
        return font.system || _this2.isValidCustomFont(font.family);
      });
      if (this.fonts.length !== oldLength) {
        this.updateRenderer();
        this.changed();
      }
    }

    /**
     * @param {string} family Untrusted font name input
     * @returns {boolean} true if the family is valid for a system font
     */
  }, {
    key: "isValidSystemFont",
    value: function isValidSystemFont(family) {
      return /^[-\w ]+$/.test(family);
    }

    /**
     * @param {string} family Untrusted font name input
     * @returns {boolean} true if the family is valid for a custom font
     */
  }, {
    key: "isValidCustomFont",
    value: function isValidCustomFont(family) {
      return /^[-\w ]+$/.test(family) && !this.restrictedFonts.has(family.toLowerCase());
    }

    /**
     * @deprecated only exists for extension compatibility, use isValidSystemFont or isValidCustomFont instead
     */
  }, {
    key: "isValidFamily",
    value: function isValidFamily(family) {
      return this.isValidSystemFont(family) && this.isValidCustomFont(family);
    }

    /**
     * @param {string} family Untrusted font name input
     * @returns {string}
     */
  }, {
    key: "getUnusedSystemFont",
    value: function getUnusedSystemFont(family) {
      return StringUtil.caseInsensitiveUnusedName(removeInvalidCharacters(family), this.fonts.map(function (i) {
        return i.family;
      }));
    }

    /**
     * @param {string} family Untrusted font name input
     * @returns {string}
     */
  }, {
    key: "getUnusedCustomFont",
    value: function getUnusedCustomFont(family) {
      return StringUtil.caseInsensitiveUnusedName(removeInvalidCharacters(family), [].concat(_toConsumableArray(this.fonts.map(function (i) {
        return i.family;
      })), _toConsumableArray(this.restrictedFonts)));
    }

    /**
     * @param {string} family
     * @returns {boolean}
     */
  }, {
    key: "hasFont",
    value: function hasFont(family) {
      return !!this.fonts.find(function (i) {
        return i.family.toLowerCase() === family.toLowerCase();
      });
    }
  }, {
    key: "changed",
    value: function changed() {
      this.emit('change');
    }

    /**
     * @param {string} family
     * @param {string} fallback
     */
  }, {
    key: "addSystemFont",
    value: function addSystemFont(family, fallback) {
      if (!this.isValidSystemFont(family)) {
        throw new Error('Invalid system font family');
      }
      var oldFont = addOrUpdateFont(this.fonts, {
        system: true,
        family: family,
        fallback: fallback
      });
      if (oldFont && !oldFont.system) {
        this.updateRenderer();
      }
      this.changed();
    }

    /**
     * @param {string} family
     * @param {string} fallback
     * @param {Asset} asset scratch-storage asset
     */
  }, {
    key: "addCustomFont",
    value: function addCustomFont(family, fallback, asset) {
      if (!this.isValidCustomFont(family)) {
        throw new Error('Invalid custom font family');
      }
      addOrUpdateFont(this.fonts, {
        system: false,
        family: family,
        fallback: fallback,
        asset: asset
      });
      this.updateRenderer();
      this.changed();
    }

    /**
     * @returns {Array<{system: boolean; name: string; family: string; data: Uint8Array | null; format: string | null}>}
     */
  }, {
    key: "getFonts",
    value: function getFonts() {
      return this.fonts.map(function (font) {
        return {
          system: font.system,
          name: font.family,
          family: "\"".concat(font.family, "\", ").concat(font.fallback),
          data: font.asset ? font.asset.data : null,
          format: font.asset ? font.asset.dataFormat : null
        };
      });
    }

    /**
     * @param {number} index Corresponds to index from getFonts()
     */
  }, {
    key: "deleteFont",
    value: function deleteFont(index) {
      var _this$fonts$splice = this.fonts.splice(index, 1),
        _this$fonts$splice2 = _slicedToArray(_this$fonts$splice, 1),
        removed = _this$fonts$splice2[0];
      if (!removed.system) {
        this.updateRenderer();
      }
      this.changed();
    }
  }, {
    key: "clear",
    value: function clear() {
      var hadNonSystemFont = this.fonts.some(function (i) {
        return !i.system;
      });
      this.fonts = [];
      if (hadNonSystemFont) {
        this.updateRenderer();
      }
      this.changed();
    }
  }, {
    key: "updateRenderer",
    value: function updateRenderer() {
      if (!this.runtime.renderer || !this.runtime.renderer.setCustomFonts) {
        return;
      }
      var fontfaces = {};
      var _iterator = _createForOfIteratorHelper(this.fonts),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var font = _step.value;
          if (!font.system) {
            var uri = font.asset.encodeDataURI();
            var fontface = "@font-face { font-family: \"".concat(font.family, "\"; src: url(\"").concat(uri, "\"); }");
            var family = "\"".concat(font.family, "\", ").concat(font.fallback);
            fontfaces[family] = fontface;
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      this.runtime.renderer.setCustomFonts(fontfaces);
    }

    /**
     * Get data to save in project.json and sb3 files.
     */
  }, {
    key: "serializeJSON",
    value: function serializeJSON() {
      if (this.fonts.length === 0) {
        return null;
      }
      return this.fonts.map(function (font) {
        var serialized = {
          system: font.system,
          family: font.family,
          fallback: font.fallback
        };
        if (!font.system) {
          var asset = font.asset;
          serialized.md5ext = "".concat(asset.assetId, ".").concat(asset.dataFormat);
        }
        return serialized;
      });
    }

    /**
     * @returns {Asset[]} list of scratch-storage assets
     */
  }, {
    key: "serializeAssets",
    value: function serializeAssets() {
      return this.fonts.filter(function (i) {
        return !i.system;
      }).map(function (i) {
        return i.asset;
      });
    }

    /**
     * @param {unknown} json
     * @param {JSZip} [zip]
     * @param {boolean} [keepExisting]
     * @returns {Promise<void>}
     */
  }, {
    key: "deserialize",
    value: (function () {
      var _deserialize = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(json, zip, keepExisting) {
        var _iterator2, _step2, font, system, family, fallback, md5ext, asset, _t, _t2;
        return _regenerator().w(function (_context) {
          while (1) switch (_context.n) {
            case 0:
              if (!keepExisting) {
                this.clear();
              }
              if (Array.isArray(json)) {
                _context.n = 1;
                break;
              }
              return _context.a(2);
            case 1:
              _iterator2 = _createForOfIteratorHelper(json);
              _context.p = 2;
              _iterator2.s();
            case 3:
              if ((_step2 = _iterator2.n()).done) {
                _context.n = 12;
                break;
              }
              font = _step2.value;
              if (!(!font || _typeof(font) !== 'object')) {
                _context.n = 4;
                break;
              }
              return _context.a(3, 11);
            case 4:
              _context.p = 4;
              system = font.system;
              family = font.family;
              fallback = font.fallback;
              if (!(typeof system !== 'boolean' || typeof family !== 'string' || typeof fallback !== 'string' || this.hasFont(family))) {
                _context.n = 5;
                break;
              }
              return _context.a(3, 11);
            case 5:
              if (!system) {
                _context.n = 6;
                break;
              }
              this.addSystemFont(family, fallback);
              _context.n = 9;
              break;
            case 6:
              md5ext = font.md5ext;
              if (!(typeof md5ext !== 'string')) {
                _context.n = 7;
                break;
              }
              return _context.a(3, 11);
            case 7:
              _context.n = 8;
              return AssetUtil.getByMd5ext(this.runtime, zip, this.runtime.storage.AssetType.Font, md5ext);
            case 8:
              asset = _context.v;
              this.addCustomFont(family, fallback, asset);
            case 9:
              _context.n = 11;
              break;
            case 10:
              _context.p = 10;
              _t = _context.v;
              log.error('could not add font', _t);
            case 11:
              _context.n = 3;
              break;
            case 12:
              _context.n = 14;
              break;
            case 13:
              _context.p = 13;
              _t2 = _context.v;
              _iterator2.e(_t2);
            case 14:
              _context.p = 14;
              _iterator2.f();
              return _context.f(14);
            case 15:
              return _context.a(2);
          }
        }, _callee, this, [[4, 10], [2, 13, 14, 15]]);
      }));
      function deserialize(_x, _x2, _x3) {
        return _deserialize.apply(this, arguments);
      }
      return deserialize;
    }())
  }]);
}(EventEmitter);
module.exports = FontManager;

/***/ }),

/***/ "../vm/src/engine/tw-frame-loop.js":
/*!*****************************************!*\
  !*** ../vm/src/engine/tw-frame-loop.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// Due to the existence of features such as interpolation and "0 FPS" being treated as "screen refresh rate",
// The VM loop logic has become much more complex

// Use setTimeout to polyfill requestAnimationFrame in Node.js environments
var _requestAnimationFrame = typeof requestAnimationFrame === 'function' ? requestAnimationFrame : function (f) {
  return setTimeout(f, 1000 / 60);
};
var _cancelAnimationFrame = typeof requestAnimationFrame === 'function' ? cancelAnimationFrame : clearTimeout;
var animationFrameWrapper = function animationFrameWrapper(callback) {
  var id;
  var _handle = function handle() {
    id = _requestAnimationFrame(_handle);
    callback();
  };
  var cancel = function cancel() {
    return _cancelAnimationFrame(id);
  };
  id = _requestAnimationFrame(_handle);
  return {
    cancel: cancel
  };
};

/**
 * We've found that having an empty requestAnimationFrame loop running in the background improves frame
 * pacing in many situations. See https://github.com/TurboWarp/scratch-vm/issues/257.
 *
 * Having an extra loop running increases CPU usage and battery usage even if it's not doing anything.
 * So, we only do this when the intended framerate is high enough that the user clearly wants smooth
 * motion, and only if the user is on a platform where we have evidence that this helps:
 *  - Chrome, Edge, and other Chromium on Windows
 *
 * @param {number} framerate Intended framerate
 * @returns {boolean} true if no-op animation frame loop should be used
 */
var shouldUseNoopAnimationFrame = function shouldUseNoopAnimationFrame(framerate) {
  return framerate >= 30 && navigator.userAgent.includes('Chrome') && navigator.userAgent.includes('Windows');
};
var FrameLoop = /*#__PURE__*/function () {
  function FrameLoop(runtime) {
    _classCallCheck(this, FrameLoop);
    this.runtime = runtime;
    this.running = false;
    this.setFramerate(30);
    this.setInterpolation(false);
    this.stepCallback = this.stepCallback.bind(this);
    this.interpolationCallback = this.interpolationCallback.bind(this);
    this._stepInterval = null;
    this._interpolationAnimation = null;
    this._stepAnimation = null;
    this._noopAnimation = null;
  }
  return _createClass(FrameLoop, [{
    key: "setFramerate",
    value: function setFramerate(fps) {
      this.framerate = fps;
      this._restart();
    }
  }, {
    key: "setInterpolation",
    value: function setInterpolation(interpolation) {
      this.interpolation = interpolation;
      this._restart();
    }
  }, {
    key: "stepCallback",
    value: function stepCallback() {
      this.runtime._step();
    }
  }, {
    key: "interpolationCallback",
    value: function interpolationCallback() {
      this.runtime._renderInterpolatedPositions();
    }
  }, {
    key: "noopCallback",
    value: function noopCallback() {
      // intentional no-op, see shouldUseNoopAnimationFrame()
    }
  }, {
    key: "_restart",
    value: function _restart() {
      if (this.running) {
        this.stop();
        this.start();
      }
    }
  }, {
    key: "start",
    value: function start() {
      this.running = true;
      if (this.framerate === 0) {
        this._stepAnimation = animationFrameWrapper(this.stepCallback);
        this.runtime.currentStepTime = 1000 / 60;
      } else {
        // Interpolation should never be enabled when framerate === 0 as that's just redundant
        if (this.interpolation) {
          this._interpolationAnimation = animationFrameWrapper(this.interpolationCallback);
        } else if (shouldUseNoopAnimationFrame(this.framerate)) {
          this._noopAnimation = animationFrameWrapper(this.noopCallback);
        }
        this._stepInterval = setInterval(this.stepCallback, 1000 / this.framerate);
        this.runtime.currentStepTime = 1000 / this.framerate;
      }
    }
  }, {
    key: "stop",
    value: function stop() {
      this.running = false;
      clearInterval(this._stepInterval);
      if (this._interpolationAnimation) {
        this._interpolationAnimation.cancel();
        this._interpolationAnimation = null;
      }
      if (this._stepAnimation) {
        this._stepAnimation.cancel();
        this._stepAnimation = null;
      }
      if (this._noopAnimation) {
        this._noopAnimation.cancel();
        this._noopAnimation = null;
      }
    }
  }]);
}();
module.exports = FrameLoop;

/***/ }),

/***/ "../vm/src/engine/tw-interpolate.js":
/*!******************************************!*\
  !*** ../vm/src/engine/tw-interpolate.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
/**
 * Prepare the targets of a runtime for interpolation.
 * @param {Runtime} runtime The Runtime with targets to prepare for interpolation.
 */
var setupInitialState = function setupInitialState(runtime) {
  var renderer = runtime.renderer;
  var _iterator = _createForOfIteratorHelper(runtime.targets),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var target = _step.value;
      var directionAndScale = target._getRenderedDirectionAndScale();

      // If sprite may have been interpolated in the previous frame, reset its renderer state.
      if (renderer && target.interpolationData) {
        var drawableID = target.drawableID;
        renderer.updateDrawablePosition(drawableID, [target.x, target.y]);
        renderer.updateDrawableDirectionScale(drawableID, directionAndScale.direction, directionAndScale.scale);
        renderer.updateDrawableEffect(drawableID, 'ghost', target.effects.ghost);
      }
      if (target.visible && !target.isStage) {
        target.interpolationData = {
          x: target.x,
          y: target.y,
          direction: directionAndScale.direction,
          scale: directionAndScale.scale,
          costume: target.currentCostume,
          ghost: target.effects.ghost
        };
      } else {
        target.interpolationData = null;
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
};

/**
 * Interpolate the position of targets.
 * @param {Runtime} runtime The Runtime with targets to interpolate.
 * @param {number} time Relative time in the frame in [0-1].
 */
var interpolate = function interpolate(runtime, time) {
  var renderer = runtime.renderer;
  if (!renderer) {
    return;
  }
  var _iterator2 = _createForOfIteratorHelper(runtime.targets),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var target = _step2.value;
      // interpolationData is the initial state at the start of the frame (time 0)
      // the state on the target itself is the state at the end of the frame (time 1)
      var interpolationData = target.interpolationData;
      if (!interpolationData) {
        continue;
      }

      // Don't waste time interpolating sprites that are hidden.
      if (!target.visible) {
        continue;
      }
      var drawableID = target.drawableID;

      // Position interpolation.
      var xDistance = target.x - interpolationData.x;
      var yDistance = target.y - interpolationData.y;
      var absoluteXDistance = Math.abs(xDistance);
      var absoluteYDistance = Math.abs(yDistance);
      if (absoluteXDistance > 0.1 || absoluteYDistance > 0.1) {
        var drawable = renderer._allDrawables[drawableID];
        // Large movements are likely intended to be instantaneous.
        // getAABB is less accurate than getBounds, but it's much faster
        var bounds = drawable.getAABB();
        var tolerance = Math.min(240, Math.max(50, 1.5 * (bounds.width + bounds.height)));
        var distance = Math.sqrt(Math.pow(absoluteXDistance, 2) + Math.pow(absoluteYDistance, 2));
        if (distance < tolerance) {
          var newX = interpolationData.x + xDistance * time;
          var newY = interpolationData.y + yDistance * time;
          renderer.updateDrawablePosition(drawableID, [newX, newY]);
        }
      }

      // Effect interpolation.
      var ghostChange = target.effects.ghost - interpolationData.ghost;
      var absoluteGhostChange = Math.abs(ghostChange);
      // Large changes are likely intended to be instantaneous.
      if (absoluteGhostChange > 0 && absoluteGhostChange < 25) {
        var newGhost = target.effects.ghost + ghostChange * time;
        renderer.updateDrawableEffect(drawableID, 'ghost', newGhost);
      }

      // Interpolate scale and direction.
      var costumeUnchanged = interpolationData.costume === target.currentCostume;
      if (costumeUnchanged) {
        var _target$_getRenderedD = target._getRenderedDirectionAndScale(),
          direction = _target$_getRenderedD.direction,
          scale = _target$_getRenderedD.scale;
        var updateDrawableDirectionScale = false;

        // Interpolate direction.
        if (direction !== interpolationData.direction) {
          // Perfect 90 degree angles should not be interpolated.
          // eg. the foreground tile clones in https://scratch.mit.edu/projects/60917032/
          if (direction % 90 !== 0 || interpolationData.direction % 90 !== 0) {
            var currentRadians = direction * Math.PI / 180;
            var startingRadians = interpolationData.direction * Math.PI / 180;
            direction = Math.atan2(Math.sin(currentRadians) * time + Math.sin(startingRadians) * (1 - time), Math.cos(currentRadians) * time + Math.cos(startingRadians) * (1 - time)) * 180 / Math.PI;
            updateDrawableDirectionScale = true;
          }
        }

        // Interpolate scale.
        var startingScale = interpolationData.scale;
        if (scale[0] !== startingScale[0] || scale[1] !== startingScale[1]) {
          // Do not interpolate size when the sign of either scale differs.
          if (Math.sign(scale[0]) === Math.sign(startingScale[0]) && Math.sign(scale[1]) === Math.sign(startingScale[1])) {
            var changeX = scale[0] - startingScale[0];
            var changeY = scale[1] - startingScale[1];
            var absoluteChangeX = Math.abs(changeX);
            var absoluteChangeY = Math.abs(changeY);
            // Large changes are likely intended to be instantaneous.
            if (absoluteChangeX < 100 && absoluteChangeY < 100) {
              scale[0] = startingScale[0] + changeX * time;
              scale[1] = startingScale[1] + changeY * time;
              updateDrawableDirectionScale = true;
            }
          }
        }
        if (updateDrawableDirectionScale) {
          renderer.updateDrawableDirectionScale(drawableID, direction, scale);
        }
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
};
module.exports = {
  setupInitialState: setupInitialState,
  interpolate: interpolate
};

/***/ }),

/***/ "../vm/src/engine/tw-monitor-state.js":
/*!********************************************!*\
  !*** ../vm/src/engine/tw-monitor-state.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @typedef {import('./monitor-record')} MonitorRecord
 */

var MonitorRecord = __webpack_require__(/*! ./monitor-record */ "../vm/src/engine/monitor-record.js");
var MonitorState = /*#__PURE__*/function () {
  function MonitorState() {
    _classCallCheck(this, MonitorState);
    /**
     * @type {Map<string, MonitorRecord>}
     */
    this.map = new Map();

    /**
     * True if modified.
     * @type {boolean}
     */
    this.dirty = false;
  }

  /**
   * @param {string} id
   * @returns {MonitorRecord|null}
   */
  return _createClass(MonitorState, [{
    key: "get",
    value: function get(id) {
      return this.map.get(id);
    }

    /**
     * @param {string} id
     * @returns {boolean}
     */
  }, {
    key: "has",
    value: function has(id) {
      return this.map.has(id);
    }

    /**
     * Create or update.
     * @param {string} id
     * @param {MonitorRecord.JSDelta} delta
     */
  }, {
    key: "set",
    value: function set(id, delta) {
      if (this.map.has(id)) {
        var oldRecord = this.map.get(id);
        if (oldRecord.merge(delta)) {
          this.dirty = true;
        }
      } else {
        this.map.set(id, delta instanceof MonitorRecord ? delta : new MonitorRecord(delta));
        this.dirty = true;
      }
    }

    /**
     * @param {string} id
     */
  }, {
    key: "delete",
    value: function _delete(id) {
      if (this.map.has(id)) {
        this.map["delete"](id);
        this.dirty = true;
      }
    }

    /**
     * Removes monitors that do not satisfy callback.
     * @param {(record: MonitorRecord) => boolean} callback Returns true to keep.
     */
  }, {
    key: "filter",
    value: function filter(callback) {
      for (var _i = 0, _Array$from = Array.from(this.map.keys()); _i < _Array$from.length; _i++) {
        var id = _Array$from[_i];
        var record = this.map.get(id);
        if (!callback(record)) {
          this.map["delete"](id);
          this.dirty = true;
        }
      }
    }

    /**
     * @returns {boolean} true if no monitors
     */
  }, {
    key: "empty",
    value: function empty() {
      return this.map.size === 0;
    }

    /**
     * @returns {number}
     */
  }, {
    key: "size",
    get: function get() {
      return this.map.size;
    }

    /**
     * @returns {MonitorRecord[]}
     */
  }, {
    key: "values",
    value: function values() {
      return Array.from(this.map.values());
    }

    /**
     * For compatibility with immutable.js.
     * @returns {MonitorRecord[]}
     */
  }, {
    key: "valueSeq",
    value: function valueSeq() {
      return this.values();
    }

    /**
     * You should not perform write operations on the clone.
     * @returns {MonitorState}
     */
  }, {
    key: "shallowClone",
    value: function shallowClone() {
      var result = new MonitorState();
      result.map = this.map;
      return result;
    }
  }]);
}();
module.exports = MonitorState;

/***/ }),

/***/ "../vm/src/engine/tw-platform.js":
/*!***************************************!*\
  !*** ../vm/src/engine/tw-platform.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// Forks should change this.
// This can be accessed externally on `vm.runtime.platform`

module.exports = {
  name: 'TurboWarp',
  url: 'https://turbowarp.org/'
};

/***/ }),

/***/ "../vm/src/engine/variable.js":
/*!************************************!*\
  !*** ../vm/src/engine/variable.js ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @fileoverview
 * Object representing a Scratch variable.
 */

var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var xmlEscape = __webpack_require__(/*! ../util/xml-escape */ "../vm/src/util/xml-escape.js");
var Variable = /*#__PURE__*/function () {
  /**
   * @param {string | null} id Id of the variable.
   * @param {string} name Name of the variable.
   * @param {string} type Type of the variable, one of '' or 'list'
   * @param {boolean} isCloud Whether the variable is stored in the cloud.
   * @constructor
   */
  function Variable(id, name, type, isCloud) {
    _classCallCheck(this, Variable);
    /** @type {string} */
    this.id = id || uid();
    this.name = name;
    this.type = type;
    this.isCloud = isCloud;
    switch (this.type) {
      case Variable.SCALAR_TYPE:
        this.value = 0;
        break;
      case Variable.LIST_TYPE:
        this.value = [];
        break;
      case Variable.BROADCAST_MESSAGE_TYPE:
        this.value = this.name;
        break;
      default:
        throw new Error("Invalid variable type: ".concat(this.type));
    }
  }
  return _createClass(Variable, [{
    key: "toXML",
    value: function toXML(isLocal) {
      isLocal = isLocal === true;
      return "<variable type=\"".concat(this.type, "\" id=\"").concat(this.id, "\" islocal=\"").concat(isLocal, "\" iscloud=\"").concat(this.isCloud, "\">").concat(xmlEscape(this.name), "</variable>");
    }

    /**
     * Type representation for scalar variables.
     * This is currently represented as ''
     * for compatibility with blockly.
     * @const {string}
     */
  }], [{
    key: "SCALAR_TYPE",
    get: function get() {
      return ''; // used by compiler
    }

    /**
     * Type representation for list variables.
     * @const {string}
     */
  }, {
    key: "LIST_TYPE",
    get: function get() {
      return 'list'; // used by compiler
    }

    /**
     * Type representation for list variables.
     * @const {string}
     */
  }, {
    key: "BROADCAST_MESSAGE_TYPE",
    get: function get() {
      return 'broadcast_msg';
    }
  }]);
}();
module.exports = Variable;

/***/ }),

/***/ "../vm/src/extension-support/argument-type.js":
/*!****************************************************!*\
  !*** ../vm/src/extension-support/argument-type.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Block argument types
 * @enum {string}
 */
var ArgumentType = {
  /**
   * Numeric value with angle picker
   */
  ANGLE: 'angle',
  /**
   * Boolean value with hexagonal placeholder
   */
  BOOLEAN: 'Boolean',
  /**
   * Numeric value with color picker
   */
  COLOR: 'color',
  /**
   * Numeric value with text field
   */
  NUMBER: 'number',
  /**
   * String value with text field
   */
  STRING: 'string',
  /**
   * String value with matrix field
   */
  MATRIX: 'matrix',
  /**
   * MIDI note number with note picker (piano) field
   */
  NOTE: 'note',
  /**
   * Inline image on block (as part of the label)
   */
  IMAGE: 'image',
  /**
   * Name of costume in the current target
   */
  COSTUME: 'costume',
  /**
   * Name of sound in the current target
   */
  SOUND: 'sound'
};
module.exports = ArgumentType;

/***/ }),

/***/ "../vm/src/extension-support/block-type.js":
/*!*************************************************!*\
  !*** ../vm/src/extension-support/block-type.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Types of block
 * @enum {string}
 */
var BlockType = {
  /**
   * Boolean reporter with hexagonal shape
   */
  BOOLEAN: 'Boolean',
  /**
   * A button (not an actual block) for some special action, like making a variable
   */
  BUTTON: 'button',
  /**
   * A text label (not an actual block) for adding comments or labling blocks
   */
  LABEL: 'label',
  /**
   * Command block
   */
  COMMAND: 'command',
  /**
   * Specialized command block which may or may not run a child branch
   * The thread continues with the next block whether or not a child branch ran.
   */
  CONDITIONAL: 'conditional',
  /**
   * Specialized hat block with no implementation function
   * This stack only runs if the corresponding event is emitted by other code.
   */
  EVENT: 'event',
  /**
   * Hat block which conditionally starts a block stack
   */
  HAT: 'hat',
  /**
   * Specialized command block which may or may not run a child branch
   * If a child branch runs, the thread evaluates the loop block again.
   */
  LOOP: 'loop',
  /**
   * General reporter with numeric or string value
   */
  REPORTER: 'reporter',
  /**
   * Arbitrary scratch-blocks XML.
   */
  XML: 'xml'
};
module.exports = BlockType;

/***/ }),

/***/ "../vm/src/extension-support/extension-manager.js":
/*!********************************************************!*\
  !*** ../vm/src/extension-support/extension-manager.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var dispatch = __webpack_require__(/*! ../dispatch/central-dispatch */ "../vm/src/dispatch/central-dispatch.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var maybeFormatMessage = __webpack_require__(/*! ../util/maybe-format-message */ "../vm/src/util/maybe-format-message.js");
var BlockType = __webpack_require__(/*! ./block-type */ "../vm/src/extension-support/block-type.js");
var SecurityManager = __webpack_require__(/*! ./tw-security-manager */ "../vm/src/extension-support/tw-security-manager.js");

// These extensions are currently built into the VM repository but should not be loaded at startup.
// TODO: move these out into a separate repository?
// TODO: change extension spec so that library info, including extension ID, can be collected through static methods

var defaultBuiltinExtensions = {
  // This is an example that isn't loaded with the other core blocks,
  // but serves as a reference for loading core blocks as extensions.
  coreExample: function coreExample() {
    return __webpack_require__(/*! ../blocks/scratch3_core_example */ "../vm/src/blocks/scratch3_core_example.js");
  },
  // These are the non-core built-in extensions.
  pen: function pen() {
    return __webpack_require__(/*! ../extensions/scratch3_pen */ "../vm/src/extensions/scratch3_pen/index.js");
  },
  wedo2: function wedo2() {
    return __webpack_require__(/*! ../extensions/scratch3_wedo2 */ "../vm/src/extensions/scratch3_wedo2/index.js");
  },
  music: function music() {
    return __webpack_require__(/*! ../extensions/scratch3_music */ "../vm/src/extensions/scratch3_music/index.js");
  },
  microbit: function microbit() {
    return __webpack_require__(/*! ../extensions/scratch3_microbit */ "../vm/src/extensions/scratch3_microbit/index.js");
  },
  text2speech: function text2speech() {
    return __webpack_require__(/*! ../extensions/scratch3_text2speech */ "../vm/src/extensions/scratch3_text2speech/index.js");
  },
  translate: function translate() {
    return __webpack_require__(/*! ../extensions/scratch3_translate */ "../vm/src/extensions/scratch3_translate/index.js");
  },
  videoSensing: function videoSensing() {
    return __webpack_require__(/*! ../extensions/scratch3_video_sensing */ "../vm/src/extensions/scratch3_video_sensing/index.js");
  },
  ev3: function ev3() {
    return __webpack_require__(/*! ../extensions/scratch3_ev3 */ "../vm/src/extensions/scratch3_ev3/index.js");
  },
  makeymakey: function makeymakey() {
    return __webpack_require__(/*! ../extensions/scratch3_makeymakey */ "../vm/src/extensions/scratch3_makeymakey/index.js");
  },
  boost: function boost() {
    return __webpack_require__(/*! ../extensions/scratch3_boost */ "../vm/src/extensions/scratch3_boost/index.js");
  },
  gdxfor: function gdxfor() {
    return __webpack_require__(/*! ../extensions/scratch3_gdx_for */ "../vm/src/extensions/scratch3_gdx_for/index.js");
  },
  // tw: core extension
  tw: function tw() {
    return __webpack_require__(/*! ../extensions/tw */ "../vm/src/extensions/tw/index.js");
  }
};

/**
 * @typedef {object} ArgumentInfo - Information about an extension block argument
 * @property {ArgumentType} type - the type of value this argument can take
 * @property {*|undefined} default - the default value of this argument (default: blank)
 */

/**
 * @typedef {object} ConvertedBlockInfo - Raw extension block data paired with processed data ready for scratch-blocks
 * @property {ExtensionBlockMetadata} info - the raw block info
 * @property {object} json - the scratch-blocks JSON definition for this block
 * @property {string} xml - the scratch-blocks XML definition for this block
 */

/**
 * @typedef {object} CategoryInfo - Information about a block category
 * @property {string} id - the unique ID of this category
 * @property {string} name - the human-readable name of this category
 * @property {string|undefined} blockIconURI - optional URI for the block icon image
 * @property {string} color1 - the primary color for this category, in '#rrggbb' format
 * @property {string} color2 - the secondary color for this category, in '#rrggbb' format
 * @property {string} color3 - the tertiary color for this category, in '#rrggbb' format
 * @property {Array.<ConvertedBlockInfo>} blocks - the blocks, separators, etc. in this category
 * @property {Array.<object>} menus - the menus provided by this category
 */

/**
 * @typedef {object} PendingExtensionWorker - Information about an extension worker still initializing
 * @property {string} extensionURL - the URL of the extension to be loaded by this worker
 * @property {Function} resolve - function to call on successful worker startup
 * @property {Function} reject - function to call on failed worker startup
 */

var createExtensionService = function createExtensionService(extensionManager) {
  var service = {};
  service.registerExtensionServiceSync = extensionManager.registerExtensionServiceSync.bind(extensionManager);
  service.allocateWorker = extensionManager.allocateWorker.bind(extensionManager);
  service.onWorkerInit = extensionManager.onWorkerInit.bind(extensionManager);
  service.registerExtensionService = extensionManager.registerExtensionService.bind(extensionManager);
  return service;
};
var ExtensionManager = /*#__PURE__*/function () {
  function ExtensionManager(vm) {
    _classCallCheck(this, ExtensionManager);
    /**
     * The ID number to provide to the next extension worker.
     * @type {int}
     */
    this.nextExtensionWorker = 0;

    /**
     * FIFO queue of extensions which have been requested but not yet loaded in a worker,
     * along with promise resolution functions to call once the worker is ready or failed.
     *
     * @type {Array.<PendingExtensionWorker>}
     */
    this.pendingExtensions = [];

    /**
     * Map of worker ID to workers which have been allocated but have not yet finished initialization.
     * @type {Array.<PendingExtensionWorker>}
     */
    this.pendingWorkers = [];

    /**
     * Map of worker ID to the URL where it was loaded from.
     * @type {Array<string>}
     */
    this.workerURLs = [];

    /**
     * Map of loaded extension URLs/IDs (equivalent for built-in extensions) to service name.
     * @type {Map.<string,string>}
     * @private
     */
    this._loadedExtensions = new Map();

    /**
     * Responsible for determining security policies related to custom extensions.
     */
    this.securityManager = new SecurityManager();

    /**
     * @type {VirtualMachine}
     */
    this.vm = vm;

    /**
     * Keep a reference to the runtime so we can construct internal extension objects.
     * TODO: remove this in favor of extensions accessing the runtime as a service.
     * @type {Runtime}
     */
    this.runtime = vm.runtime;
    this.loadingAsyncExtensions = 0;
    this.asyncExtensionsLoadedCallbacks = [];
    this.builtinExtensions = Object.assign({}, defaultBuiltinExtensions);
    dispatch.setService('extensions', createExtensionService(this))["catch"](function (e) {
      log.error("ExtensionManager was unable to register extension service: ".concat(JSON.stringify(e)));
    });
  }

  /**
   * Check whether an extension is registered or is in the process of loading. This is intended to control loading or
   * adding extensions so it may return `true` before the extension is ready to be used. Use the promise returned by
   * `loadExtensionURL` if you need to wait until the extension is truly ready.
   * @param {string} extensionID - the ID of the extension.
   * @returns {boolean} - true if loaded, false otherwise.
   */
  return _createClass(ExtensionManager, [{
    key: "isExtensionLoaded",
    value: function isExtensionLoaded(extensionID) {
      return this._loadedExtensions.has(extensionID);
    }

    /**
     * Determine whether an extension with a given ID is built in to the VM, such as pen.
     * Note that "core extensions" like motion will return false here.
     * @param {string} extensionId
     * @returns {boolean}
     */
  }, {
    key: "isBuiltinExtension",
    value: function isBuiltinExtension(extensionId) {
      return Object.prototype.hasOwnProperty.call(this.builtinExtensions, extensionId);
    }

    /**
     * Synchronously load an internal extension (core or non-core) by ID. This call will
     * fail if the provided id is not does not match an internal extension.
     * @param {string} extensionId - the ID of an internal extension
     */
  }, {
    key: "loadExtensionIdSync",
    value: function loadExtensionIdSync(extensionId) {
      if (!this.isBuiltinExtension(extensionId)) {
        log.warn("Could not find extension ".concat(extensionId, " in the built in extensions."));
        return;
      }

      /** @TODO dupe handling for non-builtin extensions. See commit 670e51d33580e8a2e852b3b038bb3afc282f81b9 */
      if (this.isExtensionLoaded(extensionId)) {
        var message = "Rejecting attempt to load a second extension with ID ".concat(extensionId);
        log.warn(message);
        return;
      }
      var extension = this.builtinExtensions[extensionId]();
      var extensionInstance = new extension(this.runtime);
      var serviceName = this._registerInternalExtension(extensionInstance);
      this._loadedExtensions.set(extensionId, serviceName);
      this.runtime.compilerRegisterExtension(extensionId, extensionInstance);
    }
  }, {
    key: "addBuiltinExtension",
    value: function addBuiltinExtension(extensionId, extensionClass) {
      this.builtinExtensions[extensionId] = function () {
        return extensionClass;
      };
    }
  }, {
    key: "_isValidExtensionURL",
    value: function _isValidExtensionURL(extensionURL) {
      try {
        var parsedURL = new URL(extensionURL);
        return parsedURL.protocol === 'https:' || parsedURL.protocol === 'http:' || parsedURL.protocol === 'data:' || parsedURL.protocol === 'file:';
      } catch (e) {
        return false;
      }
    }

    /**
     * Load an extension by URL or internal extension ID
     * @param {string} extensionURL - the URL for the extension to load OR the ID of an internal extension
     * @returns {Promise} resolved once the extension is loaded and initialized or rejected on failure
     */
  }, {
    key: "loadExtensionURL",
    value: (function () {
      var _loadExtensionURL = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(extensionURL) {
        var _this = this;
        var sandboxMode, rewritten, _require, load, extensionObjects, fakeWorkerId, _iterator, _step, extensionObject, extensionInfo, serviceName, ExtensionWorker;
        return _regenerator().w(function (_context) {
          while (1) switch (_context.n) {
            case 0:
              if (!this.isBuiltinExtension(extensionURL)) {
                _context.n = 1;
                break;
              }
              this.loadExtensionIdSync(extensionURL);
              return _context.a(2);
            case 1:
              if (!this.isExtensionURLLoaded(extensionURL)) {
                _context.n = 2;
                break;
              }
              return _context.a(2);
            case 2:
              if (this._isValidExtensionURL(extensionURL)) {
                _context.n = 3;
                break;
              }
              throw new Error("Invalid extension URL: ".concat(extensionURL));
            case 3:
              this.runtime.setExternalCommunicationMethod('customExtensions', true);
              this.loadingAsyncExtensions++;
              _context.n = 4;
              return this.securityManager.getSandboxMode(extensionURL);
            case 4:
              sandboxMode = _context.v;
              _context.n = 5;
              return this.securityManager.rewriteExtensionURL(extensionURL);
            case 5:
              rewritten = _context.v;
              if (!(sandboxMode === 'unsandboxed')) {
                _context.n = 7;
                break;
              }
              _require = __webpack_require__(/*! ./tw-unsandboxed-extension-runner */ "../vm/src/extension-support/tw-unsandboxed-extension-runner.js"), load = _require.load;
              _context.n = 6;
              return load(rewritten, this.vm)["catch"](function (error) {
                return _this._failedLoadingExtensionScript(error);
              });
            case 6:
              extensionObjects = _context.v;
              fakeWorkerId = this.nextExtensionWorker++;
              this.workerURLs[fakeWorkerId] = extensionURL;
              _iterator = _createForOfIteratorHelper(extensionObjects);
              try {
                for (_iterator.s(); !(_step = _iterator.n()).done;) {
                  extensionObject = _step.value;
                  extensionInfo = extensionObject.getInfo();
                  serviceName = "unsandboxed.".concat(fakeWorkerId, ".").concat(extensionInfo.id);
                  dispatch.setServiceSync(serviceName, extensionObject);
                  dispatch.callSync('extensions', 'registerExtensionServiceSync', serviceName);
                  this._loadedExtensions.set(extensionInfo.id, serviceName);
                }
              } catch (err) {
                _iterator.e(err);
              } finally {
                _iterator.f();
              }
              this._finishedLoadingExtensionScript();
              return _context.a(2);
            case 7:
              if (!(sandboxMode === 'worker')) {
                _context.n = 8;
                break;
              }
              ExtensionWorker = __webpack_require__(/*! worker-loader?name=js/extension-worker/extension-worker.[hash].js!./extension-worker */ "../vm/node_modules/worker-loader/dist/cjs.js?name=js/extension-worker/extension-worker.[hash].js!../vm/src/extension-support/extension-worker.js");
              _context.n = 11;
              break;
            case 8:
              if (!(sandboxMode === 'iframe')) {
                _context.n = 10;
                break;
              }
              _context.n = 9;
              return __webpack_require__.e(/*! import() | iframe-extension-worker */ "iframe-extension-worker").then(__webpack_require__.t.bind(null, /*! ./tw-iframe-extension-worker */ "../vm/src/extension-support/tw-iframe-extension-worker.js", 7));
            case 9:
              ExtensionWorker = _context.v["default"];
              _context.n = 11;
              break;
            case 10:
              throw new Error("Invalid sandbox mode: ".concat(sandboxMode));
            case 11:
              return _context.a(2, new Promise(function (resolve, reject) {
                _this.pendingExtensions.push({
                  extensionURL: rewritten,
                  resolve: resolve,
                  reject: reject
                });
                dispatch.addWorker(new ExtensionWorker());
              })["catch"](function (error) {
                return _this._failedLoadingExtensionScript(error);
              }));
          }
        }, _callee, this);
      }));
      function loadExtensionURL(_x) {
        return _loadExtensionURL.apply(this, arguments);
      }
      return loadExtensionURL;
    }()
    /**
     * Wait until all async extensions have loaded
     * @returns {Promise} resolved when all async extensions have loaded
     */
    )
  }, {
    key: "allAsyncExtensionsLoaded",
    value: function allAsyncExtensionsLoaded() {
      var _this2 = this;
      if (this.loadingAsyncExtensions === 0) {
        return;
      }
      return new Promise(function (resolve, reject) {
        _this2.asyncExtensionsLoadedCallbacks.push({
          resolve: resolve,
          reject: reject
        });
      });
    }

    /**
     * Regenerate blockinfo for any loaded extensions
     * @param {string} [optExtensionId] Optional extension ID for refreshing
     * @returns {Promise} resolved once all the extensions have been reinitialized
     */
  }, {
    key: "refreshBlocks",
    value: function refreshBlocks(optExtensionId) {
      var _this3 = this;
      var refresh = function refresh(serviceName) {
        return dispatch.call(serviceName, 'getInfo').then(function (info) {
          info = _this3._prepareExtensionInfo(serviceName, info);
          dispatch.call('runtime', '_refreshExtensionPrimitives', info);
        })["catch"](function (e) {
          log.error('Failed to refresh built-in extension primitives', e);
        });
      };
      if (optExtensionId) {
        if (!this._loadedExtensions.has(optExtensionId)) {
          return Promise.reject(new Error("Unknown extension: ".concat(optExtensionId)));
        }
        return refresh(this._loadedExtensions.get(optExtensionId));
      }
      var allPromises = Array.from(this._loadedExtensions.values()).map(refresh);
      return Promise.all(allPromises);
    }
  }, {
    key: "allocateWorker",
    value: function allocateWorker() {
      var id = this.nextExtensionWorker++;
      var workerInfo = this.pendingExtensions.shift();
      this.pendingWorkers[id] = workerInfo;
      this.workerURLs[id] = workerInfo.extensionURL;
      return [id, workerInfo.extensionURL];
    }

    /**
     * Synchronously collect extension metadata from the specified service and begin the extension registration process.
     * @param {string} serviceName - the name of the service hosting the extension.
     */
  }, {
    key: "registerExtensionServiceSync",
    value: function registerExtensionServiceSync(serviceName) {
      var info = dispatch.callSync(serviceName, 'getInfo');
      this._registerExtensionInfo(serviceName, info);
    }

    /**
     * Collect extension metadata from the specified service and begin the extension registration process.
     * @param {string} serviceName - the name of the service hosting the extension.
     */
  }, {
    key: "registerExtensionService",
    value: function registerExtensionService(serviceName) {
      var _this4 = this;
      dispatch.call(serviceName, 'getInfo').then(function (info) {
        _this4._loadedExtensions.set(info.id, serviceName);
        _this4._registerExtensionInfo(serviceName, info);
        _this4._finishedLoadingExtensionScript();
      });
    }
  }, {
    key: "_finishedLoadingExtensionScript",
    value: function _finishedLoadingExtensionScript() {
      this.loadingAsyncExtensions--;
      if (this.loadingAsyncExtensions === 0) {
        this.asyncExtensionsLoadedCallbacks.forEach(function (i) {
          return i.resolve();
        });
        this.asyncExtensionsLoadedCallbacks = [];
      }
    }
  }, {
    key: "_failedLoadingExtensionScript",
    value: function _failedLoadingExtensionScript(error) {
      // Don't set the current extension counter to 0, otherwise it will go negative if another
      // extension finishes or fails to load.
      this.loadingAsyncExtensions--;
      this.asyncExtensionsLoadedCallbacks.forEach(function (i) {
        return i.reject(error);
      });
      this.asyncExtensionsLoadedCallbacks = [];
      // Re-throw error so the promise still rejects.
      throw error;
    }

    /**
     * Called by an extension worker to indicate that the worker has finished initialization.
     * @param {int} id - the worker ID.
     * @param {*?} e - the error encountered during initialization, if any.
     */
  }, {
    key: "onWorkerInit",
    value: function onWorkerInit(id, e) {
      var workerInfo = this.pendingWorkers[id];
      delete this.pendingWorkers[id];
      if (e) {
        workerInfo.reject(e);
      } else {
        workerInfo.resolve();
      }
    }

    /**
     * Register an internal (non-Worker) extension object
     * @param {object} extensionObject - the extension object to register
     * @returns {string} The name of the registered extension service
     */
  }, {
    key: "_registerInternalExtension",
    value: function _registerInternalExtension(extensionObject) {
      var extensionInfo = extensionObject.getInfo();
      var fakeWorkerId = this.nextExtensionWorker++;
      var serviceName = "extension_".concat(fakeWorkerId, "_").concat(extensionInfo.id);
      dispatch.setServiceSync(serviceName, extensionObject);
      dispatch.callSync('extensions', 'registerExtensionServiceSync', serviceName);
      return serviceName;
    }

    /**
     * Sanitize extension info then register its primitives with the VM.
     * @param {string} serviceName - the name of the service hosting the extension
     * @param {ExtensionInfo} extensionInfo - the extension's metadata
     * @private
     */
  }, {
    key: "_registerExtensionInfo",
    value: function _registerExtensionInfo(serviceName, extensionInfo) {
      extensionInfo = this._prepareExtensionInfo(serviceName, extensionInfo);
      dispatch.call('runtime', '_registerExtensionPrimitives', extensionInfo)["catch"](function (e) {
        log.error("Failed to register primitives for extension on service ".concat(serviceName, ":"), e);
      });
    }

    /**
     * Apply minor cleanup and defaults for optional extension fields.
     * TODO: make the ID unique in cases where two copies of the same extension are loaded.
     * @param {string} serviceName - the name of the service hosting this extension block
     * @param {ExtensionInfo} extensionInfo - the extension info to be sanitized
     * @returns {ExtensionInfo} - a new extension info object with cleaned-up values
     * @private
     */
  }, {
    key: "_prepareExtensionInfo",
    value: function _prepareExtensionInfo(serviceName, extensionInfo) {
      var _this5 = this;
      extensionInfo = Object.assign({}, extensionInfo);
      if (!/^[a-z0-9]+$/i.test(extensionInfo.id)) {
        throw new Error('Invalid extension id');
      }
      extensionInfo.name = extensionInfo.name || extensionInfo.id;
      extensionInfo.blocks = extensionInfo.blocks || [];
      extensionInfo.targetTypes = extensionInfo.targetTypes || [];
      extensionInfo.blocks = extensionInfo.blocks.reduce(function (results, blockInfo) {
        try {
          var result;
          switch (blockInfo) {
            case '---':
              // separator
              result = '---';
              break;
            default:
              // an ExtensionBlockMetadata object
              result = _this5._prepareBlockInfo(serviceName, blockInfo);
              break;
          }
          results.push(result);
        } catch (e) {
          // TODO: more meaningful error reporting
          log.error("Error processing block: ".concat(e.message, ", Block:\n").concat(JSON.stringify(blockInfo)));
        }
        return results;
      }, []);
      extensionInfo.menus = extensionInfo.menus || {};
      extensionInfo.menus = this._prepareMenuInfo(serviceName, extensionInfo.menus);
      return extensionInfo;
    }

    /**
     * Prepare extension menus. e.g. setup binding for dynamic menu functions.
     * @param {string} serviceName - the name of the service hosting this extension block
     * @param {Array.<MenuInfo>} menus - the menu defined by the extension.
     * @returns {Array.<MenuInfo>} - a menuInfo object with all preprocessing done.
     * @private
     */
  }, {
    key: "_prepareMenuInfo",
    value: function _prepareMenuInfo(serviceName, menus) {
      var menuNames = Object.getOwnPropertyNames(menus);
      for (var i = 0; i < menuNames.length; i++) {
        var menuName = menuNames[i];
        var menuInfo = menus[menuName];

        // If the menu description is in short form (items only) then normalize it to general form: an object with
        // its items listed in an `items` property.
        if (!menuInfo.items) {
          menuInfo = {
            items: menuInfo
          };
          menus[menuName] = menuInfo;
        }
        // If `items` is a string, it should be the name of a function in the extension object. Calling the
        // function should return an array of items to populate the menu when it is opened.
        if (typeof menuInfo.items === 'string') {
          var menuItemFunctionName = menuInfo.items;
          var serviceObject = dispatch.services[serviceName];
          // Bind the function here so we can pass a simple item generation function to Scratch Blocks later.
          menuInfo.items = this._getExtensionMenuItems.bind(this, serviceObject, menuItemFunctionName);
        }
      }
      return menus;
    }

    /**
     * Fetch the items for a particular extension menu, providing the target ID for context.
     * @param {object} extensionObject - the extension object providing the menu.
     * @param {string} menuItemFunctionName - the name of the menu function to call.
     * @returns {Array} menu items ready for scratch-blocks.
     * @private
     */
  }, {
    key: "_getExtensionMenuItems",
    value: function _getExtensionMenuItems(extensionObject, menuItemFunctionName) {
      // Fetch the items appropriate for the target currently being edited. This assumes that menus only
      // collect items when opened by the user while editing a particular target.
      var editingTarget = this.runtime.getEditingTarget() || this.runtime.getTargetForStage();
      var editingTargetID = editingTarget ? editingTarget.id : null;
      var extensionMessageContext = this.runtime.makeMessageContextForTarget(editingTarget);

      // TODO: Fix this to use dispatch.call when extensions are running in workers.
      var menuFunc = extensionObject[menuItemFunctionName];
      var menuItems = menuFunc.call(extensionObject, editingTargetID).map(function (item) {
        item = maybeFormatMessage(item, extensionMessageContext);
        switch (_typeof(item)) {
          case 'object':
            return [maybeFormatMessage(item.text, extensionMessageContext), item.value];
          case 'string':
            return [item, item];
          default:
            return item;
        }
      });
      if (!menuItems || menuItems.length < 1) {
        throw new Error("Extension menu returned no items: ".concat(menuItemFunctionName));
      }
      return menuItems;
    }

    /**
     * Apply defaults for optional block fields.
     * @param {string} serviceName - the name of the service hosting this extension block
     * @param {ExtensionBlockMetadata} blockInfo - the block info from the extension
     * @returns {ExtensionBlockMetadata} - a new block info object which has values for all relevant optional fields.
     * @private
     */
  }, {
    key: "_prepareBlockInfo",
    value: function _prepareBlockInfo(serviceName, blockInfo) {
      if (blockInfo.blockType === BlockType.XML) {
        blockInfo = Object.assign({}, blockInfo);
        blockInfo.xml = String(blockInfo.xml) || '';
        return blockInfo;
      }
      blockInfo = Object.assign({}, {
        blockType: BlockType.COMMAND,
        terminal: false,
        blockAllThreads: false,
        arguments: {}
      }, blockInfo);
      blockInfo.text = blockInfo.text || blockInfo.opcode;
      switch (blockInfo.blockType) {
        case BlockType.EVENT:
          if (blockInfo.func) {
            log.warn("Ignoring function \"".concat(blockInfo.func, "\" for event block ").concat(blockInfo.opcode));
          }
          break;
        case BlockType.BUTTON:
          if (blockInfo.opcode) {
            log.warn("Ignoring opcode \"".concat(blockInfo.opcode, "\" for button with text: ").concat(blockInfo.text));
          }
          blockInfo.callFunc = function () {
            dispatch.call(serviceName, blockInfo.func);
          };
          break;
        case BlockType.LABEL:
          if (blockInfo.opcode) {
            log.warn("Ignoring opcode \"".concat(blockInfo.opcode, "\" for label: ").concat(blockInfo.text));
          }
          break;
        default:
          {
            if (!blockInfo.opcode) {
              throw new Error('Missing opcode for block');
            }
            var funcName = blockInfo.func || blockInfo.opcode;
            var getBlockInfo = blockInfo.isDynamic ? function (args) {
              return args && args.mutation && args.mutation.blockInfo;
            } : function () {
              return blockInfo;
            };
            var callBlockFunc = function () {
              if (dispatch._isRemoteService(serviceName)) {
                return function (args, util, realBlockInfo) {
                  return dispatch.call(serviceName, funcName, args, util, realBlockInfo).then(function (result) {
                    // Scratch is only designed to handle these types.
                    // If any other value comes in such as undefined, null, an object, etc.
                    // we'll convert it to a string to avoid undefined behavior.
                    if (typeof result === 'number' || typeof result === 'string' || typeof result === 'boolean') {
                      return result;
                    }
                    return "".concat(result);
                  });
                };
              }

              // avoid promise latency if we can call direct
              var serviceObject = dispatch.services[serviceName];
              if (!serviceObject[funcName]) {
                // The function might show up later as a dynamic property of the service object
                log.warn("Could not find extension block function called ".concat(funcName));
              }
              return function (args, util, realBlockInfo) {
                return serviceObject[funcName](args, util, realBlockInfo);
              };
            }();
            blockInfo.func = function (args, util) {
              var realBlockInfo = getBlockInfo(args);
              // TODO: filter args using the keys of realBlockInfo.arguments? maybe only if sandboxed?
              return callBlockFunc(args, util, realBlockInfo);
            };
            break;
          }
      }
      return blockInfo;
    }
  }, {
    key: "getExtensionURLs",
    value: function getExtensionURLs() {
      var extensionURLs = {};
      var _iterator2 = _createForOfIteratorHelper(this._loadedExtensions.entries()),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var _step2$value = _slicedToArray(_step2.value, 2),
            extensionId = _step2$value[0],
            serviceName = _step2$value[1];
          if (Object.prototype.hasOwnProperty.call(this.builtinExtensions, extensionId)) {
            continue;
          }

          // Service names for extension workers are in the format "extension.WORKER_ID.EXTENSION_ID"
          var workerId = +serviceName.split('.')[1];
          var extensionURL = this.workerURLs[workerId];
          if (typeof extensionURL === 'string') {
            extensionURLs[extensionId] = extensionURL;
          }
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      return extensionURLs;
    }
  }, {
    key: "isExtensionURLLoaded",
    value: function isExtensionURLLoaded(url) {
      return Object.values(this.workerURLs).includes(url);
    }
  }]);
}();
module.exports = ExtensionManager;

/***/ }),

/***/ "../vm/src/extension-support/target-type.js":
/*!**************************************************!*\
  !*** ../vm/src/extension-support/target-type.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Default types of Target supported by the VM
 * @enum {string}
 */
var TargetType = {
  /**
   * Rendered target which can move, change costumes, etc.
   */
  SPRITE: 'sprite',
  /**
   * Rendered target which cannot move but can change backdrops
   */
  STAGE: 'stage'
};
module.exports = TargetType;

/***/ }),

/***/ "../vm/src/extension-support/tw-block-shape.js":
/*!*****************************************************!*\
  !*** ../vm/src/extension-support/tw-block-shape.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// Use the constants instead of manually redefining them again
var ScratchBlocksConstants = __webpack_require__(/*! ../engine/scratch-blocks-constants */ "../vm/src/engine/scratch-blocks-constants.js");

/**
 * Types of block shapes
 * @enum {number}
 */
var BlockShape = {
  /**
   * Output shape: hexagonal (booleans/predicates).
   */
  HEXAGONAL: ScratchBlocksConstants.OUTPUT_SHAPE_HEXAGONAL,
  /**
   * Output shape: rounded (numbers).
   */
  ROUND: ScratchBlocksConstants.OUTPUT_SHAPE_ROUND,
  /**
   * Output shape: squared (any/all values; strings).
   */
  SQUARE: ScratchBlocksConstants.OUTPUT_SHAPE_SQUARE
};
module.exports = BlockShape;

/***/ }),

/***/ "../vm/src/extension-support/tw-default-extension-urls.js":
/*!****************************************************************!*\
  !*** ../vm/src/extension-support/tw-default-extension-urls.js ***!
  \****************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// If a project uses an extension but does not specify a URL, it will default to
// the URLs given here, if it exists. This is useful for compatibility with other mods.

var defaults = {
  // Box2D (`griffpatch`) is not listed here because our extension is not actually
  // compatible with the original version due to fields vs inputs.

  // Scratch Lab Animated Text - https://lab.scratch.mit.edu/text/
  text: 'https://extensions.turbowarp.org/lab/text.js',
  // Turboloader's AudioStream
  audiostr: 'https://extensions.turbowarp.org/turboloader/audiostream.js',
  // https://scratch.mit.edu/discuss/topic/842592/
  faceSensing: 'https://extensions.turbowarp.org/lab/face-sensing.js'
};
module.exports = defaults;

/***/ }),

/***/ "../vm/src/extension-support/tw-extension-api-common.js":
/*!**************************************************************!*\
  !*** ../vm/src/extension-support/tw-extension-api-common.js ***!
  \**************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var ArgumentType = __webpack_require__(/*! ./argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ./block-type */ "../vm/src/extension-support/block-type.js");
var BlockShape = __webpack_require__(/*! ./tw-block-shape */ "../vm/src/extension-support/tw-block-shape.js");
var TargetType = __webpack_require__(/*! ./target-type */ "../vm/src/extension-support/target-type.js");
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var external = __webpack_require__(/*! ./tw-external */ "../vm/src/extension-support/tw-external.js");
var Scratch = {
  ArgumentType: ArgumentType,
  BlockType: BlockType,
  BlockShape: BlockShape,
  TargetType: TargetType,
  Cast: Cast,
  external: external
};
module.exports = Scratch;

/***/ }),

/***/ "../vm/src/extension-support/tw-external.js":
/*!**************************************************!*\
  !*** ../vm/src/extension-support/tw-external.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
/**
 * @param {string} url
 * @returns {void} if URL is supported
 * @throws if URL is unsupported
 */
var checkURL = function checkURL(url) {
  // URL might be a very long data: URL, so try to avoid fully parsing it if we can.
  // The notable requirement here is that the URL must be an absolute URL, not something
  // relative to where the extension is loaded from or where the extension is running.
  // This ensures that the same extension file will always load resources from the same
  // place, regardless of how it is running or packaged or whatever else.
  if (!url.startsWith('http:') && !url.startsWith('https:') && !url.startsWith('data:') && !url.startsWith('blob:')) {
    throw new Error("Unsupported URL: ".concat(url));
  }
};
var external = {};

/**
 * @param {string} url
 * @template T
 * @returns {Promise<T>}
 */
external.importModule = function (url) {
  checkURL(url);
  // Need to specify webpackIgnore so that webpack compiles this directly to a call to import()
  // instead of trying making it try to use the webpack import system.
  return import(/* webpackIgnore: true */url);
};

/**
 * @param {string} url
 * @returns {Promise<Response>}
 */
external.fetch = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(url) {
    var res;
    return _regenerator().w(function (_context) {
      while (1) switch (_context.n) {
        case 0:
          checkURL(url);
          _context.n = 1;
          return fetch(url);
        case 1:
          res = _context.v;
          if (res.ok) {
            _context.n = 2;
            break;
          }
          throw new Error("HTTP ".concat(res.status, " fetching ").concat(url));
        case 2:
          return _context.a(2, res);
      }
    }, _callee);
  }));
  return function (_x) {
    return _ref.apply(this, arguments);
  };
}();

/**
 * @param {string} url
 * @returns {Promise<string>}
 */
external.dataURL = /*#__PURE__*/function () {
  var _ref2 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(url) {
    var res, blob;
    return _regenerator().w(function (_context2) {
      while (1) switch (_context2.n) {
        case 0:
          _context2.n = 1;
          return external.fetch(url);
        case 1:
          res = _context2.v;
          _context2.n = 2;
          return res.blob();
        case 2:
          blob = _context2.v;
          return _context2.a(2, new Promise(function (resolve, reject) {
            var fr = new FileReader();
            fr.onload = function () {
              return resolve(fr.result);
            };
            fr.onerror = function () {
              return reject(fr.error);
            };
            fr.readAsDataURL(blob);
          }));
      }
    }, _callee2);
  }));
  return function (_x2) {
    return _ref2.apply(this, arguments);
  };
}();

/**
 * @param {string} url
 * @returns {Promise<Blob>}
 */
external.blob = /*#__PURE__*/function () {
  var _ref3 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee3(url) {
    var res;
    return _regenerator().w(function (_context3) {
      while (1) switch (_context3.n) {
        case 0:
          _context3.n = 1;
          return external.fetch(url);
        case 1:
          res = _context3.v;
          return _context3.a(2, res.blob());
      }
    }, _callee3);
  }));
  return function (_x3) {
    return _ref3.apply(this, arguments);
  };
}();

/**
 * @param {string} url
 * @param {string} returnExpression
 * @template T
 * @returns {Promise<T>}
 */
external.evalAndReturn = /*#__PURE__*/function () {
  var _ref4 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee4(url, returnExpression) {
    var res, text, js, fn;
    return _regenerator().w(function (_context4) {
      while (1) switch (_context4.n) {
        case 0:
          _context4.n = 1;
          return external.fetch(url);
        case 1:
          res = _context4.v;
          _context4.n = 2;
          return res.text();
        case 2:
          text = _context4.v;
          js = "".concat(text, "\nreturn ").concat(returnExpression, ";");
          fn = new Function(js);
          return _context4.a(2, fn());
      }
    }, _callee4);
  }));
  return function (_x4, _x5) {
    return _ref4.apply(this, arguments);
  };
}();
module.exports = external;

/***/ }),

/***/ "../vm/src/extension-support/tw-l10n.js":
/*!**********************************************!*\
  !*** ../vm/src/extension-support/tw-l10n.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");

/**
 * @param {VM|null} vm
 * @returns {object}
 */
var createTranslate = function createTranslate(vm) {
  var namespace = formatMessage.namespace();
  var translate = function translate(message, args) {
    if (message && _typeof(message) === 'object') {
      // already in the expected format
    } else if (typeof message === 'string') {
      message = {
        "default": message
      };
    } else {
      throw new Error('unsupported data type in translate()');
    }
    return namespace(message, args);
  };
  var generateId = function generateId(defaultMessage) {
    return "_".concat(defaultMessage);
  };
  var getLocale = function getLocale() {
    if (vm) return vm.getLocale();
    if (typeof navigator !== 'undefined') return navigator.language;
    return 'en';
  };
  var storedTranslations = {};
  translate.setup = function (newTranslations) {
    if (newTranslations) {
      storedTranslations = newTranslations;
    }
    namespace.setup({
      locale: getLocale(),
      missingTranslation: 'ignore',
      generateId: generateId,
      translations: storedTranslations
    });
  };
  Object.defineProperty(translate, 'language', {
    configurable: true,
    enumerable: true,
    get: function get() {
      return getLocale();
    }
  });
  translate.setup({});
  if (vm) {
    vm.on('LOCALE_CHANGED', function () {
      translate.setup(null);
    });
  }
  return translate;
};
module.exports = createTranslate;

/***/ }),

/***/ "../vm/src/extension-support/tw-scratchx-compatibility-layer.js":
/*!**********************************************************************!*\
  !*** ../vm/src/extension-support/tw-scratchx-compatibility-layer.js ***!
  \**********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
// ScratchX API Documentation: https://github.com/LLK/scratchx/wiki/

var ArgumentType = __webpack_require__(/*! ./argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ./block-type */ "../vm/src/extension-support/block-type.js");
var _require = __webpack_require__(/*! ./tw-scratchx-utilities */ "../vm/src/extension-support/tw-scratchx-utilities.js"),
  argumentIndexToId = _require.argumentIndexToId,
  generateExtensionId = _require.generateExtensionId;

/**
 * @typedef ScratchXDescriptor
 * @property {unknown[][]} blocks
 * @property {Record<string, unknown[]>} [menus]
 * @property {string} [url]
 * @property {string} [displayName]
 */

/**
 * @typedef ScratchXStatus
 * @property {0|1|2} status 0 is red/error, 1 is yellow/not ready, 2 is green/ready
 * @property {string} msg
 */

var parseScratchXBlockType = function parseScratchXBlockType(type) {
  if (type === '' || type === ' ' || type === 'w') {
    return {
      type: BlockType.COMMAND,
      async: type === 'w'
    };
  }
  if (type === 'r' || type === 'R') {
    return {
      type: BlockType.REPORTER,
      async: type === 'R'
    };
  }
  if (type === 'b') {
    return {
      type: BlockType.BOOLEAN,
      // ScratchX docs don't seem to mention boolean reporters that wait
      async: false
    };
  }
  if (type === 'h') {
    return {
      type: BlockType.HAT,
      async: false
    };
  }
  throw new Error("Unknown ScratchX block type: ".concat(type));
};
var isScratchCompatibleValue = function isScratchCompatibleValue(v) {
  return typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean';
};

/**
 * @param {string} argument ScratchX argument with leading % removed.
 * @param {unknown} defaultValue Default value, if any
 */
var parseScratchXArgument = function parseScratchXArgument(argument, defaultValue) {
  var result = {};
  var hasDefaultValue = isScratchCompatibleValue(defaultValue);

  // defaultValue is ignored for booleans in Scratch 3
  if (hasDefaultValue && argument !== 'b') {
    result.defaultValue = defaultValue;
  }
  if (argument === 's') {
    result.type = ArgumentType.STRING;
    if (!hasDefaultValue) {
      result.defaultValue = '';
    }
  } else if (argument === 'n') {
    result.type = ArgumentType.NUMBER;
    if (!hasDefaultValue) {
      result.defaultValue = 0;
    }
  } else if (argument[0] === 'm') {
    result.type = ArgumentType.STRING;
    var split = argument.split(/\.|:/);
    var menuName = split[1];
    result.menu = menuName;
  } else if (argument === 'b') {
    result.type = ArgumentType.BOOLEAN;
  } else {
    throw new Error("Unknown ScratchX argument type: ".concat(argument));
  }
  return result;
};
var wrapScratchXFunction = function wrapScratchXFunction(originalFunction, argumentCount, async) {
  return function (args) {
    // Convert Scratch 3's argument object to an argument list expected by ScratchX
    var argumentList = [];
    for (var i = 0; i < argumentCount; i++) {
      argumentList.push(args[argumentIndexToId(i)]);
    }
    if (async) {
      return new Promise(function (resolve) {
        originalFunction.apply(void 0, argumentList.concat([resolve]));
      });
    }
    return originalFunction.apply(void 0, argumentList);
  };
};

/**
 * @param {string} name
 * @param {ScratchXDescriptor} descriptor
 * @param {Record<string, () => unknown>} functions
 */
var convert = function convert(name, descriptor, functions) {
  var extensionId = generateExtensionId(name);
  var info = {
    id: extensionId,
    name: descriptor.displayName || name,
    blocks: [],
    color1: '#4a4a5e',
    color2: '#31323f',
    color3: '#191a21'
  };
  var scratch3Extension = {
    getInfo: function getInfo() {
      return info;
    },
    _getStatus: functions._getStatus
  };
  if (descriptor.url) {
    info.docsURI = descriptor.url;
  }
  var _iterator = _createForOfIteratorHelper(descriptor.blocks),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var blockDescriptor = _step.value;
      if (blockDescriptor.length === 1) {
        // Separator
        info.blocks.push('---');
        continue;
      }
      var scratchXBlockType = blockDescriptor[0];
      var blockText = blockDescriptor[1];
      var functionName = blockDescriptor[2];
      var defaultArgumentValues = blockDescriptor.slice(3);
      var scratchText = '';
      var argumentInfo = [];
      var blockTextParts = blockText.split(/%([\w.:]+)/g);
      for (var i = 0; i < blockTextParts.length; i++) {
        var part = blockTextParts[i];
        var isArgument = i % 2 === 1;
        if (isArgument) {
          parseScratchXArgument(part);
          var argumentIndex = Math.floor(i / 2).toString();
          var argumentDefaultValue = defaultArgumentValues[argumentIndex];
          var argumentId = argumentIndexToId(argumentIndex);
          argumentInfo[argumentId] = parseScratchXArgument(part, argumentDefaultValue);
          scratchText += "[".concat(argumentId, "]");
        } else {
          scratchText += part;
        }
      }
      var scratch3BlockType = parseScratchXBlockType(scratchXBlockType);
      var blockInfo = {
        opcode: functionName,
        blockType: scratch3BlockType.type,
        text: scratchText,
        arguments: argumentInfo
      };
      info.blocks.push(blockInfo);
      var originalFunction = functions[functionName];
      var argumentCount = argumentInfo.length;
      scratch3Extension[functionName] = wrapScratchXFunction(originalFunction, argumentCount, scratch3BlockType.async);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  var menus = descriptor.menus;
  if (menus) {
    var scratch3Menus = {};
    var _iterator2 = _createForOfIteratorHelper(Object.keys(menus) || {}),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var menuName = _step2.value;
        var menuItems = menus[menuName];
        var menuInfo = {
          items: menuItems
        };
        scratch3Menus[menuName] = menuInfo;
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
    info.menus = scratch3Menus;
  }
  return scratch3Extension;
};
var extensionNameToExtension = new Map();

/**
 * @param {*} Scratch Scratch 3.0 extension API object
 * @returns {*} ScratchX-compatible API object
 */
var createScratchX = function createScratchX(Scratch) {
  var register = function register(name, descriptor, functions) {
    var scratch3Extension = convert(name, descriptor, functions);
    extensionNameToExtension.set(name, scratch3Extension);
    Scratch.extensions.register(scratch3Extension);
  };

  /**
   * @param {string} extensionName
   * @returns {ScratchXStatus}
   */
  var getStatus = function getStatus(extensionName) {
    var extension = extensionNameToExtension.get(extensionName);
    if (extension) {
      return extension._getStatus();
    }
    return {
      status: 0,
      msg: 'does not exist'
    };
  };
  return {
    register: register,
    getStatus: getStatus
  };
};
module.exports = createScratchX;

/***/ }),

/***/ "../vm/src/extension-support/tw-scratchx-utilities.js":
/*!************************************************************!*\
  !*** ../vm/src/extension-support/tw-scratchx-utilities.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * @fileoverview
 * General ScratchX-related utilities used in multiple places.
 * Changing these functions may break projects.
 */

/**
 * @param {string} scratchXName
 * @returns {string}
 */
var generateExtensionId = function generateExtensionId(scratchXName) {
  var sanitizedName = scratchXName.replace(/[^a-z0-9]/gi, '').toLowerCase();
  return "sbx".concat(sanitizedName);
};

/**
 * @param {number} i 0-indexed index of argument in list
 * @returns {string} Scratch 3 argument name
 */
var argumentIndexToId = function argumentIndexToId(i) {
  return i.toString();
};
module.exports = {
  generateExtensionId: generateExtensionId,
  argumentIndexToId: argumentIndexToId
};

/***/ }),

/***/ "../vm/src/extension-support/tw-security-manager.js":
/*!**********************************************************!*\
  !*** ../vm/src/extension-support/tw-security-manager.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/* eslint-disable no-unused-vars */
/**
 * Responsible for determining various policies related to custom extension security.
 * The default implementation prevents automatic extension loading, but grants any
 * loaded extensions the maximum possible capabilities so as to retain compatibility
 * with a vanilla scratch-vm. You may override properties of an instance of this class
 * to customize the security policies as you see fit, for example:
 * ```js
 * vm.securityManager.getSandboxMode = (url) => {
 *   if (url.startsWith("https://example.com/")) {
 *     return "unsandboxed";
 *   }
 *   return "iframe";
 * };
 * vm.securityManager.canAutomaticallyLoadExtension = (url) => {
 *   return confirm("Automatically load extension: " + url);
 * };
 * vm.securityManager.canFetch = (url) => {
 *   return url.startsWith('https://turbowarp.org/');
 * };
 * vm.securityManager.canOpenWindow = (url) => {
 *   return url.startsWith('https://turbowarp.org/');
 * };
 * vm.securityManager.canRedirect = (url) => {
 *   return url.startsWith('https://turbowarp.org/');
 * };
 * ```
 */
var SecurityManager = /*#__PURE__*/function () {
  function SecurityManager() {
    _classCallCheck(this, SecurityManager);
  }
  return _createClass(SecurityManager, [{
    key: "getSandboxMode",
    value:
    /**
     * Determine the typeof sandbox to use for a certain custom extension.
     * @param {string} extensionURL The URL of the custom extension.
     * @returns {'worker'|'iframe'|'unsandboxed'|Promise<'worker'|'iframe'|'unsandboxed'>}
     */
    function getSandboxMode(extensionURL) {
      // Default to worker for Scratch compatibility
      return Promise.resolve('worker');
    }

    /**
     * Determine whether a custom extension that was stored inside a project may be
     * loaded. You could, for example, ask the user to confirm loading an extension
     * before resolving.
     * @param {string} extensionURL The URL of the custom extension.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canLoadExtensionFromProject",
    value: function canLoadExtensionFromProject(extensionURL) {
      // Default to false for security
      return Promise.resolve(false);
    }

    /**
     * Allows last-minute changing the real URL of the extension that gets loaded.
     * @param {*} extensionURL The URL requested to be loaded.
     * @returns {Promise<string>|string} The URL to actually load.
     */
  }, {
    key: "rewriteExtensionURL",
    value: function rewriteExtensionURL(extensionURL) {
      return Promise.resolve(extensionURL);
    }

    /**
     * Determine whether an extension is allowed to fetch a remote resource URL.
     * This only applies to unsandboxed extensions that use the appropriate Scratch.* APIs.
     * Sandboxed extensions ignore this entirely as there is no way to force them to use our APIs.
     * data: and blob: URLs are always allowed (this method is never called).
     * @param {string} resourceURL
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canFetch",
    value: function canFetch(resourceURL) {
      // By default, allow any requests.
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to open a new window or tab to a given URL.
     * This only applies to unsandboxed extensions. Sandboxed extensions are unable to open windows.
     * javascript: URLs are always rejected (this method is never called).
     * @param {string} websiteURL
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canOpenWindow",
    value: function canOpenWindow(websiteURL) {
      // By default, allow all.
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to redirect the current tab to a given URL.
     * This only applies to unsandboxed extensions. Sandboxed extensions are unable to redirect the parent
     * window, but are free to redirect their own sandboxed window.
     * javascript: URLs are always rejected (this method is never called).
     * @param {string} websiteURL
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canRedirect",
    value: function canRedirect(websiteURL) {
      // By default, allow all.
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to record audio from the user's microphone.
     * This could include raw audio data or a transcriptions.
     * Note that, even if this returns true, success is not guaranteed.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canRecordAudio",
    value: function canRecordAudio() {
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to record video from the user's camera.
     * Note that, even if this returns true, success is not guaranteed.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canRecordVideo",
    value: function canRecordVideo() {
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to read values from the user's clipboard
     * without user interaction.
     * Note that, even if this returns true, success is not guaranteed.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canReadClipboard",
    value: function canReadClipboard() {
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to show notifications.
     * Note that, even if this returns true, success is not guaranteed.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canNotify",
    value: function canNotify() {
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to find the user's precise location using GPS
     * and other techniques. Note that, even if this returns true, success is not guaranteed.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canGeolocate",
    value: function canGeolocate() {
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to embed content from a given URL.
     * @param {string} documentURL The URL of the embed.
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canEmbed",
    value: function canEmbed(documentURL) {
      return Promise.resolve(true);
    }

    /**
     * Determine whether an extension is allowed to download a URL with a given name.
     * @param {string} resourceURL The URL to download
     * @param {string} name The name of the file
     * @returns {Promise<boolean>|boolean}
     */
  }, {
    key: "canDownload",
    value: function canDownload(resourceURL, name) {
      return Promise.resolve(true);
    }
  }]);
}();
module.exports = SecurityManager;

/***/ }),

/***/ "../vm/src/extension-support/tw-unsandboxed-extension-runner.js":
/*!**********************************************************************!*\
  !*** ../vm/src/extension-support/tw-unsandboxed-extension-runner.js ***!
  \**********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
var ScratchCommon = __webpack_require__(/*! ./tw-extension-api-common */ "../vm/src/extension-support/tw-extension-api-common.js");
var createScratchX = __webpack_require__(/*! ./tw-scratchx-compatibility-layer */ "../vm/src/extension-support/tw-scratchx-compatibility-layer.js");
var AsyncLimiter = __webpack_require__(/*! ../util/async-limiter */ "../vm/src/util/async-limiter.js");
var createTranslate = __webpack_require__(/*! ./tw-l10n */ "../vm/src/extension-support/tw-l10n.js");
var staticFetch = __webpack_require__(/*! ../util/tw-static-fetch */ "../vm/src/util/tw-static-fetch.js");

/* eslint-disable require-await */

/**
 * Parse a URL object or return null.
 * @param {string} url
 * @returns {URL|null}
 */
var parseURL = function parseURL(url) {
  try {
    return new URL(url, location.href);
  } catch (e) {
    return null;
  }
};

/**
 * Sets up the global.Scratch API for an unsandboxed extension.
 * @param {VirtualMachine} vm
 * @returns {Promise<object[]>} Resolves with a list of extension objects when Scratch.extensions.register is called.
 */
var setupUnsandboxedExtensionAPI = function setupUnsandboxedExtensionAPI(vm) {
  return new Promise(function (resolve) {
    var extensionObjects = [];
    var register = function register(extensionObject) {
      extensionObjects.push(extensionObject);
      resolve(extensionObjects);
    };

    // Create a new copy of global.Scratch for each extension
    var Scratch = Object.assign({}, global.Scratch || {}, ScratchCommon);
    Scratch.extensions = {
      unsandboxed: true,
      register: register
    };
    Scratch.vm = vm;
    Scratch.renderer = vm.runtime.renderer;
    Scratch.canFetch = /*#__PURE__*/function () {
      var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(url) {
        var parsed;
        return _regenerator().w(function (_context) {
          while (1) switch (_context.n) {
            case 0:
              parsed = parseURL(url);
              if (parsed) {
                _context.n = 1;
                break;
              }
              return _context.a(2, false);
            case 1:
              if (!(parsed.protocol === 'blob:' || parsed.protocol === 'data:')) {
                _context.n = 2;
                break;
              }
              return _context.a(2, true);
            case 2:
              return _context.a(2, vm.securityManager.canFetch(parsed.href));
          }
        }, _callee);
      }));
      return function (_x) {
        return _ref.apply(this, arguments);
      };
    }();
    Scratch.canOpenWindow = /*#__PURE__*/function () {
      var _ref2 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(url) {
        var parsed;
        return _regenerator().w(function (_context2) {
          while (1) switch (_context2.n) {
            case 0:
              parsed = parseURL(url);
              if (parsed) {
                _context2.n = 1;
                break;
              }
              return _context2.a(2, false);
            case 1:
              if (!(parsed.protocol === 'javascript:')) {
                _context2.n = 2;
                break;
              }
              return _context2.a(2, false);
            case 2:
              return _context2.a(2, vm.securityManager.canOpenWindow(parsed.href));
          }
        }, _callee2);
      }));
      return function (_x2) {
        return _ref2.apply(this, arguments);
      };
    }();
    Scratch.canRedirect = /*#__PURE__*/function () {
      var _ref3 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee3(url) {
        var parsed;
        return _regenerator().w(function (_context3) {
          while (1) switch (_context3.n) {
            case 0:
              parsed = parseURL(url);
              if (parsed) {
                _context3.n = 1;
                break;
              }
              return _context3.a(2, false);
            case 1:
              if (!(parsed.protocol === 'javascript:')) {
                _context3.n = 2;
                break;
              }
              return _context3.a(2, false);
            case 2:
              return _context3.a(2, vm.securityManager.canRedirect(parsed.href));
          }
        }, _callee3);
      }));
      return function (_x3) {
        return _ref3.apply(this, arguments);
      };
    }();
    Scratch.canRecordAudio = /*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee4() {
      return _regenerator().w(function (_context4) {
        while (1) switch (_context4.n) {
          case 0:
            return _context4.a(2, vm.securityManager.canRecordAudio());
        }
      }, _callee4);
    }));
    Scratch.canRecordVideo = /*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee5() {
      return _regenerator().w(function (_context5) {
        while (1) switch (_context5.n) {
          case 0:
            return _context5.a(2, vm.securityManager.canRecordVideo());
        }
      }, _callee5);
    }));
    Scratch.canReadClipboard = /*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee6() {
      return _regenerator().w(function (_context6) {
        while (1) switch (_context6.n) {
          case 0:
            return _context6.a(2, vm.securityManager.canReadClipboard());
        }
      }, _callee6);
    }));
    Scratch.canNotify = /*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee7() {
      return _regenerator().w(function (_context7) {
        while (1) switch (_context7.n) {
          case 0:
            return _context7.a(2, vm.securityManager.canNotify());
        }
      }, _callee7);
    }));
    Scratch.canGeolocate = /*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee8() {
      return _regenerator().w(function (_context8) {
        while (1) switch (_context8.n) {
          case 0:
            return _context8.a(2, vm.securityManager.canGeolocate());
        }
      }, _callee8);
    }));
    Scratch.canEmbed = /*#__PURE__*/function () {
      var _ref9 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee9(url) {
        var parsed;
        return _regenerator().w(function (_context9) {
          while (1) switch (_context9.n) {
            case 0:
              parsed = parseURL(url);
              if (parsed) {
                _context9.n = 1;
                break;
              }
              return _context9.a(2, false);
            case 1:
              return _context9.a(2, vm.securityManager.canEmbed(parsed.href));
          }
        }, _callee9);
      }));
      return function (_x4) {
        return _ref9.apply(this, arguments);
      };
    }();
    Scratch.canDownload = /*#__PURE__*/function () {
      var _ref0 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee0(url, name) {
        var parsed;
        return _regenerator().w(function (_context0) {
          while (1) switch (_context0.n) {
            case 0:
              parsed = parseURL(url);
              if (parsed) {
                _context0.n = 1;
                break;
              }
              return _context0.a(2, false);
            case 1:
              if (!(parsed.protocol === 'javascript:')) {
                _context0.n = 2;
                break;
              }
              return _context0.a(2, false);
            case 2:
              return _context0.a(2, vm.securityManager.canDownload(url, name));
          }
        }, _callee0);
      }));
      return function (_x5, _x6) {
        return _ref0.apply(this, arguments);
      };
    }();
    Scratch.fetch = /*#__PURE__*/function () {
      var _ref1 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee1(url, options) {
        var actualURL, staticFetchResult;
        return _regenerator().w(function (_context1) {
          while (1) switch (_context1.n) {
            case 0:
              actualURL = url instanceof Request ? url.url : url;
              staticFetchResult = staticFetch(url);
              if (!staticFetchResult) {
                _context1.n = 1;
                break;
              }
              return _context1.a(2, staticFetchResult);
            case 1:
              _context1.n = 2;
              return Scratch.canFetch(actualURL);
            case 2:
              if (_context1.v) {
                _context1.n = 3;
                break;
              }
              throw new Error("Permission to fetch ".concat(actualURL, " rejected."));
            case 3:
              return _context1.a(2, fetch(url, options));
          }
        }, _callee1);
      }));
      return function (_x7, _x8) {
        return _ref1.apply(this, arguments);
      };
    }();
    Scratch.openWindow = /*#__PURE__*/function () {
      var _ref10 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee10(url, features) {
        var baseFeatures;
        return _regenerator().w(function (_context10) {
          while (1) switch (_context10.n) {
            case 0:
              _context10.n = 1;
              return Scratch.canOpenWindow(url);
            case 1:
              if (_context10.v) {
                _context10.n = 2;
                break;
              }
              throw new Error("Permission to open tab ".concat(url, " rejected."));
            case 2:
              // Use noreferrer to prevent new tab from accessing `window.opener`
              baseFeatures = 'noreferrer';
              features = features ? "".concat(baseFeatures, ",").concat(features) : baseFeatures;
              return _context10.a(2, window.open(url, '_blank', features));
          }
        }, _callee10);
      }));
      return function (_x9, _x0) {
        return _ref10.apply(this, arguments);
      };
    }();
    Scratch.redirect = /*#__PURE__*/function () {
      var _ref11 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee11(url) {
        return _regenerator().w(function (_context11) {
          while (1) switch (_context11.n) {
            case 0:
              _context11.n = 1;
              return Scratch.canRedirect(url);
            case 1:
              if (_context11.v) {
                _context11.n = 2;
                break;
              }
              throw new Error("Permission to redirect to ".concat(url, " rejected."));
            case 2:
              location.href = url;
            case 3:
              return _context11.a(2);
          }
        }, _callee11);
      }));
      return function (_x1) {
        return _ref11.apply(this, arguments);
      };
    }();
    Scratch.download = /*#__PURE__*/function () {
      var _ref12 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee12(url, name) {
        var link;
        return _regenerator().w(function (_context12) {
          while (1) switch (_context12.n) {
            case 0:
              _context12.n = 1;
              return Scratch.canDownload(url, name);
            case 1:
              if (_context12.v) {
                _context12.n = 2;
                break;
              }
              throw new Error("Permission to download ".concat(name, " rejected."));
            case 2:
              link = document.createElement('a');
              link.href = url;
              link.download = name;
              document.body.appendChild(link);
              link.click();
              link.remove();
            case 3:
              return _context12.a(2);
          }
        }, _callee12);
      }));
      return function (_x10, _x11) {
        return _ref12.apply(this, arguments);
      };
    }();
    Scratch.translate = createTranslate(vm);
    global.Scratch = Scratch;
    global.ScratchExtensions = createScratchX(Scratch);
    vm.emit('CREATE_UNSANDBOXED_EXTENSION_API', Scratch);
  });
};

/**
 * Disable the existing global.Scratch unsandboxed extension APIs.
 * This helps debug poorly designed extensions.
 */
var teardownUnsandboxedExtensionAPI = function teardownUnsandboxedExtensionAPI() {
  // We can assume global.Scratch already exists.
  global.Scratch.extensions.register = function () {
    throw new Error('Too late to register new extensions.');
  };
};

/**
 * Load an unsandboxed extension from an arbitrary URL. This is dangerous.
 * @param {string} extensionURL
 * @param {Virtualmachine} vm
 * @returns {Promise<object[]>} Resolves with a list of extension objects if the extension was loaded successfully.
 */
var loadUnsandboxedExtension = function loadUnsandboxedExtension(extensionURL, vm) {
  return new Promise(function (resolve, reject) {
    setupUnsandboxedExtensionAPI(vm).then(resolve);
    var script = document.createElement('script');
    script.onerror = function () {
      reject(new Error("Error in unsandboxed script ".concat(extensionURL, ". Check the console for more information.")));
    };
    script.src = extensionURL;
    document.body.appendChild(script);
  }).then(function (objects) {
    teardownUnsandboxedExtensionAPI();
    return objects;
  });
};

// Because loading unsandboxed extensions requires messing with global state (global.Scratch),
// only let one extension load at a time.
var limiter = new AsyncLimiter(loadUnsandboxedExtension, 1);
var load = function load(extensionURL, vm) {
  return limiter["do"](extensionURL, vm);
};
module.exports = {
  setupUnsandboxedExtensionAPI: setupUnsandboxedExtensionAPI,
  load: load
};
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../../gui/node_modules/webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "../vm/src/extensions/scratch3_boost/index.js":
/*!****************************************************!*\
  !*** ../vm/src/extensions/scratch3_boost/index.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var color = __webpack_require__(/*! ../../util/color */ "../vm/src/util/color.js");
var BLE = __webpack_require__(/*! ../../io/ble */ "../vm/src/io/ble.js");
var Base64Util = __webpack_require__(/*! ../../util/base64-util */ "../vm/src/util/base64-util.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var RateLimiter = __webpack_require__(/*! ../../util/rateLimiter.js */ "../vm/src/util/rateLimiter.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");

/**
 * The LEGO Wireless Protocol documentation used to create this extension can be found at:
 * https://lego.github.io/lego-ble-wireless-protocol-docs/index.html
 */

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var iconURI = 'data:image/png;base64,';

/**
 * Boost BLE UUIDs.
 * @enum {string}
 */
var BoostBLE = {
  service: '00001623-1212-efde-1623-785feabcd123',
  characteristic: '00001624-1212-efde-1623-785feabcd123',
  sendInterval: 100,
  sendRateMax: 20
};

/**
 * Boost Motor Max Power Add. Defines how much more power than the target speed
 * the motors may supply to reach the target speed faster.
 * Lower number == softer, slower reached target speed.
 * Higher number == harder, faster reached target speed.
 * @constant {number}
 */
var BoostMotorMaxPowerAdd = 10;

/**
 * A time interval to wait (in milliseconds) in between battery check calls.
 * @type {number}
 */
var BoostPingInterval = 5000;

/**
 * The number of continuous samples the color-sensor will evaluate color from.
 * @type {number}
 */
var BoostColorSampleSize = 5;

/**
 * Enum for Boost sensor and actuator types.
 * @readonly
 * @enum {number}
 */
var BoostIO = {
  MOTOR_WEDO: 0x01,
  MOTOR_SYSTEM: 0x02,
  BUTTON: 0x05,
  LIGHT: 0x08,
  VOLTAGE: 0x14,
  CURRENT: 0x15,
  PIEZO: 0x16,
  LED: 0x17,
  TILT_EXTERNAL: 0x22,
  MOTION_SENSOR: 0x23,
  COLOR: 0x25,
  MOTOREXT: 0x26,
  MOTORINT: 0x27,
  TILT: 0x28
};

/**
 * Enum for ids for various output command feedback types on the Boost.
 * @readonly
 * @enum {number}
 */
var BoostPortFeedback = {
  IN_PROGRESS: 0x01,
  COMPLETED: 0x02,
  DISCARDED: 0x04,
  IDLE: 0x08,
  BUSY_OR_FULL: 0x10
};

/**
 * Enum for physical Boost Ports
 * @readonly
 * @enum {number}
 */

var BoostPort10000223OrOlder = {
  A: 55,
  B: 56,
  C: 1,
  D: 2
};
var BoostPort10000224OrNewer = {
  A: 0,
  B: 1,
  C: 2,
  D: 3
};

// Set default port mapping to support the newer firmware
var BoostPort = BoostPort10000224OrNewer;

/**
 * Ids for each color sensor value used by the extension.
 * @readonly
 * @enum {string}
 */
var BoostColor = {
  ANY: 'any',
  NONE: 'none',
  RED: 'red',
  BLUE: 'blue',
  GREEN: 'green',
  YELLOW: 'yellow',
  WHITE: 'white',
  BLACK: 'black'
};

/**
 * Enum for indices for each color sensed by the Boost vision sensor.
 * @readonly
 * @enum {number}
 */
var BoostColorIndex = _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, BoostColor.NONE, 255), BoostColor.RED, 9), BoostColor.BLUE, 3), BoostColor.GREEN, 5), BoostColor.YELLOW, 7), BoostColor.WHITE, 10), BoostColor.BLACK, 0);

/**
 * Enum for Message Types
 * @readonly
 * @enum {number}
 */
var BoostMessage = {
  HUB_PROPERTIES: 0x01,
  HUB_ACTIONS: 0x02,
  HUB_ALERTS: 0x03,
  HUB_ATTACHED_IO: 0x04,
  ERROR: 0x05,
  PORT_INPUT_FORMAT_SETUP_SINGLE: 0x41,
  PORT_INPUT_FORMAT_SETUP_COMBINED: 0x42,
  PORT_INFORMATION: 0x43,
  PORT_MODEINFORMATION: 0x44,
  PORT_VALUE: 0x45,
  PORT_VALUE_COMBINED: 0x46,
  PORT_INPUT_FORMAT: 0x47,
  PORT_INPUT_FORMAT_COMBINED: 0x48,
  OUTPUT: 0x81,
  PORT_FEEDBACK: 0x82
};

/**
 * Enum for Hub Property Types
 * @readonly
 * @enum {number}
 */

var BoostHubProperty = {
  ADVERTISEMENT_NAME: 0x01,
  BUTTON: 0x02,
  FW_VERSION: 0x03,
  HW_VERSION: 0x04,
  RSSI: 0x05,
  BATTERY_VOLTAGE: 0x06,
  BATTERY_TYPE: 0x07,
  MANUFACTURER_NAME: 0x08,
  RADIO_FW_VERSION: 0x09,
  LEGO_WP_VERSION: 0x0A,
  SYSTEM_TYPE_ID: 0x0B,
  HW_NETWORK_ID: 0x0C,
  PRIMARY_MAC: 0x0D,
  SECONDARY_MAC: 0x0E,
  HW_NETWORK_FAMILY: 0x0F
};

/**
 * Enum for Hub Property Operations
 * @readonly
 * @enum {number}
 */

var BoostHubPropertyOperation = {
  SET: 0x01,
  ENABLE_UPDATES: 0x02,
  DISABLE_UPDATES: 0x03,
  RESET: 0x04,
  REQUEST_UPDATE: 0x05,
  UPDATE: 0x06
};

/**
 * Enum for Motor Subcommands (for 0x81)
 * @readonly
 * @enum {number}
 */
var BoostOutputSubCommand = {
  START_POWER: 0x01,
  START_POWER_PAIR: 0x02,
  SET_ACC_TIME: 0x05,
  SET_DEC_TIME: 0x06,
  START_SPEED: 0x07,
  START_SPEED_PAIR: 0x08,
  START_SPEED_FOR_TIME: 0x09,
  START_SPEED_FOR_TIME_PAIR: 0x0A,
  START_SPEED_FOR_DEGREES: 0x0B,
  START_SPEED_FOR_DEGREES_PAIR: 0x0C,
  GO_TO_ABS_POSITION: 0x0D,
  GO_TO_ABS_POSITION_PAIR: 0x0E,
  PRESET_ENCODER: 0x14,
  WRITE_DIRECT_MODE_DATA: 0x51
};

/**
 * Enum for Startup/Completion information for an output command.
 * Startup and completion bytes must be OR'ed to be combined to a single byte.
 * @readonly
 * @enum {number}
 */
var BoostOutputExecution = {
  // Startup information
  BUFFER_IF_NECESSARY: 0x00,
  EXECUTE_IMMEDIATELY: 0x10,
  // Completion information
  NO_ACTION: 0x00,
  COMMAND_FEEDBACK: 0x01
};

/**
 * Enum for Boost Motor end states
 * @readonly
 * @enum {number}
 */
var BoostMotorEndState = {
  FLOAT: 0,
  HOLD: 126,
  BRAKE: 127
};

/**
 * Enum for Boost Motor acceleration/deceleration profiles
 * @readyonly
 * @enum {number}
 */
var BoostMotorProfile = {
  DO_NOT_USE: 0x00,
  ACCELERATION: 0x01,
  DECELERATION: 0x02
};

/**
 * Enum for when Boost IO's are attached/detached
 * @readonly
 * @enum {number}
 */
var BoostIOEvent = {
  ATTACHED: 0x01,
  DETACHED: 0x00,
  ATTACHED_VIRTUAL: 0x02
};

/**
 * Enum for selected sensor modes.
 * @enum {number}
 */
var BoostMode = {
  TILT: 0,
  // angle (pitch/yaw)
  LED: 1,
  // Set LED to accept RGB values
  COLOR: 0,
  // Read indexed colors from Vision Sensor
  MOTOR_SENSOR: 2,
  // Set motors to report their position
  UNKNOWN: 0 // Anything else will use the default mode (mode 0)
};

/**
 * Enum for Boost motor states.
 * @param {number}
 */
var BoostMotorState = {
  OFF: 0,
  ON_FOREVER: 1,
  ON_FOR_TIME: 2,
  ON_FOR_ROTATION: 3
};

/**
 * Helper function for converting a JavaScript number to an INT32-number
 * @param {number} number - a number
 * @return {array} - a 4-byte array of Int8-values representing an INT32-number
 */
var numberToInt32Array = function numberToInt32Array(number) {
  var buffer = new ArrayBuffer(4);
  var dataview = new DataView(buffer);
  dataview.setInt32(0, number);
  return [dataview.getInt8(3), dataview.getInt8(2), dataview.getInt8(1), dataview.getInt8(0)];
};

/**
 * Helper function for converting a regular array to a Little Endian INT32-value
 * @param {Array} array - an array containing UInt8-values
 * @return {number} - a number
 */
var int32ArrayToNumber = function int32ArrayToNumber(array) {
  var i = Uint8Array.from(array);
  var d = new DataView(i.buffer);
  return d.getInt32(0, true);
};

/**
 * Manage power, direction, position, and timers for one Boost motor.
 */
var BoostMotor = /*#__PURE__*/function () {
  /**
   * Construct a Boost Motor instance.
   * @param {Boost} parent - the Boost peripheral which owns this motor.
   * @param {int} index - the zero-based index of this motor on its parent peripheral.
   */
  function BoostMotor(parent, index) {
    _classCallCheck(this, BoostMotor);
    /**
     * The Boost peripheral which owns this motor.
     * @type {Boost}
     * @private
     */
    this._parent = parent;

    /**
     * The zero-based index of this motor on its parent peripheral.
     * @type {int}
     * @private
     */
    this._index = index;

    /**
     * This motor's current direction: 1 for "this way" or -1 for "that way"
     * @type {number}
     * @private
     */
    this._direction = 1;

    /**
     * This motor's current power level, in the range [0,100].
     * @type {number}
     * @private
     */
    this._power = 50;

    /**
     * This motor's current relative position
     * @type {number}
     * @private
     */
    this._position = 0;

    /**
     * Is this motor currently moving?
     * @type {boolean}
     * @private
     */
    this._status = BoostMotorState.OFF;

    /**
     * If the motor has been turned on or is actively braking for a specific duration, this is the timeout ID for
     * the end-of-action handler. Cancel this when changing plans.
     * @type {Object}
     * @private
     */
    this._pendingDurationTimeoutId = null;

    /**
     * The starting time for the pending duration timeout.
     * @type {number}
     * @private
     */
    this._pendingDurationTimeoutStartTime = null;

    /**
     * The delay/duration of the pending duration timeout.
     * @type {number}
     * @private
     */
    this._pendingDurationTimeoutDelay = null;

    /**
     * The target position of a turn-based command.
     * @type {number}
     * @private
     */
    this._pendingRotationDestination = null;

    /**
     * If the motor has been turned on run for a specific rotation, this is the function
     * that will be called once Scratch VM gets a notification from the Move Hub.
     * @type {Object}
     * @private
     */
    this._pendingRotationPromise = null;
    this.turnOff = this.turnOff.bind(this);
  }

  /**
   * @return {int} - this motor's current direction: 1 for "this way" or -1 for "that way"
   */
  return _createClass(BoostMotor, [{
    key: "direction",
    get: function get() {
      return this._direction;
    }

    /**
     * @param {int} value - this motor's new direction: 1 for "this way" or -1 for "that way"
     */,
    set: function set(value) {
      if (value < 0) {
        this._direction = -1;
      } else {
        this._direction = 1;
      }
    }

    /**
     * @return {int} - this motor's current power level, in the range [0,100].
     */
  }, {
    key: "power",
    get: function get() {
      return this._power;
    }

    /**
     * @param {int} value - this motor's new power level, in the range [10,100].
     */,
    set: function set(value) {
      /**
       * Scale the motor power to a range between 10 and 100,
       * to make sure the motors will run with something built onto them.
       */
      if (value === 0) {
        this._power = 0;
      } else {
        this._power = MathUtil.scale(value, 1, 100, 10, 100);
      }
    }

    /**
     * @return {int} - this motor's current position, in the range of [-MIN_INT32,MAX_INT32]
     */
  }, {
    key: "position",
    get: function get() {
      return this._position;
    }

    /**
     * @param {int} value - set this motor's current position.
     */,
    set: function set(value) {
      this._position = value;
    }

    /**
     * @return {BoostMotorState} - the motor's current state.
     */
  }, {
    key: "status",
    get: function get() {
      return this._status;
    }

    /**
     * @param {BoostMotorState} value - set this motor's state.
     */,
    set: function set(value) {
      this._clearRotationState();
      this._clearDurationTimeout();
      this._status = value;
    }

    /**
     * @return {number} - time, in milliseconds, of when the pending duration timeout began.
     */
  }, {
    key: "pendingDurationTimeoutStartTime",
    get: function get() {
      return this._pendingDurationTimeoutStartTime;
    }

    /**
     * @return {number} - delay, in milliseconds, of the pending duration timeout.
     */
  }, {
    key: "pendingDurationTimeoutDelay",
    get: function get() {
      return this._pendingDurationTimeoutDelay;
    }

    /**
     * @return {number} - target position, in degrees, of the pending rotation.
     */
  }, {
    key: "pendingRotationDestination",
    get: function get() {
      return this._pendingRotationDestination;
    }

    /**
     * @return {Promise} - the Promise function for the pending rotation.
     */
  }, {
    key: "pendingRotationPromise",
    get: function get() {
      return this._pendingRotationPromise;
    }

    /**
     * @param {function} func - function to resolve pending rotation Promise
     */,
    set: function set(func) {
      this._pendingRotationPromise = func;
    }

    /**
     * Turn this motor on indefinitely
     * @private
     */
  }, {
    key: "_turnOn",
    value: function _turnOn() {
      var cmd = this._parent.generateOutputCommand(this._index, BoostOutputExecution.EXECUTE_IMMEDIATELY, BoostOutputSubCommand.START_SPEED, [this.power * this.direction, MathUtil.clamp(this.power + BoostMotorMaxPowerAdd, 0, 100), BoostMotorProfile.DO_NOT_USE]);
      this._parent.send(BoostBLE.characteristic, cmd);
    }

    /**
     * Turn this motor on indefinitely
     */
  }, {
    key: "turnOnForever",
    value: function turnOnForever() {
      this.status = BoostMotorState.ON_FOREVER;
      this._turnOn();
    }

    /**
     * Turn this motor on for a specific duration.
     * @param {number} milliseconds - run the motor for this long.
     */
  }, {
    key: "turnOnFor",
    value: function turnOnFor(milliseconds) {
      milliseconds = Math.max(0, milliseconds);
      this.status = BoostMotorState.ON_FOR_TIME;
      this._turnOn();
      this._setNewDurationTimeout(this.turnOff, milliseconds);
    }

    /**
     * Turn this motor on for a specific rotation in degrees.
     * @param {number} degrees - run the motor for this amount of degrees.
     * @param {number} direction - rotate in this direction
     */
  }, {
    key: "turnOnForDegrees",
    value: function turnOnForDegrees(degrees, direction) {
      degrees = Math.max(0, degrees);
      var cmd = this._parent.generateOutputCommand(this._index, BoostOutputExecution.EXECUTE_IMMEDIATELY ^ BoostOutputExecution.COMMAND_FEEDBACK, BoostOutputSubCommand.START_SPEED_FOR_DEGREES, [].concat(_toConsumableArray(numberToInt32Array(degrees)), [this.power * this.direction * direction, MathUtil.clamp(this.power + BoostMotorMaxPowerAdd, 0, 100), BoostMotorEndState.BRAKE, BoostMotorProfile.DO_NOT_USE]));
      this.status = BoostMotorState.ON_FOR_ROTATION;
      this._pendingRotationDestination = this.position + degrees * this.direction * direction;
      this._parent.send(BoostBLE.characteristic, cmd);
    }

    /**
     * Turn this motor off.
     * @param {boolean} [useLimiter=true] - if true, use the rate limiter
     */
  }, {
    key: "turnOff",
    value: function turnOff() {
      var useLimiter = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
      var cmd = this._parent.generateOutputCommand(this._index, BoostOutputExecution.EXECUTE_IMMEDIATELY, BoostOutputSubCommand.START_POWER, [BoostMotorEndState.FLOAT]);
      this.status = BoostMotorState.OFF;
      this._parent.send(BoostBLE.characteristic, cmd, useLimiter);
    }

    /**
     * Clear the motor action timeout, if any. Safe to call even when there is no pending timeout.
     * @private
     */
  }, {
    key: "_clearDurationTimeout",
    value: function _clearDurationTimeout() {
      if (this._pendingDurationTimeoutId !== null) {
        clearTimeout(this._pendingDurationTimeoutId);
        this._pendingDurationTimeoutId = null;
        this._pendingDurationTimeoutStartTime = null;
        this._pendingDurationTimeoutDelay = null;
      }
    }

    /**
     * Set a new motor action timeout, after clearing an existing one if necessary.
     * @param {Function} callback - to be called at the end of the timeout.
     * @param {int} delay - wait this many milliseconds before calling the callback.
     * @private
     */
  }, {
    key: "_setNewDurationTimeout",
    value: function _setNewDurationTimeout(callback, delay) {
      var _this = this;
      this._clearDurationTimeout();
      var timeoutID = setTimeout(function () {
        if (_this._pendingDurationTimeoutId === timeoutID) {
          _this._pendingDurationTimeoutId = null;
          _this._pendingDurationTimeoutStartTime = null;
          _this._pendingDurationTimeoutDelay = null;
        }
        callback();
      }, delay);
      this._pendingDurationTimeoutId = timeoutID;
      this._pendingDurationTimeoutStartTime = Date.now();
      this._pendingDurationTimeoutDelay = delay;
    }

    /**
     * Clear the motor states related to rotation-based commands, if any.
     * Safe to call even when there is no pending promise function.
     * @private
     */
  }, {
    key: "_clearRotationState",
    value: function _clearRotationState() {
      if (this._pendingRotationPromise !== null) {
        this._pendingRotationPromise();
        this._pendingRotationPromise = null;
      }
      this._pendingRotationDestination = null;
    }
  }]);
}();
/**
 * Manage communication with a Boost peripheral over a Bluetooth Low Energy client socket.
 */
var Boost = /*#__PURE__*/function () {
  function Boost(runtime, extensionId) {
    _classCallCheck(this, Boost);
    /**
     * The Scratch 3.0 runtime used to trigger the green flag button.
     * @type {Runtime}
     * @private
     */
    this._runtime = runtime;
    this._runtime.on('PROJECT_STOP_ALL', this.stopAll.bind(this));

    /**
     * The id of the extension this peripheral belongs to.
     */
    this._extensionId = extensionId;

    /**
     * A list of the ids of the physical or virtual sensors.
     * @type {string[]}
     * @private
     */
    this._ports = [];

    /**
     * A list of motors registered by the Boost hardware.
     * @type {BoostMotor[]}
     * @private
     */
    this._motors = [];

    /**
     * The most recently received value for each sensor.
     * @type {Object.<string, number>}
     * @private
     */
    this._sensors = {
      tiltX: 0,
      tiltY: 0,
      color: BoostColor.NONE,
      previousColor: BoostColor.NONE
    };

    /**
     * An array of values from the Boost Vision Sensor.
     * @type {Array}
     * @private
     */
    this._colorSamples = [];

    /**
     * The Bluetooth connection socket for reading/writing peripheral data.
     * @type {BLE}
     * @private
     */
    this._ble = null;
    this._runtime.registerPeripheralExtension(extensionId, this);

    /**
     * A rate limiter utility, to help limit the rate at which we send BLE messages
     * over the socket to Scratch Link to a maximum number of sends per second.
     * @type {RateLimiter}
     * @private
     */
    this._rateLimiter = new RateLimiter(BoostBLE.sendRateMax);

    /**
     * An interval id for the battery check interval.
     * @type {number}
     * @private
     */
    this._pingDeviceId = null;
    this.reset = this.reset.bind(this);
    this._onConnect = this._onConnect.bind(this);
    this._onMessage = this._onMessage.bind(this);
    this._pingDevice = this._pingDevice.bind(this);
  }

  /**
   * @return {number} - the latest value received for the tilt sensor's tilt about the X axis.
   */
  return _createClass(Boost, [{
    key: "tiltX",
    get: function get() {
      return this._sensors.tiltX;
    }

    /**
     * @return {number} - the latest value received for the tilt sensor's tilt about the Y axis.
     */
  }, {
    key: "tiltY",
    get: function get() {
      return this._sensors.tiltY;
    }

    /**
     * @return {number} - the latest color value received from the vision sensor.
     */
  }, {
    key: "color",
    get: function get() {
      return this._sensors.color;
    }

    /**
     * @return {number} - the previous color value received from the vision sensor.
     */
  }, {
    key: "previousColor",
    get: function get() {
      return this._sensors.previousColor;
    }

    /**
     * Look up the color id for an index received from the vision sensor.
     * @param {number} index - the color index to look up.
     * @return {BoostColor} the color id for this index.
     */
  }, {
    key: "boostColorForIndex",
    value: function boostColorForIndex(index) {
      var colorForIndex = Object.keys(BoostColorIndex).find(function (key) {
        return BoostColorIndex[key] === index;
      });
      return colorForIndex || BoostColor.NONE;
    }

    /**
     * Access a particular motor on this peripheral.
     * @param {int} index - the index of the desired motor.
     * @return {BoostMotor} - the BoostMotor instance, if any, at that index.
     */
  }, {
    key: "motor",
    value: function motor(index) {
      return this._motors[index];
    }

    /**
     * Stop all the motors that are currently running.
     */
  }, {
    key: "stopAllMotors",
    value: function stopAllMotors() {
      this._motors.forEach(function (motor) {
        if (motor) {
          // Send the motor off command without using the rate limiter.
          // This allows the stop button to stop motors even if we are
          // otherwise flooded with commands.
          motor.turnOff(false);
        }
      });
    }

    /**
     * Set the Boost peripheral's LED to a specific color.
     * @param {int} inputRGB - a 24-bit RGB color in 0xRRGGBB format.
     * @return {Promise} - a promise of the completion of the set led send operation.
     */
  }, {
    key: "setLED",
    value: function setLED(inputRGB) {
      var rgb = [inputRGB >> 16 & 0x000000FF, inputRGB >> 8 & 0x000000FF, inputRGB & 0x000000FF];
      var cmd = this.generateOutputCommand(this._ports.indexOf(BoostIO.LED), BoostOutputExecution.EXECUTE_IMMEDIATELY ^ BoostOutputExecution.COMMAND_FEEDBACK, BoostOutputSubCommand.WRITE_DIRECT_MODE_DATA, [BoostMode.LED].concat(rgb));
      return this.send(BoostBLE.characteristic, cmd);
    }

    /**
     * Sets the input mode of the LED to RGB.
     * @return {Promise} - a promise returned by the send operation.
     */
  }, {
    key: "setLEDMode",
    value: function setLEDMode() {
      var cmd = this.generateInputCommand(this._ports.indexOf(BoostIO.LED), BoostMode.LED, 0, false);
      return this.send(BoostBLE.characteristic, cmd);
    }

    /**
     * Stop the motors on the Boost peripheral.
     */
  }, {
    key: "stopAll",
    value: function stopAll() {
      if (!this.isConnected()) return;
      this.stopAllMotors();
    }

    /**
     * Called by the runtime when user wants to scan for a Boost peripheral.
     */
  }, {
    key: "scan",
    value: function scan() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this._ble = new BLE(this._runtime, this._extensionId, {
        filters: [{
          services: [BoostBLE.service],
          manufacturerData: {
            0x0397: {
              dataPrefix: [0x00, 0x40],
              mask: [0x00, 0xFF]
            }
          }
        }],
        optionalServices: []
      }, this._onConnect, this.reset);
    }

    /**
     * Called by the runtime when user wants to connect to a certain Boost peripheral.
     * @param {number} id - the id of the peripheral to connect to.
     */
  }, {
    key: "connect",
    value: function connect(id) {
      if (this._ble) {
        this._ble.connectPeripheral(id);
      }
    }

    /**
     * Disconnects from the current BLE socket and resets state.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this.reset();
    }

    /**
     * Reset all the state and timeout/interval ids.
     */
  }, {
    key: "reset",
    value: function reset() {
      this._ports = [];
      this._motors = [];
      this._sensors = {
        tiltX: 0,
        tiltY: 0,
        color: BoostColor.NONE,
        previousColor: BoostColor.NONE
      };
      if (this._pingDeviceId) {
        window.clearInterval(this._pingDeviceId);
        this._pingDeviceId = null;
      }
    }

    /**
     * Called by the runtime to detect whether the Boost peripheral is connected.
     * @return {boolean} - the connected state.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      var connected = false;
      if (this._ble) {
        connected = this._ble.isConnected();
      }
      return connected;
    }

    /**
     * Write a message to the Boost peripheral BLE socket.
     * @param {number} uuid - the UUID of the characteristic to write to
     * @param {Array} message - the message to write.
     * @param {boolean} [useLimiter=true] - if true, use the rate limiter
     * @return {Promise} - a promise result of the write operation
     */
  }, {
    key: "send",
    value: function send(uuid, message) {
      var useLimiter = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
      if (!this.isConnected()) return Promise.resolve();
      if (useLimiter) {
        if (!this._rateLimiter.okayToSend()) return Promise.resolve();
      }
      return this._ble.write(BoostBLE.service, uuid, Base64Util.uint8ArrayToBase64(message), 'base64');
    }

    /**
     * Generate a Boost 'Output Command' in the byte array format
     * (COMMON HEADER, PORT ID, EXECUTION BYTE, SUBCOMMAND ID, PAYLOAD).
     *
     * Payload is accepted as an array since these vary across different subcommands.
     *
     * @param  {number} portID - the port (Connect ID) to send a command to.
     * @param  {number} execution - Byte containing startup/completion information
     * @param  {number} subCommand - the id of the subcommand byte.
     * @param  {array}  payload    - the list of bytes to send as subcommand payload
     * @return {array}            - a generated output command.
     */
  }, {
    key: "generateOutputCommand",
    value: function generateOutputCommand(portID, execution, subCommand, payload) {
      var hubID = 0x00;
      var command = [hubID, BoostMessage.OUTPUT, portID, execution, subCommand].concat(_toConsumableArray(payload));
      command.unshift(command.length + 1); // Prepend payload with length byte;

      return command;
    }

    /**
     * Generate a Boost 'Input Command' in the byte array format
     * (COMMAND ID, COMMAND TYPE, CONNECT ID, TYPE ID, MODE, DELTA INTERVAL (4 BYTES),
     * UNIT, NOTIFICATIONS ENABLED).
     *
     * This sends a command to the Boost that sets that input format
     * of the specified inputs and sets value change notifications.
     *
     * @param  {number}  portID           - the port (Connect ID) to send a command to.
     * @param  {number}  mode                - the mode of the input sensor.
     * @param  {number}  delta               - the delta change needed to trigger notification.
     * @param  {boolean} enableNotifications - whether to enable notifications.
     * @return {array}                       - a generated input command.
     */
  }, {
    key: "generateInputCommand",
    value: function generateInputCommand(portID, mode, delta, enableNotifications) {
      var command = [0x00,
      // Hub ID
      BoostMessage.PORT_INPUT_FORMAT_SETUP_SINGLE, portID, mode].concat(numberToInt32Array(delta)).concat([enableNotifications]);
      command.unshift(command.length + 1); // Prepend payload with length byte;

      return command;
    }

    /**
     * Starts reading data from peripheral after BLE has connected.
     * @private
     */
  }, {
    key: "_onConnect",
    value: function _onConnect() {
      var _this2 = this;
      this._ble.startNotifications(BoostBLE.service, BoostBLE.characteristic, this._onMessage);
      this._pingDeviceId = window.setInterval(this._pingDevice, BoostPingInterval);

      // Send a request for firmware version.
      setTimeout(function () {
        var command = [0x00,
        // Hub ID
        BoostMessage.HUB_PROPERTIES, BoostHubProperty.FW_VERSION, BoostHubPropertyOperation.REQUEST_UPDATE];
        command.unshift(command.length + 1);
        _this2.send(BoostBLE.characteristic, command, false);
      }, 500);
    }

    /**
     * Process the sensor data from the incoming BLE characteristic.
     * @param {object} base64 - the incoming BLE data.
     * @private
     */
  }, {
    key: "_onMessage",
    value: function _onMessage(base64) {
      var data = Base64Util.base64ToUint8Array(base64);

      /**
       * First three bytes are the common header:
       * 0: Length of message
       * 1: Hub ID (always 0x00 at the moment, unused)
       * 2: Message Type
       * 3: Port ID
       * We base our switch-case on Message Type
       */

      var messageType = data[2];
      var portID = data[3];
      switch (messageType) {
        case BoostMessage.HUB_PROPERTIES:
          {
            var property = data[3];
            switch (property) {
              case BoostHubProperty.FW_VERSION:
                {
                  // Establish firmware version 1.0.00.0224 as a 32-bit signed integer (little endian)
                  var fwVersion10000224 = int32ArrayToNumber([0x24, 0x02, 0x00, 0x10]);
                  var fwHub = int32ArrayToNumber(data.slice(5, data.length));
                  if (fwHub < fwVersion10000224) {
                    BoostPort = BoostPort10000223OrOlder;
                    log.info('Move Hub firmware older than version 1.0.00.0224 detected. Using old port mapping.');
                  } else {
                    BoostPort = BoostPort10000224OrNewer;
                  }
                  break;
                }
            }
            break;
          }
        case BoostMessage.HUB_ATTACHED_IO:
          {
            // IO Attach/Detach events
            var event = data[4];
            var typeId = data[5];
            switch (event) {
              case BoostIOEvent.ATTACHED:
                this._registerSensorOrMotor(portID, typeId);
                break;
              case BoostIOEvent.DETACHED:
                this._clearPort(portID);
                break;
              case BoostIOEvent.ATTACHED_VIRTUAL:
              default:
            }
            break;
          }
        case BoostMessage.PORT_VALUE:
          {
            var type = this._ports[portID];
            switch (type) {
              case BoostIO.TILT:
                this._sensors.tiltX = data[4];
                this._sensors.tiltY = data[5];
                break;
              case BoostIO.COLOR:
                this._colorSamples.unshift(data[4]);
                if (this._colorSamples.length > BoostColorSampleSize) {
                  this._colorSamples.pop();
                  if (this._colorSamples.every(function (v, i, arr) {
                    return v === arr[0];
                  })) {
                    this._sensors.previousColor = this._sensors.color;
                    this._sensors.color = this.boostColorForIndex(this._colorSamples[0]);
                  } else {
                    this._sensors.color = BoostColor.NONE;
                  }
                } else {
                  this._sensors.color = BoostColor.NONE;
                }
                break;
              case BoostIO.MOTOREXT:
              case BoostIO.MOTORINT:
                this.motor(portID).position = int32ArrayToNumber(data.slice(4, 8));
                break;
              case BoostIO.CURRENT:
              case BoostIO.VOLTAGE:
              case BoostIO.LED:
                break;
              default:
                log.warn("Unknown sensor value! Type: ".concat(type));
            }
            break;
          }
        case BoostMessage.PORT_FEEDBACK:
          {
            var feedback = data[4];
            var motor = this.motor(portID);
            if (motor) {
              // Makes sure that commands resolve both when they actually complete and when they fail
              var isBusy = feedback & BoostPortFeedback.IN_PROGRESS;
              var commandCompleted = feedback & (BoostPortFeedback.COMPLETED ^ BoostPortFeedback.DISCARDED);
              if (!isBusy && commandCompleted) {
                if (motor.status === BoostMotorState.ON_FOR_ROTATION) {
                  motor.status = BoostMotorState.OFF;
                }
              }
            }
            break;
          }
        case BoostMessage.ERROR:
          log.warn("Error reported by hub: ".concat(data));
          break;
      }
    }

    /**
     * Ping the Boost hub. If the Boost hub has disconnected
     * for some reason, the BLE socket will get an error back and automatically
     * close the socket.
     * @private
     */
  }, {
    key: "_pingDevice",
    value: function _pingDevice() {
      this._ble.read(BoostBLE.service, BoostBLE.characteristic, false);
    }

    /**
     * Register a new sensor or motor connected at a port.  Store the type of
     * sensor or motor internally, and then register for notifications on input
     * values if it is a sensor.
     * @param {number} portID - the port to register a sensor or motor on.
     * @param {number} type - the type ID of the sensor or motor
     * @private
     */
  }, {
    key: "_registerSensorOrMotor",
    value: function _registerSensorOrMotor(portID, type) {
      // Record which port is connected to what type of device
      this._ports[portID] = type;

      // Record motor port
      if (type === BoostIO.MOTORINT || type === BoostIO.MOTOREXT) {
        this._motors[portID] = new BoostMotor(this, portID);
      }

      // Set input format for tilt or distance sensor
      var mode = null;
      var delta = 1;
      switch (type) {
        case BoostIO.MOTORINT:
        case BoostIO.MOTOREXT:
          mode = BoostMode.MOTOR_SENSOR;
          break;
        case BoostIO.COLOR:
          mode = BoostMode.COLOR;
          delta = 0;
          break;
        case BoostIO.LED:
          mode = BoostMode.LED;
          /**
           * Sets the LED to blue to give an indication on the hub
           * that it has connected successfully.
           */
          this.setLEDMode();
          this.setLED(0x0000FF);
          break;
        case BoostIO.TILT:
          mode = BoostMode.TILT;
          break;
        default:
          mode = BoostMode.UNKNOWN;
      }
      var cmd = this.generateInputCommand(portID, mode, delta, true // Receive feedback
      );
      this.send(BoostBLE.characteristic, cmd);
    }

    /**
     * Clear the sensors or motors present on the ports.
     * @param {number} portID - the port to clear.
     * @private
     */
  }, {
    key: "_clearPort",
    value: function _clearPort(portID) {
      var type = this._ports[portID];
      if (type === BoostIO.TILT) {
        this._sensors.tiltX = this._sensors.tiltY = 0;
      }
      if (type === BoostIO.COLOR) {
        this._sensors.color = BoostColor.NONE;
      }
      this._ports[portID] = 'none';
      this._motors[portID] = null;
    }
  }]);
}();
/**
 * Enum for motor specification.
 * @readonly
 * @enum {string}
 */
var BoostMotorLabel = {
  A: 'A',
  B: 'B',
  C: 'C',
  D: 'D',
  AB: 'AB',
  ALL: 'ABCD'
};

/**
 * Enum for motor direction specification.
 * @readonly
 * @enum {string}
 */
var BoostMotorDirection = {
  FORWARD: 'this way',
  BACKWARD: 'that way',
  REVERSE: 'reverse'
};

/**
 * Enum for tilt sensor direction.
 * @readonly
 * @enum {string}
 */
var BoostTiltDirection = {
  UP: 'up',
  DOWN: 'down',
  LEFT: 'left',
  RIGHT: 'right',
  ANY: 'any'
};

/**
 * Scratch 3.0 blocks to interact with a LEGO Boost peripheral.
 */
var Scratch3BoostBlocks = /*#__PURE__*/function () {
  /**
   * Construct a set of Boost blocks.
   * @param {Runtime} runtime - the Scratch 3.0 runtime.
   */
  function Scratch3BoostBlocks(runtime) {
    _classCallCheck(this, Scratch3BoostBlocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;

    // Create a new Boost peripheral instance
    this._peripheral = new Boost(this.runtime, Scratch3BoostBlocks.EXTENSION_ID);
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(Scratch3BoostBlocks, [{
    key: "getInfo",
    value: function getInfo() {
      return {
        id: Scratch3BoostBlocks.EXTENSION_ID,
        name: 'BOOST',
        blockIconURI: iconURI,
        showStatusButton: true,
        blocks: [{
          opcode: 'motorOnFor',
          text: formatMessage({
            id: 'boost.motorOnFor',
            "default": 'turn motor [MOTOR_ID] for [DURATION] seconds',
            description: 'turn a motor on for some time'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: BoostMotorLabel.A
            },
            DURATION: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          }
        }, {
          opcode: 'motorOnForRotation',
          text: formatMessage({
            id: 'boost.motorOnForRotation',
            "default": 'turn motor [MOTOR_ID] for [ROTATION] rotations',
            description: 'turn a motor on for rotation'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: BoostMotorLabel.A
            },
            ROTATION: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          }
        }, {
          opcode: 'motorOn',
          text: formatMessage({
            id: 'boost.motorOn',
            "default": 'turn motor [MOTOR_ID] on',
            description: 'turn a motor on indefinitely'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: BoostMotorLabel.A
            }
          }
        }, {
          opcode: 'motorOff',
          text: formatMessage({
            id: 'boost.motorOff',
            "default": 'turn motor [MOTOR_ID] off',
            description: 'turn a motor off'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: BoostMotorLabel.A
            }
          }
        }, {
          opcode: 'setMotorPower',
          text: formatMessage({
            id: 'boost.setMotorPower',
            "default": 'set motor [MOTOR_ID] speed to [POWER] %',
            description: 'set the motor\'s speed without turning it on'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: BoostMotorLabel.ALL
            },
            POWER: {
              type: ArgumentType.NUMBER,
              defaultValue: 100
            }
          }
        }, {
          opcode: 'setMotorDirection',
          text: formatMessage({
            id: 'boost.setMotorDirection',
            "default": 'set motor [MOTOR_ID] direction [MOTOR_DIRECTION]',
            description: 'set the motor\'s turn direction without turning it on'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: BoostMotorLabel.A
            },
            MOTOR_DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_DIRECTION',
              defaultValue: BoostMotorDirection.FORWARD
            }
          }
        }, {
          opcode: 'getMotorPosition',
          text: formatMessage({
            id: 'boost.getMotorPosition',
            "default": 'motor [MOTOR_REPORTER_ID] position',
            description: 'the position returned by the motor'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            MOTOR_REPORTER_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_REPORTER_ID',
              defaultValue: BoostMotorLabel.A
            }
          }
        }, {
          opcode: 'whenColor',
          text: formatMessage({
            id: 'boost.whenColor',
            "default": 'when [COLOR] brick seen',
            description: 'check for when color'
          }),
          blockType: BlockType.HAT,
          arguments: {
            COLOR: {
              type: ArgumentType.STRING,
              menu: 'COLOR',
              defaultValue: BoostColor.ANY
            }
          }
        }, {
          opcode: 'seeingColor',
          text: formatMessage({
            id: 'boost.seeingColor',
            "default": 'seeing [COLOR] brick?',
            description: 'is the color sensor seeing a certain color?'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            COLOR: {
              type: ArgumentType.STRING,
              menu: 'COLOR',
              defaultValue: BoostColor.ANY
            }
          }
        }, {
          opcode: 'whenTilted',
          text: formatMessage({
            id: 'boost.whenTilted',
            "default": 'when tilted [TILT_DIRECTION_ANY]',
            description: 'check when tilted in a certain direction'
          }),
          func: 'isTilted',
          blockType: BlockType.HAT,
          arguments: {
            TILT_DIRECTION_ANY: {
              type: ArgumentType.STRING,
              menu: 'TILT_DIRECTION_ANY',
              defaultValue: BoostTiltDirection.ANY
            }
          }
        }, {
          opcode: 'getTiltAngle',
          text: formatMessage({
            id: 'boost.getTiltAngle',
            "default": 'tilt angle [TILT_DIRECTION]',
            description: 'the angle returned by the tilt sensor'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            TILT_DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'TILT_DIRECTION',
              defaultValue: BoostTiltDirection.UP
            }
          }
        }, {
          opcode: 'setLightHue',
          text: formatMessage({
            id: 'boost.setLightHue',
            "default": 'set light color to [HUE]',
            description: 'set the LED color'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            HUE: {
              type: ArgumentType.NUMBER,
              defaultValue: 50
            }
          }
        }],
        menus: {
          MOTOR_ID: {
            acceptReporters: true,
            items: [{
              text: 'A',
              value: BoostMotorLabel.A
            }, {
              text: 'B',
              value: BoostMotorLabel.B
            }, {
              text: 'C',
              value: BoostMotorLabel.C
            }, {
              text: 'D',
              value: BoostMotorLabel.D
            }, {
              text: 'AB',
              value: BoostMotorLabel.AB
            }, {
              text: 'ABCD',
              value: BoostMotorLabel.ALL
            }]
          },
          MOTOR_REPORTER_ID: {
            acceptReporters: true,
            items: [{
              text: 'A',
              value: BoostMotorLabel.A
            }, {
              text: 'B',
              value: BoostMotorLabel.B
            }, {
              text: 'C',
              value: BoostMotorLabel.C
            }, {
              text: 'D',
              value: BoostMotorLabel.D
            }]
          },
          MOTOR_DIRECTION: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'boost.motorDirection.forward',
                "default": 'this way',
                description: 'label for forward element in motor direction menu for LEGO Boost extension'
              }),
              value: BoostMotorDirection.FORWARD
            }, {
              text: formatMessage({
                id: 'boost.motorDirection.backward',
                "default": 'that way',
                description: 'label for backward element in motor direction menu for LEGO Boost extension'
              }),
              value: BoostMotorDirection.BACKWARD
            }, {
              text: formatMessage({
                id: 'boost.motorDirection.reverse',
                "default": 'reverse',
                description: 'label for reverse element in motor direction menu for LEGO Boost extension'
              }),
              value: BoostMotorDirection.REVERSE
            }]
          },
          TILT_DIRECTION: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'boost.tiltDirection.up',
                "default": 'up',
                description: 'label for up element in tilt direction menu for LEGO Boost extension'
              }),
              value: BoostTiltDirection.UP
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.down',
                "default": 'down',
                description: 'label for down element in tilt direction menu for LEGO Boost extension'
              }),
              value: BoostTiltDirection.DOWN
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.left',
                "default": 'left',
                description: 'label for left element in tilt direction menu for LEGO Boost extension'
              }),
              value: BoostTiltDirection.LEFT
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.right',
                "default": 'right',
                description: 'label for right element in tilt direction menu for LEGO Boost extension'
              }),
              value: BoostTiltDirection.RIGHT
            }]
          },
          TILT_DIRECTION_ANY: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'boost.tiltDirection.up',
                "default": 'up'
              }),
              value: BoostTiltDirection.UP
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.down',
                "default": 'down'
              }),
              value: BoostTiltDirection.DOWN
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.left',
                "default": 'left'
              }),
              value: BoostTiltDirection.LEFT
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.right',
                "default": 'right'
              }),
              value: BoostTiltDirection.RIGHT
            }, {
              text: formatMessage({
                id: 'boost.tiltDirection.any',
                "default": 'any',
                description: 'label for any element in tilt direction menu for LEGO Boost extension'
              }),
              value: BoostTiltDirection.ANY
            }]
          },
          COLOR: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'boost.color.red',
                "default": 'red',
                description: 'the color red'
              }),
              value: BoostColor.RED
            }, {
              text: formatMessage({
                id: 'boost.color.blue',
                "default": 'blue',
                description: 'the color blue'
              }),
              value: BoostColor.BLUE
            }, {
              text: formatMessage({
                id: 'boost.color.green',
                "default": 'green',
                description: 'the color green'
              }),
              value: BoostColor.GREEN
            }, {
              text: formatMessage({
                id: 'boost.color.yellow',
                "default": 'yellow',
                description: 'the color yellow'
              }),
              value: BoostColor.YELLOW
            }, {
              text: formatMessage({
                id: 'boost.color.white',
                "default": 'white',
                desription: 'the color white'
              }),
              value: BoostColor.WHITE
            }, {
              text: formatMessage({
                id: 'boost.color.black',
                "default": 'black',
                description: 'the color black'
              }),
              value: BoostColor.BLACK
            }, {
              text: formatMessage({
                id: 'boost.color.any',
                "default": 'any color',
                description: 'any color'
              }),
              value: BoostColor.ANY
            }]
          }
        }
      };
    }

    /**
     * Turn specified motor(s) on for a specified duration.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to activate.
     * @property {int} DURATION - the amount of time to run the motors.
     * @return {Promise} - a promise which will resolve at the end of the duration.
     */
  }, {
    key: "motorOnFor",
    value: function motorOnFor(args) {
      var _this3 = this;
      // TODO: cast args.MOTOR_ID?
      var durationMS = Cast.toNumber(args.DURATION) * 1000;
      durationMS = MathUtil.clamp(durationMS, 0, 15000);
      return new Promise(function (resolve) {
        _this3._forEachMotor(args.MOTOR_ID, function (motorIndex) {
          var motor = _this3._peripheral.motor(motorIndex);
          if (motor) motor.turnOnFor(durationMS);
        });

        // Run for some time even when no motor is connected
        setTimeout(resolve, durationMS);
      });
    }

    /**
     * Turn specified motor(s) on for a specified rotation in full rotations.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to activate.
     * @property {int} ROTATION - the amount of full rotations to turn the motors.
     * @return {Promise} - a promise which will resolve at the end of the duration.
     */
  }, {
    key: "motorOnForRotation",
    value: function motorOnForRotation(args) {
      var _this4 = this;
      // TODO: cast args.MOTOR_ID?
      var degrees = Cast.toNumber(args.ROTATION) * 360;
      // TODO: Clamps to 100 rotations. Consider changing.
      var sign = Math.sign(degrees);
      degrees = Math.abs(MathUtil.clamp(degrees, -360000, 360000));
      var motors = [];
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        motors.push(motorIndex);
      });

      /**
       * Checks that the motors given in args.MOTOR_ID exist,
       * and maps a promise for each of the motor-commands to an array.
       */
      var promises = motors.map(function (portID) {
        var motor = _this4._peripheral.motor(portID);
        if (motor) {
          // to avoid a hanging block if power is 0, return an immediately resolving promise.
          if (motor.power === 0) return Promise.resolve();
          return new Promise(function (resolve) {
            motor.turnOnForDegrees(degrees, sign);
            motor.pendingRotationPromise = resolve;
          });
        }
        return null;
      });
      /**
       * Make sure all promises are resolved, i.e. all motor-commands have completed.
       * To prevent the block from returning a value, an empty function is added to the .then
       */
      return Promise.all(promises).then(function () {});
    }

    /**
     * Turn specified motor(s) on indefinitely.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to activate.
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "motorOn",
    value: function motorOn(args) {
      var _this5 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this5._peripheral.motor(motorIndex);
        if (motor) motor.turnOnForever();
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BoostBLE.sendInterval);
      });
    }

    /**
     * Turn specified motor(s) off.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to deactivate.
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "motorOff",
    value: function motorOff(args) {
      var _this6 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this6._peripheral.motor(motorIndex);
        if (motor) motor.turnOff();
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BoostBLE.sendInterval);
      });
    }

    /**
     * Set the power level of the specified motor(s).
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to be affected.
     * @property {int} POWER - the new power level for the motor(s).
     * @return {Promise} - returns a promise to make sure the block yields.
     */
  }, {
    key: "setMotorPower",
    value: function setMotorPower(args) {
      var _this7 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this7._peripheral.motor(motorIndex);
        if (motor) {
          motor.power = MathUtil.clamp(Cast.toNumber(args.POWER), 0, 100);
          switch (motor.status) {
            case BoostMotorState.ON_FOREVER:
              motor.turnOnForever();
              break;
            case BoostMotorState.ON_FOR_TIME:
              motor.turnOnFor(motor.pendingDurationTimeoutStartTime + motor.pendingDurationTimeoutDelay - Date.now());
              break;
          }
        }
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BoostBLE.sendInterval);
      });
    }

    /**
     * Set the direction of rotation for specified motor(s).
     * If the direction is 'reverse' the motor(s) will be reversed individually.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to be affected.
     * @property {MotorDirection} MOTOR_DIRECTION - the new direction for the motor(s).
     * @return {Promise} - returns a promise to make sure the block yields.
     */
  }, {
    key: "setMotorDirection",
    value: function setMotorDirection(args) {
      var _this8 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this8._peripheral.motor(motorIndex);
        if (motor) {
          switch (args.MOTOR_DIRECTION) {
            case BoostMotorDirection.FORWARD:
              motor.direction = 1;
              break;
            case BoostMotorDirection.BACKWARD:
              motor.direction = -1;
              break;
            case BoostMotorDirection.REVERSE:
              motor.direction = -motor.direction;
              break;
            default:
              log.warn("Unknown motor direction in setMotorDirection: ".concat(args.DIRECTION));
              break;
          }
          // keep the motor on if it's running, and update the pending timeout if needed
          if (motor) {
            switch (motor.status) {
              case BoostMotorState.ON_FOREVER:
                motor.turnOnForever();
                break;
              case BoostMotorState.ON_FOR_TIME:
                motor.turnOnFor(motor.pendingDurationTimeoutStartTime + motor.pendingDurationTimeoutDelay - Date.now());
                break;
            }
          }
        }
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BoostBLE.sendInterval);
      });
    }

    /**
     * @param {object} args - the block's arguments.
     * @return {number} - returns the motor's position.
     */
  }, {
    key: "getMotorPosition",
    value: function getMotorPosition(args) {
      var portID = null;
      switch (args.MOTOR_REPORTER_ID) {
        case BoostMotorLabel.A:
          portID = BoostPort.A;
          break;
        case BoostMotorLabel.B:
          portID = BoostPort.B;
          break;
        case BoostMotorLabel.C:
          portID = BoostPort.C;
          break;
        case BoostMotorLabel.D:
          portID = BoostPort.D;
          break;
        default:
          log.warn('Asked for a motor position that doesnt exist!');
          return false;
      }
      if (portID !== null && this._peripheral.motor(portID)) {
        var val = this._peripheral.motor(portID).position;
        // Boost motor A position direction is reversed by design
        // so we have to reverse the position here
        if (portID === BoostPort.A) {
          val *= -1;
        }
        return MathUtil.wrapClamp(val, 0, 360);
      }
      return 0;
    }

    /**
     * Call a callback for each motor indexed by the provided motor ID.
     * @param {MotorID} motorID - the ID specifier.
     * @param {Function} callback - the function to call with the numeric motor index for each motor.
     * @private
     */
  }, {
    key: "_forEachMotor",
    value: function _forEachMotor(motorID, callback) {
      var motors;
      switch (motorID) {
        case BoostMotorLabel.A:
          motors = [BoostPort.A];
          break;
        case BoostMotorLabel.B:
          motors = [BoostPort.B];
          break;
        case BoostMotorLabel.C:
          motors = [BoostPort.C];
          break;
        case BoostMotorLabel.D:
          motors = [BoostPort.D];
          break;
        case BoostMotorLabel.AB:
          motors = [BoostPort.A, BoostPort.B];
          break;
        case BoostMotorLabel.ALL:
          motors = [BoostPort.A, BoostPort.B, BoostPort.C, BoostPort.D];
          break;
        default:
          log.warn("Invalid motor ID: ".concat(motorID));
          motors = [];
          break;
      }
      var _iterator = _createForOfIteratorHelper(motors),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var index = _step.value;
          callback(index);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} TILT_DIRECTION_ANY - the tilt direction to test (up, down, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     */
  }, {
    key: "whenTilted",
    value: function whenTilted(args) {
      return this._isTilted(args.TILT_DIRECTION_ANY);
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} TILT_DIRECTION_ANY - the tilt direction to test (up, down, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     */
  }, {
    key: "isTilted",
    value: function isTilted(args) {
      return this._isTilted(args.TILT_DIRECTION_ANY);
    }

    /**
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} TILT_DIRECTION - the direction (up, down, left, right) to check.
     * @return {number} - the tilt sensor's angle in the specified direction.
     * Note that getTiltAngle(up) = -getTiltAngle(down) and getTiltAngle(left) = -getTiltAngle(right).
     */
  }, {
    key: "getTiltAngle",
    value: function getTiltAngle(args) {
      return this._getTiltAngle(args.TILT_DIRECTION);
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {TiltDirection} direction - the tilt direction to test (up, down, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     * @private
     */
  }, {
    key: "_isTilted",
    value: function _isTilted(direction) {
      switch (direction) {
        case BoostTiltDirection.ANY:
          return Math.abs(this._peripheral.tiltX) >= Scratch3BoostBlocks.TILT_THRESHOLD || Math.abs(this._peripheral.tiltY) >= Scratch3BoostBlocks.TILT_THRESHOLD;
        default:
          return this._getTiltAngle(direction) >= Scratch3BoostBlocks.TILT_THRESHOLD;
      }
    }

    /**
     * @param {TiltDirection} direction - the direction (up, down, left, right) to check.
     * @return {number} - the tilt sensor's angle in the specified direction.
     * Note that getTiltAngle(up) = -getTiltAngle(down) and getTiltAngle(left) = -getTiltAngle(right).
     * @private
     */
  }, {
    key: "_getTiltAngle",
    value: function _getTiltAngle(direction) {
      switch (direction) {
        case BoostTiltDirection.UP:
          return this._peripheral.tiltY > 90 ? 256 - this._peripheral.tiltY : -this._peripheral.tiltY;
        case BoostTiltDirection.DOWN:
          return this._peripheral.tiltY > 90 ? this._peripheral.tiltY - 256 : this._peripheral.tiltY;
        case BoostTiltDirection.LEFT:
          return this._peripheral.tiltX > 90 ? this._peripheral.tiltX - 256 : this._peripheral.tiltX;
        case BoostTiltDirection.RIGHT:
          return this._peripheral.tiltX > 90 ? 256 - this._peripheral.tiltX : -this._peripheral.tiltX;
        default:
          log.warn("Unknown tilt direction in _getTiltAngle: ".concat(direction));
      }
    }

    /**
     * Edge-triggering hat function, for when the vision sensor is detecting
     * a certain color.
     * @param {object} args - the block's arguments.
     * @return {boolean} - true when the color sensor senses the specified color.
     */
  }, {
    key: "whenColor",
    value: function whenColor(args) {
      if (args.COLOR === BoostColor.ANY) {
        // For "any" color, return true if the color is not "none", and
        // the color is different from the previous color detected. This
        // allows the hat to trigger when the color changes from one color
        // to another.
        return this._peripheral.color !== BoostColor.NONE && this._peripheral.color !== this._peripheral.previousColor;
      }
      return args.COLOR === this._peripheral.color;
    }

    /**
     * A boolean reporter function, for whether the vision sensor is detecting
     * a certain color.
     * @param {object} args - the block's arguments.
     * @return {boolean} - true when the color sensor senses the specified color.
     */
  }, {
    key: "seeingColor",
    value: function seeingColor(args) {
      if (args.COLOR === BoostColor.ANY) {
        return this._peripheral.color !== BoostColor.NONE;
      }
      return args.COLOR === this._peripheral.color;
    }

    /**
     * Set the LED's hue.
     * @param {object} args - the block's arguments.
     * @property {number} HUE - the hue to set, in the range [0,100].
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "setLightHue",
    value: function setLightHue(args) {
      // Convert from [0,100] to [0,360]
      var inputHue = Cast.toNumber(args.HUE);
      inputHue = MathUtil.wrapClamp(inputHue, 0, 100);
      var hue = inputHue * 360 / 100;
      var rgbObject = color.hsvToRgb({
        h: hue,
        s: 1,
        v: 1
      });
      var rgbDecimal = color.rgbToDecimal(rgbObject);
      this._peripheral._led = inputHue;
      this._peripheral.setLED(rgbDecimal);
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BoostBLE.sendInterval);
      });
    }
  }], [{
    key: "EXTENSION_ID",
    get:
    /**
     * @return {string} - the ID of this extension.
     */
    function get() {
      return 'boost';
    }

    /**
     * @return {number} - the tilt sensor counts as "tilted" if its tilt angle meets or exceeds this threshold.
     */
  }, {
    key: "TILT_THRESHOLD",
    get: function get() {
      return 15;
    }
  }]);
}();
module.exports = Scratch3BoostBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_ev3/index.js":
/*!**************************************************!*\
  !*** ../vm/src/extensions/scratch3_ev3/index.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var uid = __webpack_require__(/*! ../../util/uid */ "../vm/src/util/uid.js");
var BT = __webpack_require__(/*! ../../io/bt */ "../vm/src/io/bt.js");
var Base64Util = __webpack_require__(/*! ../../util/base64-util */ "../vm/src/util/base64-util.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var RateLimiter = __webpack_require__(/*! ../../util/rateLimiter.js */ "../vm/src/util/rateLimiter.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/svg+xml;base64,';

/**
 * String with Ev3 expected pairing pin.
 * @readonly
 */
var Ev3PairingPin = '1234';

/**
 * A maximum number of BT message sends per second, to be enforced by the rate limiter.
 * @type {number}
 */
var BTSendRateMax = 40;

/**
 * Enum for Ev3 parameter encodings of various argument and return values.
 * Found in the 'EV3 Firmware Developer Kit', section4, page 9, at
 * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
 *
 * The format for these values is:
 * 0xxxxxxx for Short Format
 * 1ttt-bbb for Long Format
 *
 * @readonly
 * @enum {number}
 */
var Ev3Encoding = {
  ONE_BYTE: 0x81,
  // = 0b1000-001, "1 byte to follow"
  TWO_BYTES: 0x82,
  // = 0b1000-010, "2 bytes to follow"
  FOUR_BYTES: 0x83,
  // = 0b1000-011, "4 bytes to follow"
  GLOBAL_VARIABLE_ONE_BYTE: 0xE1,
  // = 0b1110-001, "1 byte to follow"
  GLOBAL_CONSTANT_INDEX_0: 0x20,
  // = 0b00100000
  GLOBAL_VARIABLE_INDEX_0: 0x60 // = 0b01100000
};

/**
 * Enum for Ev3 direct command types.
 * Found in the 'EV3 Communication Developer Kit', section 4, page 24, at
 * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
 * @readonly
 * @enum {number}
 */
var Ev3Command = {
  DIRECT_COMMAND_REPLY: 0x00,
  DIRECT_COMMAND_NO_REPLY: 0x80,
  DIRECT_REPLY: 0x02
};

/**
 * Enum for Ev3 commands opcodes.
 * Found in the 'EV3 Firmware Developer Kit', section 4, page 10, at
 * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
 * @readonly
 * @enum {number}
 */
var Ev3Opcode = {
  OPOUTPUT_STEP_SPEED: 0xAE,
  OPOUTPUT_TIME_SPEED: 0xAF,
  OPOUTPUT_STOP: 0xA3,
  OPOUTPUT_RESET: 0xA2,
  OPOUTPUT_STEP_SYNC: 0xB0,
  OPOUTPUT_TIME_SYNC: 0xB1,
  OPOUTPUT_GET_COUNT: 0xB3,
  OPSOUND: 0x94,
  OPSOUND_CMD_TONE: 1,
  OPSOUND_CMD_STOP: 0,
  OPINPUT_DEVICE_LIST: 0x98,
  OPINPUT_READSI: 0x9D
};

/**
 * Enum for Ev3 values used as arguments to various opcodes.
 * Found in the 'EV3 Firmware Developer Kit', section4, page 10-onwards, at
 * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
 * @readonly
 * @enum {number}
 */
var Ev3Args = {
  LAYER: 0,
  // always 0, chained EV3s not supported
  COAST: 0,
  BRAKE: 1,
  RAMP: 50,
  // time in milliseconds
  DO_NOT_CHANGE_TYPE: 0,
  MAX_DEVICES: 32 // 'Normally 32' from pg. 46
};

/**
 * Enum for Ev3 device type numbers.
 * Found in the 'EV3 Firmware Developer Kit', section 5, page 100, at
 * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
 * @readonly
 * @enum {string}
 */
var Ev3Device = {
  29: 'color',
  30: 'ultrasonic',
  32: 'gyro',
  16: 'touch',
  8: 'mediumMotor',
  7: 'largeMotor',
  126: 'none',
  125: 'none'
};

/**
 * Enum for Ev3 device modes.
 * Found in the 'EV3 Firmware Developer Kit', section 5, page 100, at
 * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
 * @readonly
 * @enum {number}
 */
var Ev3Mode = {
  touch: 0,
  // touch
  color: 1,
  // ambient
  ultrasonic: 1,
  // inch
  none: 0
};

/**
 * Enum for Ev3 device labels used in the Scratch blocks/UI.
 * @readonly
 * @enum {string}
 */
var Ev3Label = {
  touch: 'button',
  color: 'brightness',
  ultrasonic: 'distance'
};

/**
 * Manage power, direction, and timers for one EV3 motor.
 */
var EV3Motor = /*#__PURE__*/function () {
  /**
   * Construct a EV3 Motor instance, which could be of type 'largeMotor' or
   * 'mediumMotor'.
   *
   * @param {EV3} parent - the EV3 peripheral which owns this motor.
   * @param {int} index - the zero-based index of this motor on its parent peripheral.
   * @param {string} type - the type of motor (i.e. 'largeMotor' or 'mediumMotor').
   */
  function EV3Motor(parent, index, type) {
    _classCallCheck(this, EV3Motor);
    /**
     * The EV3 peripheral which owns this motor.
     * @type {EV3}
     * @private
     */
    this._parent = parent;

    /**
     * The zero-based index of this motor on its parent peripheral.
     * @type {int}
     * @private
     */
    this._index = index;

    /**
     * The type of EV3 motor this could be: 'largeMotor' or 'mediumMotor'.
     * @type {string}
     * @private
     */
    this._type = type;

    /**
     * This motor's current direction: 1 for "clockwise" or -1 for "counterclockwise"
     * @type {number}
     * @private
     */
    this._direction = 1;

    /**
     * This motor's current power level, in the range [0,100].
     * @type {number}
     * @private
     */
    this._power = 50;

    /**
     * This motor's current position, in the range [0,360].
     * @type {number}
     * @private
     */
    this._position = 0;

    /**
     * An ID for the current coast command, to help override multiple coast
     * commands sent in succession.
     * @type {number}
     * @private
     */
    this._commandID = null;

    /**
     * A delay, in milliseconds, to add to coasting, to make sure that a brake
     * first takes effect if one was sent.
     * @type {number}
     * @private
     */
    this._coastDelay = 1000;
  }

  /**
   * @return {string} - this motor's type: 'largeMotor' or 'mediumMotor'
   */
  return _createClass(EV3Motor, [{
    key: "type",
    get: function get() {
      return this._type;
    }

    /**
     * @param {string} value - this motor's new type: 'largeMotor' or 'mediumMotor'
     */,
    set: function set(value) {
      this._type = value;
    }

    /**
     * @return {int} - this motor's current direction: 1 for "clockwise" or -1 for "counterclockwise"
     */
  }, {
    key: "direction",
    get: function get() {
      return this._direction;
    }

    /**
     * @param {int} value - this motor's new direction: 1 for "clockwise" or -1 for "counterclockwise"
     */,
    set: function set(value) {
      if (value < 0) {
        this._direction = -1;
      } else {
        this._direction = 1;
      }
    }

    /**
     * @return {int} - this motor's current power level, in the range [0,100].
     */
  }, {
    key: "power",
    get: function get() {
      return this._power;
    }

    /**
     * @param {int} value - this motor's new power level, in the range [0,100].
     */,
    set: function set(value) {
      this._power = value;
    }

    /**
     * @return {int} - this motor's current position, in the range [-inf,inf].
     */
  }, {
    key: "position",
    get: function get() {
      return this._position;
    }

    /**
     * @param {int} array - this motor's new position, in the range [0,360].
     */,
    set: function set(array) {
      // tachoValue from Paula
      var value = array[0] + array[1] * 256 + array[2] * 256 * 256 + array[3] * 256 * 256 * 256;
      if (value > 0x7fffffff) {
        value = value - 0x100000000;
      }
      this._position = value;
    }

    /**
     * Turn this motor on for a specific duration.
     * Found in the 'EV3 Firmware Developer Kit', page 56, at
     * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
     *
     * Opcode arguments:
     * (Data8) LAYER – Specify chain layer number [0 - 3]
     * (Data8) NOS – Output bit field [0x00 – 0x0F]
     * (Data8) SPEED – Power level, [-100 – 100]
     * (Data32) STEP1 – Time in milliseconds for ramp up
     * (Data32) STEP2 – Time in milliseconds for continues run
     * (Data32) STEP3 – Time in milliseconds for ramp down
     * (Data8) BRAKE - Specify break level [0: Float, 1: Break]
     *
     * @param {number} milliseconds - run the motor for this long.
     */
  }, {
    key: "turnOnFor",
    value: function turnOnFor(milliseconds) {
      if (this._power === 0) return;
      var port = this._portMask(this._index);
      var n = milliseconds;
      var speed = this._power * this._direction;
      var ramp = Ev3Args.RAMP;
      var byteCommand = [];
      byteCommand[0] = Ev3Opcode.OPOUTPUT_TIME_SPEED;

      // If speed is less than zero, make it positive and multiply the input
      // value by -1
      if (speed < 0) {
        speed = -1 * speed;
        n = -1 * n;
      }
      // If the input value is less than 0
      var dir = n < 0 ? 0x100 - speed : speed; // step negative or positive
      n = Math.abs(n);
      // Setup motor run duration and ramping behavior
      var rampup = ramp;
      var rampdown = ramp;
      var run = n - ramp * 2;
      if (run < 0) {
        rampup = Math.floor(n / 2);
        run = 0;
        rampdown = n - rampup;
      }
      // Generate motor command values
      var runcmd = this._runValues(run);
      byteCommand = byteCommand.concat([Ev3Args.LAYER, port, Ev3Encoding.ONE_BYTE, dir & 0xff, Ev3Encoding.ONE_BYTE, rampup]).concat(runcmd.concat([Ev3Encoding.ONE_BYTE, rampdown, Ev3Args.BRAKE]));
      var cmd = this._parent.generateCommand(Ev3Command.DIRECT_COMMAND_NO_REPLY, byteCommand);
      this._parent.send(cmd);
      this.coastAfter(milliseconds);
    }

    /**
     * Set the motor to coast after a specified amount of time.
     * @param {number} time - the time in milliseconds.
     */
  }, {
    key: "coastAfter",
    value: function coastAfter(time) {
      var _this = this;
      if (this._power === 0) return;

      // Set the motor command id to check before starting coast
      var commandId = uid();
      this._commandID = commandId;

      // Send coast message
      setTimeout(function () {
        // Do not send coast if another motor command changed the command id.
        if (_this._commandID === commandId) {
          _this.coast();
          _this._commandID = null;
        }
      }, time + this._coastDelay); // add a delay so the brake takes effect
    }

    /**
     * Set the motor to coast.
     */
  }, {
    key: "coast",
    value: function coast() {
      if (this._power === 0) return;
      var cmd = this._parent.generateCommand(Ev3Command.DIRECT_COMMAND_NO_REPLY, [Ev3Opcode.OPOUTPUT_STOP, Ev3Args.LAYER, this._portMask(this._index),
      // port output bit field
      Ev3Args.COAST]);
      this._parent.send(cmd, false); // don't use rate limiter to ensure motor stops
    }

    /**
     * Generate motor run values for a given input.
     * @param  {number} run - run input.
     * @return {array} - run values as a byte array.
     */
  }, {
    key: "_runValues",
    value: function _runValues(run) {
      // If run duration is less than max 16-bit integer
      if (run < 0x7fff) {
        return [Ev3Encoding.TWO_BYTES, run & 0xff, run >> 8 & 0xff];
      }

      // Run forever
      return [Ev3Encoding.FOUR_BYTES, run & 0xff, run >> 8 & 0xff, run >> 16 & 0xff, run >> 24 & 0xff];
    }

    /**
     * Return a port value for the EV3 that is in the format for 'output bit field'
     * as 1/2/4/8, generally needed for motor ports, instead of the typical 0/1/2/3.
     * The documentation in the 'EV3 Firmware Developer Kit' for motor port arguments
     * is sometimes mistaken, but we believe motor ports are mostly addressed this way.
     * @param {number} port - the port number to convert to an 'output bit field'.
     * @return {number} - the converted port number.
     */
  }, {
    key: "_portMask",
    value: function _portMask(port) {
      return Math.pow(2, port);
    }
  }]);
}();
var EV3 = /*#__PURE__*/function () {
  function EV3(runtime, extensionId) {
    _classCallCheck(this, EV3);
    /**
     * The Scratch 3.0 runtime used to trigger the green flag button.
     * @type {Runtime}
     * @private
     */
    this._runtime = runtime;
    this._runtime.on('PROJECT_STOP_ALL', this.stopAll.bind(this));

    /**
     * The id of the extension this peripheral belongs to.
     */
    this._extensionId = extensionId;

    /**
     * A list of the names of the sensors connected in ports 1,2,3,4.
     * @type {string[]}
     * @private
     */
    this._sensorPorts = [];

    /**
     * A list of the names of the motors connected in ports A,B,C,D.
     * @type {string[]}
     * @private
     */
    this._motorPorts = [];

    /**
     * The state of all sensor values.
     * @type {string[]}
     * @private
     */
    this._sensors = {
      distance: 0,
      brightness: 0,
      buttons: [0, 0, 0, 0]
    };

    /**
     * The motors which this EV3 could possibly have connected.
     * @type {string[]}
     * @private
     */
    this._motors = [null, null, null, null];

    /**
     * The polling interval, in milliseconds.
     * @type {number}
     * @private
     */
    this._pollingInterval = 150;

    /**
     * The polling interval ID.
     * @type {number}
     * @private
     */
    this._pollingIntervalID = null;

    /**
     * The counter keeping track of polling cycles.
     * @type {string[]}
     * @private
     */
    this._pollingCounter = 0;

    /**
     * The Bluetooth socket connection for reading/writing peripheral data.
     * @type {BT}
     * @private
     */
    this._bt = null;
    this._runtime.registerPeripheralExtension(extensionId, this);

    /**
     * A rate limiter utility, to help limit the rate at which we send BT messages
     * over the socket to Scratch Link to a maximum number of sends per second.
     * @type {RateLimiter}
     * @private
     */
    this._rateLimiter = new RateLimiter(BTSendRateMax);
    this.reset = this.reset.bind(this);
    this._onConnect = this._onConnect.bind(this);
    this._onMessage = this._onMessage.bind(this);
    this._pollValues = this._pollValues.bind(this);
  }
  return _createClass(EV3, [{
    key: "distance",
    get: function get() {
      var value = this._sensors.distance > 100 ? 100 : this._sensors.distance;
      value = value < 0 ? 0 : value;
      value = Math.round(100 * value) / 100;
      return value;
    }
  }, {
    key: "brightness",
    get: function get() {
      return this._sensors.brightness;
    }

    /**
     * Access a particular motor on this peripheral.
     * @param {int} index - the zero-based index of the desired motor.
     * @return {EV3Motor} - the EV3Motor instance, if any, at that index.
     */
  }, {
    key: "motor",
    value: function motor(index) {
      return this._motors[index];
    }
  }, {
    key: "isButtonPressed",
    value: function isButtonPressed(port) {
      return this._sensors.buttons[port] === 1;
    }
  }, {
    key: "beep",
    value: function beep(freq, time) {
      var cmd = this.generateCommand(Ev3Command.DIRECT_COMMAND_NO_REPLY, [Ev3Opcode.OPSOUND, Ev3Opcode.OPSOUND_CMD_TONE, Ev3Encoding.ONE_BYTE, 2, Ev3Encoding.TWO_BYTES, freq, freq >> 8, Ev3Encoding.TWO_BYTES, time, time >> 8]);
      this.send(cmd);
    }
  }, {
    key: "stopAll",
    value: function stopAll() {
      this.stopAllMotors();
      this.stopSound();
    }
  }, {
    key: "stopSound",
    value: function stopSound() {
      var cmd = this.generateCommand(Ev3Command.DIRECT_COMMAND_NO_REPLY, [Ev3Opcode.OPSOUND, Ev3Opcode.OPSOUND_CMD_STOP]);
      this.send(cmd, false); // don't use rate limiter to ensure sound stops
    }
  }, {
    key: "stopAllMotors",
    value: function stopAllMotors() {
      this._motors.forEach(function (motor) {
        if (motor) {
          motor.coast();
        }
      });
    }

    /**
     * Called by the runtime when user wants to scan for an EV3 peripheral.
     */
  }, {
    key: "scan",
    value: function scan() {
      if (this._bt) {
        this._bt.disconnect();
      }
      this._bt = new BT(this._runtime, this._extensionId, {
        majorDeviceClass: 8,
        minorDeviceClass: 1
      }, this._onConnect, this.reset, this._onMessage);
    }

    /**
     * Called by the runtime when user wants to connect to a certain EV3 peripheral.
     * @param {number} id - the id of the peripheral to connect to.
     */
  }, {
    key: "connect",
    value: function connect(id) {
      if (this._bt) {
        this._bt.connectPeripheral(id, Ev3PairingPin);
      }
    }

    /**
     * Called by the runtime when user wants to disconnect from the EV3 peripheral.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._bt) {
        this._bt.disconnect();
      }
      this.reset();
    }

    /**
     * Reset all the state and timeout/interval ids.
     */
  }, {
    key: "reset",
    value: function reset() {
      this._sensorPorts = [];
      this._motorPorts = [];
      this._sensors = {
        distance: 0,
        brightness: 0,
        buttons: [0, 0, 0, 0]
      };
      this._motors = [null, null, null, null];
      if (this._pollingIntervalID) {
        window.clearInterval(this._pollingIntervalID);
        this._pollingIntervalID = null;
      }
    }

    /**
     * Called by the runtime to detect whether the EV3 peripheral is connected.
     * @return {boolean} - the connected state.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      var connected = false;
      if (this._bt) {
        connected = this._bt.isConnected();
      }
      return connected;
    }

    /**
     * Send a message to the peripheral BT socket.
     * @param {Uint8Array} message - the message to send.
     * @param {boolean} [useLimiter=true] - if true, use the rate limiter
     * @return {Promise} - a promise result of the send operation.
     */
  }, {
    key: "send",
    value: function send(message) {
      var useLimiter = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
      if (!this.isConnected()) return Promise.resolve();
      if (useLimiter) {
        if (!this._rateLimiter.okayToSend()) return Promise.resolve();
      }
      return this._bt.sendMessage({
        message: Base64Util.uint8ArrayToBase64(message),
        encoding: 'base64'
      });
    }

    /**
     * Genrates direct commands that are sent to the EV3 as a single or compounded byte arrays.
     * See 'EV3 Communication Developer Kit', section 4, page 24 at
     * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits.
     *
     * Direct commands are one of two types:
     * DIRECT_COMMAND_NO_REPLY = a direct command where no reply is expected
     * DIRECT_COMMAND_REPLY = a direct command where a reply is expected, and the
     * number and length of returned values needs to be specified.
     *
     * The direct command byte array sent takes the following format:
     * Byte 0 - 1: Command size, Little Endian. Command size not including these 2 bytes
     * Byte 2 - 3: Message counter, Little Endian. Forth running counter
     * Byte 4:     Command type. Either DIRECT_COMMAND_REPLY or DIRECT_COMMAND_NO_REPLY
     * Byte 5 - 6: Reservation (allocation) of global and local variables using a compressed format
     *             (globals reserved in byte 5 and the 2 lsb of byte 6, locals reserved in the upper
     *             6 bits of byte 6) – see documentation for more details.
     * Byte 7 - n: Byte codes as a single command or compound commands (I.e. more commands composed
     *             as a small program)
     *
     * @param {number} type - the direct command type.
     * @param {string} byteCommands - a compound array of EV3 Opcode + arguments.
     * @param {number} allocation - the allocation of global and local vars needed for replies.
     * @return {array} - generated complete command byte array, with header and compounded commands.
     */
  }, {
    key: "generateCommand",
    value: function generateCommand(type, byteCommands) {
      var allocation = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 0;
      // Header (Bytes 0 - 6)
      var command = [];
      command[2] = 0; // Message counter unused for now
      command[3] = 0; // Message counter unused for now
      command[4] = type;
      command[5] = allocation & 0xFF;
      command[6] = allocation >> 8 && 0xFF;

      // Bytecodes (Bytes 7 - n)
      command = command.concat(byteCommands);

      // Calculate command length minus first two header bytes
      var len = command.length - 2;
      command[0] = len & 0xFF;
      command[1] = len >> 8 && 0xFF;
      return command;
    }

    /**
     * When the EV3 peripheral connects, start polling for sensor and motor values.
     * @private
     */
  }, {
    key: "_onConnect",
    value: function _onConnect() {
      this._pollingIntervalID = window.setInterval(this._pollValues, this._pollingInterval);
    }

    /**
     * Poll the EV3 for sensor and motor input values, based on the list of
     * known connected sensors and motors. This is sent as many compound commands
     * in a direct command, with a reply expected.
     *
     * See 'EV3 Firmware Developer Kit', section 4.8, page 46, at
     * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits
     * for a list of polling/input device commands and their arguments.
     *
     * @private
     */
  }, {
    key: "_pollValues",
    value: function _pollValues() {
      if (!this.isConnected()) {
        window.clearInterval(this._pollingIntervalID);
        return;
      }
      var cmds = []; // compound command
      var allocation = 0;
      var sensorCount = 0;

      // Reset the list of devices every 20 counts
      if (this._pollingCounter % 20 === 0) {
        // GET DEVICE LIST
        cmds[0] = Ev3Opcode.OPINPUT_DEVICE_LIST;
        cmds[1] = Ev3Encoding.ONE_BYTE;
        cmds[2] = Ev3Args.MAX_DEVICES;
        cmds[3] = Ev3Encoding.GLOBAL_VARIABLE_INDEX_0;
        cmds[4] = Ev3Encoding.GLOBAL_VARIABLE_ONE_BYTE;
        cmds[5] = Ev3Encoding.GLOBAL_CONSTANT_INDEX_0;

        // Command and payload lengths
        allocation = 33;
        this._updateDevices = true;
      } else {
        // GET SENSOR VALUES FOR CONNECTED SENSORS
        var index = 0;
        for (var i = 0; i < 4; i++) {
          if (this._sensorPorts[i] !== 'none') {
            cmds[index + 0] = Ev3Opcode.OPINPUT_READSI;
            cmds[index + 1] = Ev3Args.LAYER;
            cmds[index + 2] = i; // PORT
            cmds[index + 3] = Ev3Args.DO_NOT_CHANGE_TYPE;
            cmds[index + 4] = Ev3Mode[this._sensorPorts[i]];
            cmds[index + 5] = Ev3Encoding.GLOBAL_VARIABLE_ONE_BYTE;
            cmds[index + 6] = sensorCount * 4; // GLOBAL INDEX
            index += 7;
          }
          sensorCount++;
        }

        // GET MOTOR POSITION VALUES, EVEN IF NO MOTOR PRESENT
        for (var _i = 0; _i < 4; _i++) {
          cmds[index + 0] = Ev3Opcode.OPOUTPUT_GET_COUNT;
          cmds[index + 1] = Ev3Args.LAYER;
          cmds[index + 2] = _i; // PORT (incorrectly specified as 'Output bit field' in LEGO docs)
          cmds[index + 3] = Ev3Encoding.GLOBAL_VARIABLE_ONE_BYTE;
          cmds[index + 4] = sensorCount * 4; // GLOBAL INDEX
          index += 5;
          sensorCount++;
        }

        // Command and payload lengths
        allocation = sensorCount * 4;
      }
      var cmd = this.generateCommand(Ev3Command.DIRECT_COMMAND_REPLY, cmds, allocation);
      this.send(cmd);
      this._pollingCounter++;
    }

    /**
     * Message handler for incoming EV3 reply messages, either a list of connected
     * devices (sensors and motors) or the values of the connected sensors and motors.
     *
     * See 'EV3 Communication Developer Kit', section 4.1, page 24 at
     * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits
     * for more details on direct reply formats.
     *
     * The direct reply byte array sent takes the following format:
     * Byte 0 – 1: Reply size, Little Endian. Reply size not including these 2 bytes
     * Byte 2 – 3: Message counter, Little Endian. Equals the Direct Command
     * Byte 4:     Reply type. Either DIRECT_REPLY or DIRECT_REPLY_ERROR
     * Byte 5 - n: Resonse buffer. I.e. the content of the by the Command reserved global variables.
     *             I.e. if the command reserved 64 bytes, these bytes will be placed in the reply
     *             packet as the bytes 5 to 68.
     *
     * See 'EV3 Firmware Developer Kit', section 4.8, page 56 at
     * https://education.lego.com/en-us/support/mindstorms-ev3/developer-kits
     * for direct response buffer formats for various commands.
     *
     * @param {object} params - incoming message parameters
     * @private
     */
  }, {
    key: "_onMessage",
    value: function _onMessage(params) {
      var message = params.message;
      var data = Base64Util.base64ToUint8Array(message);
      if (data[4] !== Ev3Command.DIRECT_REPLY) {
        return;
      }
      if (this._updateDevices) {
        // PARSE DEVICE LIST
        for (var i = 0; i < 4; i++) {
          var deviceType = Ev3Device[data[i + 5]];
          // if returned device type is null, use 'none'
          this._sensorPorts[i] = deviceType ? deviceType : 'none';
        }
        for (var _i2 = 0; _i2 < 4; _i2++) {
          var _deviceType = Ev3Device[data[_i2 + 21]];
          // if returned device type is null, use 'none'
          this._motorPorts[_i2] = _deviceType ? _deviceType : 'none';
        }
        for (var m = 0; m < 4; m++) {
          var type = this._motorPorts[m];
          if (type !== 'none' && !this._motors[m]) {
            // add new motor if don't already have one
            this._motors[m] = new EV3Motor(this, m, type);
          }
          if (type === 'none' && this._motors[m]) {
            // clear old motor
            this._motors[m] = null;
          }
        }
        this._updateDevices = false;

        // eslint-disable-next-line no-undefined
      } else if (!this._sensorPorts.includes(undefined) && !this._motorPorts.includes(undefined)) {
        // PARSE SENSOR VALUES
        var offset = 5; // start reading sensor values at byte 5
        for (var _i3 = 0; _i3 < 4; _i3++) {
          // array 2 float
          var buffer = new Uint8Array([data[offset], data[offset + 1], data[offset + 2], data[offset + 3]]).buffer;
          var view = new DataView(buffer);
          var value = view.getFloat32(0, true);
          if (Ev3Label[this._sensorPorts[_i3]] === 'button') {
            // Read a button value per port
            this._sensors.buttons[_i3] = value ? value : 0;
          } else if (Ev3Label[this._sensorPorts[_i3]]) {
            // if valid
            // Read brightness / distance values and set to 0 if null
            this._sensors[Ev3Label[this._sensorPorts[_i3]]] = value ? value : 0;
          }
          offset += 4;
        }

        // PARSE MOTOR POSITION VALUES, EVEN IF NO MOTOR PRESENT
        for (var _i4 = 0; _i4 < 4; _i4++) {
          var positionArray = [data[offset], data[offset + 1], data[offset + 2], data[offset + 3]];
          if (this._motors[_i4]) {
            this._motors[_i4].position = positionArray;
          }
          offset += 4;
        }
      }
    }
  }]);
}();
/**
 * Enum for motor port names.
 * Note: if changed, will break compatibility with previously saved projects.
 * @readonly
 * @enum {string}
 */
var Ev3MotorMenu = ['A', 'B', 'C', 'D'];

/**
 * Enum for sensor port names.
 * Note: if changed, will break compatibility with previously saved projects.
 * @readonly
 * @enum {string}
 */
var Ev3SensorMenu = ['1', '2', '3', '4'];
var Scratch3Ev3Blocks = /*#__PURE__*/function () {
  /**
   * Creates a new instance of the EV3 extension.
   * @param  {object} runtime VM runtime
   * @constructor
   */
  function Scratch3Ev3Blocks(runtime) {
    _classCallCheck(this, Scratch3Ev3Blocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;

    // Create a new EV3 peripheral instance
    this._peripheral = new EV3(this.runtime, Scratch3Ev3Blocks.EXTENSION_ID);
    this._playNoteForPicker = this._playNoteForPicker.bind(this);
    this.runtime.on('PLAY_NOTE', this._playNoteForPicker);
  }

  /**
   * Define the EV3 extension.
   * @return {object} Extension description.
   */
  return _createClass(Scratch3Ev3Blocks, [{
    key: "getInfo",
    value: function getInfo() {
      return {
        id: Scratch3Ev3Blocks.EXTENSION_ID,
        name: 'LEGO EV3',
        blockIconURI: blockIconURI,
        showStatusButton: true,
        blocks: [{
          opcode: 'motorTurnClockwise',
          text: formatMessage({
            id: 'ev3.motorTurnClockwise',
            "default": 'motor [PORT] turn this way for [TIME] seconds',
            description: 'turn a motor clockwise for some time'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            PORT: {
              type: ArgumentType.STRING,
              menu: 'motorPorts',
              defaultValue: 0
            },
            TIME: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          }
        }, {
          opcode: 'motorTurnCounterClockwise',
          text: formatMessage({
            id: 'ev3.motorTurnCounterClockwise',
            "default": 'motor [PORT] turn that way for [TIME] seconds',
            description: 'turn a motor counter-clockwise for some time'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            PORT: {
              type: ArgumentType.STRING,
              menu: 'motorPorts',
              defaultValue: 0
            },
            TIME: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          }
        }, {
          opcode: 'motorSetPower',
          text: formatMessage({
            id: 'ev3.motorSetPower',
            "default": 'motor [PORT] set power [POWER] %',
            description: 'set a motor\'s power to some value'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            PORT: {
              type: ArgumentType.STRING,
              menu: 'motorPorts',
              defaultValue: 0
            },
            POWER: {
              type: ArgumentType.NUMBER,
              defaultValue: 100
            }
          }
        }, {
          opcode: 'getMotorPosition',
          text: formatMessage({
            id: 'ev3.getMotorPosition',
            "default": 'motor [PORT] position',
            description: 'get the measured degrees a motor has turned'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            PORT: {
              type: ArgumentType.STRING,
              menu: 'motorPorts',
              defaultValue: 0
            }
          }
        }, {
          opcode: 'whenButtonPressed',
          text: formatMessage({
            id: 'ev3.whenButtonPressed',
            "default": 'when button [PORT] pressed',
            description: 'when a button connected to a port is pressed'
          }),
          blockType: BlockType.HAT,
          arguments: {
            PORT: {
              type: ArgumentType.STRING,
              menu: 'sensorPorts',
              defaultValue: 0
            }
          }
        }, {
          opcode: 'whenDistanceLessThan',
          text: formatMessage({
            id: 'ev3.whenDistanceLessThan',
            "default": 'when distance < [DISTANCE]',
            description: 'when the value measured by the distance sensor is less than some value'
          }),
          blockType: BlockType.HAT,
          arguments: {
            DISTANCE: {
              type: ArgumentType.NUMBER,
              defaultValue: 5
            }
          }
        }, {
          opcode: 'whenBrightnessLessThan',
          text: formatMessage({
            id: 'ev3.whenBrightnessLessThan',
            "default": 'when brightness < [DISTANCE]',
            description: 'when value measured by brightness sensor is less than some value'
          }),
          blockType: BlockType.HAT,
          arguments: {
            DISTANCE: {
              type: ArgumentType.NUMBER,
              defaultValue: 50
            }
          }
        }, {
          opcode: 'buttonPressed',
          text: formatMessage({
            id: 'ev3.buttonPressed',
            "default": 'button [PORT] pressed?',
            description: 'is a button on some port pressed?'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            PORT: {
              type: ArgumentType.STRING,
              menu: 'sensorPorts',
              defaultValue: 0
            }
          }
        }, {
          opcode: 'getDistance',
          text: formatMessage({
            id: 'ev3.getDistance',
            "default": 'distance',
            description: 'gets measured distance'
          }),
          blockType: BlockType.REPORTER
        }, {
          opcode: 'getBrightness',
          text: formatMessage({
            id: 'ev3.getBrightness',
            "default": 'brightness',
            description: 'gets measured brightness'
          }),
          blockType: BlockType.REPORTER
        }, {
          opcode: 'beep',
          text: formatMessage({
            id: 'ev3.beepNote',
            "default": 'beep note [NOTE] for [TIME] secs',
            description: 'play some note on EV3 for some time'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            NOTE: {
              type: ArgumentType.NOTE,
              defaultValue: 60
            },
            TIME: {
              type: ArgumentType.NUMBER,
              defaultValue: 0.5
            }
          }
        }],
        menus: {
          motorPorts: {
            acceptReporters: true,
            items: this._formatMenu(Ev3MotorMenu)
          },
          sensorPorts: {
            acceptReporters: true,
            items: this._formatMenu(Ev3SensorMenu)
          }
        }
      };
    }
  }, {
    key: "motorTurnClockwise",
    value: function motorTurnClockwise(args) {
      var _this2 = this;
      var port = Cast.toNumber(args.PORT);
      var time = Cast.toNumber(args.TIME) * 1000;
      time = MathUtil.clamp(time, 0, 15000);
      return new Promise(function (resolve) {
        _this2._forEachMotor(port, function (motorIndex) {
          var motor = _this2._peripheral.motor(motorIndex);
          if (motor) {
            motor.direction = 1;
            motor.turnOnFor(time);
          }
        });

        // Run for some time even when no motor is connected
        setTimeout(resolve, time);
      });
    }
  }, {
    key: "motorTurnCounterClockwise",
    value: function motorTurnCounterClockwise(args) {
      var _this3 = this;
      var port = Cast.toNumber(args.PORT);
      var time = Cast.toNumber(args.TIME) * 1000;
      time = MathUtil.clamp(time, 0, 15000);
      return new Promise(function (resolve) {
        _this3._forEachMotor(port, function (motorIndex) {
          var motor = _this3._peripheral.motor(motorIndex);
          if (motor) {
            motor.direction = -1;
            motor.turnOnFor(time);
          }
        });

        // Run for some time even when no motor is connected
        setTimeout(resolve, time);
      });
    }
  }, {
    key: "motorSetPower",
    value: function motorSetPower(args) {
      var _this4 = this;
      var port = Cast.toNumber(args.PORT);
      var power = MathUtil.clamp(Cast.toNumber(args.POWER), 0, 100);
      this._forEachMotor(port, function (motorIndex) {
        var motor = _this4._peripheral.motor(motorIndex);
        if (motor) {
          motor.power = power;
        }
      });
    }
  }, {
    key: "getMotorPosition",
    value: function getMotorPosition(args) {
      var port = Cast.toNumber(args.PORT);
      if (![0, 1, 2, 3].includes(port)) {
        return;
      }
      var motor = this._peripheral.motor(port);
      var position = 0;
      if (motor) {
        position = MathUtil.wrapClamp(motor.position, 0, 360);
      }
      return position;
    }
  }, {
    key: "whenButtonPressed",
    value: function whenButtonPressed(args) {
      var port = Cast.toNumber(args.PORT);
      if (![0, 1, 2, 3].includes(port)) {
        return;
      }
      return this._peripheral.isButtonPressed(port);
    }
  }, {
    key: "whenDistanceLessThan",
    value: function whenDistanceLessThan(args) {
      var distance = MathUtil.clamp(Cast.toNumber(args.DISTANCE), 0, 100);
      return this._peripheral.distance < distance;
    }
  }, {
    key: "whenBrightnessLessThan",
    value: function whenBrightnessLessThan(args) {
      var brightness = MathUtil.clamp(Cast.toNumber(args.DISTANCE), 0, 100);
      return this._peripheral.brightness < brightness;
    }
  }, {
    key: "buttonPressed",
    value: function buttonPressed(args) {
      var port = Cast.toNumber(args.PORT);
      if (![0, 1, 2, 3].includes(port)) {
        return;
      }
      return this._peripheral.isButtonPressed(port);
    }
  }, {
    key: "getDistance",
    value: function getDistance() {
      return this._peripheral.distance;
    }
  }, {
    key: "getBrightness",
    value: function getBrightness() {
      return this._peripheral.brightness;
    }
  }, {
    key: "_playNoteForPicker",
    value: function _playNoteForPicker(note, category) {
      if (category !== this.getInfo().name) return;
      this.beep({
        NOTE: note,
        TIME: 0.25
      });
    }
  }, {
    key: "beep",
    value: function beep(args) {
      var _this5 = this;
      var note = MathUtil.clamp(Cast.toNumber(args.NOTE), 47, 99); // valid EV3 sounds
      var time = Cast.toNumber(args.TIME) * 1000;
      time = MathUtil.clamp(time, 0, 3000);
      if (time === 0) {
        return; // don't send a beep time of 0
      }
      return new Promise(function (resolve) {
        // https://en.wikipedia.org/wiki/MIDI_tuning_standard#Frequency_values
        var freq = Math.pow(2, (note - 69 + 12) / 12) * 440;
        _this5._peripheral.beep(freq, time);

        // Run for some time even when no piezo is connected.
        setTimeout(resolve, time);
      });
    }

    /**
     * Call a callback for each motor indexed by the provided motor ID.
     *
     * Note: This way of looping through motors is currently unnecessary, but could be
     * useful if an 'all motors' option is added in the future (see WeDo2 extension).
     *
     * @param {MotorID} motorID - the ID specifier.
     * @param {Function} callback - the function to call with the numeric motor index for each motor.
     * @private
     */
  }, {
    key: "_forEachMotor",
    value: function _forEachMotor(motorID, callback) {
      var motors;
      switch (motorID) {
        case 0:
          motors = [0];
          break;
        case 1:
          motors = [1];
          break;
        case 2:
          motors = [2];
          break;
        case 3:
          motors = [3];
          break;
        default:
          log.warn("Invalid motor ID: ".concat(motorID));
          motors = [];
          break;
      }
      var _iterator = _createForOfIteratorHelper(motors),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var index = _step.value;
          callback(index);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * Formats menus into a format suitable for block menus, and loading previously
     * saved projects:
     * [
     *   {
     *    text: label,
     *    value: index
     *   },
     *   {
     *    text: label,
     *    value: index
     *   },
     *   etc...
     * ]
     *
     * @param {array} menu - a menu to format.
     * @return {object} - a formatted menu as an object.
     * @private
     */
  }, {
    key: "_formatMenu",
    value: function _formatMenu(menu) {
      var m = [];
      for (var i = 0; i < menu.length; i++) {
        var obj = {};
        obj.text = menu[i];
        obj.value = i.toString();
        m.push(obj);
      }
      return m;
    }
  }], [{
    key: "EXTENSION_ID",
    get:
    /**
     * The ID of the extension.
     * @return {string} the id
     */
    function get() {
      return 'ev3';
    }
  }]);
}();
module.exports = Scratch3Ev3Blocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_gdx_for/index.js":
/*!******************************************************!*\
  !*** ../vm/src/extensions/scratch3_gdx_for/index.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var BLE = __webpack_require__(/*! ../../io/ble */ "../vm/src/io/ble.js");
var godirect = __webpack_require__(/*! @vernier/godirect/dist/godirect.min.umd.js */ "../vm/node_modules/@vernier/godirect/dist/godirect.min.umd.js");
var ScratchLinkDeviceAdapter = __webpack_require__(/*! ./scratch-link-device-adapter */ "../vm/src/extensions/scratch3_gdx_for/scratch-link-device-adapter.js");

/**
 * Icon png to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/png;base64,';

/**
 * Icon png to be displayed in the blocks category menu, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var menuIconURI = 'data:image/png;base64,';

/**
 * Enum for Vernier godirect protocol.
 * @readonly
 * @enum {string}
 */
var BLEUUID = {
  service: 'd91714ef-28b9-4f91-ba16-f0d9a604f112',
  commandChar: 'f4bf14a6-c7d5-4b6d-8aa8-df1a7c83adcb',
  responseChar: 'b41e6675-a329-40e0-aa01-44d2f444babe'
};

/**
 * A time interval to wait (in milliseconds) before reporting to the BLE socket
 * that data has stopped coming from the peripheral.
 */
var BLETimeout = 4500;

/**
 * A string to report to the BLE socket when the GdxFor has stopped receiving data.
 * @type {string}
 */
var BLEDataStoppedError = 'Force and Acceleration extension stopped receiving data';

/**
 * Sensor ID numbers for the GDX-FOR.
 */
var GDXFOR_SENSOR = {
  FORCE: 1,
  ACCELERATION_X: 2,
  ACCELERATION_Y: 3,
  ACCELERATION_Z: 4,
  SPIN_SPEED_X: 5,
  SPIN_SPEED_Y: 6,
  SPIN_SPEED_Z: 7
};

/**
 * The update rate, in milliseconds, for sensor data input from the peripheral.
 */
var GDXFOR_UPDATE_RATE = 80;

/**
 * Threshold for pushing and pulling force, for the whenForcePushedOrPulled hat block.
 * @type {number}
 */
var FORCE_THRESHOLD = 5;

/**
 * Threshold for acceleration magnitude, for the "shaken" gesture.
 * @type {number}
 */
var SHAKEN_THRESHOLD = 30;

/**
 * Threshold for acceleration magnitude, to check if we are facing up.
 * @type {number}
 */
var FACING_THRESHOLD = 9;

/**
 * An offset for the facing threshold, used to check that we are no longer facing up.
 * @type {number}
 */
var FACING_THRESHOLD_OFFSET = 5;

/**
 * Threshold for acceleration magnitude, below which we are in freefall.
 * @type {number}
 */
var FREEFALL_THRESHOLD = 0.5;

/**
 * Factor used to account for influence of rotation during freefall.
 * @type {number}
 */
var FREEFALL_ROTATION_FACTOR = 0.3;

/**
 * Threshold in degrees for reporting that the sensor is tilted.
 * @type {number}
 */
var TILT_THRESHOLD = 15;

/**
 * Acceleration due to gravity, in m/s^2.
 * @type {number}
 */
var GRAVITY = 9.8;

/**
 * Manage communication with a GDX-FOR peripheral over a Scratch Link client socket.
 */
var GdxFor = /*#__PURE__*/function () {
  /**
   * Construct a GDX-FOR communication object.
   * @param {Runtime} runtime - the Scratch 3.0 runtime
   * @param {string} extensionId - the id of the extension
   */
  function GdxFor(runtime, extensionId) {
    _classCallCheck(this, GdxFor);
    /**
     * The Scratch 3.0 runtime used to trigger the green flag button.
     * @type {Runtime}
     * @private
     */
    this._runtime = runtime;

    /**
     * The BluetoothLowEnergy connection socket for reading/writing peripheral data.
     * @type {BLE}
     * @private
     */
    this._ble = null;

    /**
     * An @vernier/godirect Device
     * @type {Device}
     * @private
     */
    this._device = null;
    this._runtime.registerPeripheralExtension(extensionId, this);

    /**
     * The id of the extension this peripheral belongs to.
     */
    this._extensionId = extensionId;

    /**
     * The most recently received value for each sensor.
     * @type {Object.<string, number>}
     * @private
     */
    this._sensors = {
      force: 0,
      accelerationX: 0,
      accelerationY: 0,
      accelerationZ: 0,
      spinSpeedX: 0,
      spinSpeedY: 0,
      spinSpeedZ: 0
    };

    /**
     * Interval ID for data reading timeout.
     * @type {number}
     * @private
     */
    this._timeoutID = null;
    this.reset = this.reset.bind(this);
    this._onConnect = this._onConnect.bind(this);
  }

  /**
   * Called by the runtime when user wants to scan for a peripheral.
   */
  return _createClass(GdxFor, [{
    key: "scan",
    value: function scan() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this._ble = new BLE(this._runtime, this._extensionId, {
        filters: [{
          namePrefix: 'GDX-FOR'
        }],
        optionalServices: [BLEUUID.service]
      }, this._onConnect, this.reset);
    }

    /**
     * Called by the runtime when user wants to connect to a certain peripheral.
     * @param {number} id - the id of the peripheral to connect to.
     */
  }, {
    key: "connect",
    value: function connect(id) {
      if (this._ble) {
        this._ble.connectPeripheral(id);
      }
    }

    /**
     * Called by the runtime when a user exits the connection popup.
     * Disconnect from the GDX FOR.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this.reset();
    }

    /**
     * Reset all the state and timeout/interval ids.
     */
  }, {
    key: "reset",
    value: function reset() {
      this._sensors = {
        force: 0,
        accelerationX: 0,
        accelerationY: 0,
        accelerationZ: 0,
        spinSpeedX: 0,
        spinSpeedY: 0,
        spinSpeedZ: 0
      };
      if (this._timeoutID) {
        window.clearInterval(this._timeoutID);
        this._timeoutID = null;
      }
    }

    /**
     * Return true if connected to the goforce device.
     * @return {boolean} - whether the goforce is connected.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      var connected = false;
      if (this._ble) {
        connected = this._ble.isConnected();
      }
      return connected;
    }

    /**
     * Starts reading data from peripheral after BLE has connected to it.
     * @private
     */
  }, {
    key: "_onConnect",
    value: function _onConnect() {
      var _this = this;
      var adapter = new ScratchLinkDeviceAdapter(this._ble, BLEUUID);
      godirect.createDevice(adapter, {
        open: true,
        startMeasurements: false
      }).then(function (device) {
        // Setup device
        _this._device = device;
        _this._device.keepValues = false; // todo: possibly remove after updating Vernier godirect module

        // Enable sensors
        _this._device.sensors.forEach(function (sensor) {
          sensor.setEnabled(true);
        });

        // Set sensor value-update behavior
        _this._device.on('measurements-started', function () {
          var enabledSensors = _this._device.sensors.filter(function (s) {
            return s.enabled;
          });
          enabledSensors.forEach(function (sensor) {
            sensor.on('value-changed', function (s) {
              _this._onSensorValueChanged(s);
            });
          });
          _this._timeoutID = window.setInterval(function () {
            return _this._ble.handleDisconnectError(BLEDataStoppedError);
          }, BLETimeout);
        });

        // Start device
        _this._device.start(GDXFOR_UPDATE_RATE);
      });
    }

    /**
     * Handler for sensor value changes from the goforce device.
     * @param {object} sensor - goforce device sensor whose value has changed
     * @private
     */
  }, {
    key: "_onSensorValueChanged",
    value: function _onSensorValueChanged(sensor) {
      var _this2 = this;
      switch (sensor.number) {
        case GDXFOR_SENSOR.FORCE:
          // Normalize the force, which can be measured between -50 and 50 N,
          // to be a value between -100 and 100.
          this._sensors.force = MathUtil.clamp(sensor.value * 2, -100, 100);
          break;
        case GDXFOR_SENSOR.ACCELERATION_X:
          this._sensors.accelerationX = sensor.value;
          break;
        case GDXFOR_SENSOR.ACCELERATION_Y:
          this._sensors.accelerationY = sensor.value;
          break;
        case GDXFOR_SENSOR.ACCELERATION_Z:
          this._sensors.accelerationZ = sensor.value;
          break;
        case GDXFOR_SENSOR.SPIN_SPEED_X:
          this._sensors.spinSpeedX = this._spinSpeedFromGyro(sensor.value);
          break;
        case GDXFOR_SENSOR.SPIN_SPEED_Y:
          this._sensors.spinSpeedY = this._spinSpeedFromGyro(sensor.value);
          break;
        case GDXFOR_SENSOR.SPIN_SPEED_Z:
          this._sensors.spinSpeedZ = this._spinSpeedFromGyro(sensor.value);
          break;
      }
      // cancel disconnect timeout and start a new one
      window.clearInterval(this._timeoutID);
      this._timeoutID = window.setInterval(function () {
        return _this2._ble.handleDisconnectError(BLEDataStoppedError);
      }, BLETimeout);
    }
  }, {
    key: "_spinSpeedFromGyro",
    value: function _spinSpeedFromGyro(val) {
      var framesPerSec = 1000 / this._runtime.currentStepTime;
      val = MathUtil.radToDeg(val);
      val = val / framesPerSec; // convert to from degrees per sec to degrees per frame
      val = val * -1;
      return val;
    }
  }, {
    key: "getForce",
    value: function getForce() {
      return this._sensors.force;
    }
  }, {
    key: "getTiltFrontBack",
    value: function getTiltFrontBack() {
      var back = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
      var x = this.getAccelerationX();
      var y = this.getAccelerationY();
      var z = this.getAccelerationZ();

      // Compute the yz unit vector
      var y2 = y * y;
      var z2 = z * z;
      var value = y2 + z2;
      value = Math.sqrt(value);

      // For sufficiently small zy vector values we are essentially at 90 degrees.
      // The following snaps to 90 and avoids divide-by-zero errors.
      // The snap factor was derived through observation -- just enough to
      // still allow single degree steps up to 90 (..., 87, 88, 89, 90).
      if (value < 0.35) {
        value = x < 0 ? 90 : -90;
      } else {
        value = x / value;
        value = Math.atan(value);
        value = MathUtil.radToDeg(value) * -1;
      }

      // Back is the inverse of front
      if (back) value *= -1;
      return value;
    }
  }, {
    key: "getTiltLeftRight",
    value: function getTiltLeftRight() {
      var right = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
      var x = this.getAccelerationX();
      var y = this.getAccelerationY();
      var z = this.getAccelerationZ();

      // Compute the yz unit vector
      var x2 = x * x;
      var z2 = z * z;
      var value = x2 + z2;
      value = Math.sqrt(value);

      // For sufficiently small zy vector values we are essentially at 90 degrees.
      // The following snaps to 90 and avoids divide-by-zero errors.
      // The snap factor was derived through observation -- just enough to
      // still allow single degree steps up to 90 (..., 87, 88, 89, 90).
      if (value < 0.35) {
        value = y < 0 ? 90 : -90;
      } else {
        value = y / value;
        value = Math.atan(value);
        value = MathUtil.radToDeg(value) * -1;
      }

      // Right is the inverse of left
      if (right) value *= -1;
      return value;
    }
  }, {
    key: "getAccelerationX",
    value: function getAccelerationX() {
      return this._sensors.accelerationX;
    }
  }, {
    key: "getAccelerationY",
    value: function getAccelerationY() {
      return this._sensors.accelerationY;
    }
  }, {
    key: "getAccelerationZ",
    value: function getAccelerationZ() {
      return this._sensors.accelerationZ;
    }
  }, {
    key: "getSpinSpeedX",
    value: function getSpinSpeedX() {
      return this._sensors.spinSpeedX;
    }
  }, {
    key: "getSpinSpeedY",
    value: function getSpinSpeedY() {
      return this._sensors.spinSpeedY;
    }
  }, {
    key: "getSpinSpeedZ",
    value: function getSpinSpeedZ() {
      return this._sensors.spinSpeedZ;
    }
  }]);
}();
/**
 * Enum for pushed and pulled menu options.
 * @readonly
 * @enum {string}
 */
var PushPullValues = {
  PUSHED: 'pushed',
  PULLED: 'pulled'
};

/**
 * Enum for motion gesture menu options.
 * @readonly
 * @enum {string}
 */
var GestureValues = {
  SHAKEN: 'shaken',
  STARTED_FALLING: 'started falling',
  TURNED_FACE_UP: 'turned face up',
  TURNED_FACE_DOWN: 'turned face down'
};

/**
 * Enum for tilt axis menu options.
 * @readonly
 * @enum {string}
 */
var TiltAxisValues = {
  FRONT: 'front',
  BACK: 'back',
  LEFT: 'left',
  RIGHT: 'right',
  ANY: 'any'
};

/**
 * Enum for axis menu options.
 * @readonly
 * @enum {string}
 */
var AxisValues = {
  X: 'x',
  Y: 'y',
  Z: 'z'
};

/**
 * Scratch 3.0 blocks to interact with a GDX-FOR peripheral.
 */
var Scratch3GdxForBlocks = /*#__PURE__*/function () {
  /**
   * Construct a set of GDX-FOR blocks.
   * @param {Runtime} runtime - the Scratch 3.0 runtime.
   */
  function Scratch3GdxForBlocks(runtime) {
    _classCallCheck(this, Scratch3GdxForBlocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;

    // Create a new GdxFor peripheral instance
    this._peripheral = new GdxFor(this.runtime, Scratch3GdxForBlocks.EXTENSION_ID);
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(Scratch3GdxForBlocks, [{
    key: "AXIS_MENU",
    get: function get() {
      return [{
        text: 'x',
        value: AxisValues.X
      }, {
        text: 'y',
        value: AxisValues.Y
      }, {
        text: 'z',
        value: AxisValues.Z
      }];
    }
  }, {
    key: "TILT_MENU",
    get: function get() {
      return [{
        text: formatMessage({
          id: 'gdxfor.tiltDirectionMenu.front',
          "default": 'front',
          description: 'label for front element in tilt direction picker for gdxfor extension'
        }),
        value: TiltAxisValues.FRONT
      }, {
        text: formatMessage({
          id: 'gdxfor.tiltDirectionMenu.back',
          "default": 'back',
          description: 'label for back element in tilt direction picker for gdxfor extension'
        }),
        value: TiltAxisValues.BACK
      }, {
        text: formatMessage({
          id: 'gdxfor.tiltDirectionMenu.left',
          "default": 'left',
          description: 'label for left element in tilt direction picker for gdxfor extension'
        }),
        value: TiltAxisValues.LEFT
      }, {
        text: formatMessage({
          id: 'gdxfor.tiltDirectionMenu.right',
          "default": 'right',
          description: 'label for right element in tilt direction picker for gdxfor extension'
        }),
        value: TiltAxisValues.RIGHT
      }];
    }
  }, {
    key: "TILT_MENU_ANY",
    get: function get() {
      return [].concat(_toConsumableArray(this.TILT_MENU), [{
        text: formatMessage({
          id: 'gdxfor.tiltDirectionMenu.any',
          "default": 'any',
          description: 'label for any direction element in tilt direction picker for gdxfor extension'
        }),
        value: TiltAxisValues.ANY
      }]);
    }
  }, {
    key: "PUSH_PULL_MENU",
    get: function get() {
      return [{
        text: formatMessage({
          id: 'gdxfor.pushed',
          "default": 'pushed',
          description: 'the force sensor was pushed inward'
        }),
        value: PushPullValues.PUSHED
      }, {
        text: formatMessage({
          id: 'gdxfor.pulled',
          "default": 'pulled',
          description: 'the force sensor was pulled outward'
        }),
        value: PushPullValues.PULLED
      }];
    }
  }, {
    key: "GESTURE_MENU",
    get: function get() {
      return [{
        text: formatMessage({
          id: 'gdxfor.shaken',
          "default": 'shaken',
          description: 'the sensor was shaken'
        }),
        value: GestureValues.SHAKEN
      }, {
        text: formatMessage({
          id: 'gdxfor.startedFalling',
          "default": 'started falling',
          description: 'the sensor started free falling'
        }),
        value: GestureValues.STARTED_FALLING
      }, {
        text: formatMessage({
          id: 'gdxfor.turnedFaceUp',
          "default": 'turned face up',
          description: 'the sensor was turned to face up'
        }),
        value: GestureValues.TURNED_FACE_UP
      }, {
        text: formatMessage({
          id: 'gdxfor.turnedFaceDown',
          "default": 'turned face down',
          description: 'the sensor was turned to face down'
        }),
        value: GestureValues.TURNED_FACE_DOWN
      }];
    }
  }, {
    key: "getInfo",
    value: function getInfo() {
      return {
        id: Scratch3GdxForBlocks.EXTENSION_ID,
        name: Scratch3GdxForBlocks.EXTENSION_NAME,
        blockIconURI: blockIconURI,
        menuIconURI: menuIconURI,
        showStatusButton: true,
        blocks: [{
          opcode: 'whenGesture',
          text: formatMessage({
            id: 'gdxfor.whenGesture',
            "default": 'when [GESTURE]',
            description: 'when the sensor detects a gesture'
          }),
          blockType: BlockType.HAT,
          arguments: {
            GESTURE: {
              type: ArgumentType.STRING,
              menu: 'gestureOptions',
              defaultValue: GestureValues.SHAKEN
            }
          }
        }, {
          opcode: 'whenForcePushedOrPulled',
          text: formatMessage({
            id: 'gdxfor.whenForcePushedOrPulled',
            "default": 'when force sensor [PUSH_PULL]',
            description: 'when the force sensor is pushed or pulled'
          }),
          blockType: BlockType.HAT,
          arguments: {
            PUSH_PULL: {
              type: ArgumentType.STRING,
              menu: 'pushPullOptions',
              defaultValue: PushPullValues.PUSHED
            }
          }
        }, {
          opcode: 'getForce',
          text: formatMessage({
            id: 'gdxfor.getForce',
            "default": 'force',
            description: 'gets force'
          }),
          blockType: BlockType.REPORTER
        }, '---', {
          opcode: 'whenTilted',
          text: formatMessage({
            id: 'gdxfor.whenTilted',
            "default": 'when tilted [TILT]',
            description: 'when the sensor detects tilt'
          }),
          blockType: BlockType.HAT,
          arguments: {
            TILT: {
              type: ArgumentType.STRING,
              menu: 'tiltAnyOptions',
              defaultValue: TiltAxisValues.ANY
            }
          }
        }, {
          opcode: 'isTilted',
          text: formatMessage({
            id: 'gdxfor.isTilted',
            "default": 'tilted [TILT]?',
            description: 'is the device tilted?'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            TILT: {
              type: ArgumentType.STRING,
              menu: 'tiltAnyOptions',
              defaultValue: TiltAxisValues.ANY
            }
          }
        }, {
          opcode: 'getTilt',
          text: formatMessage({
            id: 'gdxfor.getTilt',
            "default": 'tilt angle [TILT]',
            description: 'gets tilt'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            TILT: {
              type: ArgumentType.STRING,
              menu: 'tiltOptions',
              defaultValue: TiltAxisValues.FRONT
            }
          }
        }, '---', {
          opcode: 'isFreeFalling',
          text: formatMessage({
            id: 'gdxfor.isFreeFalling',
            "default": 'falling?',
            description: 'is the device in free fall?'
          }),
          blockType: BlockType.BOOLEAN
        }, {
          opcode: 'getSpinSpeed',
          text: formatMessage({
            id: 'gdxfor.getSpin',
            "default": 'spin speed [DIRECTION]',
            description: 'gets spin speed'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'axisOptions',
              defaultValue: AxisValues.Z
            }
          }
        }, {
          opcode: 'getAcceleration',
          text: formatMessage({
            id: 'gdxfor.getAcceleration',
            "default": 'acceleration [DIRECTION]',
            description: 'gets acceleration'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'axisOptions',
              defaultValue: AxisValues.X
            }
          }
        }],
        menus: {
          pushPullOptions: {
            acceptReporters: true,
            items: this.PUSH_PULL_MENU
          },
          gestureOptions: {
            acceptReporters: true,
            items: this.GESTURE_MENU
          },
          axisOptions: {
            acceptReporters: true,
            items: this.AXIS_MENU
          },
          tiltOptions: {
            acceptReporters: true,
            items: this.TILT_MENU
          },
          tiltAnyOptions: {
            acceptReporters: true,
            items: this.TILT_MENU_ANY
          }
        }
      };
    }
  }, {
    key: "whenForcePushedOrPulled",
    value: function whenForcePushedOrPulled(args) {
      switch (args.PUSH_PULL) {
        case PushPullValues.PUSHED:
          return this._peripheral.getForce() < FORCE_THRESHOLD * -1;
        case PushPullValues.PULLED:
          return this._peripheral.getForce() > FORCE_THRESHOLD;
        default:
          log.warn("unknown push/pull value in whenForcePushedOrPulled: ".concat(args.PUSH_PULL));
          return false;
      }
    }
  }, {
    key: "getForce",
    value: function getForce() {
      return Math.round(this._peripheral.getForce());
    }
  }, {
    key: "whenGesture",
    value: function whenGesture(args) {
      switch (args.GESTURE) {
        case GestureValues.SHAKEN:
          return this.gestureMagnitude() > SHAKEN_THRESHOLD;
        case GestureValues.STARTED_FALLING:
          return this.isFreeFalling();
        case GestureValues.TURNED_FACE_UP:
          return this._isFacing(GestureValues.TURNED_FACE_UP);
        case GestureValues.TURNED_FACE_DOWN:
          return this._isFacing(GestureValues.TURNED_FACE_DOWN);
        default:
          log.warn("unknown gesture value in whenGesture: ".concat(args.GESTURE));
          return false;
      }
    }
  }, {
    key: "_isFacing",
    value: function _isFacing(direction) {
      if (typeof this._facingUp === 'undefined') {
        this._facingUp = false;
      }
      if (typeof this._facingDown === 'undefined') {
        this._facingDown = false;
      }

      // If the sensor is already facing up or down, reduce the threshold.
      // This prevents small fluctations in acceleration while it is being
      // turned from causing the hat block to trigger multiple times.
      var threshold = FACING_THRESHOLD;
      if (this._facingUp || this._facingDown) {
        threshold -= FACING_THRESHOLD_OFFSET;
      }
      this._facingUp = this._peripheral.getAccelerationZ() > threshold;
      this._facingDown = this._peripheral.getAccelerationZ() < threshold * -1;
      switch (direction) {
        case GestureValues.TURNED_FACE_UP:
          return this._facingUp;
        case GestureValues.TURNED_FACE_DOWN:
          return this._facingDown;
        default:
          return false;
      }
    }
  }, {
    key: "whenTilted",
    value: function whenTilted(args) {
      return this._isTilted(args.TILT);
    }
  }, {
    key: "isTilted",
    value: function isTilted(args) {
      return this._isTilted(args.TILT);
    }
  }, {
    key: "getTilt",
    value: function getTilt(args) {
      return this._getTiltAngle(args.TILT);
    }
  }, {
    key: "_isTilted",
    value: function _isTilted(direction) {
      switch (direction) {
        case TiltAxisValues.ANY:
          return this._getTiltAngle(TiltAxisValues.FRONT) > TILT_THRESHOLD || this._getTiltAngle(TiltAxisValues.BACK) > TILT_THRESHOLD || this._getTiltAngle(TiltAxisValues.LEFT) > TILT_THRESHOLD || this._getTiltAngle(TiltAxisValues.RIGHT) > TILT_THRESHOLD;
        default:
          return this._getTiltAngle(direction) > TILT_THRESHOLD;
      }
    }
  }, {
    key: "_getTiltAngle",
    value: function _getTiltAngle(direction) {
      // Tilt values are calculated using acceleration due to gravity,
      // so we need to return 0 when the peripheral is not connected.
      if (!this._peripheral.isConnected()) {
        return 0;
      }
      switch (direction) {
        case TiltAxisValues.FRONT:
          return Math.round(this._peripheral.getTiltFrontBack(true));
        case TiltAxisValues.BACK:
          return Math.round(this._peripheral.getTiltFrontBack(false));
        case TiltAxisValues.LEFT:
          return Math.round(this._peripheral.getTiltLeftRight(true));
        case TiltAxisValues.RIGHT:
          return Math.round(this._peripheral.getTiltLeftRight(false));
        default:
          log.warn("Unknown direction in getTilt: ".concat(direction));
      }
    }
  }, {
    key: "getSpinSpeed",
    value: function getSpinSpeed(args) {
      switch (args.DIRECTION) {
        case AxisValues.X:
          return Math.round(this._peripheral.getSpinSpeedX());
        case AxisValues.Y:
          return Math.round(this._peripheral.getSpinSpeedY());
        case AxisValues.Z:
          return Math.round(this._peripheral.getSpinSpeedZ());
        default:
          log.warn("Unknown direction in getSpinSpeed: ".concat(args.DIRECTION));
      }
    }
  }, {
    key: "getAcceleration",
    value: function getAcceleration(args) {
      switch (args.DIRECTION) {
        case AxisValues.X:
          return Math.round(this._peripheral.getAccelerationX());
        case AxisValues.Y:
          return Math.round(this._peripheral.getAccelerationY());
        case AxisValues.Z:
          return Math.round(this._peripheral.getAccelerationZ());
        default:
          log.warn("Unknown direction in getAcceleration: ".concat(args.DIRECTION));
      }
    }

    /**
     * @param {number} x - x axis vector
     * @param {number} y - y axis vector
     * @param {number} z - z axis vector
     * @return {number} - the magnitude of a three dimension vector.
     */
  }, {
    key: "magnitude",
    value: function magnitude(x, y, z) {
      return Math.sqrt(x * x + y * y + z * z);
    }
  }, {
    key: "accelMagnitude",
    value: function accelMagnitude() {
      return this.magnitude(this._peripheral.getAccelerationX(), this._peripheral.getAccelerationY(), this._peripheral.getAccelerationZ());
    }
  }, {
    key: "gestureMagnitude",
    value: function gestureMagnitude() {
      return this.accelMagnitude() - GRAVITY;
    }
  }, {
    key: "spinMagnitude",
    value: function spinMagnitude() {
      return this.magnitude(this._peripheral.getSpinSpeedX(), this._peripheral.getSpinSpeedY(), this._peripheral.getSpinSpeedZ());
    }
  }, {
    key: "isFreeFalling",
    value: function isFreeFalling() {
      // When the peripheral is not connected, the acceleration magnitude
      // is 0 instead of ~9.8, which ends up calculating as a positive
      // free fall; so we need to return 'false' here to prevent returning 'true'.
      if (!this._peripheral.isConnected()) {
        return false;
      }
      var accelMag = this.accelMagnitude();
      var spinMag = this.spinMagnitude();

      // We want to account for rotation during freefall,
      // so we tack on a an estimated "rotational effect"
      // The FREEFALL_ROTATION_FACTOR const is used to both scale the
      // gyro measurements and convert them to radians/second.
      // So, we compare our accel magnitude against:
      // FREEFALL_THRESHOLD + (some_scaled_magnitude_of_rotation).
      var ffThresh = FREEFALL_THRESHOLD + FREEFALL_ROTATION_FACTOR * spinMag;
      return accelMag < ffThresh;
    }
  }], [{
    key: "EXTENSION_NAME",
    get:
    /**
     * @return {string} - the name of this extension.
     */
    function get() {
      return 'Force and Acceleration';
    }

    /**
     * @return {string} - the ID of this extension.
     */
  }, {
    key: "EXTENSION_ID",
    get: function get() {
      return 'gdxfor';
    }
  }]);
}();
module.exports = Scratch3GdxForBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_gdx_for/scratch-link-device-adapter.js":
/*!****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_gdx_for/scratch-link-device-adapter.js ***!
  \****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Base64Util = __webpack_require__(/*! ../../util/base64-util */ "../vm/src/util/base64-util.js");

/**
 * Adapter class
 */
var ScratchLinkDeviceAdapter = /*#__PURE__*/function () {
  function ScratchLinkDeviceAdapter(socket, _ref) {
    var service = _ref.service,
      commandChar = _ref.commandChar,
      responseChar = _ref.responseChar;
    _classCallCheck(this, ScratchLinkDeviceAdapter);
    this.socket = socket;
    this._service = service;
    this._commandChar = commandChar;
    this._responseChar = responseChar;
    this._onResponse = this._onResponse.bind(this);
    this._deviceOnResponse = null;
  }
  return _createClass(ScratchLinkDeviceAdapter, [{
    key: "godirectAdapter",
    get: function get() {
      return true;
    }
  }, {
    key: "writeCommand",
    value: function writeCommand(commandBuffer) {
      var data = Base64Util.uint8ArrayToBase64(commandBuffer);
      return this.socket.write(this._service, this._commandChar, data, 'base64');
    }
  }, {
    key: "setup",
    value: function setup(_ref2) {
      var onResponse = _ref2.onResponse;
      this._deviceOnResponse = onResponse;
      return this.socket.startNotifications(this._service, this._responseChar, this._onResponse);

      // TODO:
      // How do we find out from scratch link if communication closes?
    }
  }, {
    key: "_onResponse",
    value: function _onResponse(base64) {
      var array = Base64Util.base64ToUint8Array(base64);
      var response = new DataView(array.buffer);
      return this._deviceOnResponse(response);
    }
  }]);
}();
module.exports = ScratchLinkDeviceAdapter;

/***/ }),

/***/ "../vm/src/extensions/scratch3_makeymakey/index.js":
/*!*********************************************************!*\
  !*** ../vm/src/extensions/scratch3_makeymakey/index.js ***!
  \*********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/svg+xml;base64,';

/**
 * Length of the buffer to store key presses for the "when keys pressed in order" hat
 * @type {number}
 */
var KEY_BUFFER_LENGTH = 100;

/**
 * Timeout in milliseconds to reset the completed flag for a sequence.
 * @type {number}
 */
var SEQUENCE_HAT_TIMEOUT = 100;

/**
 * An id for the space key on a keyboard.
 */
var KEY_ID_SPACE = 'SPACE';

/**
 * An id for the left arrow key on a keyboard.
 */
var KEY_ID_LEFT = 'LEFT';

/**
 * An id for the right arrow key on a keyboard.
 */
var KEY_ID_RIGHT = 'RIGHT';

/**
 * An id for the up arrow key on a keyboard.
 */
var KEY_ID_UP = 'UP';

/**
 * An id for the down arrow key on a keyboard.
 */
var KEY_ID_DOWN = 'DOWN';

/**
 * Names used by keyboard io for keys used in scratch.
 * @enum {string}
 */
var SCRATCH_KEY_NAME = _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, KEY_ID_SPACE, 'space'), KEY_ID_LEFT, 'left arrow'), KEY_ID_UP, 'up arrow'), KEY_ID_RIGHT, 'right arrow'), KEY_ID_DOWN, 'down arrow');

/**
 * Class for the makey makey blocks in Scratch 3.0
 * @constructor
 */
var Scratch3MakeyMakeyBlocks = /*#__PURE__*/function () {
  function Scratch3MakeyMakeyBlocks(runtime) {
    var _this = this;
    _classCallCheck(this, Scratch3MakeyMakeyBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * A toggle that alternates true and false each frame, so that an
     * edge-triggered hat can trigger on every other frame.
     * @type {boolean}
     */
    this.frameToggle = false;

    // Set an interval that toggles the frameToggle every frame.
    setInterval(function () {
      _this.frameToggle = !_this.frameToggle;
    }, this.runtime.currentStepTime);
    this.keyPressed = this.keyPressed.bind(this);
    this.runtime.on('KEY_PRESSED', this.keyPressed);
    this._clearkeyPressBuffer = this._clearkeyPressBuffer.bind(this);
    this.runtime.on('PROJECT_STOP_ALL', this._clearkeyPressBuffer);

    /*
     * An object containing a set of sequence objects.
     * These are the key sequences currently being detected by the "when
     * keys pressed in order" hat block. Each sequence is keyed by its
     * string representation (the sequence's value in the menu, which is a
     * string of KEY_IDs separated by spaces). Each sequence object
     * has an array property (an array of KEY_IDs) and a boolean
     * completed property that is true when the sequence has just been
     * pressed.
     * @type {object}
     */
    this.sequences = {};

    /*
     * An array of the key codes of recently pressed keys.
     * @type {array}
     */
    this.keyPressBuffer = [];
  }

  /*
  * Localized short-form names of the space bar and arrow keys, for use in the
  * displayed menu items of the "when keys pressed in order" block.
  * @type {object}
  */
  return _createClass(Scratch3MakeyMakeyBlocks, [{
    key: "KEY_TEXT_SHORT",
    get: function get() {
      return _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, KEY_ID_SPACE, formatMessage({
        id: 'makeymakey.spaceKey',
        "default": 'space',
        description: 'The space key on a computer keyboard.'
      })), KEY_ID_LEFT, formatMessage({
        id: 'makeymakey.leftArrowShort',
        "default": 'left',
        description: 'Short name for the left arrow key on a computer keyboard.'
      })), KEY_ID_UP, formatMessage({
        id: 'makeymakey.upArrowShort',
        "default": 'up',
        description: 'Short name for the up arrow key on a computer keyboard.'
      })), KEY_ID_RIGHT, formatMessage({
        id: 'makeymakey.rightArrowShort',
        "default": 'right',
        description: 'Short name for the right arrow key on a computer keyboard.'
      })), KEY_ID_DOWN, formatMessage({
        id: 'makeymakey.downArrowShort',
        "default": 'down',
        description: 'Short name for the down arrow key on a computer keyboard.'
      }));
    }

    /*
     * An array of strings of KEY_IDs representing the default set of
     * key sequences for use by the "when keys pressed in order" block.
     * @type {array}
     */
  }, {
    key: "DEFAULT_SEQUENCES",
    get: function get() {
      return ["".concat(KEY_ID_LEFT, " ").concat(KEY_ID_UP, " ").concat(KEY_ID_RIGHT), "".concat(KEY_ID_RIGHT, " ").concat(KEY_ID_UP, " ").concat(KEY_ID_LEFT), "".concat(KEY_ID_LEFT, " ").concat(KEY_ID_RIGHT), "".concat(KEY_ID_RIGHT, " ").concat(KEY_ID_LEFT), "".concat(KEY_ID_UP, " ").concat(KEY_ID_DOWN), "".concat(KEY_ID_DOWN, " ").concat(KEY_ID_UP), "".concat(KEY_ID_UP, " ").concat(KEY_ID_RIGHT, " ").concat(KEY_ID_DOWN, " ").concat(KEY_ID_LEFT), "".concat(KEY_ID_UP, " ").concat(KEY_ID_LEFT, " ").concat(KEY_ID_DOWN, " ").concat(KEY_ID_RIGHT), "".concat(KEY_ID_UP, " ").concat(KEY_ID_UP, " ").concat(KEY_ID_DOWN, " ").concat(KEY_ID_DOWN, " ") + "".concat(KEY_ID_LEFT, " ").concat(KEY_ID_RIGHT, " ").concat(KEY_ID_LEFT, " ").concat(KEY_ID_RIGHT)];
    }

    /**
     * @returns {object} metadata for this extension and its blocks.
     */
  }, {
    key: "getInfo",
    value: function getInfo() {
      return {
        id: 'makeymakey',
        name: 'Makey Makey',
        blockIconURI: blockIconURI,
        blocks: [{
          opcode: 'whenMakeyKeyPressed',
          text: formatMessage({
            id: 'makeymakey.whenKeyPressed',
            "default": 'when [KEY] key pressed',
            description: 'when a keyboard key is pressed'
          }),
          blockType: BlockType.HAT,
          arguments: {
            KEY: {
              type: ArgumentType.STRING,
              menu: 'KEY',
              defaultValue: KEY_ID_SPACE
            }
          }
        }, {
          opcode: 'whenCodePressed',
          text: formatMessage({
            id: 'makeymakey.whenKeysPressedInOrder',
            "default": 'when [SEQUENCE] pressed in order',
            description: 'when a sequence of keyboard keys is pressed in a specific order'
          }),
          blockType: BlockType.HAT,
          arguments: {
            SEQUENCE: {
              type: ArgumentType.STRING,
              menu: 'SEQUENCE',
              defaultValue: this.DEFAULT_SEQUENCES[0]
            }
          }
        }],
        menus: {
          KEY: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'makeymakey.spaceKey',
                "default": 'space',
                description: 'The space key on a computer keyboard.'
              }),
              value: KEY_ID_SPACE
            }, {
              text: formatMessage({
                id: 'makeymakey.upArrow',
                "default": 'up arrow',
                description: 'The up arrow key on a computer keyboard.'
              }),
              value: KEY_ID_UP
            }, {
              text: formatMessage({
                id: 'makeymakey.downArrow',
                "default": 'down arrow',
                description: 'The down arrow key on a computer keyboard.'
              }),
              value: KEY_ID_DOWN
            }, {
              text: formatMessage({
                id: 'makeymakey.rightArrow',
                "default": 'right arrow',
                description: 'The right arrow key on a computer keyboard.'
              }),
              value: KEY_ID_RIGHT
            }, {
              text: formatMessage({
                id: 'makeymakey.leftArrow',
                "default": 'left arrow',
                description: 'The left arrow key on a computer keyboard.'
              }),
              value: KEY_ID_LEFT
            }, {
              text: 'w',
              value: 'w'
            }, {
              text: 'a',
              value: 'a'
            }, {
              text: 's',
              value: 's'
            }, {
              text: 'd',
              value: 'd'
            }, {
              text: 'f',
              value: 'f'
            }, {
              text: 'g',
              value: 'g'
            }]
          },
          SEQUENCE: {
            acceptReporters: true,
            items: this.buildSequenceMenu(this.DEFAULT_SEQUENCES)
          }
        }
      };
    }

    /*
     * Build the menu of key sequences.
     * @param {array} sequencesArray an array of strings of KEY_IDs.
     * @returns {array} an array of objects with text and value properties.
     */
  }, {
    key: "buildSequenceMenu",
    value: function buildSequenceMenu(sequencesArray) {
      var _this2 = this;
      return sequencesArray.map(function (str) {
        return _this2.getMenuItemForSequenceString(str);
      });
    }

    /*
     * Create a menu item for a sequence string.
     * @param {string} sequenceString a string of KEY_IDs.
     * @return {object} an object with text and value properties.
     */
  }, {
    key: "getMenuItemForSequenceString",
    value: function getMenuItemForSequenceString(sequenceString) {
      var _this3 = this;
      var sequenceArray = sequenceString.split(' ');
      sequenceArray = sequenceArray.map(function (str) {
        return _this3.KEY_TEXT_SHORT[str];
      });
      return {
        text: sequenceArray.join(' '),
        value: sequenceString
      };
    }

    /*
     * Check whether a keyboard key is currently pressed.
     * Also, toggle the results of the test on alternate frames, so that the
     * hat block fires repeatedly.
     * @param {object} args - the block arguments.
     * @property {number} KEY - a key code.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "whenMakeyKeyPressed",
    value: function whenMakeyKeyPressed(args, util) {
      var key = args.KEY;
      // Convert the key arg, if it is a KEY_ID, to the key name used by
      // the Keyboard io module.
      if (SCRATCH_KEY_NAME[args.KEY]) {
        key = SCRATCH_KEY_NAME[args.KEY];
      }
      var isDown = util.ioQuery('keyboard', 'getKeyIsDown', [key]);
      return isDown && this.frameToggle;
    }

    /*
     * A function called on the KEY_PRESSED event, to update the key press
     * buffer and check if any of the key sequences have been completed.
     * @param {string} key A scratch key name.
     */
  }, {
    key: "keyPressed",
    value: function keyPressed(key) {
      var _this4 = this;
      // Store only the first word of the Scratch key name, so that e.g. when
      // "left arrow" is pressed, we store "LEFT", which matches KEY_ID_LEFT
      key = key.split(' ')[0];
      key = key.toUpperCase();
      this.keyPressBuffer.push(key);
      // Keep the buffer under the length limit
      if (this.keyPressBuffer.length > KEY_BUFFER_LENGTH) {
        this.keyPressBuffer.shift();
      }
      // Check the buffer for each sequence in use
      var _loop = function _loop(str) {
        var arr = _this4.sequences[str].array;
        // Bail out if we don't have enough presses for this sequence
        if (_this4.keyPressBuffer.length < arr.length) {
          return 1; // continue
        }
        var missFlag = false;
        // Slice the buffer to the length of the sequence we're checking
        var bufferSegment = _this4.keyPressBuffer.slice(-1 * arr.length);
        for (var i = 0; i < arr.length; i++) {
          if (arr[i] !== bufferSegment[i]) {
            missFlag = true;
          }
        }
        // If the miss flag is false, the sequence matched the buffer
        if (!missFlag) {
          _this4.sequences[str].completed = true;
          // Clear the completed flag after a timeout. This is necessary because
          // the hat is edge-triggered (not event triggered). Multiple hats
          // may be checking the same sequence, so this timeout gives them enough
          // time to all trigger before resetting the flag.
          setTimeout(function () {
            _this4.sequences[str].completed = false;
          }, SEQUENCE_HAT_TIMEOUT);
        }
      };
      for (var str in this.sequences) {
        if (_loop(str)) continue;
      }
    }

    /**
     * Clear the key press buffer.
     */
  }, {
    key: "_clearkeyPressBuffer",
    value: function _clearkeyPressBuffer() {
      this.keyPressBuffer = [];
    }

    /*
     * Add a key sequence to the set currently being checked on each key press.
     * @param {string} sequenceString a string of space-separated KEY_IDs.
     * @param {array} sequenceArray an array of KEY_IDs.
     */
  }, {
    key: "addSequence",
    value: function addSequence(sequenceString, sequenceArray) {
      // If we already have this sequence string, return.
      if (Object.prototype.hasOwnProperty.call(this.sequences, sequenceString)) {
        return;
      }
      this.sequences[sequenceString] = {
        array: sequenceArray,
        completed: false
      };
    }

    /*
     * Check whether a key sequence was recently completed.
     * @param {object} args The block arguments.
     * @property {number} SEQUENCE A string of KEY_IDs.
     */
  }, {
    key: "whenCodePressed",
    value: function whenCodePressed(args) {
      var sequenceString = Cast.toString(args.SEQUENCE).toUpperCase();
      var sequenceArray = sequenceString.split(' ');
      if (sequenceArray.length < 2) {
        return;
      }
      this.addSequence(sequenceString, sequenceArray);
      return this.sequences[sequenceString].completed;
    }
  }]);
}();
module.exports = Scratch3MakeyMakeyBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_microbit/index.js":
/*!*******************************************************!*\
  !*** ../vm/src/extensions/scratch3_microbit/index.js ***!
  \*******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");
var cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var BLE = __webpack_require__(/*! ../../io/ble */ "../vm/src/io/ble.js");
var Base64Util = __webpack_require__(/*! ../../util/base64-util */ "../vm/src/util/base64-util.js");

/**
 * Icon png to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/png;base64,';

/**
 * Enum for micro:bit BLE command protocol.
 * https://github.com/scratchfoundation/scratch-microbit-firmware/blob/master/protocol.md
 * @readonly
 * @enum {number}
 */
var BLECommand = {
  CMD_PIN_CONFIG: 0x80,
  CMD_DISPLAY_TEXT: 0x81,
  CMD_DISPLAY_LED: 0x82
};

/**
 * A time interval to wait (in milliseconds) before reporting to the BLE socket
 * that data has stopped coming from the peripheral.
 */
var BLETimeout = 4500;

/**
 * A time interval to wait (in milliseconds) while a block that sends a BLE message is running.
 * @type {number}
 */
var BLESendInterval = 100;

/**
 * A string to report to the BLE socket when the micro:bit has stopped receiving data.
 * @type {string}
 */
var BLEDataStoppedError = 'micro:bit extension stopped receiving data';

/**
 * Enum for micro:bit protocol.
 * https://github.com/scratchfoundation/scratch-microbit-firmware/blob/master/protocol.md
 * @readonly
 * @enum {string}
 */
var BLEUUID = {
  service: 0xf005,
  rxChar: '5261da01-fa7e-42ab-850b-7c80220097cc',
  txChar: '5261da02-fa7e-42ab-850b-7c80220097cc'
};

/**
 * Manage communication with a MicroBit peripheral over a Scrath Link client socket.
 */
var MicroBit = /*#__PURE__*/function () {
  /**
   * Construct a MicroBit communication object.
   * @param {Runtime} runtime - the Scratch 3.0 runtime
   * @param {string} extensionId - the id of the extension
   */
  function MicroBit(runtime, extensionId) {
    _classCallCheck(this, MicroBit);
    /**
     * The Scratch 3.0 runtime used to trigger the green flag button.
     * @type {Runtime}
     * @private
     */
    this._runtime = runtime;

    /**
     * The BluetoothLowEnergy connection socket for reading/writing peripheral data.
     * @type {BLE}
     * @private
     */
    this._ble = null;
    this._runtime.registerPeripheralExtension(extensionId, this);

    /**
     * The id of the extension this peripheral belongs to.
     */
    this._extensionId = extensionId;

    /**
     * The most recently received value for each sensor.
     * @type {Object.<string, number>}
     * @private
     */
    this._sensors = {
      tiltX: 0,
      tiltY: 0,
      buttonA: 0,
      buttonB: 0,
      touchPins: [0, 0, 0],
      gestureState: 0,
      ledMatrixState: new Uint8Array(5)
    };

    /**
     * The most recently received value for each gesture.
     * @type {Object.<string, Object>}
     * @private
     */
    this._gestures = {
      moving: false,
      move: {
        active: false,
        timeout: false
      },
      shake: {
        active: false,
        timeout: false
      },
      jump: {
        active: false,
        timeout: false
      }
    };

    /**
     * Interval ID for data reading timeout.
     * @type {number}
     * @private
     */
    this._timeoutID = null;

    /**
     * A flag that is true while we are busy sending data to the BLE socket.
     * @type {boolean}
     * @private
     */
    this._busy = false;

    /**
     * ID for a timeout which is used to clear the busy flag if it has been
     * true for a long time.
     */
    this._busyTimeoutID = null;
    this.reset = this.reset.bind(this);
    this._onConnect = this._onConnect.bind(this);
    this._onMessage = this._onMessage.bind(this);
  }

  /**
   * @param {string} text - the text to display.
   * @return {Promise} - a Promise that resolves when writing to peripheral.
   */
  return _createClass(MicroBit, [{
    key: "displayText",
    value: function displayText(text) {
      var output = new Uint8Array(text.length);
      for (var i = 0; i < text.length; i++) {
        output[i] = text.charCodeAt(i);
      }
      return this.send(BLECommand.CMD_DISPLAY_TEXT, output);
    }

    /**
     * @param {Uint8Array} matrix - the matrix to display.
     * @return {Promise} - a Promise that resolves when writing to peripheral.
     */
  }, {
    key: "displayMatrix",
    value: function displayMatrix(matrix) {
      return this.send(BLECommand.CMD_DISPLAY_LED, matrix);
    }

    /**
     * @return {number} - the latest value received for the tilt sensor's tilt about the X axis.
     */
  }, {
    key: "tiltX",
    get: function get() {
      return this._sensors.tiltX;
    }

    /**
     * @return {number} - the latest value received for the tilt sensor's tilt about the Y axis.
     */
  }, {
    key: "tiltY",
    get: function get() {
      return this._sensors.tiltY;
    }

    /**
     * @return {boolean} - the latest value received for the A button.
     */
  }, {
    key: "buttonA",
    get: function get() {
      return this._sensors.buttonA;
    }

    /**
     * @return {boolean} - the latest value received for the B button.
     */
  }, {
    key: "buttonB",
    get: function get() {
      return this._sensors.buttonB;
    }

    /**
     * @return {number} - the latest value received for the motion gesture states.
     */
  }, {
    key: "gestureState",
    get: function get() {
      return this._sensors.gestureState;
    }

    /**
     * @return {Uint8Array} - the current state of the 5x5 LED matrix.
     */
  }, {
    key: "ledMatrixState",
    get: function get() {
      return this._sensors.ledMatrixState;
    }

    /**
     * Called by the runtime when user wants to scan for a peripheral.
     */
  }, {
    key: "scan",
    value: function scan() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this._ble = new BLE(this._runtime, this._extensionId, {
        filters: [{
          services: [BLEUUID.service]
        }]
      }, this._onConnect, this.reset);
    }

    /**
     * Called by the runtime when user wants to connect to a certain peripheral.
     * @param {number} id - the id of the peripheral to connect to.
     */
  }, {
    key: "connect",
    value: function connect(id) {
      if (this._ble) {
        this._ble.connectPeripheral(id);
      }
    }

    /**
     * Disconnect from the micro:bit.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this.reset();
    }

    /**
     * Reset all the state and timeout/interval ids.
     */
  }, {
    key: "reset",
    value: function reset() {
      if (this._timeoutID) {
        window.clearTimeout(this._timeoutID);
        this._timeoutID = null;
      }
    }

    /**
     * Return true if connected to the micro:bit.
     * @return {boolean} - whether the micro:bit is connected.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      var connected = false;
      if (this._ble) {
        connected = this._ble.isConnected();
      }
      return connected;
    }

    /**
     * Send a message to the peripheral BLE socket.
     * @param {number} command - the BLE command hex.
     * @param {Uint8Array} message - the message to write
     */
  }, {
    key: "send",
    value: function send(command, message) {
      var _this = this;
      if (!this.isConnected()) return;
      if (this._busy) return;

      // Set a busy flag so that while we are sending a message and waiting for
      // the response, additional messages are ignored.
      this._busy = true;

      // Set a timeout after which to reset the busy flag. This is used in case
      // a BLE message was sent for which we never received a response, because
      // e.g. the peripheral was turned off after the message was sent. We reset
      // the busy flag after a while so that it is possible to try again later.
      this._busyTimeoutID = window.setTimeout(function () {
        _this._busy = false;
      }, 5000);
      var output = new Uint8Array(message.length + 1);
      output[0] = command; // attach command to beginning of message
      for (var i = 0; i < message.length; i++) {
        output[i + 1] = message[i];
      }
      var data = Base64Util.uint8ArrayToBase64(output);
      this._ble.write(BLEUUID.service, BLEUUID.txChar, data, 'base64', true).then(function () {
        _this._busy = false;
        window.clearTimeout(_this._busyTimeoutID);
      });
    }

    /**
     * Starts reading data from peripheral after BLE has connected to it.
     * @private
     */
  }, {
    key: "_onConnect",
    value: function _onConnect() {
      var _this2 = this;
      this._ble.read(BLEUUID.service, BLEUUID.rxChar, true, this._onMessage);
      this._timeoutID = window.setTimeout(function () {
        return _this2._ble.handleDisconnectError(BLEDataStoppedError);
      }, BLETimeout);
    }

    /**
     * Process the sensor data from the incoming BLE characteristic.
     * @param {object} base64 - the incoming BLE data.
     * @private
     */
  }, {
    key: "_onMessage",
    value: function _onMessage(base64) {
      var _this3 = this;
      // parse data
      var data = Base64Util.base64ToUint8Array(base64);
      this._sensors.tiltX = data[1] | data[0] << 8;
      if (this._sensors.tiltX > 1 << 15) this._sensors.tiltX -= 1 << 16;
      this._sensors.tiltY = data[3] | data[2] << 8;
      if (this._sensors.tiltY > 1 << 15) this._sensors.tiltY -= 1 << 16;
      this._sensors.buttonA = data[4];
      this._sensors.buttonB = data[5];
      this._sensors.touchPins[0] = data[6];
      this._sensors.touchPins[1] = data[7];
      this._sensors.touchPins[2] = data[8];
      this._sensors.gestureState = data[9];

      // cancel disconnect timeout and start a new one
      window.clearTimeout(this._timeoutID);
      this._timeoutID = window.setTimeout(function () {
        return _this3._ble.handleDisconnectError(BLEDataStoppedError);
      }, BLETimeout);
    }

    /**
     * @param {number} pin - the pin to check touch state.
     * @return {number} - the latest value received for the touch pin states.
     * @private
     */
  }, {
    key: "_checkPinState",
    value: function _checkPinState(pin) {
      return this._sensors.touchPins[pin];
    }
  }]);
}();
/**
 * Enum for tilt sensor direction.
 * @readonly
 * @enum {string}
 */
var MicroBitTiltDirection = {
  FRONT: 'front',
  BACK: 'back',
  LEFT: 'left',
  RIGHT: 'right',
  ANY: 'any'
};

/**
 * Enum for micro:bit gestures.
 * @readonly
 * @enum {string}
 */
var MicroBitGestures = {
  MOVED: 'moved',
  SHAKEN: 'shaken',
  JUMPED: 'jumped'
};

/**
 * Enum for micro:bit buttons.
 * @readonly
 * @enum {string}
 */
var MicroBitButtons = {
  A: 'A',
  B: 'B',
  ANY: 'any'
};

/**
 * Enum for micro:bit pin states.
 * @readonly
 * @enum {string}
 */
var MicroBitPinState = {
  ON: 'on',
  OFF: 'off'
};

/**
 * Scratch 3.0 blocks to interact with a MicroBit peripheral.
 */
var Scratch3MicroBitBlocks = /*#__PURE__*/function () {
  /**
   * Construct a set of MicroBit blocks.
   * @param {Runtime} runtime - the Scratch 3.0 runtime.
   */
  function Scratch3MicroBitBlocks(runtime) {
    _classCallCheck(this, Scratch3MicroBitBlocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;

    // Create a new MicroBit peripheral instance
    this._peripheral = new MicroBit(this.runtime, Scratch3MicroBitBlocks.EXTENSION_ID);
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(Scratch3MicroBitBlocks, [{
    key: "BUTTONS_MENU",
    get:
    /**
     * @return {array} - text and values for each buttons menu element
     */
    function get() {
      return [{
        text: 'A',
        value: MicroBitButtons.A
      }, {
        text: 'B',
        value: MicroBitButtons.B
      }, {
        text: formatMessage({
          id: 'microbit.buttonsMenu.any',
          "default": 'any',
          description: 'label for "any" element in button picker for micro:bit extension'
        }),
        value: MicroBitButtons.ANY
      }];
    }

    /**
     * @return {array} - text and values for each gestures menu element
     */
  }, {
    key: "GESTURES_MENU",
    get: function get() {
      return [{
        text: formatMessage({
          id: 'microbit.gesturesMenu.moved',
          "default": 'moved',
          description: 'label for moved gesture in gesture picker for micro:bit extension'
        }),
        value: MicroBitGestures.MOVED
      }, {
        text: formatMessage({
          id: 'microbit.gesturesMenu.shaken',
          "default": 'shaken',
          description: 'label for shaken gesture in gesture picker for micro:bit extension'
        }),
        value: MicroBitGestures.SHAKEN
      }, {
        text: formatMessage({
          id: 'microbit.gesturesMenu.jumped',
          "default": 'jumped',
          description: 'label for jumped gesture in gesture picker for micro:bit extension'
        }),
        value: MicroBitGestures.JUMPED
      }];
    }

    /**
     * @return {array} - text and values for each pin state menu element
     */
  }, {
    key: "PIN_STATE_MENU",
    get: function get() {
      return [{
        text: formatMessage({
          id: 'microbit.pinStateMenu.on',
          "default": 'on',
          description: 'label for on element in pin state picker for micro:bit extension'
        }),
        value: MicroBitPinState.ON
      }, {
        text: formatMessage({
          id: 'microbit.pinStateMenu.off',
          "default": 'off',
          description: 'label for off element in pin state picker for micro:bit extension'
        }),
        value: MicroBitPinState.OFF
      }];
    }

    /**
     * @return {array} - text and values for each tilt direction menu element
     */
  }, {
    key: "TILT_DIRECTION_MENU",
    get: function get() {
      return [{
        text: formatMessage({
          id: 'microbit.tiltDirectionMenu.front',
          "default": 'front',
          description: 'label for front element in tilt direction picker for micro:bit extension'
        }),
        value: MicroBitTiltDirection.FRONT
      }, {
        text: formatMessage({
          id: 'microbit.tiltDirectionMenu.back',
          "default": 'back',
          description: 'label for back element in tilt direction picker for micro:bit extension'
        }),
        value: MicroBitTiltDirection.BACK
      }, {
        text: formatMessage({
          id: 'microbit.tiltDirectionMenu.left',
          "default": 'left',
          description: 'label for left element in tilt direction picker for micro:bit extension'
        }),
        value: MicroBitTiltDirection.LEFT
      }, {
        text: formatMessage({
          id: 'microbit.tiltDirectionMenu.right',
          "default": 'right',
          description: 'label for right element in tilt direction picker for micro:bit extension'
        }),
        value: MicroBitTiltDirection.RIGHT
      }];
    }

    /**
     * @return {array} - text and values for each tilt direction (plus "any") menu element
     */
  }, {
    key: "TILT_DIRECTION_ANY_MENU",
    get: function get() {
      return [].concat(_toConsumableArray(this.TILT_DIRECTION_MENU), [{
        text: formatMessage({
          id: 'microbit.tiltDirectionMenu.any',
          "default": 'any',
          description: 'label for any direction element in tilt direction picker for micro:bit extension'
        }),
        value: MicroBitTiltDirection.ANY
      }]);
    }
  }, {
    key: "getInfo",
    value: function getInfo() {
      return {
        id: Scratch3MicroBitBlocks.EXTENSION_ID,
        name: Scratch3MicroBitBlocks.EXTENSION_NAME,
        blockIconURI: blockIconURI,
        showStatusButton: true,
        blocks: [{
          opcode: 'whenButtonPressed',
          text: formatMessage({
            id: 'microbit.whenButtonPressed',
            "default": 'when [BTN] button pressed',
            description: 'when the selected button on the micro:bit is pressed'
          }),
          blockType: BlockType.HAT,
          arguments: {
            BTN: {
              type: ArgumentType.STRING,
              menu: 'buttons',
              defaultValue: MicroBitButtons.A
            }
          }
        }, {
          opcode: 'isButtonPressed',
          text: formatMessage({
            id: 'microbit.isButtonPressed',
            "default": '[BTN] button pressed?',
            description: 'is the selected button on the micro:bit pressed?'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            BTN: {
              type: ArgumentType.STRING,
              menu: 'buttons',
              defaultValue: MicroBitButtons.A
            }
          }
        }, '---', {
          opcode: 'whenGesture',
          text: formatMessage({
            id: 'microbit.whenGesture',
            "default": 'when [GESTURE]',
            description: 'when the selected gesture is detected by the micro:bit'
          }),
          blockType: BlockType.HAT,
          arguments: {
            GESTURE: {
              type: ArgumentType.STRING,
              menu: 'gestures',
              defaultValue: MicroBitGestures.MOVED
            }
          }
        }, '---', {
          opcode: 'displaySymbol',
          text: formatMessage({
            id: 'microbit.displaySymbol',
            "default": 'display [MATRIX]',
            description: 'display a pattern on the micro:bit display'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MATRIX: {
              type: ArgumentType.MATRIX,
              defaultValue: '0101010101100010101000100'
            }
          }
        }, {
          opcode: 'displayText',
          text: formatMessage({
            id: 'microbit.displayText',
            "default": 'display text [TEXT]',
            description: 'display text on the micro:bit display'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            TEXT: {
              type: ArgumentType.STRING,
              defaultValue: formatMessage({
                id: 'microbit.defaultTextToDisplay',
                "default": 'Hello!',
                description: "default text to display.\n                                IMPORTANT - the micro:bit only supports letters a-z, A-Z.\n                                Please substitute a default word in your language\n                                that can be written with those characters,\n                                substitute non-accented characters or leave it as \"Hello!\".\n                                Check the micro:bit site documentation for details"
              })
            }
          }
        }, {
          opcode: 'displayClear',
          text: formatMessage({
            id: 'microbit.clearDisplay',
            "default": 'clear display',
            description: 'display nothing on the micro:bit display'
          }),
          blockType: BlockType.COMMAND
        }, '---', {
          opcode: 'whenTilted',
          text: formatMessage({
            id: 'microbit.whenTilted',
            "default": 'when tilted [DIRECTION]',
            description: 'when the micro:bit is tilted in a direction'
          }),
          blockType: BlockType.HAT,
          arguments: {
            DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'tiltDirectionAny',
              defaultValue: MicroBitTiltDirection.ANY
            }
          }
        }, {
          opcode: 'isTilted',
          text: formatMessage({
            id: 'microbit.isTilted',
            "default": 'tilted [DIRECTION]?',
            description: 'is the micro:bit is tilted in a direction?'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'tiltDirectionAny',
              defaultValue: MicroBitTiltDirection.ANY
            }
          }
        }, {
          opcode: 'getTiltAngle',
          text: formatMessage({
            id: 'microbit.tiltAngle',
            "default": 'tilt angle [DIRECTION]',
            description: 'how much the micro:bit is tilted in a direction'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'tiltDirection',
              defaultValue: MicroBitTiltDirection.FRONT
            }
          }
        }, '---', {
          opcode: 'whenPinConnected',
          text: formatMessage({
            id: 'microbit.whenPinConnected',
            "default": 'when pin [PIN] connected',
            description: 'when the pin detects a connection to Earth/Ground'
          }),
          blockType: BlockType.HAT,
          arguments: {
            PIN: {
              type: ArgumentType.STRING,
              menu: 'touchPins',
              defaultValue: '0'
            }
          }
        }],
        menus: {
          buttons: {
            acceptReporters: true,
            items: this.BUTTONS_MENU
          },
          gestures: {
            acceptReporters: true,
            items: this.GESTURES_MENU
          },
          pinState: {
            acceptReporters: true,
            items: this.PIN_STATE_MENU
          },
          tiltDirection: {
            acceptReporters: true,
            items: this.TILT_DIRECTION_MENU
          },
          tiltDirectionAny: {
            acceptReporters: true,
            items: this.TILT_DIRECTION_ANY_MENU
          },
          touchPins: {
            acceptReporters: true,
            items: ['0', '1', '2']
          }
        }
      };
    }

    /**
     * Test whether the A or B button is pressed
     * @param {object} args - the block's arguments.
     * @return {boolean} - true if the button is pressed.
     */
  }, {
    key: "whenButtonPressed",
    value: function whenButtonPressed(args) {
      if (args.BTN === 'any') {
        return this._peripheral.buttonA | this._peripheral.buttonB;
      } else if (args.BTN === 'A') {
        return this._peripheral.buttonA;
      } else if (args.BTN === 'B') {
        return this._peripheral.buttonB;
      }
      return false;
    }

    /**
     * Test whether the A or B button is pressed
     * @param {object} args - the block's arguments.
     * @return {boolean} - true if the button is pressed.
     */
  }, {
    key: "isButtonPressed",
    value: function isButtonPressed(args) {
      if (args.BTN === 'any') {
        return (this._peripheral.buttonA | this._peripheral.buttonB) !== 0;
      } else if (args.BTN === 'A') {
        return this._peripheral.buttonA !== 0;
      } else if (args.BTN === 'B') {
        return this._peripheral.buttonB !== 0;
      }
      return false;
    }

    /**
     * Test whether the micro:bit is moving
     * @param {object} args - the block's arguments.
     * @return {boolean} - true if the micro:bit is moving.
     */
  }, {
    key: "whenGesture",
    value: function whenGesture(args) {
      var gesture = cast.toString(args.GESTURE);
      if (gesture === 'moved') {
        return this._peripheral.gestureState >> 2 & 1;
      } else if (gesture === 'shaken') {
        return this._peripheral.gestureState & 1;
      } else if (gesture === 'jumped') {
        return this._peripheral.gestureState >> 1 & 1;
      }
      return false;
    }

    /**
     * Display a predefined symbol on the 5x5 LED matrix.
     * @param {object} args - the block's arguments.
     * @return {Promise} - a Promise that resolves after a tick.
     */
  }, {
    key: "displaySymbol",
    value: function displaySymbol(args) {
      var symbol = cast.toString(args.MATRIX).replace(/\s/g, '');
      var reducer = function reducer(accumulator, c, index) {
        var value = c === '0' ? accumulator : accumulator + Math.pow(2, index);
        return value;
      };
      var hex = symbol.split('').reduce(reducer, 0);
      if (hex !== null) {
        this._peripheral.ledMatrixState[0] = hex & 0x1F;
        this._peripheral.ledMatrixState[1] = hex >> 5 & 0x1F;
        this._peripheral.ledMatrixState[2] = hex >> 10 & 0x1F;
        this._peripheral.ledMatrixState[3] = hex >> 15 & 0x1F;
        this._peripheral.ledMatrixState[4] = hex >> 20 & 0x1F;
        this._peripheral.displayMatrix(this._peripheral.ledMatrixState);
      }
      return new Promise(function (resolve) {
        setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Display text on the 5x5 LED matrix.
     * @param {object} args - the block's arguments.
     * @return {Promise} - a Promise that resolves after the text is done printing.
     * Note the limit is 19 characters
     * The print time is calculated by multiplying the number of horizontal pixels
     * by the default scroll delay of 120ms.
     * The number of horizontal pixels = 6px for each character in the string,
     * 1px before the string, and 5px after the string.
     */
  }, {
    key: "displayText",
    value: function displayText(args) {
      var text = String(args.TEXT).substring(0, 19);
      if (text.length > 0) this._peripheral.displayText(text);
      var yieldDelay = 120 * (6 * text.length + 6);
      return new Promise(function (resolve) {
        setTimeout(function () {
          resolve();
        }, yieldDelay);
      });
    }

    /**
     * Turn all 5x5 matrix LEDs off.
     * @return {Promise} - a Promise that resolves after a tick.
     */
  }, {
    key: "displayClear",
    value: function displayClear() {
      for (var i = 0; i < 5; i++) {
        this._peripheral.ledMatrixState[i] = 0;
      }
      this._peripheral.displayMatrix(this._peripheral.ledMatrixState);
      return new Promise(function (resolve) {
        setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} DIRECTION - the tilt direction to test (front, back, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     */
  }, {
    key: "whenTilted",
    value: function whenTilted(args) {
      return this._isTilted(args.DIRECTION);
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} DIRECTION - the tilt direction to test (front, back, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     */
  }, {
    key: "isTilted",
    value: function isTilted(args) {
      return this._isTilted(args.DIRECTION);
    }

    /**
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} DIRECTION - the direction (front, back, left, right) to check.
     * @return {number} - the tilt sensor's angle in the specified direction.
     * Note that getTiltAngle(front) = -getTiltAngle(back) and getTiltAngle(left) = -getTiltAngle(right).
     */
  }, {
    key: "getTiltAngle",
    value: function getTiltAngle(args) {
      return this._getTiltAngle(args.DIRECTION);
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {TiltDirection} direction - the tilt direction to test (front, back, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     * @private
     */
  }, {
    key: "_isTilted",
    value: function _isTilted(direction) {
      switch (direction) {
        case MicroBitTiltDirection.ANY:
          return Math.abs(this._peripheral.tiltX / 10) >= Scratch3MicroBitBlocks.TILT_THRESHOLD || Math.abs(this._peripheral.tiltY / 10) >= Scratch3MicroBitBlocks.TILT_THRESHOLD;
        default:
          return this._getTiltAngle(direction) >= Scratch3MicroBitBlocks.TILT_THRESHOLD;
      }
    }

    /**
     * @param {TiltDirection} direction - the direction (front, back, left, right) to check.
     * @return {number} - the tilt sensor's angle in the specified direction.
     * Note that getTiltAngle(front) = -getTiltAngle(back) and getTiltAngle(left) = -getTiltAngle(right).
     * @private
     */
  }, {
    key: "_getTiltAngle",
    value: function _getTiltAngle(direction) {
      switch (direction) {
        case MicroBitTiltDirection.FRONT:
          return Math.round(this._peripheral.tiltY / -10);
        case MicroBitTiltDirection.BACK:
          return Math.round(this._peripheral.tiltY / 10);
        case MicroBitTiltDirection.LEFT:
          return Math.round(this._peripheral.tiltX / -10);
        case MicroBitTiltDirection.RIGHT:
          return Math.round(this._peripheral.tiltX / 10);
        default:
          log.warn("Unknown tilt direction in _getTiltAngle: ".concat(direction));
      }
    }

    /**
     * @param {object} args - the block's arguments.
     * @return {boolean} - the touch pin state.
     * @private
     */
  }, {
    key: "whenPinConnected",
    value: function whenPinConnected(args) {
      var pin = parseInt(args.PIN, 10);
      if (isNaN(pin)) return;
      if (pin < 0 || pin > 2) return false;
      return this._peripheral._checkPinState(pin);
    }
  }], [{
    key: "EXTENSION_NAME",
    get:
    /**
     * @return {string} - the name of this extension.
     */
    function get() {
      return 'micro:bit';
    }

    /**
     * @return {string} - the ID of this extension.
     */
  }, {
    key: "EXTENSION_ID",
    get: function get() {
      return 'microbit';
    }

    /**
     * @return {number} - the tilt sensor counts as "tilted" if its tilt angle meets or exceeds this threshold.
     */
  }, {
    key: "TILT_THRESHOLD",
    get: function get() {
      return 15;
    }
  }]);
}();
module.exports = Scratch3MicroBitBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/1-snare.mp3":
/*!********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/1-snare.mp3 ***!
  \********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/344dc88f4d5187f0d86f2f13c9ef1727.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/10-wood-block.mp3":
/*!**************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/10-wood-block.mp3 ***!
  \**************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/e75c65e1679e0cc2800caa5c47823daa.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/11-cowbell.mp3":
/*!***********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/11-cowbell.mp3 ***!
  \***********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/fefb7e2fcd4d311a5a7c3de5a8ae5b64.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/12-triangle.mp3":
/*!************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/12-triangle.mp3 ***!
  \************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/a42b5a072afdc2d1aa55accfb1168fb3.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/13-bongo.mp3":
/*!*********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/13-bongo.mp3 ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/26a4aecd5158e8663b90d5edfba5e25a.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/14-conga.mp3":
/*!*********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/14-conga.mp3 ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/352dc6cd98d372680a5297f6ffc50ec4.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/15-cabasa.mp3":
/*!**********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/15-cabasa.mp3 ***!
  \**********************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = "data:audio/mpeg;base64,"

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/16-guiro.mp3":
/*!*********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/16-guiro.mp3 ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/fcb21f71cf289f827e01b18b7c3fdc85.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/17-vibraslap.mp3":
/*!*************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/17-vibraslap.mp3 ***!
  \*************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/367d7189e8dac8a0ca2828830a91db83.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/18-cuica.mp3":
/*!*********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/18-cuica.mp3 ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/dd8f636a1df01f3d3768998081387ea4.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/2-bass-drum.mp3":
/*!************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/2-bass-drum.mp3 ***!
  \************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = "data:audio/mpeg;base64,"

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/3-side-stick.mp3":
/*!*************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/3-side-stick.mp3 ***!
  \*************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/311ef566eba52b1729fd02d8944b4378.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/4-crash-cymbal.mp3":
/*!***************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/4-crash-cymbal.mp3 ***!
  \***************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/1bff35b71e6f52017b123be4f9904d54.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/5-open-hi-hat.mp3":
/*!**************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/5-open-hi-hat.mp3 ***!
  \**************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/b74faacb619dad0b5f3871a04a6bcd0e.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/6-closed-hi-hat.mp3":
/*!****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/6-closed-hi-hat.mp3 ***!
  \****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/c40ccf288c211a9441bb714db775d334.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/7-tambourine.mp3":
/*!*************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/7-tambourine.mp3 ***!
  \*************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/2291f2138819385c9a5750130f254f90.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/8-hand-clap.mp3":
/*!************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/8-hand-clap.mp3 ***!
  \************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/484c379b1da007ecb5f66780a119fa2e.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/drums/9-claves.mp3":
/*!*********************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/drums/9-claves.mp3 ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/1c9099a7cf1c142492de1603b20b0d44.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/108.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/108.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/7231a404390e5fe96856a5ec5182745c.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/24.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/24.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/06fd0604a4bf62a2b706edc23e49a086.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/36.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/36.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/52ac58d8ef4e53d6449e592b68359d46.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/48.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/48.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/af0caec5f8ac3a35c9769e1e07918e14.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/60.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/60.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/579529cc3c40887f6ecc771c4b39809d.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/72.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/72.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/048d12d9e01a42ab48fb6ce57100f6f8.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/84.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/84.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/41ebcde49032d543af1a2c843a725834.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/96.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/1-piano/96.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/c5b3d36982acf8f9acc53c3964100460.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/10-clarinet/48.mp3":
/*!*********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/10-clarinet/48.mp3 ***!
  \*********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/3299cad6fda8c0a3d957a5ea75060677.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/10-clarinet/60.mp3":
/*!*********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/10-clarinet/60.mp3 ***!
  \*********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/bf99fbafe9a01e45da63a6874cd8f4b2.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/36.mp3":
/*!**********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/36.mp3 ***!
  \**********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/2bae7059d4f81f344b02cc34567e548d.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/60.mp3":
/*!**********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/60.mp3 ***!
  \**********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/d4708e8517c8e85b5c1948c845e662ba.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/84.mp3":
/*!**********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/84.mp3 ***!
  \**********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/4734f3fe917e80ae53b9f1f4e3d62147.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/12-flute/60.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/12-flute/60.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/96d18383c71ad5ceff1152859b46c508.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/12-flute/72.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/12-flute/72.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/b5f8889e91e6d51fc294dfd1125c1240.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/13-wooden-flute/60.mp3":
/*!*************************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/13-wooden-flute/60.mp3 ***!
  \*************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/8589d7e6689f31ec2c251be25ac95c1b.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/13-wooden-flute/72.mp3":
/*!*************************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/13-wooden-flute/72.mp3 ***!
  \*************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/2ec048ddd7a7e30b94f8d24c22892112.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/36.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/36.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/527756fb4ee6a19086a69e0ea8355b5c.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/48.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/48.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/f151dc9a8eb14b4d9c64795e1cbae7e0.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/60.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/60.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/3d55b957c09e11fe0d75f4f2a84bdf53.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/15-choir/48.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/15-choir/48.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/e3af35844a01f8f64d2bea326db21a35.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/15-choir/60.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/15-choir/60.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/52d442dc8e71307ec251603d1edd35f0.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/15-choir/72.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/15-choir/72.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/5610fba29744cce750f1da6c6091ecac.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/16-vibraphone/60.mp3":
/*!***********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/16-vibraphone/60.mp3 ***!
  \***********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/19161c89a6ca04006606ca3f4f91fdec.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/16-vibraphone/72.mp3":
/*!***********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/16-vibraphone/72.mp3 ***!
  \***********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/2352f743e6b883d53e0b09b84ddea79b.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/17-music-box/60.mp3":
/*!**********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/17-music-box/60.mp3 ***!
  \**********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/9d76e4ccf26072c4bddaa81a765ab08a.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/18-steel-drum/60.mp3":
/*!***********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/18-steel-drum/60.mp3 ***!
  \***********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/25c294b62fe9e614db125a4f65cc6044.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/19-marimba/60.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/19-marimba/60.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/a386a58fa9c00de141c32b5b11e5327e.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/2-electric-piano/60.mp3":
/*!**************************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/2-electric-piano/60.mp3 ***!
  \**************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/c20d558f1fc823a96de910456827d90c.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/20-synth-lead/60.mp3":
/*!***********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/20-synth-lead/60.mp3 ***!
  \***********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/912e19a8fe314f46ab3df3fed9578a29.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/21-synth-pad/60.mp3":
/*!**********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/21-synth-pad/60.mp3 ***!
  \**********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/2613dd347943b2ed7c6edb22e0cf7e0a.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/3-organ/60.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/3-organ/60.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/6d912e9240790715a077d422cdbc19a5.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/4-guitar/60.mp3":
/*!******************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/4-guitar/60.mp3 ***!
  \******************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/b2b4ad5307e24c6627f2077478017981.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/5-electric-guitar/60.mp3":
/*!***************************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/5-electric-guitar/60.mp3 ***!
  \***************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/ecea3eba4a9e187eb6cc7cc6a6b4bb4d.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/6-bass/36.mp3":
/*!****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/6-bass/36.mp3 ***!
  \****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/a16ff5830a119789f5bc5d29a42ace64.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/6-bass/48.mp3":
/*!****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/6-bass/48.mp3 ***!
  \****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/7cf82cd9bd601cfee5e715c0eb5343ef.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/7-pizzicato/60.mp3":
/*!*********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/7-pizzicato/60.mp3 ***!
  \*********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/bd1e18eb36346861c5c57be8ed2ba5e3.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/8-cello/36.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/8-cello/36.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/23d5956ad3641d4663a17353c71477b0.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/8-cello/48.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/8-cello/48.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/f44e9579ce45c384ed76054ee5e98270.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/8-cello/60.mp3":
/*!*****************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/8-cello/60.mp3 ***!
  \*****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/32e0021542e34f09a747322d11868f23.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/36.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/36.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/1c8b575f37b1a519fc3cdc5183bb85c0.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/48.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/48.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/a9eccab64d64a3114344d56af142741e.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/60.mp3":
/*!********************************************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/60.mp3 ***!
  \********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/1ac03a53ecc806b652579f9f8da7c58a.mp3";

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/index.js":
/*!****************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/index.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Clone = __webpack_require__(/*! ../../util/clone */ "../vm/src/util/clone.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var Timer = __webpack_require__(/*! ../../util/timer */ "../vm/src/util/timer.js");

/**
 * The instrument and drum sounds, loaded as static assets.
 * @type {object}
 */
var assetData = {};
try {
  assetData = __webpack_require__(/*! ./manifest */ "../vm/src/extensions/scratch3_music/manifest.js");
} catch (e) {
  // Non-webpack environment, don't worry about assets.
}

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/svg+xml;base64,';

/**
 * Icon svg to be displayed in the category menu, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var menuIconURI = 'data:image/svg+xml;base64,';

/**
 * Class for the music-related blocks in Scratch 3.0
 * @param {Runtime} runtime - the runtime instantiating this block package.
 * @constructor
 */
var Scratch3MusicBlocks = /*#__PURE__*/function () {
  function Scratch3MusicBlocks(runtime) {
    _classCallCheck(this, Scratch3MusicBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * The number of drum and instrument sounds currently being played simultaneously.
     * @type {number}
     * @private
     */
    this._concurrencyCounter = 0;

    /**
     * An array of sound players, one for each drum sound.
     * @type {Array}
     * @private
     */
    this._drumPlayers = [];

    /**
     * An array of arrays of sound players. Each instrument has one or more audio players.
     * @type {Array[]}
     * @private
     */
    this._instrumentPlayerArrays = [];

    /**
     * An array of arrays of sound players. Each instrument mya have an audio player for each playable note.
     * @type {Array[]}
     * @private
     */
    this._instrumentPlayerNoteArrays = [];

    /**
     * An array of audio bufferSourceNodes. Each time you play an instrument or drum sound,
     * a bufferSourceNode is created. We keep references to them to make sure their onended
     * events can fire.
     * @type {Array}
     * @private
     */
    this._bufferSources = [];
    this._loadAllSounds();
    this._onTargetCreated = this._onTargetCreated.bind(this);
    this.runtime.on('targetWasCreated', this._onTargetCreated);
    this._playNoteForPicker = this._playNoteForPicker.bind(this);
    this.runtime.on('PLAY_NOTE', this._playNoteForPicker);
  }

  /**
   * Decode the full set of drum and instrument sounds, and store the audio buffers in arrays.
   */
  return _createClass(Scratch3MusicBlocks, [{
    key: "_loadAllSounds",
    value: function _loadAllSounds() {
      var _this = this;
      var loadingPromises = [];
      this.DRUM_INFO.forEach(function (drumInfo, index) {
        var filePath = "drums/".concat(drumInfo.fileName);
        var promise = _this._storeSound(filePath, index, _this._drumPlayers);
        loadingPromises.push(promise);
      });
      this.INSTRUMENT_INFO.forEach(function (instrumentInfo, instrumentIndex) {
        _this._instrumentPlayerArrays[instrumentIndex] = [];
        _this._instrumentPlayerNoteArrays[instrumentIndex] = [];
        instrumentInfo.samples.forEach(function (sample, noteIndex) {
          var filePath = "instruments/".concat(instrumentInfo.dirName, "/").concat(sample);
          var promise = _this._storeSound(filePath, noteIndex, _this._instrumentPlayerArrays[instrumentIndex]);
          loadingPromises.push(promise);
        });
      });
      Promise.all(loadingPromises).then(function () {
        // @TODO: Update the extension status indicator.
      });
    }

    /**
     * Decode a sound and store the player in an array.
     * @param {string} filePath - the audio file name.
     * @param {number} index - the index at which to store the audio player.
     * @param {array} playerArray - the array of players in which to store it.
     * @return {Promise} - a promise which will resolve once the sound has been stored.
     */
  }, {
    key: "_storeSound",
    value: function _storeSound(filePath, index, playerArray) {
      var _this2 = this;
      var fullPath = "".concat(filePath, ".mp3");
      if (!assetData[fullPath]) return;
      var soundFile = assetData[fullPath];
      return fetch(soundFile).then(function (r) {
        return r.arrayBuffer();
      }).then(function (soundBuffer) {
        return _this2._decodeSound(soundBuffer);
      }).then(function (player) {
        playerArray[index] = player;
      });
    }

    /**
     * Decode a sound and return a promise with the audio buffer.
     * @param  {ArrayBuffer} soundBuffer - a buffer containing the encoded audio.
     * @return {Promise} - a promise which will resolve once the sound has decoded.
     */
  }, {
    key: "_decodeSound",
    value: function _decodeSound(soundBuffer) {
      var engine = this.runtime.audioEngine;
      if (!engine) {
        return Promise.reject(new Error('No Audio Context Detected'));
      }

      // Check for newer promise-based API
      return engine.decodeSoundPlayer({
        data: {
          buffer: soundBuffer
        }
      });
    }

    /**
     * Create data for a menu in scratch-blocks format, consisting of an array of objects with text and
     * value properties. The text is a translated string, and the value is one-indexed.
     * @param  {object[]} info - An array of info objects each having a name property.
     * @return {array} - An array of objects with text and value properties.
     * @private
     */
  }, {
    key: "_buildMenu",
    value: function _buildMenu(info) {
      return info.map(function (entry, index) {
        var obj = {};
        obj.text = entry.name;
        obj.value = String(index + 1);
        return obj;
      });
    }

    /**
     * An array of info about each drum.
     * @type {object[]}
     * @param {string} name - the translatable name to display in the drums menu.
     * @param {string} fileName - the name of the audio file containing the drum sound.
     */
  }, {
    key: "DRUM_INFO",
    get: function get() {
      return [{
        name: formatMessage({
          id: 'music.drumSnare',
          "default": '(1) Snare Drum',
          description: 'Sound of snare drum as used in a standard drum kit'
        }),
        fileName: '1-snare'
      }, {
        name: formatMessage({
          id: 'music.drumBass',
          "default": '(2) Bass Drum',
          description: 'Sound of bass drum as used in a standard drum kit'
        }),
        fileName: '2-bass-drum'
      }, {
        name: formatMessage({
          id: 'music.drumSideStick',
          "default": '(3) Side Stick',
          description: 'Sound of a drum stick hitting the side of a drum (usually the snare)'
        }),
        fileName: '3-side-stick'
      }, {
        name: formatMessage({
          id: 'music.drumCrashCymbal',
          "default": '(4) Crash Cymbal',
          description: 'Sound of a drum stick hitting a crash cymbal'
        }),
        fileName: '4-crash-cymbal'
      }, {
        name: formatMessage({
          id: 'music.drumOpenHiHat',
          "default": '(5) Open Hi-Hat',
          description: 'Sound of a drum stick hitting a hi-hat while open'
        }),
        fileName: '5-open-hi-hat'
      }, {
        name: formatMessage({
          id: 'music.drumClosedHiHat',
          "default": '(6) Closed Hi-Hat',
          description: 'Sound of a drum stick hitting a hi-hat while closed'
        }),
        fileName: '6-closed-hi-hat'
      }, {
        name: formatMessage({
          id: 'music.drumTambourine',
          "default": '(7) Tambourine',
          description: 'Sound of a tambourine being struck'
        }),
        fileName: '7-tambourine'
      }, {
        name: formatMessage({
          id: 'music.drumHandClap',
          "default": '(8) Hand Clap',
          description: 'Sound of two hands clapping together'
        }),
        fileName: '8-hand-clap'
      }, {
        name: formatMessage({
          id: 'music.drumClaves',
          "default": '(9) Claves',
          description: 'Sound of claves being struck together'
        }),
        fileName: '9-claves'
      }, {
        name: formatMessage({
          id: 'music.drumWoodBlock',
          "default": '(10) Wood Block',
          description: 'Sound of a wood block being struck'
        }),
        fileName: '10-wood-block'
      }, {
        name: formatMessage({
          id: 'music.drumCowbell',
          "default": '(11) Cowbell',
          description: 'Sound of a cowbell being struck'
        }),
        fileName: '11-cowbell'
      }, {
        name: formatMessage({
          id: 'music.drumTriangle',
          "default": '(12) Triangle',
          description: 'Sound of a triangle (instrument) being struck'
        }),
        fileName: '12-triangle'
      }, {
        name: formatMessage({
          id: 'music.drumBongo',
          "default": '(13) Bongo',
          description: 'Sound of a bongo being struck'
        }),
        fileName: '13-bongo'
      }, {
        name: formatMessage({
          id: 'music.drumConga',
          "default": '(14) Conga',
          description: 'Sound of a conga being struck'
        }),
        fileName: '14-conga'
      }, {
        name: formatMessage({
          id: 'music.drumCabasa',
          "default": '(15) Cabasa',
          description: 'Sound of a cabasa being shaken'
        }),
        fileName: '15-cabasa'
      }, {
        name: formatMessage({
          id: 'music.drumGuiro',
          "default": '(16) Guiro',
          description: 'Sound of a guiro being played'
        }),
        fileName: '16-guiro'
      }, {
        name: formatMessage({
          id: 'music.drumVibraslap',
          "default": '(17) Vibraslap',
          description: 'Sound of a Vibraslap being played'
        }),
        fileName: '17-vibraslap'
      }, {
        name: formatMessage({
          id: 'music.drumCuica',
          "default": '(18) Cuica',
          description: 'Sound of a cuica being played'
        }),
        fileName: '18-cuica'
      }];
    }

    /**
     * An array of info about each instrument.
     * @type {object[]}
     * @param {string} name - the translatable name to display in the instruments menu.
     * @param {string} dirName - the name of the directory containing audio samples for this instrument.
     * @param {number} [releaseTime] - an optional duration for the release portion of each note.
     * @param {number[]} samples - an array of numbers representing the MIDI note number for each
     *                           sampled sound used to play this instrument.
     */
  }, {
    key: "INSTRUMENT_INFO",
    get: function get() {
      return [{
        name: formatMessage({
          id: 'music.instrumentPiano',
          "default": '(1) Piano',
          description: 'Sound of a piano'
        }),
        dirName: '1-piano',
        releaseTime: 0.5,
        samples: [24, 36, 48, 60, 72, 84, 96, 108]
      }, {
        name: formatMessage({
          id: 'music.instrumentElectricPiano',
          "default": '(2) Electric Piano',
          description: 'Sound of an electric piano'
        }),
        dirName: '2-electric-piano',
        releaseTime: 0.5,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentOrgan',
          "default": '(3) Organ',
          description: 'Sound of an organ'
        }),
        dirName: '3-organ',
        releaseTime: 0.5,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentGuitar',
          "default": '(4) Guitar',
          description: 'Sound of an accoustic guitar'
        }),
        dirName: '4-guitar',
        releaseTime: 0.5,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentElectricGuitar',
          "default": '(5) Electric Guitar',
          description: 'Sound of an electric guitar'
        }),
        dirName: '5-electric-guitar',
        releaseTime: 0.5,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentBass',
          "default": '(6) Bass',
          description: 'Sound of an accoustic upright bass'
        }),
        dirName: '6-bass',
        releaseTime: 0.25,
        samples: [36, 48]
      }, {
        name: formatMessage({
          id: 'music.instrumentPizzicato',
          "default": '(7) Pizzicato',
          description: 'Sound of a string instrument (e.g. violin) being plucked'
        }),
        dirName: '7-pizzicato',
        releaseTime: 0.25,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentCello',
          "default": '(8) Cello',
          description: 'Sound of a cello being played with a bow'
        }),
        dirName: '8-cello',
        releaseTime: 0.1,
        samples: [36, 48, 60]
      }, {
        name: formatMessage({
          id: 'music.instrumentTrombone',
          "default": '(9) Trombone',
          description: 'Sound of a trombone being played'
        }),
        dirName: '9-trombone',
        samples: [36, 48, 60]
      }, {
        name: formatMessage({
          id: 'music.instrumentClarinet',
          "default": '(10) Clarinet',
          description: 'Sound of a clarinet being played'
        }),
        dirName: '10-clarinet',
        samples: [48, 60]
      }, {
        name: formatMessage({
          id: 'music.instrumentSaxophone',
          "default": '(11) Saxophone',
          description: 'Sound of a saxophone being played'
        }),
        dirName: '11-saxophone',
        samples: [36, 60, 84]
      }, {
        name: formatMessage({
          id: 'music.instrumentFlute',
          "default": '(12) Flute',
          description: 'Sound of a flute being played'
        }),
        dirName: '12-flute',
        samples: [60, 72]
      }, {
        name: formatMessage({
          id: 'music.instrumentWoodenFlute',
          "default": '(13) Wooden Flute',
          description: 'Sound of a wooden flute being played'
        }),
        dirName: '13-wooden-flute',
        samples: [60, 72]
      }, {
        name: formatMessage({
          id: 'music.instrumentBassoon',
          "default": '(14) Bassoon',
          description: 'Sound of a bassoon being played'
        }),
        dirName: '14-bassoon',
        samples: [36, 48, 60]
      }, {
        name: formatMessage({
          id: 'music.instrumentChoir',
          "default": '(15) Choir',
          description: 'Sound of a choir singing'
        }),
        dirName: '15-choir',
        releaseTime: 0.25,
        samples: [48, 60, 72]
      }, {
        name: formatMessage({
          id: 'music.instrumentVibraphone',
          "default": '(16) Vibraphone',
          description: 'Sound of a vibraphone being struck'
        }),
        dirName: '16-vibraphone',
        releaseTime: 0.5,
        samples: [60, 72]
      }, {
        name: formatMessage({
          id: 'music.instrumentMusicBox',
          "default": '(17) Music Box',
          description: 'Sound of a music box playing'
        }),
        dirName: '17-music-box',
        releaseTime: 0.25,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentSteelDrum',
          "default": '(18) Steel Drum',
          description: 'Sound of a steel drum being struck'
        }),
        dirName: '18-steel-drum',
        releaseTime: 0.5,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentMarimba',
          "default": '(19) Marimba',
          description: 'Sound of a marimba being struck'
        }),
        dirName: '19-marimba',
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentSynthLead',
          "default": '(20) Synth Lead',
          description: 'Sound of a "lead" synthesizer being played'
        }),
        dirName: '20-synth-lead',
        releaseTime: 0.1,
        samples: [60]
      }, {
        name: formatMessage({
          id: 'music.instrumentSynthPad',
          "default": '(21) Synth Pad',
          description: 'Sound of a "pad" synthesizer being played'
        }),
        dirName: '21-synth-pad',
        releaseTime: 0.25,
        samples: [60]
      }];
    }

    /**
     * An array that is a mapping from MIDI instrument numbers to Scratch instrument numbers.
     * @type {number[]}
     */
  }, {
    key: "MIDI_INSTRUMENTS",
    get: function get() {
      return [
      // Acoustic Grand, Bright Acoustic, Electric Grand, Honky-Tonk
      1, 1, 1, 1,
      // Electric Piano 1, Electric Piano 2, Harpsichord, Clavinet
      2, 2, 4, 4,
      // Celesta, Glockenspiel, Music Box, Vibraphone
      17, 17, 17, 16,
      // Marimba, Xylophone, Tubular Bells, Dulcimer
      19, 16, 17, 17,
      // Drawbar Organ, Percussive Organ, Rock Organ, Church Organ
      3, 3, 3, 3,
      // Reed Organ, Accordion, Harmonica, Tango Accordion
      3, 3, 3, 3,
      // Nylon String Guitar, Steel String Guitar, Electric Jazz Guitar, Electric Clean Guitar
      4, 4, 5, 5,
      // Electric Muted Guitar, Overdriven Guitar,Distortion Guitar, Guitar Harmonics
      5, 5, 5, 5,
      // Acoustic Bass, Electric Bass (finger), Electric Bass (pick), Fretless Bass
      6, 6, 6, 6,
      // Slap Bass 1, Slap Bass 2, Synth Bass 1, Synth Bass 2
      6, 6, 6, 6,
      // Violin, Viola, Cello, Contrabass
      8, 8, 8, 8,
      // Tremolo Strings, Pizzicato Strings, Orchestral Strings, Timpani
      8, 7, 8, 19,
      // String Ensemble 1, String Ensemble 2, SynthStrings 1, SynthStrings 2
      8, 8, 8, 8,
      // Choir Aahs, Voice Oohs, Synth Voice, Orchestra Hit
      15, 15, 15, 19,
      // Trumpet, Trombone, Tuba, Muted Trumpet
      9, 9, 9, 9,
      // French Horn, Brass Section, SynthBrass 1, SynthBrass 2
      9, 9, 9, 9,
      // Soprano Sax, Alto Sax, Tenor Sax, Baritone Sax
      11, 11, 11, 11,
      // Oboe, English Horn, Bassoon, Clarinet
      14, 14, 14, 10,
      // Piccolo, Flute, Recorder, Pan Flute
      12, 12, 13, 13,
      // Blown Bottle, Shakuhachi, Whistle, Ocarina
      13, 13, 12, 12,
      // Lead 1 (square), Lead 2 (sawtooth), Lead 3 (calliope), Lead 4 (chiff)
      20, 20, 20, 20,
      // Lead 5 (charang), Lead 6 (voice), Lead 7 (fifths), Lead 8 (bass+lead)
      20, 20, 20, 20,
      // Pad 1 (new age), Pad 2 (warm), Pad 3 (polysynth), Pad 4 (choir)
      21, 21, 21, 21,
      // Pad 5 (bowed), Pad 6 (metallic), Pad 7 (halo), Pad 8 (sweep)
      21, 21, 21, 21,
      // FX 1 (rain), FX 2 (soundtrack), FX 3 (crystal), FX 4 (atmosphere)
      21, 21, 21, 21,
      // FX 5 (brightness), FX 6 (goblins), FX 7 (echoes), FX 8 (sci-fi)
      21, 21, 21, 21,
      // Sitar, Banjo, Shamisen, Koto
      4, 4, 4, 4,
      // Kalimba, Bagpipe, Fiddle, Shanai
      17, 14, 8, 10,
      // Tinkle Bell, Agogo, Steel Drums, Woodblock
      17, 17, 18, 19,
      // Taiko Drum, Melodic Tom, Synth Drum, Reverse Cymbal
      1, 1, 1, 1,
      // Guitar Fret Noise, Breath Noise, Seashore, Bird Tweet
      21, 21, 21, 21,
      // Telephone Ring, Helicopter, Applause, Gunshot
      21, 21, 21, 21];
    }

    /**
     * An array that is a mapping from MIDI drum numbers in range (35..81) to Scratch drum numbers.
     * It's in the format [drumNum, pitch, decay].
     * The pitch and decay properties are not currently being used.
     * @type {Array[]}
     */
  }, {
    key: "MIDI_DRUMS",
    get: function get() {
      return [[1, -4],
      // "BassDrum" in 2.0, "Bass Drum" in 3.0 (which was "Tom" in 2.0)
      [1, 0],
      // Same as just above
      [2, 0], [0, 0], [7, 0], [0, 2], [1, -6, 4], [5, 0], [1, -3, 3.2], [5, 0],
      // "HiHatPedal" in 2.0, "Closed Hi-Hat" in 3.0
      [1, 0, 3], [4, -8], [1, 4, 3], [1, 7, 2.7], [3, -8], [1, 10, 2.7], [4, -2], [3, -11], [4, 2], [6, 0], [3, 0, 3.5], [10, 0], [3, -8, 3.5], [16, -6], [4, 2], [12, 2], [12, 0], [13, 0, 0.2], [13, 0, 2], [13, -5, 2], [12, 12], [12, 5], [10, 19], [10, 12], [14, 0], [14, 0],
      // "Maracas" in 2.0, "Cabasa" in 3.0 (TODO: pitch up?)
      [17, 12], [17, 5], [15, 0],
      // "GuiroShort" in 2.0, "Guiro" in 3.0 (which was "GuiroLong" in 2.0) (TODO: decay?)
      [15, 0], [8, 0], [9, 0], [9, -4], [17, -5], [17, 0], [11, -6, 1], [11, -6, 3]];
    }

    /**
     * The key to load & store a target's music-related state.
     * @type {string}
     */
  }, {
    key: "_getMusicState",
    value:
    /**
     * @param {Target} target - collect music state for this target.
     * @returns {MusicState} the mutable music state associated with that target. This will be created if necessary.
     * @private
     */
    function _getMusicState(target) {
      var musicState = target.getCustomState(Scratch3MusicBlocks.STATE_KEY);
      if (!musicState) {
        musicState = Clone.simple(Scratch3MusicBlocks.DEFAULT_MUSIC_STATE);
        target.setCustomState(Scratch3MusicBlocks.STATE_KEY, musicState);
      }
      return musicState;
    }

    /**
     * When a music-playing Target is cloned, clone the music state.
     * @param {Target} newTarget - the newly created target.
     * @param {Target} [sourceTarget] - the target used as a source for the new clone, if any.
     * @listens Runtime#event:targetWasCreated
     * @private
     */
  }, {
    key: "_onTargetCreated",
    value: function _onTargetCreated(newTarget, sourceTarget) {
      if (sourceTarget) {
        var musicState = sourceTarget.getCustomState(Scratch3MusicBlocks.STATE_KEY);
        if (musicState) {
          newTarget.setCustomState(Scratch3MusicBlocks.STATE_KEY, Clone.simple(musicState));
        }
      }
    }

    /**
     * @returns {object} metadata for this extension and its blocks.
     */
  }, {
    key: "getInfo",
    value: function getInfo() {
      return {
        id: 'music',
        name: formatMessage({
          id: 'music.categoryName',
          "default": 'Music',
          description: 'Label for the Music extension category'
        }),
        menuIconURI: menuIconURI,
        blockIconURI: blockIconURI,
        blocks: [{
          opcode: 'playDrumForBeats',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.playDrumForBeats',
            "default": 'play drum [DRUM] for [BEATS] beats',
            description: 'play drum sample for a number of beats'
          }),
          arguments: {
            DRUM: {
              type: ArgumentType.NUMBER,
              menu: 'DRUM',
              defaultValue: 1
            },
            BEATS: {
              type: ArgumentType.NUMBER,
              defaultValue: 0.25
            }
          }
        }, {
          opcode: 'midiPlayDrumForBeats',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.midiPlayDrumForBeats',
            "default": 'play drum [DRUM] for [BEATS] beats',
            description: 'play drum sample for a number of beats according to a mapping of MIDI codes'
          }),
          arguments: {
            DRUM: {
              type: ArgumentType.NUMBER,
              menu: 'DRUM',
              defaultValue: 1
            },
            BEATS: {
              type: ArgumentType.NUMBER,
              defaultValue: 0.25
            }
          },
          hideFromPalette: true
        }, {
          opcode: 'restForBeats',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.restForBeats',
            "default": 'rest for [BEATS] beats',
            description: 'rest (play no sound) for a number of beats'
          }),
          arguments: {
            BEATS: {
              type: ArgumentType.NUMBER,
              defaultValue: 0.25
            }
          }
        }, {
          opcode: 'playNoteForBeats',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.playNoteForBeats',
            "default": 'play note [NOTE] for [BEATS] beats',
            description: 'play a note for a number of beats'
          }),
          arguments: {
            NOTE: {
              type: ArgumentType.NOTE,
              defaultValue: 60
            },
            BEATS: {
              type: ArgumentType.NUMBER,
              defaultValue: 0.25
            }
          }
        }, {
          opcode: 'setInstrument',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.setInstrument',
            "default": 'set instrument to [INSTRUMENT]',
            description: 'set the instrument (e.g. piano, guitar, trombone) for notes played'
          }),
          arguments: {
            INSTRUMENT: {
              type: ArgumentType.NUMBER,
              menu: 'INSTRUMENT',
              defaultValue: 1
            }
          }
        }, {
          opcode: 'midiSetInstrument',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.midiSetInstrument',
            "default": 'set instrument to [INSTRUMENT]',
            description: 'set the instrument for notes played according to a mapping of MIDI codes'
          }),
          arguments: {
            INSTRUMENT: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          hideFromPalette: true
        }, {
          opcode: 'setTempo',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.setTempo',
            "default": 'set tempo to [TEMPO]',
            description: 'set tempo (speed) for notes, drums, and rests played'
          }),
          arguments: {
            TEMPO: {
              type: ArgumentType.NUMBER,
              defaultValue: 60
            }
          }
        }, {
          opcode: 'changeTempo',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'music.changeTempo',
            "default": 'change tempo by [TEMPO]',
            description: 'change tempo (speed) for notes, drums, and rests played'
          }),
          arguments: {
            TEMPO: {
              type: ArgumentType.NUMBER,
              defaultValue: 20
            }
          }
        }, {
          opcode: 'getTempo',
          text: formatMessage({
            id: 'music.getTempo',
            "default": 'tempo',
            description: 'get the current tempo (speed) for notes, drums, and rests played'
          }),
          blockType: BlockType.REPORTER
        }],
        menus: {
          DRUM: {
            acceptReporters: true,
            items: this._buildMenu(this.DRUM_INFO)
          },
          INSTRUMENT: {
            acceptReporters: true,
            items: this._buildMenu(this.INSTRUMENT_INFO)
          }
        }
      };
    }
  }, {
    key: "_isConcurrencyLimited",
    value: function _isConcurrencyLimited() {
      return this.runtime.runtimeOptions.miscLimits && this._concurrencyCounter > Scratch3MusicBlocks.CONCURRENCY_LIMIT;
    }

    /**
     * Play a drum sound for some number of beats.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     * @property {int} DRUM - the number of the drum to play.
     * @property {number} BEATS - the duration in beats of the drum sound.
     */
  }, {
    key: "playDrumForBeats",
    value: function playDrumForBeats(args, util) {
      this._playDrumForBeats(args.DRUM, args.BEATS, util);
    }

    /**
     * Play a drum sound for some number of beats according to the range of "MIDI" drum codes supported.
     * This block is implemented for compatibility with old Scratch projects that use the
     * 'drum:duration:elapsed:from:' block.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "midiPlayDrumForBeats",
    value: function midiPlayDrumForBeats(args, util) {
      var drumNum = Cast.toNumber(args.DRUM);
      drumNum = Math.round(drumNum);
      var midiDescription = this.MIDI_DRUMS[drumNum - 35];
      if (midiDescription) {
        drumNum = midiDescription[0];
      } else {
        drumNum = 2; // Default instrument used in Scratch 2.0
      }
      drumNum += 1; // drumNum input to _playDrumForBeats is one-indexed
      this._playDrumForBeats(drumNum, args.BEATS, util);
    }

    /**
     * Internal code to play a drum sound for some number of beats.
     * @param {number} drumNum - the drum number.
     * @param {beats} beats - the duration in beats to pause after playing the sound.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "_playDrumForBeats",
    value: function _playDrumForBeats(drumNum, beats, util) {
      if (this._stackTimerNeedsInit(util)) {
        drumNum = Cast.toNumber(drumNum);
        drumNum = Math.round(drumNum);
        drumNum -= 1; // drums are one-indexed
        drumNum = MathUtil.wrapClamp(drumNum, 0, this.DRUM_INFO.length - 1);
        beats = Cast.toNumber(beats);
        beats = this._clampBeats(beats);
        this._playDrumNum(util, drumNum);
        this._startStackTimer(util, this._beatsToSec(beats));
      } else {
        this._checkStackTimer(util);
      }
    }

    /**
     * Play a drum sound using its 0-indexed number.
     * @param {object} util - utility object provided by the runtime.
     * @param {number} drumNum - the number of the drum to play.
     * @private
     */
  }, {
    key: "_playDrumNum",
    value: function _playDrumNum(util, drumNum) {
      var _this3 = this;
      if (util.runtime.audioEngine === null) return;
      if (util.target.sprite.soundBank === null) return;
      // If we're playing too many sounds, do not play the drum sound.
      if (this._isConcurrencyLimited()) {
        return;
      }
      var player = this._drumPlayers[drumNum];
      if (typeof player === 'undefined') return;
      if (player.isPlaying && !player.isStarting) {
        // Take the internal player state and create a new player with it.
        // `.play` does this internally but then instructs the sound to
        // stop.
        player.take();
      }
      var engine = util.runtime.audioEngine;
      var context = engine.audioContext;
      var volumeGain = context.createGain();
      volumeGain.gain.setValueAtTime(util.target.volume / 100, engine.currentTime);
      volumeGain.connect(engine.getInputNode());
      this._concurrencyCounter++;
      player.once('stop', function () {
        _this3._concurrencyCounter--;
      });
      player.play();
      // Connect the player to the gain node.
      player.connect({
        getInputNode: function getInputNode() {
          return volumeGain;
        }
      });
    }

    /**
     * Rest for some number of beats.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     * @property {number} BEATS - the duration in beats of the rest.
     */
  }, {
    key: "restForBeats",
    value: function restForBeats(args, util) {
      if (this._stackTimerNeedsInit(util)) {
        var beats = Cast.toNumber(args.BEATS);
        beats = this._clampBeats(beats);
        this._startStackTimer(util, this._beatsToSec(beats));
      } else {
        this._checkStackTimer(util);
      }
    }

    /**
     * Play a note using the current musical instrument for some number of beats.
     * This function processes the arguments, and handles the timing of the block's execution.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     * @property {number} NOTE - the pitch of the note to play, interpreted as a MIDI note number.
     * @property {number} BEATS - the duration in beats of the note.
     */
  }, {
    key: "playNoteForBeats",
    value: function playNoteForBeats(args, util) {
      if (this._stackTimerNeedsInit(util)) {
        var note = Cast.toNumber(args.NOTE);
        note = MathUtil.clamp(note, Scratch3MusicBlocks.MIDI_NOTE_RANGE.min, Scratch3MusicBlocks.MIDI_NOTE_RANGE.max);
        var beats = Cast.toNumber(args.BEATS);
        beats = this._clampBeats(beats);
        // If the duration is 0, do not play the note. In Scratch 2.0, "play drum for 0 beats" plays the drum,
        // but "play note for 0 beats" is silent.
        if (beats === 0) return;
        var durationSec = this._beatsToSec(beats);
        this._playNote(util, note, durationSec);
        this._startStackTimer(util, durationSec);
      } else {
        this._checkStackTimer(util);
      }
    }
  }, {
    key: "_playNoteForPicker",
    value: function _playNoteForPicker(noteNum, category) {
      if (category !== this.getInfo().name) return;
      var util = {
        runtime: this.runtime,
        target: this.runtime.getEditingTarget()
      };
      this._playNote(util, noteNum, 0.25);
    }

    /**
     * Play a note using the current instrument for a duration in seconds.
     * This function actually plays the sound, and handles the timing of the sound, including the
     * "release" portion of the sound, which continues briefly after the block execution has finished.
     * @param {object} util - utility object provided by the runtime.
     * @param {number} note - the pitch of the note to play, interpreted as a MIDI note number.
     * @param {number} durationSec - the duration in seconds to play the note.
     * @private
     */
  }, {
    key: "_playNote",
    value: function _playNote(util, note, durationSec) {
      var _this4 = this;
      if (util.runtime.audioEngine === null) return;
      if (util.target.sprite.soundBank === null) return;

      // If we're playing too many sounds, do not play the note.
      if (this._isConcurrencyLimited()) {
        return;
      }

      // Determine which of the audio samples for this instrument to play
      var musicState = this._getMusicState(util.target);
      var inst = musicState.currentInstrument;
      var instrumentInfo = this.INSTRUMENT_INFO[inst];
      var sampleArray = instrumentInfo.samples;
      var sampleIndex = this._selectSampleIndexForNote(note, sampleArray);

      // If the audio sample has not loaded yet, bail out
      if (typeof this._instrumentPlayerArrays[inst] === 'undefined') return;
      if (typeof this._instrumentPlayerArrays[inst][sampleIndex] === 'undefined') return;

      // Fetch the sound player to play the note.
      var engine = util.runtime.audioEngine;
      if (!this._instrumentPlayerNoteArrays[inst][note]) {
        this._instrumentPlayerNoteArrays[inst][note] = this._instrumentPlayerArrays[inst][sampleIndex].take();
      }
      var player = this._instrumentPlayerNoteArrays[inst][note];
      if (player.isPlaying && !player.isStarting) {
        // Take the internal player state and create a new player with it.
        // `.play` does this internally but then instructs the sound to
        // stop.
        player.take();
      }

      // Set its pitch.
      var sampleNote = sampleArray[sampleIndex];
      var notePitchInterval = this._ratioForPitchInterval(note - sampleNote);

      // Create gain nodes for this note's volume and release, and chain them
      // to the output.
      var context = engine.audioContext;
      var volumeGain = context.createGain();
      volumeGain.gain.setValueAtTime(util.target.volume / 100, engine.currentTime);
      var releaseGain = context.createGain();
      volumeGain.connect(releaseGain);
      releaseGain.connect(engine.getInputNode());

      // Schedule the release of the note, ramping its gain down to zero,
      // and then stopping the sound.
      var releaseDuration = this.INSTRUMENT_INFO[inst].releaseTime;
      if (typeof releaseDuration === 'undefined') {
        releaseDuration = 0.01;
      }
      var releaseStart = context.currentTime + durationSec;
      var releaseEnd = releaseStart + releaseDuration;
      releaseGain.gain.setValueAtTime(1, releaseStart);
      releaseGain.gain.linearRampToValueAtTime(0.0001, releaseEnd);
      this._concurrencyCounter++;
      player.once('stop', function () {
        _this4._concurrencyCounter--;
      });

      // Start playing the note
      player.play();
      // Connect the player to the gain node.
      player.connect({
        getInputNode: function getInputNode() {
          return volumeGain;
        }
      });
      // Set playback now after play creates the outputNode.
      player.outputNode.playbackRate.value = notePitchInterval;
      // Schedule playback to stop.
      player.outputNode.stop(releaseEnd);
    }

    /**
     * The samples array for each instrument is the set of pitches of the available audio samples.
     * This function selects the best one to use to play a given input note, and returns its index
     * in the samples array.
     * @param  {number} note - the input note to select a sample for.
     * @param  {number[]} samples - an array of the pitches of the available samples.
     * @return {index} the index of the selected sample in the samples array.
     * @private
     */
  }, {
    key: "_selectSampleIndexForNote",
    value: function _selectSampleIndexForNote(note, samples) {
      // Step backwards through the array of samples, i.e. in descending pitch, in order to find
      // the sample that is the closest one below (or matching) the pitch of the input note.
      for (var i = samples.length - 1; i >= 0; i--) {
        if (note >= samples[i]) {
          return i;
        }
      }
      return 0;
    }

    /**
     * Calcuate the frequency ratio for a given musical interval.
     * @param  {number} interval - the pitch interval to convert.
     * @return {number} a ratio corresponding to the input interval.
     * @private
     */
  }, {
    key: "_ratioForPitchInterval",
    value: function _ratioForPitchInterval(interval) {
      return Math.pow(2, interval / 12);
    }

    /**
     * Clamp a duration in beats to the allowed min and max duration.
     * @param  {number} beats - a duration in beats.
     * @return {number} - the clamped duration.
     * @private
     */
  }, {
    key: "_clampBeats",
    value: function _clampBeats(beats) {
      return MathUtil.clamp(beats, Scratch3MusicBlocks.BEAT_RANGE.min, Scratch3MusicBlocks.BEAT_RANGE.max);
    }

    /**
     * Convert a number of beats to a number of seconds, using the current tempo.
     * @param  {number} beats - number of beats to convert to secs.
     * @return {number} seconds - number of seconds `beats` will last.
     * @private
     */
  }, {
    key: "_beatsToSec",
    value: function _beatsToSec(beats) {
      return 60 / this.getTempo() * beats;
    }

    /**
     * Check if the stack timer needs initialization.
     * @param {object} util - utility object provided by the runtime.
     * @return {boolean} - true if the stack timer needs to be initialized.
     * @private
     */
  }, {
    key: "_stackTimerNeedsInit",
    value: function _stackTimerNeedsInit(util) {
      return !util.stackFrame.timer;
    }

    /**
     * Start the stack timer and the yield the thread if necessary.
     * @param {object} util - utility object provided by the runtime.
     * @param {number} duration - a duration in seconds to set the timer for.
     * @private
     */
  }, {
    key: "_startStackTimer",
    value: function _startStackTimer(util, duration) {
      util.stackFrame.timer = new Timer();
      util.stackFrame.timer.start();
      util.stackFrame.duration = duration;
      util["yield"]();
    }

    /**
     * Check the stack timer, and if its time is not up yet, yield the thread.
     * @param {object} util - utility object provided by the runtime.
     * @private
     */
  }, {
    key: "_checkStackTimer",
    value: function _checkStackTimer(util) {
      var timeElapsed = util.stackFrame.timer.timeElapsed();
      if (timeElapsed < util.stackFrame.duration * 1000) {
        util["yield"]();
      }
    }

    /**
     * Select an instrument for playing notes.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     * @property {int} INSTRUMENT - the number of the instrument to select.
     */
  }, {
    key: "setInstrument",
    value: function setInstrument(args, util) {
      this._setInstrument(args.INSTRUMENT, util, false);
    }

    /**
     * Select an instrument for playing notes according to a mapping of MIDI codes to Scratch instrument numbers.
     * This block is implemented for compatibility with old Scratch projects that use the 'midiInstrument:' block.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     * @property {int} INSTRUMENT - the MIDI number of the instrument to select.
     */
  }, {
    key: "midiSetInstrument",
    value: function midiSetInstrument(args, util) {
      this._setInstrument(args.INSTRUMENT, util, true);
    }

    /**
     * Internal code to select an instrument for playing notes. If mapMidi is true, set the instrument according to
     * the MIDI to Scratch instrument mapping.
     * @param {number} instNum - the instrument number.
     * @param {object} util - utility object provided by the runtime.
     * @param {boolean} mapMidi - whether or not instNum is a MIDI instrument number.
     */
  }, {
    key: "_setInstrument",
    value: function _setInstrument(instNum, util, mapMidi) {
      var musicState = this._getMusicState(util.target);
      instNum = Cast.toNumber(instNum);
      instNum = Math.round(instNum);
      instNum -= 1; // instruments are one-indexed
      if (mapMidi) {
        instNum = (this.MIDI_INSTRUMENTS[instNum] || 0) - 1;
      }
      instNum = MathUtil.wrapClamp(instNum, 0, this.INSTRUMENT_INFO.length - 1);
      musicState.currentInstrument = instNum;
    }

    /**
     * Set the current tempo to a new value.
     * @param {object} args - the block arguments.
     * @property {number} TEMPO - the tempo, in beats per minute.
     */
  }, {
    key: "setTempo",
    value: function setTempo(args) {
      var tempo = Cast.toNumber(args.TEMPO);
      this._updateTempo(tempo);
    }

    /**
     * Change the current tempo by some amount.
     * @param {object} args - the block arguments.
     * @property {number} TEMPO - the amount to change the tempo, in beats per minute.
     */
  }, {
    key: "changeTempo",
    value: function changeTempo(args) {
      var change = Cast.toNumber(args.TEMPO);
      var tempo = change + this.getTempo();
      this._updateTempo(tempo);
    }

    /**
     * Update the current tempo, clamping it to the min and max allowable range.
     * @param {number} tempo - the tempo to set, in beats per minute.
     * @private
     */
  }, {
    key: "_updateTempo",
    value: function _updateTempo(tempo) {
      tempo = MathUtil.clamp(tempo, Scratch3MusicBlocks.TEMPO_RANGE.min, Scratch3MusicBlocks.TEMPO_RANGE.max);
      var stage = this.runtime.getTargetForStage();
      if (stage) {
        stage.tempo = tempo;
      }
    }

    /**
     * Get the current tempo.
     * @return {number} - the current tempo, in beats per minute.
     */
  }, {
    key: "getTempo",
    value: function getTempo() {
      var stage = this.runtime.getTargetForStage();
      if (stage) {
        return stage.tempo;
      }
      return 60;
    }
  }], [{
    key: "STATE_KEY",
    get: function get() {
      return 'Scratch.music';
    }

    /**
     * The default music-related state, to be used when a target has no existing music state.
     * @type {MusicState}
     */
  }, {
    key: "DEFAULT_MUSIC_STATE",
    get: function get() {
      return {
        currentInstrument: 0
      };
    }

    /**
     * The minimum and maximum MIDI note numbers, for clamping the input to play note.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "MIDI_NOTE_RANGE",
    get: function get() {
      return {
        min: 0,
        max: 130
      };
    }

    /**
     * The minimum and maximum beat values, for clamping the duration of play note, play drum and rest.
     * 100 beats at the default tempo of 60bpm is 100 seconds.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "BEAT_RANGE",
    get: function get() {
      return {
        min: 0,
        max: 100
      };
    }

    /** The minimum and maximum tempo values, in bpm.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "TEMPO_RANGE",
    get: function get() {
      return {
        min: 20,
        max: 500
      };
    }

    /**
     * The maximum number of sounds to allow to play simultaneously.
     * @type {number}
     */
  }, {
    key: "CONCURRENCY_LIMIT",
    get: function get() {
      return 30;
    }
  }]);
}();
module.exports = Scratch3MusicBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_music/manifest.js":
/*!*******************************************************!*\
  !*** ../vm/src/extensions/scratch3_music/manifest.js ***!
  \*******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = {
  'drums/1-snare.mp3': __webpack_require__(/*! ./assets/drums/1-snare.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/1-snare.mp3"),
  'drums/2-bass-drum.mp3': __webpack_require__(/*! ./assets/drums/2-bass-drum.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/2-bass-drum.mp3"),
  'drums/3-side-stick.mp3': __webpack_require__(/*! ./assets/drums/3-side-stick.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/3-side-stick.mp3"),
  'drums/4-crash-cymbal.mp3': __webpack_require__(/*! ./assets/drums/4-crash-cymbal.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/4-crash-cymbal.mp3"),
  'drums/5-open-hi-hat.mp3': __webpack_require__(/*! ./assets/drums/5-open-hi-hat.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/5-open-hi-hat.mp3"),
  'drums/6-closed-hi-hat.mp3': __webpack_require__(/*! ./assets/drums/6-closed-hi-hat.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/6-closed-hi-hat.mp3"),
  'drums/7-tambourine.mp3': __webpack_require__(/*! ./assets/drums/7-tambourine.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/7-tambourine.mp3"),
  'drums/8-hand-clap.mp3': __webpack_require__(/*! ./assets/drums/8-hand-clap.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/8-hand-clap.mp3"),
  'drums/9-claves.mp3': __webpack_require__(/*! ./assets/drums/9-claves.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/9-claves.mp3"),
  'drums/10-wood-block.mp3': __webpack_require__(/*! ./assets/drums/10-wood-block.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/10-wood-block.mp3"),
  'drums/11-cowbell.mp3': __webpack_require__(/*! ./assets/drums/11-cowbell.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/11-cowbell.mp3"),
  'drums/12-triangle.mp3': __webpack_require__(/*! ./assets/drums/12-triangle.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/12-triangle.mp3"),
  'drums/13-bongo.mp3': __webpack_require__(/*! ./assets/drums/13-bongo.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/13-bongo.mp3"),
  'drums/14-conga.mp3': __webpack_require__(/*! ./assets/drums/14-conga.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/14-conga.mp3"),
  'drums/15-cabasa.mp3': __webpack_require__(/*! ./assets/drums/15-cabasa.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/15-cabasa.mp3"),
  'drums/16-guiro.mp3': __webpack_require__(/*! ./assets/drums/16-guiro.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/16-guiro.mp3"),
  'drums/17-vibraslap.mp3': __webpack_require__(/*! ./assets/drums/17-vibraslap.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/17-vibraslap.mp3"),
  'drums/18-cuica.mp3': __webpack_require__(/*! ./assets/drums/18-cuica.mp3 */ "../vm/src/extensions/scratch3_music/assets/drums/18-cuica.mp3"),
  'instruments/1-piano/24.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/24.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/24.mp3"),
  'instruments/1-piano/36.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/36.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/36.mp3"),
  'instruments/1-piano/48.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/48.mp3"),
  'instruments/1-piano/60.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/60.mp3"),
  'instruments/1-piano/72.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/72.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/72.mp3"),
  'instruments/1-piano/84.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/84.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/84.mp3"),
  'instruments/1-piano/96.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/96.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/96.mp3"),
  'instruments/1-piano/108.mp3': __webpack_require__(/*! ./assets/instruments/1-piano/108.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/1-piano/108.mp3"),
  'instruments/2-electric-piano/60.mp3': __webpack_require__(/*! ./assets/instruments/2-electric-piano/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/2-electric-piano/60.mp3"),
  'instruments/3-organ/60.mp3': __webpack_require__(/*! ./assets/instruments/3-organ/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/3-organ/60.mp3"),
  'instruments/4-guitar/60.mp3': __webpack_require__(/*! ./assets/instruments/4-guitar/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/4-guitar/60.mp3"),
  'instruments/5-electric-guitar/60.mp3': __webpack_require__(/*! ./assets/instruments/5-electric-guitar/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/5-electric-guitar/60.mp3"),
  'instruments/6-bass/36.mp3': __webpack_require__(/*! ./assets/instruments/6-bass/36.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/6-bass/36.mp3"),
  'instruments/6-bass/48.mp3': __webpack_require__(/*! ./assets/instruments/6-bass/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/6-bass/48.mp3"),
  'instruments/7-pizzicato/60.mp3': __webpack_require__(/*! ./assets/instruments/7-pizzicato/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/7-pizzicato/60.mp3"),
  'instruments/8-cello/36.mp3': __webpack_require__(/*! ./assets/instruments/8-cello/36.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/8-cello/36.mp3"),
  'instruments/8-cello/48.mp3': __webpack_require__(/*! ./assets/instruments/8-cello/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/8-cello/48.mp3"),
  'instruments/8-cello/60.mp3': __webpack_require__(/*! ./assets/instruments/8-cello/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/8-cello/60.mp3"),
  'instruments/9-trombone/36.mp3': __webpack_require__(/*! ./assets/instruments/9-trombone/36.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/36.mp3"),
  'instruments/9-trombone/48.mp3': __webpack_require__(/*! ./assets/instruments/9-trombone/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/48.mp3"),
  'instruments/9-trombone/60.mp3': __webpack_require__(/*! ./assets/instruments/9-trombone/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/9-trombone/60.mp3"),
  'instruments/10-clarinet/48.mp3': __webpack_require__(/*! ./assets/instruments/10-clarinet/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/10-clarinet/48.mp3"),
  'instruments/10-clarinet/60.mp3': __webpack_require__(/*! ./assets/instruments/10-clarinet/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/10-clarinet/60.mp3"),
  'instruments/11-saxophone/36.mp3': __webpack_require__(/*! ./assets/instruments/11-saxophone/36.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/36.mp3"),
  'instruments/11-saxophone/60.mp3': __webpack_require__(/*! ./assets/instruments/11-saxophone/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/60.mp3"),
  'instruments/11-saxophone/84.mp3': __webpack_require__(/*! ./assets/instruments/11-saxophone/84.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/11-saxophone/84.mp3"),
  'instruments/12-flute/60.mp3': __webpack_require__(/*! ./assets/instruments/12-flute/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/12-flute/60.mp3"),
  'instruments/12-flute/72.mp3': __webpack_require__(/*! ./assets/instruments/12-flute/72.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/12-flute/72.mp3"),
  'instruments/13-wooden-flute/60.mp3': __webpack_require__(/*! ./assets/instruments/13-wooden-flute/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/13-wooden-flute/60.mp3"),
  'instruments/13-wooden-flute/72.mp3': __webpack_require__(/*! ./assets/instruments/13-wooden-flute/72.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/13-wooden-flute/72.mp3"),
  'instruments/14-bassoon/36.mp3': __webpack_require__(/*! ./assets/instruments/14-bassoon/36.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/36.mp3"),
  'instruments/14-bassoon/48.mp3': __webpack_require__(/*! ./assets/instruments/14-bassoon/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/48.mp3"),
  'instruments/14-bassoon/60.mp3': __webpack_require__(/*! ./assets/instruments/14-bassoon/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/14-bassoon/60.mp3"),
  'instruments/15-choir/48.mp3': __webpack_require__(/*! ./assets/instruments/15-choir/48.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/15-choir/48.mp3"),
  'instruments/15-choir/60.mp3': __webpack_require__(/*! ./assets/instruments/15-choir/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/15-choir/60.mp3"),
  'instruments/15-choir/72.mp3': __webpack_require__(/*! ./assets/instruments/15-choir/72.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/15-choir/72.mp3"),
  'instruments/16-vibraphone/60.mp3': __webpack_require__(/*! ./assets/instruments/16-vibraphone/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/16-vibraphone/60.mp3"),
  'instruments/16-vibraphone/72.mp3': __webpack_require__(/*! ./assets/instruments/16-vibraphone/72.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/16-vibraphone/72.mp3"),
  'instruments/17-music-box/60.mp3': __webpack_require__(/*! ./assets/instruments/17-music-box/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/17-music-box/60.mp3"),
  'instruments/18-steel-drum/60.mp3': __webpack_require__(/*! ./assets/instruments/18-steel-drum/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/18-steel-drum/60.mp3"),
  'instruments/19-marimba/60.mp3': __webpack_require__(/*! ./assets/instruments/19-marimba/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/19-marimba/60.mp3"),
  'instruments/20-synth-lead/60.mp3': __webpack_require__(/*! ./assets/instruments/20-synth-lead/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/20-synth-lead/60.mp3"),
  'instruments/21-synth-pad/60.mp3': __webpack_require__(/*! ./assets/instruments/21-synth-pad/60.mp3 */ "../vm/src/extensions/scratch3_music/assets/instruments/21-synth-pad/60.mp3")
};

/***/ }),

/***/ "../vm/src/extensions/scratch3_pen/index.js":
/*!**************************************************!*\
  !*** ../vm/src/extensions/scratch3_pen/index.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var TargetType = __webpack_require__(/*! ../../extension-support/target-type */ "../vm/src/extension-support/target-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var Clone = __webpack_require__(/*! ../../util/clone */ "../vm/src/util/clone.js");
var Color = __webpack_require__(/*! ../../util/color */ "../vm/src/util/color.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");
var StageLayering = __webpack_require__(/*! ../../engine/stage-layering */ "../vm/src/engine/stage-layering.js");

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/svg+xml;base64,';

/**
 * Enum for pen color parameter values.
 * @readonly
 * @enum {string}
 */
var ColorParam = {
  COLOR: 'color',
  SATURATION: 'saturation',
  BRIGHTNESS: 'brightness',
  TRANSPARENCY: 'transparency'
};

/**
 * @typedef {object} PenState - the pen state associated with a particular target.
 * @property {Boolean} penDown - tracks whether the pen should draw for this target.
 * @property {number} color - the current color (hue) of the pen.
 * @property {PenAttributes} penAttributes - cached pen attributes for the renderer. This is the authoritative value for
 *   diameter but not for pen color.
 */

/**
 * Host for the Pen-related blocks in Scratch 3.0
 * @param {Runtime} runtime - the runtime instantiating this block package.
 * @constructor
 */
var Scratch3PenBlocks = /*#__PURE__*/function () {
  function Scratch3PenBlocks(runtime) {
    _classCallCheck(this, Scratch3PenBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * The ID of the renderer Drawable corresponding to the pen layer.
     * @type {int}
     * @private
     */
    this._penDrawableId = -1;

    /**
     * The ID of the renderer Skin corresponding to the pen layer.
     * @type {int}
     * @private
     */
    this._penSkinId = -1;
    this._onTargetCreated = this._onTargetCreated.bind(this);
    this._onTargetMoved = this._onTargetMoved.bind(this);
    runtime.on('targetWasCreated', this._onTargetCreated);
    runtime.on('RUNTIME_DISPOSED', this.clear.bind(this));
  }

  /**
   * The default pen state, to be used when a target has no existing pen state.
   * @type {PenState}
   */
  return _createClass(Scratch3PenBlocks, [{
    key: "_clampPenSize",
    value:
    /**
     * Clamp a pen size value to the range allowed by the pen.
     * @param {number} requestedSize - the requested pen size.
     * @returns {number} the clamped size.
     * @private
     */
    function _clampPenSize(requestedSize) {
      if (this.runtime.renderer && this.runtime.renderer.useHighQualityRender || !this.runtime.runtimeOptions.miscLimits) {
        return Math.max(0, requestedSize);
      }
      return MathUtil.clamp(requestedSize, Scratch3PenBlocks.PEN_SIZE_RANGE.min, Scratch3PenBlocks.PEN_SIZE_RANGE.max);
    }

    /**
     * Retrieve the ID of the renderer "Skin" corresponding to the pen layer. If
     * the pen Skin doesn't yet exist, create it.
     * @returns {int} the Skin ID of the pen layer, or -1 on failure.
     * @private
     */
  }, {
    key: "_getPenLayerID",
    value: function _getPenLayerID() {
      var renderer = this.runtime.renderer;
      if (this._penSkinId < 0 && renderer) {
        this._penSkinId = renderer.createPenSkin();
        this._penDrawableId = renderer.createDrawable(StageLayering.PEN_LAYER);
        if (renderer.markDrawableAsNoninteractive) {
          renderer.markDrawableAsNoninteractive(this._penDrawableId);
        }
        renderer.updateDrawableSkinId(this._penDrawableId, this._penSkinId);
      }
      return this._penSkinId;
    }

    /**
     * @param {Target} target - collect pen state for this target. Probably, but not necessarily, a RenderedTarget.
     * @returns {PenState} the mutable pen state associated with that target. This will be created if necessary.
     * @private
     */
  }, {
    key: "_getPenState",
    value: function _getPenState(target) {
      var penState = target._customState['Scratch.pen'];
      if (!penState) {
        penState = Clone.simple(Scratch3PenBlocks.DEFAULT_PEN_STATE);
        target.setCustomState(Scratch3PenBlocks.STATE_KEY, penState);
      }
      return penState;
    }

    /**
     * When a pen-using Target is cloned, clone the pen state.
     * @param {Target} newTarget - the newly created target.
     * @param {Target} [sourceTarget] - the target used as a source for the new clone, if any.
     * @listens Runtime#event:targetWasCreated
     * @private
     */
  }, {
    key: "_onTargetCreated",
    value: function _onTargetCreated(newTarget, sourceTarget) {
      if (sourceTarget) {
        var penState = sourceTarget.getCustomState(Scratch3PenBlocks.STATE_KEY);
        if (penState) {
          newTarget.setCustomState(Scratch3PenBlocks.STATE_KEY, Clone.simple(penState));
          if (penState.penDown) {
            newTarget.onTargetMoved = this._onTargetMoved;
          }
        }
      }
    }

    /**
     * Handle a target which has moved. This only fires when the pen is down.
     * @param {RenderedTarget} target - the target which has moved.
     * @param {number} oldX - the previous X position.
     * @param {number} oldY - the previous Y position.
     * @param {boolean} isForce - whether the movement was forced.
     * @private
     */
  }, {
    key: "_onTargetMoved",
    value: function _onTargetMoved(target, oldX, oldY, isForce) {
      // Only move the pen if the movement isn't forced (ie. dragged).
      if (!isForce) {
        var penSkinId = this._getPenLayerID();
        if (penSkinId >= 0) {
          var penState = this._getPenState(target);
          this.runtime.renderer.penLine(penSkinId, penState.penAttributes, oldX, oldY, target.x, target.y);
          this.runtime.requestRedraw();
        }
      }
    }

    /**
     * Wrap a color input into the range (0,100).
     * @param {number} value - the value to be wrapped.
     * @returns {number} the wrapped value.
     * @private
     */
  }, {
    key: "_wrapColor",
    value: function _wrapColor(value) {
      return MathUtil.wrapClamp(value, 0, 100);
    }

    /**
     * Initialize color parameters menu with localized strings
     * @returns {array} of the localized text and values for each menu element
     * @private
     */
  }, {
    key: "_initColorParam",
    value: function _initColorParam() {
      return [{
        text: formatMessage({
          id: 'pen.colorMenu.color',
          "default": 'color',
          description: 'label for color element in color picker for pen extension'
        }),
        value: ColorParam.COLOR
      }, {
        text: formatMessage({
          id: 'pen.colorMenu.saturation',
          "default": 'saturation',
          description: 'label for saturation element in color picker for pen extension'
        }),
        value: ColorParam.SATURATION
      }, {
        text: formatMessage({
          id: 'pen.colorMenu.brightness',
          "default": 'brightness',
          description: 'label for brightness element in color picker for pen extension'
        }),
        value: ColorParam.BRIGHTNESS
      }, {
        text: formatMessage({
          id: 'pen.colorMenu.transparency',
          "default": 'transparency',
          description: 'label for transparency element in color picker for pen extension'
        }),
        value: ColorParam.TRANSPARENCY
      }];
    }

    /**
     * Clamp a pen color parameter to the range (0,100).
     * @param {number} value - the value to be clamped.
     * @returns {number} the clamped value.
     * @private
     */
  }, {
    key: "_clampColorParam",
    value: function _clampColorParam(value) {
      return MathUtil.clamp(value, 0, 100);
    }

    /**
     * Convert an alpha value to a pen transparency value.
     * Alpha ranges from 0 to 1, where 0 is transparent and 1 is opaque.
     * Transparency ranges from 0 to 100, where 0 is opaque and 100 is transparent.
     * @param {number} alpha - the input alpha value.
     * @returns {number} the transparency value.
     * @private
     */
  }, {
    key: "_alphaToTransparency",
    value: function _alphaToTransparency(alpha) {
      return (1.0 - alpha) * 100.0;
    }

    /**
     * Convert a pen transparency value to an alpha value.
     * Alpha ranges from 0 to 1, where 0 is transparent and 1 is opaque.
     * Transparency ranges from 0 to 100, where 0 is opaque and 100 is transparent.
     * @param {number} transparency - the input transparency value.
     * @returns {number} the alpha value.
     * @private
     */
  }, {
    key: "_transparencyToAlpha",
    value: function _transparencyToAlpha(transparency) {
      return 1.0 - transparency / 100.0;
    }

    /**
     * @returns {object} metadata for this extension and its blocks.
     */
  }, {
    key: "getInfo",
    value: function getInfo() {
      return {
        id: 'pen',
        name: formatMessage({
          id: 'pen.categoryName',
          "default": 'Pen',
          description: 'Label for the pen extension category'
        }),
        blockIconURI: blockIconURI,
        blocks: [
        // tw: additional message when on the stage for clarity
        {
          blockType: BlockType.LABEL,
          text: formatMessage({
            id: 'tw.pen.stageSelected',
            "default": 'Stage selected: less pen blocks',
            description: 'Label that appears in the Pen category when the stage is selected'
          }),
          filter: [TargetType.STAGE]
        }, {
          opcode: 'clear',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.clear',
            "default": 'erase all',
            description: 'erase all pen trails and stamps'
          })
        }, {
          opcode: 'stamp',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.stamp',
            "default": 'stamp',
            description: 'render current costume on the background'
          }),
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'penDown',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.penDown',
            "default": 'pen down',
            description: 'start leaving a trail when the sprite moves'
          }),
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'penUp',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.penUp',
            "default": 'pen up',
            description: 'stop leaving a trail behind the sprite'
          }),
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'setPenColorToColor',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.setColor',
            "default": 'set pen color to [COLOR]',
            description: 'set the pen color to a particular (RGB) value'
          }),
          arguments: {
            COLOR: {
              type: ArgumentType.COLOR
            }
          },
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'changePenColorParamBy',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.changeColorParam',
            "default": 'change pen [COLOR_PARAM] by [VALUE]',
            description: 'change the state of a pen color parameter'
          }),
          arguments: {
            COLOR_PARAM: {
              type: ArgumentType.STRING,
              menu: 'colorParam',
              defaultValue: ColorParam.COLOR
            },
            VALUE: {
              type: ArgumentType.NUMBER,
              defaultValue: 10
            }
          },
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'setPenColorParamTo',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.setColorParam',
            "default": 'set pen [COLOR_PARAM] to [VALUE]',
            description: 'set the state for a pen color parameter e.g. saturation'
          }),
          arguments: {
            COLOR_PARAM: {
              type: ArgumentType.STRING,
              menu: 'colorParam',
              defaultValue: ColorParam.COLOR
            },
            VALUE: {
              type: ArgumentType.NUMBER,
              defaultValue: 50
            }
          },
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'changePenSizeBy',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.changeSize',
            "default": 'change pen size by [SIZE]',
            description: 'change the diameter of the trail left by a sprite'
          }),
          arguments: {
            SIZE: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          filter: [TargetType.SPRITE]
        }, {
          opcode: 'setPenSizeTo',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.setSize',
            "default": 'set pen size to [SIZE]',
            description: 'set the diameter of a trail left by a sprite'
          }),
          arguments: {
            SIZE: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          filter: [TargetType.SPRITE]
        }, /* Legacy blocks, should not be shown in flyout */
        {
          opcode: 'setPenShadeToNumber',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.setShade',
            "default": 'set pen shade to [SHADE]',
            description: 'legacy pen blocks - set pen shade'
          }),
          arguments: {
            SHADE: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          hideFromPalette: true
        }, {
          opcode: 'changePenShadeBy',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.changeShade',
            "default": 'change pen shade by [SHADE]',
            description: 'legacy pen blocks - change pen shade'
          }),
          arguments: {
            SHADE: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          hideFromPalette: true
        }, {
          opcode: 'setPenHueToNumber',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.setHue',
            "default": 'set pen color to [HUE]',
            description: 'legacy pen blocks - set pen color to number'
          }),
          arguments: {
            HUE: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          hideFromPalette: true
        }, {
          opcode: 'changePenHueBy',
          blockType: BlockType.COMMAND,
          text: formatMessage({
            id: 'pen.changeHue',
            "default": 'change pen color by [HUE]',
            description: 'legacy pen blocks - change pen color'
          }),
          arguments: {
            HUE: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          },
          hideFromPalette: true
        }],
        menus: {
          colorParam: {
            acceptReporters: true,
            items: this._initColorParam()
          }
        }
      };
    }

    /**
     * The pen "clear" block clears the pen layer's contents.
     */
  }, {
    key: "clear",
    value: function clear() {
      // used by compiler
      var penSkinId = this._getPenLayerID();
      if (penSkinId >= 0) {
        this.runtime.renderer.penClear(penSkinId);
        this.runtime.requestRedraw();
      }
    }

    /**
     * The pen "stamp" block stamps the current drawable's image onto the pen layer.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "stamp",
    value: function stamp(args, util) {
      this._stamp(util.target);
    }
  }, {
    key: "_stamp",
    value: function _stamp(target) {
      // used by compiler
      var penSkinId = this._getPenLayerID();
      if (penSkinId >= 0) {
        this.runtime.renderer.penStamp(penSkinId, target.drawableID);
        this.runtime.requestRedraw();
      }
    }

    /**
     * The pen "pen down" block causes the target to leave pen trails on future motion.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "penDown",
    value: function penDown(args, util) {
      this._penDown(util.target);
    }
  }, {
    key: "_penDown",
    value: function _penDown(target) {
      // used by compiler
      var penState = this._getPenState(target);
      if (!penState.penDown) {
        penState.penDown = true;
        target.onTargetMoved = this._onTargetMoved;
      }
      var penSkinId = this._getPenLayerID();
      if (penSkinId >= 0) {
        this.runtime.renderer.penPoint(penSkinId, penState.penAttributes, target.x, target.y);
        this.runtime.requestRedraw();
      }
    }

    /**
     * The pen "pen up" block stops the target from leaving pen trails.
     * @param {object} args - the block arguments.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "penUp",
    value: function penUp(args, util) {
      this._penUp(util.target);
    }
  }, {
    key: "_penUp",
    value: function _penUp(target) {
      // used by compiler
      var penState = this._getPenState(target);
      if (penState.penDown) {
        penState.penDown = false;
        target.onTargetMoved = null;
      }
    }

    /**
     * The pen "set pen color to {color}" block sets the pen to a particular RGB color.
     * The transparency is reset to 0.
     * @param {object} args - the block arguments.
     *  @property {int} COLOR - the color to set, expressed as a 24-bit RGB value (0xRRGGBB).
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "setPenColorToColor",
    value: function setPenColorToColor(args, util) {
      this._setPenColorToColor(args.COLOR, util.target);
    }
  }, {
    key: "_setPenColorToColor",
    value: function _setPenColorToColor(color, target) {
      // used by compiler
      var penState = this._getPenState(target);
      var rgb = Cast.toRgbColorObject(color);
      var hsv = Color.rgbToHsv(rgb);
      penState.color = hsv.h / 360 * 100;
      penState.saturation = hsv.s * 100;
      penState.brightness = hsv.v * 100;
      if (Object.prototype.hasOwnProperty.call(rgb, 'a')) {
        penState.transparency = 100 * (1 - rgb.a / 255.0);
      } else {
        penState.transparency = 0;
      }

      // Set the legacy "shade" value the same way scratch 2 did.
      penState._shade = penState.brightness / 2;
      this._updatePenColor(penState);
    }

    /**
     * Update the cached color from the color, saturation, brightness and transparency values
     * in the provided PenState object.
     * @param {PenState} penState - the pen state to update.
     * @private
     */
  }, {
    key: "_updatePenColor",
    value: function _updatePenColor(penState) {
      var rgb = Color.hsvToRgb({
        h: penState.color * 360 / 100,
        s: penState.saturation / 100,
        v: penState.brightness / 100
      });
      penState.penAttributes.color4f[0] = rgb.r / 255.0;
      penState.penAttributes.color4f[1] = rgb.g / 255.0;
      penState.penAttributes.color4f[2] = rgb.b / 255.0;
      penState.penAttributes.color4f[3] = this._transparencyToAlpha(penState.transparency);
    }

    /**
     * Set or change a single color parameter on the pen state, and update the pen color.
     * @param {ColorParam} param - the name of the color parameter to set or change.
     * @param {number} value - the value to set or change the param by.
     * @param {PenState} penState - the pen state to update.
     * @param {boolean} change - if true change param by value, if false set param to value.
     * @private
     */
  }, {
    key: "_setOrChangeColorParam",
    value: function _setOrChangeColorParam(param, value, penState, change) {
      // used by compiler
      switch (param) {
        case ColorParam.COLOR:
          penState.color = this._wrapColor(value + (change ? penState.color : 0));
          break;
        case ColorParam.SATURATION:
          penState.saturation = this._clampColorParam(value + (change ? penState.saturation : 0));
          break;
        case ColorParam.BRIGHTNESS:
          penState.brightness = this._clampColorParam(value + (change ? penState.brightness : 0));
          break;
        case ColorParam.TRANSPARENCY:
          penState.transparency = this._clampColorParam(value + (change ? penState.transparency : 0));
          break;
        default:
          log.warn("Tried to set or change unknown color parameter: ".concat(param));
      }
      this._updatePenColor(penState);
    }

    /**
     * The "change pen {ColorParam} by {number}" block changes one of the pen's color parameters
     * by a given amound.
     * @param {object} args - the block arguments.
     *  @property {ColorParam} COLOR_PARAM - the name of the selected color parameter.
     *  @property {number} VALUE - the amount to change the selected parameter by.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "changePenColorParamBy",
    value: function changePenColorParamBy(args, util) {
      var penState = this._getPenState(util.target);
      this._setOrChangeColorParam(args.COLOR_PARAM, Cast.toNumber(args.VALUE), penState, true);
    }

    /**
     * The "set pen {ColorParam} to {number}" block sets one of the pen's color parameters
     * to a given amound.
     * @param {object} args - the block arguments.
     *  @property {ColorParam} COLOR_PARAM - the name of the selected color parameter.
     *  @property {number} VALUE - the amount to set the selected parameter to.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "setPenColorParamTo",
    value: function setPenColorParamTo(args, util) {
      var penState = this._getPenState(util.target);
      this._setOrChangeColorParam(args.COLOR_PARAM, Cast.toNumber(args.VALUE), penState, false);
    }

    /**
     * The pen "change pen size by {number}" block changes the pen size by the given amount.
     * @param {object} args - the block arguments.
     *  @property {number} SIZE - the amount of desired size change.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "changePenSizeBy",
    value: function changePenSizeBy(args, util) {
      this._changePenSizeBy(Cast.toNumber(args.SIZE), util.target);
    }
  }, {
    key: "_changePenSizeBy",
    value: function _changePenSizeBy(size, target) {
      // used by compiler
      var penAttributes = this._getPenState(target).penAttributes;
      penAttributes.diameter = this._clampPenSize(penAttributes.diameter + size);
    }

    /**
     * The pen "set pen size to {number}" block sets the pen size to the given amount.
     * @param {object} args - the block arguments.
     *  @property {number} SIZE - the amount of desired size change.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "setPenSizeTo",
    value: function setPenSizeTo(args, util) {
      this._setPenSizeTo(Cast.toNumber(args.SIZE), util.target);
    }
  }, {
    key: "_setPenSizeTo",
    value: function _setPenSizeTo(size, target) {
      // used by compiler
      var penAttributes = this._getPenState(target).penAttributes;
      penAttributes.diameter = this._clampPenSize(size);
    }

    /* LEGACY OPCODES */
    /**
     * Scratch 2 "hue" param is equivelant to twice the new "color" param.
     * @param {object} args - the block arguments.
     *  @property {number} HUE - the amount to set the hue to.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "setPenHueToNumber",
    value: function setPenHueToNumber(args, util) {
      this._setPenHueToNumber(Cast.toNumber(args.HUE), util.target);
    }
  }, {
    key: "_setPenHueToNumber",
    value: function _setPenHueToNumber(hueValue, target) {
      var penState = this._getPenState(target);
      var colorValue = hueValue / 2;
      this._setOrChangeColorParam(ColorParam.COLOR, colorValue, penState, false);
      this._setOrChangeColorParam(ColorParam.TRANSPARENCY, 0, penState, false);
      this._legacyUpdatePenColor(penState);
    }

    /**
     * Scratch 2 "hue" param is equivelant to twice the new "color" param.
     * @param {object} args - the block arguments.
     *  @property {number} HUE - the amount of desired hue change.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "changePenHueBy",
    value: function changePenHueBy(args, util) {
      this._changePenHueBy(Cast.toNumber(args.HUE), util.target);
    }
  }, {
    key: "_changePenHueBy",
    value: function _changePenHueBy(hueChange, target) {
      // used by compiler
      var penState = this._getPenState(target);
      var colorChange = hueChange / 2;
      this._setOrChangeColorParam(ColorParam.COLOR, colorChange, penState, true);
      this._legacyUpdatePenColor(penState);
    }

    /**
     * Use legacy "set shade" code to calculate RGB value for shade,
     * then convert back to HSV and store those components.
     * It is important to also track the given shade in penState._shade
     * because it cannot be accurately backed out of the new HSV later.
     * @param {object} args - the block arguments.
     *  @property {number} SHADE - the amount to set the shade to.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "setPenShadeToNumber",
    value: function setPenShadeToNumber(args, util) {
      this._setPenShadeToNumber(Cast.toNumber(args.SHADE), util.target);
    }
  }, {
    key: "_setPenShadeToNumber",
    value: function _setPenShadeToNumber(shade, target) {
      var penState = this._getPenState(target);
      var newShade = Cast.toNumber(shade);

      // Wrap clamp the new shade value the way scratch 2 did.
      newShade = newShade % 200;
      if (newShade < 0) newShade += 200;

      // And store the shade that was used to compute this new color for later use.
      penState._shade = newShade;
      this._legacyUpdatePenColor(penState);
    }

    /**
     * Because "shade" cannot be backed out of hsv consistently, use the previously
     * stored penState._shade to make the shade change.
     * @param {object} args - the block arguments.
     *  @property {number} SHADE - the amount of desired shade change.
     * @param {object} util - utility object provided by the runtime.
     */
  }, {
    key: "changePenShadeBy",
    value: function changePenShadeBy(args, util) {
      this._changePenShadeBy(args.SHADE, util.target);
    }
  }, {
    key: "_changePenShadeBy",
    value: function _changePenShadeBy(shade, target) {
      var penState = this._getPenState(target);
      var shadeChange = Cast.toNumber(shade);
      this._setPenShadeToNumber(penState._shade + shadeChange, target);
    }

    /**
     * Update the pen state's color from its hue & shade values, Scratch 2.0 style.
     * @param {object} penState - update the HSV & RGB values in this pen state from its hue & shade values.
     * @private
     */
  }, {
    key: "_legacyUpdatePenColor",
    value: function _legacyUpdatePenColor(penState) {
      // Create the new color in RGB using the scratch 2 "shade" model
      var rgb = Color.hsvToRgb({
        h: penState.color * 360 / 100,
        s: 1,
        v: 1
      });
      var shade = penState._shade > 100 ? 200 - penState._shade : penState._shade;
      if (shade < 50) {
        rgb = Color.mixRgb(Color.RGB_BLACK, rgb, (10 + shade) / 60);
      } else {
        rgb = Color.mixRgb(rgb, Color.RGB_WHITE, (shade - 50) / 60);
      }

      // Update the pen state according to new color
      var hsv = Color.rgbToHsv(rgb);
      penState.color = 100 * hsv.h / 360;
      penState.saturation = 100 * hsv.s;
      penState.brightness = 100 * hsv.v;
      this._updatePenColor(penState);
    }
  }], [{
    key: "DEFAULT_PEN_STATE",
    get: function get() {
      return {
        penDown: false,
        color: 66.66,
        saturation: 100,
        brightness: 100,
        transparency: 0,
        _shade: 50,
        // Used only for legacy `change shade by` blocks
        penAttributes: {
          color4f: [0, 0, 1, 1],
          diameter: 1
        }
      };
    }

    /**
     * The minimum and maximum allowed pen size.
     * The maximum is twice the diagonal of the stage, so that even an
     * off-stage sprite can fill it.
     * @type {{min: number, max: number}}
     */
  }, {
    key: "PEN_SIZE_RANGE",
    get: function get() {
      return {
        min: 1,
        max: 1200
      };
    }

    /**
     * The key to load & store a target's pen-related state.
     * @type {string}
     */
  }, {
    key: "STATE_KEY",
    get: function get() {
      // tw: We've hardcoded this value in various places for slight performance gains
      // Make sure to update those if this changes.
      return 'Scratch.pen';
    }
  }]);
}();
module.exports = Scratch3PenBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_text2speech/index.js":
/*!**********************************************************!*\
  !*** ../vm/src/extensions/scratch3_text2speech/index.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var languageNames = __webpack_require__(/*! scratch-translate-extension-languages */ "../vm/node_modules/scratch-translate-extension-languages/languages.json");
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var Clone = __webpack_require__(/*! ../../util/clone */ "../vm/src/util/clone.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");
var _require = __webpack_require__(/*! ../../util/fetch-with-timeout */ "../vm/src/util/fetch-with-timeout.js"),
  fetchWithTimeout = _require.fetchWithTimeout;

/**
 * Icon svg to be displayed in the blocks category menu, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var menuIconURI = 'data:image/svg+xml;base64,';

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/svg+xml;base64,';

/**
 * The url of the synthesis server.
 * @type {string}
 */
var SERVER_HOST = 'https://synthesis-service.scratch.mit.edu';

/**
 * How long to wait in ms before timing out requests to synthesis server.
 * @type {int}
 */
var SERVER_TIMEOUT = 10000; // 10 seconds

/**
 * Volume for playback of speech sounds, as a percentage.
 * @type {number}
 */
var SPEECH_VOLUME = 250;

/**
 * An id for one of the voices.
 */
var ALTO_ID = 'ALTO';

/**
 * An id for one of the voices.
 */
var TENOR_ID = 'TENOR';

/**
 * An id for one of the voices.
 */
var SQUEAK_ID = 'SQUEAK';

/**
 * An id for one of the voices.
 */
var GIANT_ID = 'GIANT';

/**
 * An id for one of the voices.
 */
var KITTEN_ID = 'KITTEN';

/**
 * Playback rate for the tenor voice, for cases where we have only a female gender voice.
 */
var FEMALE_TENOR_RATE = 0.89; // -2 semitones

/**
 * Playback rate for the giant voice, for cases where we have only a female gender voice.
 */
var FEMALE_GIANT_RATE = 0.79; // -4 semitones

/**
 * Language ids. The value for each language id is a valid Scratch locale.
 */
var ARABIC_ID = 'ar';
var CHINESE_ID = 'zh-cn';
var DANISH_ID = 'da';
var DUTCH_ID = 'nl';
var ENGLISH_ID = 'en';
var FRENCH_ID = 'fr';
var GERMAN_ID = 'de';
var HINDI_ID = 'hi';
var ICELANDIC_ID = 'is';
var ITALIAN_ID = 'it';
var JAPANESE_ID = 'ja';
var KOREAN_ID = 'ko';
var NORWEGIAN_ID = 'nb';
var POLISH_ID = 'pl';
var PORTUGUESE_BR_ID = 'pt-br';
var PORTUGUESE_ID = 'pt';
var ROMANIAN_ID = 'ro';
var RUSSIAN_ID = 'ru';
var SPANISH_ID = 'es';
var SPANISH_419_ID = 'es-419';
var SWEDISH_ID = 'sv';
var TURKISH_ID = 'tr';
var WELSH_ID = 'cy';

/**
 * Class for the text2speech blocks.
 * @constructor
 */
var Scratch3Text2SpeechBlocks = /*#__PURE__*/function () {
  function Scratch3Text2SpeechBlocks(runtime) {
    _classCallCheck(this, Scratch3Text2SpeechBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * Map of soundPlayers by sound id.
     * @type {Map<string, SoundPlayer>}
     */
    this._soundPlayers = new Map();
    this._stopAllSpeech = this._stopAllSpeech.bind(this);
    if (this.runtime) {
      this.runtime.on('PROJECT_STOP_ALL', this._stopAllSpeech);
    }
    this._onTargetCreated = this._onTargetCreated.bind(this);
    if (this.runtime) {
      runtime.on('targetWasCreated', this._onTargetCreated);
    }

    /**
     * A list of all Scratch locales that are supported by the extension.
     * @type {Array}
     */
    this._supportedLocales = this._getSupportedLocales();
  }

  /**
   * An object with info for each voice.
   */
  return _createClass(Scratch3Text2SpeechBlocks, [{
    key: "VOICE_INFO",
    get: function get() {
      return _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, ALTO_ID, {
        name: formatMessage({
          id: 'text2speech.alto',
          "default": 'alto',
          description: 'Name for a voice with ambiguous gender.'
        }),
        gender: 'female',
        playbackRate: 1
      }), TENOR_ID, {
        name: formatMessage({
          id: 'text2speech.tenor',
          "default": 'tenor',
          description: 'Name for a voice with ambiguous gender.'
        }),
        gender: 'male',
        playbackRate: 1
      }), SQUEAK_ID, {
        name: formatMessage({
          id: 'text2speech.squeak',
          "default": 'squeak',
          description: 'Name for a funny voice with a high pitch.'
        }),
        gender: 'female',
        playbackRate: 1.19 // +3 semitones
      }), GIANT_ID, {
        name: formatMessage({
          id: 'text2speech.giant',
          "default": 'giant',
          description: 'Name for a funny voice with a low pitch.'
        }),
        gender: 'male',
        playbackRate: 0.84 // -3 semitones
      }), KITTEN_ID, {
        name: formatMessage({
          id: 'text2speech.kitten',
          "default": 'kitten',
          description: 'A baby cat.'
        }),
        gender: 'female',
        playbackRate: 1.41 // +6 semitones
      });
    }

    /**
     * An object with information for each language.
     *
     * A note on the different sets of locales referred to in this extension:
     *
     * SCRATCH LOCALE
     *      Set by the editor, and used to store the language state in the project.
     *      Listed in l10n: https://github.com/scratchfoundation/scratch-l10n/blob/master/src/supported-locales.js
     * SUPPORTED LOCALE
     *      A Scratch locale that has a corresponding extension locale.
     * EXTENSION LOCALE
     *      A locale corresponding to one of the available spoken languages
     *      in the extension. There can be multiple supported locales for a single
     *      extension locale. For example, for both written versions of chinese,
     *      zh-cn and zh-tw, we use a single spoken language (Mandarin). So there
     *      are two supported locales, with a single extension locale.
     * SPEECH SYNTH LOCALE
     *      A different locale code system, used by our speech synthesis service.
     *      Each extension locale has a speech synth locale.
     */
  }, {
    key: "LANGUAGE_INFO",
    get: function get() {
      var _ref2;
      return _ref2 = {}, _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_ref2, ARABIC_ID, {
        name: 'Arabic',
        locales: ['ar'],
        speechSynthLocale: 'arb',
        singleGender: true
      }), CHINESE_ID, {
        name: 'Chinese (Mandarin)',
        locales: ['zh-cn', 'zh-tw'],
        speechSynthLocale: 'cmn-CN',
        singleGender: true
      }), DANISH_ID, {
        name: 'Danish',
        locales: ['da'],
        speechSynthLocale: 'da-DK'
      }), DUTCH_ID, {
        name: 'Dutch',
        locales: ['nl'],
        speechSynthLocale: 'nl-NL'
      }), ENGLISH_ID, {
        name: 'English',
        locales: ['en'],
        speechSynthLocale: 'en-US'
      }), FRENCH_ID, {
        name: 'French',
        locales: ['fr'],
        speechSynthLocale: 'fr-FR'
      }), GERMAN_ID, {
        name: 'German',
        locales: ['de'],
        speechSynthLocale: 'de-DE'
      }), HINDI_ID, {
        name: 'Hindi',
        locales: ['hi'],
        speechSynthLocale: 'hi-IN',
        singleGender: true
      }), ICELANDIC_ID, {
        name: 'Icelandic',
        locales: ['is'],
        speechSynthLocale: 'is-IS'
      }), ITALIAN_ID, {
        name: 'Italian',
        locales: ['it'],
        speechSynthLocale: 'it-IT'
      }), _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_ref2, JAPANESE_ID, {
        name: 'Japanese',
        locales: ['ja', 'ja-hira'],
        speechSynthLocale: 'ja-JP'
      }), KOREAN_ID, {
        name: 'Korean',
        locales: ['ko'],
        speechSynthLocale: 'ko-KR',
        singleGender: true
      }), NORWEGIAN_ID, {
        name: 'Norwegian',
        locales: ['nb', 'nn'],
        speechSynthLocale: 'nb-NO',
        singleGender: true
      }), POLISH_ID, {
        name: 'Polish',
        locales: ['pl'],
        speechSynthLocale: 'pl-PL'
      }), PORTUGUESE_BR_ID, {
        name: 'Portuguese (Brazilian)',
        locales: ['pt-br'],
        speechSynthLocale: 'pt-BR'
      }), PORTUGUESE_ID, {
        name: 'Portuguese (European)',
        locales: ['pt'],
        speechSynthLocale: 'pt-PT'
      }), ROMANIAN_ID, {
        name: 'Romanian',
        locales: ['ro'],
        speechSynthLocale: 'ro-RO',
        singleGender: true
      }), RUSSIAN_ID, {
        name: 'Russian',
        locales: ['ru'],
        speechSynthLocale: 'ru-RU'
      }), SPANISH_ID, {
        name: 'Spanish (European)',
        locales: ['es'],
        speechSynthLocale: 'es-ES'
      }), SPANISH_419_ID, {
        name: 'Spanish (Latin American)',
        locales: ['es-419'],
        speechSynthLocale: 'es-US'
      }), _defineProperty(_defineProperty(_defineProperty(_ref2, SWEDISH_ID, {
        name: 'Swedish',
        locales: ['sv'],
        speechSynthLocale: 'sv-SE',
        singleGender: true
      }), TURKISH_ID, {
        name: 'Turkish',
        locales: ['tr'],
        speechSynthLocale: 'tr-TR',
        singleGender: true
      }), WELSH_ID, {
        name: 'Welsh',
        locales: ['cy'],
        speechSynthLocale: 'cy-GB',
        singleGender: true
      });
    }

    /**
     * The key to load & store a target's text2speech state.
     * @return {string} The key.
     */
  }, {
    key: "DEFAULT_LANGUAGE",
    get:
    /**
     * A default language to use for speech synthesis.
     * @type {string}
     */
    function get() {
      return ENGLISH_ID;
    }

    /**
     * @param {Target} target - collect  state for this target.
     * @returns {Text2SpeechState} the mutable state associated with that target. This will be created if necessary.
     * @private
     */
  }, {
    key: "_getState",
    value: function _getState(target) {
      var state = target.getCustomState(Scratch3Text2SpeechBlocks.STATE_KEY);
      if (!state) {
        state = Clone.simple(Scratch3Text2SpeechBlocks.DEFAULT_TEXT2SPEECH_STATE);
        target.setCustomState(Scratch3Text2SpeechBlocks.STATE_KEY, state);
      }
      return state;
    }

    /**
     * When a Target is cloned, clone the state.
     * @param {Target} newTarget - the newly created target.
     * @param {Target} [sourceTarget] - the target used as a source for the new clone, if any.
     * @listens Runtime#event:targetWasCreated
     * @private
     */
  }, {
    key: "_onTargetCreated",
    value: function _onTargetCreated(newTarget, sourceTarget) {
      if (sourceTarget) {
        var state = sourceTarget.getCustomState(Scratch3Text2SpeechBlocks.STATE_KEY);
        if (state) {
          newTarget.setCustomState(Scratch3Text2SpeechBlocks.STATE_KEY, Clone.simple(state));
        }
      }
    }

    /**
     * @returns {object} metadata for this extension and its blocks.
     */
  }, {
    key: "getInfo",
    value: function getInfo() {
      // Only localize the default input to the "speak" block if we are in a
      // supported language.
      var defaultTextToSpeak = 'hello';
      if (this.isSupportedLanguage(this.getEditorLanguage())) {
        defaultTextToSpeak = formatMessage({
          id: 'text2speech.defaultTextToSpeak',
          "default": 'hello',
          description: 'hello: the default text to speak'
        });
      }
      return {
        id: 'text2speech',
        name: formatMessage({
          id: 'text2speech.categoryName',
          "default": 'Text to Speech',
          description: 'Name of the Text to Speech extension.'
        }),
        blockIconURI: blockIconURI,
        menuIconURI: menuIconURI,
        blocks: [{
          opcode: 'speakAndWait',
          text: formatMessage({
            id: 'text2speech.speakAndWaitBlock',
            "default": 'speak [WORDS]',
            description: 'Speak some words.'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            WORDS: {
              type: ArgumentType.STRING,
              defaultValue: defaultTextToSpeak
            }
          }
        }, {
          opcode: 'setVoice',
          text: formatMessage({
            id: 'text2speech.setVoiceBlock',
            "default": 'set voice to [VOICE]',
            description: 'Set the voice for speech synthesis.'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            VOICE: {
              type: ArgumentType.STRING,
              menu: 'voices',
              defaultValue: ALTO_ID
            }
          }
        }, {
          opcode: 'setLanguage',
          text: formatMessage({
            id: 'text2speech.setLanguageBlock',
            "default": 'set language to [LANGUAGE]',
            description: 'Set the language for speech synthesis.'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            LANGUAGE: {
              type: ArgumentType.STRING,
              menu: 'languages',
              defaultValue: this.getCurrentLanguage()
            }
          }
        }],
        menus: {
          voices: {
            acceptReporters: true,
            items: this.getVoiceMenu()
          },
          languages: {
            acceptReporters: true,
            items: this.getLanguageMenu()
          }
        }
      };
    }

    /**
     * Get the language code currently set in the editor, or fall back to the
     * browser locale.
     * @return {string} a Scratch locale code.
     */
  }, {
    key: "getEditorLanguage",
    value: function getEditorLanguage() {
      var locale = formatMessage.setup().locale || navigator.language || navigator.userLanguage || this.DEFAULT_LANGUAGE;
      return locale.toLowerCase();
    }

    /**
     * Get the language code currently set for the extension.
     * @returns {string} a Scratch locale code.
     */
  }, {
    key: "getCurrentLanguage",
    value: function getCurrentLanguage() {
      var stage = this.runtime.getTargetForStage();
      if (!stage) return this.DEFAULT_LANGUAGE;
      // If no language has been set, set it to the editor locale (or default).
      if (!stage.textToSpeechLanguage) {
        this.setCurrentLanguage(this.getEditorLanguage());
      }
      return stage.textToSpeechLanguage;
    }

    /**
     * Set the language code for the extension.
     * It is stored in the stage so it can be saved and loaded with the project.
     * @param {string} locale a locale code.
     */
  }, {
    key: "setCurrentLanguage",
    value: function setCurrentLanguage(locale) {
      var stage = this.runtime.getTargetForStage();
      if (!stage) return;
      if (this.isSupportedLanguage(locale)) {
        stage.textToSpeechLanguage = this._getExtensionLocaleForSupportedLocale(locale);
      }

      // Support language names dropped onto the menu via reporter block
      // such as a variable containing a language name (in any language),
      // or the translate extension's language reporter.
      var localeForDroppedName = languageNames.nameMap[locale.toLowerCase()];
      if (localeForDroppedName && this.isSupportedLanguage(localeForDroppedName)) {
        stage.textToSpeechLanguage = this._getExtensionLocaleForSupportedLocale(localeForDroppedName);
      }

      // If the language is null, set it to the default language.
      // This can occur e.g. if the extension was loaded with the editor
      // set to a language that is not in the list.
      if (!stage.textToSpeechLanguage) {
        stage.textToSpeechLanguage = this.DEFAULT_LANGUAGE;
      }
    }

    /**
     * Get the extension locale for a supported locale, or null.
     * @param {string} locale a locale code.
     * @returns {?string} a locale supported by the extension.
     */
  }, {
    key: "_getExtensionLocaleForSupportedLocale",
    value: function _getExtensionLocaleForSupportedLocale(locale) {
      for (var lang in this.LANGUAGE_INFO) {
        if (this.LANGUAGE_INFO[lang].locales.includes(locale)) {
          return lang;
        }
      }
      log.error("cannot find extension locale for locale ".concat(locale));
    }

    /**
     * Get the locale code used by the speech synthesis server corresponding to
     * the current language code set for the extension.
     * @returns {string} a speech synthesis locale.
     */
  }, {
    key: "_getSpeechSynthLocale",
    value: function _getSpeechSynthLocale() {
      var speechSynthLocale = this.LANGUAGE_INFO[this.DEFAULT_LANGUAGE].speechSynthLocale;
      if (this.LANGUAGE_INFO[this.getCurrentLanguage()]) {
        speechSynthLocale = this.LANGUAGE_INFO[this.getCurrentLanguage()].speechSynthLocale;
      }
      return speechSynthLocale;
    }

    /**
     * Get an array of the locales supported by this extension.
     * @returns {Array} An array of locale strings.
     */
  }, {
    key: "_getSupportedLocales",
    value: function _getSupportedLocales() {
      var _this = this;
      return Object.keys(this.LANGUAGE_INFO).reduce(function (acc, lang) {
        return acc.concat(_this.LANGUAGE_INFO[lang].locales);
      }, []);
    }

    /**
     * Check if a Scratch language code is in the list of supported languages for the
     * speech synthesis service.
     * @param {string} languageCode the language code to check.
     * @returns {boolean} true if the language code is supported.
     */
  }, {
    key: "isSupportedLanguage",
    value: function isSupportedLanguage(languageCode) {
      return this._supportedLocales.includes(languageCode);
    }

    /**
     * Get the menu of voices for the "set voice" block.
     * @return {array} the text and value for each menu item.
     */
  }, {
    key: "getVoiceMenu",
    value: function getVoiceMenu() {
      var _this2 = this;
      return Object.keys(this.VOICE_INFO).map(function (voiceId) {
        return {
          text: _this2.VOICE_INFO[voiceId].name,
          value: voiceId
        };
      });
    }

    /**
     * Get the localized menu of languages for the "set language" block.
     * For each language:
     *   if there is a custom translated spoken language name, use that;
     *   otherwise use the translation in the languageNames menuMap;
     *   otherwise fall back to the untranslated name in LANGUAGE_INFO.
     * @return {array} the text and value for each menu item.
     */
  }, {
    key: "getLanguageMenu",
    value: function getLanguageMenu() {
      var _this3 = this;
      var editorLanguage = this.getEditorLanguage();
      // Get the array of localized language names
      var localizedNameMap = {};
      var nameArray = languageNames.menuMap[editorLanguage];
      if (nameArray) {
        // Also get any localized names of spoken languages
        var spokenNameArray = [];
        if (languageNames.spokenLanguages) {
          spokenNameArray = languageNames.spokenLanguages[editorLanguage];
          nameArray = nameArray.concat(spokenNameArray);
        }
        // Create a map of language code to localized name
        // The localized spoken language names have been concatenated onto
        // the end of the name array, so the result of the forEach below is
        // when there is both a written language name (e.g. 'Chinese
        // (simplified)') and a spoken language name (e.g. 'Chinese
        // (Mandarin)', we always use the spoken version.
        nameArray.forEach(function (lang) {
          localizedNameMap[lang.code] = lang.name;
        });
      }
      return Object.keys(this.LANGUAGE_INFO).map(function (key) {
        var name = _this3.LANGUAGE_INFO[key].name;
        var localizedName = localizedNameMap[key];
        if (localizedName) {
          name = localizedName;
        }
        // Uppercase the first character of the name
        name = name.charAt(0).toUpperCase() + name.slice(1);
        return {
          text: name,
          value: key
        };
      });
    }

    /**
     * Set the voice for speech synthesis for this sprite.
     * @param  {object} args Block arguments
     * @param {object} util Utility object provided by the runtime.
     */
  }, {
    key: "setVoice",
    value: function setVoice(args, util) {
      var state = this._getState(util.target);
      var voice = args.VOICE;

      // If the arg is a dropped number, treat it as a voice index
      var voiceNum = parseInt(voice, 10);
      if (!isNaN(voiceNum)) {
        voiceNum -= 1; // Treat dropped args as one-indexed
        voiceNum = MathUtil.wrapClamp(voiceNum, 0, Object.keys(this.VOICE_INFO).length - 1);
        voice = Object.keys(this.VOICE_INFO)[voiceNum];
      }

      // Only set the voice if the arg is a valid voice id.
      if (Object.keys(this.VOICE_INFO).includes(voice)) {
        state.voiceId = voice;
      }
    }

    /**
     * Set the language for speech synthesis.
     * @param  {object} args Block arguments
     */
  }, {
    key: "setLanguage",
    value: function setLanguage(args) {
      this.setCurrentLanguage(args.LANGUAGE);
    }

    /**
     * Stop all currently playing speech sounds.
     */
  }, {
    key: "_stopAllSpeech",
    value: function _stopAllSpeech() {
      this._soundPlayers.forEach(function (player) {
        player.stop();
      });
    }

    /**
     * Convert the provided text into a sound file and then play the file.
     * @param  {object} args Block arguments
     * @param {object} util Utility object provided by the runtime.
     * @return {Promise} A promise that resolves after playing the sound
     */
  }, {
    key: "speakAndWait",
    value: function speakAndWait(args, util) {
      var _this4 = this;
      // Cast input to string
      var words = Cast.toString(args.WORDS);
      var locale = this._getSpeechSynthLocale();
      var state = this._getState(util.target);
      var gender = this.VOICE_INFO[state.voiceId].gender;
      var playbackRate = this.VOICE_INFO[state.voiceId].playbackRate;

      // Special case for voices where the synthesis service only provides a
      // single gender voice. In that case, always request the female voice,
      // and set special playback rates for the tenor and giant voices.
      if (this.LANGUAGE_INFO[this.getCurrentLanguage()].singleGender) {
        gender = 'female';
        if (state.voiceId === TENOR_ID) {
          playbackRate = FEMALE_TENOR_RATE;
        }
        if (state.voiceId === GIANT_ID) {
          playbackRate = FEMALE_GIANT_RATE;
        }
      }
      if (state.voiceId === KITTEN_ID) {
        words = words.replace(/\S+/g, 'meow');
        locale = this.LANGUAGE_INFO[this.DEFAULT_LANGUAGE].speechSynthLocale;
      }

      // Build up URL
      var path = "".concat(SERVER_HOST, "/synth");
      path += "?locale=".concat(locale);
      path += "&gender=".concat(gender);
      path += "&text=".concat(encodeURIComponent(words.substring(0, 128)));

      // Perform HTTP request to get audio file
      return fetchWithTimeout(path, {}, SERVER_TIMEOUT).then(function (res) {
        if (res.status !== 200) {
          throw new Error("HTTP ".concat(res.status, " error reaching translation service"));
        }
        return res.arrayBuffer();
      }).then(function (buffer) {
        // Play the sound
        var sound = {
          data: {
            buffer: buffer
          }
        };
        return _this4.runtime.audioEngine.decodeSoundPlayer(sound);
      }).then(function (soundPlayer) {
        _this4._soundPlayers.set(soundPlayer.id, soundPlayer);
        soundPlayer.setPlaybackRate(playbackRate);

        // Increase the volume
        var engine = _this4.runtime.audioEngine;
        var chain = engine.createEffectChain();
        chain.set('volume', SPEECH_VOLUME);
        soundPlayer.connect(chain);
        soundPlayer.play();
        return new Promise(function (resolve) {
          soundPlayer.on('stop', function () {
            _this4._soundPlayers["delete"](soundPlayer.id);
            resolve();
          });
        });
      })["catch"](function (err) {
        log.warn(err);
      });
    }
  }], [{
    key: "STATE_KEY",
    get: function get() {
      return 'Scratch.text2speech';
    }

    /**
     * The default state, to be used when a target has no existing state.
     * @type {Text2SpeechState}
     */
  }, {
    key: "DEFAULT_TEXT2SPEECH_STATE",
    get: function get() {
      return {
        voiceId: ALTO_ID
      };
    }
  }]);
}();
module.exports = Scratch3Text2SpeechBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_translate/index.js":
/*!********************************************************!*\
  !*** ../vm/src/extensions/scratch3_translate/index.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");
var _require = __webpack_require__(/*! ../../util/fetch-with-timeout */ "../vm/src/util/fetch-with-timeout.js"),
  fetchWithTimeout = _require.fetchWithTimeout;
var languageNames = __webpack_require__(/*! scratch-translate-extension-languages */ "../vm/node_modules/scratch-translate-extension-languages/languages.json");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");

/**
 * Icon svg to be displayed in the blocks category menu, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var menuIconURI = 'data:image/png;base64,';

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/png;base64,';

/**
 * The url of the translate server.
 * @type {string}
 */
var serverURL = 'https://trampoline.turbowarp.org/translate/';

/**
 * How long to wait in ms before timing out requests to translate server.
 * @type {int}
 */
var serverTimeoutMs = 10000; // 10 seconds (chosen arbitrarily).

/**
 * Class for the translate block in Scratch 3.0.
 * @constructor
 */
var Scratch3TranslateBlocks = /*#__PURE__*/function () {
  function Scratch3TranslateBlocks() {
    _classCallCheck(this, Scratch3TranslateBlocks);
    /**
     * Language code of the viewer, based on their locale.
     * @type {string}
     * @private
     */
    this._viewerLanguageCode = this.getViewerLanguageCode();

    /**
     * List of supported language name and language code pairs, for use in the block menu.
     * Filled in by getInfo so it is updated when the interface language changes.
     * @type {Array.<object.<string, string>>}
     * @private
     */
    this._supportedLanguages = [];

    /**
     * A randomly selected language code, for use as the default value in the language menu.
     * Properly filled in getInfo so it is updated when the interface languages changes.
     * @type {string}
     * @private
     */
    this._randomLanguageCode = 'en';

    /**
     * The result from the most recent translation.
     * @type {string}
     * @private
     */
    this._translateResult = '';

    /**
     * The language of the text most recently translated.
     * @type {string}
     * @private
     */
    this._lastLangTranslated = '';

    /**
     * The text most recently translated.
     * @type {string}
     * @private
     */
    this._lastTextTranslated = '';
  }

  /**
   * The key to load & store a target's translate state.
   * @return {string} The key.
   */
  return _createClass(Scratch3TranslateBlocks, [{
    key: "getInfo",
    value:
    /**
     * @returns {object} metadata for this extension and its blocks.
     */
    function getInfo() {
      this._supportedLanguages = this._getSupportedLanguages(this.getViewerLanguageCode());
      this._randomLanguageCode = this._supportedLanguages[Math.floor(Math.random() * this._supportedLanguages.length)].value;
      return {
        id: 'translate',
        name: formatMessage({
          id: 'translate.categoryName',
          "default": 'Translate',
          description: 'Name of extension that adds translate blocks'
        }),
        blockIconURI: blockIconURI,
        menuIconURI: menuIconURI,
        blocks: [{
          opcode: 'getTranslate',
          text: formatMessage({
            id: 'translate.translateBlock',
            "default": 'translate [WORDS] to [LANGUAGE]',
            description: 'translate some text to a different language'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            WORDS: {
              type: ArgumentType.STRING,
              defaultValue: formatMessage({
                id: 'translate.defaultTextToTranslate',
                "default": 'hello',
                description: 'hello: the default text to translate'
              })
            },
            LANGUAGE: {
              type: ArgumentType.STRING,
              menu: 'languages',
              defaultValue: this._randomLanguageCode
            }
          }
        }, {
          opcode: 'getViewerLanguage',
          text: formatMessage({
            id: 'translate.viewerLanguage',
            "default": 'language',
            description: 'the languge of the project viewer'
          }),
          blockType: BlockType.REPORTER,
          arguments: {}
        }],
        menus: {
          languages: {
            acceptReporters: true,
            items: this._supportedLanguages
          }
        }
      };
    }

    /**
     * Computes a list of language code and name pairs for the given language.
     * @param {string} code The language code to get the list of language pairs
     * @return {Array.<object.<string, string>>} An array of languge name and
     *   language code pairs.
     * @private
     */
  }, {
    key: "_getSupportedLanguages",
    value: function _getSupportedLanguages(code) {
      return languageNames.menuMap[code].map(function (entry) {
        var obj = {
          text: entry.name,
          value: entry.code
        };
        return obj;
      });
    }
    /**
     * Get the human readable language value for the reporter block.
     * @return {string} the language name of the project viewer.
     */
  }, {
    key: "getViewerLanguage",
    value: function getViewerLanguage() {
      var _this = this;
      this._viewerLanguageCode = this.getViewerLanguageCode();
      var names = languageNames.menuMap[this._viewerLanguageCode];
      var langNameObj = names.find(function (obj) {
        return obj.code === _this._viewerLanguageCode;
      });

      // If we don't have a name entry yet, try looking it up via the Google langauge
      // code instead of Scratch's (e.g. for es-419 we look up es to get espanol)
      if (!langNameObj && languageNames.scratchToGoogleMap[this._viewerLanguageCode]) {
        var lookupCode = languageNames.scratchToGoogleMap[this._viewerLanguageCode];
        langNameObj = names.find(function (obj) {
          return obj.code === lookupCode;
        });
      }
      var langName = this._viewerLanguageCode;
      if (langNameObj) {
        langName = langNameObj.name;
      }
      return langName;
    }

    /**
     * Get the viewer's language code.
     * @return {string} the language code.
     */
  }, {
    key: "getViewerLanguageCode",
    value: function getViewerLanguageCode() {
      var locale = formatMessage.setup().locale;
      var viewerLanguages = [locale].concat(navigator.languages);
      var languageKeys = Object.keys(languageNames.menuMap);
      // Return the first entry in viewerLanguages that matches
      // one of the available language keys.
      var languageCode = viewerLanguages.reduce(function (acc, lang) {
        if (acc) {
          return acc;
        }
        if (languageKeys.indexOf(lang.toLowerCase()) > -1) {
          return lang;
        }
        return acc;
      }, '') || 'en';
      return languageCode.toLowerCase();
    }

    /**
     * Get a language code from a block argument. The arg can be a language code
     * or a language name, written in any language.
     * @param  {object} arg A block argument.
     * @return {string} A language code.
     */
  }, {
    key: "getLanguageCodeFromArg",
    value: function getLanguageCodeFromArg(arg) {
      var languageArg = Cast.toString(arg).toLowerCase();
      // Check if the arg matches a language code in the menu.
      if (Object.prototype.hasOwnProperty.call(languageNames.menuMap, languageArg)) {
        return languageArg;
      }
      // Check for a dropped-in language name, and convert to a language code.
      if (Object.prototype.hasOwnProperty.call(languageNames.nameMap, languageArg)) {
        return languageNames.nameMap[languageArg];
      }

      // There are some languages we launched in the language menu that Scratch did not
      // end up launching in. In order to keep projects that may have had that menu item
      // working, check for those language codes and let them through.
      // Examples: 'ab', 'hi'.
      if (languageNames.previouslySupported.indexOf(languageArg) !== -1) {
        return languageArg;
      }
      // Default to English.
      return 'en';
    }

    /**
     * Translates the text in the translate block to the language specified in the menu.
     * @param {object} args - the block arguments.
     * @return {Promise} - a promise that resolves after the response from the translate server.
     */
  }, {
    key: "getTranslate",
    value: function getTranslate(args) {
      // If the text contains only digits 0-9 and nothing else, return it without
      // making a request.
      if (/^\d+$/.test(args.WORDS)) return Promise.resolve(args.WORDS);

      // Don't remake the request if we already have the value.
      if (this._lastTextTranslated === args.WORDS && this._lastLangTranslated === args.LANGUAGE) {
        return this._translateResult;
      }
      var lang = this.getLanguageCodeFromArg(args.LANGUAGE);
      var urlBase = "".concat(serverURL, "translate?language=");
      urlBase += lang;
      urlBase += '&text=';
      urlBase += encodeURIComponent(args.WORDS);
      var tempThis = this;
      var translatePromise = fetchWithTimeout(urlBase, {}, serverTimeoutMs).then(function (response) {
        return response.text();
      }).then(function (responseText) {
        var translated = JSON.parse(responseText).result;
        tempThis._translateResult = translated;
        // Cache what we just translated so we don't keep making the
        // same call over and over.
        tempThis._lastTextTranslated = args.WORDS;
        tempThis._lastLangTranslated = args.LANGUAGE;
        return translated;
      })["catch"](function (err) {
        log.warn("error fetching translate result! ".concat(err));
        return args.WORDS;
      });
      return translatePromise;
    }
  }], [{
    key: "STATE_KEY",
    get: function get() {
      return 'Scratch.translate';
    }
  }]);
}();
module.exports = Scratch3TranslateBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_video_sensing/index.js":
/*!************************************************************!*\
  !*** ../vm/src/extensions/scratch3_video_sensing/index.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Runtime = __webpack_require__(/*! ../../engine/runtime */ "../vm/src/engine/runtime.js");
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Clone = __webpack_require__(/*! ../../util/clone */ "../vm/src/util/clone.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var Video = __webpack_require__(/*! ../../io/video */ "../vm/src/io/video.js");
var VideoMotion = __webpack_require__(/*! ./library */ "../vm/src/extensions/scratch3_video_sensing/library.js");

/**
 * Icon svg to be displayed in the blocks category menu, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var menuIconURI = 'data:image/svg+xml;base64,';

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var blockIconURI = 'data:image/svg+xml;base64,';

/**
 * Sensor attribute video sensor block should report.
 * @readonly
 * @enum {string}
 */
var SensingAttribute = {
  /** The amount of motion. */
  MOTION: 'motion',
  /** The direction of the motion. */
  DIRECTION: 'direction'
};

/**
 * Subject video sensor block should report for.
 * @readonly
 * @enum {string}
 */
var SensingSubject = {
  /** The sensor traits of the whole stage. */
  STAGE: 'Stage',
  /** The senosr traits of the area overlapped by this sprite. */
  SPRITE: 'this sprite'
};

/**
 * States the video sensing activity can be set to.
 * @readonly
 * @enum {string}
 */
var VideoState = {
  /** Video turned off. */
  OFF: 'off',
  /** Video turned on with default y axis mirroring. */
  ON: 'on',
  /** Video turned on without default y axis mirroring. */
  ON_FLIPPED: 'on-flipped'
};

/**
 * Class for the motion-related blocks in Scratch 3.0
 * @param {Runtime} runtime - the runtime instantiating this block package.
 * @constructor
 */
var Scratch3VideoSensingBlocks = /*#__PURE__*/function () {
  function Scratch3VideoSensingBlocks(runtime) {
    _classCallCheck(this, Scratch3VideoSensingBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;

    /**
     * The motion detection algoritm used to power the motion amount and
     * direction values.
     * @type {VideoMotion}
     */
    this.detect = new VideoMotion();

    /**
     * The last millisecond epoch timestamp that the video stream was
     * analyzed.
     * @type {number}
     */
    this._lastUpdate = null;

    /**
     * A flag to determine if this extension has been installed in a project.
     * It is set to false the first time getInfo is run.
     * @type {boolean}
     */
    this.firstInstall = true;
    if (this.runtime.ioDevices) {
      // Configure the video device with values from globally stored locations.
      this.runtime.on(Runtime.PROJECT_LOADED, this.updateVideoDisplay.bind(this));

      // Clear target motion state values when the project starts.
      this.runtime.on(Runtime.PROJECT_RUN_START, this.reset.bind(this));

      // Kick off looping the analysis logic.
      this._loop();
    }
  }

  /**
   * After analyzing a frame the amount of milliseconds until another frame
   * is analyzed.
   * @type {number}
   */
  return _createClass(Scratch3VideoSensingBlocks, [{
    key: "globalVideoTransparency",
    get:
    /**
     * The transparency setting of the video preview stored in a value
     * accessible by any object connected to the virtual machine.
     * @type {number}
     */
    function get() {
      var stage = this.runtime.getTargetForStage();
      if (stage) {
        return stage.videoTransparency;
      }
      return 50;
    },
    set: function set(transparency) {
      var stage = this.runtime.getTargetForStage();
      if (stage) {
        stage.videoTransparency = transparency;
      }
    }

    /**
     * The video state of the video preview stored in a value accessible by any
     * object connected to the virtual machine.
     * @type {number}
     */
  }, {
    key: "globalVideoState",
    get: function get() {
      var stage = this.runtime.getTargetForStage();
      if (stage) {
        return stage.videoState;
      }
      // Though the default value for the stage is normally 'on', we need to default
      // to 'off' here to prevent the video device from briefly activating
      // while waiting for stage targets to be installed that say it should be off
      return VideoState.OFF;
    },
    set: function set(state) {
      var stage = this.runtime.getTargetForStage();
      if (stage) {
        stage.videoState = state;
      }
    }

    /**
     * Get the latest values for video transparency and state,
     * and set the video device to use them.
     */
  }, {
    key: "updateVideoDisplay",
    value: function updateVideoDisplay() {
      this.setVideoTransparency({
        TRANSPARENCY: this.globalVideoTransparency
      });
      this.videoToggle({
        VIDEO_STATE: this.globalVideoState
      });
    }

    /**
     * Reset the extension's data motion detection data. This will clear out
     * for example old frames, so the first analyzed frame will not be compared
     * against a frame from before reset was called.
     */
  }, {
    key: "reset",
    value: function reset() {
      this.detect.reset();
      var targets = this.runtime.targets;
      for (var i = 0; i < targets.length; i++) {
        var state = targets[i].getCustomState(Scratch3VideoSensingBlocks.STATE_KEY);
        if (state) {
          state.motionAmount = 0;
          state.motionDirection = 0;
        }
      }
    }

    /**
     * Occasionally step a loop to sample the video, stamp it to the preview
     * skin, and add a TypedArray copy of the canvas's pixel data.
     * @private
     */
  }, {
    key: "_loop",
    value: function _loop() {
      var loopTime = Math.max(this.runtime.currentStepTime, Scratch3VideoSensingBlocks.INTERVAL);
      this._loopInterval = setTimeout(this._loop.bind(this), loopTime);

      // Add frame to detector
      var time = Date.now();
      if (this._lastUpdate === null) {
        this._lastUpdate = time;
      }
      var offset = time - this._lastUpdate;
      if (offset > Scratch3VideoSensingBlocks.INTERVAL) {
        var frame = this.runtime.ioDevices.video.getFrame({
          format: Video.FORMAT_IMAGE_DATA,
          dimensions: Scratch3VideoSensingBlocks.DIMENSIONS
        });
        if (frame) {
          this._lastUpdate = time;
          this.detect.addFrame(frame.data);
        }
      }
    }

    /**
     * Stop the video sampling loop. Only used for testing.
     */
  }, {
    key: "_stopLoop",
    value: function _stopLoop() {
      clearTimeout(this._loopInterval);
    }

    /**
     * Create data for a menu in scratch-blocks format, consisting of an array
     * of objects with text and value properties. The text is a translated
     * string, and the value is one-indexed.
     * @param {object[]} info - An array of info objects each having a name
     *   property.
     * @return {array} - An array of objects with text and value properties.
     * @private
     */
  }, {
    key: "_buildMenu",
    value: function _buildMenu(info) {
      return info.map(function (entry, index) {
        var obj = {};
        obj.text = entry.name;
        obj.value = entry.value || String(index + 1);
        return obj;
      });
    }

    /**
     * @param {Target} target - collect motion state for this target.
     * @returns {MotionState} the mutable motion state associated with that
     *   target. This will be created if necessary.
     * @private
     */
  }, {
    key: "_getMotionState",
    value: function _getMotionState(target) {
      var motionState = target.getCustomState(Scratch3VideoSensingBlocks.STATE_KEY);
      if (!motionState) {
        motionState = Clone.simple(Scratch3VideoSensingBlocks.DEFAULT_MOTION_STATE);
        target.setCustomState(Scratch3VideoSensingBlocks.STATE_KEY, motionState);
      }
      return motionState;
    }
  }, {
    key: "ATTRIBUTE_INFO",
    get:
    /**
     * An array of choices of whether a reporter should return the frame's
     * motion amount or direction.
     * @type {object[]}
     * @param {string} name - the translatable name to display in sensor
     *   attribute menu
     * @param {string} value - the serializable value of the attribute
     */
    function get() {
      return [{
        name: formatMessage({
          id: 'videoSensing.motion',
          "default": 'motion',
          description: 'Attribute for the "video [ATTRIBUTE] on [SUBJECT]" block'
        }),
        value: SensingAttribute.MOTION
      }, {
        name: formatMessage({
          id: 'videoSensing.direction',
          "default": 'direction',
          description: 'Attribute for the "video [ATTRIBUTE] on [SUBJECT]" block'
        }),
        value: SensingAttribute.DIRECTION
      }];
    }
  }, {
    key: "SUBJECT_INFO",
    get:
    /**
     * An array of info about the subject choices.
     * @type {object[]}
     * @param {string} name - the translatable name to display in the subject menu
     * @param {string} value - the serializable value of the subject
     */
    function get() {
      return [{
        name: formatMessage({
          id: 'videoSensing.sprite',
          "default": 'sprite',
          description: 'Subject for the "video [ATTRIBUTE] on [SUBJECT]" block'
        }),
        value: SensingSubject.SPRITE
      }, {
        name: formatMessage({
          id: 'videoSensing.stage',
          "default": 'stage',
          description: 'Subject for the "video [ATTRIBUTE] on [SUBJECT]" block'
        }),
        value: SensingSubject.STAGE
      }];
    }

    /**
     * States the video sensing activity can be set to.
     * @readonly
     * @enum {string}
     */
  }, {
    key: "VIDEO_STATE_INFO",
    get:
    /**
     * An array of info on video state options for the "turn video [STATE]" block.
     * @type {object[]}
     * @param {string} name - the translatable name to display in the video state menu
     * @param {string} value - the serializable value stored in the block
     */
    function get() {
      return [{
        name: formatMessage({
          id: 'videoSensing.off',
          "default": 'off',
          description: 'Option for the "turn video [STATE]" block'
        }),
        value: VideoState.OFF
      }, {
        name: formatMessage({
          id: 'videoSensing.on',
          "default": 'on',
          description: 'Option for the "turn video [STATE]" block'
        }),
        value: VideoState.ON
      }, {
        name: formatMessage({
          id: 'videoSensing.onFlipped',
          "default": 'on flipped',
          description: 'Option for the "turn video [STATE]" block that causes the video to be flipped' + ' horizontally (reversed as in a mirror)'
        }),
        value: VideoState.ON_FLIPPED
      }];
    }

    /**
     * @returns {object} metadata for this extension and its blocks.
     */
  }, {
    key: "getInfo",
    value: function getInfo() {
      // Set the video display properties to defaults the first time
      // getInfo is run. This turns on the video device when it is
      // first added to a project, and is overwritten by a PROJECT_LOADED
      // event listener that later calls updateVideoDisplay
      if (this.firstInstall) {
        this.globalVideoState = VideoState.ON;
        this.globalVideoTransparency = 50;
        this.updateVideoDisplay();
        this.firstInstall = false;
      }

      // Return extension definition
      return {
        id: 'videoSensing',
        name: formatMessage({
          id: 'videoSensing.categoryName',
          "default": 'Video Sensing',
          description: 'Label for the video sensing extension category'
        }),
        blockIconURI: blockIconURI,
        menuIconURI: menuIconURI,
        blocks: [{
          // @todo this hat needs to be set itself to restart existing
          // threads like Scratch 2's behaviour.
          opcode: 'whenMotionGreaterThan',
          text: formatMessage({
            id: 'videoSensing.whenMotionGreaterThan',
            "default": 'when video motion > [REFERENCE]',
            description: 'Event that triggers when the amount of motion is greater than [REFERENCE]'
          }),
          blockType: BlockType.HAT,
          arguments: {
            REFERENCE: {
              type: ArgumentType.NUMBER,
              defaultValue: 10
            }
          }
        }, {
          opcode: 'videoOn',
          blockType: BlockType.REPORTER,
          text: formatMessage({
            id: 'videoSensing.videoOn',
            "default": 'video [ATTRIBUTE] on [SUBJECT]',
            description: 'Reporter that returns the amount of [ATTRIBUTE] for the selected [SUBJECT]'
          }),
          arguments: {
            ATTRIBUTE: {
              type: ArgumentType.NUMBER,
              menu: 'ATTRIBUTE',
              defaultValue: SensingAttribute.MOTION
            },
            SUBJECT: {
              type: ArgumentType.NUMBER,
              menu: 'SUBJECT',
              defaultValue: SensingSubject.SPRITE
            }
          }
        }, {
          opcode: 'videoToggle',
          text: formatMessage({
            id: 'videoSensing.videoToggle',
            "default": 'turn video [VIDEO_STATE]',
            description: 'Controls display of the video preview layer'
          }),
          arguments: {
            VIDEO_STATE: {
              type: ArgumentType.NUMBER,
              menu: 'VIDEO_STATE',
              defaultValue: VideoState.ON
            }
          }
        }, {
          opcode: 'setVideoTransparency',
          text: formatMessage({
            id: 'videoSensing.setVideoTransparency',
            "default": 'set video transparency to [TRANSPARENCY]',
            description: 'Controls transparency of the video preview layer'
          }),
          arguments: {
            TRANSPARENCY: {
              type: ArgumentType.NUMBER,
              defaultValue: 50
            }
          }
        }],
        menus: {
          ATTRIBUTE: {
            acceptReporters: true,
            items: this._buildMenu(this.ATTRIBUTE_INFO)
          },
          SUBJECT: {
            acceptReporters: true,
            items: this._buildMenu(this.SUBJECT_INFO)
          },
          VIDEO_STATE: {
            acceptReporters: true,
            items: this._buildMenu(this.VIDEO_STATE_INFO)
          }
        }
      };
    }

    /**
     * Analyze a part of the frame that a target overlaps.
     * @param {Target} target - a target to determine where to analyze
     * @returns {MotionState} the motion state for the given target
     */
  }, {
    key: "_analyzeLocalMotion",
    value: function _analyzeLocalMotion(target) {
      var drawable = this.runtime.renderer._allDrawables[target.drawableID];
      var state = this._getMotionState(target);
      this.detect.getLocalMotion(drawable, state);
      return state;
    }

    /**
     * A scratch reporter block handle that analyzes the last two frames and
     * depending on the arguments, returns the motion or direction for the
     * whole stage or just the target sprite.
     * @param {object} args - the block arguments
     * @param {BlockUtility} util - the block utility
     * @returns {number} the motion amount or direction of the stage or sprite
     */
  }, {
    key: "videoOn",
    value: function videoOn(args, util) {
      if (this.runtime.privacyRestrictionsActive) {
        return -1;
      }
      this.detect.analyzeFrame();
      var state = this.detect;
      if (args.SUBJECT === SensingSubject.SPRITE) {
        state = this._analyzeLocalMotion(util.target);
      }
      if (args.ATTRIBUTE === SensingAttribute.MOTION) {
        return state.motionAmount;
      }
      return state.motionDirection;
    }

    /**
     * A scratch hat block edge handle that analyzes the last two frames where
     * the target sprite overlaps and if it has more motion than the given
     * reference value.
     * @param {object} args - the block arguments
     * @param {BlockUtility} util - the block utility
     * @returns {boolean} true if the sprite overlaps more motion than the
     *   reference
     */
  }, {
    key: "whenMotionGreaterThan",
    value: function whenMotionGreaterThan(args, util) {
      if (this.runtime.privacyRestrictionsActive) {
        return false;
      }
      this.detect.analyzeFrame();
      var state = this._analyzeLocalMotion(util.target);
      return state.motionAmount > Number(args.REFERENCE);
    }

    /**
     * A scratch command block handle that configures the video state from
     * passed arguments.
     * @param {object} args - the block arguments
     * @param {VideoState} args.VIDEO_STATE - the video state to set the device to
     */
  }, {
    key: "videoToggle",
    value: function videoToggle(args) {
      var state = args.VIDEO_STATE;
      this.globalVideoState = state;
      if (state === VideoState.OFF) {
        this.runtime.ioDevices.video.disableVideo();
      } else {
        this.runtime.ioDevices.video.enableVideo();
        // Mirror if state is ON. Do not mirror if state is ON_FLIPPED.
        this.runtime.ioDevices.video.mirror = state === VideoState.ON;
      }
    }

    /**
     * A scratch command block handle that configures the video preview's
     * transparency from passed arguments.
     * @param {object} args - the block arguments
     * @param {number} args.TRANSPARENCY - the transparency to set the video
     *   preview to
     */
  }, {
    key: "setVideoTransparency",
    value: function setVideoTransparency(args) {
      var transparency = Cast.toNumber(args.TRANSPARENCY);
      this.globalVideoTransparency = transparency;
      this.runtime.ioDevices.video.setPreviewGhost(transparency);
    }
  }], [{
    key: "INTERVAL",
    get: function get() {
      return 33;
    }

    /**
     * Dimensions the video stream is analyzed at after its rendered to the
     * sample canvas.
     * @type {Array.<number>}
     */
  }, {
    key: "DIMENSIONS",
    get: function get() {
      return [480, 360];
    }

    /**
     * The key to load & store a target's motion-related state.
     * @type {string}
     */
  }, {
    key: "STATE_KEY",
    get: function get() {
      return 'Scratch.videoSensing';
    }

    /**
     * The default motion-related state, to be used when a target has no existing motion state.
     * @type {MotionState}
     */
  }, {
    key: "DEFAULT_MOTION_STATE",
    get: function get() {
      return {
        motionFrameNumber: 0,
        motionAmount: 0,
        motionDirection: 0
      };
    }
  }, {
    key: "SensingAttribute",
    get: function get() {
      return SensingAttribute;
    }
  }, {
    key: "SensingSubject",
    get: function get() {
      return SensingSubject;
    }
  }, {
    key: "VideoState",
    get: function get() {
      return VideoState;
    }
  }]);
}();
module.exports = Scratch3VideoSensingBlocks;

/***/ }),

/***/ "../vm/src/extensions/scratch3_video_sensing/library.js":
/*!**************************************************************!*\
  !*** ../vm/src/extensions/scratch3_video_sensing/library.js ***!
  \**************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @file library.js
 *
 * Tony Hwang and John Maloney, January 2011
 * Michael "Z" Goddard, March 2018
 *
 * Video motion sensing primitives.
 */

var _require = __webpack_require__(/*! ./math */ "../vm/src/extensions/scratch3_video_sensing/math.js"),
  motionVector = _require.motionVector,
  scratchAtan2 = _require.scratchAtan2;

/**
 * The width of the intended resolution to analyze for motion.
 * @type {number}
 */
var WIDTH = 480;

/**
 * The height of the intended resolution to analyze for motion.
 * @type {number}
 */
var HEIGHT = 360;

/**
 * A constant value to scale the magnitude of the x and y components called u
 * and v. This creates the motionAmount value.
 *
 * Old note: chosen empirically to give a range of roughly 0-100
 *
 * @type {number}
 */
var AMOUNT_SCALE = 100;

/**
 * A constant value to scale the magnitude of the x and y components called u
 * and v in the local motion derivative. This creates the motionAmount value on
 * a target's motion state.
 *
 * Old note: note 2e-4 * activePixelNum is an experimentally tuned threshold
 * for my logitech Pro 9000 webcam - TTH
 *
 * @type {number}
 */
var LOCAL_AMOUNT_SCALE = AMOUNT_SCALE * 2e-4;

/**
 * The motion amount must be higher than the THRESHOLD to calculate a new
 * direction value.
 * @type {number}
 */
var THRESHOLD = 10;

/**
 * The size of the radius of the window of summarized values when considering
 * the motion inside the full resolution of the sample.
 * @type {number}
 */
var WINSIZE = 8;

/**
 * A ceiling for the motionAmount stored to a local target's motion state. The
 * motionAmount is not allowed to be larger than LOCAL_MAX_AMOUNT.
 * @type {number}
 */
var LOCAL_MAX_AMOUNT = 100;

/**
 * The motion amount for a target's local motion must be higher than the
 * LOCAL_THRESHOLD to calculate a new direction value.
 * @type {number}
 */
var LOCAL_THRESHOLD = THRESHOLD / 3;

/**
 * Store the necessary image pixel data to compares frames of a video and
 * detect an amount and direction of motion in the full sample or in a
 * specified area.
 * @constructor
 */
var VideoMotion = /*#__PURE__*/function () {
  function VideoMotion() {
    _classCallCheck(this, VideoMotion);
    /**
     * The number of frames that have been added from a source.
     * @type {number}
     */
    this.frameNumber = 0;

    /**
     * The frameNumber last analyzed.
     * @type {number}
     */
    this.lastAnalyzedFrame = 0;

    /**
     * The amount of motion detected in the current frame.
     * @type {number}
     */
    this.motionAmount = 0;

    /**
     * The direction the motion detected in the frame is general moving in.
     * @type {number}
     */
    this.motionDirection = 0;

    /**
     * A copy of the current frame's pixel values. A index of the array is
     * represented in RGBA. The lowest byte is red. The next is green. The
     * next is blue. And the last is the alpha value of that pixel.
     * @type {Uint32Array}
     */
    this.curr = null;

    /**
     * A copy of the last frame's pixel values.
     * @type {Uint32Array}
     */
    this.prev = null;

    /**
     * A buffer for holding one component of a pixel's full value twice.
     * One for the current value. And one for the last value.
     * @type {number}
     */
    this._arrays = new ArrayBuffer(WIDTH * HEIGHT * 2 * 1);

    /**
     * A clamped uint8 view of _arrays. One component of each index of the
     * curr member is copied into this array.
     * @type {number}
     */
    this._curr = new Uint8ClampedArray(this._arrays, WIDTH * HEIGHT * 0 * 1, WIDTH * HEIGHT);

    /**
     * A clamped uint8 view of _arrays. One component of each index of the
     * prev member is copied into this array.
     * @type {number}
     */
    this._prev = new Uint8ClampedArray(this._arrays, WIDTH * HEIGHT * 1 * 1, WIDTH * HEIGHT);
  }

  /**
   * Reset internal state so future frame analysis does not consider values
   * from before this method was called.
   */
  return _createClass(VideoMotion, [{
    key: "reset",
    value: function reset() {
      this.frameNumber = 0;
      this.lastAnalyzedFrame = 0;
      this.motionAmount = this.motionDirection = 0;
      this.prev = this.curr = null;
    }

    /**
     * Add a frame to be next analyzed. The passed array represent a pixel with
     * each index in the RGBA format.
     * @param {Uint32Array} source - a source frame of pixels to copy
     */
  }, {
    key: "addFrame",
    value: function addFrame(source) {
      this.frameNumber++;

      // Swap curr to prev.
      this.prev = this.curr;
      // Create a clone of the array so any modifications made to the source
      // array do not affect the work done in here.
      this.curr = new Uint32Array(source.buffer.slice(0));

      // Swap _prev and _curr. Copy one of the color components of the new
      // array into _curr overwriting what was the old _prev data.
      var _tmp = this._prev;
      this._prev = this._curr;
      this._curr = _tmp;
      for (var i = 0; i < this.curr.length; i++) {
        this._curr[i] = this.curr[i] & 0xff;
      }
    }

    /**
     * Analyze the current frame against the previous frame determining the
     * amount of motion and direction of the motion.
     */
  }, {
    key: "analyzeFrame",
    value: function analyzeFrame() {
      if (!this.curr || !this.prev) {
        this.motionAmount = this.motionDirection = -1;
        // Don't have two frames to analyze yet
        return;
      }

      // Return early if new data has not been received.
      if (this.lastAnalyzedFrame === this.frameNumber) {
        return;
      }
      this.lastAnalyzedFrame = this.frameNumber;
      var curr = this._curr,
        prev = this._prev;
      var winStep = WINSIZE * 2 + 1;
      var wmax = WIDTH - WINSIZE - 1;
      var hmax = HEIGHT - WINSIZE - 1;

      // Accumulate 2d motion vectors from groups of pixels and average it
      // later.
      var uu = 0;
      var vv = 0;
      var n = 0;

      // Iterate over groups of cells building up the components to determine
      // a motion vector for each cell instead of the whole frame to avoid
      // integer overflows.
      for (var i = WINSIZE + 1; i < hmax; i += winStep) {
        for (var j = WINSIZE + 1; j < wmax; j += winStep) {
          var A2 = 0;
          var A1B2 = 0;
          var B1 = 0;
          var C1 = 0;
          var C2 = 0;

          // This is a performance critical math region.
          var address = (i - WINSIZE) * WIDTH + j - WINSIZE;
          var nextAddress = address + winStep;
          var maxAddress = (i + WINSIZE) * WIDTH + j + WINSIZE;
          for (; address <= maxAddress; address += WIDTH - winStep, nextAddress += WIDTH) {
            for (; address <= nextAddress; address += 1) {
              // The difference in color between the last frame and
              // the current frame.
              var gradT = prev[address] - curr[address];
              // The difference between the pixel to the left and the
              // pixel to the right.
              var gradX = curr[address - 1] - curr[address + 1];
              // The difference between the pixel above and the pixel
              // below.
              var gradY = curr[address - WIDTH] - curr[address + WIDTH];

              // Add the combined values of this pixel to previously
              // considered pixels.
              A2 += gradX * gradX;
              A1B2 += gradX * gradY;
              B1 += gradY * gradY;
              C2 += gradX * gradT;
              C1 += gradY * gradT;
            }
          }

          // Use the accumalated values from the for loop to determine a
          // motion direction.
          var _motionVector = motionVector(A2, A1B2, B1, C2, C1),
            u = _motionVector.u,
            v = _motionVector.v;

          // If u and v are within negative winStep to positive winStep,
          // add them to a sum that will later be averaged.
          if (-winStep < u && u < winStep && -winStep < v && v < winStep) {
            uu += u;
            vv += v;
            n++;
          }
        }
      }

      // Average the summed vector values of all of the motion groups.
      uu /= n;
      vv /= n;

      // Scale the magnitude of the averaged UV vector.
      this.motionAmount = Math.round(AMOUNT_SCALE * Math.hypot(uu, vv));
      if (this.motionAmount > THRESHOLD) {
        // Scratch direction
        this.motionDirection = scratchAtan2(vv, uu);
      }
    }

    /**
     * Build motion amount and direction values based on stored current and
     * previous frame that overlaps a given drawable.
     * @param {Drawable} drawable - touchable and bounded drawable to build motion for
     * @param {MotionState} state - state to store built values to
     */
  }, {
    key: "getLocalMotion",
    value: function getLocalMotion(drawable, state) {
      if (!this.curr || !this.prev) {
        state.motionAmount = state.motionDirection = -1;
        // Don't have two frames to analyze yet
        return;
      }

      // Skip if the current frame has already been considered for this state.
      if (state.motionFrameNumber !== this.frameNumber) {
        var prev = this._prev,
          curr = this._curr;

        // The public APIs for Renderer#isTouching manage keeping the matrix and
        // silhouette up-to-date, which is needed for drawable#isTouching to work (used below)
        drawable.updateCPURenderAttributes();

        // Restrict the region the amount and direction are built from to
        // the area of the current frame overlapped by the given drawable's
        // bounding box.
        var boundingRect = drawable.getFastBounds();
        // Transform the bounding box from scratch space to a space from 0,
        // 0 to WIDTH, HEIGHT.
        var xmin = Math.max(Math.floor(boundingRect.left + WIDTH / 2), 1);
        var xmax = Math.min(Math.floor(boundingRect.right + WIDTH / 2), WIDTH - 1);
        var ymin = Math.max(Math.floor(HEIGHT / 2 - boundingRect.top), 1);
        var ymax = Math.min(Math.floor(HEIGHT / 2 - boundingRect.bottom), HEIGHT - 1);
        var A2 = 0;
        var A1B2 = 0;
        var B1 = 0;
        var C1 = 0;
        var C2 = 0;
        var scaleFactor = 0;
        var position = [0, 0, 0];

        // This is a performance critical math region.
        for (var i = ymin; i < ymax; i++) {
          for (var j = xmin; j < xmax; j++) {
            // i and j are in a coordinate planning ranging from 0 to
            // HEIGHT and 0 to WIDTH. Transform that into Scratch's
            // range of HEIGHT / 2 to -HEIGHT / 2 and -WIDTH / 2 to
            // WIDTH / 2;
            position[0] = j - WIDTH / 2;
            position[1] = HEIGHT / 2 - i;
            // Consider only pixels in the drawable that can touch the
            // edge or other drawables. Empty space in the current skin
            // is skipped.
            if (drawable.isTouching(position)) {
              var address = i * WIDTH + j;
              // The difference in color between the last frame and
              // the current frame.
              var gradT = prev[address] - curr[address];
              // The difference between the pixel to the left and the
              // pixel to the right.
              var gradX = curr[address - 1] - curr[address + 1];
              // The difference between the pixel above and the pixel
              // below.
              var gradY = curr[address - WIDTH] - curr[address + WIDTH];

              // Add the combined values of this pixel to previously
              // considered pixels.
              A2 += gradX * gradX;
              A1B2 += gradX * gradY;
              B1 += gradY * gradY;
              C2 += gradX * gradT;
              C1 += gradY * gradT;
              scaleFactor++;
            }
          }
        }

        // Use the accumalated values from the for loop to determine a
        // motion direction.
        var _motionVector2 = motionVector(A2, A1B2, B1, C2, C1),
          u = _motionVector2.u,
          v = _motionVector2.v;
        var activePixelNum = 0;
        if (scaleFactor) {
          // Store the area of the sprite in pixels
          activePixelNum = scaleFactor;
          scaleFactor /= 2 * WINSIZE * 2 * WINSIZE;
          u = u / scaleFactor;
          v = v / scaleFactor;
        }

        // Scale the magnitude of the averaged UV vector and the number of
        // overlapping drawable pixels.
        state.motionAmount = Math.round(LOCAL_AMOUNT_SCALE * activePixelNum * Math.hypot(u, v));
        if (state.motionAmount > LOCAL_MAX_AMOUNT) {
          // Clip all magnitudes greater than 100.
          state.motionAmount = LOCAL_MAX_AMOUNT;
        }
        if (state.motionAmount > LOCAL_THRESHOLD) {
          // Scratch direction.
          state.motionDirection = scratchAtan2(v, u);
        }

        // Skip future calls on this state until a new frame is added.
        state.motionFrameNumber = this.frameNumber;
      }
    }
  }]);
}();
module.exports = VideoMotion;

/***/ }),

/***/ "../vm/src/extensions/scratch3_video_sensing/math.js":
/*!***********************************************************!*\
  !*** ../vm/src/extensions/scratch3_video_sensing/math.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * A constant value helping to transform a value in radians to degrees.
 * @type {number}
 */
var TO_DEGREE = 180 / Math.PI;

/**
 * A object reused to save on memory allocation returning u and v vector from
 * motionVector.
 * @type {UV}
 */
var _motionVectorOut = {
  u: 0,
  v: 0
};

/**
 * Determine a motion vector combinations of the color component difference on
 * the x axis, y axis, and temporal axis.
 * @param {number} A2 - a sum of x axis squared
 * @param {number} A1B2 - a sum of x axis times y axis
 * @param {number} B1 - a sum of y axis squared
 * @param {number} C2 - a sum of x axis times temporal axis
 * @param {number} C1 - a sum of y axis times temporal axis
 * @param {UV} out - optional object to store return UV info in
 * @returns {UV} a uv vector representing the motion for the given input
 */
var motionVector = function motionVector(A2, A1B2, B1, C2, C1) {
  var out = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : _motionVectorOut;
  // Compare sums of X * Y and sums of X squared and Y squared.
  var delta = A1B2 * A1B2 - A2 * B1;
  if (delta) {
    // System is not singular - solving by Kramer method.
    var deltaX = -(C1 * A1B2 - C2 * B1);
    var deltaY = -(A1B2 * C2 - A2 * C1);
    var Idelta = 8 / delta;
    out.u = deltaX * Idelta;
    out.v = deltaY * Idelta;
  } else {
    // Singular system - find optical flow in gradient direction.
    var Norm = (A1B2 + A2) * (A1B2 + A2) + (B1 + A1B2) * (B1 + A1B2);
    if (Norm) {
      var IGradNorm = 8 / Norm;
      var temp = -(C1 + C2) * IGradNorm;
      out.u = (A1B2 + A2) * temp;
      out.v = (B1 + A1B2) * temp;
    } else {
      out.u = 0;
      out.v = 0;
    }
  }
  return out;
};

/**
 * Translate an angle in degrees with the range -180 to 180 rotated to
 * Scratch's reference angle.
 * @param {number} degrees - angle in range -180 to 180
 * @returns {number} angle from Scratch's reference angle
 */
var scratchDegrees = function scratchDegrees(degrees) {
  return (degrees + 270) % 360 - 180;
};

/**
 * Get the angle of the y and x component of a 2d vector in degrees in
 * Scratch's coordinate plane.
 * @param {number} y - the y component of a 2d vector
 * @param {number} x - the x component of a 2d vector
 * @returns {number} angle in degrees in Scratch's coordinate plane
 */
var scratchAtan2 = function scratchAtan2(y, x) {
  return scratchDegrees(Math.atan2(y, x) * TO_DEGREE);
};
module.exports = {
  motionVector: motionVector,
  scratchDegrees: scratchDegrees,
  scratchAtan2: scratchAtan2
};

/***/ }),

/***/ "../vm/src/extensions/scratch3_wedo2/index.js":
/*!****************************************************!*\
  !*** ../vm/src/extensions/scratch3_wedo2/index.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var color = __webpack_require__(/*! ../../util/color */ "../vm/src/util/color.js");
var BLE = __webpack_require__(/*! ../../io/ble */ "../vm/src/io/ble.js");
var Base64Util = __webpack_require__(/*! ../../util/base64-util */ "../vm/src/util/base64-util.js");
var MathUtil = __webpack_require__(/*! ../../util/math-util */ "../vm/src/util/math-util.js");
var RateLimiter = __webpack_require__(/*! ../../util/rateLimiter.js */ "../vm/src/util/rateLimiter.js");
var log = __webpack_require__(/*! ../../util/log */ "../vm/src/util/log.js");

/**
 * Icon svg to be displayed at the left edge of each extension block, encoded as a data URI.
 * @type {string}
 */
// eslint-disable-next-line max-len
var iconURI = 'data:image/png;base64,';

/**
 * A list of WeDo 2.0 BLE service UUIDs.
 * @enum
 */
var BLEService = {
  DEVICE_SERVICE: '00001523-1212-efde-1523-785feabcd123',
  IO_SERVICE: '00004f0e-1212-efde-1523-785feabcd123'
};

/**
 * A list of WeDo 2.0 BLE characteristic UUIDs.
 *
 * Characteristics on DEVICE_SERVICE:
 * - ATTACHED_IO
 *
 * Characteristics on IO_SERVICE:
 * - INPUT_VALUES
 * - INPUT_COMMAND
 * - OUTPUT_COMMAND
 *
 * @enum
 */
var BLECharacteristic = {
  ATTACHED_IO: '00001527-1212-efde-1523-785feabcd123',
  LOW_VOLTAGE_ALERT: '00001528-1212-efde-1523-785feabcd123',
  INPUT_VALUES: '00001560-1212-efde-1523-785feabcd123',
  INPUT_COMMAND: '00001563-1212-efde-1523-785feabcd123',
  OUTPUT_COMMAND: '00001565-1212-efde-1523-785feabcd123'
};

/**
 * A time interval to wait (in milliseconds) in between battery check calls.
 * @type {number}
 */
var BLEBatteryCheckInterval = 5000;

/**
 * A time interval to wait (in milliseconds) while a block that sends a BLE message is running.
 * @type {number}
 */
var BLESendInterval = 100;

/**
 * A maximum number of BLE message sends per second, to be enforced by the rate limiter.
 * @type {number}
 */
var BLESendRateMax = 20;

/**
 * Enum for WeDo 2.0 sensor and output types.
 * @readonly
 * @enum {number}
 */
var WeDo2Device = {
  MOTOR: 1,
  PIEZO: 22,
  LED: 23,
  TILT: 34,
  DISTANCE: 35
};

/**
 * Enum for connection/port ids assigned to internal WeDo 2.0 output devices.
 * @readonly
 * @enum {number}
 */
// TODO: Check for these more accurately at startup?
var WeDo2ConnectID = {
  LED: 6,
  PIEZO: 5
};

/**
 * Enum for ids for various output commands on the WeDo 2.0.
 * @readonly
 * @enum {number}
 */
var WeDo2Command = {
  MOTOR_POWER: 1,
  PLAY_TONE: 2,
  STOP_TONE: 3,
  WRITE_RGB: 4,
  SET_VOLUME: 255
};

/**
 * Enum for modes for input sensors on the WeDo 2.0.
 * @enum {number}
 */
var WeDo2Mode = {
  TILT: 0,
  // angle
  DISTANCE: 0,
  // detect
  LED: 1 // RGB
};

/**
 * Enum for units for input sensors on the WeDo 2.0.
 *
 * 0 = raw
 * 1 = percent
 *
 * @enum {number}
 */
var WeDo2Unit = {
  TILT: 0,
  DISTANCE: 1,
  LED: 0
};

/**
 * Manage power, direction, and timers for one WeDo 2.0 motor.
 */
var WeDo2Motor = /*#__PURE__*/function () {
  /**
   * Construct a WeDo 2.0 Motor instance.
   * @param {WeDo2} parent - the WeDo 2.0 peripheral which owns this motor.
   * @param {int} index - the zero-based index of this motor on its parent peripheral.
   */
  function WeDo2Motor(parent, index) {
    _classCallCheck(this, WeDo2Motor);
    /**
     * The WeDo 2.0 peripheral which owns this motor.
     * @type {WeDo2}
     * @private
     */
    this._parent = parent;

    /**
     * The zero-based index of this motor on its parent peripheral.
     * @type {int}
     * @private
     */
    this._index = index;

    /**
     * This motor's current direction: 1 for "this way" or -1 for "that way"
     * @type {number}
     * @private
     */
    this._direction = 1;

    /**
     * This motor's current power level, in the range [0,100].
     * @type {number}
     * @private
     */
    this._power = 100;

    /**
     * Is this motor currently moving?
     * @type {boolean}
     * @private
     */
    this._isOn = false;

    /**
     * If the motor has been turned on or is actively braking for a specific duration, this is the timeout ID for
     * the end-of-action handler. Cancel this when changing plans.
     * @type {Object}
     * @private
     */
    this._pendingTimeoutId = null;

    /**
     * The starting time for the pending timeout.
     * @type {Object}
     * @private
     */
    this._pendingTimeoutStartTime = null;

    /**
     * The delay/duration of the pending timeout.
     * @type {Object}
     * @private
     */
    this._pendingTimeoutDelay = null;
    this.startBraking = this.startBraking.bind(this);
    this.turnOff = this.turnOff.bind(this);
  }

  /**
   * @return {number} - the duration of active braking after a call to startBraking(). Afterward, turn the motor off.
   * @constructor
   */
  return _createClass(WeDo2Motor, [{
    key: "direction",
    get:
    /**
     * @return {int} - this motor's current direction: 1 for "this way" or -1 for "that way"
     */
    function get() {
      return this._direction;
    }

    /**
     * @param {int} value - this motor's new direction: 1 for "this way" or -1 for "that way"
     */,
    set: function set(value) {
      if (value < 0) {
        this._direction = -1;
      } else {
        this._direction = 1;
      }
    }

    /**
     * @return {int} - this motor's current power level, in the range [0,100].
     */
  }, {
    key: "power",
    get: function get() {
      return this._power;
    }

    /**
     * @param {int} value - this motor's new power level, in the range [0,100].
     */,
    set: function set(value) {
      var p = Math.max(0, Math.min(value, 100));
      // Lego Wedo 2.0 hub only turns motors at power range [30 - 100], so
      // map value from [0 - 100] to [30 - 100].
      if (p === 0) {
        this._power = 0;
      } else {
        var delta = 100 / p;
        this._power = 30 + 70 / delta;
      }
    }

    /**
     * @return {boolean} - true if this motor is currently moving, false if this motor is off or braking.
     */
  }, {
    key: "isOn",
    get: function get() {
      return this._isOn;
    }

    /**
     * @return {boolean} - time, in milliseconds, of when the pending timeout began.
     */
  }, {
    key: "pendingTimeoutStartTime",
    get: function get() {
      return this._pendingTimeoutStartTime;
    }

    /**
     * @return {boolean} - delay, in milliseconds, of the pending timeout.
     */
  }, {
    key: "pendingTimeoutDelay",
    get: function get() {
      return this._pendingTimeoutDelay;
    }

    /**
     * Turn this motor on indefinitely.
     */
  }, {
    key: "turnOn",
    value: function turnOn() {
      var cmd = this._parent.generateOutputCommand(this._index + 1, WeDo2Command.MOTOR_POWER, [this._power * this._direction] // power in range 0-100
      );
      this._parent.send(BLECharacteristic.OUTPUT_COMMAND, cmd);
      this._isOn = true;
      this._clearTimeout();
    }

    /**
     * Turn this motor on for a specific duration.
     * @param {number} milliseconds - run the motor for this long.
     */
  }, {
    key: "turnOnFor",
    value: function turnOnFor(milliseconds) {
      if (this._power === 0) return;
      milliseconds = Math.max(0, milliseconds);
      this.turnOn();
      this._setNewTimeout(this.startBraking, milliseconds);
    }

    /**
     * Start active braking on this motor. After a short time, the motor will turn off.
     */
  }, {
    key: "startBraking",
    value: function startBraking() {
      if (this._power === 0) return;
      var cmd = this._parent.generateOutputCommand(this._index + 1, WeDo2Command.MOTOR_POWER, [127] // 127 = break
      );
      this._parent.send(BLECharacteristic.OUTPUT_COMMAND, cmd);
      this._isOn = false;
      this._setNewTimeout(this.turnOff, WeDo2Motor.BRAKE_TIME_MS);
    }

    /**
     * Turn this motor off.
     * @param {boolean} [useLimiter=true] - if true, use the rate limiter
     */
  }, {
    key: "turnOff",
    value: function turnOff() {
      var useLimiter = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
      if (this._power === 0) return;
      var cmd = this._parent.generateOutputCommand(this._index + 1, WeDo2Command.MOTOR_POWER, [0] // 0 = stop
      );
      this._parent.send(BLECharacteristic.OUTPUT_COMMAND, cmd, useLimiter);
      this._isOn = false;
    }

    /**
     * Clear the motor action timeout, if any. Safe to call even when there is no pending timeout.
     * @private
     */
  }, {
    key: "_clearTimeout",
    value: function _clearTimeout() {
      if (this._pendingTimeoutId !== null) {
        clearTimeout(this._pendingTimeoutId);
        this._pendingTimeoutId = null;
        this._pendingTimeoutStartTime = null;
        this._pendingTimeoutDelay = null;
      }
    }

    /**
     * Set a new motor action timeout, after clearing an existing one if necessary.
     * @param {Function} callback - to be called at the end of the timeout.
     * @param {int} delay - wait this many milliseconds before calling the callback.
     * @private
     */
  }, {
    key: "_setNewTimeout",
    value: function _setNewTimeout(callback, delay) {
      var _this = this;
      this._clearTimeout();
      var timeoutID = setTimeout(function () {
        if (_this._pendingTimeoutId === timeoutID) {
          _this._pendingTimeoutId = null;
          _this._pendingTimeoutStartTime = null;
          _this._pendingTimeoutDelay = null;
        }
        callback();
      }, delay);
      this._pendingTimeoutId = timeoutID;
      this._pendingTimeoutStartTime = Date.now();
      this._pendingTimeoutDelay = delay;
    }
  }], [{
    key: "BRAKE_TIME_MS",
    get: function get() {
      return 1000;
    }
  }]);
}();
/**
 * Manage communication with a WeDo 2.0 peripheral over a Bluetooth Low Energy client socket.
 */
var WeDo2 = /*#__PURE__*/function () {
  function WeDo2(runtime, extensionId) {
    _classCallCheck(this, WeDo2);
    /**
     * The Scratch 3.0 runtime used to trigger the green flag button.
     * @type {Runtime}
     * @private
     */
    this._runtime = runtime;
    this._runtime.on('PROJECT_STOP_ALL', this.stopAll.bind(this));

    /**
     * The id of the extension this peripheral belongs to.
     */
    this._extensionId = extensionId;

    /**
     * A list of the ids of the motors or sensors in ports 1 and 2.
     * @type {string[]}
     * @private
     */
    this._ports = ['none', 'none'];

    /**
     * The motors which this WeDo 2.0 could possibly have.
     * @type {WeDo2Motor[]}
     * @private
     */
    this._motors = [null, null];

    /**
     * The most recently received value for each sensor.
     * @type {Object.<string, number>}
     * @private
     */
    this._sensors = {
      tiltX: 0,
      tiltY: 0,
      distance: 0
    };

    /**
     * The Bluetooth connection socket for reading/writing peripheral data.
     * @type {BLE}
     * @private
     */
    this._ble = null;
    this._runtime.registerPeripheralExtension(extensionId, this);

    /**
     * A rate limiter utility, to help limit the rate at which we send BLE messages
     * over the socket to Scratch Link to a maximum number of sends per second.
     * @type {RateLimiter}
     * @private
     */
    this._rateLimiter = new RateLimiter(BLESendRateMax);

    /**
     * An interval id for the battery check interval.
     * @type {number}
     * @private
     */
    this._batteryLevelIntervalId = null;
    this.reset = this.reset.bind(this);
    this._onConnect = this._onConnect.bind(this);
    this._onMessage = this._onMessage.bind(this);
    this._checkBatteryLevel = this._checkBatteryLevel.bind(this);
  }

  /**
   * @return {number} - the latest value received for the tilt sensor's tilt about the X axis.
   */
  return _createClass(WeDo2, [{
    key: "tiltX",
    get: function get() {
      return this._sensors.tiltX;
    }

    /**
     * @return {number} - the latest value received for the tilt sensor's tilt about the Y axis.
     */
  }, {
    key: "tiltY",
    get: function get() {
      return this._sensors.tiltY;
    }

    /**
     * @return {number} - the latest value received from the distance sensor.
     */
  }, {
    key: "distance",
    get: function get() {
      return this._sensors.distance;
    }

    /**
     * Access a particular motor on this peripheral.
     * @param {int} index - the zero-based index of the desired motor.
     * @return {WeDo2Motor} - the WeDo2Motor instance, if any, at that index.
     */
  }, {
    key: "motor",
    value: function motor(index) {
      return this._motors[index];
    }

    /**
     * Stop all the motors that are currently running.
     */
  }, {
    key: "stopAllMotors",
    value: function stopAllMotors() {
      this._motors.forEach(function (motor) {
        if (motor) {
          // Send the motor off command without using the rate limiter.
          // This allows the stop button to stop motors even if we are
          // otherwise flooded with commands.
          motor.turnOff(false);
        }
      });
    }

    /**
     * Set the WeDo 2.0 peripheral's LED to a specific color.
     * @param {int} inputRGB - a 24-bit RGB color in 0xRRGGBB format.
     * @return {Promise} - a promise of the completion of the set led send operation.
     */
  }, {
    key: "setLED",
    value: function setLED(inputRGB) {
      var rgb = [inputRGB >> 16 & 0x000000FF, inputRGB >> 8 & 0x000000FF, inputRGB & 0x000000FF];
      var cmd = this.generateOutputCommand(WeDo2ConnectID.LED, WeDo2Command.WRITE_RGB, rgb);
      return this.send(BLECharacteristic.OUTPUT_COMMAND, cmd);
    }

    /**
     * Sets the input mode of the LED to RGB.
     * @return {Promise} - a promise returned by the send operation.
     */
  }, {
    key: "setLEDMode",
    value: function setLEDMode() {
      var cmd = this.generateInputCommand(WeDo2ConnectID.LED, WeDo2Device.LED, WeDo2Mode.LED, 0, WeDo2Unit.LED, false);
      return this.send(BLECharacteristic.INPUT_COMMAND, cmd);
    }

    /**
     * Switch off the LED on the WeDo 2.0.
     * @return {Promise} - a promise of the completion of the stop led send operation.
     */
  }, {
    key: "stopLED",
    value: function stopLED() {
      var cmd = this.generateOutputCommand(WeDo2ConnectID.LED, WeDo2Command.WRITE_RGB, [0, 0, 0]);
      return this.send(BLECharacteristic.OUTPUT_COMMAND, cmd);
    }

    /**
     * Play a tone from the WeDo 2.0 peripheral for a specific amount of time.
     * @param {int} tone - the pitch of the tone, in Hz.
     * @param {int} milliseconds - the duration of the note, in milliseconds.
     * @return {Promise} - a promise of the completion of the play tone send operation.
     */
  }, {
    key: "playTone",
    value: function playTone(tone, milliseconds) {
      var cmd = this.generateOutputCommand(WeDo2ConnectID.PIEZO, WeDo2Command.PLAY_TONE, [tone, tone >> 8, milliseconds, milliseconds >> 8]);
      return this.send(BLECharacteristic.OUTPUT_COMMAND, cmd);
    }

    /**
     * Stop the tone playing from the WeDo 2.0 peripheral, if any.
     * @return {Promise} - a promise that the command sent.
     */
  }, {
    key: "stopTone",
    value: function stopTone() {
      var cmd = this.generateOutputCommand(WeDo2ConnectID.PIEZO, WeDo2Command.STOP_TONE);

      // Send this command without using the rate limiter, because it is
      // only triggered by the stop button.
      return this.send(BLECharacteristic.OUTPUT_COMMAND, cmd, false);
    }

    /**
     * Stop the tone playing and motors on the WeDo 2.0 peripheral.
     */
  }, {
    key: "stopAll",
    value: function stopAll() {
      if (!this.isConnected()) return;
      this.stopTone();
      this.stopAllMotors();
    }

    /**
     * Called by the runtime when user wants to scan for a WeDo 2.0 peripheral.
     */
  }, {
    key: "scan",
    value: function scan() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this._ble = new BLE(this._runtime, this._extensionId, {
        filters: [{
          services: [BLEService.DEVICE_SERVICE]
        }],
        optionalServices: [BLEService.IO_SERVICE]
      }, this._onConnect, this.reset);
    }

    /**
     * Called by the runtime when user wants to connect to a certain WeDo 2.0 peripheral.
     * @param {number} id - the id of the peripheral to connect to.
     */
  }, {
    key: "connect",
    value: function connect(id) {
      if (this._ble) {
        this._ble.connectPeripheral(id);
      }
    }

    /**
     * Disconnects from the current BLE socket.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._ble) {
        this._ble.disconnect();
      }
      this.reset();
    }

    /**
     * Reset all the state and timeout/interval ids.
     */
  }, {
    key: "reset",
    value: function reset() {
      this._ports = ['none', 'none'];
      this._motors = [null, null];
      this._sensors = {
        tiltX: 0,
        tiltY: 0,
        distance: 0
      };
      if (this._batteryLevelIntervalId) {
        window.clearInterval(this._batteryLevelIntervalId);
        this._batteryLevelIntervalId = null;
      }
    }

    /**
     * Called by the runtime to detect whether the WeDo 2.0 peripheral is connected.
     * @return {boolean} - the connected state.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      var connected = false;
      if (this._ble) {
        connected = this._ble.isConnected();
      }
      return connected;
    }

    /**
     * Write a message to the WeDo 2.0 peripheral BLE socket.
     * @param {number} uuid - the UUID of the characteristic to write to
     * @param {Array} message - the message to write.
     * @param {boolean} [useLimiter=true] - if true, use the rate limiter
     * @return {Promise} - a promise result of the write operation
     */
  }, {
    key: "send",
    value: function send(uuid, message) {
      var useLimiter = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
      if (!this.isConnected()) return Promise.resolve();
      if (useLimiter) {
        if (!this._rateLimiter.okayToSend()) return Promise.resolve();
      }
      return this._ble.write(BLEService.IO_SERVICE, uuid, Base64Util.uint8ArrayToBase64(message), 'base64');
    }

    /**
     * Generate a WeDo 2.0 'Output Command' in the byte array format
     * (CONNECT ID, COMMAND ID, NUMBER OF BYTES, VALUES ...).
     *
     * This sends a command to the WeDo 2.0 to actuate the specified outputs.
     *
     * @param  {number} connectID - the port (Connect ID) to send a command to.
     * @param  {number} commandID - the id of the byte command.
     * @param  {array}  values    - the list of values to write to the command.
     * @return {array}            - a generated output command.
     */
  }, {
    key: "generateOutputCommand",
    value: function generateOutputCommand(connectID, commandID) {
      var values = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;
      var command = [connectID, commandID];
      if (values) {
        command = command.concat(values.length).concat(values);
      }
      return command;
    }

    /**
     * Generate a WeDo 2.0 'Input Command' in the byte array format
     * (COMMAND ID, COMMAND TYPE, CONNECT ID, TYPE ID, MODE, DELTA INTERVAL (4 BYTES),
     * UNIT, NOTIFICATIONS ENABLED).
     *
     * This sends a command to the WeDo 2.0 that sets that input format
     * of the specified inputs and sets value change notifications.
     *
     * @param  {number}  connectID           - the port (Connect ID) to send a command to.
     * @param  {number}  type                - the type of input sensor.
     * @param  {number}  mode                - the mode of the input sensor.
     * @param  {number}  delta               - the delta change needed to trigger notification.
     * @param  {array}   units               - the unit of the input sensor value.
     * @param  {boolean} enableNotifications - whether to enable notifications.
     * @return {array}                       - a generated input command.
     */
  }, {
    key: "generateInputCommand",
    value: function generateInputCommand(connectID, type, mode, delta, units, enableNotifications) {
      var command = [1,
      // Command ID = 1 = "Sensor Format"
      2,
      // Command Type = 2 = "Write"
      connectID, type, mode, delta, 0,
      // Delta Interval Byte 2
      0,
      // Delta Interval Byte 3
      0,
      // Delta Interval Byte 4
      units, enableNotifications ? 1 : 0];
      return command;
    }

    /**
     * Sets LED mode and initial color and starts reading data from peripheral after BLE has connected.
     * @private
     */
  }, {
    key: "_onConnect",
    value: function _onConnect() {
      this.setLEDMode();
      this.setLED(0x0000FF);
      this._ble.startNotifications(BLEService.DEVICE_SERVICE, BLECharacteristic.ATTACHED_IO, this._onMessage);
      this._batteryLevelIntervalId = window.setInterval(this._checkBatteryLevel, BLEBatteryCheckInterval);
    }

    /**
     * Process the sensor data from the incoming BLE characteristic.
     * @param {object} base64 - the incoming BLE data.
     * @private
     */
  }, {
    key: "_onMessage",
    value: function _onMessage(base64) {
      var data = Base64Util.base64ToUint8Array(base64);
      // log.info(data);

      /**
       * If first byte of data is '1' or '2', then either clear the
       * sensor present in ports 1 or 2 or set their format.
       *
       * If first byte of data is anything else, read incoming sensor value.
       */
      switch (data[0]) {
        case 1:
        case 2:
          {
            var connectID = data[0];
            if (data[1] === 0) {
              // clear sensor or motor
              this._clearPort(connectID);
            } else {
              // register sensor or motor
              this._registerSensorOrMotor(connectID, data[3]);
            }
            break;
          }
        default:
          {
            // read incoming sensor value
            var _connectID = data[1];
            var type = this._ports[_connectID - 1];
            if (type === WeDo2Device.DISTANCE) {
              this._sensors.distance = data[2];
            }
            if (type === WeDo2Device.TILT) {
              this._sensors.tiltX = data[2];
              this._sensors.tiltY = data[3];
            }
            break;
          }
      }
    }

    /**
     * Check the battery level on the WeDo 2.0. If the WeDo 2.0 has disconnected
     * for some reason, the BLE socket will get an error back and automatically
     * close the socket.
     */
  }, {
    key: "_checkBatteryLevel",
    value: function _checkBatteryLevel() {
      this._ble.read(BLEService.DEVICE_SERVICE, BLECharacteristic.LOW_VOLTAGE_ALERT, false);
    }

    /**
     * Register a new sensor or motor connected at a port.  Store the type of
     * sensor or motor internally, and then register for notifications on input
     * values if it is a sensor.
     * @param {number} connectID - the port to register a sensor or motor on.
     * @param {number} type - the type ID of the sensor or motor
     * @private
     */
  }, {
    key: "_registerSensorOrMotor",
    value: function _registerSensorOrMotor(connectID, type) {
      // Record which port is connected to what type of device
      this._ports[connectID - 1] = type;

      // Record motor port
      if (type === WeDo2Device.MOTOR) {
        this._motors[connectID - 1] = new WeDo2Motor(this, connectID - 1);
      } else {
        // Set input format for tilt or distance sensor
        var typeString = type === WeDo2Device.DISTANCE ? 'DISTANCE' : 'TILT';
        var cmd = this.generateInputCommand(connectID, type, WeDo2Mode[typeString], 1, WeDo2Unit[typeString], true);
        this.send(BLECharacteristic.INPUT_COMMAND, cmd);
        this._ble.startNotifications(BLEService.IO_SERVICE, BLECharacteristic.INPUT_VALUES, this._onMessage);
      }
    }

    /**
     * Clear the sensor or motor present at port 1 or 2.
     * @param {number} connectID - the port to clear.
     * @private
     */
  }, {
    key: "_clearPort",
    value: function _clearPort(connectID) {
      var type = this._ports[connectID - 1];
      if (type === WeDo2Device.TILT) {
        this._sensors.tiltX = this._sensors.tiltY = 0;
      }
      if (type === WeDo2Device.DISTANCE) {
        this._sensors.distance = 0;
      }
      this._ports[connectID - 1] = 'none';
      this._motors[connectID - 1] = null;
    }
  }]);
}();
/**
 * Enum for motor specification.
 * @readonly
 * @enum {string}
 */
var WeDo2MotorLabel = {
  DEFAULT: 'motor',
  A: 'motor A',
  B: 'motor B',
  ALL: 'all motors'
};

/**
 * Enum for motor direction specification.
 * @readonly
 * @enum {string}
 */
var WeDo2MotorDirection = {
  FORWARD: 'this way',
  BACKWARD: 'that way',
  REVERSE: 'reverse'
};

/**
 * Enum for tilt sensor direction.
 * @readonly
 * @enum {string}
 */
var WeDo2TiltDirection = {
  UP: 'up',
  DOWN: 'down',
  LEFT: 'left',
  RIGHT: 'right',
  ANY: 'any'
};

/**
 * Scratch 3.0 blocks to interact with a LEGO WeDo 2.0 peripheral.
 */
var Scratch3WeDo2Blocks = /*#__PURE__*/function () {
  /**
   * Construct a set of WeDo 2.0 blocks.
   * @param {Runtime} runtime - the Scratch 3.0 runtime.
   */
  function Scratch3WeDo2Blocks(runtime) {
    _classCallCheck(this, Scratch3WeDo2Blocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;

    // Create a new WeDo 2.0 peripheral instance
    this._peripheral = new WeDo2(this.runtime, Scratch3WeDo2Blocks.EXTENSION_ID);
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(Scratch3WeDo2Blocks, [{
    key: "getInfo",
    value: function getInfo() {
      return {
        id: Scratch3WeDo2Blocks.EXTENSION_ID,
        name: 'WeDo 2.0',
        blockIconURI: iconURI,
        showStatusButton: true,
        blocks: [{
          opcode: 'motorOnFor',
          text: formatMessage({
            id: 'wedo2.motorOnFor',
            "default": 'turn [MOTOR_ID] on for [DURATION] seconds',
            description: 'turn a motor on for some time'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: WeDo2MotorLabel.DEFAULT
            },
            DURATION: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            }
          }
        }, {
          opcode: 'motorOn',
          text: formatMessage({
            id: 'wedo2.motorOn',
            "default": 'turn [MOTOR_ID] on',
            description: 'turn a motor on indefinitely'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: WeDo2MotorLabel.DEFAULT
            }
          }
        }, {
          opcode: 'motorOff',
          text: formatMessage({
            id: 'wedo2.motorOff',
            "default": 'turn [MOTOR_ID] off',
            description: 'turn a motor off'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: WeDo2MotorLabel.DEFAULT
            }
          }
        }, {
          opcode: 'startMotorPower',
          text: formatMessage({
            id: 'wedo2.startMotorPower',
            "default": 'set [MOTOR_ID] power to [POWER]',
            description: 'set the motor\'s power and turn it on'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: WeDo2MotorLabel.DEFAULT
            },
            POWER: {
              type: ArgumentType.NUMBER,
              defaultValue: 100
            }
          }
        }, {
          opcode: 'setMotorDirection',
          text: formatMessage({
            id: 'wedo2.setMotorDirection',
            "default": 'set [MOTOR_ID] direction to [MOTOR_DIRECTION]',
            description: 'set the motor\'s turn direction'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            MOTOR_ID: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_ID',
              defaultValue: WeDo2MotorLabel.DEFAULT
            },
            MOTOR_DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'MOTOR_DIRECTION',
              defaultValue: WeDo2MotorDirection.FORWARD
            }
          }
        }, {
          opcode: 'setLightHue',
          text: formatMessage({
            id: 'wedo2.setLightHue',
            "default": 'set light color to [HUE]',
            description: 'set the LED color'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            HUE: {
              type: ArgumentType.NUMBER,
              defaultValue: 50
            }
          }
        }, {
          opcode: 'playNoteFor',
          text: formatMessage({
            id: 'wedo2.playNoteFor',
            "default": 'play note [NOTE] for [DURATION] seconds',
            description: 'play a certain note for some time'
          }),
          blockType: BlockType.COMMAND,
          arguments: {
            NOTE: {
              type: ArgumentType.NUMBER,
              // TODO: ArgumentType.MIDI_NOTE?
              defaultValue: 60
            },
            DURATION: {
              type: ArgumentType.NUMBER,
              defaultValue: 0.5
            }
          },
          hideFromPalette: true
        }, {
          opcode: 'whenDistance',
          text: formatMessage({
            id: 'wedo2.whenDistance',
            "default": 'when distance [OP] [REFERENCE]',
            description: 'check for when distance is < or > than reference'
          }),
          blockType: BlockType.HAT,
          arguments: {
            OP: {
              type: ArgumentType.STRING,
              menu: 'OP',
              defaultValue: '<'
            },
            REFERENCE: {
              type: ArgumentType.NUMBER,
              defaultValue: 50
            }
          }
        }, {
          opcode: 'whenTilted',
          text: formatMessage({
            id: 'wedo2.whenTilted',
            "default": 'when tilted [TILT_DIRECTION_ANY]',
            description: 'check when tilted in a certain direction'
          }),
          func: 'isTilted',
          blockType: BlockType.HAT,
          arguments: {
            TILT_DIRECTION_ANY: {
              type: ArgumentType.STRING,
              menu: 'TILT_DIRECTION_ANY',
              defaultValue: WeDo2TiltDirection.ANY
            }
          }
        }, {
          opcode: 'getDistance',
          text: formatMessage({
            id: 'wedo2.getDistance',
            "default": 'distance',
            description: 'the value returned by the distance sensor'
          }),
          blockType: BlockType.REPORTER
        }, {
          opcode: 'isTilted',
          text: formatMessage({
            id: 'wedo2.isTilted',
            "default": 'tilted [TILT_DIRECTION_ANY]?',
            description: 'whether the tilt sensor is tilted'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            TILT_DIRECTION_ANY: {
              type: ArgumentType.STRING,
              menu: 'TILT_DIRECTION_ANY',
              defaultValue: WeDo2TiltDirection.ANY
            }
          }
        }, {
          opcode: 'getTiltAngle',
          text: formatMessage({
            id: 'wedo2.getTiltAngle',
            "default": 'tilt angle [TILT_DIRECTION]',
            description: 'the angle returned by the tilt sensor'
          }),
          blockType: BlockType.REPORTER,
          arguments: {
            TILT_DIRECTION: {
              type: ArgumentType.STRING,
              menu: 'TILT_DIRECTION',
              defaultValue: WeDo2TiltDirection.UP
            }
          }
        }],
        menus: {
          MOTOR_ID: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'wedo2.motorId.default',
                "default": 'motor',
                description: 'label for motor element in motor menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorLabel.DEFAULT
            }, {
              text: formatMessage({
                id: 'wedo2.motorId.a',
                "default": 'motor A',
                description: 'label for motor A element in motor menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorLabel.A
            }, {
              text: formatMessage({
                id: 'wedo2.motorId.b',
                "default": 'motor B',
                description: 'label for motor B element in motor menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorLabel.B
            }, {
              text: formatMessage({
                id: 'wedo2.motorId.all',
                "default": 'all motors',
                description: 'label for all motors element in motor menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorLabel.ALL
            }]
          },
          MOTOR_DIRECTION: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'wedo2.motorDirection.forward',
                "default": 'this way',
                description: 'label for forward element in motor direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorDirection.FORWARD
            }, {
              text: formatMessage({
                id: 'wedo2.motorDirection.backward',
                "default": 'that way',
                description: 'label for backward element in motor direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorDirection.BACKWARD
            }, {
              text: formatMessage({
                id: 'wedo2.motorDirection.reverse',
                "default": 'reverse',
                description: 'label for reverse element in motor direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2MotorDirection.REVERSE
            }]
          },
          TILT_DIRECTION: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'wedo2.tiltDirection.up',
                "default": 'up',
                description: 'label for up element in tilt direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2TiltDirection.UP
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.down',
                "default": 'down',
                description: 'label for down element in tilt direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2TiltDirection.DOWN
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.left',
                "default": 'left',
                description: 'label for left element in tilt direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2TiltDirection.LEFT
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.right',
                "default": 'right',
                description: 'label for right element in tilt direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2TiltDirection.RIGHT
            }]
          },
          TILT_DIRECTION_ANY: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'wedo2.tiltDirection.up',
                "default": 'up'
              }),
              value: WeDo2TiltDirection.UP
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.down',
                "default": 'down'
              }),
              value: WeDo2TiltDirection.DOWN
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.left',
                "default": 'left'
              }),
              value: WeDo2TiltDirection.LEFT
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.right',
                "default": 'right'
              }),
              value: WeDo2TiltDirection.RIGHT
            }, {
              text: formatMessage({
                id: 'wedo2.tiltDirection.any',
                "default": 'any',
                description: 'label for any element in tilt direction menu for LEGO WeDo 2 extension'
              }),
              value: WeDo2TiltDirection.ANY
            }]
          },
          OP: {
            acceptReporters: true,
            items: ['<', '>']
          }
        }
      };
    }

    /**
     * Turn specified motor(s) on for a specified duration.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to activate.
     * @property {int} DURATION - the amount of time to run the motors.
     * @return {Promise} - a promise which will resolve at the end of the duration.
     */
  }, {
    key: "motorOnFor",
    value: function motorOnFor(args) {
      var _this2 = this;
      // TODO: cast args.MOTOR_ID?
      var durationMS = Cast.toNumber(args.DURATION) * 1000;
      durationMS = MathUtil.clamp(durationMS, 0, 15000);
      return new Promise(function (resolve) {
        _this2._forEachMotor(args.MOTOR_ID, function (motorIndex) {
          var motor = _this2._peripheral.motor(motorIndex);
          if (motor) {
            motor.turnOnFor(durationMS);
          }
        });

        // Run for some time even when no motor is connected
        setTimeout(resolve, durationMS);
      });
    }

    /**
     * Turn specified motor(s) on indefinitely.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to activate.
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "motorOn",
    value: function motorOn(args) {
      var _this3 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this3._peripheral.motor(motorIndex);
        if (motor) {
          motor.turnOn();
        }
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Turn specified motor(s) off.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to deactivate.
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "motorOff",
    value: function motorOff(args) {
      var _this4 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this4._peripheral.motor(motorIndex);
        if (motor) {
          motor.turnOff();
        }
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Turn specified motor(s) off.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to be affected.
     * @property {int} POWER - the new power level for the motor(s).
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "startMotorPower",
    value: function startMotorPower(args) {
      var _this5 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this5._peripheral.motor(motorIndex);
        if (motor) {
          motor.power = MathUtil.clamp(Cast.toNumber(args.POWER), 0, 100);
          motor.turnOn();
        }
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Set the direction of rotation for specified motor(s).
     * If the direction is 'reverse' the motor(s) will be reversed individually.
     * @param {object} args - the block's arguments.
     * @property {MotorID} MOTOR_ID - the motor(s) to be affected.
     * @property {MotorDirection} MOTOR_DIRECTION - the new direction for the motor(s).
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "setMotorDirection",
    value: function setMotorDirection(args) {
      var _this6 = this;
      // TODO: cast args.MOTOR_ID?
      this._forEachMotor(args.MOTOR_ID, function (motorIndex) {
        var motor = _this6._peripheral.motor(motorIndex);
        if (motor) {
          switch (args.MOTOR_DIRECTION) {
            case WeDo2MotorDirection.FORWARD:
              motor.direction = 1;
              break;
            case WeDo2MotorDirection.BACKWARD:
              motor.direction = -1;
              break;
            case WeDo2MotorDirection.REVERSE:
              motor.direction = -motor.direction;
              break;
            default:
              log.warn("Unknown motor direction in setMotorDirection: ".concat(args.DIRECTION));
              break;
          }
          // keep the motor on if it's running, and update the pending timeout if needed
          if (motor.isOn) {
            if (motor.pendingTimeoutDelay) {
              motor.turnOnFor(motor.pendingTimeoutStartTime + motor.pendingTimeoutDelay - Date.now());
            } else {
              motor.turnOn();
            }
          }
        }
      });
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Set the LED's hue.
     * @param {object} args - the block's arguments.
     * @property {number} HUE - the hue to set, in the range [0,100].
     * @return {Promise} - a Promise that resolves after some delay.
     */
  }, {
    key: "setLightHue",
    value: function setLightHue(args) {
      // Convert from [0,100] to [0,360]
      var inputHue = Cast.toNumber(args.HUE);
      inputHue = MathUtil.wrapClamp(inputHue, 0, 100);
      var hue = inputHue * 360 / 100;
      var rgbObject = color.hsvToRgb({
        h: hue,
        s: 1,
        v: 1
      });
      var rgbDecimal = color.rgbToDecimal(rgbObject);
      this._peripheral.setLED(rgbDecimal);
      return new Promise(function (resolve) {
        window.setTimeout(function () {
          resolve();
        }, BLESendInterval);
      });
    }

    /**
     * Make the WeDo 2.0 peripheral play a MIDI note for the specified duration.
     * @param {object} args - the block's arguments.
     * @property {number} NOTE - the MIDI note to play.
     * @property {number} DURATION - the duration of the note, in seconds.
     * @return {Promise} - a promise which will resolve at the end of the duration.
     */
  }, {
    key: "playNoteFor",
    value: function playNoteFor(args) {
      var _this7 = this;
      var durationMS = Cast.toNumber(args.DURATION) * 1000;
      durationMS = MathUtil.clamp(durationMS, 0, 3000);
      var note = MathUtil.clamp(Cast.toNumber(args.NOTE), 25, 125); // valid WeDo 2.0 sounds
      if (durationMS === 0) return; // WeDo 2.0 plays duration '0' forever
      return new Promise(function (resolve) {
        var tone = _this7._noteToTone(note);
        _this7._peripheral.playTone(tone, durationMS);

        // Run for some time even when no piezo is connected
        setTimeout(resolve, durationMS);
      });
    }

    /**
     * Compare the distance sensor's value to a reference.
     * @param {object} args - the block's arguments.
     * @property {string} OP - the comparison operation: '<' or '>'.
     * @property {number} REFERENCE - the value to compare against.
     * @return {boolean} - the result of the comparison, or false on error.
     */
  }, {
    key: "whenDistance",
    value: function whenDistance(args) {
      switch (args.OP) {
        case '<':
          return this._peripheral.distance < Cast.toNumber(args.REFERENCE);
        case '>':
          return this._peripheral.distance > Cast.toNumber(args.REFERENCE);
        default:
          log.warn("Unknown comparison operator in whenDistance: ".concat(args.OP));
          return false;
      }
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} TILT_DIRECTION_ANY - the tilt direction to test (up, down, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     */
  }, {
    key: "whenTilted",
    value: function whenTilted(args) {
      return this._isTilted(args.TILT_DIRECTION_ANY);
    }

    /**
     * @return {number} - the distance sensor's value, scaled to the [0,100] range.
     */
  }, {
    key: "getDistance",
    value: function getDistance() {
      return this._peripheral.distance;
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} TILT_DIRECTION_ANY - the tilt direction to test (up, down, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     */
  }, {
    key: "isTilted",
    value: function isTilted(args) {
      return this._isTilted(args.TILT_DIRECTION_ANY);
    }

    /**
     * @param {object} args - the block's arguments.
     * @property {TiltDirection} TILT_DIRECTION - the direction (up, down, left, right) to check.
     * @return {number} - the tilt sensor's angle in the specified direction.
     * Note that getTiltAngle(up) = -getTiltAngle(down) and getTiltAngle(left) = -getTiltAngle(right).
     */
  }, {
    key: "getTiltAngle",
    value: function getTiltAngle(args) {
      return this._getTiltAngle(args.TILT_DIRECTION);
    }

    /**
     * Test whether the tilt sensor is currently tilted.
     * @param {TiltDirection} direction - the tilt direction to test (up, down, left, right, or any).
     * @return {boolean} - true if the tilt sensor is tilted past a threshold in the specified direction.
     * @private
     */
  }, {
    key: "_isTilted",
    value: function _isTilted(direction) {
      switch (direction) {
        case WeDo2TiltDirection.ANY:
          return this._getTiltAngle(WeDo2TiltDirection.UP) >= Scratch3WeDo2Blocks.TILT_THRESHOLD || this._getTiltAngle(WeDo2TiltDirection.DOWN) >= Scratch3WeDo2Blocks.TILT_THRESHOLD || this._getTiltAngle(WeDo2TiltDirection.LEFT) >= Scratch3WeDo2Blocks.TILT_THRESHOLD || this._getTiltAngle(WeDo2TiltDirection.RIGHT) >= Scratch3WeDo2Blocks.TILT_THRESHOLD;
        default:
          return this._getTiltAngle(direction) >= Scratch3WeDo2Blocks.TILT_THRESHOLD;
      }
    }

    /**
     * @param {TiltDirection} direction - the direction (up, down, left, right) to check.
     * @return {number} - the tilt sensor's angle in the specified direction.
     * Note that getTiltAngle(up) = -getTiltAngle(down) and getTiltAngle(left) = -getTiltAngle(right).
     * @private
     */
  }, {
    key: "_getTiltAngle",
    value: function _getTiltAngle(direction) {
      switch (direction) {
        case WeDo2TiltDirection.UP:
          return this._peripheral.tiltY > 45 ? 256 - this._peripheral.tiltY : -this._peripheral.tiltY;
        case WeDo2TiltDirection.DOWN:
          return this._peripheral.tiltY > 45 ? this._peripheral.tiltY - 256 : this._peripheral.tiltY;
        case WeDo2TiltDirection.LEFT:
          return this._peripheral.tiltX > 45 ? 256 - this._peripheral.tiltX : -this._peripheral.tiltX;
        case WeDo2TiltDirection.RIGHT:
          return this._peripheral.tiltX > 45 ? this._peripheral.tiltX - 256 : this._peripheral.tiltX;
        default:
          log.warn("Unknown tilt direction in _getTiltAngle: ".concat(direction));
      }
    }

    /**
     * Call a callback for each motor indexed by the provided motor ID.
     * @param {MotorID} motorID - the ID specifier.
     * @param {Function} callback - the function to call with the numeric motor index for each motor.
     * @private
     */
  }, {
    key: "_forEachMotor",
    value: function _forEachMotor(motorID, callback) {
      var motors;
      switch (motorID) {
        case WeDo2MotorLabel.A:
          motors = [0];
          break;
        case WeDo2MotorLabel.B:
          motors = [1];
          break;
        case WeDo2MotorLabel.ALL:
        case WeDo2MotorLabel.DEFAULT:
          motors = [0, 1];
          break;
        default:
          log.warn("Invalid motor ID: ".concat(motorID));
          motors = [];
          break;
      }
      var _iterator = _createForOfIteratorHelper(motors),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var index = _step.value;
          callback(index);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * @param {number} midiNote - the MIDI note value to convert.
     * @return {number} - the frequency, in Hz, corresponding to that MIDI note value.
     * @private
     */
  }, {
    key: "_noteToTone",
    value: function _noteToTone(midiNote) {
      // Note that MIDI note 69 is A4, 440 Hz
      return 440 * Math.pow(2, (midiNote - 69) / 12);
    }
  }], [{
    key: "EXTENSION_ID",
    get:
    /**
     * @return {string} - the ID of this extension.
     */
    function get() {
      return 'wedo2';
    }

    /**
     * @return {number} - the tilt sensor counts as "tilted" if its tilt angle meets or exceeds this threshold.
     */
  }, {
    key: "TILT_THRESHOLD",
    get: function get() {
      return 15;
    }
  }]);
}();
module.exports = Scratch3WeDo2Blocks;

/***/ }),

/***/ "../vm/src/extensions/tw/index.js":
/*!****************************************!*\
  !*** ../vm/src/extensions/tw/index.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var BlockType = __webpack_require__(/*! ../../extension-support/block-type */ "../vm/src/extension-support/block-type.js");
var ArgumentType = __webpack_require__(/*! ../../extension-support/argument-type */ "../vm/src/extension-support/argument-type.js");
var Cast = __webpack_require__(/*! ../../util/cast */ "../vm/src/util/cast.js");

// eslint-disable-next-line max-len
var iconURI = "data:image/svg+xml;base64,".concat(btoa('<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32"><path fill="none" stroke="#fff" stroke-width="11.51815371" d="M24.457 7.707a18.41 18.41 0 0 0-.365 2.31c-.02.224 0 .507.06.852.061.405.092.689.092.851 0 .527-.345.79-1.034.79-.446 0-.74-.131-.881-.395-.02-.446-.01-1.054.03-1.824.04-.912.061-1.52.061-1.824-.02 0-.05-.02-.091-.06a98.522 98.522 0 0 0-5.32.364c-.04.264-.04.588 0 .973l.122 1.094c-.081.629-.122 1.56-.122 2.797.061.527.091 2.786.091 6.779v2.219c0 .344.051.587.152.73h1.885c.77-.102 1.155.222 1.155.972 0 .446-.213.76-.638.942-.264.102-.73.122-1.399.061-.405-.04-.881-.05-1.428-.03-.75.101-1.662.182-2.736.243-1.094.06-1.763-.091-2.006-.456-.162-.243-.162-.496 0-.76.283-.446 1.023-.669 2.219-.669.628 0 .942-.172.942-.516 0-.183-.01-.355-.03-.517 0-.507.01-.953.03-1.338.06-1.094.06-2.634 0-4.62-.081-2.878-.05-5.462.091-7.752l-.09-.09c-.63.04-1.805.03-3.527-.031-.081 0-.7.04-1.854.121.283 1.946.446 3.334.486 4.165l-.06.82c-.021.305-.274.457-.76.457-.386 0-.71-.73-.973-2.19-.122-.87-.244-1.752-.365-2.644 0-.142-.071-.385-.213-.73-.122-.364-.39-.97-.39-1.152 0-.641.593-.489 1.363-.61.06 0 .162.01.304.03.142.02.243.03.304.03H17.1a57.098 57.098 0 0 0 5.411-.486c.122-.06.304-.121.547-.182.426-.04.79.06 1.095.304.304.223.405.547.304.972z"/><path fill="none" stroke="#ff4c4c" stroke-width="5.75909785" d="M24.333 7.71q-.244 1.065-.365 2.311-.03.335.06.851.092.608.092.851 0 .79-1.034.79-.669 0-.881-.394-.03-.67.03-1.824.06-1.368.06-1.824-.03 0-.09-.061-2.827.122-5.32.365-.06.395 0 .973l.122 1.094q-.122.942-.122 2.796.091.79.091 6.78v2.218q0 .517.152.73h1.885q1.155-.152 1.155.973 0 .668-.638.942-.396.152-1.399.06-.608-.06-1.428-.03-1.125.152-2.736.243-1.642.092-2.006-.456-.244-.364 0-.76.425-.668 2.219-.668.942 0 .942-.517 0-.274-.03-.517 0-.76.03-1.337.091-1.642 0-4.62-.122-4.317.091-7.752l-.091-.091q-.942.06-3.526-.03-.122 0-1.854.12.425 2.919.486 4.165l-.06.821q-.031.456-.76.456-.578 0-.974-2.189-.182-1.307-.364-2.644 0-.213-.213-.73-.182-.547-.182-.82 0-.76 1.155-.943.09 0 .304.03.212.03.304.03h7.538q2.797-.12 5.411-.485.182-.092.547-.183.639-.06 1.095.304.456.335.304.973z"/><path fill="#fff" d="M24.31 7.714q-.243 1.064-.365 2.31-.03.335.061.852.091.608.091.85 0 .791-1.033.791-.67 0-.882-.395-.03-.669.03-1.824.061-1.368.061-1.824-.03 0-.09-.06-2.828.121-5.32.364-.061.396 0 .973l.121 1.094q-.121.943-.121 2.797.09.79.09 6.779v2.219q0 .517.153.73h1.884q1.156-.153 1.156.972 0 .669-.639.942-.395.152-1.398.061-.608-.06-1.429-.03-1.125.152-2.736.243-1.641.091-2.006-.456-.243-.365 0-.76.426-.669 2.22-.669.941 0 .941-.516 0-.274-.03-.517 0-.76.03-1.338.092-1.641 0-4.62-.121-4.317.092-7.752l-.092-.09q-.942.06-3.526-.031-.121 0-1.854.121.426 2.919.486 4.165l-.06.82q-.03.457-.76.457-.578 0-.973-2.19-.182-1.306-.365-2.644 0-.212-.213-.73-.182-.546-.182-.82 0-.76 1.155-.942.091 0 .304.03t.304.03h7.539q2.796-.121 5.41-.486.183-.091.548-.182.638-.061 1.094.304.456.334.304.972z"/></svg>'));

/**
 * Class for TurboWarp blocks
 * @constructor
 */
var TurboWarpBlocks = /*#__PURE__*/function () {
  function TurboWarpBlocks(runtime) {
    _classCallCheck(this, TurboWarpBlocks);
    /**
     * The runtime instantiating this block package.
     * @type {Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(TurboWarpBlocks, [{
    key: "getInfo",
    value: function getInfo() {
      return {
        id: 'tw',
        name: 'TurboWarp',
        color1: '#ff4c4c',
        color2: '#e64444',
        color3: '#c73a3a',
        docsURI: 'https://docs.turbowarp.org/blocks',
        menuIconURI: iconURI,
        blockIconURI: iconURI,
        blocks: [{
          opcode: 'getLastKeyPressed',
          text: formatMessage({
            id: 'tw.blocks.lastKeyPressed',
            "default": 'last key pressed',
            description: 'Block that returns the last key that was pressed'
          }),
          blockType: BlockType.REPORTER
        }, {
          opcode: 'getButtonIsDown',
          text: formatMessage({
            id: 'tw.blocks.buttonIsDown',
            "default": '[MOUSE_BUTTON] mouse button down?',
            description: 'Block that returns whether a specific mouse button is down'
          }),
          blockType: BlockType.BOOLEAN,
          arguments: {
            MOUSE_BUTTON: {
              type: ArgumentType.NUMBER,
              menu: 'mouseButton',
              defaultValue: '0'
            }
          }
        }],
        menus: {
          mouseButton: {
            items: [{
              text: formatMessage({
                id: 'tw.blocks.mouseButton.primary',
                "default": '(0) primary',
                description: 'Dropdown item to select primary (usually left) mouse button'
              }),
              value: '0'
            }, {
              text: formatMessage({
                id: 'tw.blocks.mouseButton.middle',
                "default": '(1) middle',
                description: 'Dropdown item to select middle mouse button'
              }),
              value: '1'
            }, {
              text: formatMessage({
                id: 'tw.blocks.mouseButton.secondary',
                "default": '(2) secondary',
                description: 'Dropdown item to select secondary (usually right) mouse button'
              }),
              value: '2'
            }],
            acceptReporters: true
          }
        }
      };
    }
  }, {
    key: "getLastKeyPressed",
    value: function getLastKeyPressed(args, util) {
      return util.ioQuery('keyboard', 'getLastKeyPressed');
    }
  }, {
    key: "getButtonIsDown",
    value: function getButtonIsDown(args, util) {
      var button = Cast.toNumber(args.MOUSE_BUTTON);
      return util.ioQuery('mouse', 'getButtonIsDown', [button]);
    }
  }]);
}();
module.exports = TurboWarpBlocks;

/***/ }),

/***/ "../vm/src/import/load-costume.js":
/*!****************************************!*\
  !*** ../vm/src/import/load-costume.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var AsyncLimiter = __webpack_require__(/*! ../util/async-limiter */ "../vm/src/util/async-limiter.js");
var _require = __webpack_require__(/*! @turbowarp/scratch-svg-renderer */ "../vm/node_modules/@turbowarp/scratch-svg-renderer/src/index.js"),
  loadSvgString = _require.loadSvgString,
  serializeSvgToString = _require.serializeSvgToString;
var _require2 = __webpack_require__(/*! ../serialization/tw-costume-import-export */ "../vm/src/serialization/tw-costume-import-export.js"),
  parseVectorMetadata = _require2.parseVectorMetadata;
var loadVector_ = function loadVector_(costume, runtime, rotationCenter, optVersion) {
  return new Promise(function (resolve) {
    var svgString = costume.asset.decodeText();

    // TW: We allow SVGs to specify their rotation center using a special comment.
    if (typeof rotationCenter === 'undefined') {
      var parsedRotationCenter = parseVectorMetadata(svgString);
      if (parsedRotationCenter) {
        rotationCenter = parsedRotationCenter;
        costume.rotationCenterX = rotationCenter[0];
        costume.rotationCenterY = rotationCenter[1];
      }
    }

    // SVG Renderer load fixes "quirks" associated with Scratch 2 projects
    if (optVersion && optVersion === 2) {
      // scratch-svg-renderer fixes syntax that causes loading issues,
      // and if optVersion is 2, fixes "quirks" associated with Scratch 2 SVGs,
      var fixedSvgString = serializeSvgToString(loadSvgString(svgString, true /* fromVersion2 */));

      // If the string changed, put back into storage
      if (svgString !== fixedSvgString) {
        svgString = fixedSvgString;
        var storage = runtime.storage;
        costume.asset.encodeTextData(fixedSvgString, storage.DataFormat.SVG, true);
        costume.assetId = costume.asset.assetId;
        costume.md5 = "".concat(costume.assetId, ".").concat(costume.dataFormat);
      }
    }

    // createSVGSkin does the right thing if rotationCenter isn't provided, so it's okay if it's
    // undefined here
    costume.skinId = runtime.renderer.createSVGSkin(svgString, rotationCenter);
    costume.size = runtime.renderer.getSkinSize(costume.skinId);
    // Now we should have a rotationCenter even if we didn't before
    if (!rotationCenter) {
      rotationCenter = runtime.renderer.getSkinRotationCenter(costume.skinId);
      costume.rotationCenterX = rotationCenter[0];
      costume.rotationCenterY = rotationCenter[1];
      costume.bitmapResolution = 1;
    }
    if (runtime.isPackaged) {
      costume.asset = null;
    }
    resolve(costume);
  });
};
var canvasPool = function () {
  /**
   * A pool of canvas objects that can be reused to reduce memory
   * allocations. And time spent in those allocations and the later garbage
   * collection.
   */
  var CanvasPool = /*#__PURE__*/function () {
    function CanvasPool() {
      _classCallCheck(this, CanvasPool);
      this.pool = [];
      this.clearSoon = null;
    }

    /**
     * After a short wait period clear the pool to let the VM collect
     * garbage.
     */
    return _createClass(CanvasPool, [{
      key: "clear",
      value: function clear() {
        var _this = this;
        if (!this.clearSoon) {
          this.clearSoon = new Promise(function (resolve) {
            return setTimeout(resolve, 1000);
          }).then(function () {
            _this.pool.length = 0;
            _this.clearSoon = null;
          });
        }
      }

      /**
       * Return a canvas. Create the canvas if the pool is empty.
       * @returns {HTMLCanvasElement} A canvas element.
       */
    }, {
      key: "create",
      value: function create() {
        return this.pool.pop() || document.createElement('canvas');
      }

      /**
       * Release the canvas to be reused.
       * @param {HTMLCanvasElement} canvas A canvas element.
       */
    }, {
      key: "release",
      value: function release(canvas) {
        this.clear();
        this.pool.push(canvas);
      }
    }]);
  }();
  return new CanvasPool();
}();

/**
 * @param {string} src URL of image
 * @returns {Promise<HTMLImageElement>}
 */
var readAsImageElement = function readAsImageElement(src) {
  return new Promise(function (resolve, reject) {
    var image = new Image();
    image.onload = function () {
      resolve(image);
      image.onload = null;
      image.onerror = null;
    };
    image.onerror = function () {
      reject(new Error('Costume load failed. Asset could not be read.'));
      image.onload = null;
      image.onerror = null;
    };
    image.src = src;
  });
};

/**
 * @param {Asset} asset scratch-storage asset
 * @returns {Promise<HTMLImageElement|ImageBitmap>}
 */
var _persistentReadImage = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(asset) {
    var firstError, i, imageBitmap, _t;
    return _regenerator().w(function (_context) {
      while (1) switch (_context.n) {
        case 0:
          i = 0;
        case 1:
          if (!(i < 3)) {
            _context.n = 9;
            break;
          }
          _context.p = 2;
          if (!(typeof createImageBitmap === 'function')) {
            _context.n = 5;
            break;
          }
          _context.n = 3;
          return createImageBitmap(new Blob([asset.data.buffer], {
            type: asset.assetType.contentType
          }));
        case 3:
          imageBitmap = _context.v;
          if (imageBitmap) {
            _context.n = 4;
            break;
          }
          throw new Error("createImageBitmap resolved with ".concat(imageBitmap));
        case 4:
          return _context.a(2, imageBitmap);
        case 5:
          _context.n = 6;
          return readAsImageElement(asset.encodeDataURI());
        case 6:
          return _context.a(2, _context.v);
        case 7:
          _context.p = 7;
          _t = _context.v;
          if (!firstError) {
            firstError = _t;
          }
          log.warn(_t);
          _context.n = 8;
          return new Promise(function (resolve) {
            return setTimeout(resolve, Math.random() * 2000);
          });
        case 8:
          i++;
          _context.n = 1;
          break;
        case 9:
          throw firstError;
        case 10:
          return _context.a(2);
      }
    }, _callee, null, [[2, 7]]);
  }));
  return function _persistentReadImage(_x) {
    return _ref.apply(this, arguments);
  };
}();

// Browsers break when we do too many createImageBitmap at the same time.
var readImage = new AsyncLimiter(_persistentReadImage, 25);

/**
 * Return a promise to fetch a bitmap from storage and return it as a canvas
 * If the costume has bitmapResolution 1, it will be converted to bitmapResolution 2 here (the standard for Scratch 3)
 * If the costume has a text layer asset, which is a text part from Scratch 1.4, then this function
 * will merge the two image assets. See the issue LLK/scratch-vm#672 for more information.
 * @param {!object} costume - the Scratch costume object.
 * @param {!Runtime} runtime - Scratch runtime, used to access the v2BitmapAdapter
 * @param {?object} rotationCenter - optionally passed in coordinates for the center of rotation for the image. If
 *     none is given, the rotation center of the costume will be set to the middle of the costume later on.
 * @property {number} costume.bitmapResolution - the resolution scale for a bitmap costume.
 * @returns {?Promise} - a promise which will resolve to an object {canvas, rotationCenter, assetMatchesBase},
 *     or reject on error.
 *     assetMatchesBase is true if the asset matches the base layer; false if it required adjustment
 */
var fetchBitmapCanvas_ = function fetchBitmapCanvas_(costume, runtime, rotationCenter) {
  if (!costume || !costume.asset) {
    // TODO: We can probably remove this check...
    // TODO: reject with an Error (breaking API change!)
    // eslint-disable-next-line prefer-promise-reject-errors
    return Promise.reject('Costume load failed. Assets were missing.');
  }
  if (!runtime.v2BitmapAdapter) {
    // TODO: reject with an Error (breaking API change!)
    // eslint-disable-next-line prefer-promise-reject-errors
    return Promise.reject('No V2 Bitmap adapter present.');
  }
  return Promise.all([costume.asset, costume.textLayerAsset].map(function (asset) {
    if (!asset) {
      return null;
    }
    return readImage["do"](asset);
  })).then(function (_ref2) {
    var _ref3 = _slicedToArray(_ref2, 2),
      baseImageElement = _ref3[0],
      textImageElement = _ref3[1];
    if (!baseImageElement) {
      throw new Error('Loading bitmap costume base failed.');
    }
    var scale = costume.bitmapResolution === 1 ? 2 : 1;
    var imageOrCanvas;
    var canvas;
    if (textImageElement) {
      canvas = canvasPool.create();
      canvas.width = baseImageElement.width;
      canvas.height = baseImageElement.height;
      var ctx = canvas.getContext('2d');
      ctx.drawImage(baseImageElement, 0, 0);
      ctx.drawImage(textImageElement, 0, 0);
      imageOrCanvas = canvas;
    } else {
      imageOrCanvas = baseImageElement;
    }
    if (scale !== 1) {
      // resize() returns a new canvas.
      imageOrCanvas = runtime.v2BitmapAdapter.resize(imageOrCanvas, imageOrCanvas.width * scale, imageOrCanvas.height * scale);
      // Old canvas is no longer used.
      if (canvas) {
        canvasPool.release(canvas);
      }
    }

    // This informs TurboWarp/scratch-render that this canvas won't be reused by the canvas pool,
    // which helps it optimize memory use.
    imageOrCanvas.reusable = false;

    // By scaling, we've converted it to bitmap resolution 2
    if (rotationCenter) {
      rotationCenter[0] = rotationCenter[0] * scale;
      rotationCenter[1] = rotationCenter[1] * scale;
      costume.rotationCenterX = rotationCenter[0];
      costume.rotationCenterY = rotationCenter[1];
    }
    costume.bitmapResolution = 2;

    // Clean up the costume object
    delete costume.textLayerMD5;
    delete costume.textLayerAsset;
    return {
      image: imageOrCanvas,
      rotationCenter: rotationCenter,
      // True if the asset matches the base layer; false if it required adjustment
      assetMatchesBase: scale === 1 && !textImageElement
    };
  })["finally"](function () {
    // Clean up the text layer properties if it fails to load
    delete costume.textLayerMD5;
    delete costume.textLayerAsset;
  });
};
var toDataURL = function toDataURL(imageOrCanvas) {
  if (imageOrCanvas instanceof HTMLCanvasElement) {
    return imageOrCanvas.toDataURL();
  }
  var canvas = canvasPool.create();
  canvas.width = imageOrCanvas.width;
  canvas.height = imageOrCanvas.height;
  var ctx = canvas.getContext('2d');
  ctx.drawImage(imageOrCanvas, 0, 0);
  var url = canvas.toDataURL();
  canvasPool.release(canvas);
  return url;
};
var loadBitmap_ = function loadBitmap_(costume, runtime, _rotationCenter) {
  return fetchBitmapCanvas_(costume, runtime, _rotationCenter).then(function (fetched) {
    var updateCostumeAsset = function updateCostumeAsset(dataURI) {
      if (!runtime.v2BitmapAdapter) {
        // TODO: This might be a bad practice since the returned
        // promise isn't acted on. If this is something we should be
        // creating a rejected promise for we should also catch it
        // somewhere and act on that error (like logging).
        //
        // Return a rejection to stop executing updateCostumeAsset.
        // TODO: reject with an Error (breaking API change!)
        // eslint-disable-next-line prefer-promise-reject-errors
        return Promise.reject('No V2 Bitmap adapter present.');
      }
      var storage = runtime.storage;
      costume.asset = storage.createAsset(storage.AssetType.ImageBitmap, storage.DataFormat.PNG, runtime.v2BitmapAdapter.convertDataURIToBinary(dataURI), null, true // generate md5
      );
      costume.dataFormat = storage.DataFormat.PNG;
      costume.assetId = costume.asset.assetId;
      costume.md5 = "".concat(costume.assetId, ".").concat(costume.dataFormat);
    };
    if (!fetched.assetMatchesBase) {
      updateCostumeAsset(toDataURL(fetched.image));
    }
    return fetched;
  }).then(function (_ref4) {
    var image = _ref4.image,
      rotationCenter = _ref4.rotationCenter;
    // createBitmapSkin does the right thing if costume.rotationCenter is undefined.
    // That will be the case if you upload a bitmap asset or create one by taking a photo.
    var center;
    if (rotationCenter) {
      // fetchBitmapCanvas will ensure that the costume's bitmap resolution is 2 and its rotation center is
      // scaled to match, so it's okay to always divide by 2.
      center = [rotationCenter[0] / 2, rotationCenter[1] / 2];
    }

    // TODO: costume.bitmapResolution will always be 2 at this point because of fetchBitmapCanvas_, so we don't
    // need to pass it in here.
    costume.skinId = runtime.renderer.createBitmapSkin(image, costume.bitmapResolution, center);
    var renderSize = runtime.renderer.getSkinSize(costume.skinId);
    costume.size = [renderSize[0] * 2, renderSize[1] * 2]; // Actual size, since all bitmaps are resolution 2

    if (!rotationCenter) {
      rotationCenter = runtime.renderer.getSkinRotationCenter(costume.skinId);
      // Actual rotation center, since all bitmaps are resolution 2
      costume.rotationCenterX = rotationCenter[0] * 2;
      costume.rotationCenterY = rotationCenter[1] * 2;
      costume.bitmapResolution = 2;
    }
    if (runtime.isPackaged) {
      costume.asset = null;
    }
    return costume;
  });
};

// Handle all manner of costume errors with a Gray Question Mark (default costume)
// and preserve as much of the original costume data as possible
// Returns a promise of a costume
var handleCostumeLoadError = function handleCostumeLoadError(costume, runtime) {
  // Keep track of the old asset information until we're done loading the default costume
  var oldAsset = costume.asset; // could be null
  var oldAssetId = costume.assetId;
  var oldRotationX = costume.rotationCenterX;
  var oldRotationY = costume.rotationCenterY;
  var oldBitmapResolution = costume.bitmapResolution;
  var oldDataFormat = costume.dataFormat;
  var AssetType = runtime.storage.AssetType;
  var isVector = costume.dataFormat === AssetType.ImageVector.runtimeFormat;

  // Use default asset if original fails to load
  costume.assetId = isVector ? runtime.storage.defaultAssetId.ImageVector : runtime.storage.defaultAssetId.ImageBitmap;
  costume.asset = runtime.storage.get(costume.assetId);
  costume.md5 = "".concat(costume.assetId, ".").concat(costume.asset.dataFormat);
  var defaultCostumePromise = isVector ? loadVector_(costume, runtime) : loadBitmap_(costume, runtime);
  return defaultCostumePromise.then(function (loadedCostume) {
    loadedCostume.broken = {};
    loadedCostume.broken.assetId = oldAssetId;
    loadedCostume.broken.md5 = "".concat(oldAssetId, ".").concat(oldDataFormat);

    // Should be null if we got here because the costume was missing
    loadedCostume.broken.asset = oldAsset;
    loadedCostume.broken.dataFormat = oldDataFormat;
    loadedCostume.broken.rotationCenterX = oldRotationX;
    loadedCostume.broken.rotationCenterY = oldRotationY;
    loadedCostume.broken.bitmapResolution = oldBitmapResolution;
    return loadedCostume;
  });
};

/**
 * Initialize a costume from an asset asynchronously.
 * Do not call this unless there is a renderer attached.
 * @param {!object} costume - the Scratch costume object.
 * @property {int} skinId - the ID of the costume's render skin, once installed.
 * @property {number} rotationCenterX - the X component of the costume's origin.
 * @property {number} rotationCenterY - the Y component of the costume's origin.
 * @property {number} [bitmapResolution] - the resolution scale for a bitmap costume.
 * @property {!Asset} costume.asset - the asset of the costume loaded from storage.
 * @param {!Runtime} runtime - Scratch runtime, used to access the storage module.
 * @param {?int} optVersion - Version of Scratch that the costume comes from. If this is set
 *     to 2, scratch 3 will perform an upgrade step to handle quirks in SVGs from Scratch 2.0.
 * @returns {?Promise} - a promise which will resolve after skinId is set, or null on error.
 */
var loadCostumeFromAsset = function loadCostumeFromAsset(costume, runtime, optVersion) {
  costume.assetId = costume.asset.assetId;
  var renderer = runtime.renderer;
  if (!renderer) {
    log.warn('No rendering module present; cannot load costume: ', costume.name);
    return Promise.resolve(costume);
  }
  var AssetType = runtime.storage.AssetType;
  var rotationCenter;
  // Use provided rotation center and resolution if they are defined. Bitmap resolution
  // should only ever be 1 or 2.
  if (typeof costume.rotationCenterX === 'number' && !isNaN(costume.rotationCenterX) && typeof costume.rotationCenterY === 'number' && !isNaN(costume.rotationCenterY)) {
    rotationCenter = [costume.rotationCenterX, costume.rotationCenterY];
  }
  if (costume.asset.assetType.runtimeFormat === AssetType.ImageVector.runtimeFormat) {
    return loadVector_(costume, runtime, rotationCenter, optVersion)["catch"](function (error) {
      log.warn("Error loading vector image: ".concat(error));
      return handleCostumeLoadError(costume, runtime);
    });
  }
  return loadBitmap_(costume, runtime, rotationCenter, optVersion)["catch"](function (error) {
    log.warn("Error loading bitmap image: ".concat(error));
    return handleCostumeLoadError(costume, runtime);
  });
};

/**
 * Load a costume's asset into memory asynchronously.
 * Do not call this unless there is a renderer attached.
 * @param {!string} md5ext - the MD5 and extension of the costume to be loaded.
 * @param {!object} costume - the Scratch costume object.
 * @property {int} skinId - the ID of the costume's render skin, once installed.
 * @property {number} rotationCenterX - the X component of the costume's origin.
 * @property {number} rotationCenterY - the Y component of the costume's origin.
 * @property {number} [bitmapResolution] - the resolution scale for a bitmap costume.
 * @param {!Runtime} runtime - Scratch runtime, used to access the storage module.
 * @param {?int} optVersion - Version of Scratch that the costume comes from. If this is set
 *     to 2, scratch 3 will perform an upgrade step to handle quirks in SVGs from Scratch 2.0.
 * @returns {?Promise} - a promise which will resolve after skinId is set, or null on error.
 */
var loadCostume = function loadCostume(md5ext, costume, runtime, optVersion) {
  var idParts = StringUtil.splitFirst(md5ext, '.');
  var md5 = idParts[0];
  var ext = idParts[1].toLowerCase();
  costume.dataFormat = ext;
  if (costume.asset) {
    // Costume comes with asset. It could be coming from image upload, drag and drop, or file
    return loadCostumeFromAsset(costume, runtime, optVersion);
  }

  // Need to load the costume from storage. The server should have a reference to this md5.
  if (!runtime.storage) {
    log.warn('No storage module present; cannot load costume asset: ', md5ext);
    return Promise.resolve(costume);
  }
  if (!runtime.storage.defaultAssetId) {
    log.warn("No default assets found");
    return Promise.resolve(costume);
  }
  var AssetType = runtime.storage.AssetType;
  var assetType = ext === 'svg' ? AssetType.ImageVector : AssetType.ImageBitmap;
  var costumePromise = runtime.storage.load(assetType, md5, ext);
  var textLayerPromise;
  if (costume.textLayerMD5) {
    textLayerPromise = runtime.storage.load(AssetType.ImageBitmap, costume.textLayerMD5, 'png');
  } else {
    textLayerPromise = Promise.resolve(null);
  }
  return Promise.all([costumePromise, textLayerPromise]).then(function (assetArray) {
    if (assetArray[0]) {
      costume.asset = assetArray[0];
    } else {
      return handleCostumeLoadError(costume, runtime);
    }
    if (assetArray[1]) {
      costume.textLayerAsset = assetArray[1];
    }
    return loadCostumeFromAsset(costume, runtime, optVersion);
  })["catch"](function (error) {
    // Handle case where storage.load rejects with errors
    // instead of resolving null
    log.warn('Error loading costume: ', error);
    return handleCostumeLoadError(costume, runtime);
  });
};
module.exports = {
  loadCostume: loadCostume,
  loadCostumeFromAsset: loadCostumeFromAsset
};

/***/ }),

/***/ "../vm/src/import/load-sound.js":
/*!**************************************!*\
  !*** ../vm/src/import/load-sound.js ***!
  \**************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/**
 * Initialize a sound from an asset asynchronously.
 * @param {!object} sound - the Scratch sound object.
 * @property {string} md5 - the MD5 and extension of the sound to be loaded.
 * @property {Buffer} data - sound data will be written here once loaded.
 * @param {!Asset} soundAsset - the asset loaded from storage.
 * @param {!Runtime} runtime - Scratch runtime, used to access the storage module.
 * @param {SoundBank} soundBank - Scratch Audio SoundBank to add sounds to.
 * @returns {!Promise} - a promise which will resolve to the sound when ready.
 */
var loadSoundFromAsset = function loadSoundFromAsset(sound, soundAsset, runtime, soundBank) {
  sound.assetId = soundAsset.assetId;
  if (!runtime.audioEngine) {
    log.warn('No audio engine present; cannot load sound asset: ', sound.md5);
    return Promise.resolve(sound);
  }
  return runtime.audioEngine.decodeSoundPlayer(Object.assign({}, sound, {
    data: soundAsset.data
  })).then(function (soundPlayer) {
    sound.soundId = soundPlayer.id;
    // Set the sound sample rate and sample count based on the
    // the audio buffer from the audio engine since the sound
    // gets resampled by the audio engine
    var soundBuffer = soundPlayer.buffer;
    sound.rate = soundBuffer.sampleRate;
    sound.sampleCount = soundBuffer.length;
    if (soundBank !== null) {
      soundBank.addSoundPlayer(soundPlayer);
    }
    if (runtime.isPackaged) {
      sound.asset = null;
    }
    return sound;
  });
};

// Handle sound loading errors by replacing the runtime sound with the
// default sound from storage, but keeping track of the original sound metadata
// in a `broken` field
var handleSoundLoadError = function handleSoundLoadError(sound, runtime, soundBank) {
  // Keep track of the old asset information until we're done loading the default sound
  var oldAsset = sound.asset; // could be null
  var oldAssetId = sound.assetId;
  var oldSample = sound.sampleCount;
  var oldRate = sound.rate;
  var oldFormat = sound.format;
  var oldDataFormat = sound.dataFormat;

  // Use default asset if original fails to load
  sound.assetId = runtime.storage.defaultAssetId.Sound;
  sound.asset = runtime.storage.get(sound.assetId);
  sound.md5 = "".concat(sound.assetId, ".").concat(sound.asset.dataFormat);
  return loadSoundFromAsset(sound, sound.asset, runtime, soundBank).then(function (loadedSound) {
    loadedSound.broken = {};
    loadedSound.broken.assetId = oldAssetId;
    loadedSound.broken.md5 = "".concat(oldAssetId, ".").concat(oldDataFormat);

    // Should be null if we got here because the sound was missing
    loadedSound.broken.asset = oldAsset;
    loadedSound.broken.sampleCount = oldSample;
    loadedSound.broken.rate = oldRate;
    loadedSound.broken.format = oldFormat;
    loadedSound.broken.dataFormat = oldDataFormat;
    return loadedSound;
  });
};

/**
 * Load a sound's asset into memory asynchronously.
 * @param {!object} sound - the Scratch sound object.
 * @property {string} md5 - the MD5 and extension of the sound to be loaded.
 * @property {Buffer} data - sound data will be written here once loaded.
 * @param {!Runtime} runtime - Scratch runtime, used to access the storage module.
 * @param {SoundBank} soundBank - Scratch Audio SoundBank to add sounds to.
 * @returns {!Promise} - a promise which will resolve to the sound when ready.
 */
var loadSound = function loadSound(sound, runtime, soundBank) {
  if (!runtime.storage) {
    log.warn('No storage module present; cannot load sound asset: ', sound.md5);
    return Promise.resolve(sound);
  }
  var idParts = StringUtil.splitFirst(sound.md5, '.');
  var md5 = idParts[0];
  var ext = idParts[1].toLowerCase();
  sound.dataFormat = ext;
  return (sound.asset && Promise.resolve(sound.asset) || runtime.storage.load(runtime.storage.AssetType.Sound, md5, ext)).then(function (soundAsset) {
    sound.asset = soundAsset;
    if (!soundAsset) {
      log.warn('Failed to find sound data: ', sound.md5);
      return handleSoundLoadError(sound, runtime, soundBank);
    }
    return loadSoundFromAsset(sound, soundAsset, runtime, soundBank);
  })["catch"](function (e) {
    log.warn("Failed to load sound: ".concat(sound.md5, " with error: ").concat(e));
    return handleSoundLoadError(sound, runtime, soundBank);
  });
};
module.exports = {
  loadSound: loadSound,
  loadSoundFromAsset: loadSoundFromAsset
};

/***/ }),

/***/ "../vm/src/index.js":
/*!**************************!*\
  !*** ../vm/src/index.js ***!
  \**************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var VirtualMachine = __webpack_require__(/*! ./virtual-machine */ "../vm/src/virtual-machine.js");
module.exports = VirtualMachine;

/***/ }),

/***/ "../vm/src/io/ble.js":
/*!***************************!*\
  !*** ../vm/src/io/ble.js ***!
  \***************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var JSONRPC = __webpack_require__(/*! ../util/jsonrpc */ "../vm/src/util/jsonrpc.js");
var BLE = /*#__PURE__*/function (_JSONRPC) {
  /**
   * A BLE peripheral socket object.  It handles connecting, over web sockets, to
   * BLE peripherals, and reading and writing data to them.
   * @param {Runtime} runtime - the Runtime for sending/receiving GUI update events.
   * @param {string} extensionId - the id of the extension using this socket.
   * @param {object} peripheralOptions - the list of options for peripheral discovery.
   * @param {object} connectCallback - a callback for connection.
   * @param {object} resetCallback - a callback for resetting extension state.
   */
  function BLE(runtime, extensionId, peripheralOptions, connectCallback) {
    var _this;
    var resetCallback = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : null;
    _classCallCheck(this, BLE);
    _this = _callSuper(this, BLE);
    _this._socket = runtime.getScratchLinkSocket('BLE');
    _this._socket.setOnOpen(_this.requestPeripheral.bind(_this));
    _this._socket.setOnClose(_this.handleDisconnectError.bind(_this));
    _this._socket.setOnError(_this._handleRequestError.bind(_this));
    _this._socket.setHandleMessage(_this._handleMessage.bind(_this));
    _this._sendMessage = _this._socket.sendMessage.bind(_this._socket);
    _this._availablePeripherals = {};
    _this._connectCallback = connectCallback;
    _this._connected = false;
    _this._characteristicDidChangeCallback = null;
    _this._resetCallback = resetCallback;
    _this._discoverTimeoutID = null;
    _this._extensionId = extensionId;
    _this._peripheralOptions = peripheralOptions;
    _this._runtime = runtime;
    _this._socket.open();
    return _this;
  }

  /**
   * Request connection to the peripheral.
   * If the web socket is not yet open, request when the socket promise resolves.
   */
  _inherits(BLE, _JSONRPC);
  return _createClass(BLE, [{
    key: "requestPeripheral",
    value: function requestPeripheral() {
      var _this2 = this;
      this._availablePeripherals = {};
      if (this._discoverTimeoutID) {
        window.clearTimeout(this._discoverTimeoutID);
      }
      this._discoverTimeoutID = window.setTimeout(this._handleDiscoverTimeout.bind(this), 15000);
      this.sendRemoteRequest('discover', this._peripheralOptions)["catch"](function (e) {
        _this2._handleRequestError(e);
      });
    }

    /**
     * Try connecting to the input peripheral id, and then call the connect
     * callback if connection is successful.
     * @param {number} id - the id of the peripheral to connect to
     */
  }, {
    key: "connectPeripheral",
    value: function connectPeripheral(id) {
      var _this3 = this;
      this.sendRemoteRequest('connect', {
        peripheralId: id
      }).then(function () {
        _this3._connected = true;
        _this3._runtime.emit(_this3._runtime.constructor.PERIPHERAL_CONNECTED);
        _this3._connectCallback();
      })["catch"](function (e) {
        _this3._handleRequestError(e);
      });
    }

    /**
     * Close the websocket.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._connected) {
        this._connected = false;
      }
      if (this._socket.isOpen()) {
        this._socket.close();
      }
      if (this._discoverTimeoutID) {
        window.clearTimeout(this._discoverTimeoutID);
      }

      // Sets connection status icon to orange
      this._runtime.emit(this._runtime.constructor.PERIPHERAL_DISCONNECTED);
    }

    /**
     * @return {bool} whether the peripheral is connected.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      return this._connected;
    }

    /**
     * Start receiving notifications from the specified ble service.
     * @param {number} serviceId - the ble service to read.
     * @param {number} characteristicId - the ble characteristic to get notifications from.
     * @param {object} onCharacteristicChanged - callback for characteristic change notifications.
     * @return {Promise} - a promise from the remote startNotifications request.
     */
  }, {
    key: "startNotifications",
    value: function startNotifications(serviceId, characteristicId) {
      var _this4 = this;
      var onCharacteristicChanged = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;
      var params = {
        serviceId: serviceId,
        characteristicId: characteristicId
      };
      this._characteristicDidChangeCallback = onCharacteristicChanged;
      return this.sendRemoteRequest('startNotifications', params)["catch"](function (e) {
        _this4.handleDisconnectError(e);
      });
    }

    /**
     * Read from the specified ble service.
     * @param {number} serviceId - the ble service to read.
     * @param {number} characteristicId - the ble characteristic to read.
     * @param {boolean} optStartNotifications - whether to start receiving characteristic change notifications.
     * @param {object} onCharacteristicChanged - callback for characteristic change notifications.
     * @return {Promise} - a promise from the remote read request.
     */
  }, {
    key: "read",
    value: function read(serviceId, characteristicId) {
      var _this5 = this;
      var optStartNotifications = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
      var onCharacteristicChanged = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
      var params = {
        serviceId: serviceId,
        characteristicId: characteristicId
      };
      if (optStartNotifications) {
        params.startNotifications = true;
      }
      if (onCharacteristicChanged) {
        this._characteristicDidChangeCallback = onCharacteristicChanged;
      }
      return this.sendRemoteRequest('read', params)["catch"](function (e) {
        _this5.handleDisconnectError(e);
      });
    }

    /**
     * Write data to the specified ble service.
     * @param {number} serviceId - the ble service to write.
     * @param {number} characteristicId - the ble characteristic to write.
     * @param {string} message - the message to send.
     * @param {string} encoding - the message encoding type.
     * @param {boolean} withResponse - if true, resolve after peripheral's response.
     * @return {Promise} - a promise from the remote send request.
     */
  }, {
    key: "write",
    value: function write(serviceId, characteristicId, message) {
      var _this6 = this;
      var encoding = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
      var withResponse = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : null;
      var params = {
        serviceId: serviceId,
        characteristicId: characteristicId,
        message: message
      };
      if (encoding) {
        params.encoding = encoding;
      }
      if (withResponse !== null) {
        params.withResponse = withResponse;
      }
      return this.sendRemoteRequest('write', params)["catch"](function (e) {
        _this6.handleDisconnectError(e);
      });
    }

    /**
     * Handle a received call from the socket.
     * @param {string} method - a received method label.
     * @param {object} params - a received list of parameters.
     * @return {object} - optional return value.
     */
  }, {
    key: "didReceiveCall",
    value: function didReceiveCall(method, params) {
      switch (method) {
        case 'didDiscoverPeripheral':
          this._availablePeripherals[params.peripheralId] = params;
          this._runtime.emit(this._runtime.constructor.PERIPHERAL_LIST_UPDATE, this._availablePeripherals);
          if (this._discoverTimeoutID) {
            window.clearTimeout(this._discoverTimeoutID);
          }
          break;
        case 'userDidPickPeripheral':
          this._availablePeripherals[params.peripheralId] = params;
          this._runtime.emit(this._runtime.constructor.USER_PICKED_PERIPHERAL, this._availablePeripherals);
          if (this._discoverTimeoutID) {
            window.clearTimeout(this._discoverTimeoutID);
          }
          break;
        case 'userDidNotPickPeripheral':
          this._runtime.emit(this._runtime.constructor.PERIPHERAL_SCAN_TIMEOUT);
          if (this._discoverTimeoutID) {
            window.clearTimeout(this._discoverTimeoutID);
          }
          break;
        case 'characteristicDidChange':
          if (this._characteristicDidChangeCallback) {
            this._characteristicDidChangeCallback(params.message);
          }
          break;
        case 'ping':
          return 42;
      }
    }

    /**
     * Handle an error resulting from losing connection to a peripheral.
     *
     * This could be due to:
     * - battery depletion
     * - going out of bluetooth range
     * - being powered down
     *
     * Disconnect the socket, and if the extension using this socket has a
     * reset callback, call it. Finally, emit an error to the runtime.
     */
  }, {
    key: "handleDisconnectError",
    value: function handleDisconnectError(/* e */
    ) {
      // log.error(`BLE error: ${JSON.stringify(e)}`);

      if (!this._connected) return;
      this.disconnect();
      if (this._resetCallback) {
        this._resetCallback();
      }
      this._runtime.emit(this._runtime.constructor.PERIPHERAL_CONNECTION_LOST_ERROR, {
        message: "Scratch lost connection to",
        extensionId: this._extensionId
      });
    }
  }, {
    key: "_handleRequestError",
    value: function _handleRequestError(/* e */
    ) {
      // log.error(`BLE error: ${JSON.stringify(e)}`);

      this._runtime.emit(this._runtime.constructor.PERIPHERAL_REQUEST_ERROR, {
        message: "Scratch lost connection to",
        extensionId: this._extensionId
      });
    }
  }, {
    key: "_handleDiscoverTimeout",
    value: function _handleDiscoverTimeout() {
      if (this._discoverTimeoutID) {
        window.clearTimeout(this._discoverTimeoutID);
      }
      this._runtime.emit(this._runtime.constructor.PERIPHERAL_SCAN_TIMEOUT);
    }
  }]);
}(JSONRPC);
module.exports = BLE;

/***/ }),

/***/ "../vm/src/io/bt.js":
/*!**************************!*\
  !*** ../vm/src/io/bt.js ***!
  \**************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var JSONRPC = __webpack_require__(/*! ../util/jsonrpc */ "../vm/src/util/jsonrpc.js");
var BT = /*#__PURE__*/function (_JSONRPC) {
  /**
   * A BT peripheral socket object.  It handles connecting, over web sockets, to
   * BT peripherals, and reading and writing data to them.
   * @param {Runtime} runtime - the Runtime for sending/receiving GUI update events.
   * @param {string} extensionId - the id of the extension using this socket.
   * @param {object} peripheralOptions - the list of options for peripheral discovery.
   * @param {object} connectCallback - a callback for connection.
   * @param {object} resetCallback - a callback for resetting extension state.
   * @param {object} messageCallback - a callback for message sending.
   */
  function BT(runtime, extensionId, peripheralOptions, connectCallback) {
    var _this;
    var resetCallback = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : null;
    var messageCallback = arguments.length > 5 ? arguments[5] : undefined;
    _classCallCheck(this, BT);
    _this = _callSuper(this, BT);
    _this._socket = runtime.getScratchLinkSocket('BT');
    _this._socket.setOnOpen(_this.requestPeripheral.bind(_this));
    _this._socket.setOnError(_this._handleRequestError.bind(_this));
    _this._socket.setOnClose(_this.handleDisconnectError.bind(_this));
    _this._socket.setHandleMessage(_this._handleMessage.bind(_this));
    _this._sendMessage = _this._socket.sendMessage.bind(_this._socket);
    _this._availablePeripherals = {};
    _this._connectCallback = connectCallback;
    _this._connected = false;
    _this._characteristicDidChangeCallback = null;
    _this._resetCallback = resetCallback;
    _this._discoverTimeoutID = null;
    _this._extensionId = extensionId;
    _this._peripheralOptions = peripheralOptions;
    _this._messageCallback = messageCallback;
    _this._runtime = runtime;
    _this._socket.open();
    return _this;
  }

  /**
   * Request connection to the peripheral.
   * If the web socket is not yet open, request when the socket promise resolves.
   */
  _inherits(BT, _JSONRPC);
  return _createClass(BT, [{
    key: "requestPeripheral",
    value: function requestPeripheral() {
      var _this2 = this;
      this._availablePeripherals = {};
      if (this._discoverTimeoutID) {
        window.clearTimeout(this._discoverTimeoutID);
      }
      this._discoverTimeoutID = window.setTimeout(this._handleDiscoverTimeout.bind(this), 15000);
      this.sendRemoteRequest('discover', this._peripheralOptions)["catch"](function (e) {
        return _this2._handleRequestError(e);
      });
    }

    /**
     * Try connecting to the input peripheral id, and then call the connect
     * callback if connection is successful.
     * @param {number} id - the id of the peripheral to connect to
     * @param {string} pin - an optional pin for pairing
     */
  }, {
    key: "connectPeripheral",
    value: function connectPeripheral(id) {
      var _this3 = this;
      var pin = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;
      var params = {
        peripheralId: id
      };
      if (pin) {
        params.pin = pin;
      }
      this.sendRemoteRequest('connect', params).then(function () {
        _this3._connected = true;
        _this3._runtime.emit(_this3._runtime.constructor.PERIPHERAL_CONNECTED);
        _this3._connectCallback();
      })["catch"](function (e) {
        _this3._handleRequestError(e);
      });
    }

    /**
     * Close the websocket.
     */
  }, {
    key: "disconnect",
    value: function disconnect() {
      if (this._connected) {
        this._connected = false;
      }
      if (this._socket.isOpen()) {
        this._socket.close();
      }
      if (this._discoverTimeoutID) {
        window.clearTimeout(this._discoverTimeoutID);
      }

      // Sets connection status icon to orange
      this._runtime.emit(this._runtime.constructor.PERIPHERAL_DISCONNECTED);
    }

    /**
     * @return {bool} whether the peripheral is connected.
     */
  }, {
    key: "isConnected",
    value: function isConnected() {
      return this._connected;
    }
  }, {
    key: "sendMessage",
    value: function sendMessage(options) {
      var _this4 = this;
      return this.sendRemoteRequest('send', options)["catch"](function (e) {
        _this4.handleDisconnectError(e);
      });
    }

    /**
     * Handle a received call from the socket.
     * @param {string} method - a received method label.
     * @param {object} params - a received list of parameters.
     * @return {object} - optional return value.
     */
  }, {
    key: "didReceiveCall",
    value: function didReceiveCall(method, params) {
      // TODO: Add peripheral 'undiscover' handling
      switch (method) {
        case 'didDiscoverPeripheral':
          this._availablePeripherals[params.peripheralId] = params;
          this._runtime.emit(this._runtime.constructor.PERIPHERAL_LIST_UPDATE, this._availablePeripherals);
          if (this._discoverTimeoutID) {
            window.clearTimeout(this._discoverTimeoutID);
          }
          break;
        case 'userDidPickPeripheral':
          this._availablePeripherals[params.peripheralId] = params;
          this._runtime.emit(this._runtime.constructor.USER_PICKED_PERIPHERAL, this._availablePeripherals);
          if (this._discoverTimeoutID) {
            window.clearTimeout(this._discoverTimeoutID);
          }
          break;
        case 'userDidNotPickPeripheral':
          this._runtime.emit(this._runtime.constructor.PERIPHERAL_SCAN_TIMEOUT);
          if (this._discoverTimeoutID) {
            window.clearTimeout(this._discoverTimeoutID);
          }
          break;
        case 'didReceiveMessage':
          this._messageCallback(params); // TODO: refine?
          break;
        default:
          return 'nah';
      }
    }

    /**
     * Handle an error resulting from losing connection to a peripheral.
     *
     * This could be due to:
     * - battery depletion
     * - going out of bluetooth range
     * - being powered down
     *
     * Disconnect the socket, and if the extension using this socket has a
     * reset callback, call it. Finally, emit an error to the runtime.
     */
  }, {
    key: "handleDisconnectError",
    value: function handleDisconnectError(/* e */
    ) {
      // log.error(`BT error: ${JSON.stringify(e)}`);

      if (!this._connected) return;
      this.disconnect();
      if (this._resetCallback) {
        this._resetCallback();
      }
      this._runtime.emit(this._runtime.constructor.PERIPHERAL_CONNECTION_LOST_ERROR, {
        message: "Scratch lost connection to",
        extensionId: this._extensionId
      });
    }
  }, {
    key: "_handleRequestError",
    value: function _handleRequestError(/* e */
    ) {
      // log.error(`BT error: ${JSON.stringify(e)}`);

      this._runtime.emit(this._runtime.constructor.PERIPHERAL_REQUEST_ERROR, {
        message: "Scratch lost connection to",
        extensionId: this._extensionId
      });
    }
  }, {
    key: "_handleDiscoverTimeout",
    value: function _handleDiscoverTimeout() {
      if (this._discoverTimeoutID) {
        window.clearTimeout(this._discoverTimeoutID);
      }
      this._runtime.emit(this._runtime.constructor.PERIPHERAL_SCAN_TIMEOUT);
    }
  }]);
}(JSONRPC);
module.exports = BT;

/***/ }),

/***/ "../vm/src/io/clock.js":
/*!*****************************!*\
  !*** ../vm/src/io/clock.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Timer = __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js");
var Clock = /*#__PURE__*/function () {
  function Clock(runtime) {
    _classCallCheck(this, Clock);
    this._projectTimer = new Timer({
      now: function now() {
        return runtime.currentMSecs;
      }
    });
    this._projectTimer.start();
    this._pausedTime = null;
    this._paused = false;
    /**
     * Reference to the owning Runtime.
     * @type{!Runtime}
     */
    this.runtime = runtime;
  }
  return _createClass(Clock, [{
    key: "projectTimer",
    value: function projectTimer() {
      if (this._paused) {
        return this._pausedTime / 1000;
      }
      return this._projectTimer.timeElapsed() / 1000;
    }
  }, {
    key: "pause",
    value: function pause() {
      this._paused = true;
      this._pausedTime = this._projectTimer.timeElapsed();
    }
  }, {
    key: "resume",
    value: function resume() {
      this._paused = false;
      var dt = this._projectTimer.timeElapsed() - this._pausedTime;
      this._projectTimer.startTime += dt;
    }
  }, {
    key: "resetProjectTimer",
    value: function resetProjectTimer() {
      this._projectTimer.start();
    }
  }]);
}();
module.exports = Clock;

/***/ }),

/***/ "../vm/src/io/cloud.js":
/*!*****************************!*\
  !*** ../vm/src/io/cloud.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Variable = __webpack_require__(/*! ../engine/variable */ "../vm/src/engine/variable.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var Cloud = /*#__PURE__*/function () {
  /**
   * @typedef updateVariable
   * @param {string} name The name of the cloud variable to update on the server
   * @param {(string | number)} value The value to update the cloud variable with.
   */

  /**
   * A cloud data provider, responsible for managing the connection to the
   * cloud data server and for posting data about cloud data activity to
   * this IO device.
   * @typedef {object} CloudProvider
   * @property {updateVariable} updateVariable A function which sends a cloud variable
   * update to the cloud data server.
   * @property {Function} requestCloseConnection A function which closes
   * the connection to the cloud data server.
   */

  /**
   * Part of a cloud io data post indicating a cloud variable update.
   * @typedef {object} VarUpdateData
   * @property {string} name The name of the variable to update
   * @property {(number | string)} value The scalar value to update the variable with
   */

  /**
   * A cloud io data post message.
   * @typedef {object} CloudIOData
   * @property {VarUpdateData} varUpdate A {@link VarUpdateData} message indicating
   * a cloud variable update
   */

  /**
   * Cloud IO Device responsible for sending and receiving messages from
   * cloud provider (mananging the cloud server connection) and interacting
   * with cloud variables in the current project.
   * @param {Runtime} runtime The runtime context for this cloud io device.
   */
  function Cloud(runtime) {
    _classCallCheck(this, Cloud);
    /**
     * Reference to the cloud data provider, responsible for mananging
     * the web socket connection to the cloud data server.
     * @type {?CloudProvider}
     */
    this.provider = null;

    /**
     * Reference to the runtime that owns this cloud io device.
     * @type {!Runtime}
     */
    this.runtime = runtime;

    /**
     * Reference to the stage target which owns the cloud variables
     * in the project.
     * @type {?Target}
     */
    this.stage = null;
  }

  /**
   * Set a reference to the cloud data provider.
   * @param {CloudProvider} provider The cloud data provider
   */
  return _createClass(Cloud, [{
    key: "setProvider",
    value: function setProvider(provider) {
      this.provider = provider;
    }

    /**
     * Set a reference to the stage target which owns the
     * cloud variables in the project.
     * @param {Target} stage The stage target
     */
  }, {
    key: "setStage",
    value: function setStage(stage) {
      this.stage = stage;
    }

    /**
     * Handle incoming data to this io device.
     * @param {CloudIOData} data The {@link CloudIOData} object to process
     */
  }, {
    key: "postData",
    value: function postData(data) {
      if (data.varUpdate) {
        this.updateCloudVariable(data.varUpdate);
      }
    }
  }, {
    key: "requestCreateVariable",
    value: function requestCreateVariable(variable) {
      if (this.runtime.canAddCloudVariable()) {
        if (this.provider) {
          this.provider.createVariable(variable.name, variable.value);
          // We'll set the cloud flag and update the
          // cloud variable limit when we actually
          // get a confirmation from the cloud data server
        }
      } // TODO else track creation for later
    }

    /**
     * Request the cloud data provider to update the given variable with
     * the given value. Does nothing if this io device does not have a provider set.
     * @param {string} name The name of the variable to update
     * @param {string | number} value The value to update the variable with
     */
  }, {
    key: "requestUpdateVariable",
    value: function requestUpdateVariable(name, value) {
      if (this.provider) {
        this.provider.updateVariable(name, value);
      }
    }

    /**
     * Request the cloud data provider to rename the variable with the given name
     * to the given new name. Does nothing if this io device does not have a provider set.
     * @param {string} oldName The name of the variable to rename
     * @param {string | number} newName The new name for the variable
     */
  }, {
    key: "requestRenameVariable",
    value: function requestRenameVariable(oldName, newName) {
      if (this.provider) {
        this.provider.renameVariable(oldName, newName);
      }
    }

    /**
     * Request the cloud data provider to delete the variable with the given name
     * Does nothing if this io device does not have a provider set.
     * @param {string} name The name of the variable to delete
     */
  }, {
    key: "requestDeleteVariable",
    value: function requestDeleteVariable(name) {
      if (this.provider) {
        this.provider.deleteVariable(name);
      }
    }

    /**
     * Update a cloud variable in the runtime based on the message received
     * from the cloud provider.
     * @param {VarData} varUpdate A {@link VarData} object describing
     * a cloud variable update received from the cloud data provider.
     */
  }, {
    key: "updateCloudVariable",
    value: function updateCloudVariable(varUpdate) {
      var varName = varUpdate.name;
      var variable = this.stage.lookupVariableByNameAndType(varName, Variable.SCALAR_TYPE);
      if (!variable || !variable.isCloud) {
        log.warn("Received an update for a cloud variable that does not exist: ".concat(varName));
        return;
      }
      variable.value = varUpdate.value;
    }

    /**
     * Request the cloud data provider to close the web socket connection and
     * clear this io device of references to the cloud data provider and the
     * stage.
     */
  }, {
    key: "clear",
    value: function clear() {
      if (!this.provider) return;
      this.provider.requestCloseConnection();
      this.provider = null;
      this.stage = null;
    }
  }]);
}();
module.exports = Cloud;

/***/ }),

/***/ "../vm/src/io/keyboard.js":
/*!********************************!*\
  !*** ../vm/src/io/keyboard.js ***!
  \********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");

/**
 * Names used internally for keys used in scratch, also known as "scratch keys".
 * @enum {string}
 */
var KEY_NAME = {
  SPACE: 'space',
  LEFT: 'left arrow',
  UP: 'up arrow',
  RIGHT: 'right arrow',
  DOWN: 'down arrow',
  ENTER: 'enter',
  // tw: extra keys
  BACKSPACE: 'backspace',
  DELETE: 'delete',
  SHIFT: 'shift',
  CAPS_LOCK: 'caps lock',
  SCROLL_LOCK: 'scroll lock',
  CONTROL: 'control',
  ESCAPE: 'escape',
  INSERT: 'insert',
  HOME: 'home',
  END: 'end',
  PAGE_UP: 'page up',
  PAGE_DOWN: 'page down'
};

/**
 * A set of the names of Scratch keys.
 * @type {Set<string>}
 */
var KEY_NAME_SET = new Set(Object.values(KEY_NAME));
var Keyboard = /*#__PURE__*/function () {
  function Keyboard(runtime) {
    _classCallCheck(this, Keyboard);
    /**
     * List of currently pressed scratch keys.
     * A scratch key is:
     * A key you can press on a keyboard, excluding modifier keys.
     * An uppercase string of length one;
     *     except for special key names for arrow keys and space (e.g. 'left arrow').
     * Can be a non-english unicode letter like: æ ø ש נ 手 廿.
     * @type{Array.<string>}
     */
    this._keysPressed = [];
    /**
     * Reference to the owning Runtime.
     * Can be used, for example, to activate hats.
     * @type{!Runtime}
     */
    this.runtime = runtime;
    // tw: track last pressed key
    this.lastKeyPressed = '';
    this._numeralKeyCodesToStringKey = new Map();
    /**
     * Set of Scratch keys used by the project.
     */
    this._usedKeys = new Set();
  }

  /**
   * Convert from a keyboard event key name to a Scratch key name.
   * @param  {string} keyString the input key string.
   * @return {string} the corresponding Scratch key, or an empty string.
   */
  return _createClass(Keyboard, [{
    key: "_keyStringToScratchKey",
    value: function _keyStringToScratchKey(keyString) {
      keyString = Cast.toString(keyString);
      // Convert space and arrow keys to their Scratch key names.
      switch (keyString) {
        case ' ':
          return KEY_NAME.SPACE;
        case 'ArrowLeft':
        case 'Left':
          return KEY_NAME.LEFT;
        case 'ArrowUp':
        case 'Up':
          return KEY_NAME.UP;
        case 'Right':
        case 'ArrowRight':
          return KEY_NAME.RIGHT;
        case 'Down':
        case 'ArrowDown':
          return KEY_NAME.DOWN;
        case 'Enter':
          return KEY_NAME.ENTER;
        // tw: extra keys
        case 'Backspace':
          return KEY_NAME.BACKSPACE;
        case 'Delete':
          return KEY_NAME.DELETE;
        case 'Shift':
          return KEY_NAME.SHIFT;
        case 'CapsLock':
          return KEY_NAME.CAPS_LOCK;
        case 'ScrollLock':
          return KEY_NAME.SCROLL_LOCK;
        case 'Control':
          return KEY_NAME.CONTROL;
        case 'Escape':
          return KEY_NAME.ESCAPE;
        case 'Insert':
          return KEY_NAME.INSERT;
        case 'Home':
          return KEY_NAME.HOME;
        case 'End':
          return KEY_NAME.END;
        case 'PageUp':
          return KEY_NAME.PAGE_UP;
        case 'PageDown':
          return KEY_NAME.PAGE_DOWN;
      }
      // Ignore modifier keys
      if (keyString.length > 1) {
        return '';
      }
      // tw: toUpperCase() happens later. We need to track key case.
      return keyString;
    }

    /**
     * Convert from a block argument to a Scratch key name.
     * @param  {string} keyArg the input arg.
     * @return {string} the corresponding Scratch key.
     */
  }, {
    key: "_keyArgToScratchKey",
    value: function _keyArgToScratchKey(keyArg) {
      // If a number was dropped in, try to convert from ASCII to Scratch key.
      if (typeof keyArg === 'number') {
        // Check for the ASCII range containing numbers, some punctuation,
        // and uppercase letters.
        if (keyArg >= 48 && keyArg <= 90) {
          return String.fromCharCode(keyArg);
        }
        switch (keyArg) {
          case 32:
            return KEY_NAME.SPACE;
          case 37:
            return KEY_NAME.LEFT;
          case 38:
            return KEY_NAME.UP;
          case 39:
            return KEY_NAME.RIGHT;
          case 40:
            return KEY_NAME.DOWN;
        }
      }
      keyArg = Cast.toString(keyArg);

      // If the arg matches a special key name, return it.
      // No special keys have a name that is only 1 character long, so we can avoid the lookup
      // entirely in the most common case.
      if (keyArg.length > 1 && KEY_NAME_SET.has(keyArg)) {
        return keyArg;
      }

      // Use only the first character.
      if (keyArg.length > 1) {
        keyArg = keyArg[0];
      }

      // Check for the space character.
      if (keyArg === ' ') {
        return KEY_NAME.SPACE;
      }
      // tw: support Scratch 2 hacked blocks
      // There are more hacked blocks but most of them get mangled by Scratch 2 -> Scratch 3 conversion
      if (keyArg === '\r') {
        // this probably belongs upstream
        return KEY_NAME.ENTER;
      }
      if (keyArg === "\x1B") {
        return KEY_NAME.ESCAPE;
      }
      return keyArg.toUpperCase();
    }

    /**
     * Keyboard DOM event handler.
     * @param  {object} data Data from DOM event.
     */
  }, {
    key: "postData",
    value: function postData(data) {
      if (!data.key) return;
      // tw: convert single letter keys to uppercase because of changes in _keyStringToScratchKey
      var scratchKeyCased = this._keyStringToScratchKey(data.key);
      var scratchKey = scratchKeyCased.length === 1 ? scratchKeyCased.toUpperCase() : scratchKeyCased;
      if (scratchKey === '') return;
      var index = this._keysPressed.indexOf(scratchKey);
      if (data.isDown) {
        // tw: track last pressed key
        this.lastKeyPressed = scratchKeyCased;
        this.runtime.emit('KEY_PRESSED', scratchKey);
        // If not already present, add to the list.
        if (index < 0) {
          this._keysPressed.push(scratchKey);
        }
      } else if (index > -1) {
        // If already present, remove from the list.
        this._keysPressed.splice(index, 1);
      }
      // Fix for https://github.com/LLK/scratch-vm/issues/2271
      if (Object.prototype.hasOwnProperty.call(data, 'keyCode')) {
        var keyCode = data.keyCode;
        if (this._numeralKeyCodesToStringKey.has(keyCode)) {
          var lastKeyOfSameCode = this._numeralKeyCodesToStringKey.get(keyCode);
          if (lastKeyOfSameCode !== scratchKey) {
            var indexToUnpress = this._keysPressed.indexOf(lastKeyOfSameCode);
            if (indexToUnpress !== -1) {
              this._keysPressed.splice(indexToUnpress, 1);
            }
          }
        }
        this._numeralKeyCodesToStringKey.set(keyCode, scratchKey);
      }
    }

    /**
     * Get key down state for a specified key.
     * @param  {Any} keyArg key argument.
     * @return {boolean} Is the specified key down?
     */
  }, {
    key: "getKeyIsDown",
    value: function getKeyIsDown(keyArg) {
      if (keyArg === 'any') {
        return this._keysPressed.length > 0;
      }
      var scratchKey = this._keyArgToScratchKey(keyArg);
      this._usedKeys.add(scratchKey);
      return this._keysPressed.indexOf(scratchKey) > -1;
    }

    // tw: expose last pressed key
  }, {
    key: "getLastKeyPressed",
    value: function getLastKeyPressed() {
      return this.lastKeyPressed;
    }

    /**
     * @param {string} scratchKey Scratch key
     * @returns {boolean} true if the project has used this key
     */
  }, {
    key: "hasUsedKey",
    value: function hasUsedKey(scratchKey) {
      return this._usedKeys.has(scratchKey);
    }
  }]);
}();
module.exports = Keyboard;

/***/ }),

/***/ "../vm/src/io/mouse.js":
/*!*****************************!*\
  !*** ../vm/src/io/mouse.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");
var roundToThreeDecimals = function roundToThreeDecimals(number) {
  return Math.round(number * 1000) / 1000;
};
var Mouse = /*#__PURE__*/function () {
  function Mouse(runtime) {
    _classCallCheck(this, Mouse);
    this._clientX = 0;
    this._clientY = 0;
    this._scratchX = 0;
    this._scratchY = 0;
    this._buttons = new Set();
    this.usesRightClickDown = false;
    this._isDown = false;
    /**
     * Reference to the owning Runtime.
     * Can be used, for example, to activate hats.
     * @type{!Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * Activate "event_whenthisspriteclicked" hats.
   * @param  {Target} target to trigger hats on.
   * @private
   */
  return _createClass(Mouse, [{
    key: "_activateClickHats",
    value: function _activateClickHats(target) {
      // Activate both "this sprite clicked" and "stage clicked"
      // They were separated into two opcodes for labeling,
      // but should act the same way.
      // Intentionally not checking isStage to make it work when sharing blocks.
      // @todo the blocks should be converted from one to another when shared
      this.runtime.startHats('event_whenthisspriteclicked', null, target);
      this.runtime.startHats('event_whenstageclicked', null, target);
    }

    /**
     * Find a target by XY location
     * @param  {number} x X position to be sent to the renderer.
     * @param  {number} y Y position to be sent to the renderer.
     * @return {Target} the target at that location
     * @private
     */
  }, {
    key: "_pickTarget",
    value: function _pickTarget(x, y) {
      if (this.runtime.renderer) {
        var drawableID = this.runtime.renderer.pick(x, y);
        for (var i = 0; i < this.runtime.targets.length; i++) {
          var target = this.runtime.targets[i];
          if (Object.prototype.hasOwnProperty.call(target, 'drawableID') && target.drawableID === drawableID) {
            return target;
          }
        }
      }
      // Return the stage if no target was found
      return this.runtime.getTargetForStage();
    }

    /**
     * Mouse DOM event handler.
     * @param  {object} data Data from DOM event.
     */
  }, {
    key: "postData",
    value: function postData(data) {
      if (typeof data.x === 'number') {
        this._clientX = data.x;
        this._scratchX = MathUtil.clamp(this.runtime.stageWidth * (data.x / data.canvasWidth - 0.5), -(this.runtime.stageWidth / 2), this.runtime.stageWidth / 2);
      }
      if (typeof data.y === 'number') {
        this._clientY = data.y;
        this._scratchY = MathUtil.clamp(-this.runtime.stageHeight * (data.y / data.canvasHeight - 0.5), -(this.runtime.stageHeight / 2), this.runtime.stageHeight / 2);
      }
      if (typeof data.isDown !== 'undefined') {
        // If no button specified, default to left button for compatibility
        var button = typeof data.button === 'undefined' ? 0 : data.button;
        if (data.isDown) {
          this._buttons.add(button);
        } else {
          this._buttons["delete"](button);
        }
        var previousDownState = this._isDown;
        this._isDown = data.isDown;

        // Do not trigger if down state has not changed
        if (previousDownState === this._isDown) return;

        // Never trigger click hats at the end of a drag
        if (data.wasDragged) return;

        // Do not activate click hats for clicks outside canvas bounds
        if (!(data.x > 0 && data.x < data.canvasWidth && data.y > 0 && data.y < data.canvasHeight)) return;

        // target will not exist if project is still loading
        var target = this._pickTarget(data.x, data.y);
        if (target) {
          var isNewMouseDown = !previousDownState && this._isDown;
          var isNewMouseUp = previousDownState && !this._isDown;

          // Draggable targets start click hats on mouse up.
          // Non-draggable targets start click hats on mouse down.
          if (target.draggable && isNewMouseUp) {
            this._activateClickHats(target);
          } else if (!target.draggable && isNewMouseDown) {
            this._activateClickHats(target);
          }
        }
      }
    }

    /**
     * Get the X position of the mouse in client coordinates.
     * @return {number} Non-clamped X position of the mouse cursor.
     */
  }, {
    key: "getClientX",
    value: function getClientX() {
      return this._clientX;
    }

    /**
     * Get the Y position of the mouse in client coordinates.
     * @return {number} Non-clamped Y position of the mouse cursor.
     */
  }, {
    key: "getClientY",
    value: function getClientY() {
      return this._clientY;
    }

    /**
     * Get the X position of the mouse in scratch coordinates.
     * @return {number} Clamped and integer rounded X position of the mouse cursor.
     */
  }, {
    key: "getScratchX",
    value: function getScratchX() {
      if (this.runtime.runtimeOptions.miscLimits) {
        return Math.round(this._scratchX);
      }
      return roundToThreeDecimals(this._scratchX);
    }

    /**
     * Get the Y position of the mouse in scratch coordinates.
     * @return {number} Clamped and integer rounded Y position of the mouse cursor.
     */
  }, {
    key: "getScratchY",
    value: function getScratchY() {
      if (this.runtime.runtimeOptions.miscLimits) {
        return Math.round(this._scratchY);
      }
      return roundToThreeDecimals(this._scratchY);
    }

    /**
     * Get the down state of the mouse.
     * @return {boolean} Is the mouse down?
     */
  }, {
    key: "getIsDown",
    value: function getIsDown() {
      return this._isDown;
    }

    /**
     * tw: Get the down state of a specific button of the mouse.
     * @param {number} button The ID of the button. 0 = left, 1 = middle, 2 = right
     * @return {boolean} Is the mouse button down?
     */
  }, {
    key: "getButtonIsDown",
    value: function getButtonIsDown(button) {
      if (button === 2) {
        this.usesRightClickDown = true;
      }
      return this._buttons.has(button);
    }
  }]);
}();
module.exports = Mouse;

/***/ }),

/***/ "../vm/src/io/mouseWheel.js":
/*!**********************************!*\
  !*** ../vm/src/io/mouseWheel.js ***!
  \**********************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var MouseWheel = /*#__PURE__*/function () {
  function MouseWheel(runtime) {
    _classCallCheck(this, MouseWheel);
    /**
     * Reference to the owning Runtime.
     * @type{!Runtime}
     */
    this.runtime = runtime;
  }

  /**
   * Mouse wheel DOM event handler.
   * @param  {object} data Data from DOM event.
   */
  return _createClass(MouseWheel, [{
    key: "postData",
    value: function postData(data) {
      var matchFields = {};
      if (data.deltaY < 0) {
        matchFields.KEY_OPTION = 'up arrow';
      } else if (data.deltaY > 0) {
        matchFields.KEY_OPTION = 'down arrow';
      } else {
        return;
      }
      this.runtime.startHats('event_whenkeypressed', matchFields);
    }
  }]);
}();
module.exports = MouseWheel;

/***/ }),

/***/ "../vm/src/io/userData.js":
/*!********************************!*\
  !*** ../vm/src/io/userData.js ***!
  \********************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var UserData = /*#__PURE__*/function () {
  function UserData() {
    _classCallCheck(this, UserData);
    this._username = '';
  }

  /**
   * Handler for updating the username
   * @param {object} data Data posted to this ioDevice.
   * @property {!string} username The new username.
   */
  return _createClass(UserData, [{
    key: "postData",
    value: function postData(data) {
      this._username = data.username;
    }

    /**
     * Getter for username. Initially empty string, until set via postData.
     * @returns {!string} The current username
     */
  }, {
    key: "getUsername",
    value: function getUsername() {
      return this._username;
    }
  }]);
}();
module.exports = UserData;

/***/ }),

/***/ "../vm/src/io/video.js":
/*!*****************************!*\
  !*** ../vm/src/io/video.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _construct(t, e, r) { if (_isNativeReflectConstruct()) return Reflect.construct.apply(null, arguments); var o = [null]; o.push.apply(o, e); var p = new (t.bind.apply(t, o))(); return r && _setPrototypeOf(p, r.prototype), p; }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var StageLayering = __webpack_require__(/*! ../engine/stage-layering */ "../vm/src/engine/stage-layering.js");
var Video = /*#__PURE__*/function () {
  function Video(runtime) {
    _classCallCheck(this, Video);
    this.runtime = runtime;

    /**
     * @typedef VideoProvider
     * @property {Function} enableVideo - Requests camera access from the user, and upon success,
     * enables the video feed
     * @property {Function} disableVideo - Turns off the video feed
     * @property {Function} getFrame - Return frame data from the video feed in
     * specified dimensions, format, and mirroring.
     */
    this.provider = null;

    /**
     * Id representing a Scratch Renderer skin the video is rendered to for
     * previewing.
     * @type {number}
     */
    this._skinId = -1;

    /**
     * Id for a drawable using the video's skin that will render as a video
     * preview.
     * @type {Drawable}
     */
    this._drawable = -1;

    /**
     * Store the last state of the video transparency ghost effect
     * @type {number}
     */
    this._ghost = 0;

    /**
     * Store a flag that allows the preview to be forced transparent.
     * @type {number}
     */
    this._forceTransparentPreview = false;
  }
  return _createClass(Video, [{
    key: "setProvider",
    value:
    /**
     * Set a video provider for this device. A default implementation of
     * a video provider can be found in scratch-gui/src/lib/video/video-provider
     * @param {VideoProvider} provider - Video provider to use
     */
    function setProvider(provider) {
      this.provider = provider;
    }

    /**
     * Request video be enabled.  Sets up video, creates video skin and enables preview.
     *
     * ioDevices.video.requestVideo()
     *
     * @return {Promise.<Video>} resolves a promise to this IO device when video is ready.
     */
  }, {
    key: "enableVideo",
    value: function enableVideo() {
      var _this = this;
      if (!this.provider) return null;
      return this.provider.enableVideo().then(function () {
        return _this._setupPreview();
      });
    }

    /**
     * Disable video stream (turn video off)
     * @return {void}
     */
  }, {
    key: "disableVideo",
    value: function disableVideo() {
      this._disablePreview();
      if (!this.provider) return null;
      this.provider.disableVideo();
    }

    /**
     * Return frame data from the video feed in a specified dimensions, format, and mirroring.
     *
     * @param {object} frameInfo A descriptor of the frame you would like to receive.
     * @param {Array.<number>} frameInfo.dimensions [width, height] array of numbers.  Defaults to [480,360]
     * @param {boolean} frameInfo.mirror If you specificly want a mirror/non-mirror frame, defaults to the global
     *                                   mirror state (ioDevices.video.mirror)
     * @param {string} frameInfo.format Requested video format, available formats are 'image-data' and 'canvas'.
     * @param {number} frameInfo.cacheTimeout Will reuse previous image data if the time since capture is less than
     *                                        the cacheTimeout.  Defaults to 16ms.
     *
     * @return {ArrayBuffer|Canvas|string|null} Frame data in requested format, null when errors.
     */
  }, {
    key: "getFrame",
    value: function getFrame(_ref) {
      var _ref$dimensions = _ref.dimensions,
        dimensions = _ref$dimensions === void 0 ? Video.DIMENSIONS : _ref$dimensions,
        _ref$mirror = _ref.mirror,
        mirror = _ref$mirror === void 0 ? this.mirror : _ref$mirror,
        _ref$format = _ref.format,
        format = _ref$format === void 0 ? Video.FORMAT_IMAGE_DATA : _ref$format,
        _ref$cacheTimeout = _ref.cacheTimeout,
        cacheTimeout = _ref$cacheTimeout === void 0 ? this._frameCacheTimeout : _ref$cacheTimeout;
      if (this.provider) return this.provider.getFrame({
        dimensions: dimensions,
        mirror: mirror,
        format: format,
        cacheTimeout: cacheTimeout
      });
      return null;
    }

    /**
     * Set the preview ghost effect
     * @param {number} ghost from 0 (visible) to 100 (invisible) - ghost effect
     */
  }, {
    key: "setPreviewGhost",
    value: function setPreviewGhost(ghost) {
      this._ghost = ghost;
      // Confirm that the default value has been changed to a valid id for the drawable
      if (this._drawable !== -1) {
        this.runtime.renderer.updateDrawableEffect(this._drawable, 'ghost', this._forceTransparentPreview ? 100 : ghost);
      }
    }
  }, {
    key: "_disablePreview",
    value: function _disablePreview() {
      if (this._skinId !== -1) {
        this.runtime.renderer.updateBitmapSkin(this._skinId, _construct(ImageData, _toConsumableArray(Video.DIMENSIONS)), 1);
        this.runtime.renderer.updateDrawableVisible(this._drawable, false);
      }
      this._renderPreviewFrame = null;
    }
  }, {
    key: "_setupPreview",
    value: function _setupPreview() {
      var _this2 = this;
      var renderer = this.runtime.renderer;
      if (!renderer) return;
      if (this._skinId === -1 && this._drawable === -1) {
        this._skinId = renderer.createBitmapSkin(_construct(ImageData, _toConsumableArray(Video.DIMENSIONS)), 1);
        this._drawable = renderer.createDrawable(StageLayering.VIDEO_LAYER);
        renderer.updateDrawableSkinId(this._drawable, this._skinId);
        // TW: Video probably contains the user's face. This is private information.
        // This API won't exist if we're using a vanilla scratch-render
        if (renderer.markSkinAsPrivate) {
          renderer.markSkinAsPrivate(this._skinId);
        }
        if (renderer.markDrawableAsNoninteractive) {
          renderer.markDrawableAsNoninteractive(this._drawable);
        }
      }

      // if we haven't already created and started a preview frame render loop, do so
      if (!this._renderPreviewFrame) {
        renderer.updateDrawableEffect(this._drawable, 'ghost', this._forceTransparentPreview ? 100 : this._ghost);
        renderer.updateDrawableVisible(this._drawable, true);
        this._renderPreviewFrame = function () {
          clearTimeout(_this2._renderPreviewTimeout);
          if (!_this2._renderPreviewFrame) {
            return;
          }
          _this2._renderPreviewTimeout = setTimeout(_this2._renderPreviewFrame, _this2.runtime.currentStepTime);
          var imageData = _this2.getFrame({
            format: Video.FORMAT_IMAGE_DATA,
            cacheTimeout: _this2.runtime.currentStepTime
          });
          if (!imageData) {
            renderer.updateBitmapSkin(_this2._skinId, _construct(ImageData, _toConsumableArray(Video.DIMENSIONS)), 1);
            return;
          }
          renderer.updateBitmapSkin(_this2._skinId, imageData, 1);
          _this2.runtime.requestRedraw();
        };
        this._renderPreviewFrame();
      }
    }
  }, {
    key: "videoReady",
    get: function get() {
      if (this.provider) return this.provider.videoReady;
      return false;
    }

    /**
     * Method implemented by all IO devices to allow external changes.
     * The only change available externally is hiding the preview, used e.g. to
     * prevent drawing the preview into project thumbnails.
     * @param {object} - data passed to this IO device.
     * @property {boolean} forceTransparentPreview - whether the preview should be forced transparent.
     */
  }, {
    key: "postData",
    value: function postData(_ref2) {
      var forceTransparentPreview = _ref2.forceTransparentPreview;
      this._forceTransparentPreview = forceTransparentPreview;
      // Setting the ghost to the current value will pick up the forceTransparentPreview
      // flag and override the current ghost. The complexity is to prevent blocks
      // from overriding forceTransparentPreview
      this.setPreviewGhost(this._ghost);
    }
  }], [{
    key: "FORMAT_IMAGE_DATA",
    get: function get() {
      return 'image-data';
    }
  }, {
    key: "FORMAT_CANVAS",
    get: function get() {
      return 'canvas';
    }

    /**
     * Dimensions the video stream is analyzed at after its rendered to the
     * sample canvas.
     * @type {Array.<number>}
     */
  }, {
    key: "DIMENSIONS",
    get: function get() {
      return [480, 360];
    }

    /**
     * Order preview drawable is inserted at in the renderer.
     * @type {number}
     */
  }, {
    key: "ORDER",
    get: function get() {
      return 1;
    }
  }]);
}();
module.exports = Video;

/***/ }),

/***/ "../vm/src/serialization/deserialize-assets.js":
/*!*****************************************************!*\
  !*** ../vm/src/serialization/deserialize-assets.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var JSZip = __webpack_require__(/*! @turbowarp/jszip */ "../vm/node_modules/@turbowarp/jszip/dist/jszip.min.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");

/**
 * Deserializes sound from file into storage cache so that it can
 * be loaded into the runtime.
 * @param {object} sound Descriptor for sound from sb3 file
 * @param {Runtime} runtime The runtime containing the storage to cache the sounds in
 * @param {JSZip} zip The zip containing the sound file being described by `sound`
 * @param {string} assetFileName Optional file name for the given asset
 * (sb2 files have filenames of the form [int].[ext],
 * sb3 files have filenames of the form [md5].[ext])
 * @return {Promise} Promise that resolves after the described sound has been stored
 * into the runtime storage cache, the sound was already stored, or an error has
 * occurred.
 */
var deserializeSound = function deserializeSound(sound, runtime, zip, assetFileName) {
  var fileName = assetFileName ? assetFileName : sound.md5;
  var storage = runtime.storage;
  if (!storage) {
    log.warn('No storage module present; cannot load sound asset: ', fileName);
    return Promise.resolve(null);
  }
  if (!zip) {
    // Zip will not be provided if loading project json from server
    return Promise.resolve(null);
  }
  var soundFile = zip.file(fileName);
  if (!soundFile) {
    // look for assetfile in a flat list of files, or in a folder
    var fileMatch = new RegExp("^([^/]*/)?".concat(fileName, "$"));
    soundFile = zip.file(fileMatch)[0]; // use first matching file
  }
  if (!soundFile) {
    log.error("Could not find sound file associated with the ".concat(sound.name, " sound."));
    return Promise.resolve(null);
  }
  if (!JSZip.support.uint8array) {
    log.error('JSZip uint8array is not supported in this browser.');
    return Promise.resolve(null);
  }
  var dataFormat = sound.dataFormat.toLowerCase() === 'mp3' ? storage.DataFormat.MP3 : storage.DataFormat.WAV;
  return soundFile.async('uint8array').then(function (data) {
    return storage.createAsset(storage.AssetType.Sound, dataFormat, data, null, true);
  }).then(function (asset) {
    sound.asset = asset;
    sound.assetId = asset.assetId;
    sound.md5 = "".concat(asset.assetId, ".").concat(asset.dataFormat);
  });
};

/**
 * Deserializes costume from file into storage cache so that it can
 * be loaded into the runtime.
 * @param {object} costume Descriptor for costume from sb3 file
 * @param {Runtime} runtime The runtime containing the storage to cache the costumes in
 * @param {JSZip} zip The zip containing the costume file being described by `costume`
 * @param {string} assetFileName Optional file name for the given asset
 * (sb2 files have filenames of the form [int].[ext],
 * sb3 files have filenames of the form [md5].[ext])
 * @param {string} textLayerFileName Optional file name for the given asset's text layer
 * (sb2 only; files have filenames of the form [int].png)
 * @return {Promise} Promise that resolves after the described costume has been stored
 * into the runtime storage cache, the costume was already stored, or an error has
 * occurred.
 */
var deserializeCostume = function deserializeCostume(costume, runtime, zip, assetFileName, textLayerFileName) {
  var storage = runtime.storage;
  var assetId = costume.assetId;
  var fileName = assetFileName ? assetFileName : "".concat(assetId, ".").concat(costume.dataFormat);
  if (!storage) {
    log.warn('No storage module present; cannot load costume asset: ', fileName);
    return Promise.resolve(null);
  }
  if (costume.asset) {
    // When uploading a sprite from an image file, the asset data will be provided
    // @todo Cache the asset data somewhere and pull it out here
    return Promise.resolve(storage.createAsset(costume.asset.assetType, costume.asset.dataFormat, new Uint8Array(Object.keys(costume.asset.data).map(function (key) {
      return costume.asset.data[key];
    })), null, true)).then(function (asset) {
      costume.asset = asset;
      costume.assetId = asset.assetId;
      costume.md5 = "".concat(asset.assetId, ".").concat(asset.dataFormat);
    });
  }
  if (!zip) {
    // Zip will not be provided if loading project json from server
    return Promise.resolve(null);
  }
  var costumeFile = zip.file(fileName);
  if (!costumeFile) {
    // look for assetfile in a flat list of files, or in a folder
    var fileMatch = new RegExp("^([^/]*/)?".concat(fileName, "$"));
    costumeFile = zip.file(fileMatch)[0]; // use the first matched file
  }
  if (!costumeFile) {
    log.error("Could not find costume file associated with the ".concat(costume.name, " costume."));
    return Promise.resolve(null);
  }
  var assetType = null;
  var costumeFormat = costume.dataFormat.toLowerCase();
  if (costumeFormat === 'svg') {
    assetType = storage.AssetType.ImageVector;
  } else if (['png', 'bmp', 'jpeg', 'jpg', 'gif'].indexOf(costumeFormat) >= 0) {
    assetType = storage.AssetType.ImageBitmap;
  } else {
    log.error("Unexpected file format for costume: ".concat(costumeFormat));
  }
  if (!JSZip.support.uint8array) {
    log.error('JSZip uint8array is not supported in this browser.');
    return Promise.resolve(null);
  }

  // textLayerMD5 exists if there is a text layer, which is a png of text from Scratch 1.4
  // that was opened in Scratch 2.0. In this case, set costume.textLayerAsset.
  var textLayerFilePromise;
  if (costume.textLayerMD5) {
    var textLayerFile = zip.file(textLayerFileName);
    if (!textLayerFile) {
      log.error("Could not find text layer file associated with the ".concat(costume.name, " costume."));
      return Promise.resolve(null);
    }
    textLayerFilePromise = textLayerFile.async('uint8array').then(function (data) {
      return storage.createAsset(storage.AssetType.ImageBitmap, 'png', data, costume.textLayerMD5);
    }).then(function (asset) {
      costume.textLayerAsset = asset;
    });
  } else {
    textLayerFilePromise = Promise.resolve(null);
  }
  return Promise.all([textLayerFilePromise, costumeFile.async('uint8array').then(function (data) {
    return storage.createAsset(assetType,
    // TODO eventually we want to map non-png's to their actual file types?
    costumeFormat, data, null, true);
  }).then(function (asset) {
    costume.asset = asset;
    costume.assetId = asset.assetId;
    costume.md5 = "".concat(asset.assetId, ".").concat(asset.dataFormat);
  })]);
};
module.exports = {
  deserializeSound: deserializeSound,
  deserializeCostume: deserializeCostume
};

/***/ }),

/***/ "../vm/src/serialization/sb2.js":
/*!**************************************!*\
  !*** ../vm/src/serialization/sb2.js ***!
  \**************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n2 = 0, F = function F() {}; return { s: F, n: function n() { return _n2 >= r.length ? { done: !0 } : { done: !1, value: r[_n2++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
/**
 * @fileoverview
 * Partial implementation of an SB2 JSON importer.
 * Parses provided JSON and then generates all needed
 * scratch-vm runtime structures.
 */

var Blocks = __webpack_require__(/*! ../engine/blocks */ "../vm/src/engine/blocks.js");
var RenderedTarget = __webpack_require__(/*! ../sprites/rendered-target */ "../vm/src/sprites/rendered-target.js");
var Sprite = __webpack_require__(/*! ../sprites/sprite */ "../vm/src/sprites/sprite.js");
var Color = __webpack_require__(/*! ../util/color */ "../vm/src/util/color.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");
var specMap = __webpack_require__(/*! ./sb2_specmap */ "../vm/src/serialization/sb2_specmap.js");
var Comment = __webpack_require__(/*! ../engine/comment */ "../vm/src/engine/comment.js");
var Variable = __webpack_require__(/*! ../engine/variable */ "../vm/src/engine/variable.js");
var MonitorRecord = __webpack_require__(/*! ../engine/monitor-record */ "../vm/src/engine/monitor-record.js");
var StageLayering = __webpack_require__(/*! ../engine/stage-layering */ "../vm/src/engine/stage-layering.js");
var ScratchXUtilities = __webpack_require__(/*! ../extension-support/tw-scratchx-utilities */ "../vm/src/extension-support/tw-scratchx-utilities.js");
var _require = __webpack_require__(/*! ../import/load-costume.js */ "../vm/src/import/load-costume.js"),
  loadCostume = _require.loadCostume;
var _require2 = __webpack_require__(/*! ../import/load-sound.js */ "../vm/src/import/load-sound.js"),
  loadSound = _require2.loadSound;
var _require3 = __webpack_require__(/*! ./deserialize-assets.js */ "../vm/src/serialization/deserialize-assets.js"),
  deserializeCostume = _require3.deserializeCostume,
  deserializeSound = _require3.deserializeSound;

// Constants used during deserialization of an SB2 file
var CORE_EXTENSIONS = ['argument', 'control', 'data', 'event', 'looks', 'math', 'motion', 'operator', 'procedures', 'sensing', 'sound'];

// Adjust script coordinates to account for
// larger block size in scratch-blocks.
// @todo: Determine more precisely the right formulas here.
var WORKSPACE_X_SCALE = 1.5;
var WORKSPACE_Y_SCALE = 2.2;

// By examining ScratchX projects, we've found that ScratchX can use either "\u001f" or "."
// to separate the extension name from the extension method opcode eg. "Text To Speech.say"
// eslint-disable-next-line no-control-regex
var SCRATCHX_OPCODE_SEPARATOR = /\u001f|\./;

/**
 * @param {string} opcode
 * @returns {boolean}
 */
var isPossiblyScratchXBlock = function isPossiblyScratchXBlock(opcode) {
  return SCRATCHX_OPCODE_SEPARATOR.test(opcode);
};

/**
 * @param {string} opcode
 * @returns {string}
 */
var mapScratchXOpcode = function mapScratchXOpcode(opcode) {
  var _opcode$split = opcode.split(SCRATCHX_OPCODE_SEPARATOR),
    _opcode$split2 = _slicedToArray(_opcode$split, 2),
    extensionName = _opcode$split2[0],
    extensionMethod = _opcode$split2[1];
  var newOpcodeBase = ScratchXUtilities.generateExtensionId(extensionName);
  return "".concat(newOpcodeBase, "_").concat(extensionMethod);
};

/**
 * @param {object} block
 * @returns {object}
 */
var mapScratchXBlock = function mapScratchXBlock(block) {
  var opcode = block[0];
  var argumentCount = block.length - 1;
  var args = [];
  for (var i = 0; i < argumentCount; i++) {
    args.push({
      type: 'input',
      inputOp: 'text',
      inputName: ScratchXUtilities.argumentIndexToId(i)
    });
  }
  return {
    opcode: mapScratchXOpcode(opcode),
    argMap: args
  };
};

/**
 * Convert a Scratch 2.0 procedure string (e.g., "my_procedure %s %b %n")
 * into an argument map. This allows us to provide the expected inputs
 * to a mutated procedure call.
 * @param {string} procCode Scratch 2.0 procedure string.
 * @return {object} Argument map compatible with those in sb2specmap.
 */
var parseProcedureArgMap = function parseProcedureArgMap(procCode) {
  var argMap = [{} // First item in list is op string.
  ];
  var INPUT_PREFIX = 'input';
  var inputCount = 0;
  // Split by %n, %b, %s.
  var parts = procCode.split(/(?=[^\\]%[nbs])/);
  for (var i = 0; i < parts.length; i++) {
    var part = parts[i].trim();
    if (part.substring(0, 1) === '%') {
      var argType = part.substring(1, 2);
      var arg = {
        type: 'input',
        inputName: INPUT_PREFIX + inputCount++
      };
      if (argType === 'n') {
        arg.inputOp = 'math_number';
      } else if (argType === 's') {
        arg.inputOp = 'text';
      } else if (argType === 'b') {
        arg.inputOp = 'boolean';
      }
      argMap.push(arg);
    }
  }
  return argMap;
};

/**
 * Generate a list of "argument IDs" for procdefs and caller mutations.
 * IDs just end up being `input0`, `input1`, ... which is good enough.
 * @param {string} procCode Scratch 2.0 procedure string.
 * @return {Array.<string>} Array of argument id strings.
 */
var parseProcedureArgIds = function parseProcedureArgIds(procCode) {
  return parseProcedureArgMap(procCode).map(function (arg) {
    return arg.inputName;
  }).filter(function (name) {
    return name;
  }); // Filter out unnamed inputs which are labels
};

/**
 * Flatten a block tree into a block list.
 * Children are temporarily stored on the `block.children` property.
 * @param {Array.<object>} blocks list generated by `parseBlockList`.
 * @return {Array.<object>} Flattened list to be passed to `blocks.createBlock`.
 */
var _flatten = function flatten(blocks) {
  var finalBlocks = [];
  for (var i = 0; i < blocks.length; i++) {
    var block = blocks[i];
    finalBlocks.push(block);
    if (block.children) {
      finalBlocks = finalBlocks.concat(_flatten(block.children));
    }
    delete block.children;
  }
  return finalBlocks;
};

/**
 * Parse any list of blocks from SB2 JSON into a list of VM-format blocks.
 * Could be used to parse a top-level script,
 * a list of blocks in a branch (e.g., in forever),
 * or a list of blocks in an argument (e.g., move [pick random...]).
 * @param {Array.<object>} blockList SB2 JSON-format block list.
 * @param {Function} addBroadcastMsg function to update broadcast message name map
 * @param {Function} getVariableId function to retreive a variable's ID based on name
 * @param {ImportedExtensionsInfo} extensions - (in/out) parsed extension information will be stored here.
 * @param {ParseState} parseState - info on the state of parsing beyond the current block.
 * @param {object<int, Comment>} comments - Comments from sb2 project that need to be attached to blocks.
 * They are indexed in this object by the sb2 flattened block list index indicating
 * which block they should attach to.
 * @param {int} commentIndex The current index of the top block in this list if it were in a flattened
 * list of all blocks for the target
 * @return {Array<Array.<object>|int>} Tuple where first item is the Scratch VM-format block list, and
 * second item is the updated comment index
 */
var parseBlockList = function parseBlockList(blockList, addBroadcastMsg, getVariableId, extensions, parseState, comments, commentIndex) {
  var resultingList = [];
  var previousBlock = null; // For setting next.
  for (var i = 0; i < blockList.length; i++) {
    var block = blockList[i];
    // eslint-disable-next-line no-use-before-define
    var parsedBlockAndComments = _parseBlock3(block, addBroadcastMsg, getVariableId, extensions, parseState, comments, commentIndex);
    var parsedBlock = parsedBlockAndComments[0];
    // Update commentIndex
    commentIndex = parsedBlockAndComments[1];
    if (!parsedBlock) continue;
    if (previousBlock) {
      parsedBlock.parent = previousBlock.id;
      previousBlock.next = parsedBlock.id;
    }
    previousBlock = parsedBlock;
    resultingList.push(parsedBlock);
  }
  return [resultingList, commentIndex];
};

/**
 * Parse a Scratch object's scripts into VM blocks.
 * This should only handle top-level scripts that include X, Y coordinates.
 * @param {!object} scripts Scripts object from SB2 JSON.
 * @param {!Blocks} blocks Blocks object to load parsed blocks into.
 * @param {Function} addBroadcastMsg function to update broadcast message name map
 * @param {Function} getVariableId function to retreive a variable's ID based on name
 * @param {ImportedExtensionsInfo} extensions - (in/out) parsed extension information will be stored here.
 * @param {object} comments Comments that need to be attached to the blocks that need to be parsed
 */
var parseScripts = function parseScripts(scripts, blocks, addBroadcastMsg, getVariableId, extensions, comments) {
  // Keep track of the index of the current script being
  // parsed in order to attach block comments correctly
  var scriptIndexForComment = 0;
  for (var i = 0; i < scripts.length; i++) {
    var script = scripts[i];
    var scriptX = script[0];
    var scriptY = script[1];
    var blockList = script[2];
    var parseState = {};
    var _parseBlockList = parseBlockList(blockList, addBroadcastMsg, getVariableId, extensions, parseState, comments, scriptIndexForComment),
      _parseBlockList2 = _slicedToArray(_parseBlockList, 2),
      parsedBlockList = _parseBlockList2[0],
      newCommentIndex = _parseBlockList2[1];
    scriptIndexForComment = newCommentIndex;
    if (parsedBlockList[0]) {
      parsedBlockList[0].x = scriptX * WORKSPACE_X_SCALE;
      parsedBlockList[0].y = scriptY * WORKSPACE_Y_SCALE;
      parsedBlockList[0].topLevel = true;
      parsedBlockList[0].parent = null;
    }
    // Flatten children and create add the blocks.
    var convertedBlocks = _flatten(parsedBlockList);
    for (var j = 0; j < convertedBlocks.length; j++) {
      blocks.createBlock(convertedBlocks[j]);
    }
  }
};

/**
 * Create a callback for assigning fixed IDs to imported variables
 * Generator stores the global variable mapping in a closure
 * @param {!string} targetId the id of the target to scope the variable to
 * @return {string} variable ID
 */
var generateVariableIdGetter = function () {
  var globalVariableNameMap = {};
  var namer = function namer(targetId, name, type) {
    return "".concat(targetId, "-").concat(StringUtil.replaceUnsafeChars(name), "-").concat(type);
  };
  return function (targetId, topLevel) {
    // Reset the global variable map if topLevel
    if (topLevel) globalVariableNameMap = {};
    return function (name, type) {
      if (topLevel) {
        // Store the name/id pair in the globalVariableNameMap
        globalVariableNameMap["".concat(name, "-").concat(type)] = namer(targetId, name, type);
        return globalVariableNameMap["".concat(name, "-").concat(type)];
      }
      // Not top-level, so first check the global name map
      if (globalVariableNameMap["".concat(name, "-").concat(type)]) return globalVariableNameMap["".concat(name, "-").concat(type)];
      return namer(targetId, name, type);
    };
  };
}();
var globalBroadcastMsgStateGenerator = function () {
  var broadcastMsgNameMap = {};
  var allBroadcastFields = [];
  var emptyStringName = uid();
  return function (topLevel) {
    if (topLevel) broadcastMsgNameMap = {};
    return {
      broadcastMsgMapUpdater: function broadcastMsgMapUpdater(name, field) {
        name = name.toLowerCase();
        if (name === '') {
          name = emptyStringName;
        }
        broadcastMsgNameMap[name] = "broadcastMsgId-".concat(StringUtil.replaceUnsafeChars(name));
        allBroadcastFields.push(field);
        return broadcastMsgNameMap[name];
      },
      globalBroadcastMsgs: broadcastMsgNameMap,
      allBroadcastFields: allBroadcastFields,
      emptyMsgName: emptyStringName
    };
  };
}();

/**
 * Parse a single monitor object and create all its in-memory VM objects.
 *
 * It is important that monitors are parsed last,
 * - after all sprite targets have finished parsing, and
 * - after the rest of the stage has finished parsing.
 *
 * It is specifically important that all the scripts in the project
 * have been parsed and all the relevant targets exist, have uids,
 * and have their variables initialized.
 * Calling this function before these things are true, will result in
 * undefined behavior.
 * @param {!object} object - From-JSON "Monitor object"
 * @param {!Runtime} runtime - (in/out) Runtime object to load monitor info into.
 * @param {!Array.<Target>} targets - Targets have already been parsed.
 * @param {ImportedExtensionsInfo} extensions - (in/out) parsed extension information will be stored here.
 */

var parseMonitorObject = function parseMonitorObject(object, runtime, targets, extensions) {
  // If we can't find the block in the spec map, ignore it.
  // This happens for things like Lego Wedo 1.0 monitors.
  var mapped = specMap[object.cmd];
  if (!mapped) {
    log.warn("Could not find monitor block with opcode: ".concat(object.cmd));
    return;
  }
  // In scratch 2.0, there are two monitors that now correspond to extension
  // blocks (tempo and video motion/direction). In the case of the
  // video motion/direction block, this reporter is not monitorable in Scratch 3.0.
  // In the case of the tempo block, we should import it and load the music extension
  // only when the monitor is actually visible.

  var opcode = specMap[object.cmd].opcode;
  var extIndex = opcode.indexOf('_');
  var extID = opcode.substring(0, extIndex);
  if (extID === 'videoSensing') {
    return;
  } else if (CORE_EXTENSIONS.indexOf(extID) === -1 && extID !== '' && !extensions.extensionIDs.has(extID) && !object.visible) {
    // Don't import this monitor if it refers to a non-core extension that
    // doesn't exist anywhere else in the project and it isn't visible.
    // This should only apply to the tempo block at this point since
    // there are no other sb2 blocks that are now extension monitors.
    return;
  }
  var target = null;
  // List blocks don't come in with their target name set.
  // Find the target by searching for a target with matching variable name/type.
  if (!Object.prototype.hasOwnProperty.call(object, 'target')) {
    var _loop = function _loop() {
      var currTarget = targets[i];
      var listVariables = Object.keys(currTarget.variables).filter(function (key) {
        var variable = currTarget.variables[key];
        return variable.type === Variable.LIST_TYPE && variable.name === object.listName;
      });
      if (listVariables.length > 0) {
        target = currTarget; // Keep this target for later use
        object.target = currTarget.getName(); // Set target name to normalize with other monitors
      }
    };
    for (var i = 0; i < targets.length; i++) {
      _loop();
    }
  }

  // Get the target for this monitor, if not gotten above.
  target = target || targets.filter(function (t) {
    return t.getName() === object.target;
  })[0];
  if (!target) throw new Error('Cannot create monitor for target that cannot be found by name');

  // Create var id getter to make block naming/parsing easier, variables already created.
  var getVariableId = generateVariableIdGetter(target.id, false);
  // eslint-disable-next-line no-use-before-define
  var _parseBlock = _parseBlock3([object.cmd, object.param],
    // Scratch 2 monitor blocks only have one param.
    null,
    // `addBroadcastMsg`, not needed for monitor blocks.
    getVariableId, extensions, {}, null,
    // `comments`, not needed for monitor blocks
    null // `commentIndex`, not needed for monitor blocks
    ),
    _parseBlock2 = _slicedToArray(_parseBlock, 2),
    block = _parseBlock2[0],
    _ = _parseBlock2[1];

  // Monitor blocks have special IDs to match the toolbox obtained from the getId
  // function in the runtime.monitorBlocksInfo. Variable monitors, however,
  // get their IDs from the variable id they reference.
  if (object.cmd === 'getVar:') {
    block.id = getVariableId(object.param, Variable.SCALAR_TYPE);
  } else if (object.cmd === 'contentsOfList:') {
    block.id = getVariableId(object.param, Variable.LIST_TYPE);
  } else if (Object.prototype.hasOwnProperty.call(runtime.monitorBlockInfo, block.opcode)) {
    block.id = runtime.monitorBlockInfo[block.opcode].getId(target.id, block.fields);
  } else {
    // If the opcode can't be found in the runtime monitorBlockInfo,
    // then default to using the block opcode as the id instead.
    // This is for extension monitors, and assumes that extension monitors
    // cannot be sprite specific.
    block.id = block.opcode;
  }

  // Block needs a targetId if it is targetting something other than the stage
  block.targetId = target.isStage ? null : target.id;

  // Property required for running monitored blocks.
  block.isMonitored = object.visible;
  var existingMonitorBlock = runtime.monitorBlocks._blocks[block.id];
  if (existingMonitorBlock) {
    // A monitor block already exists if the toolbox has been loaded and
    // the monitor block is not target specific (because the block gets recycled).
    // Update the existing block with the relevant monitor information.
    existingMonitorBlock.isMonitored = object.visible;
    existingMonitorBlock.targetId = block.targetId;
  } else {
    // Blocks can be created with children, flatten and add to monitorBlocks.
    var newBlocks = _flatten([block]);
    for (var _i = 0; _i < newBlocks.length; _i++) {
      runtime.monitorBlocks.createBlock(newBlocks[_i]);
    }
  }

  // Convert numbered mode into strings for better understandability.
  switch (object.mode) {
    case 1:
      object.mode = 'default';
      break;
    case 2:
      object.mode = 'large';
      break;
    case 3:
      object.mode = 'slider';
      break;
  }

  // Create a monitor record for the runtime's monitorState
  runtime.requestAddMonitor(new MonitorRecord({
    id: block.id,
    targetId: block.targetId,
    spriteName: block.targetId ? object.target : null,
    opcode: block.opcode,
    params: runtime.monitorBlocks._getBlockParams(block),
    value: '',
    mode: object.mode,
    sliderMin: object.sliderMin,
    sliderMax: object.sliderMax,
    isDiscrete: object.isDiscrete,
    x: object.x,
    y: object.y,
    width: object.width,
    height: object.height,
    visible: object.visible
  }));
};

/**
 * Parse the assets of a single "Scratch object" and load them. This
 * preprocesses objects to support loading the data for those assets over a
 * network while the objects are further processed into Blocks, Sprites, and a
 * list of needed Extensions.
 * @param {!object} object - From-JSON "Scratch object:" sprite, stage, watcher.
 * @param {!Runtime} runtime - Runtime object to load all structures into.
 * @param {boolean} topLevel - Whether this is the top-level object (stage).
 * @param {?object} zip - Optional zipped assets for local file import
 * @return {?{costumePromises:Array.<Promise>,soundPromises:Array.<Promise>,soundBank:SoundBank,children:object}}
 *   Object of arrays of promises and child objects for asset objects used in
 *   Sprites. As well as a SoundBank for the sound assets. null for unsupported
 *   objects.
 */
var _parseScratchAssets = function parseScratchAssets(object, runtime, topLevel, zip) {
  if (!Object.prototype.hasOwnProperty.call(object, 'objName')) {
    // Skip parsing monitors. Or any other objects missing objName.
    return null;
  }
  var assets = {
    costumePromises: [],
    soundPromises: [],
    soundBank: runtime.audioEngine && runtime.audioEngine.createBank(),
    children: []
  };

  // Costumes from JSON.
  var costumePromises = assets.costumePromises;
  if (Object.prototype.hasOwnProperty.call(object, 'costumes')) {
    var _loop2 = function _loop2() {
      var costumeSource = object.costumes[i];
      var bitmapResolution = costumeSource.bitmapResolution || 1;
      var costume = {
        name: costumeSource.costumeName,
        bitmapResolution: bitmapResolution,
        rotationCenterX: topLevel ? 240 * bitmapResolution : costumeSource.rotationCenterX,
        rotationCenterY: topLevel ? 180 * bitmapResolution : costumeSource.rotationCenterY,
        // TODO we eventually want this next property to be called
        // md5ext to reflect what it actually contains, however this
        // will be a very extensive change across many repositories
        // and should be done carefully and altogether
        md5: costumeSource.baseLayerMD5,
        skinId: null
      };
      var md5ext = costumeSource.baseLayerMD5;
      var idParts = StringUtil.splitFirst(md5ext, '.');
      var md5 = idParts[0];
      var ext;
      if (idParts.length === 2 && idParts[1]) {
        ext = idParts[1];
      } else {
        // Default to 'png' if baseLayerMD5 is not formatted correctly
        ext = 'png';
        // Fix costume md5 for later
        costume.md5 = "".concat(costume.md5, ".").concat(ext);
      }
      costume.dataFormat = ext;
      costume.assetId = md5;
      if (costumeSource.textLayerMD5) {
        costume.textLayerMD5 = StringUtil.splitFirst(costumeSource.textLayerMD5, '.')[0];
      }
      // If there is no internet connection, or if the asset is not in storage
      // for some reason, and we are doing a local .sb2 import, (e.g. zip is provided)
      // the file name of the costume should be the baseLayerID followed by the file ext
      var assetFileName = "".concat(costumeSource.baseLayerID, ".").concat(ext);
      var textLayerFileName = costumeSource.textLayerID ? "".concat(costumeSource.textLayerID, ".png") : null;
      costumePromises.push(runtime.wrapAssetRequest(function () {
        return deserializeCostume(costume, runtime, zip, assetFileName, textLayerFileName).then(function () {
          return loadCostume(costume.md5, costume, runtime, 2 /* optVersion */);
        });
      }));
    };
    for (var i = 0; i < object.costumes.length; i++) {
      _loop2();
    }
  }
  // Sounds from JSON
  var soundBank = assets.soundBank,
    soundPromises = assets.soundPromises;
  if (Object.prototype.hasOwnProperty.call(object, 'sounds')) {
    var _loop3 = function _loop3() {
      var soundSource = object.sounds[s];
      var sound = {
        name: soundSource.soundName,
        format: soundSource.format,
        rate: soundSource.rate,
        sampleCount: soundSource.sampleCount,
        // TODO we eventually want this next property to be called
        // md5ext to reflect what it actually contains, however this
        // will be a very extensive change across many repositories
        // and should be done carefully and altogether
        // (for example, the audio engine currently relies on this
        // property to be named 'md5')
        md5: soundSource.md5,
        data: null
      };
      var md5ext = soundSource.md5;
      var idParts = StringUtil.splitFirst(md5ext, '.');
      var md5 = idParts[0];
      var ext = idParts[1].toLowerCase();
      sound.dataFormat = ext;
      sound.assetId = md5;
      // If there is no internet connection, or if the asset is not in storage
      // for some reason, and we are doing a local .sb2 import, (e.g. zip is provided)
      // the file name of the sound should be the soundID (provided from the project.json)
      // followed by the file ext
      var assetFileName = "".concat(soundSource.soundID, ".").concat(ext);
      soundPromises.push(runtime.wrapAssetRequest(function () {
        return deserializeSound(sound, runtime, zip, assetFileName).then(function () {
          return loadSound(sound, runtime, soundBank);
        });
      }));
    };
    for (var s = 0; s < object.sounds.length; s++) {
      _loop3();
    }
  }

  // The stage will have child objects; recursively process them.
  var childrenAssets = assets.children;
  if (object.children) {
    for (var m = 0; m < object.children.length; m++) {
      childrenAssets.push(_parseScratchAssets(object.children[m], runtime, false, zip));
    }
  }
  return assets;
};

/**
 * Parse a single "Scratch object" and create all its in-memory VM objects.
 * TODO: parse the "info" section, especially "savedExtensions"
 * @param {!object} object - From-JSON "Scratch object:" sprite, stage, watcher.
 * @param {!Runtime} runtime - Runtime object to load all structures into.
 * @param {ImportedExtensionsInfo} extensions - (in/out) parsed extension information will be stored here.
 * @param {boolean} topLevel - Whether this is the top-level object (stage).
 * @param {?object} zip - Optional zipped assets for local file import
 * @param {object} assets - Promises for assets of this scratch object grouped
 *   into costumes and sounds
 * @return {!Promise.<Array.<Target>>} Promise for the loaded targets when ready, or null for unsupported objects.
 */
var _parseScratchObject = function parseScratchObject(object, runtime, extensions, topLevel, zip, assets) {
  if (!Object.prototype.hasOwnProperty.call(object, 'objName')) {
    if (Object.prototype.hasOwnProperty.call(object, 'listName')) {
      // Shim these objects so they can be processed as monitors
      object.cmd = 'contentsOfList:';
      object.param = object.listName;
      object.mode = 'list';
    }
    // Defer parsing monitors until targets are all parsed
    object.deferredMonitor = true;
    return Promise.resolve(object);
  }

  // Blocks container for this object.
  var blocks = new Blocks(runtime);
  // @todo: For now, load all Scratch objects (stage/sprites) as a Sprite.
  var sprite = new Sprite(blocks, runtime);
  // Sprite/stage name from JSON.
  if (Object.prototype.hasOwnProperty.call(object, 'objName')) {
    if (topLevel && object.objName !== 'Stage') {
      var _iterator = _createForOfIteratorHelper(object.children),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var child = _step.value;
          if (!Object.prototype.hasOwnProperty.call(child, 'objName') && child.target === object.objName) {
            child.target = 'Stage';
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      object.objName = 'Stage';
    }
    sprite.name = object.objName;
  }
  // Costumes from JSON.
  var costumePromises = assets.costumePromises;
  // Sounds from JSON
  var soundBank = assets.soundBank,
    soundPromises = assets.soundPromises;

  // Create the first clone, and load its run-state from JSON.
  var target = sprite.createClone(topLevel ? StageLayering.BACKGROUND_LAYER : StageLayering.SPRITE_LAYER);
  var getVariableId = generateVariableIdGetter(target.id, topLevel);
  var globalBroadcastMsgObj = globalBroadcastMsgStateGenerator(topLevel);
  var addBroadcastMsg = globalBroadcastMsgObj.broadcastMsgMapUpdater;

  // Load target properties from JSON.
  if (Object.prototype.hasOwnProperty.call(object, 'variables')) {
    for (var j = 0; j < object.variables.length; j++) {
      var variable = object.variables[j];
      // A variable is a cloud variable if:
      // - the project says it's a cloud variable, and
      // - it's a stage variable, and
      // - the runtime can support another cloud variable
      var isCloud = variable.isPersistent && topLevel && runtime.canAddCloudVariable();
      var newVariable = new Variable(getVariableId(variable.name, Variable.SCALAR_TYPE), variable.name, Variable.SCALAR_TYPE, isCloud);
      if (isCloud) runtime.addCloudVariable();
      newVariable.value = variable.value;
      target.variables[newVariable.id] = newVariable;
    }
  }

  // If included, parse any and all comments on the object (this includes top-level
  // workspace comments as well as comments attached to specific blocks)
  var blockComments = {};
  if (Object.prototype.hasOwnProperty.call(object, 'scriptComments')) {
    var comments = object.scriptComments.map(function (commentDesc) {
      var _commentDesc = _slicedToArray(commentDesc, 7),
        commentX = _commentDesc[0],
        commentY = _commentDesc[1],
        commentWidth = _commentDesc[2],
        commentHeight = _commentDesc[3],
        commentFullSize = _commentDesc[4],
        flattenedBlockIndex = _commentDesc[5],
        commentText = _commentDesc[6];
      var isBlockComment = commentDesc[5] >= 0;
      var newComment = new Comment(null,
      // generate a new id for this comment
      commentText,
      // text content of sb2 comment
      // Only serialize x & y position of comment if it's a workspace comment
      // If it's a block comment, we'll let scratch-blocks handle positioning
      isBlockComment ? null : commentX * WORKSPACE_X_SCALE, isBlockComment ? null : commentY * WORKSPACE_Y_SCALE, commentWidth * WORKSPACE_X_SCALE, commentHeight * WORKSPACE_Y_SCALE, !commentFullSize);
      if (isBlockComment) {
        // commentDesc[5] refers to the index of the block that this
        // comment is attached to --  in a flattened version of the
        // scripts array.
        // If commentDesc[5] is -1, this is a workspace comment (we don't need to do anything
        // extra at this point), otherwise temporarily save the flattened script array
        // index as the blockId property of the new comment. We will
        // change this to refer to the actual block id of the corresponding
        // block when that block gets created
        newComment.blockId = flattenedBlockIndex;
        // Add this comment to the block comments object with its script index
        // as the key
        if (Object.prototype.hasOwnProperty.call(blockComments, flattenedBlockIndex)) {
          blockComments[flattenedBlockIndex].push(newComment);
        } else {
          blockComments[flattenedBlockIndex] = [newComment];
        }
      }
      return newComment;
    });

    // Add all the comments that were just created to the target.comments,
    // referenced by id
    comments.forEach(function (comment) {
      target.comments[comment.id] = comment;
    });
  }

  // If included, parse any and all scripts/blocks on the object.
  if (Object.prototype.hasOwnProperty.call(object, 'scripts')) {
    parseScripts(object.scripts, blocks, addBroadcastMsg, getVariableId, extensions, blockComments);
  }

  // If there are any comments referring to a numerical block ID, make them
  // workspace comments. These are comments that were originally created as
  // block comments, detached from the block, and then had the associated
  // block deleted.
  // These comments should be imported as workspace comments
  // by making their blockIDs (which currently refer to non-existing blocks)
  // null (See #1452).
  for (var commentIndex in blockComments) {
    var currBlockComments = blockComments[commentIndex];
    currBlockComments.forEach(function (c) {
      if (typeof c.blockId === 'number') {
        c.blockId = null;
      }
    });
  }

  // Update stage specific blocks (e.g. sprite clicked <=> stage clicked)
  blocks.updateTargetSpecificBlocks(topLevel); // topLevel = isStage

  if (Object.prototype.hasOwnProperty.call(object, 'lists')) {
    for (var k = 0; k < object.lists.length; k++) {
      var list = object.lists[k];
      var _newVariable = new Variable(getVariableId(list.listName, Variable.LIST_TYPE), list.listName, Variable.LIST_TYPE, false);
      _newVariable.value = list.contents;
      target.variables[_newVariable.id] = _newVariable;
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'scratchX')) {
    target.x = object.scratchX;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'scratchY')) {
    target.y = object.scratchY;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'direction')) {
    // Sometimes the direction can be outside of the range: LLK/scratch-gui#5806
    // wrapClamp it (like we do on RenderedTarget.setDirection)
    target.direction = MathUtil.wrapClamp(object.direction, -179, 180);
  }
  if (Object.prototype.hasOwnProperty.call(object, 'isDraggable')) {
    target.draggable = object.isDraggable;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'scale')) {
    // SB2 stores as 1.0 = 100%; we use % in the VM.
    target.size = object.scale * 100;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'visible')) {
    target.visible = object.visible;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'currentCostumeIndex')) {
    // Current costume index can sometimes be a floating
    // point number, use Math.floor to come up with an appropriate index
    // and clamp it to the actual number of costumes the object has for good measure.
    target.currentCostume = MathUtil.clamp(Math.floor(object.currentCostumeIndex), 0, object.costumes.length - 1);
  }
  if (Object.prototype.hasOwnProperty.call(object, 'rotationStyle')) {
    if (object.rotationStyle === 'none') {
      target.rotationStyle = RenderedTarget.ROTATION_STYLE_NONE;
    } else if (object.rotationStyle === 'leftRight') {
      target.rotationStyle = RenderedTarget.ROTATION_STYLE_LEFT_RIGHT;
    } else if (object.rotationStyle === 'normal') {
      target.rotationStyle = RenderedTarget.ROTATION_STYLE_ALL_AROUND;
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'tempoBPM')) {
    target.tempo = object.tempoBPM;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'videoAlpha')) {
    // SB2 stores alpha as opacity, where 1.0 is opaque.
    // We convert to a percentage, and invert it so 100% is full transparency.
    target.videoTransparency = 100 - 100 * object.videoAlpha;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'info')) {
    if (Object.prototype.hasOwnProperty.call(object.info, 'videoOn')) {
      if (object.info.videoOn) {
        target.videoState = RenderedTarget.VIDEO_STATE.ON;
      } else {
        target.videoState = RenderedTarget.VIDEO_STATE.OFF;
      }
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'indexInLibrary')) {
    // Temporarily store the 'indexInLibrary' property from the sb2 file
    // so that we can correctly order sprites in the target pane.
    // This will be deleted after we are done parsing and ordering the targets list.
    target.targetPaneOrder = object.indexInLibrary;
  }
  target.isStage = topLevel;
  Promise.all(costumePromises).then(function (costumes) {
    sprite.costumes = costumes;
  });
  Promise.all(soundPromises).then(function (sounds) {
    sprite.sounds = sounds;
    // Make sure if soundBank is undefined, sprite.soundBank is then null.
    sprite.soundBank = soundBank || null;
  });

  // The stage will have child objects; recursively process them.
  var childrenPromises = [];
  if (object.children) {
    for (var m = 0; m < object.children.length; m++) {
      childrenPromises.push(_parseScratchObject(object.children[m], runtime, extensions, false, zip, assets.children[m]));
    }
  }

  // Parse extension list from ScratchX projects.
  if (topLevel) {
    var savedExtensions = object.info && object.info.savedExtensions;
    if (Array.isArray(savedExtensions)) {
      var _iterator2 = _createForOfIteratorHelper(savedExtensions),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var extension = _step2.value;
          var id = ScratchXUtilities.generateExtensionId(extension.extensionName);
          var url = extension.javascriptURL;
          extensions.extensionURLs.set(id, url);
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }
  }
  return Promise.all(costumePromises.concat(soundPromises)).then(function () {
    return Promise.all(childrenPromises).then(function (children) {
      // Need create broadcast msgs as variables after
      // all other targets have finished processing.
      if (target.isStage) {
        var allBroadcastMsgs = globalBroadcastMsgObj.globalBroadcastMsgs;
        var allBroadcastMsgFields = globalBroadcastMsgObj.allBroadcastFields;
        var oldEmptyMsgName = globalBroadcastMsgObj.emptyMsgName;
        if (allBroadcastMsgs[oldEmptyMsgName]) {
          // Find a fresh 'messageN'
          var currIndex = 1;
          while (allBroadcastMsgs["message".concat(currIndex)]) {
            currIndex += 1;
          }
          var newEmptyMsgName = "message".concat(currIndex);
          // Add the new empty message name to the broadcast message
          // name map, and assign it the old id.
          // Then, delete the old entry in map.
          allBroadcastMsgs[newEmptyMsgName] = allBroadcastMsgs[oldEmptyMsgName];
          delete allBroadcastMsgs[oldEmptyMsgName];
          // Now update all the broadcast message fields with
          // the new empty message name.
          for (var i = 0; i < allBroadcastMsgFields.length; i++) {
            if (allBroadcastMsgFields[i].value === '') {
              allBroadcastMsgFields[i].value = newEmptyMsgName;
            }
          }
        }
        // Traverse the broadcast message name map and create
        // broadcast messages as variables on the stage (which is this
        // target).
        for (var msgName in allBroadcastMsgs) {
          var msgId = allBroadcastMsgs[msgName];
          var newMsg = new Variable(msgId, msgName, Variable.BROADCAST_MESSAGE_TYPE, false);
          target.variables[newMsg.id] = newMsg;
        }
      }
      var targets = [target];
      var deferredMonitors = [];
      for (var n = 0; n < children.length; n++) {
        if (children[n]) {
          if (children[n].deferredMonitor) {
            deferredMonitors.push(children[n]);
          } else {
            targets = targets.concat(children[n]);
          }
        }
      }
      // It is important that monitors are parsed last
      // - after all sprite targets have finished parsing
      // - and this is the last thing that happens in the stage parsing
      // It is specifically important that all the scripts in the project
      // have been parsed and all the relevant targets exist, have uids,
      // and have their variables initialized.
      for (var _n = 0; _n < deferredMonitors.length; _n++) {
        parseMonitorObject(deferredMonitors[_n], runtime, targets, extensions);
      }
      return targets;
    });
  });
};
var reorderParsedTargets = function reorderParsedTargets(targets) {
  // Reorder parsed targets based on the temporary targetPaneOrder property
  // and then delete it.

  var reordered = targets.map(function (t, index) {
    t.layerOrder = index;
    return t;
  }).sort(function (a, b) {
    return a.targetPaneOrder - b.targetPaneOrder;
  });

  // Delete the temporary target pane ordering since we shouldn't need it anymore.
  reordered.forEach(function (t) {
    delete t.targetPaneOrder;
  });
  return reordered;
};

/**
 * Top-level handler. Parse provided JSON,
 * and process the top-level object (the stage object).
 * @param {!object} json SB2-format JSON to load.
 * @param {!Runtime} runtime Runtime object to load all structures into.
 * @param {boolean=} optForceSprite If set, treat as sprite (Sprite2).
 * @param {?object} zip Optional zipped assets for local file import
 * @return {Promise.<ImportedProject>} Promise that resolves to the loaded targets when ready.
 */
var sb2import = function sb2import(json, runtime, optForceSprite, zip) {
  var extensions = {
    extensionIDs: new Set(),
    extensionURLs: new Map()
  };
  return Promise.resolve(_parseScratchAssets(json, runtime, !optForceSprite, zip))
  // Force this promise to wait for the next loop in the js tick. Let
  // storage have some time to send off asset requests.
  .then(function (assets) {
    return Promise.resolve(assets);
  }).then(function (assets) {
    return _parseScratchObject(json, runtime, extensions, !optForceSprite, zip, assets);
  }).then(reorderParsedTargets).then(function (targets) {
    return {
      targets: targets,
      extensions: extensions
    };
  });
};

/**
 * Given the sb2 block, inspect the specmap for a translation method or object.
 * @param {!object} block a sb2 formatted block
 * @return {object} specmap block to parse this opcode
 */
var specMapBlock = function specMapBlock(block) {
  var opcode = block[0];
  var mapped = opcode && specMap[opcode];
  if (!mapped) {
    if (opcode && isPossiblyScratchXBlock(opcode)) {
      return mapScratchXBlock(block);
    }
    log.warn("Couldn't find SB2 block: ".concat(opcode));
    return null;
  }
  if (typeof mapped === 'function') {
    return mapped(block);
  }
  return mapped;
};

/**
 * Parse a single SB2 JSON-formatted block and its children.
 * @param {!object} sb2block SB2 JSON-formatted block.
 * @param {Function} addBroadcastMsg function to update broadcast message name map
 * @param {Function} getVariableId function to retrieve a variable's ID based on name
 * @param {ImportedExtensionsInfo} extensions - (in/out) parsed extension information will be stored here.
 * @param {ParseState} parseState - info on the state of parsing beyond the current block.
 * @param {object<int, Comment>} comments - Comments from sb2 project that need to be attached to blocks.
 * They are indexed in this object by the sb2 flattened block list index indicating
 * which block they should attach to.
 * @param {int} commentIndex The comment index for the block to be parsed if it were in a flattened
 * list of all blocks for the target
 * @return {Array.<object|int>} Tuple where first item is the Scratch VM-format block (or null if unsupported object),
 * and second item is the updated comment index (after this block and its children are parsed)
 */
var _parseBlock3 = function parseBlock(sb2block, addBroadcastMsg, getVariableId, extensions, parseState, comments, commentIndex) {
  var commentsForParsedBlock = comments && typeof commentIndex === 'number' && !isNaN(commentIndex) ? comments[commentIndex] : null;
  var blockMetadata = specMapBlock(sb2block);
  if (!blockMetadata) {
    // No block opcode found, exclude this block, increment the commentIndex,
    // make all block comments into workspace comments and send them to zero/zero
    // to prevent serialization issues.
    if (commentsForParsedBlock) {
      commentsForParsedBlock.forEach(function (comment) {
        comment.blockId = null;
        comment.x = comment.y = 0;
      });
    }
    return [null, commentIndex + 1];
  }
  var oldOpcode = sb2block[0];

  // If the block is from an extension, record it.
  var index = blockMetadata.opcode.indexOf('_');
  var prefix = blockMetadata.opcode.substring(0, index);
  if (CORE_EXTENSIONS.indexOf(prefix) === -1) {
    if (prefix !== '') extensions.extensionIDs.add(prefix);
  }

  // Block skeleton.
  var activeBlock = {
    id: uid(),
    // Generate a new block unique ID.
    opcode: blockMetadata.opcode,
    // Converted, e.g. "motion_movesteps".
    inputs: {},
    // Inputs to this block and the blocks they point to.
    fields: {},
    // Fields on this block and their values.
    next: null,
    // Next block.
    shadow: false,
    // No shadow blocks in an SB2 by default.
    children: [] // Store any generated children, flattened in `flatten`.
  };

  // Attach any comments to this block..
  if (commentsForParsedBlock) {
    // Attach only the last comment to the block, make all others workspace comments
    activeBlock.comment = commentsForParsedBlock[commentsForParsedBlock.length - 1].id;
    commentsForParsedBlock.forEach(function (comment) {
      if (comment.id === activeBlock.comment) {
        comment.blockId = activeBlock.id;
      } else {
        // All other comments don't get a block ID and are sent back to zero.
        // This is important, because if they have `null` x/y, serialization breaks.
        comment.blockId = null;
        comment.x = comment.y = 0;
      }
    });
  }
  commentIndex++;
  var parentExpectedArg = parseState.expectedArg;

  // For a procedure call, generate argument map from proc string.
  if (oldOpcode === 'call') {
    blockMetadata.argMap = parseProcedureArgMap(sb2block[1]);
  }
  // Look at the expected arguments in `blockMetadata.argMap.`
  // The basic problem here is to turn positional SB2 arguments into
  // non-positional named Scratch VM arguments.
  for (var i = 0; i < blockMetadata.argMap.length; i++) {
    var expectedArg = blockMetadata.argMap[i];
    var providedArg = sb2block[i + 1]; // (i = 0 is opcode)
    // Whether the input is obscuring a shadow.
    var shadowObscured = false;
    // Positional argument is an input.
    if (expectedArg.type === 'input') {
      // Create a new block and input metadata.
      var inputUid = uid();
      activeBlock.inputs[expectedArg.inputName] = {
        name: expectedArg.inputName,
        block: null,
        shadow: null
      };
      if (_typeof(providedArg) === 'object' && providedArg) {
        // Block or block list occupies the input.
        var innerBlocks = void 0;
        parseState.expectedArg = expectedArg;
        if (_typeof(providedArg[0]) === 'object' && providedArg[0]) {
          // Block list occupies the input.
          var _parseBlockList3 = parseBlockList(providedArg, addBroadcastMsg, getVariableId, extensions, parseState, comments, commentIndex);
          var _parseBlockList4 = _slicedToArray(_parseBlockList3, 2);
          innerBlocks = _parseBlockList4[0];
          commentIndex = _parseBlockList4[1];
        } else {
          // Single block occupies the input.
          var parsedBlockDesc = _parseBlock3(providedArg, addBroadcastMsg, getVariableId, extensions, parseState, comments, commentIndex);
          innerBlocks = parsedBlockDesc[0] ? [parsedBlockDesc[0]] : [];
          // Update commentIndex
          commentIndex = parsedBlockDesc[1];
        }
        parseState.expectedArg = parentExpectedArg;

        // Check if innerBlocks is not an empty list.
        // An empty list indicates that all the inner blocks from the sb2 have
        // unknown opcodes and have been skipped.
        if (innerBlocks.length > 0) {
          var previousBlock = null;
          for (var j = 0; j < innerBlocks.length; j++) {
            if (j === 0) {
              innerBlocks[j].parent = activeBlock.id;
            } else {
              innerBlocks[j].parent = previousBlock;
            }
            previousBlock = innerBlocks[j].id;
          }
          activeBlock.inputs[expectedArg.inputName].block = innerBlocks[0].id;
          activeBlock.children = activeBlock.children.concat(innerBlocks);
        }

        // Obscures any shadow.
        shadowObscured = true;
      }
      // Generate a shadow block to occupy the input.
      if (!expectedArg.inputOp) {
        // Undefined inputOp. inputOp should always be defined for inputs.
        log.warn("Unknown input operation for input ".concat(expectedArg.inputName, " of opcode ").concat(activeBlock.opcode, "."));
        continue;
      }
      if (expectedArg.inputOp === 'boolean' || expectedArg.inputOp === 'substack') {
        // No editable shadow input; e.g., for a boolean.
        continue;
      }
      // Each shadow has a field generated for it automatically.
      // Value to be filled in the field.
      var fieldValue = providedArg;
      // Shadows' field names match the input name, except for these:
      var fieldName = expectedArg.inputName;
      if (expectedArg.inputOp === 'math_number' || expectedArg.inputOp === 'math_whole_number' || expectedArg.inputOp === 'math_positive_number' || expectedArg.inputOp === 'math_integer' || expectedArg.inputOp === 'math_angle') {
        fieldName = 'NUM';
        // Fields are given Scratch 2.0 default values if obscured.
        if (shadowObscured) {
          fieldValue = 10;
        }
      } else if (expectedArg.inputOp === 'text') {
        fieldName = 'TEXT';
        if (shadowObscured) {
          fieldValue = '';
        }
      } else if (expectedArg.inputOp === 'colour_picker') {
        // Convert SB2 color to hex.
        fieldValue = Color.decimalToHex(providedArg);
        fieldName = 'COLOUR';
        if (shadowObscured) {
          fieldValue = '#990000';
        }
      } else if (expectedArg.inputOp === 'event_broadcast_menu') {
        fieldName = 'BROADCAST_OPTION';
        if (shadowObscured) {
          fieldValue = '';
        }
      } else if (expectedArg.inputOp === 'sensing_of_object_menu') {
        if (shadowObscured) {
          fieldValue = '_stage_';
        } else if (fieldValue === 'Stage') {
          fieldValue = '_stage_';
        }
      } else if (expectedArg.inputOp === 'note') {
        if (shadowObscured) {
          fieldValue = 60;
        }
      } else if (expectedArg.inputOp === 'music.menu.DRUM') {
        if (shadowObscured) {
          fieldValue = 1;
        }
      } else if (expectedArg.inputOp === 'music.menu.INSTRUMENT') {
        if (shadowObscured) {
          fieldValue = 1;
        }
      } else if (expectedArg.inputOp === 'videoSensing.menu.ATTRIBUTE') {
        if (shadowObscured) {
          fieldValue = 'motion';
        }
      } else if (expectedArg.inputOp === 'videoSensing.menu.SUBJECT') {
        if (shadowObscured) {
          fieldValue = 'this sprite';
        }
      } else if (expectedArg.inputOp === 'videoSensing.menu.VIDEO_STATE') {
        if (shadowObscured) {
          fieldValue = 'on';
        }
      } else if (shadowObscured) {
        // Filled drop-down menu.
        fieldValue = '';
      }
      var fields = {};
      fields[fieldName] = {
        name: fieldName,
        value: fieldValue
      };
      // event_broadcast_menus have some extra properties to add to the
      // field and a different value than the rest
      if (expectedArg.inputOp === 'event_broadcast_menu') {
        // Need to update the broadcast message name map with
        // the value of this field.
        // Also need to provide the fields[fieldName] object,
        // so that we can later update its value property, e.g.
        // if sb2 message name is empty string, we will later
        // replace this field's value with messageN
        // once we can traverse through all the existing message names
        // and come up with a fresh messageN.
        var broadcastId = addBroadcastMsg(fieldValue, fields[fieldName]);
        fields[fieldName].id = broadcastId;
        fields[fieldName].variableType = expectedArg.variableType;
      }
      activeBlock.children.push({
        id: inputUid,
        opcode: expectedArg.inputOp,
        inputs: {},
        fields: fields,
        next: null,
        topLevel: false,
        parent: activeBlock.id,
        shadow: true
      });
      activeBlock.inputs[expectedArg.inputName].shadow = inputUid;
      // If no block occupying the input, alias to the shadow.
      if (!activeBlock.inputs[expectedArg.inputName].block) {
        activeBlock.inputs[expectedArg.inputName].block = inputUid;
      }
    } else if (expectedArg.type === 'field') {
      // Add as a field on this block.
      activeBlock.fields[expectedArg.fieldName] = {
        name: expectedArg.fieldName,
        value: providedArg
      };
      if (expectedArg.fieldName === 'CURRENTMENU') {
        // In 3.0, the field value of the `sensing_current` block
        // is in all caps.
        activeBlock.fields[expectedArg.fieldName].value = providedArg.toUpperCase();
        if (providedArg === 'day of week') {
          activeBlock.fields[expectedArg.fieldName].value = 'DAYOFWEEK';
        }
      }
      if (expectedArg.fieldName === 'VARIABLE') {
        // Add `id` property to variable fields
        activeBlock.fields[expectedArg.fieldName].id = getVariableId(providedArg, Variable.SCALAR_TYPE);
      } else if (expectedArg.fieldName === 'LIST') {
        // Add `id` property to variable fields
        activeBlock.fields[expectedArg.fieldName].id = getVariableId(providedArg, Variable.LIST_TYPE);
      } else if (expectedArg.fieldName === 'BROADCAST_OPTION') {
        // Add the name in this field to the broadcast msg name map.
        // Also need to provide the fields[fieldName] object,
        // so that we can later update its value property, e.g.
        // if sb2 message name is empty string, we will later
        // replace this field's value with messageN
        // once we can traverse through all the existing message names
        // and come up with a fresh messageN.
        var _broadcastId = addBroadcastMsg(providedArg, activeBlock.fields[expectedArg.fieldName]);
        activeBlock.fields[expectedArg.fieldName].id = _broadcastId;
      }
      var varType = expectedArg.variableType;
      if (typeof varType === 'string') {
        activeBlock.fields[expectedArg.fieldName].variableType = varType;
      }
    }
  }

  // Updates for blocks that have new menus (e.g. in Looks)
  switch (oldOpcode) {
    case 'comeToFront':
      activeBlock.fields.FRONT_BACK = {
        name: 'FRONT_BACK',
        value: 'front'
      };
      break;
    case 'goBackByLayers:':
      activeBlock.fields.FORWARD_BACKWARD = {
        name: 'FORWARD_BACKWARD',
        value: 'backward'
      };
      break;
    case 'backgroundIndex':
      activeBlock.fields.NUMBER_NAME = {
        name: 'NUMBER_NAME',
        value: 'number'
      };
      break;
    case 'sceneName':
      activeBlock.fields.NUMBER_NAME = {
        name: 'NUMBER_NAME',
        value: 'name'
      };
      break;
    case 'costumeIndex':
      activeBlock.fields.NUMBER_NAME = {
        name: 'NUMBER_NAME',
        value: 'number'
      };
      break;
    case 'costumeName':
      activeBlock.fields.NUMBER_NAME = {
        name: 'NUMBER_NAME',
        value: 'name'
      };
      break;
  }

  // Special cases to generate mutations.
  if (oldOpcode === 'stopScripts') {
    // Mutation for stop block: if the argument is 'other scripts',
    // the block needs a next connection.
    if (sb2block[1] === 'other scripts in sprite' || sb2block[1] === 'other scripts in stage') {
      activeBlock.mutation = {
        tagName: 'mutation',
        hasnext: 'true',
        children: []
      };
    }
  } else if (oldOpcode === 'procDef') {
    // Mutation for procedure definition:
    // store all 2.0 proc data.
    var procData = sb2block.slice(1);
    // Create a new block and input metadata.
    var _inputUid = uid();
    var inputName = 'custom_block';
    activeBlock.inputs[inputName] = {
      name: inputName,
      block: _inputUid,
      shadow: _inputUid
    };
    activeBlock.children = [{
      id: _inputUid,
      opcode: 'procedures_prototype',
      inputs: {},
      fields: {},
      next: null,
      shadow: true,
      children: [],
      mutation: {
        tagName: 'mutation',
        proccode: procData[0],
        // e.g., "abc %n %b %s"
        argumentnames: JSON.stringify(procData[1]),
        // e.g. ['arg1', 'arg2']
        argumentids: JSON.stringify(parseProcedureArgIds(procData[0])),
        argumentdefaults: JSON.stringify(procData[2]),
        // e.g., [1, 'abc']
        warp: procData[3],
        // Warp mode, e.g., true/false.
        children: []
      }
    }];
  } else if (oldOpcode === 'call') {
    // Mutation for procedure call:
    // string for proc code (e.g., "abc %n %b %s").
    activeBlock.mutation = {
      tagName: 'mutation',
      children: [],
      proccode: sb2block[1],
      argumentids: JSON.stringify(parseProcedureArgIds(sb2block[1]))
    };
  } else if (oldOpcode === 'getParam') {
    var returnCode = sb2block[2];

    // Ensure the returnCode is "b" if used in a boolean input.
    if (parentExpectedArg && parentExpectedArg.inputOp === 'boolean' && returnCode !== 'b') {
      returnCode = 'b';
    }

    // Assign correct opcode based on the block shape.
    switch (returnCode) {
      case 'r':
        activeBlock.opcode = 'argument_reporter_string_number';
        break;
      case 'b':
        activeBlock.opcode = 'argument_reporter_boolean';
        break;
    }
  }
  return [activeBlock, commentIndex];
};
module.exports = {
  deserialize: sb2import
};

/***/ }),

/***/ "../vm/src/serialization/sb2_specmap.js":
/*!**********************************************!*\
  !*** ../vm/src/serialization/sb2_specmap.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
/**
 * @fileoverview
 * The specMap below handles a few pieces of "translation" work between
 * the SB2 JSON format and the data we need to run a project
 * in the Scratch 3.0 VM.
 * Notably:
 *  - Map 2.0 and 1.4 opcodes (forward:) into 3.0-format (motion_movesteps).
 *  - Map ordered, unnamed args to unordered, named inputs and fields.
 * Keep this up-to-date as 3.0 blocks are renamed, changed, etc.
 * Originally this was generated largely by a hand-guided scripting process.
 * The relevant data lives here:
 * https://github.com/scratchfoundation/scratch-flash/blob/master/src/Specs.as
 * (for the old opcode and argument order).
 * and here:
 * https://github.com/scratchfoundation/scratch-blocks/tree/develop/blocks_vertical
 * (for the new opcodes and argument names).
 * and here:
 * https://github.com/scratchfoundation/scratch-blocks/blob/develop/tests/
 * (for the shadow blocks created for each block).
 * I started with the `commands` array in Specs.as, and discarded irrelevant
 * properties. By hand, I matched the opcode name to the 3.0 opcode.
 * Finally, I filled in the expected arguments as below.
 */

var Variable = __webpack_require__(/*! ../engine/variable */ "../vm/src/engine/variable.js");

/**
 * @typedef {object} SB2SpecMap_blockInfo
 * @property {string} opcode - the Scratch 3.0 block opcode. Use 'extensionID.opcode' for extension opcodes.
 * @property {Array.<SB2SpecMap_argInfo>} argMap - metadata for this block's arguments.
 */

/**
 * @typedef {object} SB2SpecMap_argInfo
 * @property {string} type - the type of this arg (such as 'input' or 'field')
 * @property {string} inputOp - the scratch-blocks shadow type for this arg
 * @property {string} inputName - the name this argument will take when provided to the block implementation
 */

/**
 * Mapping of Scratch 2.0 opcode to Scratch 3.0 block metadata.
 * @type {object.<SB2SpecMap_blockInfo>}
 */
var specMap = {
  'forward:': {
    opcode: 'motion_movesteps',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'STEPS'
    }]
  },
  'turnRight:': {
    opcode: 'motion_turnright',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DEGREES'
    }]
  },
  'turnLeft:': {
    opcode: 'motion_turnleft',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DEGREES'
    }]
  },
  'heading:': {
    opcode: 'motion_pointindirection',
    argMap: [{
      type: 'input',
      inputOp: 'math_angle',
      inputName: 'DIRECTION'
    }]
  },
  'pointTowards:': {
    opcode: 'motion_pointtowards',
    argMap: [{
      type: 'input',
      inputOp: 'motion_pointtowards_menu',
      inputName: 'TOWARDS'
    }]
  },
  'gotoX:y:': {
    opcode: 'motion_gotoxy',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'X'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'Y'
    }]
  },
  'gotoSpriteOrMouse:': {
    opcode: 'motion_goto',
    argMap: [{
      type: 'input',
      inputOp: 'motion_goto_menu',
      inputName: 'TO'
    }]
  },
  'glideSecs:toX:y:elapsed:from:': {
    opcode: 'motion_glidesecstoxy',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SECS'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'X'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'Y'
    }]
  },
  'changeXposBy:': {
    opcode: 'motion_changexby',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DX'
    }]
  },
  'xpos:': {
    opcode: 'motion_setx',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'X'
    }]
  },
  'changeYposBy:': {
    opcode: 'motion_changeyby',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DY'
    }]
  },
  'ypos:': {
    opcode: 'motion_sety',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'Y'
    }]
  },
  'bounceOffEdge': {
    opcode: 'motion_ifonedgebounce',
    argMap: []
  },
  'setRotationStyle': {
    opcode: 'motion_setrotationstyle',
    argMap: [{
      type: 'field',
      fieldName: 'STYLE'
    }]
  },
  'xpos': {
    opcode: 'motion_xposition',
    argMap: []
  },
  'ypos': {
    opcode: 'motion_yposition',
    argMap: []
  },
  'heading': {
    opcode: 'motion_direction',
    argMap: []
  },
  'scrollRight': {
    opcode: 'motion_scroll_right',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DISTANCE'
    }]
  },
  'scrollUp': {
    opcode: 'motion_scroll_up',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DISTANCE'
    }]
  },
  'scrollAlign': {
    opcode: 'motion_align_scene',
    argMap: [{
      type: 'field',
      fieldName: 'ALIGNMENT'
    }]
  },
  'xScroll': {
    opcode: 'motion_xscroll',
    argMap: []
  },
  'yScroll': {
    opcode: 'motion_yscroll',
    argMap: []
  },
  'say:duration:elapsed:from:': {
    opcode: 'looks_sayforsecs',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'MESSAGE'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SECS'
    }]
  },
  'say:': {
    opcode: 'looks_say',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'MESSAGE'
    }]
  },
  'think:duration:elapsed:from:': {
    opcode: 'looks_thinkforsecs',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'MESSAGE'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SECS'
    }]
  },
  'think:': {
    opcode: 'looks_think',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'MESSAGE'
    }]
  },
  'show': {
    opcode: 'looks_show',
    argMap: []
  },
  'hide': {
    opcode: 'looks_hide',
    argMap: []
  },
  'hideAll': {
    opcode: 'looks_hideallsprites',
    argMap: []
  },
  'lookLike:': {
    opcode: 'looks_switchcostumeto',
    argMap: [{
      type: 'input',
      inputOp: 'looks_costume',
      inputName: 'COSTUME'
    }]
  },
  'nextCostume': {
    opcode: 'looks_nextcostume',
    argMap: []
  },
  'startScene': {
    opcode: 'looks_switchbackdropto',
    argMap: [{
      type: 'input',
      inputOp: 'looks_backdrops',
      inputName: 'BACKDROP'
    }]
  },
  'changeGraphicEffect:by:': {
    opcode: 'looks_changeeffectby',
    argMap: [{
      type: 'field',
      fieldName: 'EFFECT'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'CHANGE'
    }]
  },
  'setGraphicEffect:to:': {
    opcode: 'looks_seteffectto',
    argMap: [{
      type: 'field',
      fieldName: 'EFFECT'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'VALUE'
    }]
  },
  'filterReset': {
    opcode: 'looks_cleargraphiceffects',
    argMap: []
  },
  'changeSizeBy:': {
    opcode: 'looks_changesizeby',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'CHANGE'
    }]
  },
  'setSizeTo:': {
    opcode: 'looks_setsizeto',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SIZE'
    }]
  },
  'changeStretchBy:': {
    opcode: 'looks_changestretchby',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'CHANGE'
    }]
  },
  'setStretchTo:': {
    opcode: 'looks_setstretchto',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'STRETCH'
    }]
  },
  'comeToFront': {
    opcode: 'looks_gotofrontback',
    argMap: []
  },
  'goBackByLayers:': {
    opcode: 'looks_goforwardbackwardlayers',
    argMap: [{
      type: 'input',
      inputOp: 'math_integer',
      inputName: 'NUM'
    }]
  },
  'costumeIndex': {
    opcode: 'looks_costumenumbername',
    argMap: []
  },
  'costumeName': {
    opcode: 'looks_costumenumbername',
    argMap: []
  },
  'sceneName': {
    opcode: 'looks_backdropnumbername',
    argMap: []
  },
  'scale': {
    opcode: 'looks_size',
    argMap: []
  },
  'startSceneAndWait': {
    opcode: 'looks_switchbackdroptoandwait',
    argMap: [{
      type: 'input',
      inputOp: 'looks_backdrops',
      inputName: 'BACKDROP'
    }]
  },
  'nextScene': {
    opcode: 'looks_nextbackdrop',
    argMap: []
  },
  'backgroundIndex': {
    opcode: 'looks_backdropnumbername',
    argMap: []
  },
  'playSound:': {
    opcode: 'sound_play',
    argMap: [{
      type: 'input',
      inputOp: 'sound_sounds_menu',
      inputName: 'SOUND_MENU'
    }]
  },
  'doPlaySoundAndWait': {
    opcode: 'sound_playuntildone',
    argMap: [{
      type: 'input',
      inputOp: 'sound_sounds_menu',
      inputName: 'SOUND_MENU'
    }]
  },
  'stopAllSounds': {
    opcode: 'sound_stopallsounds',
    argMap: []
  },
  'playDrum': {
    opcode: 'music_playDrumForBeats',
    argMap: [{
      type: 'input',
      inputOp: 'music_menu_DRUM',
      inputName: 'DRUM'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'BEATS'
    }]
  },
  'drum:duration:elapsed:from:': {
    opcode: 'music_midiPlayDrumForBeats',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'DRUM'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'BEATS'
    }]
  },
  'rest:elapsed:from:': {
    opcode: 'music_restForBeats',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'BEATS'
    }]
  },
  'noteOn:duration:elapsed:from:': {
    opcode: 'music_playNoteForBeats',
    argMap: [{
      type: 'input',
      inputOp: 'note',
      inputName: 'NOTE'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'BEATS'
    }]
  },
  'instrument:': {
    opcode: 'music_setInstrument',
    argMap: [{
      type: 'input',
      inputOp: 'music_menu_INSTRUMENT',
      inputName: 'INSTRUMENT'
    }]
  },
  'midiInstrument:': {
    opcode: 'music_midiSetInstrument',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'INSTRUMENT'
    }]
  },
  'changeVolumeBy:': {
    opcode: 'sound_changevolumeby',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'VOLUME'
    }]
  },
  'setVolumeTo:': {
    opcode: 'sound_setvolumeto',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'VOLUME'
    }]
  },
  'volume': {
    opcode: 'sound_volume',
    argMap: []
  },
  'changeTempoBy:': {
    opcode: 'music_changeTempo',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'TEMPO'
    }]
  },
  'setTempoTo:': {
    opcode: 'music_setTempo',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'TEMPO'
    }]
  },
  'tempo': {
    opcode: 'music_getTempo',
    argMap: []
  },
  'clearPenTrails': {
    opcode: 'pen_clear',
    argMap: []
  },
  'stampCostume': {
    opcode: 'pen_stamp',
    argMap: []
  },
  'putPenDown': {
    opcode: 'pen_penDown',
    argMap: []
  },
  'putPenUp': {
    opcode: 'pen_penUp',
    argMap: []
  },
  'penColor:': {
    opcode: 'pen_setPenColorToColor',
    argMap: [{
      type: 'input',
      inputOp: 'colour_picker',
      inputName: 'COLOR'
    }]
  },
  'changePenHueBy:': {
    opcode: 'pen_changePenHueBy',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'HUE'
    }]
  },
  'setPenHueTo:': {
    opcode: 'pen_setPenHueToNumber',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'HUE'
    }]
  },
  'changePenShadeBy:': {
    opcode: 'pen_changePenShadeBy',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SHADE'
    }]
  },
  'setPenShadeTo:': {
    opcode: 'pen_setPenShadeToNumber',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SHADE'
    }]
  },
  'changePenSizeBy:': {
    opcode: 'pen_changePenSizeBy',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SIZE'
    }]
  },
  'penSize:': {
    opcode: 'pen_setPenSizeTo',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'SIZE'
    }]
  },
  'senseVideoMotion': {
    opcode: 'videoSensing_videoOn',
    argMap: [{
      type: 'input',
      inputOp: 'videoSensing_menu_ATTRIBUTE',
      inputName: 'ATTRIBUTE'
    }, {
      type: 'input',
      inputOp: 'videoSensing_menu_SUBJECT',
      inputName: 'SUBJECT'
    }]
  },
  'whenGreenFlag': {
    opcode: 'event_whenflagclicked',
    argMap: []
  },
  'whenKeyPressed': {
    opcode: 'event_whenkeypressed',
    argMap: [{
      type: 'field',
      fieldName: 'KEY_OPTION'
    }]
  },
  'whenClicked': {
    opcode: 'event_whenthisspriteclicked',
    argMap: []
  },
  'whenSceneStarts': {
    opcode: 'event_whenbackdropswitchesto',
    argMap: [{
      type: 'field',
      fieldName: 'BACKDROP'
    }]
  },
  'whenSensorGreaterThan': function whenSensorGreaterThan(_ref) {
    var _ref2 = _slicedToArray(_ref, 2),
      sensor = _ref2[1];
    if (sensor === 'video motion') {
      return {
        opcode: 'videoSensing_whenMotionGreaterThan',
        argMap: [
        // skip the first arg, since we converted to a video specific sensing block
        {}, {
          type: 'input',
          inputOp: 'math_number',
          inputName: 'REFERENCE'
        }]
      };
    }
    return {
      opcode: 'event_whengreaterthan',
      argMap: [{
        type: 'field',
        fieldName: 'WHENGREATERTHANMENU'
      }, {
        type: 'input',
        inputOp: 'math_number',
        inputName: 'VALUE'
      }]
    };
  },
  'whenIReceive': {
    opcode: 'event_whenbroadcastreceived',
    argMap: [{
      type: 'field',
      fieldName: 'BROADCAST_OPTION',
      variableType: Variable.BROADCAST_MESSAGE_TYPE
    }]
  },
  'broadcast:': {
    opcode: 'event_broadcast',
    argMap: [{
      type: 'input',
      inputOp: 'event_broadcast_menu',
      inputName: 'BROADCAST_INPUT',
      variableType: Variable.BROADCAST_MESSAGE_TYPE
    }]
  },
  'doBroadcastAndWait': {
    opcode: 'event_broadcastandwait',
    argMap: [{
      type: 'input',
      inputOp: 'event_broadcast_menu',
      inputName: 'BROADCAST_INPUT',
      variableType: Variable.BROADCAST_MESSAGE_TYPE
    }]
  },
  'wait:elapsed:from:': {
    opcode: 'control_wait',
    argMap: [{
      type: 'input',
      inputOp: 'math_positive_number',
      inputName: 'DURATION'
    }]
  },
  'doRepeat': {
    opcode: 'control_repeat',
    argMap: [{
      type: 'input',
      inputOp: 'math_whole_number',
      inputName: 'TIMES'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'doForever': {
    opcode: 'control_forever',
    argMap: [{
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'doIf': {
    opcode: 'control_if',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'CONDITION'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'doIfElse': {
    opcode: 'control_if_else',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'CONDITION'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK2'
    }]
  },
  'doWaitUntil': {
    opcode: 'control_wait_until',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'CONDITION'
    }]
  },
  'doUntil': {
    opcode: 'control_repeat_until',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'CONDITION'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'doWhile': {
    opcode: 'control_while',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'CONDITION'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'doForLoop': {
    opcode: 'control_for_each',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE'
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'VALUE'
    }, {
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'stopScripts': {
    opcode: 'control_stop',
    argMap: [{
      type: 'field',
      fieldName: 'STOP_OPTION'
    }]
  },
  'whenCloned': {
    opcode: 'control_start_as_clone',
    argMap: []
  },
  'createCloneOf': {
    opcode: 'control_create_clone_of',
    argMap: [{
      type: 'input',
      inputOp: 'control_create_clone_of_menu',
      inputName: 'CLONE_OPTION'
    }]
  },
  'deleteClone': {
    opcode: 'control_delete_this_clone',
    argMap: []
  },
  'COUNT': {
    opcode: 'control_get_counter',
    argMap: []
  },
  'INCR_COUNT': {
    opcode: 'control_incr_counter',
    argMap: []
  },
  'CLR_COUNT': {
    opcode: 'control_clear_counter',
    argMap: []
  },
  'warpSpeed': {
    opcode: 'control_all_at_once',
    argMap: [{
      type: 'input',
      inputOp: 'substack',
      inputName: 'SUBSTACK'
    }]
  },
  'touching:': {
    opcode: 'sensing_touchingobject',
    argMap: [{
      type: 'input',
      inputOp: 'sensing_touchingobjectmenu',
      inputName: 'TOUCHINGOBJECTMENU'
    }]
  },
  'touchingColor:': {
    opcode: 'sensing_touchingcolor',
    argMap: [{
      type: 'input',
      inputOp: 'colour_picker',
      inputName: 'COLOR'
    }]
  },
  'color:sees:': {
    opcode: 'sensing_coloristouchingcolor',
    argMap: [{
      type: 'input',
      inputOp: 'colour_picker',
      inputName: 'COLOR'
    }, {
      type: 'input',
      inputOp: 'colour_picker',
      inputName: 'COLOR2'
    }]
  },
  'distanceTo:': {
    opcode: 'sensing_distanceto',
    argMap: [{
      type: 'input',
      inputOp: 'sensing_distancetomenu',
      inputName: 'DISTANCETOMENU'
    }]
  },
  'doAsk': {
    opcode: 'sensing_askandwait',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'QUESTION'
    }]
  },
  'answer': {
    opcode: 'sensing_answer',
    argMap: []
  },
  'keyPressed:': {
    opcode: 'sensing_keypressed',
    argMap: [{
      type: 'input',
      inputOp: 'sensing_keyoptions',
      inputName: 'KEY_OPTION'
    }]
  },
  'mousePressed': {
    opcode: 'sensing_mousedown',
    argMap: []
  },
  'mouseX': {
    opcode: 'sensing_mousex',
    argMap: []
  },
  'mouseY': {
    opcode: 'sensing_mousey',
    argMap: []
  },
  'soundLevel': {
    opcode: 'sensing_loudness',
    argMap: []
  },
  'isLoud': {
    opcode: 'sensing_loud',
    argMap: []
  },
  // 'senseVideoMotion': {
  //     opcode: 'sensing_videoon',
  //     argMap: [
  //         {
  //             type: 'input',
  //             inputOp: 'sensing_videoonmenuone',
  //             inputName: 'VIDEOONMENU1'
  //         },
  //         {
  //             type: 'input',
  //             inputOp: 'sensing_videoonmenutwo',
  //             inputName: 'VIDEOONMENU2'
  //         }
  //     ]
  // },
  'setVideoState': {
    opcode: 'videoSensing_videoToggle',
    argMap: [{
      type: 'input',
      inputOp: 'videoSensing_menu_VIDEO_STATE',
      inputName: 'VIDEO_STATE'
    }]
  },
  'setVideoTransparency': {
    opcode: 'videoSensing_setVideoTransparency',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'TRANSPARENCY'
    }]
  },
  'timer': {
    opcode: 'sensing_timer',
    argMap: []
  },
  'timerReset': {
    opcode: 'sensing_resettimer',
    argMap: []
  },
  'getAttribute:of:': {
    opcode: 'sensing_of',
    argMap: [{
      type: 'field',
      fieldName: 'PROPERTY'
    }, {
      type: 'input',
      inputOp: 'sensing_of_object_menu',
      inputName: 'OBJECT'
    }]
  },
  'timeAndDate': {
    opcode: 'sensing_current',
    argMap: [{
      type: 'field',
      fieldName: 'CURRENTMENU'
    }]
  },
  'timestamp': {
    opcode: 'sensing_dayssince2000',
    argMap: []
  },
  'getUserName': {
    opcode: 'sensing_username',
    argMap: []
  },
  'getUserId': {
    opcode: 'sensing_userid',
    argMap: []
  },
  '+': {
    opcode: 'operator_add',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM1'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM2'
    }]
  },
  '-': {
    opcode: 'operator_subtract',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM1'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM2'
    }]
  },
  '*': {
    opcode: 'operator_multiply',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM1'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM2'
    }]
  },
  '/': {
    opcode: 'operator_divide',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM1'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM2'
    }]
  },
  'randomFrom:to:': {
    opcode: 'operator_random',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'FROM'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'TO'
    }]
  },
  '<': {
    opcode: 'operator_lt',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'OPERAND1'
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'OPERAND2'
    }]
  },
  '=': {
    opcode: 'operator_equals',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'OPERAND1'
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'OPERAND2'
    }]
  },
  '>': {
    opcode: 'operator_gt',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'OPERAND1'
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'OPERAND2'
    }]
  },
  '&': {
    opcode: 'operator_and',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'OPERAND1'
    }, {
      type: 'input',
      inputOp: 'boolean',
      inputName: 'OPERAND2'
    }]
  },
  '|': {
    opcode: 'operator_or',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'OPERAND1'
    }, {
      type: 'input',
      inputOp: 'boolean',
      inputName: 'OPERAND2'
    }]
  },
  'not': {
    opcode: 'operator_not',
    argMap: [{
      type: 'input',
      inputOp: 'boolean',
      inputName: 'OPERAND'
    }]
  },
  'concatenate:with:': {
    opcode: 'operator_join',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'STRING1'
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'STRING2'
    }]
  },
  'letter:of:': {
    opcode: 'operator_letter_of',
    argMap: [{
      type: 'input',
      inputOp: 'math_whole_number',
      inputName: 'LETTER'
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'STRING'
    }]
  },
  'stringLength:': {
    opcode: 'operator_length',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'STRING'
    }]
  },
  '%': {
    opcode: 'operator_mod',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM1'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM2'
    }]
  },
  'rounded': {
    opcode: 'operator_round',
    argMap: [{
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM'
    }]
  },
  'computeFunction:of:': {
    opcode: 'operator_mathop',
    argMap: [{
      type: 'field',
      fieldName: 'OPERATOR'
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'NUM'
    }]
  },
  'readVariable': {
    opcode: 'data_variable',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE',
      variableType: Variable.SCALAR_TYPE
    }]
  },
  // Scratch 2 uses this alternative variable getter opcode only in monitors,
  // blocks use the `readVariable` opcode above.
  'getVar:': {
    opcode: 'data_variable',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE',
      variableType: Variable.SCALAR_TYPE
    }]
  },
  'setVar:to:': {
    opcode: 'data_setvariableto',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE',
      variableType: Variable.SCALAR_TYPE
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'VALUE'
    }]
  },
  'changeVar:by:': {
    opcode: 'data_changevariableby',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE',
      variableType: Variable.SCALAR_TYPE
    }, {
      type: 'input',
      inputOp: 'math_number',
      inputName: 'VALUE'
    }]
  },
  'showVariable:': {
    opcode: 'data_showvariable',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE',
      variableType: Variable.SCALAR_TYPE
    }]
  },
  'hideVariable:': {
    opcode: 'data_hidevariable',
    argMap: [{
      type: 'field',
      fieldName: 'VARIABLE',
      variableType: Variable.SCALAR_TYPE
    }]
  },
  'contentsOfList:': {
    opcode: 'data_listcontents',
    argMap: [{
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'append:toList:': {
    opcode: 'data_addtolist',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'ITEM'
    }, {
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'deleteLine:ofList:': {
    opcode: 'data_deleteoflist',
    argMap: [{
      type: 'input',
      inputOp: 'math_integer',
      inputName: 'INDEX'
    }, {
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'insert:at:ofList:': {
    opcode: 'data_insertatlist',
    argMap: [{
      type: 'input',
      inputOp: 'text',
      inputName: 'ITEM'
    }, {
      type: 'input',
      inputOp: 'math_integer',
      inputName: 'INDEX'
    }, {
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'setLine:ofList:to:': {
    opcode: 'data_replaceitemoflist',
    argMap: [{
      type: 'input',
      inputOp: 'math_integer',
      inputName: 'INDEX'
    }, {
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'ITEM'
    }]
  },
  'getLine:ofList:': {
    opcode: 'data_itemoflist',
    argMap: [{
      type: 'input',
      inputOp: 'math_integer',
      inputName: 'INDEX'
    }, {
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'lineCountOfList:': {
    opcode: 'data_lengthoflist',
    argMap: [{
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'list:contains:': {
    opcode: 'data_listcontainsitem',
    argMap: [{
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }, {
      type: 'input',
      inputOp: 'text',
      inputName: 'ITEM'
    }]
  },
  'showList:': {
    opcode: 'data_showlist',
    argMap: [{
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'hideList:': {
    opcode: 'data_hidelist',
    argMap: [{
      type: 'field',
      fieldName: 'LIST',
      variableType: Variable.LIST_TYPE
    }]
  },
  'procDef': {
    opcode: 'procedures_definition',
    argMap: []
  },
  'getParam': {
    // Doesn't map to single opcode. Import step assigns final correct opcode.
    opcode: 'argument_reporter_string_number',
    argMap: [{
      type: 'field',
      fieldName: 'VALUE'
    }]
  },
  'call': {
    opcode: 'procedures_call',
    argMap: []
  }
};

/**
 * Add to the specMap entries for an opcode from a Scratch 2.0 extension. Two entries will be made with the same
 * metadata; this is done to support projects saved by both older and newer versions of the Scratch 2.0 editor.
 * @param {string} sb2Extension - the Scratch 2.0 name of the extension
 * @param {string} sb2Opcode - the Scratch 2.0 opcode
 * @param {SB2SpecMap_blockInfo} blockInfo - the Scratch 3.0 block info
 */
var addExtensionOp = function addExtensionOp(sb2Extension, sb2Opcode, blockInfo) {
  /**
   * This string separates the name of an extension and the name of an opcode in more recent Scratch 2.0 projects.
   * Earlier projects used '.' as a separator, up until we added the 'LEGO WeDo 2.0' extension...
   * @type {string}
   */
  var sep = "\x1F"; // Unicode Unit Separator

  // make one entry for projects saved by recent versions of the Scratch 2.0 editor
  specMap["".concat(sb2Extension).concat(sep).concat(sb2Opcode)] = blockInfo;

  // make a second for projects saved by older versions of the Scratch 2.0 editor
  specMap["".concat(sb2Extension, ".").concat(sb2Opcode)] = blockInfo;
};
var weDo2 = 'LEGO WeDo 2.0';
addExtensionOp(weDo2, 'motorOnFor', {
  opcode: 'wedo2_motorOnFor',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_MOTOR_ID',
    inputName: 'MOTOR_ID'
  }, {
    type: 'input',
    inputOp: 'math_number',
    inputName: 'DURATION'
  }]
});
addExtensionOp(weDo2, 'motorOn', {
  opcode: 'wedo2_motorOn',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_MOTOR_ID',
    inputName: 'MOTOR_ID'
  }]
});
addExtensionOp(weDo2, 'motorOff', {
  opcode: 'wedo2_motorOff',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_MOTOR_ID',
    inputName: 'MOTOR_ID'
  }]
});
addExtensionOp(weDo2, 'startMotorPower', {
  opcode: 'wedo2_startMotorPower',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_MOTOR_ID',
    inputName: 'MOTOR_ID'
  }, {
    type: 'input',
    inputOp: 'math_number',
    inputName: 'POWER'
  }]
});
addExtensionOp(weDo2, 'setMotorDirection', {
  opcode: 'wedo2_setMotorDirection',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_MOTOR_ID',
    inputName: 'MOTOR_ID'
  }, {
    type: 'input',
    inputOp: 'wedo2_menu_MOTOR_DIRECTION',
    inputName: 'MOTOR_DIRECTION'
  }]
});
addExtensionOp(weDo2, 'setLED', {
  opcode: 'wedo2_setLightHue',
  argMap: [{
    type: 'input',
    inputOp: 'math_number',
    inputName: 'HUE'
  }]
});
addExtensionOp(weDo2, 'playNote', {
  opcode: 'wedo2_playNoteFor',
  argMap: [{
    type: 'input',
    inputOp: 'math_number',
    inputName: 'NOTE'
  }, {
    type: 'input',
    inputOp: 'math_number',
    inputName: 'DURATION'
  }]
});
addExtensionOp(weDo2, 'whenDistance', {
  opcode: 'wedo2_whenDistance',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_OP',
    inputName: 'OP'
  }, {
    type: 'input',
    inputOp: 'math_number',
    inputName: 'REFERENCE'
  }]
});
addExtensionOp(weDo2, 'whenTilted', {
  opcode: 'wedo2_whenTilted',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_TILT_DIRECTION_ANY',
    inputName: 'TILT_DIRECTION_ANY'
  }]
});
addExtensionOp(weDo2, 'getDistance', {
  opcode: 'wedo2_getDistance',
  argMap: []
});
addExtensionOp(weDo2, 'isTilted', {
  opcode: 'wedo2_isTilted',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_TILT_DIRECTION_ANY',
    inputName: 'TILT_DIRECTION_ANY'
  }]
});
addExtensionOp(weDo2, 'getTilt', {
  opcode: 'wedo2_getTiltAngle',
  argMap: [{
    type: 'input',
    inputOp: 'wedo2_menu_TILT_DIRECTION',
    inputName: 'TILT_DIRECTION'
  }]
});
module.exports = specMap;

/***/ }),

/***/ "../vm/src/serialization/sb3.js":
/*!**************************************!*\
  !*** ../vm/src/serialization/sb3.js ***!
  \**************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
/**
 * @fileoverview
 * An SB3 serializer and deserializer. Parses provided
 * JSON and then generates all needed scratch-vm runtime structures.
 */

var Runtime = __webpack_require__(/*! ../engine/runtime */ "../vm/src/engine/runtime.js");
var Blocks = __webpack_require__(/*! ../engine/blocks */ "../vm/src/engine/blocks.js");
var Sprite = __webpack_require__(/*! ../sprites/sprite */ "../vm/src/sprites/sprite.js");
var Variable = __webpack_require__(/*! ../engine/variable */ "../vm/src/engine/variable.js");
var Comment = __webpack_require__(/*! ../engine/comment */ "../vm/src/engine/comment.js");
var MonitorRecord = __webpack_require__(/*! ../engine/monitor-record */ "../vm/src/engine/monitor-record.js");
var StageLayering = __webpack_require__(/*! ../engine/stage-layering */ "../vm/src/engine/stage-layering.js");
var log = __webpack_require__(/*! ../util/log */ "../vm/src/util/log.js");
var uid = __webpack_require__(/*! ../util/uid */ "../vm/src/util/uid.js");
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var VariableUtil = __webpack_require__(/*! ../util/variable-util */ "../vm/src/util/variable-util.js");
var compress = __webpack_require__(/*! ./tw-compress-sb3 */ "../vm/src/serialization/tw-compress-sb3.js");
var _require = __webpack_require__(/*! ../import/load-costume.js */ "../vm/src/import/load-costume.js"),
  loadCostume = _require.loadCostume;
var _require2 = __webpack_require__(/*! ../import/load-sound.js */ "../vm/src/import/load-sound.js"),
  loadSound = _require2.loadSound;
var _require3 = __webpack_require__(/*! ./deserialize-assets.js */ "../vm/src/serialization/deserialize-assets.js"),
  deserializeCostume = _require3.deserializeCostume,
  deserializeSound = _require3.deserializeSound;
var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * @typedef {object} ImportedProject
 * @property {Array.<Target>} targets - the imported Scratch 3.0 target objects.
 * @property {ImportedExtensionsInfo} extensions - the ID of each extension actually used by this project.
 */

/**
 * @typedef {object} ImportedExtensionsInfo
 * @property {Set.<string>} extensionIDs - the ID of each extension actually in use by blocks in this project.
 * @property {Map.<string, string>} extensionURLs - map of ID => URL from project metadata. May not match extensionIDs.
 */

// Constants used during serialization and deserialization
var INPUT_SAME_BLOCK_SHADOW = 1; // unobscured shadow
var INPUT_BLOCK_NO_SHADOW = 2; // no shadow
var INPUT_DIFF_BLOCK_SHADOW = 3; // obscured shadow
// There shouldn't be a case where block is null, but shadow is present...

// Constants used during deserialization of an SB3 file
var CORE_EXTENSIONS = ['argument', 'colour', 'control', 'data', 'event', 'looks', 'math', 'motion', 'operator', 'procedures', 'sensing', 'sound'];

// Constants referring to 'primitive' blocks that are usually shadows,
// or in the case of variables and lists, appear quite often in projects
// math_number
var MATH_NUM_PRIMITIVE = 4; // there's no reason these constants can't collide
// math_positive_number
var POSITIVE_NUM_PRIMITIVE = 5; // with the above, but removing duplication for clarity
// math_whole_number
var WHOLE_NUM_PRIMITIVE = 6;
// math_integer
var INTEGER_NUM_PRIMITIVE = 7;
// math_angle
var ANGLE_NUM_PRIMITIVE = 8;
// colour_picker
var COLOR_PICKER_PRIMITIVE = 9;
// text
var TEXT_PRIMITIVE = 10;
// event_broadcast_menu
var BROADCAST_PRIMITIVE = 11;
// data_variable
var VAR_PRIMITIVE = 12;
// data_listcontents
var LIST_PRIMITIVE = 13;

// Map block opcodes to the above primitives and the name of the field we can use
// to find the value of the field
var primitiveOpcodeInfoMap = {
  math_number: [MATH_NUM_PRIMITIVE, 'NUM'],
  math_positive_number: [POSITIVE_NUM_PRIMITIVE, 'NUM'],
  math_whole_number: [WHOLE_NUM_PRIMITIVE, 'NUM'],
  math_integer: [INTEGER_NUM_PRIMITIVE, 'NUM'],
  math_angle: [ANGLE_NUM_PRIMITIVE, 'NUM'],
  colour_picker: [COLOR_PICKER_PRIMITIVE, 'COLOUR'],
  text: [TEXT_PRIMITIVE, 'TEXT'],
  event_broadcast_menu: [BROADCAST_PRIMITIVE, 'BROADCAST_OPTION'],
  data_variable: [VAR_PRIMITIVE, 'VARIABLE'],
  data_listcontents: [LIST_PRIMITIVE, 'LIST']
};

// We don't enforce this limit, but Scratch does, so we need to handle it for compatibility.
var UPSTREAM_MAX_COMMENT_LENGTH = 8000;

/**
 * Serializes primitives described above into a more compact format
 * @param {object} block the block to serialize
 * @return {array} An array representing the information in the block,
 * or null if the given block is not one of the primitives described above.
 */
var serializePrimitiveBlock = function serializePrimitiveBlock(block) {
  // Returns an array represeting a primitive block or null if not one of
  // the primitive types above
  if (hasOwnProperty.call(primitiveOpcodeInfoMap, block.opcode)) {
    var primitiveInfo = primitiveOpcodeInfoMap[block.opcode];
    var primitiveConstant = primitiveInfo[0];
    var fieldName = primitiveInfo[1];
    var field = block.fields[fieldName];
    var primitiveDesc = [primitiveConstant, field.value];
    if (block.opcode === 'event_broadcast_menu') {
      primitiveDesc.push(field.id);
    } else if (block.opcode === 'data_variable' || block.opcode === 'data_listcontents') {
      primitiveDesc.push(field.id);
      if (block.topLevel) {
        primitiveDesc.push(block.x ? Math.round(block.x) : 0);
        primitiveDesc.push(block.y ? Math.round(block.y) : 0);
      }
    }
    return primitiveDesc;
  }
  return null;
};

/**
 * Serializes the inputs field of a block in a compact form using
 * constants described above to represent the relationship between the
 * inputs of this block (e.g. if there is an unobscured shadow, an obscured shadow
 * -- a block plugged into a droppable input -- or, if there is just a block).
 * Based on this relationship, serializes the ids of the block and shadow (if present)
 *
 * @param {object} inputs The inputs to serialize
 * @return {object} An object representing the serialized inputs
 */
var serializeInputs = function serializeInputs(inputs) {
  var obj = Object.create(null);
  for (var inputName in inputs) {
    if (!hasOwnProperty.call(inputs, inputName)) continue;
    // if block and shadow refer to the same block, only serialize one
    if (inputs[inputName].block === inputs[inputName].shadow) {
      // has block and shadow, and they are the same
      obj[inputName] = [INPUT_SAME_BLOCK_SHADOW, inputs[inputName].block];
    } else if (inputs[inputName].shadow === null) {
      // does not have shadow
      obj[inputName] = [INPUT_BLOCK_NO_SHADOW, inputs[inputName].block];
    } else {
      // block and shadow are both present and are different
      obj[inputName] = [INPUT_DIFF_BLOCK_SHADOW, inputs[inputName].block, inputs[inputName].shadow];
    }
  }
  return obj;
};

/**
 * Serialize the fields of a block in a more compact form.
 * @param {object} fields The fields object to serialize
 * @return {object} An object representing the serialized fields
 */
var serializeFields = function serializeFields(fields) {
  var obj = Object.create(null);
  for (var fieldName in fields) {
    if (!hasOwnProperty.call(fields, fieldName)) continue;
    obj[fieldName] = [fields[fieldName].value];
    if (Object.prototype.hasOwnProperty.call(fields[fieldName], 'id')) {
      obj[fieldName].push(fields[fieldName].id);
    }
  }
  return obj;
};

/**
 * Serialize the given block in the SB3 format with some compression of inputs,
 * fields, and primitives.
 * @param {object} block The block to serialize
 * @return {object | array} A serialized representation of the block. This is an
 * array if the block is one of the primitive types described above or an object,
 * if not.
 */
var serializeBlock = function serializeBlock(block) {
  var serializedPrimitive = serializePrimitiveBlock(block);
  if (serializedPrimitive) return serializedPrimitive;
  // If serializedPrimitive is null, proceed with serializing a non-primitive block
  var obj = Object.create(null);
  obj.opcode = block.opcode;
  // NOTE: this is extremely important to serialize even if null;
  // not serializing `next: null` results in strange behavior with block
  // execution
  obj.next = block.next;
  obj.parent = block.parent;
  obj.inputs = serializeInputs(block.inputs);
  obj.fields = serializeFields(block.fields);
  obj.shadow = block.shadow;
  if (block.topLevel) {
    obj.topLevel = true;
    obj.x = block.x ? Math.round(block.x) : 0;
    obj.y = block.y ? Math.round(block.y) : 0;
  } else {
    obj.topLevel = false;
  }
  if (block.mutation) {
    obj.mutation = block.mutation;
  }
  if (block.comment) {
    obj.comment = block.comment;
  }
  return obj;
};

/**
 * Compresses the serialized inputs replacing block/shadow ids that refer to
 * one of the primitives with the primitive itself. E.g.
 *
 * blocks: {
 *      aUidForMyBlock: {
 *          inputs: {
 *               MYINPUT: [1, 'aUidForAnUnobscuredShadowPrimitive']
 *          }
 *      },
 *      aUidForAnUnobscuredShadowPrimitive: [4, 10]
 *      // the above is a primitive representing a 'math_number' with value 10
 * }
 *
 * becomes:
 *
 * blocks: {
 *      aUidForMyBlock: {
 *          inputs: {
 *               MYINPUT: [1, [4, 10]]
 *          }
 *      }
 * }
 * Note: this function modifies the given blocks object in place
 * @param {object} block The block with inputs to compress
 * @param {objec} blocks The object containing all the blocks currently getting serialized
 * @return {object} The serialized block with compressed inputs
 */
var compressInputTree = function compressInputTree(block, blocks) {
  // This is the second pass on the block
  // so the inputs field should be an object of key - array pairs
  var serializedInputs = block.inputs;
  for (var inputName in serializedInputs) {
    // don't need to check for hasOwnProperty because of how we constructed
    // inputs
    var currInput = serializedInputs[inputName];
    // traverse currInput skipping the first element, which describes whether the block
    // and shadow are the same
    for (var i = 1; i < currInput.length; i++) {
      if (!currInput[i]) continue; // need this check b/c block/shadow can be null
      var blockOrShadowID = currInput[i];
      // replace element of currInput directly
      // (modifying input block directly)
      var blockOrShadow = blocks[blockOrShadowID];
      if (Array.isArray(blockOrShadow)) {
        currInput[i] = blockOrShadow;
        // Modifying blocks in place!
        delete blocks[blockOrShadowID];
      }
    }
  }
  return block;
};

/**
 * Get sanitized non-core extension ID for a given sb3 opcode.
 * Note that this should never return a URL. If in the future the SB3 loader supports loading extensions by URL, this
 * ID should be used to (for example) look up the extension's full URL from a table in the SB3's JSON.
 * @param {!string} opcode The opcode to examine for extension.
 * @return {?string} The extension ID, if it exists and is not a core extension.
 */
var getExtensionIdForOpcode = function getExtensionIdForOpcode(opcode) {
  // Allowed ID characters are those matching the regular expression [\w-]: A-Z, a-z, 0-9, and hyphen ("-").
  var index = opcode.indexOf('_');
  var forbiddenSymbols = /[^\w-]/g;
  var prefix = opcode.substring(0, index).replace(forbiddenSymbols, '-');
  if (CORE_EXTENSIONS.indexOf(prefix) === -1) {
    if (prefix !== '') return prefix;
  }
};

/**
 * @param {Set<string>|string[]} extensionIDs Project extension IDs
 * @param {Runtime} runtime
 * @returns {Record<string, string>|null} extension ID -> URL map, or null if no custom extensions.
 */
var getExtensionURLsToSave = function getExtensionURLsToSave(extensionIDs, runtime) {
  // Extension manager only exists when runtime is wrapped by VirtualMachine
  if (!runtime.extensionManager) {
    return null;
  }

  // We'll save the extensions in the format:
  // {
  //   "extensionid": "https://...",
  //   "otherid": "https://..."
  // }
  // Which lets the VM know which URLs correspond to which IDs, which is useful when the project
  // is being loaded. For example, if the extension is eventually converted to a builtin extension
  // or if it is already loaded, then it doesn't need to fetch the script again.
  var extensionURLs = runtime.extensionManager.getExtensionURLs();
  var toSave = {};
  var _iterator = _createForOfIteratorHelper(extensionIDs),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var extension = _step.value;
      var url = extensionURLs[extension];
      if (typeof url === 'string') {
        toSave[extension] = url;
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  if (Object.keys(toSave).length === 0) {
    return null;
  }
  return toSave;
};

/**
 * Serialize the given blocks object (representing all the blocks for the target
 * currently being serialized.)
 * @param {object} blocks The blocks to be serialized
 * @return {Array} An array of the serialized blocks with compressed inputs and
 * compressed primitives and the list of all extension IDs present
 * in the serialized blocks.
 */
var serializeBlocks = function serializeBlocks(blocks) {
  var obj = Object.create(null);
  var extensionIDs = new Set();
  for (var blockID in blocks) {
    if (!Object.prototype.hasOwnProperty.call(blocks, blockID)) continue;
    obj[blockID] = serializeBlock(blocks[blockID], blocks);
    var extensionID = getExtensionIdForOpcode(blocks[blockID].opcode);
    if (extensionID) {
      extensionIDs.add(extensionID);
    }
  }
  // once we have completed a first pass, do a second pass on block inputs
  for (var _blockID in obj) {
    // don't need to do the hasOwnProperty check here since we
    // created an object that doesn't get extra properties/functions
    var serializedBlock = obj[_blockID];
    // caution, this function deletes parts of this object in place as
    // it's traversing it
    obj[_blockID] = compressInputTree(serializedBlock, obj);
    // second pass on connecting primitives to serialized inputs directly
  }
  // Do one last pass and remove any top level shadows (these are caused by
  // a bug: LLK/scratch-vm#1011, and this pass should be removed once that is
  // completely fixed)
  for (var _blockID2 in obj) {
    var _serializedBlock = obj[_blockID2];
    // If the current block is serialized as a primitive (e.g. it's an array
    // instead of an object), AND it is not one of the top level primitives
    // e.g. variable getter or list getter, then it should be deleted as it's
    // a shadow block, and there are no blocks that reference it, otherwise
    // they would have been compressed in the last pass)
    if (Array.isArray(_serializedBlock) && [VAR_PRIMITIVE, LIST_PRIMITIVE].indexOf(_serializedBlock[0]) < 0) {
      log.warn("Found an unexpected top level primitive with block ID: ".concat(_blockID2, "; deleting it from serialized blocks."));
      delete obj[_blockID2];
    }
  }
  return [obj, Array.from(extensionIDs)];
};

/**
 * @param {unknown} blocks Output of serializeStandaloneBlocks
 * @returns {{blocks: Block[], extensionURLs: Map<string, string>}}
 */
var deserializeStandaloneBlocks = function deserializeStandaloneBlocks(blocks) {
  // deep clone to ensure it's safe to modify later
  blocks = JSON.parse(JSON.stringify(blocks));
  if (blocks.extensionURLs) {
    var extensionURLs = new Map();
    for (var _i = 0, _Object$entries = Object.entries(blocks.extensionURLs); _i < _Object$entries.length; _i++) {
      var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
        id = _Object$entries$_i[0],
        url = _Object$entries$_i[1];
      extensionURLs.set(id, url);
    }
    return {
      blocks: blocks.blocks,
      extensionURLs: extensionURLs
    };
  }

  // Vanilla Scratch format is just a list of block objects
  return {
    blocks: blocks,
    extensionURLs: new Map()
  };
};

/**
 * @param {Block[]} blocks List of block objects.
 * @param {Runtime} runtime Runtime
 * @returns {object} Something that can be understood by deserializeStandaloneBlocks
 */
var serializeStandaloneBlocks = function serializeStandaloneBlocks(blocks, runtime) {
  var extensionIDs = new Set();
  var _iterator2 = _createForOfIteratorHelper(blocks),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var block = _step2.value;
      var extensionID = getExtensionIdForOpcode(block.opcode);
      if (extensionID) {
        extensionIDs.add(extensionID);
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  var extensionURLs = getExtensionURLsToSave(extensionIDs, runtime);
  if (extensionURLs) {
    return {
      blocks: blocks,
      // same format as project.json
      extensionURLs: extensionURLs
    };
  }
  // Vanilla Scratch always just uses the block array as-is. To reduce compatibility concerns
  // we too will use that when possible.
  return blocks;
};

/**
 * Serialize the given costume.
 * @param {object} costume The costume to be serialized.
 * @return {object} A serialized representation of the costume.
 */
var serializeCostume = function serializeCostume(costume) {
  var obj = Object.create(null);
  obj.name = costume.name;
  var costumeToSerialize = costume.broken || costume;
  obj.bitmapResolution = costumeToSerialize.bitmapResolution;
  obj.dataFormat = costumeToSerialize.dataFormat.toLowerCase();
  obj.assetId = costumeToSerialize.assetId;

  // serialize this property with the name 'md5ext' because that's
  // what it's actually referring to. TODO runtime objects need to be
  // updated to actually refer to this as 'md5ext' instead of 'md5'
  // but that change should be made carefully since it is very
  // pervasive
  obj.md5ext = costumeToSerialize.md5;
  obj.rotationCenterX = costumeToSerialize.rotationCenterX;
  obj.rotationCenterY = costumeToSerialize.rotationCenterY;
  return obj;
};

/**
 * Serialize the given sound.
 * @param {object} sound The sound to be serialized.
 * @return {object} A serialized representation of the sound.
 */
var serializeSound = function serializeSound(sound) {
  var obj = Object.create(null);
  obj.name = sound.name;
  var soundToSerialize = sound.broken || sound;
  obj.assetId = soundToSerialize.assetId;
  obj.dataFormat = soundToSerialize.dataFormat.toLowerCase();
  obj.format = soundToSerialize.format;
  obj.rate = soundToSerialize.rate;
  obj.sampleCount = soundToSerialize.sampleCount;
  // serialize this property with the name 'md5ext' because that's
  // what it's actually referring to. TODO runtime objects need to be
  // updated to actually refer to this as 'md5ext' instead of 'md5'
  // but that change should be made carefully since it is very
  // pervasive
  obj.md5ext = soundToSerialize.md5;
  return obj;
};

/**
 * Serialize the given variables object.
 * @param {object} variables The variables to be serialized.
 * @return {object} A serialized representation of the variables. They get
 * separated by type to compress the representation of each given variable and
 * reduce duplicate information.
 */
var serializeVariables = function serializeVariables(variables) {
  var obj = Object.create(null);
  // separate out variables into types at the top level so we don't have
  // keep track of a type for each
  obj.variables = Object.create(null);
  obj.lists = Object.create(null);
  obj.broadcasts = Object.create(null);
  for (var varId in variables) {
    var v = variables[varId];
    if (v.type === Variable.BROADCAST_MESSAGE_TYPE) {
      obj.broadcasts[varId] = v.value; // name and value is the same for broadcast msgs
      continue;
    }
    if (v.type === Variable.LIST_TYPE) {
      obj.lists[varId] = [v.name, v.value];
      continue;
    }

    // otherwise should be a scalar type
    obj.variables[varId] = [v.name, v.value];
    // only scalar vars have the potential to be cloud vars
    if (v.isCloud) obj.variables[varId].push(true);
  }
  return obj;
};
var serializeComments = function serializeComments(comments) {
  var obj = Object.create(null);
  for (var commentId in comments) {
    if (!Object.prototype.hasOwnProperty.call(comments, commentId)) continue;
    var comment = comments[commentId];
    var serializedComment = Object.create(null);
    serializedComment.blockId = comment.blockId;
    serializedComment.x = comment.x;
    serializedComment.y = comment.y;
    serializedComment.width = comment.width;
    serializedComment.height = comment.height;
    serializedComment.minimized = comment.minimized;
    if (comment.text.length > UPSTREAM_MAX_COMMENT_LENGTH) {
      // Upstream's scratch-parser will refuse to load projects if the text is too long, so to maximize
      // compatibility and minimize redundancy we'll store a truncated version in .text and the rest in
      // another field
      serializedComment.text = comment.text.substring(0, UPSTREAM_MAX_COMMENT_LENGTH);
      serializedComment.extraText = comment.text.substring(UPSTREAM_MAX_COMMENT_LENGTH);
    } else {
      serializedComment.text = comment.text;
    }
    obj[commentId] = serializedComment;
  }
  return obj;
};

/**
 * Serialize the given target. Only serialize properties that are necessary
 * for saving and loading this target.
 * @param {object} target The target to be serialized.
 * @param {Set} extensions A set of extensions to add extension IDs to
 * @return {object} A serialized representation of the given target.
 */
var serializeTarget = function serializeTarget(target, extensions) {
  var obj = Object.create(null);
  var targetExtensions = [];
  obj.isStage = target.isStage;
  obj.name = obj.isStage ? 'Stage' : target.name;
  var vars = serializeVariables(target.variables);
  obj.variables = vars.variables;
  obj.lists = vars.lists;
  obj.broadcasts = vars.broadcasts;
  var _serializeBlocks = serializeBlocks(target.blocks);
  var _serializeBlocks2 = _slicedToArray(_serializeBlocks, 2);
  obj.blocks = _serializeBlocks2[0];
  targetExtensions = _serializeBlocks2[1];
  obj.comments = serializeComments(target.comments);

  // TODO remove this check/patch when (#1901) is fixed
  if (target.currentCostume < 0 || target.currentCostume >= target.costumes.length) {
    log.warn("currentCostume property for target ".concat(target.name, " is out of range"));
    target.currentCostume = MathUtil.clamp(target.currentCostume, 0, target.costumes.length - 1);
  }
  obj.currentCostume = target.currentCostume;
  obj.costumes = target.costumes.map(serializeCostume);
  obj.sounds = target.sounds.map(serializeSound);
  if (Object.prototype.hasOwnProperty.call(target, 'volume')) obj.volume = target.volume;
  if (Object.prototype.hasOwnProperty.call(target, 'layerOrder')) obj.layerOrder = target.layerOrder;
  if (obj.isStage) {
    // Only the stage should have these properties
    if (Object.prototype.hasOwnProperty.call(target, 'tempo')) {
      obj.tempo = target.tempo;
    }
    if (Object.prototype.hasOwnProperty.call(target, 'videoTransparency')) {
      obj.videoTransparency = target.videoTransparency;
    }
    if (Object.prototype.hasOwnProperty.call(target, 'videoState')) {
      obj.videoState = target.videoState;
    }
    if (Object.prototype.hasOwnProperty.call(target, 'textToSpeechLanguage')) {
      obj.textToSpeechLanguage = target.textToSpeechLanguage;
    }
  } else {
    // The stage does not need the following properties, but sprites should
    obj.visible = target.visible;
    obj.x = target.x;
    obj.y = target.y;
    obj.size = target.size;
    obj.direction = target.direction;
    obj.draggable = target.draggable;
    obj.rotationStyle = target.rotationStyle;
  }

  // Add found extensions to the extensions object
  targetExtensions.forEach(function (extensionId) {
    extensions.add(extensionId);
  });
  return obj;
};

/**
 * @param {Record<string, unknown>} extensionStorage extensionStorage object
 * @param {Set<string>} extensions extension IDs
 * @returns {Record<string, unknown>|null}
 */
var serializeExtensionStorage = function serializeExtensionStorage(extensionStorage, extensions) {
  var result = {};
  var isEmpty = true;
  for (var _i2 = 0, _Object$entries2 = Object.entries(extensionStorage); _i2 < _Object$entries2.length; _i2++) {
    var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i2], 2),
      key = _Object$entries2$_i[0],
      value = _Object$entries2$_i[1];
    if (extensions.has(key) && value !== null && typeof value !== 'undefined') {
      isEmpty = false;
      result[key] = extensionStorage[key];
    }
  }
  if (isEmpty) {
    return null;
  }
  return result;
};
var getSimplifiedLayerOrdering = function getSimplifiedLayerOrdering(targets) {
  var layerOrders = targets.map(function (t) {
    return t.getLayerOrder();
  });
  return MathUtil.reducedSortOrdering(layerOrders);
};
var serializeMonitors = function serializeMonitors(monitors, runtime, extensions) {
  // Monitors position is always stored as position from top-left corner in 480x360 stage.
  var xOffset = (runtime.stageWidth - 480) / 2;
  var yOffset = (runtime.stageHeight - 360) / 2;
  return monitors.valueSeq()
  // Don't include hidden monitors from extensions
  // https://github.com/LLK/scratch-vm/issues/2331
  .filter(function (monitorData) {
    var extensionID = getExtensionIdForOpcode(monitorData.opcode);
    if (!extensionID) {
      // Native block, always safe
      return true;
    }
    if (monitorData.visible) {
      extensions.add(extensionID);
      return true;
    }
    return false;
  }).map(function (monitorData) {
    var serializedMonitor = {
      id: monitorData.id,
      mode: monitorData.mode,
      opcode: monitorData.opcode,
      params: monitorData.params,
      spriteName: monitorData.spriteName,
      value: Array.isArray(monitorData.value) ? [] : 0,
      width: monitorData.width,
      height: monitorData.height,
      x: monitorData.x - xOffset,
      y: monitorData.y - yOffset,
      visible: monitorData.visible
    };
    if (monitorData.mode !== 'list') {
      serializedMonitor.sliderMin = monitorData.sliderMin;
      serializedMonitor.sliderMax = monitorData.sliderMax;
      serializedMonitor.isDiscrete = monitorData.isDiscrete;
    }
    return serializedMonitor;
  });
};

/**
 * Serializes the specified VM runtime.
 * @param {!Runtime} runtime VM runtime instance to be serialized.
 * @param {string=} targetId Optional target id if serializing only a single target
 * @return {object} Serialized runtime instance.
 */
var serialize = function serialize(runtime, targetId) {
  var _ref = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {},
    _ref$allowOptimizatio = _ref.allowOptimization,
    allowOptimization = _ref$allowOptimizatio === void 0 ? true : _ref$allowOptimizatio;
  // Fetch targets
  var obj = Object.create(null);
  // Create extension set to hold extension ids found while serializing targets
  var extensions = new Set();
  var originalTargetsToSerialize = targetId ? [runtime.getTargetById(targetId)] : runtime.targets.filter(function (target) {
    return target.isOriginal;
  });
  var layerOrdering = getSimplifiedLayerOrdering(originalTargetsToSerialize);
  var flattenedOriginalTargets = originalTargetsToSerialize.map(function (t) {
    return t.toJSON();
  });

  // If the renderer is attached, and we're serializing a whole project (not a sprite)
  // add a temporary layerOrder property to each target.
  if (runtime.renderer && !targetId) {
    flattenedOriginalTargets.forEach(function (t, index) {
      t.layerOrder = layerOrdering[index];
    });
  }
  var serializedTargets = flattenedOriginalTargets.map(function (t) {
    return serializeTarget(t, extensions);
  }).map(function (serialized, index) {
    // can't serialize extensionStorage until the list of used extensions is fully known
    var target = originalTargetsToSerialize[index];
    var targetExtensionStorage = serializeExtensionStorage(target.extensionStorage, extensions);
    if (targetExtensionStorage) {
      serialized.extensionStorage = targetExtensionStorage;
    }
    return serialized;
  });
  var fonts = runtime.fontManager.serializeJSON();
  if (targetId) {
    var target = serializedTargets[0];
    if (extensions.size) {
      // Vanilla Scratch doesn't include extensions in sprites, so don't add this if it's not needed
      target.extensions = Array.from(extensions);
    }
    var _extensionURLs = getExtensionURLsToSave(extensions, runtime);
    if (_extensionURLs) {
      target.extensionURLs = _extensionURLs;
    }
    if (fonts) {
      target.customFonts = fonts;
    }
    return serializedTargets[0];
  }
  var globalExtensionStorage = serializeExtensionStorage(runtime.extensionStorage, extensions);
  if (globalExtensionStorage) {
    obj.extensionStorage = globalExtensionStorage;
  }
  obj.targets = serializedTargets;
  obj.monitors = serializeMonitors(runtime.getMonitorState(), runtime, extensions);
  obj.extensions = Array.from(extensions);
  var extensionURLs = getExtensionURLsToSave(extensions, runtime);
  if (extensionURLs) {
    obj.extensionURLs = extensionURLs;
  }
  if (fonts) {
    obj.customFonts = fonts;
  }

  // Assemble metadata
  var meta = Object.create(null);
  meta.semver = '3.0.0';
  // TW: There isn't a good reason to put the full version number in the json, so we don't.
  meta.vm = '0.2.0';
  if (runtime.origin) {
    meta.origin = runtime.origin;
  }

  // Attach full user agent string to metadata if available
  meta.agent = '';
  // TW: Never include full user agent to slightly improve user privacy
  // if (typeof navigator !== 'undefined') meta.agent = navigator.userAgent;

  // TW: Attach copy of platform information
  meta.platform = Object.assign({}, runtime.platform);

  // Assemble payload and return
  obj.meta = meta;
  if (allowOptimization) {
    compress(obj);
  }
  return obj;
};

/**
 * Deserialize a block input descriptors. This is either a
 * block id or a serialized primitive, e.g. an array
 * (see serializePrimitiveBlock function).
 * @param {string | array} inputDescOrId The block input descriptor to be serialized.
 * @param {string} parentId The id of the parent block for this input block.
 * @param {boolean} isShadow Whether or not this input block is a shadow.
 * @param {object} blocks The entire blocks object currently in the process of getting serialized.
 * @return {object} The deserialized input descriptor.
 */
var deserializeInputDesc = function deserializeInputDesc(inputDescOrId, parentId, isShadow, blocks) {
  if (!Array.isArray(inputDescOrId)) return inputDescOrId;
  var primitiveObj = Object.create(null);
  var newId = uid();
  primitiveObj.id = newId;
  primitiveObj.next = null;
  primitiveObj.parent = parentId;
  primitiveObj.shadow = isShadow;
  primitiveObj.inputs = Object.create(null);
  // need a reference to parent id
  switch (inputDescOrId[0]) {
    case MATH_NUM_PRIMITIVE:
      {
        primitiveObj.opcode = 'math_number';
        primitiveObj.fields = {
          NUM: {
            name: 'NUM',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case POSITIVE_NUM_PRIMITIVE:
      {
        primitiveObj.opcode = 'math_positive_number';
        primitiveObj.fields = {
          NUM: {
            name: 'NUM',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case WHOLE_NUM_PRIMITIVE:
      {
        primitiveObj.opcode = 'math_whole_number';
        primitiveObj.fields = {
          NUM: {
            name: 'NUM',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case INTEGER_NUM_PRIMITIVE:
      {
        primitiveObj.opcode = 'math_integer';
        primitiveObj.fields = {
          NUM: {
            name: 'NUM',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case ANGLE_NUM_PRIMITIVE:
      {
        primitiveObj.opcode = 'math_angle';
        primitiveObj.fields = {
          NUM: {
            name: 'NUM',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case COLOR_PICKER_PRIMITIVE:
      {
        primitiveObj.opcode = 'colour_picker';
        primitiveObj.fields = {
          COLOUR: {
            name: 'COLOUR',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case TEXT_PRIMITIVE:
      {
        primitiveObj.opcode = 'text';
        primitiveObj.fields = {
          TEXT: {
            name: 'TEXT',
            value: inputDescOrId[1]
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case BROADCAST_PRIMITIVE:
      {
        primitiveObj.opcode = 'event_broadcast_menu';
        primitiveObj.fields = {
          BROADCAST_OPTION: {
            name: 'BROADCAST_OPTION',
            value: inputDescOrId[1],
            id: inputDescOrId[2],
            variableType: Variable.BROADCAST_MESSAGE_TYPE
          }
        };
        primitiveObj.topLevel = false;
        break;
      }
    case VAR_PRIMITIVE:
      {
        primitiveObj.opcode = 'data_variable';
        primitiveObj.fields = {
          VARIABLE: {
            name: 'VARIABLE',
            value: inputDescOrId[1],
            id: inputDescOrId[2],
            variableType: Variable.SCALAR_TYPE
          }
        };
        if (inputDescOrId.length > 3) {
          primitiveObj.topLevel = true;
          primitiveObj.x = inputDescOrId[3];
          primitiveObj.y = inputDescOrId[4];
        }
        break;
      }
    case LIST_PRIMITIVE:
      {
        primitiveObj.opcode = 'data_listcontents';
        primitiveObj.fields = {
          LIST: {
            name: 'LIST',
            value: inputDescOrId[1],
            id: inputDescOrId[2],
            variableType: Variable.LIST_TYPE
          }
        };
        if (inputDescOrId.length > 3) {
          primitiveObj.topLevel = true;
          primitiveObj.x = inputDescOrId[3];
          primitiveObj.y = inputDescOrId[4];
        }
        break;
      }
    default:
      {
        log.error("Found unknown primitive type during deserialization: ".concat(JSON.stringify(inputDescOrId)));
        return null;
      }
  }
  blocks[newId] = primitiveObj;
  return newId;
};

/**
 * Deserialize the given block inputs.
 * @param {object} inputs The inputs to deserialize.
 * @param {string} parentId The block id of the parent block
 * @param {object} blocks The object representing the entire set of blocks currently
 * in the process of getting deserialized.
 * @return {object} The deserialized and uncompressed inputs.
 */
var deserializeInputs = function deserializeInputs(inputs, parentId, blocks) {
  // Explicitly not using Object.create(null) here
  // because we call prototype functions later in the vm
  var obj = {};
  for (var inputName in inputs) {
    if (!hasOwnProperty.call(inputs, inputName)) continue;
    var inputDescArr = inputs[inputName];
    // If this block has already been deserialized (it's not an array) skip it
    if (!Array.isArray(inputDescArr)) continue;
    var block = null;
    var shadow = null;
    var blockShadowInfo = inputDescArr[0];
    if (blockShadowInfo === INPUT_SAME_BLOCK_SHADOW) {
      // block and shadow are the same id, and only one is provided
      block = shadow = deserializeInputDesc(inputDescArr[1], parentId, true, blocks);
    } else if (blockShadowInfo === INPUT_BLOCK_NO_SHADOW) {
      block = deserializeInputDesc(inputDescArr[1], parentId, false, blocks);
    } else {
      // assume INPUT_DIFF_BLOCK_SHADOW
      block = deserializeInputDesc(inputDescArr[1], parentId, false, blocks);
      shadow = deserializeInputDesc(inputDescArr[2], parentId, true, blocks);
    }
    obj[inputName] = {
      name: inputName,
      block: block,
      shadow: shadow
    };
  }
  return obj;
};

/**
 * Deserialize the given block fields.
 * @param {object} fields The fields to be deserialized
 * @return {object} The deserialized and uncompressed block fields.
 */
var deserializeFields = function deserializeFields(fields) {
  // Explicitly not using Object.create(null) here
  // because we call prototype functions later in the vm
  var obj = {};
  for (var fieldName in fields) {
    if (!hasOwnProperty.call(fields, fieldName)) continue;
    var fieldDescArr = fields[fieldName];
    // If this block has already been deserialized (it's not an array) skip it
    if (!Array.isArray(fieldDescArr)) continue;
    obj[fieldName] = {
      name: fieldName,
      value: fieldDescArr[0]
    };
    if (fieldDescArr.length > 1) {
      obj[fieldName].id = fieldDescArr[1];
    }
    if (fieldName === 'BROADCAST_OPTION') {
      obj[fieldName].variableType = Variable.BROADCAST_MESSAGE_TYPE;
    } else if (fieldName === 'VARIABLE') {
      obj[fieldName].variableType = Variable.SCALAR_TYPE;
    } else if (fieldName === 'LIST') {
      obj[fieldName].variableType = Variable.LIST_TYPE;
    }
  }
  return obj;
};

/**
 * Covnert serialized INPUT and FIELD primitives back to hydrated block templates.
 * Should be able to deserialize a format that has already been deserialized.  The only
 * "east" path to adding new targets/code requires going through deserialize, so it should
 * work with pre-parsed deserialized blocks.
 *
 * @param {object} blocks Serialized SB3 "blocks" property of a target. Will be mutated.
 * @return {object} input is modified and returned
 */
var deserializeBlocks = function deserializeBlocks(blocks) {
  for (var blockId in blocks) {
    if (!Object.prototype.hasOwnProperty.call(blocks, blockId)) {
      continue;
    }
    var block = blocks[blockId];
    if (Array.isArray(block)) {
      // this is one of the primitives
      // delete the old entry in object.blocks and replace it w/the
      // deserialized object
      delete blocks[blockId];
      deserializeInputDesc(block, null, false, blocks);
      continue;
    }
    block.id = blockId; // add id back to block since it wasn't serialized
    block.inputs = deserializeInputs(block.inputs, blockId, blocks);
    block.fields = deserializeFields(block.fields);
  }
  return blocks;
};

/**
 * Parse the assets of a single "Scratch object" and load them. This
 * preprocesses objects to support loading the data for those assets over a
 * network while the objects are further processed into Blocks, Sprites, and a
 * list of needed Extensions.
 * @param {!object} object From-JSON "Scratch object:" sprite, stage, watcher.
 * @param {!Runtime} runtime Runtime object to load all structures into.
 * @param {JSZip} zip Sb3 file describing this project (to load assets from)
 * @return {?{costumePromises:Array.<Promise>,soundPromises:Array.<Promise>,soundBank:SoundBank}}
 * Object of arrays of promises for asset objects used in Sprites. As well as a
 * SoundBank for the sound assets. null for unsupported objects.
 */
var parseScratchAssets = function parseScratchAssets(object, runtime, zip) {
  if (!Object.prototype.hasOwnProperty.call(object, 'name')) {
    // Watcher/monitor - skip this object until those are implemented in VM.
    // @todo
    return Promise.resolve(null);
  }
  var assets = {
    costumePromises: null,
    soundPromises: null,
    soundBank: runtime.audioEngine && runtime.audioEngine.createBank()
  };

  // Costumes from JSON.
  assets.costumePromises = (object.costumes || []).map(function (costumeSource) {
    // @todo: Make sure all the relevant metadata is being pulled out.
    var costume = {
      // costumeSource only has an asset if an image is being uploaded as
      // a sprite
      asset: costumeSource.asset,
      assetId: costumeSource.assetId,
      skinId: null,
      name: costumeSource.name,
      bitmapResolution: costumeSource.bitmapResolution,
      rotationCenterX: costumeSource.rotationCenterX,
      rotationCenterY: costumeSource.rotationCenterY
    };
    var dataFormat = costumeSource.dataFormat || costumeSource.assetType && costumeSource.assetType.runtimeFormat ||
    // older format
    'png'; // if all else fails, guess that it might be a PNG
    var costumeMd5Ext = Object.prototype.hasOwnProperty.call(costumeSource, 'md5ext') ? costumeSource.md5ext : "".concat(costumeSource.assetId, ".").concat(dataFormat);
    costume.md5 = costumeMd5Ext;
    costume.dataFormat = dataFormat;
    // deserializeCostume should be called on the costume object we're
    // creating above instead of the source costume object, because this way
    // we're always loading the 'sb3' representation of the costume
    // any translation that needs to happen will happen in the process
    // of building up the costume object into an sb3 format
    return runtime.wrapAssetRequest(function () {
      return deserializeCostume(costume, runtime, zip).then(function () {
        return loadCostume(costumeMd5Ext, costume, runtime);
      });
    });
    // Only attempt to load the costume after the deserialization
    // process has been completed
  });
  // Sounds from JSON
  assets.soundPromises = (object.sounds || []).map(function (soundSource) {
    var sound = {
      assetId: soundSource.assetId,
      format: soundSource.format,
      rate: soundSource.rate,
      sampleCount: soundSource.sampleCount,
      name: soundSource.name,
      // TODO we eventually want this property to be called md5ext,
      // but there are many things relying on this particular name at the
      // moment, so this translation is very important
      md5: soundSource.md5ext,
      dataFormat: soundSource.dataFormat,
      data: null
    };
    // deserializeSound should be called on the sound object we're
    // creating above instead of the source sound object, because this way
    // we're always loading the 'sb3' representation of the costume
    // any translation that needs to happen will happen in the process
    // of building up the costume object into an sb3 format
    return runtime.wrapAssetRequest(function () {
      return deserializeSound(sound, runtime, zip).then(function () {
        return loadSound(sound, runtime, assets.soundBank);
      });
    });
    // Only attempt to load the sound after the deserialization
    // process has been completed.
  });
  return assets;
};

/**
 * Parse a single "Scratch object" and create all its in-memory VM objects.
 * @param {!object} object From-JSON "Scratch object:" sprite, stage, watcher.
 * @param {!Runtime} runtime Runtime object to load all structures into.
 * @param {ImportedExtensionsInfo} extensions - (in/out) parsed extension information will be stored here.
 * @param {JSZip} zip Sb3 file describing this project (to load assets from)
 * @param {object} assets - Promises for assets of this scratch object grouped
 *   into costumes and sounds
 * @return {!Promise.<Target>} Promise for the target created (stage or sprite), or null for unsupported objects.
 */
var parseScratchObject = function parseScratchObject(object, runtime, extensions, zip, assets) {
  if (!Object.prototype.hasOwnProperty.call(object, 'name')) {
    // Watcher/monitor - skip this object until those are implemented in VM.
    // @todo
    return Promise.resolve(null);
  }
  // Blocks container for this object.
  var blocks = new Blocks(runtime);

  // @todo: For now, load all Scratch objects (stage/sprites) as a Sprite.
  var sprite = new Sprite(blocks, runtime);

  // Sprite/stage name from JSON.
  if (Object.prototype.hasOwnProperty.call(object, 'name')) {
    sprite.name = object.name;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'blocks')) {
    deserializeBlocks(object.blocks);
    // Take a second pass to create objects and add extensions
    for (var blockId in object.blocks) {
      if (!Object.prototype.hasOwnProperty.call(object.blocks, blockId)) continue;
      var blockJSON = object.blocks[blockId];
      blocks.createBlock(blockJSON);

      // If the block is from an extension, record it.
      var extensionID = getExtensionIdForOpcode(blockJSON.opcode);
      if (extensionID) {
        extensions.extensionIDs.add(extensionID);
      }
    }
  }
  // Costumes from JSON.
  var costumePromises = assets.costumePromises;
  // Sounds from JSON
  var soundBank = assets.soundBank,
    soundPromises = assets.soundPromises;
  // Create the first clone, and load its run-state from JSON.
  var target = sprite.createClone(object.isStage ? StageLayering.BACKGROUND_LAYER : StageLayering.SPRITE_LAYER);
  // Load target properties from JSON.
  if (Object.prototype.hasOwnProperty.call(object, 'tempo')) {
    target.tempo = object.tempo;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'volume')) {
    target.volume = object.volume;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'videoTransparency')) {
    target.videoTransparency = object.videoTransparency;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'videoState')) {
    target.videoState = object.videoState;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'textToSpeechLanguage')) {
    target.textToSpeechLanguage = object.textToSpeechLanguage;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'variables')) {
    for (var varId in object.variables) {
      var variable = object.variables[varId];
      // A variable is a cloud variable if:
      // - the project says it's a cloud variable, and
      // - it's a stage variable, and
      // - the runtime can support another cloud variable
      var isCloud = variable.length === 3 && variable[2] && object.isStage && runtime.canAddCloudVariable();
      var newVariable = new Variable(varId,
      // var id is the index of the variable desc array in the variables obj
      variable[0],
      // name of the variable
      Variable.SCALAR_TYPE,
      // type of the variable
      isCloud);
      if (isCloud) runtime.addCloudVariable();
      newVariable.value = variable[1];
      target.variables[newVariable.id] = newVariable;
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'lists')) {
    for (var listId in object.lists) {
      var list = object.lists[listId];
      var newList = new Variable(listId, list[0], Variable.LIST_TYPE, false);
      newList.value = list[1];
      target.variables[newList.id] = newList;
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'broadcasts')) {
    for (var broadcastId in object.broadcasts) {
      var broadcast = object.broadcasts[broadcastId];
      var newBroadcast = new Variable(broadcastId, broadcast, Variable.BROADCAST_MESSAGE_TYPE, false);
      // no need to explicitly set the value, variable constructor
      // sets the value to the same as the name for broadcast msgs
      target.variables[newBroadcast.id] = newBroadcast;
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'comments')) {
    for (var commentId in object.comments) {
      var comment = object.comments[commentId];
      var newComment = new Comment(commentId,
      // text has a length limit, so anything extra got saved in extraText
      comment.text + (typeof comment.extraText === 'string' ? comment.extraText : ''), comment.x, comment.y, comment.width, comment.height, comment.minimized);
      if (comment.blockId) {
        newComment.blockId = comment.blockId;
      }
      target.comments[newComment.id] = newComment;
    }
  }
  if (Object.prototype.hasOwnProperty.call(object, 'x')) {
    target.x = object.x;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'y')) {
    target.y = object.y;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'direction')) {
    // Sometimes the direction can be outside of the range: LLK/scratch-gui#5806
    // wrapClamp it (like we do on RenderedTarget.setDirection)
    target.direction = MathUtil.wrapClamp(object.direction, -179, 180);
  }
  if (Object.prototype.hasOwnProperty.call(object, 'size')) {
    target.size = object.size;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'visible')) {
    target.visible = object.visible;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'currentCostume')) {
    target.currentCostume = MathUtil.clamp(object.currentCostume, 0, object.costumes.length - 1);
  }
  if (Object.prototype.hasOwnProperty.call(object, 'rotationStyle')) {
    target.rotationStyle = object.rotationStyle;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'isStage')) {
    target.isStage = object.isStage;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'targetPaneOrder')) {
    // Temporarily store the 'targetPaneOrder' property
    // so that we can correctly order sprites in the target pane.
    // This will be deleted after we are done parsing and ordering the targets list.
    target.targetPaneOrder = object.targetPaneOrder;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'draggable')) {
    target.draggable = object.draggable;
  }
  if (Object.prototype.hasOwnProperty.call(object, 'extensionStorage')) {
    target.extensionStorage = object.extensionStorage;
  }
  Promise.all(costumePromises).then(function (costumes) {
    sprite.costumes = costumes;
  });
  Promise.all(soundPromises).then(function (sounds) {
    sprite.sounds = sounds;
    // Make sure if soundBank is undefined, sprite.soundBank is then null.
    sprite.soundBank = soundBank || null;
  });
  return Promise.all(costumePromises.concat(soundPromises)).then(function () {
    return target;
  });
};
var deserializeMonitor = function deserializeMonitor(monitorData, runtime, targets, extensions) {
  // Monitors position is always stored as position from top-left corner in 480x360 stage.
  var xOffset = (runtime.stageWidth - 480) / 2;
  var yOffset = (runtime.stageHeight - 360) / 2;
  monitorData.x += xOffset;
  monitorData.y += yOffset;
  monitorData.x = MathUtil.clamp(monitorData.x, 0, runtime.stageWidth);
  monitorData.y = MathUtil.clamp(monitorData.y, 0, runtime.stageHeight);

  // If the serialized monitor has spriteName defined, look up the sprite
  // by name in the given list of targets and update the monitor's targetId
  // to match the sprite's id.
  if (monitorData.spriteName) {
    var filteredTargets = targets.filter(function (t) {
      return t.sprite.name === monitorData.spriteName;
    });
    if (filteredTargets && filteredTargets.length > 0) {
      monitorData.targetId = filteredTargets[0].id;
    } else {
      log.warn("Tried to deserialize sprite specific monitor ".concat(monitorData.opcode, " but could not find sprite ").concat(monitorData.spriteName, "."));
    }
  }

  // Get information about this monitor, if it exists, given the monitor's opcode.
  // This will be undefined for extension blocks
  var monitorBlockInfo = runtime.monitorBlockInfo[monitorData.opcode];

  // Due to a bug (see https://github.com/scratchfoundation/scratch-vm/pull/2322), renamed list monitors may have been serialized
  // with an outdated/incorrect LIST parameter. Fix it up to use the current name of the actual corresponding list.
  if (monitorData.opcode === 'data_listcontents') {
    var listTarget = monitorData.targetId ? targets.find(function (t) {
      return t.id === monitorData.targetId;
    }) : targets.find(function (t) {
      return t.isStage;
    });
    if (listTarget && Object.prototype.hasOwnProperty.call(listTarget.variables, monitorData.id)) {
      monitorData.params.LIST = listTarget.variables[monitorData.id].name;
    }
  }

  // Convert the serialized monitorData params into the block fields structure
  var fields = {};
  for (var paramKey in monitorData.params) {
    var field = {
      name: paramKey,
      value: monitorData.params[paramKey]
    };
    fields[paramKey] = field;
  }

  // Variables, lists, and non-sprite-specific monitors, including any extension
  // monitors should already have the correct monitor ID serialized in the monitorData,
  // find the correct id for all other monitors.
  if (monitorData.opcode !== 'data_variable' && monitorData.opcode !== 'data_listcontents' && monitorBlockInfo && monitorBlockInfo.isSpriteSpecific) {
    monitorData.id = monitorBlockInfo.getId(monitorData.targetId, fields);
  } else {
    // Replace unsafe characters in monitor ID, if there are any.
    // These would have come from projects that were originally 2.0 projects
    // that had unsafe characters in the variable name (and then the name was
    // used as part of the variable ID when importing the project).
    monitorData.id = StringUtil.replaceUnsafeChars(monitorData.id);
  }

  // If the runtime already has a monitor block for this monitor's id,
  // update the existing block with the relevant monitor information.
  var existingMonitorBlock = runtime.monitorBlocks._blocks[monitorData.id];
  if (existingMonitorBlock) {
    // A monitor block already exists if the toolbox has been loaded and
    // the monitor block is not target specific (because the block gets recycled).
    existingMonitorBlock.isMonitored = monitorData.visible;
    existingMonitorBlock.targetId = monitorData.targetId;
  } else {
    // If a monitor block doesn't already exist for this monitor,
    // construct a monitor block to add to the monitor blocks container
    var monitorBlock = {
      id: monitorData.id,
      opcode: monitorData.opcode,
      inputs: {},
      // Assuming that monitor blocks don't have droppable fields
      fields: fields,
      topLevel: true,
      next: null,
      parent: null,
      shadow: false,
      x: 0,
      y: 0,
      isMonitored: monitorData.visible,
      targetId: monitorData.targetId
    };

    // Variables and lists have additional properties
    // stored in their fields, update this info in the
    // monitor block fields
    if (monitorData.opcode === 'data_variable') {
      var _field = monitorBlock.fields.VARIABLE;
      _field.id = monitorData.id;
      _field.variableType = Variable.SCALAR_TYPE;
    } else if (monitorData.opcode === 'data_listcontents') {
      var _field2 = monitorBlock.fields.LIST;
      _field2.id = monitorData.id;
      _field2.variableType = Variable.LIST_TYPE;
    }
    runtime.monitorBlocks.createBlock(monitorBlock);

    // If the block is from an extension, record it.
    var extensionID = getExtensionIdForOpcode(monitorBlock.opcode);
    if (extensionID) {
      extensions.extensionIDs.add(extensionID);
    }
  }
  runtime.requestAddMonitor(new MonitorRecord(monitorData));
};

// Replace variable IDs throughout the project with
// xml-safe versions.
// This is to fix up projects imported from 2.0 where xml-unsafe names
// were getting added to the variable ids.
var replaceUnsafeCharsInVariableIds = function replaceUnsafeCharsInVariableIds(targets) {
  var allVarRefs = VariableUtil.getAllVarRefsForTargets(targets, true);
  // Re-id the variables in the actual targets
  targets.forEach(function (t) {
    Object.keys(t.variables).forEach(function (id) {
      var newId = StringUtil.replaceUnsafeChars(id);
      if (newId === id) return;
      t.variables[id].id = newId;
      t.variables[newId] = t.variables[id];
      delete t.variables[id];
    });
  });

  // Replace the IDs in the blocks refrencing variables or lists
  for (var id in allVarRefs) {
    var newId = StringUtil.replaceUnsafeChars(id);
    if (id === newId) continue; // ID was already safe, skip
    // We're calling this on the stage target because we need a
    // target to call on but this shouldn't matter because we're passing
    // in all the varRefs we want to operate on
    VariableUtil.updateVariableIdentifiers(allVarRefs[id], newId);
  }
  return targets;
};

/**
 * @param {object} json
 * @param {Runtime} runtime
 * @returns {void|Promise<void>} Resolves when the user has acknowledged any compatibilities, if any exist.
 */
var checkPlatformCompatibility = function checkPlatformCompatibility(json, runtime) {
  if (!json.meta || !json.meta.platform) {
    return;
  }
  var projectPlatform = json.meta.platform.name;
  if (projectPlatform === runtime.platform.name) {
    return;
  }
  var pending = runtime.listenerCount(Runtime.PLATFORM_MISMATCH);
  if (pending === 0) {
    return;
  }
  return new Promise(function (resolve) {
    runtime.emit(Runtime.PLATFORM_MISMATCH, json.meta.platform, function () {
      pending--;
      if (pending === 0) {
        resolve();
      }
    });
  });
};

/**
 * Deserialize the specified representation of a VM runtime and loads it into the provided runtime instance.
 * @param  {object} json - JSON representation of a VM runtime.
 * @param  {Runtime} runtime - Runtime instance
 * @param {JSZip} zip - Sb3 file describing this project (to load assets from)
 * @param {boolean} isSingleSprite - If true treat as single sprite, else treat as whole project
 * @returns {Promise.<ImportedProject>} Promise that resolves to the list of targets after the project is deserialized
 */
var deserialize = /*#__PURE__*/function () {
  var _ref2 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(json, runtime, zip, isSingleSprite) {
    var extensions, _i3, _Object$entries3, _Object$entries3$_i, id, url, fontPromise, targetObjects, monitorObjects;
    return _regenerator().w(function (_context) {
      while (1) switch (_context.n) {
        case 0:
          _context.n = 1;
          return checkPlatformCompatibility(json, runtime);
        case 1:
          extensions = {
            extensionIDs: new Set(),
            extensionURLs: new Map()
          }; // Store the origin field (e.g. project originated at CSFirst) so that we can save it again.
          if (json.meta && json.meta.origin) {
            // eslint-disable-next-line require-atomic-updates
            runtime.origin = json.meta.origin;
          } else {
            // eslint-disable-next-line require-atomic-updates
            runtime.origin = null;
          }

          // Extract custom extension IDs, if they exist.
          if (json.extensionURLs) {
            for (_i3 = 0, _Object$entries3 = Object.entries(json.extensionURLs); _i3 < _Object$entries3.length; _i3++) {
              _Object$entries3$_i = _slicedToArray(_Object$entries3[_i3], 2), id = _Object$entries3$_i[0], url = _Object$entries3$_i[1];
              extensions.extensionURLs.set(id, url);
            }
          }

          // Extract any custom fonts before loading costumes.

          if (json.customFonts) {
            fontPromise = runtime.fontManager.deserialize(json.customFonts, zip, isSingleSprite);
          } else {
            fontPromise = Promise.resolve();
          }

          // First keep track of the current target order in the json,
          // then sort by the layer order property before parsing the targets
          // so that their corresponding render drawables can be created in
          // their layer order (e.g. back to front)
          targetObjects = ((isSingleSprite ? [json] : json.targets) || []).map(function (t, i) {
            return Object.assign(t, {
              targetPaneOrder: i
            });
          }).sort(function (a, b) {
            return a.layerOrder - b.layerOrder;
          });
          monitorObjects = json.monitors || [];
          return _context.a(2, fontPromise.then(function () {
            return targetObjects.map(function (target) {
              return parseScratchAssets(target, runtime, zip);
            });
          })
          // Force this promise to wait for the next loop in the js tick. Let
          // storage have some time to send off asset requests.
          .then(function (assets) {
            return Promise.resolve(assets);
          }).then(function (assets) {
            return Promise.all(targetObjects.map(function (target, index) {
              return parseScratchObject(target, runtime, extensions, zip, assets[index]);
            }));
          }).then(function (targets) {
            return targets // Re-sort targets back into original sprite-pane ordering
            .map(function (t, i) {
              // Add layer order property to deserialized targets.
              // This property is used to initialize executable targets in
              // the correct order and is deleted in VM's installTargets function
              t.layerOrder = i;
              return t;
            }).sort(function (a, b) {
              return a.targetPaneOrder - b.targetPaneOrder;
            }).map(function (t) {
              // Delete the temporary properties used for
              // sprite pane ordering and stage layer ordering
              delete t.targetPaneOrder;
              return t;
            });
          }).then(function (targets) {
            return replaceUnsafeCharsInVariableIds(targets);
          }).then(function (targets) {
            monitorObjects.map(function (monitorDesc) {
              return deserializeMonitor(monitorDesc, runtime, targets, extensions);
            });
            if (Object.prototype.hasOwnProperty.call(json, 'extensionStorage')) {
              runtime.extensionStorage = json.extensionStorage;
            }
            return targets;
          }).then(function (targets) {
            return {
              targets: targets,
              extensions: extensions
            };
          }));
      }
    }, _callee);
  }));
  return function deserialize(_x, _x2, _x3, _x4) {
    return _ref2.apply(this, arguments);
  };
}();
module.exports = {
  serialize: serialize,
  deserialize: deserialize,
  deserializeBlocks: deserializeBlocks,
  serializeBlocks: serializeBlocks,
  deserializeStandaloneBlocks: deserializeStandaloneBlocks,
  serializeStandaloneBlocks: serializeStandaloneBlocks,
  getExtensionIdForOpcode: getExtensionIdForOpcode
};

/***/ }),

/***/ "../vm/src/serialization/serialize-assets.js":
/*!***************************************************!*\
  !*** ../vm/src/serialization/serialize-assets.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Serialize all the assets of the given type ('sounds' or 'costumes')
 * in the provided runtime into an array of file descriptors.
 * A file descriptor is an object containing the name of the file
 * to be written and the contents of the file, the serialized asset.
 * @param {Runtime} runtime The runtime with the assets to be serialized
 * @param {string} assetType The type of assets to be serialized: 'sounds' | 'costumes'
 * @param {string=} optTargetId Optional target id to serialize assets for
 * @returns {Array<object>} An array of file descriptors for each asset
 */
var serializeAssets = function serializeAssets(runtime, assetType, optTargetId) {
  var targets = optTargetId ? [runtime.getTargetById(optTargetId)] : runtime.targets;
  var assetDescs = [];
  for (var i = 0; i < targets.length; i++) {
    var currTarget = targets[i];
    var currAssets = currTarget.sprite[assetType];
    for (var j = 0; j < currAssets.length; j++) {
      var currAsset = currAssets[j];
      var asset = currAsset.broken ? currAsset.broken.asset : currAsset.asset;
      if (asset) {
        // Serialize asset if it exists, otherwise skip
        assetDescs.push({
          fileName: "".concat(asset.assetId, ".").concat(asset.dataFormat),
          fileContent: asset.data
        });
      }
    }
  }
  return assetDescs;
};

/**
 * Serialize all the sounds in the provided runtime or, if a target id is provided,
 * in the specified target into an array of file descriptors.
 * A file descriptor is an object containing the name of the file
 * to be written and the contents of the file, the serialized sound.
 * @param {Runtime} runtime The runtime with the sounds to be serialized
 * @param {string=} optTargetId Optional targetid for serializing sounds of a single target
 * @returns {Array<object>} An array of file descriptors for each sound
 */
var serializeSounds = function serializeSounds(runtime, optTargetId) {
  return serializeAssets(runtime, 'sounds', optTargetId);
};

/**
 * Serialize all the costumes in the provided runtime into an array of file
 * descriptors. A file descriptor is an object containing the name of the file
 * to be written and the contents of the file, the serialized costume.
 * @param {Runtime} runtime The runtime with the costumes to be serialized
 * @param {string} optTargetId Optional targetid for serializing costumes of a single target
 * @returns {Array<object>} An array of file descriptors for each costume
 */
var serializeCostumes = function serializeCostumes(runtime, optTargetId) {
  return serializeAssets(runtime, 'costumes', optTargetId);
};
module.exports = {
  serializeSounds: serializeSounds,
  serializeCostumes: serializeCostumes
};

/***/ }),

/***/ "../vm/src/serialization/tw-compress-sb3.js":
/*!**************************************************!*\
  !*** ../vm/src/serialization/tw-compress-sb3.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// We don't generate new IDs using numbers at this time because their enumeration
// order can affect script execution order as they always come first.
// https://tc39.es/ecma262/#sec-ordinaryownpropertykeys
var SOUP = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ!#%()*+,-./:;=?@[]^_`{|}~';
var generateId = function generateId(i) {
  var str = '';
  while (i >= 0) {
    str = SOUP[i % SOUP.length] + str;
    i = Math.floor(i / SOUP.length) - 1;
  }
  return str;
};
var Pool = /*#__PURE__*/function () {
  function Pool() {
    _classCallCheck(this, Pool);
    this.generatedIds = new Map();
    this.references = new Map();
    this.skippedIds = new Set();
    // IDs in Object.keys(vm.runtime.monitorBlocks._blocks) already have meaning, so make sure to skip those
    // We don't bother listing many here because most would take more than ten million items to be used
    this.skippedIds.add('of');
  }
  return _createClass(Pool, [{
    key: "skip",
    value: function skip(id) {
      this.skippedIds.add(id);
    }
  }, {
    key: "addReference",
    value: function addReference(id) {
      var currentCount = this.references.get(id) || 0;
      this.references.set(id, currentCount + 1);
    }
  }, {
    key: "generateNewIds",
    value: function generateNewIds() {
      var entries = Array.from(this.references.entries());
      // The most used original IDs should get the shortest new IDs.
      entries.sort(function (a, b) {
        return b[1] - a[1];
      });
      var i = 0;
      for (var _i = 0, _entries = entries; _i < _entries.length; _i++) {
        var entry = _entries[_i];
        var oldId = entry[0];
        var newId = generateId(i);
        while (this.skippedIds.has(newId)) {
          i++;
          newId = generateId(i);
        }
        this.generatedIds.set(oldId, newId);
        i++;
      }
    }
  }, {
    key: "getNewId",
    value: function getNewId(originalId) {
      if (this.generatedIds.has(originalId)) {
        return this.generatedIds.get(originalId);
      }
      return originalId;
    }
  }]);
}();
var compress = function compress(projectData) {
  // projectData is modified in-place

  // The optimization here is not optimal. This is intentional.
  // We only compress block and comment IDs because we want to maintain 100% (not 99.99%; 100%) compatibility and be
  // truly lossless. Optimizing things like variable IDs will cause things such as the editor's backpack feature
  // to misbehave.

  // We use the same variable pool for all objects to avoid any possible issues if IDs are ever treated as unique
  // within a given project.
  var pool = new Pool();
  var _iterator = _createForOfIteratorHelper(projectData.targets),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var target = _step.value;
      // While we don't compress these IDs, we need to make sure that our compressed IDs
      // do not intersect, which could happen if the project was compressed with a
      // different tool.
      for (var _i2 = 0, _Object$keys = Object.keys(target.variables); _i2 < _Object$keys.length; _i2++) {
        var variableId = _Object$keys[_i2];
        pool.skip(variableId);
      }
      for (var _i3 = 0, _Object$keys2 = Object.keys(target.lists); _i3 < _Object$keys2.length; _i3++) {
        var listId = _Object$keys2[_i3];
        pool.skip(listId);
      }
      for (var _i4 = 0, _Object$keys3 = Object.keys(target.broadcasts); _i4 < _Object$keys3.length; _i4++) {
        var broadcastId = _Object$keys3[_i4];
        pool.skip(broadcastId);
      }
      for (var _i5 = 0, _Object$keys4 = Object.keys(target.blocks); _i5 < _Object$keys4.length; _i5++) {
        var blockId = _Object$keys4[_i5];
        var block = target.blocks[blockId];
        pool.addReference(blockId);
        if (Array.isArray(block)) {
          // Compressed native
          continue;
        }
        if (block.parent) {
          pool.addReference(block.parent);
        }
        if (block.next) {
          pool.addReference(block.next);
        }
        if (block.comment) {
          pool.addReference(block.comment);
        }
        for (var _i6 = 0, _Object$values = Object.values(block.inputs); _i6 < _Object$values.length; _i6++) {
          var input = _Object$values[_i6];
          for (var i = 1; i < input.length; i++) {
            var inputValue = input[i];
            if (typeof inputValue === 'string') {
              pool.addReference(inputValue);
            }
          }
        }
      }
      for (var _i7 = 0, _Object$keys5 = Object.keys(target.comments); _i7 < _Object$keys5.length; _i7++) {
        var commentId = _Object$keys5[_i7];
        var comment = target.comments[commentId];
        pool.addReference(commentId);
        if (comment.blockId) {
          pool.addReference(comment.blockId);
        }
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  pool.generateNewIds();
  var _iterator2 = _createForOfIteratorHelper(projectData.targets),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var _target = _step2.value;
      var newBlocks = {};
      var newComments = {};
      for (var _i8 = 0, _Object$keys6 = Object.keys(_target.blocks); _i8 < _Object$keys6.length; _i8++) {
        var _blockId = _Object$keys6[_i8];
        var _block = _target.blocks[_blockId];
        newBlocks[pool.getNewId(_blockId)] = _block;
        if (Array.isArray(_block)) {
          // Compressed native
          continue;
        }
        if (_block.parent) {
          _block.parent = pool.getNewId(_block.parent);
        }
        if (_block.next) {
          _block.next = pool.getNewId(_block.next);
        }
        if (_block.comment) {
          _block.comment = pool.getNewId(_block.comment);
        }
        for (var _i9 = 0, _Object$values2 = Object.values(_block.inputs); _i9 < _Object$values2.length; _i9++) {
          var _input = _Object$values2[_i9];
          for (var _i0 = 1; _i0 < _input.length; _i0++) {
            var _inputValue = _input[_i0];
            if (typeof _inputValue === 'string') {
              _input[_i0] = pool.getNewId(_inputValue);
            }
          }
        }
      }
      for (var _i1 = 0, _Object$keys7 = Object.keys(_target.comments); _i1 < _Object$keys7.length; _i1++) {
        var _commentId = _Object$keys7[_i1];
        var _comment = _target.comments[_commentId];
        newComments[pool.getNewId(_commentId)] = _comment;
        if (_comment.blockId) {
          _comment.blockId = pool.getNewId(_comment.blockId);
        }
      }
      _target.blocks = newBlocks;
      _target.comments = newComments;
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
};
module.exports = compress;

/***/ }),

/***/ "../vm/src/serialization/tw-costume-import-export.js":
/*!***********************************************************!*\
  !*** ../vm/src/serialization/tw-costume-import-export.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// We want to preserve the rotation center of exported SVGs when they are later imported.
// Unfortunately, the SVG itself does not have sufficient information to accomplish this.
// Instead we must add a small amount of extra information to the end of exported SVGs
// that can be read on import.

// Adding this comment in scratch-paint is not a viable approach because the user can
// open projects not made with TurboWarp and we want costumes exported from there to
// have their center saved even if they haven't been edited.

var _TextEncoder;
var _TextDecoder;
if (typeof TextEncoder === 'undefined') {
  _TextEncoder = __webpack_require__(/*! text-encoding */ "./src/lib/tw-text-encoder.js").TextEncoder;
  _TextDecoder = __webpack_require__(/*! text-encoding */ "./src/lib/tw-text-encoder.js").TextDecoder;
} else {
  _TextEncoder = TextEncoder;
  _TextDecoder = TextDecoder;
}

// Using literal HTML comments tokens will cause this script to be very hard to inline in
// a <script> element, so we'll instead do this terrible hack which the minifier probably
// won't be able to optimize away.
var HTML_COMMENT_START = "<!".concat('-'.repeat(2));
var HTML_COMMENT_END = "".concat('-'.repeat(2), ">");
var regex = new RegExp("".concat(HTML_COMMENT_START, "rotationCenter:(-?[\\d\\.]+):(-?[\\d\\.]+)").concat(HTML_COMMENT_END, "$"));

/**
 * @param {string} svgString SVG source
 * @returns {[number, number]|null} The detected rotation center of the SVG, if any.
 */
var parseVectorMetadata = function parseVectorMetadata(svgString) {
  // TODO: see if this is slow on large strings
  var match = svgString.match(regex);
  if (!match) {
    return null;
  }
  var detectedX = +match[1];
  var detectedY = +match[2];
  if (Number.isNaN(detectedX) || Number.isNaN(detectedY)) {
    return null;
  }
  return [detectedX, detectedY];
};

/**
 * @param {Costume} costume scratch-vm costume object
 * @returns {Uint8Array} Binary data to export
 */
var exportCostume = function exportCostume(costume) {
  /** @type {Uint8Array} */
  var originalData = costume.asset.data;
  if (costume.dataFormat !== 'svg') {
    return originalData;
  }
  var decodedData = new _TextDecoder().decode(originalData);

  // It's okay that the regex isn't global because it can only match one item anyways.
  decodedData = decodedData.replace(regex, '');
  var centerX = costume.rotationCenterX;
  var centerY = costume.rotationCenterY;
  var extraData = "".concat(HTML_COMMENT_START, "rotationCenter:").concat(centerX, ":").concat(centerY).concat(HTML_COMMENT_END);
  decodedData += extraData;
  return new _TextEncoder().encode(decodedData);
};
module.exports = {
  parseVectorMetadata: parseVectorMetadata,
  exportCostume: exportCostume
};

/***/ }),

/***/ "../vm/src/sprites/rendered-target.js":
/*!********************************************!*\
  !*** ../vm/src/sprites/rendered-target.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var MathUtil = __webpack_require__(/*! ../util/math-util */ "../vm/src/util/math-util.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var Cast = __webpack_require__(/*! ../util/cast */ "../vm/src/util/cast.js");
var Clone = __webpack_require__(/*! ../util/clone */ "../vm/src/util/clone.js");
var Target = __webpack_require__(/*! ../engine/target */ "../vm/src/engine/target.js");
var StageLayering = __webpack_require__(/*! ../engine/stage-layering */ "../vm/src/engine/stage-layering.js");

/**
 * Rendered target: instance of a sprite (clone), or the stage.
 */
var RenderedTarget = /*#__PURE__*/function (_Target) {
  /**
   * @param {!Sprite} sprite Reference to the parent sprite.
   * @param {Runtime} runtime Reference to the runtime.
   * @constructor
   */
  function RenderedTarget(sprite, runtime) {
    var _this;
    _classCallCheck(this, RenderedTarget);
    _this = _callSuper(this, RenderedTarget, [runtime, sprite.blocks]);

    /**
     * Reference to the sprite that this is a render of.
     * @type {!Sprite}
     */
    _this.sprite = sprite;
    /**
     * Reference to the global renderer for this VM, if one exists.
     * @type {?RenderWebGL}
     */
    _this.renderer = null;
    if (_this.runtime) {
      _this.renderer = _this.runtime.renderer;
    }
    /**
     * ID of the drawable for this rendered target,
     * returned by the renderer, if rendered.
     * @type {?Number}
     */
    _this.drawableID = null;

    /**
     * Drag state of this rendered target. If true, x/y position can't be
     * changed by blocks.
     * @type {boolean}
     */
    _this.dragging = false;

    /**
     * Map of current graphic effect values.
     * @type {!Object.<string, number>}
     */
    _this.effects = {
      color: 0,
      fisheye: 0,
      whirl: 0,
      pixelate: 0,
      mosaic: 0,
      brightness: 0,
      ghost: 0
    };

    /**
     * Whether this represents an "original" non-clone rendered-target for a sprite,
     * i.e., created by the editor and not clone blocks.
     * @type {boolean}
     */
    _this.isOriginal = true;

    /**
     * Whether this rendered target represents the Scratch stage.
     * @type {boolean}
     */
    _this.isStage = false;

    /**
     * Scratch X coordinate. Currently should range from -240 to 240.
     * @type {Number}
     */
    _this.x = 0;

    /**
     * Scratch Y coordinate. Currently should range from -180 to 180.
     * @type {number}
     */
    _this.y = 0;

    /**
     * Scratch direction. Currently should range from -179 to 180.
     * @type {number}
     */
    _this.direction = 90;

    /**
     * Whether the rendered target is draggable on the stage
     * @type {boolean}
     */
    _this.draggable = false;

    /**
     * Whether the rendered target is currently visible.
     * @type {boolean}
     */
    _this.visible = true;

    /**
     * Size of rendered target as a percent of costume size.
     * @type {number}
     */
    _this.size = 100;

    /**
     * Currently selected costume index.
     * @type {number}
     */
    _this.currentCostume = 0;

    /**
     * Current rotation style.
     * @type {!string}
     */
    _this.rotationStyle = RenderedTarget.ROTATION_STYLE_ALL_AROUND;

    /**
     * Loudness for sound playback for this target, as a percentage.
     * @type {number}
     */
    _this.volume = 100;

    /**
     * Current tempo (used by the music extension).
     * This property is global to the project and stored in the stage.
     * @type {number}
     */
    _this.tempo = 60;

    /**
     * The transparency of the video (used by extensions with camera input).
     * This property is global to the project and stored in the stage.
     * @type {number}
     */
    _this.videoTransparency = 50;

    /**
     * The state of the video input (used by extensions with camera input).
     * This property is global to the project and stored in the stage.
     *
     * Defaults to ON. This setting does not turn the video by itself. A
     * video extension once loaded will set the video device to this
     * setting. Set to ON when a video extension is added in the editor the
     * video will start ON. If the extension is loaded as part of loading a
     * saved project the extension will see the value set when the stage
     * was loaded from the saved values including the video state.
     *
     * @type {string}
     */
    _this.videoState = RenderedTarget.VIDEO_STATE.ON;

    /**
     * The language to use for speech synthesis, in the text2speech extension.
     * It is initialized to null so that on extension load, we can check for
     * this and try setting it using the editor locale.
     * @type {string}
     */
    _this.textToSpeechLanguage = null;

    // Node-style event emitters have non-zero performance overhead compared to function calls, so we
    // replace some very high frequency events with these specific methods that are overridden elsewhere.
    _this.onTargetMoved = null;
    _this.onTargetVisualChange = null;
    _this.interpolationData = null;
    return _this;
  }

  /**
   * Create a drawable with the this.renderer.
   * @param {boolean} layerGroup The layer group this drawable should be added to
   */
  _inherits(RenderedTarget, _Target);
  return _createClass(RenderedTarget, [{
    key: "initDrawable",
    value: function initDrawable(layerGroup) {
      if (this.renderer) {
        this.drawableID = this.renderer.createDrawable(layerGroup);
      }
      // If we're a clone, start the hats.
      if (!this.isOriginal) {
        this.runtime.startHats('control_start_as_clone', null, this);
      }
    }
  }, {
    key: "audioPlayer",
    get: function get() {
      var _this2 = this;
      /* eslint-disable no-console */
      console.warn('get audioPlayer deprecated, please update to use .sprite.soundBank methods');
      console.warn(new Error('stack for debug').stack);
      /* eslint-enable no-console */
      var bank = this.sprite.soundBank;
      var audioPlayerProxy = {
        playSound: function playSound(soundId) {
          return bank.play(_this2, soundId);
        }
      };
      Object.defineProperty(this, 'audioPlayer', {
        configurable: false,
        enumerable: true,
        writable: false,
        value: audioPlayerProxy
      });
      return audioPlayerProxy;
    }

    /**
     * Initialize the audio player for this sprite or clone.
     */
  }, {
    key: "initAudio",
    value: function initAudio() {}

    /**
     * Rotation style for "all around"/spinning.
     * @type {string}
     */
  }, {
    key: "emitVisualChange",
    value: function emitVisualChange() {
      if (this.onTargetVisualChange) {
        this.onTargetVisualChange(this);
      }
    }

    /**
     * Set the X and Y coordinates.
     * @param {!number} x New X coordinate, in Scratch coordinates.
     * @param {!number} y New Y coordinate, in Scratch coordinates.
     * @param {?boolean} force Force setting X/Y, in case of dragging
     */
  }, {
    key: "setXY",
    value: function setXY(x, y, force) {
      // used by compiler
      if (this.isStage) return;
      if (this.dragging && !force) return;
      var oldX = this.x;
      var oldY = this.y;
      if (this.renderer) {
        var position = this.runtime.runtimeOptions.fencing ? this.renderer.getFencedPositionOfDrawable(this.drawableID, [x, y]) : [x, y];
        this.x = position[0];
        this.y = position[1];
        this.renderer.updateDrawablePosition(this.drawableID, position);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      } else {
        this.x = x;
        this.y = y;
      }
      if (this.onTargetMoved) {
        this.onTargetMoved(this, oldX, oldY, force);
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Get the rendered direction and scale, after applying rotation style.
     * @return {object<string, number>} Direction and scale to render.
     */
  }, {
    key: "_getRenderedDirectionAndScale",
    value: function _getRenderedDirectionAndScale() {
      // Default: no changes to `this.direction` or `this.scale`.
      var finalDirection = this.direction;
      var finalScale = [this.size, this.size];
      if (this.rotationStyle === RenderedTarget.ROTATION_STYLE_NONE) {
        // Force rendered direction to be 90.
        finalDirection = 90;
      } else if (this.rotationStyle === RenderedTarget.ROTATION_STYLE_LEFT_RIGHT) {
        // Force rendered direction to be 90, and flip drawable if needed.
        finalDirection = 90;
        var scaleFlip = this.direction < 0 ? -1 : 1;
        finalScale = [scaleFlip * this.size, this.size];
      }
      return {
        direction: finalDirection,
        scale: finalScale
      };
    }

    /**
     * Set the direction.
     * @param {!number} direction New direction.
     */
  }, {
    key: "setDirection",
    value: function setDirection(direction) {
      // used by compiler
      if (this.isStage) {
        return;
      }
      if (!isFinite(direction)) {
        return;
      }
      // Keep direction between -179 and +180.
      this.direction = MathUtil.wrapClamp(direction, -179, 180);
      if (this.renderer) {
        var _this$_getRenderedDir = this._getRenderedDirectionAndScale(),
          renderedDirection = _this$_getRenderedDir.direction,
          scale = _this$_getRenderedDir.scale;
        this.renderer.updateDrawableDirectionScale(this.drawableID, renderedDirection, scale);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Set draggability; i.e., whether it's able to be dragged in the player
     * @param {!boolean} draggable True if should be draggable.
     */
  }, {
    key: "setDraggable",
    value: function setDraggable(draggable) {
      if (this.isStage) return;
      this.draggable = !!draggable;
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Set visibility; i.e., whether it's shown or hidden.
     * @param {!boolean} visible True if should be shown.
     */
  }, {
    key: "setVisible",
    value: function setVisible(visible) {
      // used by compiler
      if (this.isStage) {
        return;
      }
      this.visible = !!visible;
      if (this.renderer) {
        this.renderer.updateDrawableVisible(this.drawableID, this.visible);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Set size, as a percentage of the costume size.
     * @param {!number} size Size of rendered target, as % of costume size.
     */
  }, {
    key: "setSize",
    value: function setSize(size) {
      // used by compiler
      if (this.isStage) {
        return;
      }
      if (this.renderer) {
        // Clamp to scales relative to costume and stage size.
        // See original ScratchSprite.as:setSize.
        var costumeSize = this.renderer.getCurrentSkinSize(this.drawableID);
        var origW = costumeSize[0];
        var origH = costumeSize[1];
        var fencing = this.runtime.runtimeOptions.fencing;
        var minScale = fencing ? Math.min(1, Math.max(5 / origW, 5 / origH)) : 0;
        var maxScale = fencing ? Math.min(1.5 * this.runtime.stageWidth / origW, 1.5 * this.runtime.stageHeight / origH) : Infinity;
        this.size = MathUtil.clamp(size / 100, minScale, maxScale) * 100;
        var _this$_getRenderedDir2 = this._getRenderedDirectionAndScale(),
          direction = _this$_getRenderedDir2.direction,
          scale = _this$_getRenderedDir2.scale;
        this.renderer.updateDrawableDirectionScale(this.drawableID, direction, scale);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      } else {
        // tw: setSize should update size even without a renderer
        // needed by tw-change-size-does-not-use-rounded-size.sb3 test
        this.size = size;
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Set a particular graphic effect value.
     * @param {!string} effectName Name of effect (see `RenderedTarget.prototype.effects`).
     * @param {!number} value Numerical magnitude of effect.
     */
  }, {
    key: "setEffect",
    value: function setEffect(effectName, value) {
      // used by compiler
      if (!Object.prototype.hasOwnProperty.call(this.effects, effectName)) return;
      this.effects[effectName] = value;
      if (this.renderer) {
        this.renderer.updateDrawableEffect(this.drawableID, effectName, value);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
    }

    /**
     * Clear all graphic effects on this rendered target.
     */
  }, {
    key: "clearEffects",
    value: function clearEffects() {
      // used by compiler
      for (var effectName in this.effects) {
        if (!Object.prototype.hasOwnProperty.call(this.effects, effectName)) continue;
        this.effects[effectName] = 0;
      }
      if (this.renderer) {
        for (var _effectName in this.effects) {
          if (!Object.prototype.hasOwnProperty.call(this.effects, _effectName)) continue;
          this.renderer.updateDrawableEffect(this.drawableID, _effectName, 0);
        }
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
    }

    /**
     * Set the current costume.
     * @param {number} index New index of costume.
     */
  }, {
    key: "setCostume",
    value: function setCostume(index) {
      // Keep the costume index within possible values.
      index = Math.round(index);
      if (index === Infinity || index === -Infinity || !index) {
        index = 0;
      }
      this.currentCostume = MathUtil.wrapClamp(index, 0, this.sprite.costumes.length - 1);
      if (this.renderer) {
        var costume = this.sprite.costumes[this.currentCostume];
        this.renderer.updateDrawableSkinId(this.drawableID, costume.skinId);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Add a costume, taking care to avoid duplicate names.
     * @param {!object} costumeObject Object representing the costume.
     * @param {?int} index Index at which to add costume
     */
  }, {
    key: "addCostume",
    value: function addCostume(costumeObject, index) {
      if (typeof index === 'number' && !isNaN(index)) {
        this.sprite.addCostumeAt(costumeObject, index);
      } else {
        this.sprite.addCostumeAt(costumeObject, this.sprite.costumes.length);
      }
    }

    /**
     * Rename a costume, taking care to avoid duplicate names.
     * @param {int} costumeIndex - the index of the costume to be renamed.
     * @param {string} newName - the desired new name of the costume (will be modified if already in use).
     */
  }, {
    key: "renameCostume",
    value: function renameCostume(costumeIndex, newName) {
      var usedNames = this.sprite.costumes.filter(function (costume, index) {
        return costumeIndex !== index;
      }).map(function (costume) {
        return costume.name;
      });
      var oldName = this.getCostumes()[costumeIndex].name;
      var newUnusedName = StringUtil.unusedName(newName, usedNames);
      this.getCostumes()[costumeIndex].name = newUnusedName;
      if (this.isStage) {
        // Since this is a backdrop, go through all targets and
        // update any blocks referencing the old backdrop name
        var targets = this.runtime.targets;
        for (var i = 0; i < targets.length; i++) {
          var currTarget = targets[i];
          currTarget.blocks.updateAssetName(oldName, newUnusedName, 'backdrop');
        }
      } else {
        this.blocks.updateAssetName(oldName, newUnusedName, 'costume');
      }
    }

    /**
     * Delete a costume by index.
     * @param {number} index Costume index to be deleted
     * @return {?object} The costume that was deleted or null
     * if the index was out of bounds of the costumes list or
     * this target only has one costume.
     */
  }, {
    key: "deleteCostume",
    value: function deleteCostume(index) {
      var originalCostumeCount = this.sprite.costumes.length;
      if (originalCostumeCount === 1) return null;
      if (index < 0 || index >= originalCostumeCount) {
        return null;
      }
      var deletedCostume = this.sprite.deleteCostumeAt(index);
      if (index === this.currentCostume && index === originalCostumeCount - 1) {
        this.setCostume(index - 1);
      } else if (index < this.currentCostume) {
        this.setCostume(this.currentCostume - 1);
      } else {
        this.setCostume(this.currentCostume);
      }
      this.runtime.requestTargetsUpdate(this);
      return deletedCostume;
    }

    /**
     * Add a sound, taking care to avoid duplicate names.
     * @param {!object} soundObject Object representing the sound.
     * @param {?int} index Index at which to add costume
     */
  }, {
    key: "addSound",
    value: function addSound(soundObject, index) {
      var usedNames = this.sprite.sounds.map(function (sound) {
        return sound.name;
      });
      soundObject.name = StringUtil.unusedName(soundObject.name, usedNames);
      if (typeof index === 'number' && !isNaN(index)) {
        this.sprite.sounds.splice(index, 0, soundObject);
      } else {
        this.sprite.sounds.push(soundObject);
      }
    }

    /**
     * Rename a sound, taking care to avoid duplicate names.
     * @param {int} soundIndex - the index of the sound to be renamed.
     * @param {string} newName - the desired new name of the sound (will be modified if already in use).
     */
  }, {
    key: "renameSound",
    value: function renameSound(soundIndex, newName) {
      var usedNames = this.sprite.sounds.filter(function (sound, index) {
        return soundIndex !== index;
      }).map(function (sound) {
        return sound.name;
      });
      var oldName = this.sprite.sounds[soundIndex].name;
      var newUnusedName = StringUtil.unusedName(newName, usedNames);
      this.sprite.sounds[soundIndex].name = newUnusedName;
      this.blocks.updateAssetName(oldName, newUnusedName, 'sound');
    }

    /**
     * Delete a sound by index.
     * @param {number} index Sound index to be deleted
     * @return {object} The deleted sound object, or null if no sound was deleted.
     */
  }, {
    key: "deleteSound",
    value: function deleteSound(index) {
      // Make sure the sound index is not out of bounds
      if (index < 0 || index >= this.sprite.sounds.length) {
        return null;
      }
      // Delete the sound at the given index
      var deletedSound = this.sprite.sounds.splice(index, 1)[0];
      this.runtime.requestTargetsUpdate(this);
      return deletedSound;
    }

    /**
     * Update the rotation style.
     * @param {!string} rotationStyle New rotation style.
     */
  }, {
    key: "setRotationStyle",
    value: function setRotationStyle(rotationStyle) {
      // used by compiler
      if (rotationStyle === RenderedTarget.ROTATION_STYLE_NONE) {
        this.rotationStyle = RenderedTarget.ROTATION_STYLE_NONE;
      } else if (rotationStyle === RenderedTarget.ROTATION_STYLE_ALL_AROUND) {
        this.rotationStyle = RenderedTarget.ROTATION_STYLE_ALL_AROUND;
      } else if (rotationStyle === RenderedTarget.ROTATION_STYLE_LEFT_RIGHT) {
        this.rotationStyle = RenderedTarget.ROTATION_STYLE_LEFT_RIGHT;
      }
      if (this.renderer) {
        var _this$_getRenderedDir3 = this._getRenderedDirectionAndScale(),
          direction = _this$_getRenderedDir3.direction,
          scale = _this$_getRenderedDir3.scale;
        this.renderer.updateDrawableDirectionScale(this.drawableID, direction, scale);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Get a costume index of this rendered target, by name of the costume.
     * @param {?string} costumeName Name of a costume.
     * @return {number} Index of the named costume, or -1 if not present.
     */
  }, {
    key: "getCostumeIndexByName",
    value: function getCostumeIndexByName(costumeName) {
      var costumes = this.getCostumes();
      for (var i = 0; i < costumes.length; i++) {
        if (costumes[i].name === costumeName) {
          return i;
        }
      }
      return -1;
    }

    /**
     * Get a costume of this rendered target by id.
     * @return {object} current costume
     */
  }, {
    key: "getCurrentCostume",
    value: function getCurrentCostume() {
      return this.getCostumes()[this.currentCostume];
    }

    /**
     * Get full costume list
     * @return {object[]} list of costumes
     */
  }, {
    key: "getCostumes",
    value: function getCostumes() {
      // used by compiler
      return this.sprite.costumes;
    }

    /**
     * Reorder costume list by moving costume at costumeIndex to newIndex.
     * @param {!number} costumeIndex Index of the costume to move.
     * @param {!number} newIndex New index for that costume.
     * @returns {boolean} If a change occurred (i.e. if the indices do not match)
     */
  }, {
    key: "reorderCostume",
    value: function reorderCostume(costumeIndex, newIndex) {
      newIndex = MathUtil.clamp(newIndex, 0, this.sprite.costumes.length - 1);
      costumeIndex = MathUtil.clamp(costumeIndex, 0, this.sprite.costumes.length - 1);
      if (newIndex === costumeIndex) return false;
      var currentCostume = this.getCurrentCostume();
      var costume = this.sprite.costumes[costumeIndex];

      // Use the sprite method for deleting costumes because setCostume is handled manually
      this.sprite.deleteCostumeAt(costumeIndex);
      this.addCostume(costume, newIndex);
      this.currentCostume = this.getCostumeIndexByName(currentCostume.name);
      return true;
    }

    /**
     * Reorder sound list by moving sound at soundIndex to newIndex.
     * @param {!number} soundIndex Index of the sound to move.
     * @param {!number} newIndex New index for that sound.
     * @returns {boolean} If a change occurred (i.e. if the indices do not match)
     */
  }, {
    key: "reorderSound",
    value: function reorderSound(soundIndex, newIndex) {
      newIndex = MathUtil.clamp(newIndex, 0, this.sprite.sounds.length - 1);
      soundIndex = MathUtil.clamp(soundIndex, 0, this.sprite.sounds.length - 1);
      if (newIndex === soundIndex) return false;
      var sound = this.sprite.sounds[soundIndex];
      this.deleteSound(soundIndex);
      this.addSound(sound, newIndex);
      return true;
    }

    /**
     * Get full sound list
     * @return {object[]} list of sounds
     */
  }, {
    key: "getSounds",
    value: function getSounds() {
      return this.sprite.sounds;
    }

    /**
     * Update all drawable properties for this rendered target.
     * Use when a batch has changed, e.g., when the drawable is first created.
     */
  }, {
    key: "updateAllDrawableProperties",
    value: function updateAllDrawableProperties() {
      if (this.renderer) {
        var _this$_getRenderedDir4 = this._getRenderedDirectionAndScale(),
          direction = _this$_getRenderedDir4.direction,
          scale = _this$_getRenderedDir4.scale;
        this.renderer.updateDrawablePosition(this.drawableID, [this.x, this.y]);
        this.renderer.updateDrawableDirectionScale(this.drawableID, direction, scale);
        this.renderer.updateDrawableVisible(this.drawableID, this.visible);
        var costume = this.getCostumes()[this.currentCostume];
        this.renderer.updateDrawableSkinId(this.drawableID, costume.skinId);
        for (var effectName in this.effects) {
          if (!Object.prototype.hasOwnProperty.call(this.effects, effectName)) continue;
          this.renderer.updateDrawableEffect(this.drawableID, effectName, this.effects[effectName]);
        }
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
      this.runtime.requestTargetsUpdate(this);
    }

    /**
     * Return the human-readable name for this rendered target, e.g., the sprite's name.
     * @override
     * @returns {string} Human-readable name.
     */
  }, {
    key: "getName",
    value: function getName() {
      return this.sprite.name;
    }

    /**
     * Return whether this rendered target is a sprite (not a clone, not the stage).
     * @return {boolean} True if not a clone and not the stage.
     */
  }, {
    key: "isSprite",
    value: function isSprite() {
      return !this.isStage && this.isOriginal;
    }

    /**
     * Return the rendered target's tight bounding box.
     * Includes top, left, bottom, right attributes in Scratch coordinates.
     * @return {?object} Tight bounding box, or null.
     */
  }, {
    key: "getBounds",
    value: function getBounds() {
      if (this.renderer) {
        return this.runtime.renderer.getBounds(this.drawableID);
      }
      return null;
    }

    /**
     * Return the bounding box around a slice of the top 8px of the rendered target.
     * Includes top, left, bottom, right attributes in Scratch coordinates.
     * @return {?object} Tight bounding box, or null.
     */
  }, {
    key: "getBoundsForBubble",
    value: function getBoundsForBubble() {
      if (this.renderer) {
        return this.runtime.renderer.getBoundsForBubble(this.drawableID);
      }
      return null;
    }

    /**
     * Return whether this target is touching the mouse, an edge, or a sprite.
     * @param {string} requestedObject an id for mouse or edge, or a sprite name.
     * @return {boolean} True if the sprite is touching the object.
     */
  }, {
    key: "isTouchingObject",
    value: function isTouchingObject(requestedObject) {
      // used by compiler
      if (requestedObject === '_mouse_') {
        if (!this.runtime.ioDevices.mouse) return false;
        var mouseX = this.runtime.ioDevices.mouse.getClientX();
        var mouseY = this.runtime.ioDevices.mouse.getClientY();
        return this.isTouchingPoint(mouseX, mouseY);
      } else if (requestedObject === '_edge_') {
        return this.isTouchingEdge();
      }
      return this.isTouchingSprite(requestedObject);
    }

    /**
     * Return whether touching a point.
     * @param {number} x X coordinate of test point.
     * @param {number} y Y coordinate of test point.
     * @return {boolean} True iff the rendered target is touching the point.
     */
  }, {
    key: "isTouchingPoint",
    value: function isTouchingPoint(x, y) {
      if (this.renderer) {
        return this.renderer.drawableTouching(this.drawableID, x, y);
      }
      return false;
    }

    /**
     * Return whether touching a stage edge.
     * @return {boolean} True iff the rendered target is touching the stage edge.
     */
  }, {
    key: "isTouchingEdge",
    value: function isTouchingEdge() {
      if (this.renderer) {
        var stageWidth = this.runtime.stageWidth;
        var stageHeight = this.runtime.stageHeight;
        var bounds = this.getBounds();
        if (bounds.left < -stageWidth / 2 || bounds.right > stageWidth / 2 || bounds.top > stageHeight / 2 || bounds.bottom < -stageHeight / 2) {
          return true;
        }
      }
      return false;
    }

    /**
     * Return whether touching any of a named sprite's clones.
     * @param {string} spriteName Name of the sprite.
     * @return {boolean} True iff touching a clone of the sprite.
     */
  }, {
    key: "isTouchingSprite",
    value: function isTouchingSprite(spriteName) {
      spriteName = Cast.toString(spriteName);
      var firstClone = this.runtime.getSpriteTargetByName(spriteName);
      if (!firstClone || !this.renderer) {
        return false;
      }
      // Filter out dragging targets. This means a sprite that is being dragged
      // can detect other sprites using touching <sprite>, but cannot be detected
      // by other sprites while it is being dragged. This matches Scratch 2.0 behavior.
      var drawableCandidates = firstClone.sprite.clones.filter(function (clone) {
        return !clone.dragging;
      }).map(function (clone) {
        return clone.drawableID;
      });
      return this.renderer.isTouchingDrawables(this.drawableID, drawableCandidates);
    }

    /**
     * Return whether touching a color.
     * @param {Array.<number>} rgb [r,g,b], values between 0-255.
     * @return {Promise.<boolean>} True iff the rendered target is touching the color.
     */
  }, {
    key: "isTouchingColor",
    value: function isTouchingColor(rgb) {
      // used by compiler
      if (this.renderer) {
        return this.renderer.isTouchingColor(this.drawableID, rgb);
      }
      return false;
    }

    /**
     * Return whether rendered target's color is touching a color.
     * @param {object} targetRgb {Array.<number>} [r,g,b], values between 0-255.
     * @param {object} maskRgb {Array.<number>} [r,g,b], values between 0-255.
     * @return {Promise.<boolean>} True iff the color is touching the color.
     */
  }, {
    key: "colorIsTouchingColor",
    value: function colorIsTouchingColor(targetRgb, maskRgb) {
      // used by compiler
      if (this.renderer) {
        return this.renderer.isTouchingColor(this.drawableID, targetRgb, maskRgb);
      }
      return false;
    }
  }, {
    key: "getLayerOrder",
    value: function getLayerOrder() {
      if (this.renderer) {
        return this.renderer.getDrawableOrder(this.drawableID);
      }
      return null;
    }

    /**
     * Move to the front layer.
     */
  }, {
    key: "goToFront",
    value: function goToFront() {
      // This should only ever be used for sprites // used by compiler
      if (this.renderer) {
        // Let the renderer re-order the sprite based on its knowledge
        // of what layers are present
        this.renderer.setDrawableOrder(this.drawableID, Infinity, StageLayering.SPRITE_LAYER);
      }
      this.runtime.setExecutablePosition(this, Infinity);
    }

    /**
     * Move to the back layer.
     */
  }, {
    key: "goToBack",
    value: function goToBack() {
      // This should only ever be used for sprites // used by compiler
      if (this.renderer) {
        // Let the renderer re-order the sprite based on its knowledge
        // of what layers are present
        this.renderer.setDrawableOrder(this.drawableID, -Infinity, StageLayering.SPRITE_LAYER, false);
      }
      this.runtime.setExecutablePosition(this, -Infinity);
    }

    /**
     * Move forward a number of layers.
     * @param {number} nLayers How many layers to go forward.
     */
  }, {
    key: "goForwardLayers",
    value: function goForwardLayers(nLayers) {
      // used by compiler
      if (this.renderer) {
        this.renderer.setDrawableOrder(this.drawableID, nLayers, StageLayering.SPRITE_LAYER, true);
      }
      this.runtime.moveExecutable(this, nLayers);
    }

    /**
     * Move backward a number of layers.
     * @param {number} nLayers How many layers to go backward.
     */
  }, {
    key: "goBackwardLayers",
    value: function goBackwardLayers(nLayers) {
      // used by compiler
      if (this.renderer) {
        this.renderer.setDrawableOrder(this.drawableID, -nLayers, StageLayering.SPRITE_LAYER, true);
      }
      this.runtime.moveExecutable(this, -nLayers);
    }

    /**
     * Move behind some other rendered target.
     * @param {!RenderedTarget} other Other rendered target to move behind.
     */
  }, {
    key: "goBehindOther",
    value: function goBehindOther(other) {
      if (this.renderer) {
        var otherLayer = this.renderer.setDrawableOrder(other.drawableID, 0, StageLayering.SPRITE_LAYER, true);
        this.renderer.setDrawableOrder(this.drawableID, otherLayer, StageLayering.SPRITE_LAYER);
      }
      var executionPosition = this.runtime.executableTargets.indexOf(other);
      this.runtime.setExecutablePosition(this, executionPosition);
    }

    /**
     * Keep a desired position within a fence.
     * @param {number} newX New desired X position.
     * @param {number} newY New desired Y position.
     * @param {object=} optFence Optional fence with left, right, top bottom.
     * @return {Array.<number>} Fenced X and Y coordinates.
     */
  }, {
    key: "keepInFence",
    value: function keepInFence(newX, newY, optFence) {
      var fence = optFence;
      if (!fence) {
        fence = {
          left: -this.runtime.stageWidth / 2,
          right: this.runtime.stageWidth / 2,
          top: this.runtime.stageHeight / 2,
          bottom: -this.runtime.stageHeight / 2
        };
      }
      var bounds = this.getBounds();
      if (!bounds) return;
      // Adjust the known bounds to the target position.
      bounds.left += newX - this.x;
      bounds.right += newX - this.x;
      bounds.top += newY - this.y;
      bounds.bottom += newY - this.y;
      // Find how far we need to move the target position.
      var dx = 0;
      var dy = 0;
      if (bounds.left < fence.left) {
        dx += fence.left - bounds.left;
      }
      if (bounds.right > fence.right) {
        dx += fence.right - bounds.right;
      }
      if (bounds.top > fence.top) {
        dy += fence.top - bounds.top;
      }
      if (bounds.bottom < fence.bottom) {
        dy += fence.bottom - bounds.bottom;
      }
      return [newX + dx, newY + dy];
    }

    /**
     * Make a clone, copying any run-time properties.
     * If we've hit the global clone limit, returns null.
     * @return {RenderedTarget} New clone.
     */
  }, {
    key: "makeClone",
    value: function makeClone() {
      if (!this.runtime.clonesAvailable() || this.isStage) {
        return null; // Hit max clone limit, or this is the stage.
      }
      this.runtime.changeCloneCounter(1);
      var newClone = this.sprite.createClone();
      // Copy all properties.
      newClone.x = this.x;
      newClone.y = this.y;
      newClone.direction = this.direction;
      newClone.draggable = this.draggable;
      newClone.visible = this.visible;
      newClone.size = this.size;
      newClone.currentCostume = this.currentCostume;
      newClone.rotationStyle = this.rotationStyle;
      newClone.effects = Clone.simple(this.effects);
      newClone.variables = this.duplicateVariables();
      newClone._edgeActivatedHatValues = Clone.simple(this._edgeActivatedHatValues);
      newClone.initDrawable(StageLayering.SPRITE_LAYER);
      newClone.updateAllDrawableProperties();
      return newClone;
    }

    /**
     * Make a duplicate using a duplicate sprite.
     * @return {RenderedTarget} New clone.
     */
  }, {
    key: "duplicate",
    value: function duplicate() {
      var _this3 = this;
      return this.sprite.duplicate().then(function (newSprite) {
        var newTarget = newSprite.createClone();
        // Copy all properties.
        // @todo refactor with clone methods
        newTarget.x = (Math.random() - 0.5) * 400 / 2;
        newTarget.y = (Math.random() - 0.5) * 300 / 2;
        newTarget.direction = _this3.direction;
        newTarget.draggable = _this3.draggable;
        newTarget.visible = _this3.visible;
        newTarget.size = _this3.size;
        newTarget.currentCostume = _this3.currentCostume;
        newTarget.rotationStyle = _this3.rotationStyle;
        newTarget.effects = JSON.parse(JSON.stringify(_this3.effects));
        newTarget.variables = _this3.duplicateVariables(newTarget.blocks);
        newTarget.updateAllDrawableProperties();
        return newTarget;
      });
    }

    /**
     * Called when the project receives a "green flag."
     * For a rendered target, this clears graphic effects.
     */
  }, {
    key: "onGreenFlag",
    value: function onGreenFlag() {
      this.clearEffects();
    }

    /**
     * Called when the project receives a "stop all"
     * Stop all sounds and clear graphic effects.
     */
  }, {
    key: "onStopAll",
    value: function onStopAll() {
      this.clearEffects();
    }

    /**
     * Post/edit sprite info.
     * @param {object} data An object with sprite info data to set.
     */
  }, {
    key: "postSpriteInfo",
    value: function postSpriteInfo(data) {
      var force = Object.prototype.hasOwnProperty.call(data, 'force') ? data.force : null;
      var isXChanged = Object.prototype.hasOwnProperty.call(data, 'x');
      var isYChanged = Object.prototype.hasOwnProperty.call(data, 'y');
      if (isXChanged || isYChanged) {
        this.setXY(isXChanged ? data.x : this.x, isYChanged ? data.y : this.y, force);
      }
      if (Object.prototype.hasOwnProperty.call(data, 'direction')) {
        this.setDirection(data.direction);
      }
      if (Object.prototype.hasOwnProperty.call(data, 'draggable')) {
        this.setDraggable(data.draggable);
      }
      if (Object.prototype.hasOwnProperty.call(data, 'rotationStyle')) {
        this.setRotationStyle(data.rotationStyle);
      }
      if (Object.prototype.hasOwnProperty.call(data, 'visible')) {
        this.setVisible(data.visible);
      }
      if (Object.prototype.hasOwnProperty.call(data, 'size')) {
        this.setSize(data.size);
      }
    }

    /**
     * Put the sprite into the drag state. While in effect, setXY must be forced
     */
  }, {
    key: "startDrag",
    value: function startDrag() {
      this.dragging = true;
    }

    /**
     * Remove the sprite from the drag state.
     */
  }, {
    key: "stopDrag",
    value: function stopDrag() {
      this.dragging = false;
    }

    /**
     * Serialize sprite info, used when emitting events about the sprite
     * @returns {object} Sprite data as a simple object
     */
  }, {
    key: "toJSON",
    value: function toJSON() {
      var costumes = this.getCostumes();
      return {
        id: this.id,
        name: this.getName(),
        isStage: this.isStage,
        x: this.x,
        y: this.y,
        size: this.size,
        direction: this.direction,
        draggable: this.draggable,
        currentCostume: this.currentCostume,
        costume: costumes[this.currentCostume],
        costumeCount: costumes.length,
        visible: this.visible,
        rotationStyle: this.rotationStyle,
        comments: this.comments,
        blocks: this.blocks._blocks,
        variables: this.variables,
        costumes: costumes,
        sounds: this.getSounds(),
        textToSpeechLanguage: this.textToSpeechLanguage,
        tempo: this.tempo,
        volume: this.volume,
        videoTransparency: this.videoTransparency,
        videoState: this.videoState
      };
    }

    /**
     * Dispose, destroying any run-time properties.
     */
  }, {
    key: "dispose",
    value: function dispose() {
      if (!this.isOriginal) {
        this.runtime.changeCloneCounter(-1);
      }
      this.runtime.stopForTarget(this);
      this.runtime.removeExecutable(this);
      this.sprite.removeClone(this);
      if (this.renderer && this.drawableID !== null) {
        this.renderer.destroyDrawable(this.drawableID, this.isStage ? StageLayering.BACKGROUND_LAYER : StageLayering.SPRITE_LAYER);
        if (this.visible) {
          this.emitVisualChange();
          this.runtime.requestRedraw();
        }
      }
    }
  }], [{
    key: "ROTATION_STYLE_ALL_AROUND",
    get: function get() {
      return 'all around';
    }

    /**
     * Rotation style for "left-right"/flipping.
     * @type {string}
     */
  }, {
    key: "ROTATION_STYLE_LEFT_RIGHT",
    get: function get() {
      return 'left-right';
    }

    /**
     * Rotation style for "no rotation."
     * @type {string}
     */
  }, {
    key: "ROTATION_STYLE_NONE",
    get: function get() {
      return "don't rotate";
    }

    /**
     * Available states for video input.
     * @enum {string}
     */
  }, {
    key: "VIDEO_STATE",
    get: function get() {
      return {
        OFF: 'off',
        ON: 'on',
        ON_FLIPPED: 'on-flipped'
      };
    }
  }]);
}(Target);
module.exports = RenderedTarget;

/***/ }),

/***/ "../vm/src/sprites/sprite.js":
/*!***********************************!*\
  !*** ../vm/src/sprites/sprite.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var RenderedTarget = __webpack_require__(/*! ./rendered-target */ "../vm/src/sprites/rendered-target.js");
var Blocks = __webpack_require__(/*! ../engine/blocks */ "../vm/src/engine/blocks.js");
var _require = __webpack_require__(/*! ../import/load-sound */ "../vm/src/import/load-sound.js"),
  loadSoundFromAsset = _require.loadSoundFromAsset;
var _require2 = __webpack_require__(/*! ../import/load-costume */ "../vm/src/import/load-costume.js"),
  loadCostumeFromAsset = _require2.loadCostumeFromAsset;
var newBlockIds = __webpack_require__(/*! ../util/new-block-ids */ "../vm/src/util/new-block-ids.js");
var StringUtil = __webpack_require__(/*! ../util/string-util */ "../vm/src/util/string-util.js");
var StageLayering = __webpack_require__(/*! ../engine/stage-layering */ "../vm/src/engine/stage-layering.js");
var Sprite = /*#__PURE__*/function () {
  /**
   * Sprite to be used on the Scratch stage.
   * All clones of a sprite have shared blocks, shared costumes, shared variables,
   * shared sounds, etc.
   * @param {?Blocks} blocks Shared blocks object for all clones of sprite.
   * @param {Runtime} runtime Reference to the runtime.
   * @constructor
   */
  function Sprite(blocks, runtime) {
    _classCallCheck(this, Sprite);
    this.runtime = runtime;
    if (!blocks) {
      // Shared set of blocks for all clones.
      blocks = new Blocks(runtime);
    }
    this.blocks = blocks;
    /**
     * Human-readable name for this sprite (and all clones).
     * @type {string}
     */
    this.name = '';
    /**
     * List of costumes for this sprite.
     * Each entry is an object, e.g.,
     * {
     *      skinId: 1,
     *      name: "Costume Name",
     *      bitmapResolution: 2,
     *      rotationCenterX: 0,
     *      rotationCenterY: 0
     * }
     * @type {Array.<!Object>}
     */
    this.costumes_ = [];
    /**
     * List of sounds for this sprite.
    */
    this.sounds = [];
    /**
     * List of clones for this sprite, including the original.
     * @type {Array.<!RenderedTarget>}
     */
    this.clones = [];
    this.soundBank = null;
    if (this.runtime && this.runtime.audioEngine) {
      this.soundBank = this.runtime.audioEngine.createBank();
    }
  }

  /**
   * Add an array of costumes, taking care to avoid duplicate names.
   * @param {!Array<object>} costumes Array of objects representing costumes.
   */
  return _createClass(Sprite, [{
    key: "costumes",
    get:
    /**
     * Get full costume list
     * @return {object[]} list of costumes. Note that mutating the returned list will not
     *     mutate the list on the sprite. The sprite list should be mutated by calling
     *     addCostumeAt, deleteCostumeAt, or setting costumes.
     */
    function get() {
      return this.costumes_;
    }

    /**
     * Add a costume at the given index, taking care to avoid duplicate names.
     * @param {!object} costumeObject Object representing the costume.
     * @param {!int} index Index at which to add costume
     */,
    set: function set(costumes) {
      this.costumes_ = [];
      var _iterator = _createForOfIteratorHelper(costumes),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var costume = _step.value;
          this.addCostumeAt(costume, this.costumes_.length);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }
  }, {
    key: "addCostumeAt",
    value: function addCostumeAt(costumeObject, index) {
      if (!costumeObject.name) {
        costumeObject.name = '';
      }
      var usedNames = this.costumes_.map(function (costume) {
        return costume.name;
      });
      costumeObject.name = StringUtil.unusedName(costumeObject.name, usedNames);
      this.costumes_.splice(index, 0, costumeObject);
    }

    /**
     * Delete a costume by index.
     * @param {number} index Costume index to be deleted
     * @return {?object} The deleted costume
     */
  }, {
    key: "deleteCostumeAt",
    value: function deleteCostumeAt(index) {
      return this.costumes.splice(index, 1)[0];
    }

    /**
     * Create a clone of this sprite.
     * @param {string=} optLayerGroup Optional layer group the clone's drawable should be added to
     * Defaults to the sprite layer group
     * @returns {!RenderedTarget} Newly created clone.
     */
  }, {
    key: "createClone",
    value: function createClone(optLayerGroup) {
      var newClone = new RenderedTarget(this, this.runtime);
      newClone.isOriginal = this.clones.length === 0;
      this.clones.push(newClone);
      newClone.initAudio();
      if (newClone.isOriginal) {
        // Default to the sprite layer group if optLayerGroup is not provided
        var layerGroup = typeof optLayerGroup === 'string' ? optLayerGroup : StageLayering.SPRITE_LAYER;
        newClone.initDrawable(layerGroup);
        this.runtime.fireTargetWasCreated(newClone);
      } else {
        this.runtime.fireTargetWasCreated(newClone, this.clones[0]);
      }
      return newClone;
    }

    /**
     * Disconnect a clone from this sprite. The clone is unmodified.
     * In particular, the clone's dispose() method is not called.
     * @param {!RenderedTarget} clone - the clone to be removed.
     */
  }, {
    key: "removeClone",
    value: function removeClone(clone) {
      this.runtime.fireTargetWasRemoved(clone);
      var cloneIndex = this.clones.indexOf(clone);
      if (cloneIndex >= 0) {
        this.clones.splice(cloneIndex, 1);
      }
    }
  }, {
    key: "duplicate",
    value: function duplicate() {
      var _this = this;
      var newSprite = new Sprite(null, this.runtime);
      var blocksContainer = this.blocks._blocks;
      var originalBlocks = Object.keys(blocksContainer).map(function (key) {
        return blocksContainer[key];
      });
      var copiedBlocks = JSON.parse(JSON.stringify(originalBlocks));
      newBlockIds(copiedBlocks);
      copiedBlocks.forEach(function (block) {
        newSprite.blocks.createBlock(block);
      });
      var allNames = this.runtime.targets.map(function (t) {
        return t.sprite.name;
      });
      newSprite.name = StringUtil.unusedName(this.name, allNames);
      var assetPromises = [];
      newSprite.costumes = this.costumes_.map(function (costume) {
        var newCostume = Object.assign({}, costume);
        assetPromises.push(loadCostumeFromAsset(newCostume, _this.runtime));
        return newCostume;
      });
      newSprite.sounds = this.sounds.map(function (sound) {
        var newSound = Object.assign({}, sound);
        var soundAsset = sound.asset;
        assetPromises.push(loadSoundFromAsset(newSound, soundAsset, _this.runtime, newSprite.soundBank));
        return newSound;
      });
      return Promise.all(assetPromises).then(function () {
        return newSprite;
      });
    }
  }, {
    key: "dispose",
    value: function dispose() {
      if (this.soundBank) {
        this.soundBank.dispose();
      }
    }
  }]);
}();
module.exports = Sprite;

/***/ }),

/***/ "../vm/src/util/async-limiter.js":
/*!***************************************!*\
  !*** ../vm/src/util/async-limiter.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var AsyncLimiter = /*#__PURE__*/function () {
  function AsyncLimiter(callback, maxConcurrent) {
    _classCallCheck(this, AsyncLimiter);
    this.callback = callback;
    this.maxConcurrent = maxConcurrent;
    this._current = 0;
    this._queue = [];
  }
  return _createClass(AsyncLimiter, [{
    key: "do",
    value: function _do() {
      var _this = this;
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      return new Promise(function (resolve, reject) {
        _this._queue.push([resolve, reject, args]);
        _this._startNext();
      });
    }
  }, {
    key: "_startNext",
    value: function _startNext() {
      var _this2 = this;
      if (this._current >= this.maxConcurrent || this._queue.length === 0) {
        return;
      }
      this._current++;
      var _this$_queue$shift = this._queue.shift(),
        _this$_queue$shift2 = _slicedToArray(_this$_queue$shift, 3),
        resolve = _this$_queue$shift2[0],
        reject = _this$_queue$shift2[1],
        args = _this$_queue$shift2[2];
      this.callback.apply(null, args).then(function (result) {
        resolve(result);
        _this2._current--;
        _this2._startNext();
      })["catch"](function (error) {
        reject(error);
        _this2._current--;
        _this2._startNext();
      });
    }
  }]);
}();
module.exports = AsyncLimiter;

/***/ }),

/***/ "../vm/src/util/base64-util.js":
/*!*************************************!*\
  !*** ../vm/src/util/base64-util.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var atob = __webpack_require__(/*! atob */ "../vm/node_modules/atob/browser-atob.js");
var btoa = __webpack_require__(/*! btoa */ "../vm/node_modules/btoa/index.js");
var Base64Util = /*#__PURE__*/function () {
  function Base64Util() {
    _classCallCheck(this, Base64Util);
  }
  return _createClass(Base64Util, null, [{
    key: "base64ToUint8Array",
    value:
    /**
     * Convert a base64 encoded string to a Uint8Array.
     * @param {string} base64 - a base64 encoded string.
     * @return {Uint8Array} - a decoded Uint8Array.
     */
    function base64ToUint8Array(base64) {
      var binaryString = atob(base64);
      var len = binaryString.length;
      var array = new Uint8Array(len);
      for (var i = 0; i < len; i++) {
        array[i] = binaryString.charCodeAt(i);
      }
      return array;
    }

    /**
     * Convert a Uint8Array to a base64 encoded string.
     * @param {Uint8Array|Array<number>} array - the array to convert.
     * @return {string} - the base64 encoded string.
     */
  }, {
    key: "uint8ArrayToBase64",
    value: function uint8ArrayToBase64(array) {
      var binary = '';
      var len = array.length;
      for (var i = 0; i < len; i++) {
        binary += String.fromCharCode(array[i]);
      }
      return btoa(binary);
    }

    /**
    * Convert an array buffer to a base64 encoded string.
    * @param {array} buffer - an array buffer to convert.
    * @return {string} - the base64 encoded string.
    */
  }, {
    key: "arrayBufferToBase64",
    value: function arrayBufferToBase64(buffer) {
      return Base64Util.uint8ArrayToBase64(new Uint8Array(buffer));
    }
  }]);
}();
module.exports = Base64Util;

/***/ }),

/***/ "../vm/src/util/cast.js":
/*!******************************!*\
  !*** ../vm/src/util/cast.js ***!
  \******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Color = __webpack_require__(/*! ../util/color */ "../vm/src/util/color.js");

/**
 * @fileoverview
 * Utilities for casting and comparing Scratch data-types.
 * Scratch behaves slightly differently from JavaScript in many respects,
 * and these differences should be encapsulated below.
 * For example, in Scratch, add(1, join("hello", world")) -> 1.
 * This is because "hello world" is cast to 0.
 * In JavaScript, 1 + Number("hello" + "world") would give you NaN.
 * Use when coercing a value before computation.
 */

/**
 * Used internally by compare()
 * @param {*} val A value that evaluates to 0 in JS string-to-number conversation such as empty string, 0, or tab.
 * @returns {boolean} True if the value should not be treated as the number zero.
 */
var isNotActuallyZero = function isNotActuallyZero(val) {
  if (typeof val !== 'string') return false;
  for (var i = 0; i < val.length; i++) {
    var code = val.charCodeAt(i);
    // '0'.charCodeAt(0) === 48
    // '\t'.charCodeAt(0) === 9
    // We include tab for compatibility with scratch-www's broken trim() polyfill.
    // https://github.com/TurboWarp/scratch-vm/issues/115
    // https://scratch.mit.edu/projects/788261699/
    if (code === 48 || code === 9) {
      return false;
    }
  }
  return true;
};
var Cast = /*#__PURE__*/function () {
  function Cast() {
    _classCallCheck(this, Cast);
  }
  return _createClass(Cast, null, [{
    key: "toNumber",
    value:
    /**
     * Scratch cast to number.
     * Treats NaN as 0.
     * In Scratch 2.0, this is captured by `interp.numArg.`
     * @param {*} value Value to cast to number.
     * @return {number} The Scratch-casted number value.
     */
    function toNumber(value) {
      // If value is already a number we don't need to coerce it with
      // Number().
      if (typeof value === 'number') {
        // Scratch treats NaN as 0, when needed as a number.
        // E.g., 0 + NaN -> 0.
        if (Number.isNaN(value)) {
          return 0;
        }
        return value;
      }
      var n = Number(value);
      if (Number.isNaN(n)) {
        // Scratch treats NaN as 0, when needed as a number.
        // E.g., 0 + NaN -> 0.
        return 0;
      }
      return n;
    }

    /**
     * Scratch cast to boolean.
     * In Scratch 2.0, this is captured by `interp.boolArg.`
     * Treats some string values differently from JavaScript.
     * @param {*} value Value to cast to boolean.
     * @return {boolean} The Scratch-casted boolean value.
     */
  }, {
    key: "toBoolean",
    value: function toBoolean(value) {
      // Already a boolean?
      if (typeof value === 'boolean') {
        return value;
      }
      if (typeof value === 'string') {
        // These specific strings are treated as false in Scratch.
        if (value === '' || value === '0' || value.toLowerCase() === 'false') {
          return false;
        }
        // All other strings treated as true.
        return true;
      }
      // Coerce other values and numbers.
      return Boolean(value);
    }

    /**
     * Scratch cast to string.
     * @param {*} value Value to cast to string.
     * @return {string} The Scratch-casted string value.
     */
  }, {
    key: "toString",
    value: function toString(value) {
      return String(value);
    }

    /**
     * Cast any Scratch argument to an RGB color array to be used for the renderer.
     * @param {*} value Value to convert to RGB color array.
     * @return {Array.<number>} [r,g,b], values between 0-255.
     */
  }, {
    key: "toRgbColorList",
    value: function toRgbColorList(value) {
      var color = Cast.toRgbColorObject(value);
      return [color.r, color.g, color.b];
    }

    /**
     * Cast any Scratch argument to an RGB color object to be used for the renderer.
     * @param {*} value Value to convert to RGB color object.
     * @return {RGBOject} [r,g,b], values between 0-255.
     */
  }, {
    key: "toRgbColorObject",
    value: function toRgbColorObject(value) {
      var color;
      if (typeof value === 'string' && value.substring(0, 1) === '#') {
        color = Color.hexToRgb(value);

        // If the color wasn't *actually* a hex color, cast to black
        if (!color) color = {
          r: 0,
          g: 0,
          b: 0,
          a: 255
        };
      } else {
        color = Color.decimalToRgb(Cast.toNumber(value));
      }
      return color;
    }

    /**
     * Determine if a Scratch argument is a white space string (or null / empty).
     * @param {*} val value to check.
     * @return {boolean} True if the argument is all white spaces or null / empty.
     */
  }, {
    key: "isWhiteSpace",
    value: function isWhiteSpace(val) {
      return val === null || typeof val === 'string' && val.trim().length === 0;
    }

    /**
     * Compare two values, using Scratch cast, case-insensitive string compare, etc.
     * In Scratch 2.0, this is captured by `interp.compare.`
     * @param {*} v1 First value to compare.
     * @param {*} v2 Second value to compare.
     * @returns {number} Negative number if v1 < v2; 0 if equal; positive otherwise.
     */
  }, {
    key: "compare",
    value: function compare(v1, v2) {
      var n1 = Number(v1);
      var n2 = Number(v2);
      if (n1 === 0 && isNotActuallyZero(v1)) {
        n1 = NaN;
      } else if (n2 === 0 && isNotActuallyZero(v2)) {
        n2 = NaN;
      }
      if (isNaN(n1) || isNaN(n2)) {
        // At least one argument can't be converted to a number.
        // Scratch compares strings as case insensitive.
        var s1 = String(v1).toLowerCase();
        var s2 = String(v2).toLowerCase();
        if (s1 < s2) {
          return -1;
        } else if (s1 > s2) {
          return 1;
        }
        return 0;
      }
      // Handle the special case of Infinity
      if (n1 === Infinity && n2 === Infinity || n1 === -Infinity && n2 === -Infinity) {
        return 0;
      }
      // Compare as numbers.
      return n1 - n2;
    }

    /**
     * Determine if a Scratch argument number represents a round integer.
     * @param {*} val Value to check.
     * @return {boolean} True if number looks like an integer.
     */
  }, {
    key: "isInt",
    value: function isInt(val) {
      // Values that are already numbers.
      if (typeof val === 'number') {
        if (isNaN(val)) {
          // NaN is considered an integer.
          return true;
        }
        // True if it's "round" (e.g., 2.0 and 2).
        return val === Math.floor(val);
      } else if (typeof val === 'boolean') {
        // `True` and `false` always represent integer after Scratch cast.
        return true;
      } else if (typeof val === 'string') {
        // If it contains a decimal point, don't consider it an int.
        return val.indexOf('.') < 0;
      }
      return false;
    }
  }, {
    key: "LIST_INVALID",
    get: function get() {
      return 'INVALID';
    }
  }, {
    key: "LIST_ALL",
    get: function get() {
      return 'ALL';
    }

    /**
     * Compute a 1-based index into a list, based on a Scratch argument.
     * Two special cases may be returned:
     * LIST_ALL: if the block is referring to all of the items in the list.
     * LIST_INVALID: if the index was invalid in any way.
     * @param {*} index Scratch arg, including 1-based numbers or special cases.
     * @param {number} length Length of the list.
     * @param {boolean} acceptAll Whether it should accept "all" or not.
     * @return {(number|string)} 1-based index for list, LIST_ALL, or LIST_INVALID.
     */
  }, {
    key: "toListIndex",
    value: function toListIndex(index, length, acceptAll) {
      if (typeof index !== 'number') {
        if (index === 'all') {
          return acceptAll ? Cast.LIST_ALL : Cast.LIST_INVALID;
        }
        if (index === 'last') {
          if (length > 0) {
            return length;
          }
          return Cast.LIST_INVALID;
        } else if (index === 'random' || index === 'any') {
          if (length > 0) {
            return 1 + Math.floor(Math.random() * length);
          }
          return Cast.LIST_INVALID;
        }
      }
      index = Math.floor(Cast.toNumber(index));
      if (index < 1 || index > length) {
        return Cast.LIST_INVALID;
      }
      return index;
    }
  }]);
}();
module.exports = Cast;

/***/ }),

/***/ "../vm/src/util/clone.js":
/*!*******************************!*\
  !*** ../vm/src/util/clone.js ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * Methods for cloning JavaScript objects.
 * @type {object}
 */
var Clone = /*#__PURE__*/function () {
  function Clone() {
    _classCallCheck(this, Clone);
  }
  return _createClass(Clone, null, [{
    key: "simple",
    value:
    /**
     * Deep-clone a "simple" object: one which can be fully expressed with JSON.
     * Non-JSON values, such as functions, will be stripped from the clone.
     * @param {object} original - the object to be cloned.
     * @returns {object} a deep clone of the original object.
     */
    function simple(original) {
      return JSON.parse(JSON.stringify(original));
    }
  }]);
}();
module.exports = Clone;

/***/ }),

/***/ "../vm/src/util/color.js":
/*!*******************************!*\
  !*** ../vm/src/util/color.js ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Color = /*#__PURE__*/function () {
  function Color() {
    _classCallCheck(this, Color);
  }
  return _createClass(Color, null, [{
    key: "RGB_BLACK",
    get:
    /**
     * @typedef {object} RGBObject - An object representing a color in RGB format.
     * @property {number} r - the red component, in the range [0, 255].
     * @property {number} g - the green component, in the range [0, 255].
     * @property {number} b - the blue component, in the range [0, 255].
     */

    /**
     * @typedef {object} HSVObject - An object representing a color in HSV format.
     * @property {number} h - hue, in the range [0-359).
     * @property {number} s - saturation, in the range [0,1].
     * @property {number} v - value, in the range [0,1].
     */

    /** @type {RGBObject} */
    function get() {
      return {
        r: 0,
        g: 0,
        b: 0
      };
    }

    /** @type {RGBObject} */
  }, {
    key: "RGB_WHITE",
    get: function get() {
      return {
        r: 255,
        g: 255,
        b: 255
      };
    }

    /**
     * Convert a Scratch decimal color to a hex string, #RRGGBB.
     * @param {number} decimal RGB color as a decimal.
     * @return {string} RGB color as #RRGGBB hex string.
     */
  }, {
    key: "decimalToHex",
    value: function decimalToHex(decimal) {
      if (decimal < 0) {
        decimal += 0xFFFFFF + 1;
      }
      var hex = Number(decimal).toString(16);
      hex = "#".concat('000000'.substr(0, 6 - hex.length)).concat(hex);
      return hex;
    }

    /**
     * Convert a Scratch decimal color to an RGB color object.
     * @param {number} decimal RGB color as decimal.
     * @return {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     */
  }, {
    key: "decimalToRgb",
    value: function decimalToRgb(decimal) {
      var a = decimal >> 24 & 0xFF;
      var r = decimal >> 16 & 0xFF;
      var g = decimal >> 8 & 0xFF;
      var b = decimal & 0xFF;
      return {
        r: r,
        g: g,
        b: b,
        a: a > 0 ? a : 255
      };
    }

    /**
     * Convert a hex color (e.g., F00, #03F, #0033FF) to an RGB color object.
     * @param {!string} hex Hex representation of the color.
     * @return {RGBObject} null on failure, or rgb: {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     */
  }, {
    key: "hexToRgb",
    value: function hexToRgb(hex) {
      if (hex.startsWith('#')) {
        hex = hex.substring(1);
      }
      var parsed = parseInt(hex, 16);
      if (isNaN(parsed)) {
        return null;
      }
      if (hex.length === 6) {
        return {
          r: parsed >> 16 & 0xff,
          g: parsed >> 8 & 0xff,
          b: parsed & 0xff
        };
      } else if (hex.length === 3) {
        var r = parsed >> 8 & 0xf;
        var g = parsed >> 4 & 0xf;
        var b = parsed & 0xf;
        return {
          r: r << 4 | r,
          g: g << 4 | g,
          b: b << 4 | b
        };
      }
      return null;
    }

    /**
     * Convert an RGB color object to a hex color.
     * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     * @return {!string} Hex representation of the color.
     */
  }, {
    key: "rgbToHex",
    value: function rgbToHex(rgb) {
      return Color.decimalToHex(Color.rgbToDecimal(rgb));
    }

    /**
     * Convert an RGB color object to a Scratch decimal color.
     * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     * @return {!number} Number representing the color.
     */
  }, {
    key: "rgbToDecimal",
    value: function rgbToDecimal(rgb) {
      return (rgb.r << 16) + (rgb.g << 8) + rgb.b;
    }

    /**
    * Convert a hex color (e.g., F00, #03F, #0033FF) to a decimal color number.
    * @param {!string} hex Hex representation of the color.
    * @return {!number} Number representing the color.
    */
  }, {
    key: "hexToDecimal",
    value: function hexToDecimal(hex) {
      return Color.rgbToDecimal(Color.hexToRgb(hex));
    }

    /**
     * Convert an HSV color to RGB format.
     * @param {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
     * @return {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     */
  }, {
    key: "hsvToRgb",
    value: function hsvToRgb(hsv) {
      var h = hsv.h % 360;
      if (h < 0) h += 360;
      var s = Math.max(0, Math.min(hsv.s, 1));
      var v = Math.max(0, Math.min(hsv.v, 1));
      var i = Math.floor(h / 60);
      var f = h / 60 - i;
      var p = v * (1 - s);
      var q = v * (1 - s * f);
      var t = v * (1 - s * (1 - f));
      var r;
      var g;
      var b;
      switch (i) {
        default:
        case 0:
          r = v;
          g = t;
          b = p;
          break;
        case 1:
          r = q;
          g = v;
          b = p;
          break;
        case 2:
          r = p;
          g = v;
          b = t;
          break;
        case 3:
          r = p;
          g = q;
          b = v;
          break;
        case 4:
          r = t;
          g = p;
          b = v;
          break;
        case 5:
          r = v;
          g = p;
          b = q;
          break;
      }
      return {
        r: Math.floor(r * 255),
        g: Math.floor(g * 255),
        b: Math.floor(b * 255)
      };
    }

    /**
     * Convert an RGB color to HSV format.
     * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
     * @return {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
     */
  }, {
    key: "rgbToHsv",
    value: function rgbToHsv(rgb) {
      var r = rgb.r / 255;
      var g = rgb.g / 255;
      var b = rgb.b / 255;
      var x = Math.min(Math.min(r, g), b);
      var v = Math.max(Math.max(r, g), b);

      // For grays, hue will be arbitrarily reported as zero. Otherwise, calculate
      var h = 0;
      var s = 0;
      if (x !== v) {
        var f = r === x ? g - b : g === x ? b - r : r - g;
        var i = r === x ? 3 : g === x ? 5 : 1;
        h = (i - f / (v - x)) * 60 % 360;
        s = (v - x) / v;
      }
      return {
        h: h,
        s: s,
        v: v
      };
    }

    /**
     * Linear interpolation between rgb0 and rgb1.
     * @param {RGBObject} rgb0 - the color corresponding to fraction1 <= 0.
     * @param {RGBObject} rgb1 - the color corresponding to fraction1 >= 1.
     * @param {number} fraction1 - the interpolation parameter. If this is 0.5, for example, mix the two colors equally.
     * @return {RGBObject} the interpolated color.
     */
  }, {
    key: "mixRgb",
    value: function mixRgb(rgb0, rgb1, fraction1) {
      if (fraction1 <= 0) return rgb0;
      if (fraction1 >= 1) return rgb1;
      var fraction0 = 1 - fraction1;
      return {
        r: fraction0 * rgb0.r + fraction1 * rgb1.r,
        g: fraction0 * rgb0.g + fraction1 * rgb1.g,
        b: fraction0 * rgb0.b + fraction1 * rgb1.b
      };
    }
  }]);
}();
module.exports = Color;

/***/ }),

/***/ "../vm/src/util/fetch-with-timeout.js":
/*!********************************************!*\
  !*** ../vm/src/util/fetch-with-timeout.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {/**
 * @callback FetchFunction
 * @param {RequestInfo|URL} input
 * @param {RequestInit|undefined} [init]
 * @returns {Promise<Response>}
 */

/**
 * @type {FetchFunction}
 */
var myFetch = global.fetch;

/**
 * Tell `fetchWithTimeout` to use a specific `fetch` function.
 * By default, `fetchWithTimeout` will use the global `fetch` function.
 * If there is no global `fetch`, then `fetchWithTimeout` will fail unless provided with an alternative.
 * @param {FetchFunction} newFetch The new `fetch` function to use within fetchWithTimeout.
 */
var setFetch = function setFetch(newFetch) {
  myFetch = newFetch;
};

/**
 * Fetch a remote resource like `fetch` does, but with a time limit.
 * @param {Request|string} resource Remote resource to fetch.
 * @param {?object} init An options object containing any custom settings that you want to apply to the request.
 * @param {number} timeout The amount of time before the request is canceled, in milliseconds
 * @returns {Promise<Response>} The response from the server.
 */
var fetchWithTimeout = function fetchWithTimeout(resource, init, timeout) {
  var timeoutID = null;
  // Not supported in Safari <11
  var controller = window.AbortController ? new window.AbortController() : null;
  var signal = controller ? controller.signal : null;
  // The fetch call races a timer.
  return Promise.race([myFetch(resource, Object.assign({
    signal: signal
  }, init)).then(function (response) {
    clearTimeout(timeoutID);
    return response;
  }, function (error) {
    clearTimeout(timeoutID);
    throw error;
  }), new Promise(function (resolve, reject) {
    timeoutID = setTimeout(function () {
      if (controller) controller.abort();
      reject(new Error("Fetch timed out after ".concat(timeout, " ms")));
    }, timeout);
  })]);
};
module.exports = {
  fetchWithTimeout: fetchWithTimeout,
  setFetch: setFetch
};
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../../gui/node_modules/webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "../vm/src/util/get-monitor-id.js":
/*!****************************************!*\
  !*** ../vm/src/util/get-monitor-id.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Returns a string representing a unique id for a monitored block
 * where a single reporter block can have more than one monitor
 * (and therefore more than one monitor block) associated
 * with it (e.g. when reporter blocks have inputs).
 * @param {string} baseId The base id to use for the different monitor blocks
 * @param {object} fields The monitor block's fields object.
 */
// TODO this function should eventually be the single place where all monitor
// IDs are obtained given an opcode for the reporter block and the list of
// selected parameters.
var getMonitorIdForBlockWithArgs = function getMonitorIdForBlockWithArgs(id, fields) {
  var fieldString = '';
  for (var fieldKey in fields) {
    var fieldValue = fields[fieldKey].value;
    if (fieldKey === 'CURRENTMENU') {
      // The 'sensing_current' block has field values in all caps.
      // However, when importing from scratch 2.0, these
      // could have gotten imported as lower case field values.
      // Normalize the field value here so that we don't ever
      // end up with a different monitor ID representing the same
      // block configuration
      // Note: we are not doing this for every block field that comes into
      // this function so as not to make the faulty assumption that block
      // field values coming in would be unique after being made lower case
      fieldValue = fieldValue.toLowerCase();
    }
    fieldString += "_".concat(fieldValue);
  }
  return "".concat(id).concat(fieldString);
};
module.exports = getMonitorIdForBlockWithArgs;

/***/ }),

/***/ "../vm/src/util/jsonrpc.js":
/*!*********************************!*\
  !*** ../vm/src/util/jsonrpc.js ***!
  \*********************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var JSONRPC = /*#__PURE__*/function () {
  function JSONRPC() {
    _classCallCheck(this, JSONRPC);
    this._requestID = 0;
    this._openRequests = {};
  }

  /**
   * Make an RPC request and retrieve the result.
   * @param {string} method - the remote method to call.
   * @param {object} params - the parameters to pass to the remote method.
   * @returns {Promise} - a promise for the result of the call.
   */
  return _createClass(JSONRPC, [{
    key: "sendRemoteRequest",
    value: function sendRemoteRequest(method, params) {
      var _this = this;
      var requestID = this._requestID++;
      var promise = new Promise(function (resolve, reject) {
        _this._openRequests[requestID] = {
          resolve: resolve,
          reject: reject
        };
      });
      this._sendRequest(method, params, requestID);
      return promise;
    }

    /**
     * Make an RPC notification with no expectation of a result or callback.
     * @param {string} method - the remote method to call.
     * @param {object} params - the parameters to pass to the remote method.
     */
  }, {
    key: "sendRemoteNotification",
    value: function sendRemoteNotification(method, params) {
      this._sendRequest(method, params);
    }

    /**
     * Handle an RPC request from remote, should return a result or Promise for result, if appropriate.
     * @param {string} method - the method requested by the remote caller.
     * @param {object} params - the parameters sent with the remote caller's request.
     */
  }, {
    key: "didReceiveCall",
    value: function didReceiveCall(/* method , params */
    ) {
      throw new Error('Must override didReceiveCall');
    }
  }, {
    key: "_sendMessage",
    value: function _sendMessage(/* jsonMessageObject */
    ) {
      throw new Error('Must override _sendMessage');
    }
  }, {
    key: "_sendRequest",
    value: function _sendRequest(method, params, id) {
      var request = {
        jsonrpc: '2.0',
        method: method,
        params: params
      };
      if (id !== null) {
        request.id = id;
      }
      this._sendMessage(request);
    }
  }, {
    key: "_handleMessage",
    value: function _handleMessage(json) {
      if (json.jsonrpc !== '2.0') {
        throw new Error("Bad or missing JSON-RPC version in message: ".concat(json));
      }
      if (Object.prototype.hasOwnProperty.call(json, 'method')) {
        this._handleRequest(json);
      } else {
        this._handleResponse(json);
      }
    }
  }, {
    key: "_sendResponse",
    value: function _sendResponse(id, result, error) {
      var response = {
        jsonrpc: '2.0',
        id: id
      };
      if (error) {
        response.error = error;
      } else {
        response.result = result || null;
      }
      this._sendMessage(response);
    }
  }, {
    key: "_handleResponse",
    value: function _handleResponse(json) {
      var result = json.result,
        error = json.error,
        id = json.id;
      var openRequest = this._openRequests[id];
      delete this._openRequests[id];
      if (openRequest) {
        if (error) {
          openRequest.reject(error);
        } else {
          openRequest.resolve(result);
        }
      }
    }
  }, {
    key: "_handleRequest",
    value: function _handleRequest(json) {
      var _this2 = this;
      var method = json.method,
        params = json.params,
        id = json.id;
      var rawResult = this.didReceiveCall(method, params);
      if (id !== null && typeof id !== 'undefined') {
        Promise.resolve(rawResult).then(function (result) {
          _this2._sendResponse(id, result);
        }, function (error) {
          _this2._sendResponse(id, null, error);
        });
      }
    }
  }]);
}();
module.exports = JSONRPC;

/***/ }),

/***/ "../vm/src/util/log.js":
/*!*****************************!*\
  !*** ../vm/src/util/log.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var nanolog = __webpack_require__(/*! @turbowarp/nanolog */ "../vm/node_modules/@turbowarp/nanolog/index.js");
nanolog.enable();
module.exports = nanolog('vm');

/***/ }),

/***/ "../vm/src/util/math-util.js":
/*!***********************************!*\
  !*** ../vm/src/util/math-util.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var MathUtil = /*#__PURE__*/function () {
  function MathUtil() {
    _classCallCheck(this, MathUtil);
  }
  return _createClass(MathUtil, null, [{
    key: "degToRad",
    value:
    /**
     * Convert a value from degrees to radians.
     * @param {!number} deg Value in degrees.
     * @return {!number} Equivalent value in radians.
     */
    function degToRad(deg) {
      return deg * Math.PI / 180;
    }

    /**
     * Convert a value from radians to degrees.
     * @param {!number} rad Value in radians.
     * @return {!number} Equivalent value in degrees.
     */
  }, {
    key: "radToDeg",
    value: function radToDeg(rad) {
      return rad * 180 / Math.PI;
    }

    /**
     * Clamp a number between two limits.
     * If n < min, return min. If n > max, return max. Else, return n.
     * @param {!number} n Number to clamp.
     * @param {!number} min Minimum limit.
     * @param {!number} max Maximum limit.
     * @return {!number} Value of n clamped to min and max.
     */
  }, {
    key: "clamp",
    value: function clamp(n, min, max) {
      return Math.min(Math.max(n, min), max);
    }

    /**
     * Keep a number between two limits, wrapping "extra" into the range.
     * e.g., wrapClamp(7, 1, 5) == 2
     * wrapClamp(0, 1, 5) == 5
     * wrapClamp(-11, -10, 6) == 6, etc.
     * @param {!number} n Number to wrap.
     * @param {!number} min Minimum limit.
     * @param {!number} max Maximum limit.
     * @return {!number} Value of n wrapped between min and max.
     */
  }, {
    key: "wrapClamp",
    value: function wrapClamp(n, min, max) {
      var range = max - min + 1;
      return n - Math.floor((n - min) / range) * range;
    }

    /**
     * Convert a value from tan function in degrees.
     * @param {!number} angle in degrees
     * @return {!number} Correct tan value
     */
  }, {
    key: "tan",
    value: function tan(angle) {
      angle = angle % 360;
      switch (angle) {
        case -270:
        case 90:
          return Infinity;
        case -90:
        case 270:
          return -Infinity;
        default:
          return Math.round(Math.tan(Math.PI * angle / 180) * 1e10) / 1e10;
      }
    }

    /**
     * Given an array of unique numbers,
     * returns a reduced array such that each element of the reduced array
     * represents the position of that element in a sorted version of the
     * original array.
     * E.g. [5, 19. 13, 1] => [1, 3, 2, 0]
     * @param {Array<number>} elts The elements to sort and reduce
     * @return {Array<number>} The array of reduced orderings
     */
  }, {
    key: "reducedSortOrdering",
    value: function reducedSortOrdering(elts) {
      var sorted = elts.slice(0).sort(function (a, b) {
        return a - b;
      });
      return elts.map(function (e) {
        return sorted.indexOf(e);
      });
    }

    /**
     * Return a random number given an inclusive range and a number in that
     * range that should be excluded.
     *
     * For instance, (1, 5, 3) will only pick 1, 2, 4, or 5 (with equal
     * probability)
     *
     * @param {number} lower - The lower bound (inlcusive)
     * @param {number} upper - The upper bound (inclusive), such that lower <= upper
     * @param {number} excluded - The number to exclude (MUST be in the range)
     * @return {number} A random integer in the range [lower, upper] that is not "excluded"
     */
  }, {
    key: "inclusiveRandIntWithout",
    value: function inclusiveRandIntWithout(lower, upper, excluded) {
      // Note that subtraction is the number of items in the
      // inclusive range [lower, upper] minus 1 already
      // (e.g. in the set {3, 4, 5}, 5 - 3 = 2).
      var possibleOptions = upper - lower;
      var randInt = lower + Math.floor(Math.random() * possibleOptions);
      if (randInt >= excluded) {
        return randInt + 1;
      }
      return randInt;
    }

    /**
     * Scales a number from one range to another.
     * @param {number} i number to be scaled
     * @param {number} iMin input range minimum
     * @param {number} iMax input range maximum
     * @param {number} oMin output range minimum
     * @param {number} oMax output range maximum
     * @return {number} scaled number
     */
  }, {
    key: "scale",
    value: function scale(i, iMin, iMax, oMin, oMax) {
      var p = (i - iMin) / (iMax - iMin);
      return p * (oMax - oMin) + oMin;
    }
  }]);
}();
module.exports = MathUtil;

/***/ }),

/***/ "../vm/src/util/maybe-format-message.js":
/*!**********************************************!*\
  !*** ../vm/src/util/maybe-format-message.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");

/**
 * Check if `maybeMessage` looks like a message object, and if so pass it to `formatMessage`.
 * Otherwise, return `maybeMessage` as-is.
 * @param {*} maybeMessage - something that might be a message descriptor object.
 * @param {object} [args] - the arguments to pass to `formatMessage` if it gets called.
 * @param {string} [locale] - the locale to pass to `formatMessage` if it gets called.
 * @return {string|*} - the formatted message OR the original `maybeMessage` input.
 */
var maybeFormatMessage = function maybeFormatMessage(maybeMessage, args, locale) {
  if (maybeMessage && maybeMessage.id && maybeMessage["default"]) {
    return formatMessage(maybeMessage, args, locale);
  }
  return maybeMessage;
};
module.exports = maybeFormatMessage;

/***/ }),

/***/ "../vm/src/util/new-block-ids.js":
/*!***************************************!*\
  !*** ../vm/src/util/new-block-ids.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var uid = __webpack_require__(/*! ./uid */ "../vm/src/util/uid.js");

/**
 * Mutate the given blocks to have new IDs and update all internal ID references.
 * Does not return anything to make it clear that the blocks are updated in-place.
 * @param {array} blocks - blocks to be mutated.
 */
module.exports = function (blocks) {
  var oldToNew = {};

  // First update all top-level IDs and create old-to-new mapping
  for (var i = 0; i < blocks.length; i++) {
    var newId = uid();
    var oldId = blocks[i].id;
    blocks[i].id = oldToNew[oldId] = newId;
  }

  // Then go back through and update inputs (block/shadow)
  // and next/parent properties
  for (var _i = 0; _i < blocks.length; _i++) {
    for (var key in blocks[_i].inputs) {
      var input = blocks[_i].inputs[key];
      input.block = oldToNew[input.block];
      input.shadow = oldToNew[input.shadow];
    }
    if (blocks[_i].parent) {
      blocks[_i].parent = oldToNew[blocks[_i].parent];
    }
    if (blocks[_i].next) {
      blocks[_i].next = oldToNew[blocks[_i].next];
    }
  }
};

/***/ }),

/***/ "../vm/src/util/rateLimiter.js":
/*!*************************************!*\
  !*** ../vm/src/util/rateLimiter.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var Timer = __webpack_require__(/*! ../util/timer */ "../vm/src/util/timer.js");
var RateLimiter = /*#__PURE__*/function () {
  /**
   * A utility for limiting the rate of repetitive send operations, such as
   * bluetooth messages being sent to hardware devices. It uses the token bucket
   * strategy: a counter accumulates tokens at a steady rate, and each send costs
   * a token. If no tokens remain, it's not okay to send.
   * @param {number} maxRate the maximum number of sends allowed per second
   * @constructor
   */
  function RateLimiter(maxRate) {
    _classCallCheck(this, RateLimiter);
    /**
     * The maximum number of tokens.
     * @type {number}
     */
    this._maxTokens = maxRate;

    /**
     * The interval in milliseconds for refilling one token. It is calculated
     * so that the tokens will be filled to maximum in one second.
     * @type {number}
     */
    this._refillInterval = 1000 / maxRate;

    /**
     * The current number of tokens in the bucket.
     * @type {number}
     */
    this._count = this._maxTokens;
    this._timer = new Timer();
    this._timer.start();

    /**
     * The last time in milliseconds when the token count was updated.
     * @type {number}
     */
    this._lastUpdateTime = this._timer.timeElapsed();
  }

  /**
   * Check if it is okay to send a message, by updating the token count,
   * taking a token and then checking if we are still under the rate limit.
   * @return {boolean} true if we are under the rate limit
   */
  return _createClass(RateLimiter, [{
    key: "okayToSend",
    value: function okayToSend() {
      // Calculate the number of tokens to refill the bucket with, based on the
      // amount of time since the last refill.
      var now = this._timer.timeElapsed();
      var timeSinceRefill = now - this._lastUpdateTime;
      var refillCount = Math.floor(timeSinceRefill / this._refillInterval);

      // If we're adding at least one token, reset _lastUpdateTime to now.
      // Otherwise, don't reset it so that we can continue measuring time until
      // the next refill.
      if (refillCount > 0) {
        this._lastUpdateTime = now;
      }

      // Refill the tokens up to the maximum
      this._count = Math.min(this._maxTokens, this._count + refillCount);

      // If we have at least one token, use one, and it's okay to send.
      if (this._count > 0) {
        this._count--;
        return true;
      }
      return false;
    }
  }]);
}();
module.exports = RateLimiter;

/***/ }),

/***/ "../vm/src/util/scratch-link-websocket.js":
/*!************************************************!*\
  !*** ../vm/src/util/scratch-link-websocket.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * This class provides a ScratchLinkSocket implementation using WebSockets,
 * attempting to connect with the locally installed Scratch-Link.
 *
 * To connect with ScratchLink without WebSockets, you must implement all of the
 * public methods in this class.
 * - open()
 * - close()
 * - setOn[Open|Close|Error]
 * - setHandleMessage
 * - sendMessage(msgObj)
 * - isOpen()
 */
var ScratchLinkWebSocket = /*#__PURE__*/function () {
  function ScratchLinkWebSocket(type) {
    _classCallCheck(this, ScratchLinkWebSocket);
    this._type = type;
    this._onOpen = null;
    this._onClose = null;
    this._onError = null;
    this._handleMessage = null;
    this._ws = null;
  }
  return _createClass(ScratchLinkWebSocket, [{
    key: "open",
    value: function open() {
      var _this = this;
      if (!(this._onOpen && this._onClose && this._onError && this._handleMessage)) {
        throw new Error('Must set open, close, message and error handlers before calling open on the socket');
      }
      var pathname;
      switch (this._type) {
        case 'BLE':
          pathname = 'scratch/ble';
          break;
        case 'BT':
          pathname = 'scratch/bt';
          break;
        default:
          throw new Error("Unknown ScratchLink socket Type: ".concat(this._type));
      }

      // Try ws:// (the new way) and wss:// (the old way) simultaneously. If either connects, close the other. If we
      // were to try one and fall back to the other on failure, that could mean a delay of 30 seconds or more for
      // those who need the fallback.
      // If both connections fail we should report only one error.

      var setSocket = function setSocket(socketToUse, socketToClose) {
        socketToClose.onopen = socketToClose.onerror = null;
        socketToClose.close();
        _this._ws = socketToUse;
        _this._ws.onopen = _this._onOpen;
        _this._ws.onclose = _this._onClose;
        _this._ws.onerror = _this._onError;
        _this._ws.onmessage = _this._onMessage.bind(_this);
      };
      var ws = new WebSocket("ws://127.0.0.1:20111/".concat(pathname));
      var wss = new WebSocket("wss://device-manager.scratch.mit.edu:20110/".concat(pathname));
      var connectTimeout = setTimeout(function () {
        // neither socket succeeded before the timeout
        setSocket(ws, wss);
        _this._ws.onerror(new Event('timeout'));
      }, 15 * 1000);
      ws.onopen = function (openEvent) {
        clearTimeout(connectTimeout);
        setSocket(ws, wss);
        _this._ws.onopen(openEvent);
      };
      wss.onopen = function (openEvent) {
        clearTimeout(connectTimeout);
        setSocket(wss, ws);
        _this._ws.onopen(openEvent);
      };
      var wsError;
      var wssError;
      var errorHandler = function errorHandler() {
        // if only one has received an error, we haven't overall failed yet
        if (wsError && wssError) {
          clearTimeout(connectTimeout);
          setSocket(ws, wss);
          _this._ws.onerror(wsError);
        }
      };
      ws.onerror = function (errorEvent) {
        wsError = errorEvent;
        errorHandler();
      };
      wss.onerror = function (errorEvent) {
        wssError = errorEvent;
        errorHandler();
      };
    }
  }, {
    key: "close",
    value: function close() {
      this._ws.close();
      this._ws = null;
    }
  }, {
    key: "sendMessage",
    value: function sendMessage(message) {
      var messageText = JSON.stringify(message);
      this._ws.send(messageText);
    }
  }, {
    key: "setOnOpen",
    value: function setOnOpen(fn) {
      this._onOpen = fn;
    }
  }, {
    key: "setOnClose",
    value: function setOnClose(fn) {
      this._onClose = fn;
    }
  }, {
    key: "setOnError",
    value: function setOnError(fn) {
      this._onError = fn;
    }
  }, {
    key: "setHandleMessage",
    value: function setHandleMessage(fn) {
      this._handleMessage = fn;
    }
  }, {
    key: "isOpen",
    value: function isOpen() {
      return this._ws && this._ws.readyState === this._ws.OPEN;
    }
  }, {
    key: "_onMessage",
    value: function _onMessage(e) {
      var json = JSON.parse(e.data);
      this._handleMessage(json);
    }
  }]);
}();
module.exports = ScratchLinkWebSocket;

/***/ }),

/***/ "../vm/src/util/string-util.js":
/*!*************************************!*\
  !*** ../vm/src/util/string-util.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var log = __webpack_require__(/*! ./log */ "../vm/src/util/log.js");
var StringUtil = /*#__PURE__*/function () {
  function StringUtil() {
    _classCallCheck(this, StringUtil);
  }
  return _createClass(StringUtil, null, [{
    key: "withoutTrailingDigits",
    value: function withoutTrailingDigits(s) {
      var i = s.length - 1;
      while (i >= 0 && '0123456789'.indexOf(s.charAt(i)) > -1) i--;
      return s.slice(0, i + 1);
    }
  }, {
    key: "unusedName",
    value: function unusedName(name, existingNames) {
      if (existingNames.indexOf(name) < 0) return name;
      name = StringUtil.withoutTrailingDigits(name);
      var i = 2;
      while (existingNames.indexOf(name + i) >= 0) i++;
      return name + i;
    }

    /**
     * @param {string} name
     * @param {string[]} existingNames
     * @returns {string}
     */
  }, {
    key: "caseInsensitiveUnusedName",
    value: function caseInsensitiveUnusedName(name, existingNames) {
      var exists = function exists(needle) {
        return existingNames.some(function (i) {
          return i.toLowerCase() === needle.toLowerCase();
        });
      };
      if (!exists(name)) return name;
      name = StringUtil.withoutTrailingDigits(name);
      var i = 2;
      while (exists("".concat(name).concat(i))) i++;
      return "".concat(name).concat(i);
    }

    /**
     * Split a string on the first occurrence of a split character.
     * @param {string} text - the string to split.
     * @param {string} separator - split the text on this character.
     * @returns {string[]} - the two parts of the split string, or [text, null] if no split character found.
     * @example
     * // returns ['foo', 'tar.gz']
     * splitFirst('foo.tar.gz', '.');
     * @example
     * // returns ['foo', null]
     * splitFirst('foo', '.');
     * @example
     * // returns ['foo', '']
     * splitFirst('foo.', '.');
     */
  }, {
    key: "splitFirst",
    value: function splitFirst(text, separator) {
      var index = text.indexOf(separator);
      if (index >= 0) {
        return [text.substring(0, index), text.substring(index + 1)];
      }
      return [text, null];
    }

    /**
     * A customized version of JSON.stringify that sets Infinity/NaN to 0,
     * instead of the default (null).
     * Needed because null is not of type number, but Infinity/NaN are, which
     * can lead to serialization producing JSON that isn't valid based on the parser schema.
     * It is also consistent with the behavior of saving 2.0 projects.
     * This is only needed when stringifying an object for saving.
     *
     * @param {!object} obj - The object to serialize
     * @return {!string} The JSON.stringified string with Infinity/NaN replaced with 0
     */
  }, {
    key: "stringify",
    value: function stringify(obj) {
      return JSON.stringify(obj, function (_key, value) {
        if (typeof value === 'number' && (value === Infinity || value === -Infinity || isNaN(value))) {
          return 0;
        }
        return value;
      });
    }
    /**
     * A function to replace unsafe characters (not allowed in XML) with safe ones. This is used
     * in cases where we're replacing non-user facing strings (e.g. variable IDs).
     * When replacing user facing strings, the xmlEscape utility function should be used
     * instead so that the user facing string does not change how it displays.
     * @param {!string | !Array.<string>} unsafe Unsafe string possibly containing unicode control characters.
     * In some cases this argument may be an array (e.g. hacked inputs from 2.0)
     * @return {string} String with control characters replaced.
     */
  }, {
    key: "replaceUnsafeChars",
    value: function replaceUnsafeChars(unsafe) {
      if (typeof unsafe !== 'string') {
        if (Array.isArray(unsafe)) {
          // This happens when we have hacked blocks from 2.0
          // See #1030
          unsafe = String(unsafe);
        } else {
          log.error('Unexpected input recieved in replaceUnsafeChars');
          return unsafe;
        }
      }
      return unsafe.replace(/[<>&'"]/g, function (c) {
        switch (c) {
          case '<':
            return 'lt';
          case '>':
            return 'gt';
          case '&':
            return 'amp';
          case '\'':
            return 'apos';
          case '"':
            return 'quot';
        }
      });
    }
  }]);
}();
module.exports = StringUtil;

/***/ }),

/***/ "../vm/src/util/timer.js":
/*!*******************************!*\
  !*** ../vm/src/util/timer.js ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @fileoverview
 * A utility for accurately measuring time.
 * To use:
 * ---
 * var timer = new Timer();
 * timer.start();
 * ... pass some time ...
 * var timeDifference = timer.timeElapsed();
 * ---
 * Or, you can use the `time` function
 * to do some measurement yourself.
 */
var Timer = /*#__PURE__*/function () {
  function Timer() {
    var nowObj = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : Timer.nowObj;
    _classCallCheck(this, Timer);
    /**
     * Used to store the start time of a timer action.
     * Updated when calling `timer.start`.
     */
    this.startTime = 0;

    /**
     * Used to pass custom logic for determining the value for "now",
     * which is sometimes useful for compatibility with Scratch 2
     */
    this.nowObj = nowObj;
  }

  /**
   * Disable use of self.performance for now as it results in lower performance
   * However, instancing it like below (caching the self.performance to a local variable) negates most of the issues.
   * @type {boolean}
   */
  return _createClass(Timer, [{
    key: "time",
    value:
    /**
     * Return the currently known absolute time, in ms precision.
     * @returns {number} ms elapsed since 1 January 1970 00:00:00 UTC.
     */
    function time() {
      return this.nowObj.now();
    }

    /**
     * Start a timer for measuring elapsed time,
     * at the most accurate precision possible.
     */
  }, {
    key: "start",
    value: function start() {
      this.startTime = this.nowObj.now();
    }
  }, {
    key: "timeElapsed",
    value: function timeElapsed() {
      return this.nowObj.now() - this.startTime;
    }

    /**
     * Call a handler function after a specified amount of time has elapsed.
     * @param {function} handler - function to call after the timeout
     * @param {number} timeout - number of milliseconds to delay before calling the handler
     * @returns {number} - the ID of the new timeout
     */
  }, {
    key: "setTimeout",
    value: function setTimeout(handler, timeout) {
      return global.setTimeout(handler, timeout);
    }

    /**
     * Clear a timeout from the pending timeout pool.
     * @param {number} timeoutId - the ID returned by `setTimeout()`
     * @memberof Timer
     */
  }, {
    key: "clearTimeout",
    value: function clearTimeout(timeoutId) {
      global.clearTimeout(timeoutId);
    }
  }], [{
    key: "USE_PERFORMANCE",
    get: function get() {
      return false;
    }

    /**
     * Legacy object to allow for us to call now to get the old style date time (for backwards compatibility)
     * @deprecated This is only called via the nowObj.now() if no other means is possible...
     */
  }, {
    key: "legacyDateCode",
    get: function get() {
      return {
        now: function now() {
          return new Date().getTime();
        }
      };
    }

    /**
     * Use this object to route all time functions through single access points.
     */
  }, {
    key: "nowObj",
    get: function get() {
      if (Timer.USE_PERFORMANCE && typeof self !== 'undefined' && self.performance && 'now' in self.performance) {
        return self.performance;
      } else if (Date.now) {
        return Date;
      }
      return Timer.legacyDateCode;
    }
  }]);
}();
module.exports = Timer;
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../../gui/node_modules/webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "../vm/src/util/tw-asset-util.js":
/*!***************************************!*\
  !*** ../vm/src/util/tw-asset-util.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var StringUtil = __webpack_require__(/*! ./string-util */ "../vm/src/util/string-util.js");
var AssetUtil = /*#__PURE__*/function () {
  function AssetUtil() {
    _classCallCheck(this, AssetUtil);
  }
  return _createClass(AssetUtil, null, [{
    key: "getByMd5ext",
    value:
    /**
     * @param {Runtime} runtime runtime with storage attached
     * @param {JSZip} zip optional JSZip to search for asset in
     * @param {Storage.assetType} assetType scratch-storage asset type
     * @param {string} md5ext full md5 with file extension
     * @returns {Promise<Storage.Asset>} scratch-storage asset object
     */
    function getByMd5ext(runtime, zip, assetType, md5ext) {
      var idParts = StringUtil.splitFirst(md5ext, '.');
      var md5 = idParts[0];
      var ext = idParts[1].toLowerCase();
      if (zip) {
        // Search the root of the zip
        var file = zip.file(md5ext);

        // Search subfolders of the zip
        // This matches behavior of deserialize-assets.js
        if (!file) {
          var fileMatch = new RegExp("^([^/]*/)?".concat(md5ext, "$"));
          file = zip.file(fileMatch)[0];
        }
        if (file) {
          return runtime.wrapAssetRequest(function () {
            return file.async('uint8array').then(function (data) {
              return runtime.storage.createAsset(assetType, ext, data, md5, false);
            });
          });
        }
      }
      return runtime.wrapAssetRequest(function () {
        return runtime.storage.load(assetType, md5, ext);
      });
    }
  }]);
}();
module.exports = AssetUtil;

/***/ }),

/***/ "../vm/src/util/tw-safe-stringify.js":
/*!*******************************************!*\
  !*** ../vm/src/util/tw-safe-stringify.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var circularReplacer = function circularReplacer() {
  var seen = new WeakSet();
  return function (_, value) {
    if (_typeof(value) === 'object' && value !== null) {
      if (seen.has(value)) {
        return Array.isArray(value) ? '[...]' : '{...}';
      }
      seen.add(value);
    }
    return value;
  };
};

/**
 * Safely stringify, properly handling circular relations and -0.
 * @param {unknown} input Any value
 * @returns {string} A stringified version of the input.
 */
var safeStringify = function safeStringify(input) {
  if (_typeof(input) === 'object' && input !== null) {
    return JSON.stringify(input, circularReplacer());
  }
  // -0 stringifies as "0" by default.
  if (Object.is(input, -0)) {
    return '-0';
  }
  return "".concat(input);
};
module.exports = safeStringify;

/***/ }),

/***/ "../vm/src/util/tw-static-fetch.js":
/*!*****************************************!*\
  !*** ../vm/src/util/tw-static-fetch.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/**
 * @fileoverview
 * The new URL() and fetch() provided by the browser tend to buckle when dealing with URLs that are
 * tens of megabytes in length, which can be common when working with data: URLs in extensions.
 *
 * To help avoid that, this file can "statically" parse some data: URLs without going through
 * unreliable browser APIs.
 */

var Base64Util = __webpack_require__(/*! ./base64-util */ "../vm/src/util/base64-util.js");

/**
 * @param {string} url
 * @returns {Response|null}
 */
var staticFetch = function staticFetch(url) {
  try {
    var simpleDataUrlMatch = url.match(/^data:([/-\w\d]*);base64,/i);
    if (simpleDataUrlMatch) {
      var contentType = simpleDataUrlMatch[1].toLowerCase();
      var base64 = url.substring(simpleDataUrlMatch[0].length);
      var decoded = Base64Util.base64ToUint8Array(base64);
      return new Response(decoded, {
        headers: {
          'content-type': contentType,
          'content-length': decoded.byteLength
        }
      });
    }
  } catch (e) {
    // not robust enough yet to care about these errors
  }
  return null;
};
module.exports = staticFetch;

/***/ }),

/***/ "../vm/src/util/uid.js":
/*!*****************************!*\
  !*** ../vm/src/util/uid.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * @fileoverview UID generator, from Blockly.
 */

/**
 * Legal characters for the unique ID.
 * Should be all on a US keyboard.  No XML special characters or control codes.
 * Removed $ due to issue 251.
 * @private
 */
var soup_ = '!#%()*+,-./:;=?@[]^_`{|}~' + 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';

/**
 * Generate a unique ID, from Blockly.  This should be globally unique.
 * 87 characters ^ 20 length > 128 bits (better than a UUID).
 * @return {string} A globally unique ID string.
 */
var uid = function uid() {
  var length = 20;
  var soupLength = soup_.length;
  var id = [];
  for (var i = 0; i < length; i++) {
    id[i] = soup_.charAt(Math.random() * soupLength);
  }
  return id.join('');
};
module.exports = uid;

/***/ }),

/***/ "../vm/src/util/variable-util.js":
/*!***************************************!*\
  !*** ../vm/src/util/variable-util.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var VariableUtil = /*#__PURE__*/function () {
  function VariableUtil() {
    _classCallCheck(this, VariableUtil);
  }
  return _createClass(VariableUtil, null, [{
    key: "_mergeVarRefObjects",
    value: function _mergeVarRefObjects(accum, obj2) {
      for (var id in obj2) {
        if (accum[id]) {
          accum[id] = accum[id].concat(obj2[id]);
        } else {
          accum[id] = obj2[id];
        }
      }
      return accum;
    }

    /**
     * Get all variable/list references in the given list of targets
     * in the project.
     * @param {Array.<Target>} targets The list of targets to get the variable
     * and list references from.
     * @param {boolean} shouldIncludeBroadcast Whether to include broadcast message fields.
     * @return {object} An object with variable ids as the keys and a list of block fields referencing
     * the variable.
     */
  }, {
    key: "getAllVarRefsForTargets",
    value: function getAllVarRefsForTargets(targets, shouldIncludeBroadcast) {
      return targets.map(function (t) {
        return t.blocks.getAllVariableAndListReferences(null, shouldIncludeBroadcast);
      }).reduce(VariableUtil._mergeVarRefObjects, {});
    }

    /**
     * Give all variable references provided a new id and possibly new name.
     * @param {Array<object>} referencesToUpdate Context of the change, the object containing variable
     * references to update.
     * @param {string} newId ID of the variable that the old references should be replaced with
     * @param {?string} optNewName New variable name to merge with. The old
     * variable name in the references being updated should be replaced with this new name.
     * If this parameter is not provided or is '', no name change occurs.
     */
  }, {
    key: "updateVariableIdentifiers",
    value: function updateVariableIdentifiers(referencesToUpdate, newId, optNewName) {
      referencesToUpdate.map(function (ref) {
        ref.referencingField.id = newId;
        if (optNewName) {
          ref.referencingField.value = optNewName;
        }
        return ref;
      });
    }
  }]);
}();
module.exports = VariableUtil;

/***/ }),

/***/ "../vm/src/util/xml-escape.js":
/*!************************************!*\
  !*** ../vm/src/util/xml-escape.js ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var log = __webpack_require__(/*! ./log */ "../vm/src/util/log.js");

/**
 * Escape a string to be safe to use in XML content.
 * CC-BY-SA: hgoebl
 * https://stackoverflow.com/questions/7918868/
 * how-to-escape-xml-entities-in-javascript
 * @param {!string | !Array.<string>} unsafe Unsafe string.
 * @return {string} XML-escaped string, for use within an XML tag.
 */
var xmlEscape = function xmlEscape(unsafe) {
  if (typeof unsafe !== 'string') {
    if (Array.isArray(unsafe)) {
      // This happens when we have hacked blocks from 2.0
      // See #1030
      unsafe = String(unsafe);
    } else {
      log.error('Unexpected input recieved in replaceUnsafeChars');
      return unsafe;
    }
  }
  return unsafe.replace(/[<>&'"]/g, function (c) {
    switch (c) {
      case '<':
        return '&lt;';
      case '>':
        return '&gt;';
      case '&':
        return '&amp;';
      case '\'':
        return '&apos;';
      case '"':
        return '&quot;';
    }
  });
};
module.exports = xmlEscape;

/***/ }),

/***/ "../vm/src/virtual-machine.js":
/*!************************************!*\
  !*** ../vm/src/virtual-machine.js ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }
var _TextEncoder;
if (typeof TextEncoder === 'undefined') {
  _TextEncoder = __webpack_require__(/*! text-encoding */ "./src/lib/tw-text-encoder.js").TextEncoder;
} else {
  _TextEncoder = TextEncoder;
}
var EventEmitter = __webpack_require__(/*! events */ "./node_modules/events/events.js");
var JSZip = __webpack_require__(/*! @turbowarp/jszip */ "../vm/node_modules/@turbowarp/jszip/dist/jszip.min.js");
var Buffer = __webpack_require__(/*! buffer */ "./node_modules/buffer/index.js").Buffer;
var centralDispatch = __webpack_require__(/*! ./dispatch/central-dispatch */ "../vm/src/dispatch/central-dispatch.js");
var ExtensionManager = __webpack_require__(/*! ./extension-support/extension-manager */ "../vm/src/extension-support/extension-manager.js");
var log = __webpack_require__(/*! ./util/log */ "../vm/src/util/log.js");
var MathUtil = __webpack_require__(/*! ./util/math-util */ "../vm/src/util/math-util.js");
var Runtime = __webpack_require__(/*! ./engine/runtime */ "../vm/src/engine/runtime.js");
var RenderedTarget = __webpack_require__(/*! ./sprites/rendered-target */ "../vm/src/sprites/rendered-target.js");
var Sprite = __webpack_require__(/*! ./sprites/sprite */ "../vm/src/sprites/sprite.js");
var StringUtil = __webpack_require__(/*! ./util/string-util */ "../vm/src/util/string-util.js");
var formatMessage = __webpack_require__(/*! format-message */ "../vm/node_modules/format-message/index.js");
var Variable = __webpack_require__(/*! ./engine/variable */ "../vm/src/engine/variable.js");
var newBlockIds = __webpack_require__(/*! ./util/new-block-ids */ "../vm/src/util/new-block-ids.js");
var _require = __webpack_require__(/*! ./import/load-costume.js */ "../vm/src/import/load-costume.js"),
  loadCostume = _require.loadCostume;
var _require2 = __webpack_require__(/*! ./import/load-sound.js */ "../vm/src/import/load-sound.js"),
  loadSound = _require2.loadSound;
var _require3 = __webpack_require__(/*! ./serialization/serialize-assets */ "../vm/src/serialization/serialize-assets.js"),
  serializeSounds = _require3.serializeSounds,
  serializeCostumes = _require3.serializeCostumes;
__webpack_require__(/*! canvas-toBlob */ "../vm/node_modules/canvas-toBlob/canvas-toBlob.js");
var _require4 = __webpack_require__(/*! ./serialization/tw-costume-import-export */ "../vm/src/serialization/tw-costume-import-export.js"),
  exportCostume = _require4.exportCostume;
var Base64Util = __webpack_require__(/*! ./util/base64-util */ "../vm/src/util/base64-util.js");
var RESERVED_NAMES = ['_mouse_', '_stage_', '_edge_', '_myself_', '_random_'];
var CORE_EXTENSIONS = [
  // 'motion',
  // 'looks',
  // 'sound',
  // 'events',
  // 'control',
  // 'sensing',
  // 'operators',
  // 'variables',
  // 'myBlocks'
];

// Disable missing translation warnings in console
formatMessage.setup({
  missingTranslation: 'ignore'
});
var createRuntimeService = function createRuntimeService(runtime) {
  var service = {};
  service._refreshExtensionPrimitives = runtime._refreshExtensionPrimitives.bind(runtime);
  service._registerExtensionPrimitives = runtime._registerExtensionPrimitives.bind(runtime);
  return service;
};

/**
 * Handles connections between blocks, stage, and extensions.
 * @constructor
 */
var VirtualMachine = /*#__PURE__*/function (_EventEmitter) {
  function VirtualMachine() {
    var _this;
    _classCallCheck(this, VirtualMachine);
    _this = _callSuper(this, VirtualMachine);

    /**
     * VM runtime, to store blocks, I/O devices, sprites/targets, etc.
     * @type {!Runtime}
     */
    _this.runtime = new Runtime();
    centralDispatch.setService('runtime', createRuntimeService(_this.runtime))["catch"](function (e) {
      log.error("Failed to register runtime service: ".concat(JSON.stringify(e)));
    });

    /**
     * The "currently editing"/selected target ID for the VM.
     * Block events from any Blockly workspace are routed to this target.
     * @type {Target}
     */
    _this.editingTarget = null;

    /**
     * The currently dragging target, for redirecting IO data.
     * @type {Target}
     */
    _this._dragTarget = null;

    // Runtime emits are passed along as VM emits.
    _this.runtime.on(Runtime.SCRIPT_GLOW_ON, function (glowData) {
      _this.emit(Runtime.SCRIPT_GLOW_ON, glowData);
    });
    _this.runtime.on(Runtime.SCRIPT_GLOW_OFF, function (glowData) {
      _this.emit(Runtime.SCRIPT_GLOW_OFF, glowData);
    });
    _this.runtime.on(Runtime.BLOCK_GLOW_ON, function (glowData) {
      _this.emit(Runtime.BLOCK_GLOW_ON, glowData);
    });
    _this.runtime.on(Runtime.BLOCK_GLOW_OFF, function (glowData) {
      _this.emit(Runtime.BLOCK_GLOW_OFF, glowData);
    });
    _this.runtime.on(Runtime.PROJECT_START, function () {
      _this.emit(Runtime.PROJECT_START);
    });
    _this.runtime.on(Runtime.PROJECT_RUN_START, function () {
      _this.emit(Runtime.PROJECT_RUN_START);
    });
    _this.runtime.on(Runtime.PROJECT_RUN_STOP, function () {
      _this.emit(Runtime.PROJECT_RUN_STOP);
    });
    _this.runtime.on(Runtime.PROJECT_CHANGED, function () {
      _this.emit(Runtime.PROJECT_CHANGED);
    });
    _this.runtime.on(Runtime.VISUAL_REPORT, function (visualReport) {
      _this.emit(Runtime.VISUAL_REPORT, visualReport);
    });
    _this.runtime.on(Runtime.TARGETS_UPDATE, function (emitProjectChanged) {
      _this.emitTargetsUpdate(emitProjectChanged);
    });
    _this.runtime.on(Runtime.MONITORS_UPDATE, function (monitorList) {
      _this.emit(Runtime.MONITORS_UPDATE, monitorList);
    });
    _this.runtime.on(Runtime.BLOCK_DRAG_UPDATE, function (areBlocksOverGui) {
      _this.emit(Runtime.BLOCK_DRAG_UPDATE, areBlocksOverGui);
    });
    _this.runtime.on(Runtime.BLOCK_DRAG_END, function (blocks, topBlockId) {
      _this.emit(Runtime.BLOCK_DRAG_END, blocks, topBlockId);
    });
    _this.runtime.on(Runtime.EXTENSION_ADDED, function (categoryInfo) {
      _this.emit(Runtime.EXTENSION_ADDED, categoryInfo);
    });
    _this.runtime.on(Runtime.EXTENSION_FIELD_ADDED, function (fieldName, fieldImplementation) {
      _this.emit(Runtime.EXTENSION_FIELD_ADDED, fieldName, fieldImplementation);
    });
    _this.runtime.on(Runtime.BLOCKSINFO_UPDATE, function (categoryInfo) {
      _this.emit(Runtime.BLOCKSINFO_UPDATE, categoryInfo);
    });
    _this.runtime.on(Runtime.BLOCKS_NEED_UPDATE, function () {
      _this.emitWorkspaceUpdate();
    });
    _this.runtime.on(Runtime.TOOLBOX_EXTENSIONS_NEED_UPDATE, function () {
      _this.extensionManager.refreshBlocks();
    });
    _this.runtime.on(Runtime.PERIPHERAL_LIST_UPDATE, function (info) {
      _this.emit(Runtime.PERIPHERAL_LIST_UPDATE, info);
    });
    _this.runtime.on(Runtime.USER_PICKED_PERIPHERAL, function (info) {
      _this.emit(Runtime.USER_PICKED_PERIPHERAL, info);
    });
    _this.runtime.on(Runtime.PERIPHERAL_CONNECTED, function () {
      return _this.emit(Runtime.PERIPHERAL_CONNECTED);
    });
    _this.runtime.on(Runtime.PERIPHERAL_REQUEST_ERROR, function () {
      return _this.emit(Runtime.PERIPHERAL_REQUEST_ERROR);
    });
    _this.runtime.on(Runtime.PERIPHERAL_DISCONNECTED, function () {
      return _this.emit(Runtime.PERIPHERAL_DISCONNECTED);
    });
    _this.runtime.on(Runtime.PERIPHERAL_CONNECTION_LOST_ERROR, function (data) {
      return _this.emit(Runtime.PERIPHERAL_CONNECTION_LOST_ERROR, data);
    });
    _this.runtime.on(Runtime.PERIPHERAL_SCAN_TIMEOUT, function () {
      return _this.emit(Runtime.PERIPHERAL_SCAN_TIMEOUT);
    });
    _this.runtime.on(Runtime.MIC_LISTENING, function (listening) {
      _this.emit(Runtime.MIC_LISTENING, listening);
    });
    _this.runtime.on(Runtime.RUNTIME_STARTED, function () {
      _this.emit(Runtime.RUNTIME_STARTED);
    });
    _this.runtime.on(Runtime.RUNTIME_STOPPED, function () {
      _this.emit(Runtime.RUNTIME_STOPPED);
    });
    _this.runtime.on(Runtime.HAS_CLOUD_DATA_UPDATE, function (hasCloudData) {
      _this.emit(Runtime.HAS_CLOUD_DATA_UPDATE, hasCloudData);
    });
    _this.runtime.on(Runtime.RUNTIME_OPTIONS_CHANGED, function (runtimeOptions) {
      _this.emit(Runtime.RUNTIME_OPTIONS_CHANGED, runtimeOptions);
    });
    _this.runtime.on(Runtime.COMPILER_OPTIONS_CHANGED, function (compilerOptions) {
      _this.emit(Runtime.COMPILER_OPTIONS_CHANGED, compilerOptions);
    });
    _this.runtime.on(Runtime.FRAMERATE_CHANGED, function (framerate) {
      _this.emit(Runtime.FRAMERATE_CHANGED, framerate);
    });
    _this.runtime.on(Runtime.INTERPOLATION_CHANGED, function (framerate) {
      _this.emit(Runtime.INTERPOLATION_CHANGED, framerate);
    });
    _this.runtime.on(Runtime.STAGE_SIZE_CHANGED, function (width, height) {
      _this.emit(Runtime.STAGE_SIZE_CHANGED, width, height);
    });
    _this.runtime.on(Runtime.COMPILE_ERROR, function (target, error) {
      _this.emit(Runtime.COMPILE_ERROR, target, error);
    });
    _this.runtime.on(Runtime.ASSET_PROGRESS, function (finished, total) {
      _this.emit(Runtime.ASSET_PROGRESS, finished, total);
    });
    _this.runtime.on(Runtime.TURBO_MODE_OFF, function () {
      _this.emit(Runtime.TURBO_MODE_OFF);
    });
    _this.runtime.on(Runtime.TURBO_MODE_ON, function () {
      _this.emit(Runtime.TURBO_MODE_ON);
    });
    _this.extensionManager = new ExtensionManager(_this);
    _this.securityManager = _this.extensionManager.securityManager;
    _this.runtime.extensionManager = _this.extensionManager;

    // Load core extensions
    var _iterator = _createForOfIteratorHelper(CORE_EXTENSIONS),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var id = _step.value;
        _this.extensionManager.loadExtensionIdSync(id);
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    _this.blockListener = _this.blockListener.bind(_this);
    _this.flyoutBlockListener = _this.flyoutBlockListener.bind(_this);
    _this.monitorBlockListener = _this.monitorBlockListener.bind(_this);
    _this.variableListener = _this.variableListener.bind(_this);

    /**
     * Export some internal classes for extensions.
     */
    _this.exports = {
      Sprite: Sprite,
      RenderedTarget: RenderedTarget,
      JSZip: JSZip,
      Variable: Variable,
      these_broke_before_and_will_break_again: function these_broke_before_and_will_break_again() {
        console.warn('You are using unsupported APIs. WHEN your code breaks, do not expect help.');
        return {
          JSGenerator: __webpack_require__(/*! ./compiler/jsgen.js */ "../vm/src/compiler/jsgen.js"),
          IRGenerator: __webpack_require__(/*! ./compiler/irgen.js */ "../vm/src/compiler/irgen.js").IRGenerator,
          ScriptTreeGenerator: __webpack_require__(/*! ./compiler/irgen.js */ "../vm/src/compiler/irgen.js").ScriptTreeGenerator,
          IntermediateStackBlock: __webpack_require__(/*! ./compiler/intermediate.js */ "../vm/src/compiler/intermediate.js").IntermediateStackBlock,
          IntermediateInput: __webpack_require__(/*! ./compiler/intermediate.js */ "../vm/src/compiler/intermediate.js").IntermediateInput,
          IntermediateStack: __webpack_require__(/*! ./compiler/intermediate.js */ "../vm/src/compiler/intermediate.js").IntermediateStack,
          IntermediateScript: __webpack_require__(/*! ./compiler/intermediate.js */ "../vm/src/compiler/intermediate.js").IntermediateScript,
          IntermediateRepresentation: __webpack_require__(/*! ./compiler/intermediate.js */ "../vm/src/compiler/intermediate.js").IntermediateRepresentation,
          StackOpcode: __webpack_require__(/*! ./compiler/enums.js */ "../vm/src/compiler/enums.js").StackOpcode,
          InputOpcode: __webpack_require__(/*! ./compiler/enums.js */ "../vm/src/compiler/enums.js").InputOpcode,
          InputType: __webpack_require__(/*! ./compiler/enums.js */ "../vm/src/compiler/enums.js").InputType,
          Thread: __webpack_require__(/*! ./engine/thread.js */ "../vm/src/engine/thread.js"),
          execute: __webpack_require__(/*! ./engine/execute.js */ "../vm/src/engine/execute.js")
        };
      },
      i_will_not_ask_for_help_when_these_break: function i_will_not_ask_for_help_when_these_break() {
        _this.emit('LEGACY_EXTENSION_API', 'i_will_not_ask_for_help_when_these_break');
        var oldCompilerCompatibility = __webpack_require__(/*! ./compiler/old-compiler-compatibility.js */ "../vm/src/compiler/old-compiler-compatibility.js");
        oldCompilerCompatibility.enabled = true;
        return {
          IRGenerator: oldCompilerCompatibility.IRGeneratorStub,
          ScriptTreeGenerator: oldCompilerCompatibility.ScriptTreeGeneratorStub,
          JSGenerator: oldCompilerCompatibility.JSGeneratorStub,
          Thread: __webpack_require__(/*! ./engine/thread.js */ "../vm/src/engine/thread.js"),
          execute: __webpack_require__(/*! ./engine/execute.js */ "../vm/src/engine/execute.js")
        };
      }
    };
    return _this;
  }

  /**
   * Start running the VM - do this before anything else.
   */
  _inherits(VirtualMachine, _EventEmitter);
  return _createClass(VirtualMachine, [{
    key: "start",
    value: function start() {
      this.runtime.start();
    }

    /**
     * @deprecated Used by old versions of TurboWarp. Superceded by upstream's quit()
     */
  }, {
    key: "stop",
    value: function stop() {
      this.quit();
    }

    /**
     * Quit the VM, clearing any handles which might keep the process alive.
     * Do not use the runtime after calling this method. This method is meant for test shutdown.
     */
  }, {
    key: "quit",
    value: function quit() {
      this.runtime.quit();
    }

    /**
     * "Green flag" handler - start all threads starting with a green flag.
     */
  }, {
    key: "greenFlag",
    value: function greenFlag() {
      this.runtime.greenFlag();
    }

    /**
     * Set whether the VM is in "turbo mode."
     * When true, loops don't yield to redraw.
     * @param {boolean} turboModeOn Whether turbo mode should be set.
     */
  }, {
    key: "setTurboMode",
    value: function setTurboMode(turboModeOn) {
      this.runtime.turboMode = !!turboModeOn;
      if (this.runtime.turboMode) {
        this.emit(Runtime.TURBO_MODE_ON);
      } else {
        this.emit(Runtime.TURBO_MODE_OFF);
      }
    }

    /**
     * Set whether the VM is in 2.0 "compatibility mode."
     * When true, ticks go at 2.0 speed (30 TPS).
     * @param {boolean} compatibilityModeOn Whether compatibility mode is set.
     */
  }, {
    key: "setCompatibilityMode",
    value: function setCompatibilityMode(compatibilityModeOn) {
      this.runtime.setCompatibilityMode(!!compatibilityModeOn);
    }
  }, {
    key: "setFramerate",
    value: function setFramerate(framerate) {
      this.runtime.setFramerate(framerate);
    }
  }, {
    key: "setInterpolation",
    value: function setInterpolation(interpolationEnabled) {
      this.runtime.setInterpolation(interpolationEnabled);
    }
  }, {
    key: "setRuntimeOptions",
    value: function setRuntimeOptions(runtimeOptions) {
      this.runtime.setRuntimeOptions(runtimeOptions);
    }
  }, {
    key: "setCompilerOptions",
    value: function setCompilerOptions(compilerOptions) {
      this.runtime.setCompilerOptions(compilerOptions);
    }
  }, {
    key: "setStageSize",
    value: function setStageSize(width, height) {
      this.runtime.setStageSize(width, height);
    }
  }, {
    key: "setInEditor",
    value: function setInEditor(inEditor) {
      this.runtime.setInEditor(inEditor);
    }
  }, {
    key: "convertToPackagedRuntime",
    value: function convertToPackagedRuntime() {
      this.runtime.convertToPackagedRuntime();
    }
  }, {
    key: "addAddonBlock",
    value: function addAddonBlock(options) {
      this.runtime.addAddonBlock(options);
    }
  }, {
    key: "getAddonBlock",
    value: function getAddonBlock(procedureCode) {
      return this.runtime.getAddonBlock(procedureCode);
    }
  }, {
    key: "storeProjectOptions",
    value: function storeProjectOptions() {
      this.runtime.storeProjectOptions();
      if (this.editingTarget.isStage) {
        this.emitWorkspaceUpdate();
      }
    }
  }, {
    key: "enableDebug",
    value: function enableDebug() {
      this.runtime.enableDebug();
      return 'enabled debug mode';
    }
  }, {
    key: "handleExtensionButtonPress",
    value: function handleExtensionButtonPress(buttonData) {
      this.runtime.handleExtensionButtonPress(buttonData);
    }

    /**
     * Stop all threads and running activities.
     */
  }, {
    key: "stopAll",
    value: function stopAll() {
      this.runtime.stopAll();
    }

    /**
     * Clear out current running project data.
     */
  }, {
    key: "clear",
    value: function clear() {
      this.runtime.dispose();
      this.editingTarget = null;
      this.emitTargetsUpdate(false /* Don't emit project change */);
    }

    /**
     * Get data for playground. Data comes back in an emitted event.
     */
  }, {
    key: "getPlaygroundData",
    value: function getPlaygroundData() {
      var instance = this;
      // Only send back thread data for the current editingTarget.
      var threadData = this.runtime.threads.filter(function (thread) {
        return thread.target === instance.editingTarget;
      });
      // Remove the target key, since it's a circular reference.
      var filteredThreadData = JSON.stringify(threadData, function (key, value) {
        if (key === 'target' || key === 'blockContainer') return;
        return value;
      }, 2);
      this.emit('playgroundData', {
        blocks: this.editingTarget.blocks,
        threads: filteredThreadData
      });
    }

    /**
     * Post I/O data to the virtual devices.
     * @param {?string} device Name of virtual I/O device.
     * @param {object} data Any data object to post to the I/O device.
     */
  }, {
    key: "postIOData",
    value: function postIOData(device, data) {
      if (this.runtime.ioDevices[device]) {
        this.runtime.ioDevices[device].postData(data);
      }
    }
  }, {
    key: "setVideoProvider",
    value: function setVideoProvider(videoProvider) {
      this.runtime.ioDevices.video.setProvider(videoProvider);
    }
  }, {
    key: "setCloudProvider",
    value: function setCloudProvider(cloudProvider) {
      this.runtime.ioDevices.cloud.setProvider(cloudProvider);
    }

    /**
     * Tell the specified extension to scan for a peripheral.
     * @param {string} extensionId - the id of the extension.
     */
  }, {
    key: "scanForPeripheral",
    value: function scanForPeripheral(extensionId) {
      this.runtime.scanForPeripheral(extensionId);
    }

    /**
     * Connect to the extension's specified peripheral.
     * @param {string} extensionId - the id of the extension.
     * @param {number} peripheralId - the id of the peripheral.
     */
  }, {
    key: "connectPeripheral",
    value: function connectPeripheral(extensionId, peripheralId) {
      this.runtime.connectPeripheral(extensionId, peripheralId);
    }

    /**
     * Disconnect from the extension's connected peripheral.
     * @param {string} extensionId - the id of the extension.
     */
  }, {
    key: "disconnectPeripheral",
    value: function disconnectPeripheral(extensionId) {
      this.runtime.disconnectPeripheral(extensionId);
    }

    /**
     * Returns whether the extension has a currently connected peripheral.
     * @param {string} extensionId - the id of the extension.
     * @return {boolean} - whether the extension has a connected peripheral.
     */
  }, {
    key: "getPeripheralIsConnected",
    value: function getPeripheralIsConnected(extensionId) {
      return this.runtime.getPeripheralIsConnected(extensionId);
    }

    /**
     * Load a Scratch project from a .sb, .sb2, .sb3 or json string.
     * @param {string | object} input A json string, object, or ArrayBuffer representing the project to load.
     * @return {!Promise} Promise that resolves after targets are installed.
     */
  }, {
    key: "loadProject",
    value: function loadProject(input) {
      var _this2 = this;
      if (_typeof(input) === 'object' && !(input instanceof ArrayBuffer) && !ArrayBuffer.isView(input)) {
        // If the input is an object and not any ArrayBuffer
        // or an ArrayBuffer view (this includes all typed arrays and DataViews)
        // turn the object into a JSON string, because we suspect
        // this is a project.json as an object
        // validate expects a string or buffer as input
        // TODO not sure if we need to check that it also isn't a data view
        input = JSON.stringify(input);
      }
      var validationPromise = new Promise(function (resolve, reject) {
        var validate = __webpack_require__(/*! scratch-parser */ "../vm/node_modules/scratch-parser/index.js");
        // The second argument of false below indicates to the validator that the
        // input should be parsed/validated as an entire project (and not a single sprite)
        validate(input, false, function (error, res) {
          if (error) {
            return reject(error);
          }
          resolve(res);
        });
      })["catch"](function (error) {
        var _require5 = __webpack_require__(/*! scratch-sb1-converter */ "../vm/node_modules/scratch-sb1-converter/index.js"),
          SB1File = _require5.SB1File,
          ValidationError = _require5.ValidationError;
        try {
          var sb1 = new SB1File(input);
          var json = sb1.json;
          json.projectVersion = 2;
          return Promise.resolve([json, sb1.zip]);
        } catch (sb1Error) {
          if (sb1Error instanceof ValidationError || "".concat(sb1Error).includes('Non-ascii character in FixedAsciiString')) {
            // The input does not validate as a Scratch 1 file.
          } else {
            // The project appears to be a Scratch 1 file but it
            // could not be successfully translated into a Scratch 2
            // project.
            return Promise.reject(sb1Error);
          }
        }
        // Throw original error since the input does not appear to be
        // an SB1File.
        return Promise.reject(error);
      });
      return validationPromise.then(function (validatedInput) {
        return _this2.deserializeProject(validatedInput[0], validatedInput[1]);
      }).then(function () {
        return _this2.runtime.handleProjectLoaded();
      })["catch"](function (error) {
        // Intentionally rejecting here (want errors to be handled by caller)
        if (Object.prototype.hasOwnProperty.call(error, 'validationError')) {
          return Promise.reject(JSON.stringify(error));
        }
        return Promise.reject(error);
      });
    }

    /**
     * Load a project from the Scratch web site, by ID.
     * @param {string} id - the ID of the project to download, as a string.
     */
  }, {
    key: "downloadProjectId",
    value: function downloadProjectId(id) {
      var storage = this.runtime.storage;
      if (!storage) {
        log.error('No storage module present; cannot load project: ', id);
        return;
      }
      var vm = this;
      var promise = storage.load(storage.AssetType.Project, id);
      promise.then(function (projectAsset) {
        if (!projectAsset) {
          log.error("Failed to fetch project with id: ".concat(id));
          return null;
        }
        return vm.loadProject(projectAsset.data);
      });
    }

    /**
     * @returns {JSZip} JSZip zip object representing the sb3.
     */
  }, {
    key: "_saveProjectZip",
    value: function _saveProjectZip() {
      var projectJson = this.toJSON();

      // TODO want to eventually move zip creation out of here, and perhaps
      // into scratch-storage
      var zip = new JSZip();

      // Put everything in a zip file
      zip.file('project.json', projectJson);
      this._addFileDescsToZip(this.serializeAssets(), zip);

      // Use a fixed modification date for the files in the zip instead of letting JSZip use the
      // current time to avoid a very small metadata leak and make zipping deterministic. The magic
      // number is from the first TurboWarp/scratch-vm commit after forking
      // (4a93dab4fa3704ab7a1374b9794026b3330f3433).
      var date = new Date(1591657163000);
      for (var _i = 0, _Object$values = Object.values(zip.files); _i < _Object$values.length; _i++) {
        var file = _Object$values[_i];
        file.date = date;
      }

      // Tell JSZip to only compress file formats where there will be a significant gain.
      var COMPRESSABLE_FORMATS = ['.json', '.svg', '.wav', '.ttf', '.otf'];
      var _loop = function _loop() {
        var file = _Object$values2[_i2];
        if (COMPRESSABLE_FORMATS.some(function (ext) {
          return file.name.endsWith(ext);
        })) {
          file.options.compression = 'DEFLATE';
        } else {
          file.options.compression = 'STORE';
        }
      };
      for (var _i2 = 0, _Object$values2 = Object.values(zip.files); _i2 < _Object$values2.length; _i2++) {
        _loop();
      }
      return zip;
    }

    /**
     * @param {JSZip.OutputType} [type] JSZip output type. Defaults to 'blob' for Scratch compatibility.
     * @returns {Promise<unknown>} Compressed sb3 file in a type determined by the type argument.
     */
  }, {
    key: "saveProjectSb3",
    value: function saveProjectSb3(type) {
      return this._saveProjectZip().generateAsync({
        // Don't configure compression here. _saveProjectZip() will set it for each file.
        type: type || 'blob',
        mimeType: 'application/x.scratch.sb3'
      });
    }

    /**
     * @param {JSZip.OutputType} [type] JSZip output type. Defaults to 'arraybuffer'.
     * @returns {StreamHelper} JSZip StreamHelper object generating the compressed sb3.
     * See: https://stuk.github.io/jszip/documentation/api_streamhelper.html
     */
  }, {
    key: "saveProjectSb3Stream",
    value: function saveProjectSb3Stream(type) {
      return this._saveProjectZip().generateInternalStream({
        type: type || 'arraybuffer',
        mimeType: 'application/x.scratch.sb3',
        compression: 'DEFLATE'
      });
    }

    /**
     * tw: Serialize the project into a map of files without actually zipping the project.
     * The buffers returned are the exact same ones used internally, not copies. Avoid directly
     * manipulating them (except project.json, which is created by this function).
     * @returns {Record<string, Uint8Array>} Map of file name to the raw data for that file.
     */
  }, {
    key: "saveProjectSb3DontZip",
    value: function saveProjectSb3DontZip() {
      var projectJson = this.toJSON();
      var files = {
        'project.json': new _TextEncoder().encode(projectJson)
      };
      var _iterator2 = _createForOfIteratorHelper(this.serializeAssets()),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var fileDesc = _step2.value;
          files[fileDesc.fileName] = fileDesc.fileContent;
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      return files;
    }

    /**
     * @type {Array<object>} Array of all assets currently in the runtime
     */
  }, {
    key: "assets",
    get: function get() {
      var costumesAndSounds = this.runtime.targets.reduce(function (acc, target) {
        return acc.concat(target.sprite.sounds.map(function (sound) {
          return sound.asset;
        })).concat(target.sprite.costumes.map(function (costume) {
          return costume.asset;
        }));
      }, []);
      var fonts = this.runtime.fontManager.serializeAssets();
      return [].concat(_toConsumableArray(costumesAndSounds), _toConsumableArray(fonts));
    }

    /**
     * @param {string} targetId Optional ID of target to export
     * @returns {Array<{fileName: string; fileContent: Uint8Array;}} list of file descs
     */
  }, {
    key: "serializeAssets",
    value: function serializeAssets(targetId) {
      var costumeDescs = serializeCostumes(this.runtime, targetId);
      var soundDescs = serializeSounds(this.runtime, targetId);
      var fontDescs = this.runtime.fontManager.serializeAssets().map(function (asset) {
        return {
          fileName: "".concat(asset.assetId, ".").concat(asset.dataFormat),
          fileContent: asset.data
        };
      });
      return [].concat(_toConsumableArray(costumeDescs), _toConsumableArray(soundDescs), _toConsumableArray(fontDescs));
    }
  }, {
    key: "_addFileDescsToZip",
    value: function _addFileDescsToZip(fileDescs, zip) {
      // TODO: sort files, smallest first
      for (var i = 0; i < fileDescs.length; i++) {
        var currFileDesc = fileDescs[i];
        zip.file(currFileDesc.fileName, currFileDesc.fileContent);
      }
    }

    /**
     * Exports a sprite in the sprite3 format.
     * @param {string} targetId ID of the target to export
     * @param {string=} optZipType Optional type that the resulting
     * zip should be outputted in. Options are: base64, binarystring,
     * array, uint8array, arraybuffer, blob, or nodebuffer. Defaults to
     * blob if argument not provided.
     * See https://stuk.github.io/jszip/documentation/api_jszip/generate_async.html#type-option
     * for more information about these options.
     * @return {object} A generated zip of the sprite and its assets in the format
     * specified by optZipType or blob by default.
     */
  }, {
    key: "exportSprite",
    value: function exportSprite(targetId, optZipType) {
      var spriteJson = this.toJSON(targetId);
      var zip = new JSZip();
      zip.file('sprite.json', spriteJson);
      this._addFileDescsToZip(this.serializeAssets(targetId), zip);
      return zip.generateAsync({
        type: typeof optZipType === 'string' ? optZipType : 'blob',
        mimeType: 'application/x.scratch.sprite3',
        compression: 'DEFLATE',
        compressionOptions: {
          level: 6
        }
      });
    }

    /**
     * Export project or sprite as a Scratch 3.0 JSON representation.
     * @param {string=} optTargetId - Optional id of a sprite to serialize
     * @param {*} serializationOptions Options to pass to the serializer
     * @return {string} Serialized state of the runtime.
     */
  }, {
    key: "toJSON",
    value: function toJSON(optTargetId, serializationOptions) {
      var sb3 = __webpack_require__(/*! ./serialization/sb3 */ "../vm/src/serialization/sb3.js");
      return StringUtil.stringify(sb3.serialize(this.runtime, optTargetId, serializationOptions));
    }

    // TODO do we still need this function? Keeping it here so as not to introduce
    // a breaking change.
    /**
     * Load a project from a Scratch JSON representation.
     * @param {string} json JSON string representing a project.
     * @returns {Promise} Promise that resolves after the project has loaded
     */
  }, {
    key: "fromJSON",
    value: function fromJSON(json) {
      log.warn('fromJSON is now just a wrapper around loadProject, please use that function instead.');
      return this.loadProject(json);
    }

    /**
     * Load a project from a Scratch JSON representation.
     * @param {string} projectJSON JSON string representing a project.
     * @param {?JSZip} zip Optional zipped project containing assets to be loaded.
     * @returns {Promise} Promise that resolves after the project has loaded
     */
  }, {
    key: "deserializeProject",
    value: function deserializeProject(projectJSON, zip) {
      var _this3 = this;
      // Clear the current runtime
      this.clear();
      if (typeof performance !== 'undefined') {
        performance.mark('scratch-vm-deserialize-start');
      }
      var runtime = this.runtime;
      var deserializePromise = function deserializePromise() {
        var projectVersion = projectJSON.projectVersion;
        if (projectVersion === 2) {
          var sb2 = __webpack_require__(/*! ./serialization/sb2 */ "../vm/src/serialization/sb2.js");
          return sb2.deserialize(projectJSON, runtime, false, zip);
        }
        if (projectVersion === 3) {
          var sb3 = __webpack_require__(/*! ./serialization/sb3 */ "../vm/src/serialization/sb3.js");
          return sb3.deserialize(projectJSON, runtime, zip);
        }
        // TODO: reject with an Error (possible breaking API change!)
        // eslint-disable-next-line prefer-promise-reject-errors
        return Promise.reject('Unable to verify Scratch Project version.');
      };
      return deserializePromise().then(function (_ref) {
        var targets = _ref.targets,
          extensions = _ref.extensions;
        if (typeof performance !== 'undefined') {
          performance.mark('scratch-vm-deserialize-end');
          try {
            performance.measure('scratch-vm-deserialize', 'scratch-vm-deserialize-start', 'scratch-vm-deserialize-end');
          } catch (e) {
            // performance.measure() will throw an error if the start deserialize
            // marker was removed from memory before we finished deserializing
            // the project. We've seen this happen a couple times when loading
            // very large projects.
            log.error(e);
          }
        }
        return _this3.installTargets(targets, extensions, true);
      });
    }

    /**
     * @param {string[]} extensionIDs The IDs of the extensions
     * @param {Map<string, string>} extensionURLs A map of extension ID to URL
     */
  }, {
    key: "_loadExtensions",
    value: (function () {
      var _loadExtensions2 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(extensionIDs) {
        var extensionURLs,
          defaultExtensionURLs,
          extensionPromises,
          _iterator3,
          _step3,
          extensionID,
          url,
          _args = arguments,
          _t;
        return _regenerator().w(function (_context) {
          while (1) switch (_context.n) {
            case 0:
              extensionURLs = _args.length > 1 && _args[1] !== undefined ? _args[1] : new Map();
              defaultExtensionURLs = __webpack_require__(/*! ./extension-support/tw-default-extension-urls */ "../vm/src/extension-support/tw-default-extension-urls.js");
              extensionPromises = [];
              _iterator3 = _createForOfIteratorHelper(extensionIDs);
              _context.p = 1;
              _iterator3.s();
            case 2:
              if ((_step3 = _iterator3.n()).done) {
                _context.n = 9;
                break;
              }
              extensionID = _step3.value;
              if (!this.extensionManager.isExtensionLoaded(extensionID)) {
                _context.n = 3;
                break;
              }
              _context.n = 8;
              break;
            case 3:
              if (!this.extensionManager.isBuiltinExtension(extensionID)) {
                _context.n = 4;
                break;
              }
              // Builtin extension
              this.extensionManager.loadExtensionIdSync(extensionID);
              _context.n = 8;
              break;
            case 4:
              // Custom extension
              url = extensionURLs.get(extensionID);
              if (!url && Object.prototype.hasOwnProperty.call(defaultExtensionURLs, extensionID)) {
                url = defaultExtensionURLs[extensionID];
              }
              if (url) {
                _context.n = 5;
                break;
              }
              throw new Error("Unknown extension: ".concat(extensionID));
            case 5:
              _context.n = 6;
              return this.securityManager.canLoadExtensionFromProject(url);
            case 6:
              if (!_context.v) {
                _context.n = 7;
                break;
              }
              extensionPromises.push(this.extensionManager.loadExtensionURL(url));
              _context.n = 8;
              break;
            case 7:
              throw new Error("Permission to load extension denied: ".concat(extensionID));
            case 8:
              _context.n = 2;
              break;
            case 9:
              _context.n = 11;
              break;
            case 10:
              _context.p = 10;
              _t = _context.v;
              _iterator3.e(_t);
            case 11:
              _context.p = 11;
              _iterator3.f();
              return _context.f(11);
            case 12:
              return _context.a(2, Promise.all(extensionPromises));
          }
        }, _callee, this, [[1, 10, 11, 12]]);
      }));
      function _loadExtensions(_x) {
        return _loadExtensions2.apply(this, arguments);
      }
      return _loadExtensions;
    }()
    /**
     * Install `deserialize` results: zero or more targets after the extensions (if any) used by those targets.
     * @param {Array.<Target>} targets - the targets to be installed
     * @param {ImportedExtensionsInfo} extensions - metadata about extensions used by these targets
     * @param {boolean} wholeProject - set to true if installing a whole project, as opposed to a single sprite.
     * @returns {Promise} resolved once targets have been installed
     */
    )
  }, {
    key: "installTargets",
    value: (function () {
      var _installTargets = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(targets, extensions, wholeProject) {
        var _this4 = this;
        return _regenerator().w(function (_context2) {
          while (1) switch (_context2.n) {
            case 0:
              _context2.n = 1;
              return this.extensionManager.allAsyncExtensionsLoaded();
            case 1:
              targets = targets.filter(function (target) {
                return !!target;
              });
              return _context2.a(2, this._loadExtensions(extensions.extensionIDs, extensions.extensionURLs).then(function () {
                targets.forEach(function (target) {
                  _this4.runtime.addTarget(target);
                  (/** @type RenderedTarget */target).updateAllDrawableProperties();
                  // Ensure unique sprite name
                  if (target.isSprite()) _this4.renameSprite(target.id, target.getName());
                });
                // Sort the executable targets by layerOrder.
                // Remove layerOrder property after use.
                _this4.runtime.executableTargets.sort(function (a, b) {
                  return a.layerOrder - b.layerOrder;
                });
                targets.forEach(function (target) {
                  delete target.layerOrder;
                });

                // Select the first target for editing, e.g., the first sprite.
                if (wholeProject && targets.length > 1) {
                  _this4.editingTarget = targets[1];
                } else {
                  _this4.editingTarget = targets[0];
                }
                if (!wholeProject) {
                  _this4.editingTarget.fixUpVariableReferences();
                }
                if (wholeProject) {
                  _this4.runtime.parseProjectOptions();
                }

                // Update the VM user's knowledge of targets and blocks on the workspace.
                _this4.emitTargetsUpdate(false /* Don't emit project change */);
                _this4.emitWorkspaceUpdate();
                _this4.runtime.setEditingTarget(_this4.editingTarget);
                _this4.runtime.ioDevices.cloud.setStage(_this4.runtime.getTargetForStage());
              }));
          }
        }, _callee2, this);
      }));
      function installTargets(_x2, _x3, _x4) {
        return _installTargets.apply(this, arguments);
      }
      return installTargets;
    }()
    /**
     * Add a sprite, this could be .sprite2 or .sprite3. Unpack and validate
     * such a file first.
     * @param {string | object} input A json string, object, or ArrayBuffer representing the project to load.
     * @return {!Promise} Promise that resolves after targets are installed.
     */
    )
  }, {
    key: "addSprite",
    value: function addSprite(input) {
      var _this5 = this;
      var errorPrefix = 'Sprite Upload Error:';
      if (_typeof(input) === 'object' && !(input instanceof ArrayBuffer) && !ArrayBuffer.isView(input)) {
        // If the input is an object and not any ArrayBuffer
        // or an ArrayBuffer view (this includes all typed arrays and DataViews)
        // turn the object into a JSON string, because we suspect
        // this is a project.json as an object
        // validate expects a string or buffer as input
        // TODO not sure if we need to check that it also isn't a data view
        input = JSON.stringify(input);
      }
      var validationPromise = new Promise(function (resolve, reject) {
        var validate = __webpack_require__(/*! scratch-parser */ "../vm/node_modules/scratch-parser/index.js");
        // The second argument of true below indicates to the parser/validator
        // that the given input should be treated as a single sprite and not
        // an entire project
        validate(input, true, function (error, res) {
          if (error) return reject(error);
          resolve(res);
        });
      });
      return validationPromise.then(function (validatedInput) {
        var projectVersion = validatedInput[0].projectVersion;
        if (projectVersion === 2) {
          return _this5._addSprite2(validatedInput[0], validatedInput[1]);
        }
        if (projectVersion === 3) {
          return _this5._addSprite3(validatedInput[0], validatedInput[1]);
        }
        // TODO: reject with an Error (possible breaking API change!)
        // eslint-disable-next-line prefer-promise-reject-errors
        return Promise.reject("".concat(errorPrefix, " Unable to verify sprite version."));
      }).then(function () {
        return _this5.runtime.emitProjectChanged();
      })["catch"](function (error) {
        // Intentionally rejecting here (want errors to be handled by caller)
        if (Object.prototype.hasOwnProperty.call(error, 'validationError')) {
          return Promise.reject(JSON.stringify(error));
        }
        // TODO: reject with an Error (possible breaking API change!)
        // eslint-disable-next-line prefer-promise-reject-errors
        return Promise.reject("".concat(errorPrefix, " ").concat(error));
      });
    }

    /**
     * Add a single sprite from the "Sprite2" (i.e., SB2 sprite) format.
     * @param {object} sprite Object representing 2.0 sprite to be added.
     * @param {?ArrayBuffer} zip Optional zip of assets being referenced by json
     * @returns {Promise} Promise that resolves after the sprite is added
     */
  }, {
    key: "_addSprite2",
    value: function _addSprite2(sprite, zip) {
      var _this6 = this;
      // Validate & parse

      var sb2 = __webpack_require__(/*! ./serialization/sb2 */ "../vm/src/serialization/sb2.js");
      return sb2.deserialize(sprite, this.runtime, true, zip).then(function (_ref2) {
        var targets = _ref2.targets,
          extensions = _ref2.extensions;
        return _this6.installTargets(targets, extensions, false);
      });
    }

    /**
     * Add a single sb3 sprite.
     * @param {object} sprite Object rperesenting 3.0 sprite to be added.
     * @param {?ArrayBuffer} zip Optional zip of assets being referenced by target json
     * @returns {Promise} Promise that resolves after the sprite is added
     */
  }, {
    key: "_addSprite3",
    value: function _addSprite3(sprite, zip) {
      var _this7 = this;
      // Validate & parse
      var sb3 = __webpack_require__(/*! ./serialization/sb3 */ "../vm/src/serialization/sb3.js");
      return sb3.deserialize(sprite, this.runtime, zip, true).then(function (_ref3) {
        var targets = _ref3.targets,
          extensions = _ref3.extensions;
        return _this7.installTargets(targets, extensions, false);
      });
    }

    /**
     * Add a costume to the current editing target.
     * @param {string} md5ext - the MD5 and extension of the costume to be loaded.
     * @param {!object} costumeObject Object representing the costume.
     * @property {int} skinId - the ID of the costume's render skin, once installed.
     * @property {number} rotationCenterX - the X component of the costume's origin.
     * @property {number} rotationCenterY - the Y component of the costume's origin.
     * @property {number} [bitmapResolution] - the resolution scale for a bitmap costume.
     * @param {string} optTargetId - the id of the target to add to, if not the editing target.
     * @param {string} optVersion - if this is 2, load costume as sb2, otherwise load costume as sb3.
     * @returns {?Promise} - a promise that resolves when the costume has been added
     */
  }, {
    key: "addCostume",
    value: function addCostume(md5ext, costumeObject, optTargetId, optVersion) {
      var _this8 = this;
      var target = optTargetId ? this.runtime.getTargetById(optTargetId) : this.editingTarget;
      if (target) {
        return loadCostume(md5ext, costumeObject, this.runtime, optVersion).then(function () {
          target.addCostume(costumeObject);
          target.setCostume(target.getCostumes().length - 1);
          _this8.runtime.emitProjectChanged();
        });
      }
      // If the target cannot be found by id, return a rejected promise
      // TODO: reject with an Error (possible breaking API change!)
      // eslint-disable-next-line prefer-promise-reject-errors
      return Promise.reject();
    }

    /**
     * Add a costume loaded from the library to the current editing target.
     * @param {string} md5ext - the MD5 and extension of the costume to be loaded.
     * @param {!object} costumeObject Object representing the costume.
     * @property {int} skinId - the ID of the costume's render skin, once installed.
     * @property {number} rotationCenterX - the X component of the costume's origin.
     * @property {number} rotationCenterY - the Y component of the costume's origin.
     * @property {number} [bitmapResolution] - the resolution scale for a bitmap costume.
     * @returns {?Promise} - a promise that resolves when the costume has been added
     */
  }, {
    key: "addCostumeFromLibrary",
    value: function addCostumeFromLibrary(md5ext, costumeObject) {
      // TODO: reject with an Error (possible breaking API change!)
      // eslint-disable-next-line prefer-promise-reject-errors
      if (!this.editingTarget) return Promise.reject();
      return this.addCostume(md5ext, costumeObject, this.editingTarget.id, 2 /* optVersion */);
    }

    /**
     * Duplicate the costume at the given index. Add it at that index + 1.
     * @param {!int} costumeIndex Index of costume to duplicate
     * @returns {?Promise} - a promise that resolves when the costume has been decoded and added
     */
  }, {
    key: "duplicateCostume",
    value: function duplicateCostume(costumeIndex) {
      var _this9 = this;
      var originalCostume = this.editingTarget.getCostumes()[costumeIndex];
      var clone = Object.assign({}, originalCostume);
      var md5ext = "".concat(clone.assetId, ".").concat(clone.dataFormat);
      return loadCostume(md5ext, clone, this.runtime).then(function () {
        _this9.editingTarget.addCostume(clone, costumeIndex + 1);
        _this9.editingTarget.setCostume(costumeIndex + 1);
        _this9.emitTargetsUpdate();
      });
    }

    /**
     * Duplicate the sound at the given index. Add it at that index + 1.
     * @param {!int} soundIndex Index of sound to duplicate
     * @returns {?Promise} - a promise that resolves when the sound has been decoded and added
     */
  }, {
    key: "duplicateSound",
    value: function duplicateSound(soundIndex) {
      var _this0 = this;
      var originalSound = this.editingTarget.getSounds()[soundIndex];
      var clone = Object.assign({}, originalSound);
      return loadSound(clone, this.runtime, this.editingTarget.sprite.soundBank).then(function () {
        _this0.editingTarget.addSound(clone, soundIndex + 1);
        _this0.emitTargetsUpdate();
      });
    }

    /**
     * Rename a costume on the current editing target.
     * @param {int} costumeIndex - the index of the costume to be renamed.
     * @param {string} newName - the desired new name of the costume (will be modified if already in use).
     */
  }, {
    key: "renameCostume",
    value: function renameCostume(costumeIndex, newName) {
      this.editingTarget.renameCostume(costumeIndex, newName);
      this.emitTargetsUpdate();
    }

    /**
     * Delete a costume from the current editing target.
     * @param {int} costumeIndex - the index of the costume to be removed.
     * @return {?function} A function to restore the deleted costume, or null,
     * if no costume was deleted.
     */
  }, {
    key: "deleteCostume",
    value: function deleteCostume(costumeIndex) {
      var _this1 = this;
      var deletedCostume = this.editingTarget.deleteCostume(costumeIndex);
      if (deletedCostume) {
        var target = this.editingTarget;
        this.runtime.emitProjectChanged();
        return function () {
          target.addCostume(deletedCostume);
          _this1.emitTargetsUpdate();
        };
      }
      return null;
    }

    /**
     * Add a sound to the current editing target.
     * @param {!object} soundObject Object representing the costume.
     * @param {string} optTargetId - the id of the target to add to, if not the editing target.
     * @returns {?Promise} - a promise that resolves when the sound has been decoded and added
     */
  }, {
    key: "addSound",
    value: function addSound(soundObject, optTargetId) {
      var _this10 = this;
      var target = optTargetId ? this.runtime.getTargetById(optTargetId) : this.editingTarget;
      if (target) {
        return loadSound(soundObject, this.runtime, target.sprite.soundBank).then(function () {
          target.addSound(soundObject);
          _this10.emitTargetsUpdate();
        });
      }
      // If the target cannot be found by id, return a rejected promise
      return Promise.reject(new Error("No target with ID: ".concat(optTargetId)));
    }

    /**
     * Rename a sound on the current editing target.
     * @param {int} soundIndex - the index of the sound to be renamed.
     * @param {string} newName - the desired new name of the sound (will be modified if already in use).
     */
  }, {
    key: "renameSound",
    value: function renameSound(soundIndex, newName) {
      this.editingTarget.renameSound(soundIndex, newName);
      this.emitTargetsUpdate();
    }

    /**
     * Get a sound buffer from the audio engine.
     * @param {int} soundIndex - the index of the sound to be got.
     * @return {AudioBuffer} the sound's audio buffer.
     */
  }, {
    key: "getSoundBuffer",
    value: function getSoundBuffer(soundIndex) {
      var id = this.editingTarget.sprite.sounds[soundIndex].soundId;
      if (id && this.runtime && this.runtime.audioEngine) {
        return this.editingTarget.sprite.soundBank.getSoundPlayer(id).buffer;
      }
      return null;
    }

    /**
     * Update a sound buffer.
     * @param {int} soundIndex - the index of the sound to be updated.
     * @param {AudioBuffer} newBuffer - new audio buffer for the audio engine.
     * @param {ArrayBuffer} soundEncoding - the new (wav) encoded sound to be stored
     */
  }, {
    key: "updateSoundBuffer",
    value: function updateSoundBuffer(soundIndex, newBuffer, soundEncoding) {
      var sound = this.editingTarget.sprite.sounds[soundIndex];
      if (sound && sound.broken) delete sound.broken;
      var id = sound ? sound.soundId : null;
      if (id && this.runtime && this.runtime.audioEngine) {
        this.editingTarget.sprite.soundBank.getSoundPlayer(id).buffer = newBuffer;
      }
      // Update sound in runtime
      if (soundEncoding) {
        // Now that we updated the sound, the format should also be updated
        // so that the sound can eventually be decoded the right way.
        // Sounds that were formerly 'adpcm', but were updated in sound editor
        // will not get decoded by the audio engine correctly unless the format
        // is updated as below.
        sound.format = '';
        var storage = this.runtime.storage;
        sound.asset = storage.createAsset(storage.AssetType.Sound, storage.DataFormat.WAV, soundEncoding, null, true // generate md5
        );
        sound.assetId = sound.asset.assetId;
        sound.dataFormat = storage.DataFormat.WAV;
        sound.md5 = "".concat(sound.assetId, ".").concat(sound.dataFormat);
        sound.sampleCount = newBuffer.length;
        sound.rate = newBuffer.sampleRate;
      }
      // If soundEncoding is null, it's because gui had a problem
      // encoding the updated sound. We don't want to store anything in this
      // case, and gui should have logged an error.

      this.emitTargetsUpdate();
    }

    /**
     * Delete a sound from the current editing target.
     * @param {int} soundIndex - the index of the sound to be removed.
     * @return {?Function} A function to restore the sound that was deleted,
     * or null, if no sound was deleted.
     */
  }, {
    key: "deleteSound",
    value: function deleteSound(soundIndex) {
      var _this11 = this;
      var target = this.editingTarget;
      var deletedSound = this.editingTarget.deleteSound(soundIndex);
      if (deletedSound) {
        this.runtime.emitProjectChanged();
        var restoreFun = function restoreFun() {
          target.addSound(deletedSound);
          _this11.emitTargetsUpdate();
        };
        return restoreFun;
      }
      return null;
    }

    /**
     * Get a string representation of the image from storage.
     * @param {int} costumeIndex - the index of the costume to be got.
     * @return {string} the costume's SVG string if it's SVG,
     *     a dataURI if it's a PNG or JPG, or null if it couldn't be found or decoded.
     */
  }, {
    key: "getCostume",
    value: function getCostume(costumeIndex) {
      var asset = this.editingTarget.getCostumes()[costumeIndex].asset;
      if (!asset || !this.runtime || !this.runtime.storage) return null;
      var format = asset.dataFormat;
      if (format === this.runtime.storage.DataFormat.SVG) {
        return asset.decodeText();
      } else if (format === this.runtime.storage.DataFormat.PNG || format === this.runtime.storage.DataFormat.JPG) {
        return asset.encodeDataURI();
      }
      log.error("Unhandled format: ".concat(asset.dataFormat));
      return null;
    }

    /**
     * TW: Get the raw binary data to use when exporting a costume to the user's local file system.
     * @param {Costume} costumeObject scratch-vm costume object
     * @returns {Uint8Array}
     */
  }, {
    key: "getExportedCostume",
    value: function getExportedCostume(costumeObject) {
      return exportCostume(costumeObject);
    }

    /**
     * TW: Get a base64 string to use when exporting a costume to the user's local file system.
     * @param {Costume} costumeObject scratch-vm costume object
     * @returns {string} base64 string. Not a data: URI.
     */
  }, {
    key: "getExportedCostumeBase64",
    value: function getExportedCostumeBase64(costumeObject) {
      var binaryData = this.getExportedCostume(costumeObject);
      return Base64Util.uint8ArrayToBase64(binaryData);
    }

    /**
     * Update a costume with the given bitmap
     * @param {!int} costumeIndex - the index of the costume to be updated.
     * @param {!ImageData} bitmap - new bitmap for the renderer.
     * @param {!number} rotationCenterX x of point about which the costume rotates, relative to its upper left corner
     * @param {!number} rotationCenterY y of point about which the costume rotates, relative to its upper left corner
     * @param {!number} bitmapResolution 1 for bitmaps that have 1 pixel per unit of stage,
     *     2 for double-resolution bitmaps
     */
  }, {
    key: "updateBitmap",
    value: function updateBitmap(costumeIndex, bitmap, rotationCenterX, rotationCenterY, bitmapResolution) {
      return this._updateBitmap(this.editingTarget.getCostumes()[costumeIndex], bitmap, rotationCenterX, rotationCenterY, bitmapResolution);
    }
  }, {
    key: "_updateBitmap",
    value: function _updateBitmap(costume, bitmap, rotationCenterX, rotationCenterY, bitmapResolution) {
      var _this12 = this;
      if (!(costume && this.runtime && this.runtime.renderer)) return;
      if (costume && costume.broken) delete costume.broken;
      costume.rotationCenterX = rotationCenterX;
      costume.rotationCenterY = rotationCenterY;

      // If the bitmap originally had a zero width or height, use that value
      var bitmapWidth = bitmap.sourceWidth === 0 ? 0 : bitmap.width;
      var bitmapHeight = bitmap.sourceHeight === 0 ? 0 : bitmap.height;
      // @todo: updateBitmapSkin does not take ImageData
      var canvas = document.createElement('canvas');
      canvas.width = bitmapWidth;
      canvas.height = bitmapHeight;
      var context = canvas.getContext('2d');
      context.putImageData(bitmap, 0, 0);

      // Divide by resolution because the renderer's definition of the rotation center
      // is the rotation center divided by the bitmap resolution
      this.runtime.renderer.updateBitmapSkin(costume.skinId, canvas, bitmapResolution, [rotationCenterX / bitmapResolution, rotationCenterY / bitmapResolution]);

      // @todo there should be a better way to get from ImageData to a decodable storage format
      canvas.toBlob(function (blob) {
        var reader = new FileReader();
        reader.addEventListener('loadend', function () {
          var storage = _this12.runtime.storage;
          costume.dataFormat = storage.DataFormat.PNG;
          costume.bitmapResolution = bitmapResolution;
          costume.size = [bitmapWidth, bitmapHeight];
          costume.asset = storage.createAsset(storage.AssetType.ImageBitmap, costume.dataFormat, Buffer.from(reader.result), null,
          // id
          true // generate md5
          );
          costume.assetId = costume.asset.assetId;
          costume.md5 = "".concat(costume.assetId, ".").concat(costume.dataFormat);
          _this12.emitTargetsUpdate();
        });
        // Bitmaps with a zero width or height return null for their blob
        if (blob) {
          reader.readAsArrayBuffer(blob);
        }
      });
    }

    /**
     * Update a costume with the given SVG
     * @param {int} costumeIndex - the index of the costume to be updated.
     * @param {string} svg - new SVG for the renderer.
     * @param {number} rotationCenterX x of point about which the costume rotates, relative to its upper left corner
     * @param {number} rotationCenterY y of point about which the costume rotates, relative to its upper left corner
     */
  }, {
    key: "updateSvg",
    value: function updateSvg(costumeIndex, svg, rotationCenterX, rotationCenterY) {
      return this._updateSvg(this.editingTarget.getCostumes()[costumeIndex], svg, rotationCenterX, rotationCenterY);
    }
  }, {
    key: "_updateSvg",
    value: function _updateSvg(costume, svg, rotationCenterX, rotationCenterY) {
      if (costume && costume.broken) delete costume.broken;
      if (costume && this.runtime && this.runtime.renderer) {
        costume.rotationCenterX = rotationCenterX;
        costume.rotationCenterY = rotationCenterY;
        this.runtime.renderer.updateSVGSkin(costume.skinId, svg, [rotationCenterX, rotationCenterY]);
        costume.size = this.runtime.renderer.getSkinSize(costume.skinId);
      }
      var storage = this.runtime.storage;
      // If we're in here, we've edited an svg in the vector editor,
      // so the dataFormat should be 'svg'
      costume.dataFormat = storage.DataFormat.SVG;
      costume.bitmapResolution = 1;
      costume.asset = storage.createAsset(storage.AssetType.ImageVector, costume.dataFormat, new _TextEncoder().encode(svg), null, true // generate md5
      );
      costume.assetId = costume.asset.assetId;
      costume.md5 = "".concat(costume.assetId, ".").concat(costume.dataFormat);
      this.emitTargetsUpdate();
    }

    /**
     * Add a backdrop to the stage.
     * @param {string} md5ext - the MD5 and extension of the backdrop to be loaded.
     * @param {!object} backdropObject Object representing the backdrop.
     * @property {int} skinId - the ID of the backdrop's render skin, once installed.
     * @property {number} rotationCenterX - the X component of the backdrop's origin.
     * @property {number} rotationCenterY - the Y component of the backdrop's origin.
     * @property {number} [bitmapResolution] - the resolution scale for a bitmap backdrop.
     * @returns {?Promise} - a promise that resolves when the backdrop has been added
     */
  }, {
    key: "addBackdrop",
    value: function addBackdrop(md5ext, backdropObject) {
      var _this13 = this;
      return loadCostume(md5ext, backdropObject, this.runtime).then(function () {
        var stage = _this13.runtime.getTargetForStage();
        stage.addCostume(backdropObject);
        stage.setCostume(stage.getCostumes().length - 1);
        _this13.runtime.emitProjectChanged();
      });
    }

    /**
     * Rename a sprite.
     * @param {string} targetId ID of a target whose sprite to rename.
     * @param {string} newName New name of the sprite.
     */
  }, {
    key: "renameSprite",
    value: function renameSprite(targetId, newName) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        if (!target.isSprite()) {
          throw new Error('Cannot rename non-sprite targets.');
        }
        var sprite = target.sprite;
        if (!sprite) {
          throw new Error('No sprite associated with this target.');
        }
        if (newName && RESERVED_NAMES.indexOf(newName) === -1) {
          var names = this.runtime.targets.filter(function (runtimeTarget) {
            return runtimeTarget.isSprite() && runtimeTarget.id !== target.id;
          }).map(function (runtimeTarget) {
            return runtimeTarget.sprite.name;
          });
          var oldName = sprite.name;
          var newUnusedName = StringUtil.unusedName(newName, names);
          sprite.name = newUnusedName;
          if (oldName === newUnusedName) {
            return;
          }
          var allTargets = this.runtime.targets;
          for (var i = 0; i < allTargets.length; i++) {
            var currTarget = allTargets[i];
            currTarget.blocks.updateAssetName(oldName, newName, 'sprite');
          }
          if (newUnusedName !== oldName) this.emitTargetsUpdate();
        }
      } else {
        throw new Error('No target with the provided id.');
      }
    }

    /**
     * Delete a sprite and all its clones.
     * @param {string} targetId ID of a target whose sprite to delete.
     * @return {Function} Returns a function to restore the sprite that was deleted
     */
  }, {
    key: "deleteSprite",
    value: function deleteSprite(targetId) {
      var _this14 = this;
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        var targetIndexBeforeDelete = this.runtime.targets.map(function (t) {
          return t.id;
        }).indexOf(target.id);
        if (!target.isSprite()) {
          throw new Error('Cannot delete non-sprite targets.');
        }
        var sprite = target.sprite;
        if (!sprite) {
          throw new Error('No sprite associated with this target.');
        }
        var spritePromise = this.exportSprite(targetId, 'uint8array');
        var restoreSprite = function restoreSprite() {
          return spritePromise.then(function (spriteBuffer) {
            return _this14.addSprite(spriteBuffer);
          });
        };
        // Remove monitors from the runtime state and remove the
        // target-specific monitored blocks (e.g. local variables)
        target.deleteMonitors();
        var currentEditingTarget = this.editingTarget;
        for (var i = 0; i < sprite.clones.length; i++) {
          var clone = sprite.clones[i];
          this.runtime.stopForTarget(sprite.clones[i]);
          this.runtime.disposeTarget(sprite.clones[i]);
          // Ensure editing target is switched if we are deleting it.
          if (clone === currentEditingTarget) {
            var nextTargetIndex = Math.min(this.runtime.targets.length - 1, targetIndexBeforeDelete);
            if (this.runtime.targets.length > 0) {
              this.setEditingTarget(this.runtime.targets[nextTargetIndex].id);
            } else {
              this.editingTarget = null;
            }
          }
        }
        // Sprite object should be deleted by GC.
        this.emitTargetsUpdate();
        return restoreSprite;
      }
      throw new Error('No target with the provided id.');
    }

    /**
     * Duplicate a sprite.
     * @param {string} targetId ID of a target whose sprite to duplicate.
     * @returns {Promise} Promise that resolves when duplicated target has
     *     been added to the runtime.
     */
  }, {
    key: "duplicateSprite",
    value: function duplicateSprite(targetId) {
      var _this15 = this;
      var target = this.runtime.getTargetById(targetId);
      if (!target) {
        throw new Error('No target with the provided id.');
      } else if (!target.isSprite()) {
        throw new Error('Cannot duplicate non-sprite targets.');
      } else if (!target.sprite) {
        throw new Error('No sprite associated with this target.');
      }
      return target.duplicate().then(function (newTarget) {
        _this15.runtime.addTarget(newTarget);
        newTarget.goBehindOther(target);
        _this15.setEditingTarget(newTarget.id);
      });
    }

    /**
     * Set the audio engine for the VM/runtime
     * @param {!AudioEngine} audioEngine The audio engine to attach
     */
  }, {
    key: "attachAudioEngine",
    value: function attachAudioEngine(audioEngine) {
      this.runtime.attachAudioEngine(audioEngine);
    }

    /**
     * Set the renderer for the VM/runtime
     * @param {!RenderWebGL} renderer The renderer to attach
     */
  }, {
    key: "attachRenderer",
    value: function attachRenderer(renderer) {
      this.runtime.attachRenderer(renderer);
    }

    /**
     * @returns {RenderWebGL} The renderer attached to the vm
     */
  }, {
    key: "renderer",
    get: function get() {
      return this.runtime && this.runtime.renderer;
    }

    // @deprecated
  }, {
    key: "attachV2SVGAdapter",
    value: function attachV2SVGAdapter() {}

    /**
     * Set the bitmap adapter for the VM/runtime, which converts scratch 2
     * bitmaps to scratch 3 bitmaps. (Scratch 3 bitmaps are all bitmap resolution 2)
     * @param {!function} bitmapAdapter The adapter to attach
     */
  }, {
    key: "attachV2BitmapAdapter",
    value: function attachV2BitmapAdapter(bitmapAdapter) {
      this.runtime.attachV2BitmapAdapter(bitmapAdapter);
    }

    /**
     * Set the storage module for the VM/runtime
     * @param {!ScratchStorage} storage The storage module to attach
     */
  }, {
    key: "attachStorage",
    value: function attachStorage(storage) {
      this.runtime.attachStorage(storage);
    }

    /**
     * set the current locale and builtin messages for the VM
     * @param {!string} locale       current locale
     * @param {!object} messages     builtin messages map for current locale
     * @returns {Promise} Promise that resolves when all the blocks have been
     *     updated for a new locale (or empty if locale hasn't changed.)
     */
  }, {
    key: "setLocale",
    value: function setLocale(locale, messages) {
      if (locale !== formatMessage.setup().locale) {
        formatMessage.setup({
          locale: locale,
          translations: _defineProperty({}, locale, messages)
        });
      }
      this.emit('LOCALE_CHANGED', locale);
      return this.extensionManager.refreshBlocks();
    }

    /**
     * get the current locale for the VM
     * @returns {string} the current locale in the VM
     */
  }, {
    key: "getLocale",
    value: function getLocale() {
      return formatMessage.setup().locale;
    }

    /**
     * Handle a Blockly event for the current editing target.
     * @param {!Blockly.Event} e Any Blockly event.
     */
  }, {
    key: "blockListener",
    value: function blockListener(e) {
      if (this.editingTarget) {
        this.editingTarget.blocks.blocklyListen(e);
      }
    }

    /**
     * Handle a Blockly event for the flyout.
     * @param {!Blockly.Event} e Any Blockly event.
     */
  }, {
    key: "flyoutBlockListener",
    value: function flyoutBlockListener(e) {
      this.runtime.flyoutBlocks.blocklyListen(e);
    }

    /**
     * Handle a Blockly event for the flyout to be passed to the monitor container.
     * @param {!Blockly.Event} e Any Blockly event.
     */
  }, {
    key: "monitorBlockListener",
    value: function monitorBlockListener(e) {
      // Filter events by type, since monitor blocks only need to listen to these events.
      // Monitor blocks shouldn't be destroyed when flyout blocks are deleted.
      if (['create', 'change'].indexOf(e.type) !== -1) {
        this.runtime.monitorBlocks.blocklyListen(e);
      }
    }

    /**
     * Handle a Blockly event for the variable map.
     * @param {!Blockly.Event} e Any Blockly event.
     */
  }, {
    key: "variableListener",
    value: function variableListener(e) {
      // Filter events by type, since blocks only needs to listen to these
      // var events.
      if (['var_create', 'var_rename', 'var_delete'].indexOf(e.type) !== -1) {
        this.runtime.getTargetForStage().blocks.blocklyListen(e);
      }
    }

    /**
     * Delete all of the flyout blocks.
     */
  }, {
    key: "clearFlyoutBlocks",
    value: function clearFlyoutBlocks() {
      this.runtime.flyoutBlocks.deleteAllBlocks();
    }

    /**
     * Set an editing target. An editor UI can use this function to switch
     * between editing different targets, sprites, etc.
     * After switching the editing target, the VM may emit updates
     * to the list of targets and any attached workspace blocks
     * (see `emitTargetsUpdate` and `emitWorkspaceUpdate`).
     * @param {string} targetId Id of target to set as editing.
     */
  }, {
    key: "setEditingTarget",
    value: function setEditingTarget(targetId) {
      // Has the target id changed? If not, exit.
      if (this.editingTarget && targetId === this.editingTarget.id) {
        return;
      }
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        this.editingTarget = target;
        // Emit appropriate UI updates.
        this.emitTargetsUpdate(false /* Don't emit project change */);
        this.emitWorkspaceUpdate();
        this.runtime.setEditingTarget(target);
      }
    }

    /**
     * @param {Block[]} blockObjects
     * @returns {object}
     */
  }, {
    key: "exportStandaloneBlocks",
    value: function exportStandaloneBlocks(blockObjects) {
      var sb3 = __webpack_require__(/*! ./serialization/sb3 */ "../vm/src/serialization/sb3.js");
      var serialized = sb3.serializeStandaloneBlocks(blockObjects, this.runtime);
      return serialized;
    }

    /**
     * Called when blocks are dragged from one sprite to another. Adds the blocks to the
     * workspace of the given target.
     * @param {!Array<object>} blocks Blocks to add.
     * @param {!string} targetId Id of target to add blocks to.
     * @param {?string} optFromTargetId Optional target id indicating that blocks are being
     * shared from that target. This is needed for resolving any potential variable conflicts.
     * @return {!Promise} Promise that resolves when the extensions and blocks have been added.
     */
  }, {
    key: "shareBlocksToTarget",
    value: function shareBlocksToTarget(blocks, targetId, optFromTargetId) {
      var _this16 = this;
      var sb3 = __webpack_require__(/*! ./serialization/sb3 */ "../vm/src/serialization/sb3.js");
      var _sb3$deserializeStand = sb3.deserializeStandaloneBlocks(blocks),
        copiedBlocks = _sb3$deserializeStand.blocks,
        extensionURLs = _sb3$deserializeStand.extensionURLs;
      newBlockIds(copiedBlocks);
      var target = this.runtime.getTargetById(targetId);
      if (optFromTargetId) {
        // If the blocks are being shared from another target,
        // resolve any possible variable conflicts that may arise.
        var fromTarget = this.runtime.getTargetById(optFromTargetId);
        fromTarget.resolveVariableSharingConflictsWithTarget(copiedBlocks, target);
      }

      // Create a unique set of extensionIds that are not yet loaded
      var extensionIDs = new Set(copiedBlocks.map(function (b) {
        return sb3.getExtensionIdForOpcode(b.opcode);
      }).filter(function (id) {
        return !!id;
      }) // Remove ids that do not exist
      .filter(function (id) {
        return !_this16.extensionManager.isExtensionLoaded(id);
      }) // and remove loaded extensions
      );
      return this._loadExtensions(extensionIDs, extensionURLs).then(function () {
        copiedBlocks.forEach(function (block) {
          target.blocks.createBlock(block);
        });
        target.blocks.updateTargetSpecificBlocks(target.isStage);
      });
    }

    /**
     * Called when costumes are dragged from editing target to another target.
     * Sets the newly added costume as the current costume.
     * @param {!number} costumeIndex Index of the costume of the editing target to share.
     * @param {!string} targetId Id of target to add the costume.
     * @return {Promise} Promise that resolves when the new costume has been loaded.
     */
  }, {
    key: "shareCostumeToTarget",
    value: function shareCostumeToTarget(costumeIndex, targetId) {
      var _this17 = this;
      var originalCostume = this.editingTarget.getCostumes()[costumeIndex];
      var clone = Object.assign({}, originalCostume);
      var md5ext = "".concat(clone.assetId, ".").concat(clone.dataFormat);
      return loadCostume(md5ext, clone, this.runtime).then(function () {
        var target = _this17.runtime.getTargetById(targetId);
        if (target) {
          target.addCostume(clone);
          target.setCostume(target.getCostumes().length - 1);
        }
      });
    }

    /**
     * Called when sounds are dragged from editing target to another target.
     * @param {!number} soundIndex Index of the sound of the editing target to share.
     * @param {!string} targetId Id of target to add the sound.
     * @return {Promise} Promise that resolves when the new sound has been loaded.
     */
  }, {
    key: "shareSoundToTarget",
    value: function shareSoundToTarget(soundIndex, targetId) {
      var _this18 = this;
      var originalSound = this.editingTarget.getSounds()[soundIndex];
      var clone = Object.assign({}, originalSound);
      var target = this.runtime.getTargetById(targetId);
      return loadSound(clone, this.runtime, target.sprite.soundBank).then(function () {
        if (target) {
          target.addSound(clone);
          _this18.emitTargetsUpdate();
        }
      });
    }

    /**
     * Repopulate the workspace with the blocks of the current editingTarget. This
     * allows us to get around bugs like gui#413.
     */
  }, {
    key: "refreshWorkspace",
    value: function refreshWorkspace() {
      if (this.editingTarget) {
        this.emitWorkspaceUpdate();
        this.runtime.setEditingTarget(this.editingTarget);
        this.emitTargetsUpdate(false /* Don't emit project change */);
      }
    }

    /**
     * Emit metadata about available targets.
     * An editor UI could use this to display a list of targets and show
     * the currently editing one.
     * @param {bool} triggerProjectChange If true, also emit a project changed event.
     * Disabled selectively by updates that don't affect project serialization.
     * Defaults to true.
     */
  }, {
    key: "emitTargetsUpdate",
    value: function emitTargetsUpdate(triggerProjectChange) {
      var _this19 = this;
      if (typeof triggerProjectChange === 'undefined') triggerProjectChange = true;
      var lazyTargetList;
      var getTargetListLazily = function getTargetListLazily() {
        if (!lazyTargetList) {
          lazyTargetList = _this19.runtime.targets.filter(
          // Don't report clones.
          function (target) {
            return !Object.prototype.hasOwnProperty.call(target, 'isOriginal') || target.isOriginal;
          }).map(function (target) {
            return target.toJSON();
          });
        }
        return lazyTargetList;
      };
      this.emit('targetsUpdate', {
        // [[target id, human readable target name], ...].
        get targetList() {
          return getTargetListLazily();
        },
        // Currently editing target id.
        editingTarget: this.editingTarget ? this.editingTarget.id : null
      });
      if (triggerProjectChange) {
        this.runtime.emitProjectChanged();
      }
    }

    /**
     * Emit an Blockly/scratch-blocks compatible XML representation
     * of the current editing target's blocks.
     */
  }, {
    key: "emitWorkspaceUpdate",
    value: function emitWorkspaceUpdate() {
      var _this20 = this;
      // Create a list of broadcast message Ids according to the stage variables
      var stageVariables = this.runtime.getTargetForStage().variables;
      var messageIds = [];
      for (var varId in stageVariables) {
        if (stageVariables[varId].type === Variable.BROADCAST_MESSAGE_TYPE) {
          messageIds.push(varId);
        }
      }
      // Go through all blocks on all targets, removing referenced
      // broadcast ids from the list.
      for (var i = 0; i < this.runtime.targets.length; i++) {
        var currTarget = this.runtime.targets[i];
        var currBlocks = currTarget.blocks._blocks;
        for (var blockId in currBlocks) {
          if (currBlocks[blockId].fields.BROADCAST_OPTION) {
            var id = currBlocks[blockId].fields.BROADCAST_OPTION.id;
            var index = messageIds.indexOf(id);
            if (index !== -1) {
              messageIds = messageIds.slice(0, index).concat(messageIds.slice(index + 1));
            }
          }
        }
      }
      // Anything left in messageIds is not referenced by a block, so delete it.
      for (var _i3 = 0; _i3 < messageIds.length; _i3++) {
        var _id = messageIds[_i3];
        delete this.runtime.getTargetForStage().variables[_id];
      }
      var globalVarMap = Object.assign({}, this.runtime.getTargetForStage().variables);
      var localVarMap = this.editingTarget.isStage ? Object.create(null) : Object.assign({}, this.editingTarget.variables);
      var globalVariables = Object.keys(globalVarMap).map(function (k) {
        return globalVarMap[k];
      });
      var localVariables = Object.keys(localVarMap).map(function (k) {
        return localVarMap[k];
      });
      var workspaceComments = Object.keys(this.editingTarget.comments).map(function (k) {
        return _this20.editingTarget.comments[k];
      }).filter(function (c) {
        return c.blockId === null;
      });
      var xmlString = "<xml xmlns=\"http://www.w3.org/1999/xhtml\">\n                            <variables>\n                                ".concat(globalVariables.map(function (v) {
        return v.toXML();
      }).join(), "\n                                ").concat(localVariables.map(function (v) {
        return v.toXML(true);
      }).join(), "\n                            </variables>\n                            ").concat(workspaceComments.map(function (c) {
        return c.toXML();
      }).join(), "\n                            ").concat(this.editingTarget.blocks.toXML(this.editingTarget.comments), "\n                        </xml>");
      this.emit('workspaceUpdate', {
        xml: xmlString
      });
    }

    /**
     * Get a target id for a drawable id. Useful for interacting with the renderer
     * @param {int} drawableId The drawable id to request the target id for
     * @returns {?string} The target id, if found. Will also be null if the target found is the stage.
     */
  }, {
    key: "getTargetIdForDrawableId",
    value: function getTargetIdForDrawableId(drawableId) {
      var target = this.runtime.getTargetByDrawableId(drawableId);
      if (target && Object.prototype.hasOwnProperty.call(target, 'id') && Object.prototype.hasOwnProperty.call(target, 'isStage') && !target.isStage) {
        return target.id;
      }
      return null;
    }

    /**
     * Reorder target by index. Return whether a change was made.
     * @param {!string} targetIndex Index of the target.
     * @param {!number} newIndex index that the target should be moved to.
     * @returns {boolean} Whether a target was reordered.
     */
  }, {
    key: "reorderTarget",
    value: function reorderTarget(targetIndex, newIndex) {
      var targets = this.runtime.targets;
      targetIndex = MathUtil.clamp(targetIndex, 0, targets.length - 1);
      newIndex = MathUtil.clamp(newIndex, 0, targets.length - 1);
      if (targetIndex === newIndex) return false;
      var target = targets[targetIndex];
      targets = targets.slice(0, targetIndex).concat(targets.slice(targetIndex + 1));
      targets.splice(newIndex, 0, target);
      this.runtime.targets = targets;
      this.emitTargetsUpdate();
      return true;
    }

    /**
     * Reorder the costumes of a target if it exists. Return whether it succeeded.
     * @param {!string} targetId ID of the target which owns the costumes.
     * @param {!number} costumeIndex index of the costume to move.
     * @param {!number} newIndex index that the costume should be moved to.
     * @returns {boolean} Whether a costume was reordered.
     */
  }, {
    key: "reorderCostume",
    value: function reorderCostume(targetId, costumeIndex, newIndex) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        var reorderSuccessful = target.reorderCostume(costumeIndex, newIndex);
        if (reorderSuccessful) {
          this.runtime.emitProjectChanged();
        }
        return reorderSuccessful;
      }
      return false;
    }

    /**
     * Reorder the sounds of a target if it exists. Return whether it occured.
     * @param {!string} targetId ID of the target which owns the sounds.
     * @param {!number} soundIndex index of the sound to move.
     * @param {!number} newIndex index that the sound should be moved to.
     * @returns {boolean} Whether a sound was reordered.
     */
  }, {
    key: "reorderSound",
    value: function reorderSound(targetId, soundIndex, newIndex) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        var reorderSuccessful = target.reorderSound(soundIndex, newIndex);
        if (reorderSuccessful) {
          this.runtime.emitProjectChanged();
        }
        return reorderSuccessful;
      }
      return false;
    }

    /**
     * Put a target into a "drag" state, during which its X/Y positions will be unaffected
     * by blocks.
     * @param {string} targetId The id for the target to put into a drag state
     */
  }, {
    key: "startDrag",
    value: function startDrag(targetId) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        this._dragTarget = target;
        target.startDrag();
      }
    }

    /**
     * Remove a target from a drag state, so blocks may begin affecting X/Y position again
     * @param {string} targetId The id for the target to remove from the drag state
     */
  }, {
    key: "stopDrag",
    value: function stopDrag(targetId) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        this._dragTarget = null;
        target.stopDrag();
        this.setEditingTarget(target.sprite && target.sprite.clones[0] ? target.sprite.clones[0].id : target.id);
      }
    }

    /**
     * Post/edit sprite info for the current editing target or the drag target.
     * @param {object} data An object with sprite info data to set.
     */
  }, {
    key: "postSpriteInfo",
    value: function postSpriteInfo(data) {
      if (this._dragTarget) {
        this._dragTarget.postSpriteInfo(data);
      } else {
        this.editingTarget.postSpriteInfo(data);
      }
      // Post sprite info means the gui has changed something about a sprite,
      // either through the sprite info pane fields (e.g. direction, size) or
      // through dragging a sprite on the stage
      // Emit a project changed event.
      this.runtime.emitProjectChanged();
    }

    /**
     * Set a target's variable's value. Return whether it succeeded.
     * @param {!string} targetId ID of the target which owns the variable.
     * @param {!string} variableId ID of the variable to set.
     * @param {!*} value The new value of that variable.
     * @returns {boolean} whether the target and variable were found and updated.
     */
  }, {
    key: "setVariableValue",
    value: function setVariableValue(targetId, variableId, value) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        var variable = target.lookupVariableById(variableId);
        if (variable) {
          variable.value = value;
          if (variable.isCloud) {
            this.runtime.ioDevices.cloud.requestUpdateVariable(variable.name, variable.value);
          }
          return true;
        }
      }
      return false;
    }

    /**
     * Get a target's variable's value. Return null if the target or variable does not exist.
     * @param {!string} targetId ID of the target which owns the variable.
     * @param {!string} variableId ID of the variable to set.
     * @returns {?*} The value of the variable, or null if it could not be looked up.
     */
  }, {
    key: "getVariableValue",
    value: function getVariableValue(targetId, variableId) {
      var target = this.runtime.getTargetById(targetId);
      if (target) {
        var variable = target.lookupVariableById(variableId);
        if (variable) {
          return variable.value;
        }
      }
      return null;
    }

    /**
     * Allow VM consumer to configure the ScratchLink socket creator.
     * @param {Function} factory The custom ScratchLink socket factory.
     */
  }, {
    key: "configureScratchLinkSocketFactory",
    value: function configureScratchLinkSocketFactory(factory) {
      this.runtime.configureScratchLinkSocketFactory(factory);
    }
  }]);
}(EventEmitter);
module.exports = VirtualMachine;

/***/ }),

/***/ "./node_modules/arraybuffer-loader/index.js!../vm/test/fixtures/default.sb3":
/*!*************************************************************************!*\
  !*** ./node_modules/arraybuffer-loader!../vm/test/fixtures/default.sb3 ***!
  \*************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! ../../../gui/node_modules/arraybuffer-loader/lib/to-array-buffer.js */ "./node_modules/arraybuffer-loader/lib/to-array-buffer.js")("")

/***/ }),

/***/ "./node_modules/arraybuffer-loader/index.js!../vm/test/fixtures/tw-glide.sb3":
/*!**************************************************************************!*\
  !*** ./node_modules/arraybuffer-loader!../vm/test/fixtures/tw-glide.sb3 ***!
  \**************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! ../../../gui/node_modules/arraybuffer-loader/lib/to-array-buffer.js */ "./node_modules/arraybuffer-loader/lib/to-array-buffer.js")("")

/***/ }),

/***/ "./node_modules/arraybuffer-loader/index.js!./src/lib/default-project/override-default-project.sb3":
/*!************************************************************************************************!*\
  !*** ./node_modules/arraybuffer-loader!./src/lib/default-project/override-default-project.sb3 ***!
  \************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! ../../../node_modules/arraybuffer-loader/lib/to-array-buffer.js */ "./node_modules/arraybuffer-loader/lib/to-array-buffer.js")("")

/***/ }),

/***/ "./node_modules/css-loader/index.js?!./node_modules/postcss-loader/src/index.js?!./src/lib/normalize.css":
/*!**************************************************************************************************************!*\
  !*** ./node_modules/css-loader??ref--5-1!./node_modules/postcss-loader/src??postcss!./src/lib/normalize.css ***!
  \**************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

exports = module.exports = __webpack_require__(/*! ../../node_modules/css-loader/lib/css-base.js */ "./node_modules/css-loader/lib/css-base.js")(false);
// imports


// module
exports.push([module.i, "/*! normalize.css v3.0.3 | MIT License | github.com/necolas/normalize.css */\nhtml{font-family:sans-serif;-ms-text-size-adjust:100%;-webkit-text-size-adjust:100%}\nbody{margin:0}\narticle,aside,details,figcaption,figure,footer,header,hgroup,main,menu,nav,section,summary{display:block}\naudio,canvas,progress,video{display:inline-block;vertical-align:baseline}\naudio:not([controls]){display:none;height:0}\n[hidden],template{display:none}\na{background-color:transparent}\na:active,a:hover{outline:0}\nabbr[title]{border-bottom:1px dotted}\nb,strong{font-weight:bold}\ndfn{font-style:italic}\nh1{font-size:2em;margin:.67em 0}\nmark{background:#ff0;color:#000}\nsmall{font-size:80%}\nsub,sup{font-size:75%;line-height:0;position:relative;vertical-align:baseline}\nsup{top:-0.5em}\nsub{bottom:-0.25em}\nimg{border:0}\nsvg:not(:root){overflow:hidden}\nfigure{margin:1em 40px}\nhr{box-sizing:content-box;height:0}\npre{overflow:auto}\ncode,kbd,pre,samp{font-family:monospace,monospace;font-size:1em}\nbutton,input,optgroup,select,textarea{color:inherit;font:inherit;margin:0}\nbutton{overflow:visible}\nbutton,select{text-transform:none}\nbutton,html input[type=\"button\"],input[type=\"reset\"],input[type=\"submit\"]{-webkit-appearance:button;cursor:pointer}\nbutton[disabled],html input[disabled]{cursor:default}\nbutton::-moz-focus-inner,input::-moz-focus-inner{border:0;padding:0}\ninput{line-height:normal}\ninput[type=\"checkbox\"],input[type=\"radio\"]{box-sizing:border-box;padding:0}\ninput[type=\"number\"]::-webkit-inner-spin-button,input[type=\"number\"]::-webkit-outer-spin-button{height:auto}\ninput[type=\"search\"]{-webkit-appearance:textfield;box-sizing:content-box}\ninput[type=\"search\"]::-webkit-search-cancel-button,input[type=\"search\"]::-webkit-search-decoration{-webkit-appearance:none}\nfieldset{border:1px solid silver;margin:0 2px;padding:.35em .625em .75em}\nlegend{border:0;padding:0}\ntextarea{overflow:auto}\noptgroup{font-weight:bold}\ntable{border-collapse:collapse;border-spacing:0}\ntd,th{padding:0}\n", ""]);

// exports


/***/ }),

/***/ "./node_modules/css-loader/index.js?!./node_modules/postcss-loader/src/index.js?!./src/playground/vm-live.css":
/*!*******************************************************************************************************************!*\
  !*** ./node_modules/css-loader??ref--5-1!./node_modules/postcss-loader/src??postcss!./src/playground/vm-live.css ***!
  \*******************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

exports = module.exports = __webpack_require__(/*! ../../node_modules/css-loader/lib/css-base.js */ "./node_modules/css-loader/lib/css-base.js")(false);
// imports


// module
exports.push([module.i, ".vm-live_page_3hlL_ {\n    min-height: 100vh;\n    padding: 24px;\n    color: #f7f3ea;\n    background:\n        radial-gradient(circle at top left, rgba(255, 120, 72, 0.28), transparent 32%),\n        radial-gradient(circle at top right, rgba(72, 189, 255, 0.18), transparent 28%),\n        linear-gradient(135deg, #121417 0%, #191f2b 42%, #0b0d11 100%);\n    font-family: \"Avenir Next\", \"Trebuchet MS\", \"Segoe UI\", sans-serif;\n}\n\n.vm-live_shell_2aQwz {\n    max-width: 1360px;\n    margin: 0 auto;\n    display: grid;\n    grid-template-columns: minmax(0, 1.6fr) minmax(320px, 0.9fr);\n    gap: 24px;\n    align-items: start;\n}\n\n.vm-live_stagePanel_35nKS,\n.vm-live_controlPanel_3hQz5 {\n    position: relative;\n    overflow: hidden;\n    border: 1px solid rgba(255, 255, 255, 0.1);\n    border-radius: 28px;\n    background: rgba(8, 12, 18, 0.76);\n    box-shadow: 0 30px 90px rgba(0, 0, 0, 0.35);\n    -webkit-backdrop-filter: blur(18px);\n            backdrop-filter: blur(18px);\n}\n\n.vm-live_stagePanel_35nKS {\n    padding: 20px;\n}\n\n.vm-live_stageHeader_1xyUG {\n    display: flex;\n    justify-content: space-between;\n    gap: 16px;\n    margin-bottom: 16px;\n}\n\n.vm-live_eyebrow_3yjW0 {\n    margin: 0 0 6px;\n    font-size: 12px;\n    letter-spacing: 0.18em;\n    text-transform: uppercase;\n    color: #ffb37d;\n}\n\n.vm-live_title_39D3A {\n    margin: 0;\n    font-size: clamp(28px, 4vw, 44px);\n    line-height: 1;\n    font-weight: 700;\n}\n\n.vm-live_subtitle_8_9sh {\n    margin: 10px 0 0;\n    max-width: 42rem;\n    color: rgba(247, 243, 234, 0.75);\n    line-height: 1.5;\n}\n\n.vm-live_pills_365YH {\n    display: flex;\n    flex-wrap: wrap;\n    justify-content: flex-end;\n    align-content: flex-start;\n    gap: 10px;\n}\n\n.vm-live_pill_2MHL2 {\n    padding: 10px 14px;\n    border-radius: 999px;\n    border: 1px solid rgba(255, 255, 255, 0.1);\n    background: rgba(255, 255, 255, 0.04);\n    font-size: 12px;\n    letter-spacing: 0.05em;\n    text-transform: uppercase;\n    color: rgba(247, 243, 234, 0.82);\n}\n\n.vm-live_pillActive_2qRT6 {\n    border-color: rgba(255, 179, 125, 0.5);\n    background: rgba(255, 179, 125, 0.14);\n    color: #ffd8b6;\n}\n\n.vm-live_stageViewport_3Jaw8 {\n    position: relative;\n    aspect-ratio: 4 / 3;\n    border-radius: 24px;\n    overflow: hidden;\n    background:\n        linear-gradient(180deg, rgba(255, 255, 255, 0.05), transparent 30%),\n        linear-gradient(135deg, #1d283a 0%, #10151f 52%, #090c12 100%);\n    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.07);\n}\n\n.vm-live_canvasHost_R079a {\n    position: absolute;\n    inset: 0;\n}\n\n.vm-live_canvasHost_R079a canvas {\n    width: 100%;\n    height: 100%;\n    display: block;\n}\n\n.vm-live_overlayTop_mKiJw,\n.vm-live_overlayBottom_1N7xq {\n    position: absolute;\n    left: 16px;\n    right: 16px;\n    display: flex;\n    gap: 12px;\n    pointer-events: none;\n}\n\n.vm-live_overlayTop_mKiJw {\n    top: 16px;\n    justify-content: space-between;\n    align-items: flex-start;\n}\n\n.vm-live_overlayBottom_1N7xq {\n    bottom: 16px;\n    justify-content: space-between;\n    align-items: flex-end;\n}\n\n.vm-live_overlayCard_1rz3a {\n    padding: 12px 14px;\n    border-radius: 18px;\n    background: rgba(6, 9, 14, 0.76);\n    border: 1px solid rgba(255, 255, 255, 0.08);\n    -webkit-backdrop-filter: blur(12px);\n            backdrop-filter: blur(12px);\n    pointer-events: auto;\n}\n\n.vm-live_overlayTitle_32sVu {\n    margin: 0 0 6px;\n    font-size: 11px;\n    letter-spacing: 0.12em;\n    text-transform: uppercase;\n    color: rgba(255, 179, 125, 0.9);\n}\n\n.vm-live_overlayText_3efbM {\n    margin: 0;\n    font-size: 14px;\n    color: rgba(247, 243, 234, 0.92);\n}\n\n.vm-live_questionPanel_1zpb5 {\n    position: absolute;\n    left: 16px;\n    right: 16px;\n    bottom: 16px;\n    padding: 16px;\n    border-radius: 22px;\n    background: rgba(8, 12, 18, 0.9);\n    border: 1px solid rgba(255, 255, 255, 0.1);\n    -webkit-backdrop-filter: blur(14px);\n            backdrop-filter: blur(14px);\n}\n\n.vm-live_questionTitle_1AlbC {\n    margin: 0 0 12px;\n    font-size: 13px;\n    letter-spacing: 0.14em;\n    text-transform: uppercase;\n    color: #ffb37d;\n}\n\n.vm-live_questionText_mJmyy {\n    margin: 0 0 14px;\n    font-size: 15px;\n    line-height: 1.45;\n}\n\n.vm-live_questionForm_2VhXX {\n    display: flex;\n    gap: 10px;\n}\n\n.vm-live_questionInput_3nVGJ,\n.vm-live_textInput__2f2u,\n.vm-live_selectInput_1TnzH {\n    width: 100%;\n    min-width: 0;\n    padding: 12px 14px;\n    border-radius: 14px;\n    border: 1px solid rgba(255, 255, 255, 0.12);\n    background: rgba(255, 255, 255, 0.04);\n    color: #f7f3ea;\n    font: inherit;\n}\n\n.vm-live_questionInput_3nVGJ::-moz-placeholder, .vm-live_textInput__2f2u::-moz-placeholder {\n    color: rgba(247, 243, 234, 0.42);\n}\n\n.vm-live_questionInput_3nVGJ::placeholder,\n.vm-live_textInput__2f2u::placeholder {\n    color: rgba(247, 243, 234, 0.42);\n}\n\n.vm-live_controlPanel_3hQz5 {\n    padding: 20px;\n    display: grid;\n    gap: 16px;\n}\n\n.vm-live_section_36Jz5 {\n    padding: 16px;\n    border-radius: 22px;\n    border: 1px solid rgba(255, 255, 255, 0.08);\n    background: rgba(255, 255, 255, 0.03);\n}\n\n.vm-live_sectionTitle_EUIHs {\n    margin: 0 0 12px;\n    font-size: 13px;\n    letter-spacing: 0.14em;\n    text-transform: uppercase;\n    color: #ffb37d;\n}\n\n.vm-live_buttonRow_3SZNx {\n    display: flex;\n    flex-wrap: wrap;\n    gap: 10px;\n}\n\n.vm-live_button_39ORw,\n.vm-live_secondaryButton_1r7Iq {\n    -webkit-appearance: none;\n       -moz-appearance: none;\n            appearance: none;\n    border: none;\n    border-radius: 999px;\n    padding: 12px 16px;\n    font: inherit;\n    cursor: pointer;\n    transition: transform 120ms ease, opacity 120ms ease, background 120ms ease;\n}\n\n.vm-live_button_39ORw:hover,\n.vm-live_secondaryButton_1r7Iq:hover {\n    transform: translateY(-1px);\n}\n\n.vm-live_button_39ORw:disabled,\n.vm-live_secondaryButton_1r7Iq:disabled {\n    opacity: 0.45;\n    cursor: not-allowed;\n    transform: none;\n}\n\n.vm-live_button_39ORw {\n    color: #10151f;\n    background: linear-gradient(135deg, #ffb37d 0%, #ffd166 100%);\n}\n\n.vm-live_secondaryButton_1r7Iq {\n    color: #f7f3ea;\n    background: rgba(255, 255, 255, 0.08);\n}\n\n.vm-live_dangerButton_23xpZ {\n    background: linear-gradient(135deg, #ff7d7d 0%, #ff9e5c 100%);\n}\n\n.vm-live_controlGrid_3VJ-b {\n    display: grid;\n    gap: 12px;\n}\n\n.vm-live_fieldRow_3yOpR {\n    display: grid;\n    grid-template-columns: 1fr auto;\n    gap: 10px;\n}\n\n.vm-live_toggleList_3JZkt {\n    display: grid;\n    gap: 12px;\n}\n\n.vm-live_toggle_1ux4s {\n    display: grid;\n    grid-template-columns: 1fr auto;\n    gap: 12px;\n    align-items: center;\n}\n\n.vm-live_toggleMeta_1Iz_k {\n    display: grid;\n    gap: 3px;\n}\n\n.vm-live_toggleLabel_WKHwt {\n    font-size: 15px;\n    font-weight: 600;\n}\n\n.vm-live_toggleHint_32Lz6 {\n    font-size: 13px;\n    color: rgba(247, 243, 234, 0.62);\n}\n\n.vm-live_switch_1pp-V {\n    position: relative;\n    display: inline-flex;\n    width: 52px;\n    height: 30px;\n    border-radius: 999px;\n    background: rgba(255, 255, 255, 0.14);\n    border: 1px solid rgba(255, 255, 255, 0.08);\n    cursor: pointer;\n}\n\n.vm-live_switch_1pp-V input {\n    position: absolute;\n    inset: 0;\n    opacity: 0;\n    cursor: pointer;\n}\n\n.vm-live_switchTrack_3BP85 {\n    position: absolute;\n    inset: 0;\n    border-radius: inherit;\n}\n\n.vm-live_switchThumb_3Bj4_ {\n    position: absolute;\n    top: 3px;\n    left: 3px;\n    width: 22px;\n    height: 22px;\n    border-radius: 50%;\n    background: #f7f3ea;\n    transition: transform 140ms ease, background 140ms ease;\n}\n\n.vm-live_switch_1pp-V input:checked + .vm-live_switchTrack_3BP85 + .vm-live_switchThumb_3Bj4_ {\n    transform: translateX(22px);\n    background: #ffe5bf;\n}\n\n.vm-live_switch_1pp-V input:checked + .vm-live_switchTrack_3BP85 {\n    background: linear-gradient(135deg, rgba(255, 179, 125, 0.4), rgba(93, 198, 255, 0.35));\n}\n\n.vm-live_statsGrid_35Ihi {\n    display: grid;\n    grid-template-columns: repeat(2, minmax(0, 1fr));\n    gap: 10px;\n}\n\n.vm-live_statCard_20oo- {\n    padding: 12px;\n    border-radius: 16px;\n    background: rgba(255, 255, 255, 0.04);\n}\n\n.vm-live_statLabel_1PRli {\n    margin: 0 0 4px;\n    font-size: 11px;\n    letter-spacing: 0.1em;\n    text-transform: uppercase;\n    color: rgba(247, 243, 234, 0.58);\n}\n\n.vm-live_statValue_1ySzg {\n    margin: 0;\n    font-size: 22px;\n    font-weight: 700;\n}\n\n.vm-live_monitorList_2BDJB,\n.vm-live_logList_228o0 {\n    display: grid;\n    gap: 10px;\n}\n\n.vm-live_monitorItem_FbG_J,\n.vm-live_logItem_2VhRH {\n    padding: 12px;\n    border-radius: 16px;\n    background: rgba(255, 255, 255, 0.04);\n}\n\n.vm-live_monitorLabel_2OFJ0,\n.vm-live_logMeta_1Atbq {\n    display: block;\n    margin-bottom: 5px;\n    font-size: 11px;\n    letter-spacing: 0.08em;\n    text-transform: uppercase;\n    color: rgba(247, 243, 234, 0.58);\n}\n\n.vm-live_monitorValue_1BcRS {\n    font-size: 15px;\n    line-height: 1.4;\n    word-break: break-word;\n}\n\n.vm-live_logError_2qzey {\n    border: 1px solid rgba(255, 125, 125, 0.24);\n    background: rgba(255, 125, 125, 0.08);\n}\n\n.vm-live_emptyState_1O7Ip {\n    margin: 0;\n    color: rgba(247, 243, 234, 0.58);\n    line-height: 1.5;\n}\n\n.vm-live_hiddenInput_1doV3 {\n    display: none;\n}\n\n@media (max-width: 1080px) {\n    .vm-live_shell_2aQwz {\n        grid-template-columns: 1fr;\n    }\n\n    .vm-live_stageHeader_1xyUG {\n        flex-direction: column;\n    }\n\n    .vm-live_pills_365YH {\n        justify-content: flex-start;\n    }\n}\n\n@media (max-width: 640px) {\n    .vm-live_page_3hlL_ {\n        padding: 14px;\n    }\n\n    .vm-live_stagePanel_35nKS,\n    .vm-live_controlPanel_3hQz5 {\n        border-radius: 22px;\n    }\n\n    .vm-live_fieldRow_3yOpR,\n    .vm-live_questionForm_2VhXX {\n        grid-template-columns: 1fr;\n    }\n\n    .vm-live_statsGrid_35Ihi {\n        grid-template-columns: 1fr;\n    }\n}\n", ""]);

// exports
exports.locals = {
	"page": "vm-live_page_3hlL_",
	"shell": "vm-live_shell_2aQwz",
	"stagePanel": "vm-live_stagePanel_35nKS",
	"controlPanel": "vm-live_controlPanel_3hQz5",
	"stageHeader": "vm-live_stageHeader_1xyUG",
	"eyebrow": "vm-live_eyebrow_3yjW0",
	"title": "vm-live_title_39D3A",
	"subtitle": "vm-live_subtitle_8_9sh",
	"pills": "vm-live_pills_365YH",
	"pill": "vm-live_pill_2MHL2",
	"pillActive": "vm-live_pillActive_2qRT6",
	"stageViewport": "vm-live_stageViewport_3Jaw8",
	"canvasHost": "vm-live_canvasHost_R079a",
	"overlayTop": "vm-live_overlayTop_mKiJw",
	"overlayBottom": "vm-live_overlayBottom_1N7xq",
	"overlayCard": "vm-live_overlayCard_1rz3a",
	"overlayTitle": "vm-live_overlayTitle_32sVu",
	"overlayText": "vm-live_overlayText_3efbM",
	"questionPanel": "vm-live_questionPanel_1zpb5",
	"questionTitle": "vm-live_questionTitle_1AlbC",
	"questionText": "vm-live_questionText_mJmyy",
	"questionForm": "vm-live_questionForm_2VhXX",
	"questionInput": "vm-live_questionInput_3nVGJ",
	"textInput": "vm-live_textInput__2f2u",
	"selectInput": "vm-live_selectInput_1TnzH",
	"section": "vm-live_section_36Jz5",
	"sectionTitle": "vm-live_sectionTitle_EUIHs",
	"buttonRow": "vm-live_buttonRow_3SZNx",
	"button": "vm-live_button_39ORw",
	"secondaryButton": "vm-live_secondaryButton_1r7Iq",
	"dangerButton": "vm-live_dangerButton_23xpZ",
	"controlGrid": "vm-live_controlGrid_3VJ-b",
	"fieldRow": "vm-live_fieldRow_3yOpR",
	"toggleList": "vm-live_toggleList_3JZkt",
	"toggle": "vm-live_toggle_1ux4s",
	"toggleMeta": "vm-live_toggleMeta_1Iz_k",
	"toggleLabel": "vm-live_toggleLabel_WKHwt",
	"toggleHint": "vm-live_toggleHint_32Lz6",
	"switch": "vm-live_switch_1pp-V",
	"switchTrack": "vm-live_switchTrack_3BP85",
	"switchThumb": "vm-live_switchThumb_3Bj4_",
	"statsGrid": "vm-live_statsGrid_35Ihi",
	"statCard": "vm-live_statCard_20oo-",
	"statLabel": "vm-live_statLabel_1PRli",
	"statValue": "vm-live_statValue_1ySzg",
	"monitorList": "vm-live_monitorList_2BDJB",
	"logList": "vm-live_logList_228o0",
	"monitorItem": "vm-live_monitorItem_FbG_J",
	"logItem": "vm-live_logItem_2VhRH",
	"monitorLabel": "vm-live_monitorLabel_2OFJ0",
	"logMeta": "vm-live_logMeta_1Atbq",
	"monitorValue": "vm-live_monitorValue_1BcRS",
	"logError": "vm-live_logError_2qzey",
	"emptyState": "vm-live_emptyState_1O7Ip",
	"hiddenInput": "vm-live_hiddenInput_1doV3"
};

/***/ }),

/***/ "./node_modules/raw-loader/index.js!./src/lib/default-project/cd21514d0531fdffb22204e0ec5ed84a.svg":
/*!************************************************************************************************!*\
  !*** ./node_modules/raw-loader!./src/lib/default-project/cd21514d0531fdffb22204e0ec5ed84a.svg ***!
  \************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = "<svg version=\"1.1\" width=\"2\" height=\"2\" viewBox=\"-1 -1 2 2\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\">\n  <!-- Exported by Scratch - http://scratch.mit.edu/ -->\n</svg>"

/***/ }),

/***/ "./node_modules/raw-loader/index.js!./src/lib/default-project/dango-cat.svg":
/*!*************************************************************************!*\
  !*** ./node_modules/raw-loader!./src/lib/default-project/dango-cat.svg ***!
  \*************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = "<svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\" width=\"61.49876\" height=\"117.72954\" viewBox=\"0,0,61.49876,117.72954\"><g transform=\"translate(-209.25062,-121.13523)\"><g data-paper-data=\"{&quot;isPaintingLayer&quot;:true}\" fill-rule=\"nonzero\" stroke=\"#000000\" stroke-linecap=\"butt\" stroke-linejoin=\"miter\" stroke-miterlimit=\"10\" stroke-dasharray=\"\" stroke-dashoffset=\"0\" style=\"mix-blend-mode: normal\"><path d=\"M212.45298,121.45117c4.27378,-1.68745 3.99111,1.73524 23.39137,52.09733c11.02954,29.4711 19.76067,53.61865 16.55011,54.62697c-4.68511,1.80854 -12.36182,-22.62622 -23.39136,-52.09733c-16.89172,-48.16374 -21.64214,-52.78237 -16.55012,-54.62697z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#d99e82\" stroke-width=\"0\"/><path d=\"M231.66366,188.60508c-9.14277,0 -16.55443,-7.24756 -16.55443,-16.18792c0,-0.78587 -6.31352,-9.26285 -5.83245,-12.90786c0.40452,-2.76293 9.55433,-0.3003 11.53093,-0.54877c4.86102,-1.00187 5.4542,-2.73129 10.85596,-2.73129c1.57876,0 3.53694,-7.03389 6.07355,-5.48884c4.69159,2.85764 10.48089,15.87384 10.48089,21.67674c0,8.94036 -7.41168,16.18792 -16.55443,16.18792z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#fcb1e3\" stroke-width=\"0\"/><path d=\"M254.49773,207.54343c-3.95019,2.74614 -8.14073,4.33933 -12.55592,4.76162c-4.55084,0.43525 -8.46053,0.76116 -12.24363,-2.3874c-7.45344,-6.20327 -8.34307,-19.21037 -2.19171,-24.55014c1.44594,-1.25547 3.26814,-2.45373 4.53075,-3.07615c1.76698,-0.87105 1.91986,-1.00055 5.71606,-1.94996c6.04389,-0.9383 9.92917,-0.90806 14.55274,2.94c7.45344,6.20327 8.43469,16.90662 2.19171,24.26203z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#ffd983\" stroke-width=\"0\"/><path d=\"M232.32721,232.90573c-2.56114,-5.68425 -1.68892,-8.14742 -0.86406,-12.66379c1.34684,-6.26958 3.91259,-8.71043 9.84623,-10.86565c2.22542,-0.80831 7.74255,-2.38249 10.8511,-2.73285c3.62404,-0.35301 6.7831,0.58103 9.40006,2.45432c2.34915,1.68157 3.03827,2.18205 6.332,6.90816c2.62231,4.47889 4.4898,11.43587 0.7492,16.90554c-3.6011,5.26566 -11.02377,5.95331 -18.58348,5.95331c-8.33056,0 -15.04386,-0.62903 -17.73105,-5.95905z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#a6d388\" stroke-width=\"0\"/><path d=\"M231.87007,170.77208c-1.41148,0 -2.55572,-1.14423 -2.55572,-2.55573c0,-1.41149 1.14423,-2.55572 2.55572,-2.55572c1.4115,0 2.55572,1.14422 2.55572,2.55572c0,1.4115 -1.14422,2.55573 -2.55572,2.55573z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#000000\" stroke-width=\"1.5\"/><path d=\"M243.83381,164.03088z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#000000\" stroke-width=\"0\"/><path d=\"M245.61169,164.9569c0,1.37058 -0.22211,1.74084 -1.59269,1.74084c-0.68528,0 -0.93531,-0.35186 -1.3844,-0.80094c-0.44908,-0.44908 -0.80093,-0.99542 -0.80093,-1.68071c0,-1.37058 -0.07421,-1.74085 1.29638,-1.74085c1.37058,0 2.48163,1.11107 2.48163,2.48165z\" data-paper-data=\"{&quot;index&quot;:null}\" fill=\"#000000\" stroke-width=\"1.5\"/></g></g></svg>"

/***/ }),

/***/ "./src/lib/default-project/index.js":
/*!******************************************!*\
  !*** ./src/lib/default-project/index.js ***!
  \******************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _project_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./project-data */ "./src/lib/default-project/project-data.js");
/* harmony import */ var _arraybuffer_loader_override_default_project_sb3__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! arraybuffer-loader!./override-default-project.sb3 */ "./node_modules/arraybuffer-loader/index.js!./src/lib/default-project/override-default-project.sb3");
/* harmony import */ var _arraybuffer_loader_override_default_project_sb3__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_arraybuffer_loader_override_default_project_sb3__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _raw_loader_cd21514d0531fdffb22204e0ec5ed84a_svg__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! raw-loader!./cd21514d0531fdffb22204e0ec5ed84a.svg */ "./node_modules/raw-loader/index.js!./src/lib/default-project/cd21514d0531fdffb22204e0ec5ed84a.svg");
/* harmony import */ var _raw_loader_cd21514d0531fdffb22204e0ec5ed84a_svg__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_raw_loader_cd21514d0531fdffb22204e0ec5ed84a_svg__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _raw_loader_dango_cat_svg__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! raw-loader!./dango-cat.svg */ "./node_modules/raw-loader/index.js!./src/lib/default-project/dango-cat.svg");
/* harmony import */ var _raw_loader_dango_cat_svg__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_raw_loader_dango_cat_svg__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _tw_text_encoder__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../tw-text-encoder */ "./src/lib/tw-text-encoder.js");


/* eslint-disable import/no-unresolved */



/* eslint-enable import/no-unresolved */

var defaultProject = function defaultProject(translator) {
  if (_arraybuffer_loader_override_default_project_sb3__WEBPACK_IMPORTED_MODULE_1___default.a.byteLength > 0) {
    return [{
      id: 0,
      assetType: 'Project',
      dataFormat: 'JSON',
      data: _arraybuffer_loader_override_default_project_sb3__WEBPACK_IMPORTED_MODULE_1___default.a
    }];
  }
  var _TextEncoder;
  if (typeof _tw_text_encoder__WEBPACK_IMPORTED_MODULE_4__["TextEncoder"] === 'undefined') {
    _TextEncoder = __webpack_require__(/*! text-encoding */ "./src/lib/tw-text-encoder.js").TextEncoder;
  } else {
    _TextEncoder = _tw_text_encoder__WEBPACK_IMPORTED_MODULE_4__["TextEncoder"];
  }
  var encoder = new _TextEncoder();
  var projectJson = Object(_project_data__WEBPACK_IMPORTED_MODULE_0__["default"])(translator);
  return [{
    id: 0,
    assetType: 'Project',
    dataFormat: 'JSON',
    data: JSON.stringify(projectJson)
  }, {
    id: 'cd21514d0531fdffb22204e0ec5ed84a',
    assetType: 'ImageVector',
    dataFormat: 'SVG',
    data: encoder.encode(_raw_loader_cd21514d0531fdffb22204e0ec5ed84a_svg__WEBPACK_IMPORTED_MODULE_2___default.a)
  }, {
    id: '927d672925e7b99f7813735c484c6922',
    assetType: 'ImageVector',
    dataFormat: 'SVG',
    data: encoder.encode(_raw_loader_dango_cat_svg__WEBPACK_IMPORTED_MODULE_3___default.a)
  }];
};
/* harmony default export */ __webpack_exports__["default"] = (defaultProject);

/***/ }),

/***/ "./src/lib/default-project/project-data.js":
/*!*************************************************!*\
  !*** ./src/lib/default-project/project-data.js ***!
  \*************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react_intl__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-intl */ "./node_modules/react-intl/lib/index.es.js");
/* harmony import */ var _shared_messages__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared-messages */ "./src/lib/shared-messages.js");
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


var messages = Object(react_intl__WEBPACK_IMPORTED_MODULE_0__["defineMessages"])({
  variable: {
    "id": "gui.defaultProject.variable",
    "defaultMessage": "my variable"
  }
});
messages = _objectSpread(_objectSpread({}, messages), _shared_messages__WEBPACK_IMPORTED_MODULE_1__["default"]);

// use the default message if a translation function is not passed
var defaultTranslator = function defaultTranslator(msgObj) {
  return msgObj.defaultMessage;
};

/**
 * Generate a localized version of the default project
 * @param {function} translateFunction a function to use for translating the default names
 * @return {object} the project data json for the default project
 */
var projectData = function projectData(translateFunction) {
  var translator = translateFunction || defaultTranslator;
  return {
    targets: [{
      isStage: true,
      name: 'Stage',
      variables: {
        '`jEk@4|i[#Fk?(8x)AV.-my variable': [translator(messages.variable), 0]
      },
      lists: {},
      broadcasts: {},
      blocks: {},
      currentCostume: 0,
      costumes: [{
        assetId: 'cd21514d0531fdffb22204e0ec5ed84a',
        name: translator(messages.backdrop, {
          index: 1
        }),
        md5ext: 'cd21514d0531fdffb22204e0ec5ed84a.svg',
        dataFormat: 'svg',
        rotationCenterX: 240,
        rotationCenterY: 180
      }],
      sounds: [],
      volume: 100
    }, {
      isStage: false,
      name: translator(messages.sprite, {
        index: 1
      }),
      variables: {},
      lists: {},
      broadcasts: {},
      blocks: {},
      comments: {},
      currentCostume: 0,
      costumes: [{
        assetId: '927d672925e7b99f7813735c484c6922',
        name: translator(messages.costume, {
          index: 1
        }),
        bitmapResolution: 1,
        md5ext: '927d672925e7b99f7813735c484c6922.svg',
        dataFormat: 'svg',
        rotationCenterX: 30.74937882782359,
        rotationCenterY: 58.864768144346826
      }],
      sounds: [],
      volume: 100,
      visible: true,
      x: 0,
      y: 0,
      size: 100,
      direction: 90,
      draggable: false,
      rotationStyle: 'all around'
    }],
    meta: {
      semver: '3.0.0',
      vm: '0.1.0',
      agent: ''
    }
  };
};
/* harmony default export */ __webpack_exports__["default"] = (projectData);

/***/ }),

/***/ "./src/lib/log.js":
/*!************************!*\
  !*** ./src/lib/log.js ***!
  \************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _turbowarp_nanolog__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @turbowarp/nanolog */ "./node_modules/@turbowarp/nanolog/browser.js");
/* harmony import */ var _turbowarp_nanolog__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_turbowarp_nanolog__WEBPACK_IMPORTED_MODULE_0__);

_turbowarp_nanolog__WEBPACK_IMPORTED_MODULE_0___default.a.enable();
/* harmony default export */ __webpack_exports__["default"] = (_turbowarp_nanolog__WEBPACK_IMPORTED_MODULE_0___default()('gui'));

/***/ }),

/***/ "./src/lib/normalize.css":
/*!*******************************!*\
  !*** ./src/lib/normalize.css ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {


var content = __webpack_require__(/*! !../../node_modules/css-loader??ref--5-1!../../node_modules/postcss-loader/src??postcss!./normalize.css */ "./node_modules/css-loader/index.js?!./node_modules/postcss-loader/src/index.js?!./src/lib/normalize.css");

if(typeof content === 'string') content = [[module.i, content, '']];

var transform;
var insertInto;



var options = {"hmr":true}

options.transform = transform
options.insertInto = undefined;

var update = __webpack_require__(/*! ../../node_modules/style-loader/lib/addStyles.js */ "./node_modules/style-loader/lib/addStyles.js")(content, options);

if(content.locals) module.exports = content.locals;

if(false) {}

/***/ }),

/***/ "./src/lib/shared-messages.js":
/*!************************************!*\
  !*** ./src/lib/shared-messages.js ***!
  \************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react_intl__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-intl */ "./node_modules/react-intl/lib/index.es.js");

/* harmony default export */ __webpack_exports__["default"] = (Object(react_intl__WEBPACK_IMPORTED_MODULE_0__["defineMessages"])({
  backdrop: {
    "id": "gui.sharedMessages.backdrop",
    "defaultMessage": "backdrop{index}"
  },
  costume: {
    "id": "gui.sharedMessages.costume",
    "defaultMessage": "costume{index}"
  },
  sprite: {
    "id": "gui.sharedMessages.sprite",
    "defaultMessage": "Sprite{index}"
  },
  pop: {
    "id": "gui.sharedMessages.pop",
    "defaultMessage": "pop"
  },
  replaceProjectWarning: {
    "id": "gui.sharedMessages.replaceProjectWarning",
    "defaultMessage": "Replace contents of the current project?"
  },
  loadFromComputerTitle: {
    "id": "gui.sharedMessages.loadFromComputerTitle",
    "defaultMessage": "Load from your computer"
  }
}));

/***/ }),

/***/ "./src/lib/storage.js":
/*!****************************!*\
  !*** ./src/lib/storage.js ***!
  \****************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _turbowarp_scratch_storage__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @turbowarp/scratch-storage */ "./node_modules/@turbowarp/scratch-storage/dist/web/scratch-storage.js");
/* harmony import */ var _turbowarp_scratch_storage__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_turbowarp_scratch_storage__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _default_project__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./default-project */ "./src/lib/default-project/index.js");
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }



/**
 * Wrapper for ScratchStorage which adds default web sources.
 * @todo make this more configurable
 */
var Storage = /*#__PURE__*/function (_ScratchStorage) {
  function Storage() {
    var _this;
    _classCallCheck(this, Storage);
    _this = _callSuper(this, Storage);
    _this.cacheDefaultProject();
    return _this;
  }
  _inherits(Storage, _ScratchStorage);
  return _createClass(Storage, [{
    key: "addOfficialScratchWebStores",
    value: function addOfficialScratchWebStores() {
      this.addWebStore([this.AssetType.Project], this.getProjectGetConfig.bind(this), this.getProjectCreateConfig.bind(this), this.getProjectUpdateConfig.bind(this));
      this.addWebStore([this.AssetType.ImageVector, this.AssetType.ImageBitmap, this.AssetType.Sound, this.AssetType.Font], this.getAssetGetConfig.bind(this),
      // We set both the create and update configs to the same method because
      // storage assumes it should update if there is an assetId, but the
      // asset store uses the assetId as part of the create URI.
      this.getAssetCreateConfig.bind(this), this.getAssetCreateConfig.bind(this));
    }
  }, {
    key: "setProjectHost",
    value: function setProjectHost(projectHost) {
      this.projectHost = projectHost;
    }
  }, {
    key: "setProjectToken",
    value: function setProjectToken(projectToken) {
      this.projectToken = projectToken;
    }
  }, {
    key: "getProjectGetConfig",
    value: function getProjectGetConfig(projectAsset) {
      var path = "".concat(this.projectHost, "/").concat(projectAsset.assetId);
      var qs = this.projectToken ? "?token=".concat(this.projectToken) : '';
      return path + qs;
    }
  }, {
    key: "getProjectCreateConfig",
    value: function getProjectCreateConfig() {
      return {
        url: "".concat(this.projectHost, "/"),
        withCredentials: true
      };
    }
  }, {
    key: "getProjectUpdateConfig",
    value: function getProjectUpdateConfig(projectAsset) {
      return {
        url: "".concat(this.projectHost, "/").concat(projectAsset.assetId),
        withCredentials: true
      };
    }
  }, {
    key: "setAssetHost",
    value: function setAssetHost(assetHost) {
      this.assetHost = assetHost;
    }
  }, {
    key: "getAssetGetConfig",
    value: function getAssetGetConfig(asset) {
      return "".concat(this.assetHost, "/internalapi/asset/").concat(asset.assetId, ".").concat(asset.dataFormat, "/get/");
    }
  }, {
    key: "getAssetCreateConfig",
    value: function getAssetCreateConfig(asset) {
      return {
        // There is no such thing as updating assets, but storage assumes it
        // should update if there is an assetId, and the asset store uses the
        // assetId as part of the create URI. So, force the method to POST.
        // Then when storage finds this config to use for the "update", still POSTs
        method: 'post',
        url: "".concat(this.assetHost, "/").concat(asset.assetId, ".").concat(asset.dataFormat),
        withCredentials: true
      };
    }
  }, {
    key: "setTranslatorFunction",
    value: function setTranslatorFunction(translator) {
      this.translator = translator;
      this.cacheDefaultProject();
    }
  }, {
    key: "cacheDefaultProject",
    value: function cacheDefaultProject() {
      var _this2 = this;
      var defaultProjectAssets = Object(_default_project__WEBPACK_IMPORTED_MODULE_1__["default"])(this.translator);
      defaultProjectAssets.forEach(function (asset) {
        return _this2.builtinHelper._store(_this2.AssetType[asset.assetType], _this2.DataFormat[asset.dataFormat], asset.data, asset.id);
      });
    }
  }]);
}(_turbowarp_scratch_storage__WEBPACK_IMPORTED_MODULE_0___default.a);
var storage = new Storage();
/* harmony default export */ __webpack_exports__["default"] = (storage);

/***/ }),

/***/ "./src/lib/touch-utils.js":
/*!********************************!*\
  !*** ./src/lib/touch-utils.js ***!
  \********************************/
/*! exports provided: getEventXY */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getEventXY", function() { return getEventXY; });
var getEventXY = function getEventXY(e) {
  if (e.touches && e.touches[0]) {
    return {
      x: e.touches[0].clientX,
      y: e.touches[0].clientY
    };
  } else if (e.changedTouches && e.changedTouches[0]) {
    return {
      x: e.changedTouches[0].clientX,
      y: e.changedTouches[0].clientY
    };
  }
  return {
    x: e.clientX,
    y: e.clientY
  };
};


/***/ }),

/***/ "./src/lib/tw-polyfill.js":
/*!********************************!*\
  !*** ./src/lib/tw-polyfill.js ***!
  \********************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
/* eslint-disable no-extend-native */

if (!Blob.prototype.text) {
  Blob.prototype.text = function () {
    var _this = this;
    return new Promise(function (resolve, reject) {
      var fr = new FileReader();
      fr.onload = function () {
        return resolve(fr.result);
      };
      fr.onerror = function () {
        return reject(new Error('Cannot read blob as text'));
      };
      fr.readAsText(_this);
    });
  };
}
if (!Array.prototype.flat) {
  Array.prototype.flat = function () {
    var depth = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 1;
    var result = [];
    var _iterator = _createForOfIteratorHelper(this),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var i = _step.value;
        if (Array.isArray(i)) {
          if (depth < 1) {
            result.push(i);
          } else {
            var _iterator2 = _createForOfIteratorHelper(i.flat(depth - 1)),
              _step2;
            try {
              for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
                var j = _step2.value;
                result.push(j);
              }
            } catch (err) {
              _iterator2.e(err);
            } finally {
              _iterator2.f();
            }
          }
        } else {
          result.push(i);
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    return result;
  };
}
if (!Array.prototype.flatMap) {
  Array.prototype.flatMap = function () {
    return this.map.apply(this, arguments).flat();
  };
}
if (typeof queueMicrotask !== 'function') {
  window.queueMicrotask = function (callback) {
    Promise.resolve().then(callback);
  };
}
if (!Object.fromEntries) {
  Object.fromEntries = function (entries) {
    var object = {};
    var _iterator3 = _createForOfIteratorHelper(entries),
      _step3;
    try {
      for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
        var entry = _step3.value;
        object[entry[0]] = entry[1];
      }
    } catch (err) {
      _iterator3.e(err);
    } finally {
      _iterator3.f();
    }
    return object;
  };
}

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/Grand9K-Pixel.woff2":
/*!*************************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/Grand9K-Pixel.woff2 ***!
  \*************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/f108ac9d733ce5d45cdea3b7828e8658.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/Griffy-Regular.woff2":
/*!**************************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/Griffy-Regular.woff2 ***!
  \**************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/208e2134a0e95cbdd211a82d2863d88d.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/Knewave.woff2":
/*!*******************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/Knewave.woff2 ***!
  \*******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/2e6994cc7c70b524c649f60346bdcb3a.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/NotoSans-Medium.woff2":
/*!***************************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/NotoSans-Medium.woff2 ***!
  \***************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/cb3ac3b4a8540f50937a86af7eb4663c.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/ScratchSavers_b2.woff2":
/*!****************************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/ScratchSavers_b2.woff2 ***!
  \****************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/75f5f189fdc419824a8134db6eb63881.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/SourceSerifPro-Regular.woff2":
/*!**********************************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/SourceSerifPro-Regular.woff2 ***!
  \**********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/5796214a21c5c1727ed7b08096ee7cff.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/handlee-regular.woff2":
/*!***************************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/handlee-regular.woff2 ***!
  \***************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__.p + "static/assets/6cd3397308e11e6d925446470b906f2a.woff2";

/***/ }),

/***/ "./src/lib/tw-scratch-render-fonts/index.js":
/*!**************************************************!*\
  !*** ./src/lib/tw-scratch-render-fonts/index.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* eslint-disable import/no-commonjs */

var SansSerif = __webpack_require__(/*! ./NotoSans-Medium.woff2 */ "./src/lib/tw-scratch-render-fonts/NotoSans-Medium.woff2");
var Serif = __webpack_require__(/*! ./SourceSerifPro-Regular.woff2 */ "./src/lib/tw-scratch-render-fonts/SourceSerifPro-Regular.woff2");
var Handwriting = __webpack_require__(/*! ./handlee-regular.woff2 */ "./src/lib/tw-scratch-render-fonts/handlee-regular.woff2");
var Marker = __webpack_require__(/*! ./Knewave.woff2 */ "./src/lib/tw-scratch-render-fonts/Knewave.woff2");
var Curly = __webpack_require__(/*! ./Griffy-Regular.woff2 */ "./src/lib/tw-scratch-render-fonts/Griffy-Regular.woff2");
var Pixel = __webpack_require__(/*! ./Grand9K-Pixel.woff2 */ "./src/lib/tw-scratch-render-fonts/Grand9K-Pixel.woff2");
var Scratch = __webpack_require__(/*! ./ScratchSavers_b2.woff2 */ "./src/lib/tw-scratch-render-fonts/ScratchSavers_b2.woff2");
var log = __webpack_require__(/*! ../log */ "./src/lib/log.js")["default"];
var fontSource = {
  'Sans Serif': SansSerif,
  'Serif': Serif,
  'Handwriting': Handwriting,
  'Marker': Marker,
  'Curly': Curly,
  'Pixel': Pixel,
  'Scratch': Scratch
};
var fontData = {};
var fetchFonts = function fetchFonts() {
  var promises = [];
  var _loop = function _loop() {
    var fontName = _Object$keys[_i];
    promises.push(fetch(fontSource[fontName]).then(function (res) {
      if (!res.ok) {
        throw new Error("Cannot load font: ".concat(fontName, " (invalid HTTP response)"));
      }
      return res.blob();
    }).then(function (blob) {
      return new Promise(function (resolve, reject) {
        var fr = new FileReader();
        fr.onload = function () {
          return resolve(fr.result);
        };
        fr.onerror = function () {
          return reject(new Error("Cannot load font: ".concat(fontName, " (could not read)")));
        };
        fr.readAsDataURL(blob);
      });
    }).then(function (url) {
      fontData[fontName] = "@font-face{font-family:\"".concat(fontName, "\";src:url(\"").concat(url, "\");}");
    })["catch"](function (err) {
      log.error(err);
    }));
  };
  for (var _i = 0, _Object$keys = Object.keys(fontSource); _i < _Object$keys.length; _i++) {
    _loop();
  }
  return Promise.all(promises);
};
var addFontsToDocument = function addFontsToDocument() {
  if (document.getElementById('scratch-font-styles')) {
    return;
  }
  var css = '';
  for (var _i2 = 0, _Object$keys2 = Object.keys(fontSource); _i2 < _Object$keys2.length; _i2++) {
    var fontName = _Object$keys2[_i2];
    var fontCSS = fontData[fontName];
    if (fontCSS) {
      css += fontCSS;
    }
  }
  var documentStyleTag = document.createElement('style');
  documentStyleTag.id = 'scratch-font-styles';
  documentStyleTag.textContent = css;
  document.body.insertBefore(documentStyleTag, document.body.firstChild);
};
var waitForFontsToLoad = function waitForFontsToLoad() {
  var promises = [];
  if (document.fonts && document.fonts.load) {
    for (var fontName in fontData) {
      promises.push(document.fonts.load("12px ".concat(fontName)));
    }
  }
  return Promise.all(promises);
};
var loadFonts = function loadFonts() {
  return fetchFonts().then(function () {
    addFontsToDocument();
    return waitForFontsToLoad();
  })["catch"](function (err) {
    log.error(err);
  });
};
var getFonts = function getFonts() {
  return fontData;
};

// We have to use legacy module.exports as some parts of Scratch expect require('scratch-render-font') to be a function
module.exports = getFonts;
module.exports.loadFonts = loadFonts;
module.exports.FONTS = fontData;

/***/ }),

/***/ "./src/lib/tw-text-encoder.js":
/*!************************************!*\
  !*** ./src/lib/tw-text-encoder.js ***!
  \************************************/
/*! exports provided: TextEncoder, TextDecoder */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "TextEncoder", function() { return _TextEncoder; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "TextDecoder", function() { return _TextDecoder; });
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var _TextEncoder = typeof TextEncoder === 'function' ? TextEncoder : /*#__PURE__*/function () {
  function TextEncoder(encoding) {
    _classCallCheck(this, TextEncoder);
    if (typeof encoding !== 'undefined' && encoding !== null && encoding !== 'utf-8' && encoding !== 'utf8') {
      throw new Error('Encoding is not supported');
    }
    this.encoding = 'utf-8';
  }
  return _createClass(TextEncoder, [{
    key: "encode",
    value: function encode(str) {
      if (typeof str !== 'string') {
        throw new TypeError('Argument is not a string');
      }
      str = unescape(encodeURIComponent(str));
      var arr = new Uint8Array(str.length);
      for (var i = 0; i < str.length; i++) {
        arr[i] = str.charCodeAt(i);
      }
      return arr;
    }
  }]);
}();
var _TextDecoder = typeof TextDecoder === 'function' ? TextDecoder : /*#__PURE__*/function () {
  function TextDecoder(encoding) {
    _classCallCheck(this, TextDecoder);
    if (typeof encoding !== 'undefined' && encoding !== null && encoding !== 'utf-8' && encoding !== 'utf8') {
      throw new Error('Encoding is not supported');
    }
    this.encoding = 'utf-8';
  }
  return _createClass(TextDecoder, [{
    key: "decode",
    value: function decode(view) {
      var array = new Uint8Array(view.buffer, view.byteOffset, view.byteLength);
      var result = '';
      for (var i = 0; i < array.length; i++) {
        result += String.fromCharCode(array[i]);
      }
      return decodeURIComponent(escape(result));
    }
  }]);
}();


/***/ }),

/***/ "./src/lib/video/camera.js":
/*!*********************************!*\
  !*** ./src/lib/video/camera.js ***!
  \*********************************/
/*! exports provided: requestVideoStream, requestDisableVideo */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "requestVideoStream", function() { return requestVideoStream; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "requestDisableVideo", function() { return requestDisableVideo; });
/* harmony import */ var get_user_media_promise__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! get-user-media-promise */ "./node_modules/get-user-media-promise/lib/get-user-media-promise.js");
/* harmony import */ var get_user_media_promise__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(get_user_media_promise__WEBPACK_IMPORTED_MODULE_0__);


// Single Setup For All Video Streams used by the GUI
// While VideoProvider uses a private _singleSetup
// property to ensure that each instance of a VideoProvider
// use the same setup, this ensures that all instances
// of VideoProviders use a single stream. This way, closing a camera modal
// does not affect the video on the stage, and a program running and disabling
// video on the stage will not affect the camera modal's video.
var requestStack = [];
var requestVideoStream = function requestVideoStream(videoDesc) {
  var streamPromise;
  if (requestStack.length === 0) {
    streamPromise = get_user_media_promise__WEBPACK_IMPORTED_MODULE_0___default()({
      audio: false,
      video: videoDesc
    });
    requestStack.push(streamPromise);
  } else if (requestStack.length > 0) {
    streamPromise = requestStack[0];
    requestStack.push(true);
  }
  return streamPromise;
};
var requestDisableVideo = function requestDisableVideo() {
  requestStack.pop();
  if (requestStack.length > 0) return false;
  return true;
};


/***/ }),

/***/ "./src/lib/video/video-provider.js":
/*!*****************************************!*\
  !*** ./src/lib/video/video-provider.js ***!
  \*****************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _camera_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./camera.js */ "./src/lib/video/camera.js");
/* harmony import */ var _log_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../log.js */ "./src/lib/log.js");
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }



/**
 * Video Manager for video extensions.
 */
var VideoProvider = /*#__PURE__*/function () {
  function VideoProvider() {
    _classCallCheck(this, VideoProvider);
    /**
     * Default value for mirrored frames.
     * @type boolean
     */
    this.mirror = true;

    /**
     * Cache frames for this many ms.
     * @type number
     */
    this._frameCacheTimeout = 16;

    /**
     * DOM Video element
     * @private
     */
    this._video = null;

    /**
     * Usermedia stream track
     * @private
     */
    this._track = null;

    /**
     * Stores some canvas/frame data per resolution/mirror states
     */
    this._workspace = [];
  }
  return _createClass(VideoProvider, [{
    key: "video",
    get:
    /**
     * Get the HTML video element containing the stream
     */
    function get() {
      return this._video;
    }

    /**
     * Request video be enabled.  Sets up video, creates video skin and enables preview.
     *
     * @return {Promise.<Video>} resolves a promise to this video provider when video is ready.
     */
  }, {
    key: "enableVideo",
    value: function enableVideo() {
      this.enabled = true;
      return this._setupVideo();
    }

    /**
     * Disable video stream (turn video off)
     */
  }, {
    key: "disableVideo",
    value: function disableVideo() {
      var _this = this;
      this.enabled = false;
      // If we have begun a setup process, call _teardown after it completes
      if (this._singleSetup) {
        this._singleSetup.then(this._teardown.bind(this))["catch"](function (err) {
          return _this.onError(err);
        });
      }
    }

    /**
     * async part of disableVideo
     * @private
     */
  }, {
    key: "_teardown",
    value: function _teardown() {
      // we might be asked to re-enable before _teardown is called, just ignore it.
      if (this.enabled === false) {
        var disableTrack = Object(_camera_js__WEBPACK_IMPORTED_MODULE_0__["requestDisableVideo"])();
        this._singleSetup = null;
        // by clearing refs to video and track, we should lose our hold over the camera
        this._video = null;
        if (this._track && disableTrack) {
          this._track.stop();
        }
        this._track = null;
      }
    }

    /**
     * Return frame data from the video feed in a specified dimensions, format, and mirroring.
     *
     * @param {object} frameInfo A descriptor of the frame you would like to receive.
     * @param {Array.<number>} frameInfo.dimensions [width, height] array of numbers.  Defaults to [480,360]
     * @param {boolean} frameInfo.mirror If you specificly want a mirror/non-mirror frame, defaults to true
     * @param {string} frameInfo.format Requested video format, available formats are 'image-data' and 'canvas'.
     * @param {number} frameInfo.cacheTimeout Will reuse previous image data if the time since capture is less than
     *                                        the cacheTimeout.  Defaults to 16ms.
     *
     * @return {ArrayBuffer|Canvas|string|null} Frame data in requested format, null when errors.
     */
  }, {
    key: "getFrame",
    value: function getFrame(_ref) {
      var _ref$dimensions = _ref.dimensions,
        dimensions = _ref$dimensions === void 0 ? VideoProvider.DIMENSIONS : _ref$dimensions,
        _ref$mirror = _ref.mirror,
        mirror = _ref$mirror === void 0 ? this.mirror : _ref$mirror,
        _ref$format = _ref.format,
        format = _ref$format === void 0 ? VideoProvider.FORMAT_IMAGE_DATA : _ref$format,
        _ref$cacheTimeout = _ref.cacheTimeout,
        cacheTimeout = _ref$cacheTimeout === void 0 ? this._frameCacheTimeout : _ref$cacheTimeout;
      if (!this.videoReady) {
        return null;
      }
      var _dimensions = _slicedToArray(dimensions, 2),
        width = _dimensions[0],
        height = _dimensions[1];
      var workspace = this._getWorkspace({
        dimensions: dimensions,
        mirror: Boolean(mirror)
      });
      var _this$_video = this._video,
        videoWidth = _this$_video.videoWidth,
        videoHeight = _this$_video.videoHeight;
      var canvas = workspace.canvas,
        context = workspace.context,
        lastUpdate = workspace.lastUpdate,
        cacheData = workspace.cacheData;
      var now = Date.now();

      // if the canvas hasn't been updated...
      if (lastUpdate + cacheTimeout < now) {
        if (mirror) {
          context.scale(-1, 1);
          context.translate(width * -1, 0);
        }
        context.drawImage(this._video,
        // source x, y, width, height
        0, 0, videoWidth, videoHeight,
        // dest x, y, width, height
        0, 0, width, height);

        // context.resetTransform() doesn't work on Edge but the following should
        context.setTransform(1, 0, 0, 1, 0, 0);
        workspace.lastUpdate = now;
      }

      // each data type has it's own data cache, but the canvas is the same
      if (!cacheData[format]) {
        cacheData[format] = {
          lastUpdate: 0
        };
      }
      var formatCache = cacheData[format];
      if (formatCache.lastUpdate + cacheTimeout < now) {
        if (format === VideoProvider.FORMAT_IMAGE_DATA) {
          formatCache.lastData = context.getImageData(0, 0, width, height);
        } else if (format === VideoProvider.FORMAT_CANVAS) {
          // this will never change
          formatCache.lastUpdate = Infinity;
          formatCache.lastData = canvas;
        } else {
          _log_js__WEBPACK_IMPORTED_MODULE_1__["default"].error("video io error - unimplemented format ".concat(format));
          // cache the null result forever, don't log about it again..
          formatCache.lastUpdate = Infinity;
          formatCache.lastData = null;
        }

        // rather than set to now, this data is as stale as it's canvas is
        formatCache.lastUpdate = Math.max(workspace.lastUpdate, formatCache.lastUpdate);
      }
      return formatCache.lastData;
    }

    /**
     * Method called when an error happens.  Default implementation is just to log error.
     *
     * @abstract
     * @param {Error} error An error object from getUserMedia or other source of error.
     */
  }, {
    key: "onError",
    value: function onError(error) {
      _log_js__WEBPACK_IMPORTED_MODULE_1__["default"].error('Unhandled video io device error', error);
    }

    /**
     * Create a video stream.
     * @private
     * @return {Promise} When video has been received, rejected if video is not received
     */
  }, {
    key: "_setupVideo",
    value: function _setupVideo() {
      var _this2 = this;
      // We cache the result of this setup so that we can only ever have a single
      // video/getUserMedia request happen at a time.
      if (this._singleSetup) {
        return this._singleSetup;
      }
      this._singleSetup = Object(_camera_js__WEBPACK_IMPORTED_MODULE_0__["requestVideoStream"])({
        width: {
          min: 480,
          ideal: 640
        },
        height: {
          min: 360,
          ideal: 480
        }
      }).then(function (stream) {
        _this2._video = document.createElement('video');

        // Use the new srcObject API, falling back to createObjectURL
        try {
          _this2._video.srcObject = stream;
        } catch (error) {
          _this2._video.src = window.URL.createObjectURL(stream);
        }
        // Hint to the stream that it should load. A standard way to do this
        // is add the video tag to the DOM. Since this extension wants to
        // hide the video tag and instead render a sample of the stream into
        // the webgl rendered Scratch canvas, another hint like this one is
        // needed.
        _this2._video.play(); // Needed for Safari/Firefox, Chrome auto-plays.
        _this2._track = stream.getTracks()[0];
        return _this2;
      })["catch"](function (error) {
        _this2._singleSetup = null;
        _this2.onError(error);
      });
      return this._singleSetup;
    }
  }, {
    key: "videoReady",
    get: function get() {
      if (!this.enabled) {
        return false;
      }
      if (!this._video) {
        return false;
      }
      if (!this._track) {
        return false;
      }
      var _this$_video2 = this._video,
        videoWidth = _this$_video2.videoWidth,
        videoHeight = _this$_video2.videoHeight;
      if (typeof videoWidth !== 'number' || typeof videoHeight !== 'number') {
        return false;
      }
      if (videoWidth === 0 || videoHeight === 0) {
        return false;
      }
      return true;
    }

    /**
     * get an internal workspace for canvas/context/caches
     * this uses some document stuff to create a canvas and what not, probably needs abstraction
     * into the renderer layer?
     * @private
     * @return {object} A workspace for canvas/data storage.  Internal format not documented intentionally
     */
  }, {
    key: "_getWorkspace",
    value: function _getWorkspace(_ref2) {
      var dimensions = _ref2.dimensions,
        mirror = _ref2.mirror;
      var workspace = this._workspace.find(function (space) {
        return space.dimensions.join('-') === dimensions.join('-') && space.mirror === mirror;
      });
      if (!workspace) {
        workspace = {
          dimensions: dimensions,
          mirror: mirror,
          canvas: document.createElement('canvas'),
          lastUpdate: 0,
          cacheData: {}
        };
        workspace.canvas.width = dimensions[0];
        workspace.canvas.height = dimensions[1];
        workspace.context = workspace.canvas.getContext('2d');
        this._workspace.push(workspace);
      }
      return workspace;
    }
  }], [{
    key: "FORMAT_IMAGE_DATA",
    get: function get() {
      return 'image-data';
    }
  }, {
    key: "FORMAT_CANVAS",
    get: function get() {
      return 'canvas';
    }

    /**
     * Dimensions the video stream is analyzed at after its rendered to the
     * sample canvas.
     * @type {Array.<number>}
     */
  }, {
    key: "DIMENSIONS",
    get: function get() {
      return [480, 360];
    }

    /**
     * Order preview drawable is inserted at in the renderer.
     * @type {number}
     */
  }, {
    key: "ORDER",
    get: function get() {
      return 1;
    }
  }]);
}();
/* harmony default export */ __webpack_exports__["default"] = (VideoProvider);

/***/ }),

/***/ "./src/playground/app-target.js":
/*!**************************************!*\
  !*** ./src/playground/app-target.js ***!
  \**************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-dom */ "./node_modules/react-dom/index.js");
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react_dom__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var react_modal__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-modal */ "./node_modules/react-modal/lib/index.js");
/* harmony import */ var react_modal__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_modal__WEBPACK_IMPORTED_MODULE_1__);


var appTarget = document.getElementById('app');

// Remove everything from the target to fix macOS Safari "Save Page As",
while (appTarget.firstChild) {
  appTarget.removeChild(appTarget.firstChild);
}
Object(react_modal__WEBPACK_IMPORTED_MODULE_1__["setAppElement"])(appTarget);
var render = function render(children) {
  react_dom__WEBPACK_IMPORTED_MODULE_0___default.a.render(children, appTarget);
  if (window.SplashEnd) {
    window.SplashEnd();
  }
};
/* harmony default export */ __webpack_exports__["default"] = (render);

/***/ }),

/***/ "./src/playground/import-first.js":
/*!****************************************!*\
  !*** ./src/playground/import-first.js ***!
  \****************************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _public_path__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./public-path */ "./src/playground/public-path.js");
/* harmony import */ var _public_path__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_public_path__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _lib_tw_polyfill__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../lib/tw-polyfill */ "./src/lib/tw-polyfill.js");
/* harmony import */ var _lib_tw_polyfill__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_lib_tw_polyfill__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _lib_normalize_css__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../lib/normalize.css */ "./src/lib/normalize.css");
/* harmony import */ var _lib_normalize_css__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_lib_normalize_css__WEBPACK_IMPORTED_MODULE_2__);




/***/ }),

/***/ "./src/playground/public-path.js":
/*!***************************************!*\
  !*** ./src/playground/public-path.js ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* eslint-disable */
if (false) {}

/***/ }),

/***/ "./src/playground/vm-live.css":
/*!************************************!*\
  !*** ./src/playground/vm-live.css ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {


var content = __webpack_require__(/*! !../../node_modules/css-loader??ref--5-1!../../node_modules/postcss-loader/src??postcss!./vm-live.css */ "./node_modules/css-loader/index.js?!./node_modules/postcss-loader/src/index.js?!./src/playground/vm-live.css");

if(typeof content === 'string') content = [[module.i, content, '']];

var transform;
var insertInto;



var options = {"hmr":true}

options.transform = transform
options.insertInto = undefined;

var update = __webpack_require__(/*! ../../node_modules/style-loader/lib/addStyles.js */ "./node_modules/style-loader/lib/addStyles.js")(content, options);

if(content.locals) module.exports = content.locals;

if(false) {}

/***/ }),

/***/ "./src/playground/vm-live.jsx":
/*!************************************!*\
  !*** ./src/playground/vm-live.jsx ***!
  \************************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _import_first__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./import-first */ "./src/playground/import-first.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var scratch_render__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! scratch-render */ "./node_modules/scratch-render/src/index.js");
/* harmony import */ var scratch_render__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(scratch_render__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var scratch_vm__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! scratch-vm */ "../vm/src/index.js");
/* harmony import */ var scratch_vm__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(scratch_vm__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var scratch_audio__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! scratch-audio */ "./node_modules/scratch-audio/src/index.js");
/* harmony import */ var scratch_audio__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(scratch_audio__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var _turbowarp_scratch_svg_renderer__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @turbowarp/scratch-svg-renderer */ "./node_modules/@turbowarp/scratch-svg-renderer/src/index.js");
/* harmony import */ var _turbowarp_scratch_svg_renderer__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(_turbowarp_scratch_svg_renderer__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var _lib_storage__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../lib/storage */ "./src/lib/storage.js");
/* harmony import */ var _lib_video_video_provider__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../lib/video/video-provider */ "./src/lib/video/video-provider.js");
/* harmony import */ var _lib_touch_utils__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../lib/touch-utils */ "./src/lib/touch-utils.js");
/* harmony import */ var _app_target__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./app-target */ "./src/playground/app-target.js");
/* harmony import */ var _vm_live_css__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./vm-live.css */ "./src/playground/vm-live.css");
/* harmony import */ var _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(_vm_live_css__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var _arraybuffer_loader_vm_test_fixtures_default_sb3__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! arraybuffer-loader!../../../vm/test/fixtures/default.sb3 */ "./node_modules/arraybuffer-loader/index.js!../vm/test/fixtures/default.sb3");
/* harmony import */ var _arraybuffer_loader_vm_test_fixtures_default_sb3__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(_arraybuffer_loader_vm_test_fixtures_default_sb3__WEBPACK_IMPORTED_MODULE_12__);
/* harmony import */ var _arraybuffer_loader_vm_test_fixtures_tw_glide_sb3__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! arraybuffer-loader!../../../vm/test/fixtures/tw-glide.sb3 */ "./node_modules/arraybuffer-loader/index.js!../vm/test/fixtures/tw-glide.sb3");
/* harmony import */ var _arraybuffer_loader_vm_test_fixtures_tw_glide_sb3__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(_arraybuffer_loader_vm_test_fixtures_tw_glide_sb3__WEBPACK_IMPORTED_MODULE_13__);
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { if (r) i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n;else { var o = function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); }; o("next", 0), o("throw", 1), o("return", 2); } }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _callSuper(t, o, e) { return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e)); }
function _possibleConstructorReturn(t, e) { if (e && ("object" == _typeof(e) || "function" == typeof e)) return e; if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined"); return _assertThisInitialized(t); }
function _assertThisInitialized(e) { if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); return e; }
function _isNativeReflectConstruct() { try { var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); } catch (t) {} return (_isNativeReflectConstruct = function _isNativeReflectConstruct() { return !!t; })(); }
function _getPrototypeOf(t) { return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) { return t.__proto__ || Object.getPrototypeOf(t); }, _getPrototypeOf(t); }
function _inherits(t, e) { if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function"); t.prototype = Object.create(e && e.prototype, { constructor: { value: t, writable: !0, configurable: !0 } }), Object.defineProperty(t, "prototype", { writable: !1 }), e && _setPrototypeOf(t, e); }
function _setPrototypeOf(t, e) { return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) { return t.__proto__ = e, t; }, _setPrototypeOf(t, e); }













/* eslint-disable import/no-unresolved */


/* eslint-enable import/no-unresolved */

var SAMPLE_PROJECTS = {
  glide: {
    label: 'TurboWarp Glide Demo',
    buffer: _arraybuffer_loader_vm_test_fixtures_tw_glide_sb3__WEBPACK_IMPORTED_MODULE_13___default.a,
    autoStart: true
  },
  "default": {
    label: 'Scratch Default Cat',
    buffer: _arraybuffer_loader_vm_test_fixtures_default_sb3__WEBPACK_IMPORTED_MODULE_12___default.a,
    autoStart: false
  }
};
var DEFAULT_SAMPLE = 'glide';
var DEFAULT_PROJECT_HOST = 'https://projects.scratch.mit.edu';
var DEFAULT_ASSET_HOST = 'https://assets.scratch.mit.edu';
var isEditableElement = function isEditableElement(element) {
  return element && (element.tagName === 'INPUT' || element.tagName === 'TEXTAREA' || element.tagName === 'SELECT' || element.isContentEditable);
};
var normalizeProjectUrl = function normalizeProjectUrl(projectUrl) {
  if (!projectUrl) return '';
  if (projectUrl.startsWith('http://') || projectUrl.startsWith('https://') || projectUrl.startsWith('data:')) {
    return projectUrl;
  }
  return "https://".concat(projectUrl);
};
var createLogEntry = function createLogEntry(type, message) {
  return {
    id: "".concat(Date.now(), "-").concat(Math.random()),
    type: type,
    message: message,
    time: new Date().toLocaleTimeString()
  };
};
var Toggle = /*#__PURE__*/function (_React$PureComponent) {
  function Toggle() {
    _classCallCheck(this, Toggle);
    return _callSuper(this, Toggle, arguments);
  }
  _inherits(Toggle, _React$PureComponent);
  return _createClass(Toggle, [{
    key: "render",
    value: function render() {
      var _this$props = this.props,
        checked = _this$props.checked,
        hint = _this$props.hint,
        label = _this$props.label,
        onChange = _this$props.onChange;
      return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("label", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.toggle
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.toggleMeta
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.toggleLabel
      }, label), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.toggleHint
      }, hint)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a["switch"]
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("input", {
        checked: checked,
        onChange: onChange,
        type: "checkbox"
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.switchTrack
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.switchThumb
      })));
    }
  }]);
}(react__WEBPACK_IMPORTED_MODULE_2___default.a.PureComponent);
Toggle.propTypes = {
  checked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool.isRequired,
  hint: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string.isRequired,
  label: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string.isRequired,
  onChange: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired
};
var VMLivePage = /*#__PURE__*/function (_React$Component) {
  function VMLivePage(props) {
    var _this;
    _classCallCheck(this, VMLivePage);
    _this = _callSuper(this, VMLivePage, [props]);
    _this.canvas = document.createElement('canvas');
    _this.canvasHost = null;
    _this.vm = new scratch_vm__WEBPACK_IMPORTED_MODULE_4___default.a();
    _this.renderer = new scratch_render__WEBPACK_IMPORTED_MODULE_3___default.a(_this.canvas, -240, 240, -180, 180);
    _this.fileInputRef = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createRef();
    _this.questionInputRef = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createRef();
    _this.statsFrame = null;
    _this.lastFrameTime = 0;
    _this.lastStatsCommit = 0;
    _this.rect = null;
    _this.state = {
      autoStart: true,
      compileErrors: 0,
      compilerEnabled: true,
      compilerLog: [],
      error: '',
      fps: 0,
      isLoading: true,
      isRunning: false,
      isRuntimeStarted: false,
      monitors: [],
      offloadEnabled: false,
      projectLabel: SAMPLE_PROJECTS[DEFAULT_SAMPLE].label,
      projectUrl: '',
      question: null,
      questionAnswer: '',
      rendererBackend: 'Booting',
      selectedSample: DEFAULT_SAMPLE,
      stageSize: '480x360',
      targetCount: 0,
      turboEnabled: false,
      wasmEnabled: true,
      webglEnabled: true
    };
    _this.handleCanvasResize = _this.handleCanvasResize.bind(_this);
    _this.handleCompileError = _this.handleCompileError.bind(_this);
    _this.handleCompilerToggle = _this.handleCompilerToggle.bind(_this);
    _this.handleContextMenu = _this.handleContextMenu.bind(_this);
    _this.handleDrop = _this.handleDrop.bind(_this);
    _this.handleFileChange = _this.handleFileChange.bind(_this);
    _this.handleFilePicker = _this.handleFilePicker.bind(_this);
    _this.handleGreenFlag = _this.handleGreenFlag.bind(_this);
    _this.handleKeyDown = _this.handleKeyDown.bind(_this);
    _this.handleKeyUp = _this.handleKeyUp.bind(_this);
    _this.handleMonitorUpdate = _this.handleMonitorUpdate.bind(_this);
    _this.handleMouseDown = _this.handleMouseDown.bind(_this);
    _this.handleMouseMove = _this.handleMouseMove.bind(_this);
    _this.handleMouseUp = _this.handleMouseUp.bind(_this);
    _this.handleProjectUrlChange = _this.handleProjectUrlChange.bind(_this);
    _this.handleQuestionAnswer = _this.handleQuestionAnswer.bind(_this);
    _this.handleQuestionSubmit = _this.handleQuestionSubmit.bind(_this);
    _this.handleRemoteLoad = _this.handleRemoteLoad.bind(_this);
    _this.handleRuntimeStart = _this.handleRuntimeStart.bind(_this);
    _this.handleRuntimeStop = _this.handleRuntimeStop.bind(_this);
    _this.handleRunStart = _this.handleRunStart.bind(_this);
    _this.handleRunStop = _this.handleRunStop.bind(_this);
    _this.handleSampleChange = _this.handleSampleChange.bind(_this);
    _this.handleSampleLoad = _this.handleSampleLoad.bind(_this);
    _this.handleStopAll = _this.handleStopAll.bind(_this);
    _this.handleToggleChange = _this.handleToggleChange.bind(_this);
    _this.handleTurboToggle = _this.handleTurboToggle.bind(_this);
    _this.handleWheel = _this.handleWheel.bind(_this);
    _this.pollStats = _this.pollStats.bind(_this);
    _this.setCanvasHost = _this.setCanvasHost.bind(_this);
    return _this;
  }
  _inherits(VMLivePage, _React$Component);
  return _createClass(VMLivePage, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      var _this2 = this;
      window.vm = this.vm;
      window.renderer = this.renderer;
      _lib_storage__WEBPACK_IMPORTED_MODULE_7__["default"].setProjectHost(DEFAULT_PROJECT_HOST);
      _lib_storage__WEBPACK_IMPORTED_MODULE_7__["default"].setAssetHost(DEFAULT_ASSET_HOST);
      this.vm.attachStorage(_lib_storage__WEBPACK_IMPORTED_MODULE_7__["default"]);
      this.vm.attachRenderer(this.renderer);
      this.vm.attachV2BitmapAdapter(new _turbowarp_scratch_svg_renderer__WEBPACK_IMPORTED_MODULE_6__["BitmapAdapter"]());
      this.vm.setVideoProvider(new _lib_video_video_provider__WEBPACK_IMPORTED_MODULE_8__["default"]());
      this.vm.setCompatibilityMode(true);
      try {
        this.audioEngine = new scratch_audio__WEBPACK_IMPORTED_MODULE_5___default.a();
        this.vm.attachAudioEngine(this.audioEngine);
      } catch (e) {
        this.pushLog('error', "Audio engine unavailable: ".concat(e));
      }
      this.vm.on('PROJECT_RUN_START', this.handleRunStart);
      this.vm.on('PROJECT_RUN_STOP', this.handleRunStop);
      this.vm.on('RUNTIME_STARTED', this.handleRuntimeStart);
      this.vm.on('RUNTIME_STOPPED', this.handleRuntimeStop);
      this.vm.on('COMPILE_ERROR', this.handleCompileError);
      this.vm.on('TURBO_MODE_ON', function () {
        return _this2.setState({
          turboEnabled: true
        });
      });
      this.vm.on('TURBO_MODE_OFF', function () {
        return _this2.setState({
          turboEnabled: false
        });
      });
      this.vm.on('MONITORS_UPDATE', this.handleMonitorUpdate);
      this.vm.runtime.on('QUESTION', function (question) {
        _this2.setState({
          question: question,
          questionAnswer: ''
        }, function () {
          if (question !== null && _this2.questionInputRef.current) {
            _this2.questionInputRef.current.focus();
          }
        });
      });
      this.vm.runtime.on('PROJECT_LOADED', function () {
        var nativeSize = _this2.renderer.getNativeSize();
        var rendererBackend = _this2.renderer && _this2.renderer._gl ? 'WebGL' : 'Canvas Fallback';
        _this2.setState({
          error: '',
          isLoading: false,
          rendererBackend: rendererBackend,
          stageSize: "".concat(nativeSize[0], "x").concat(nativeSize[1]),
          targetCount: _this2.vm.runtime.targets.length
        });
        _this2.renderer.draw();
      });
      document.addEventListener('mousemove', this.handleMouseMove);
      document.addEventListener('mouseup', this.handleMouseUp);
      document.addEventListener('touchmove', this.handleMouseMove, {
        passive: false
      });
      document.addEventListener('touchend', this.handleMouseUp);
      document.addEventListener('keydown', this.handleKeyDown);
      document.addEventListener('keyup', this.handleKeyUp);
      window.addEventListener('resize', this.handleCanvasResize);
      window.addEventListener('dragover', this.handleDrop);
      window.addEventListener('drop', this.handleDrop);
      this.vm.start();
      this.handleCanvasResize();
      this.applyCompilerOptions();
      this.renderer.draw();
      this.statsFrame = requestAnimationFrame(this.pollStats);
      var searchParams = new URLSearchParams(location.search);
      var requestedProjectUrl = searchParams.get('project_url') || '';
      var sample = searchParams.get('sample') || DEFAULT_SAMPLE;
      var sampleId = SAMPLE_PROJECTS[sample] ? sample : DEFAULT_SAMPLE;
      var autoStart = searchParams.has('autoplay') ? searchParams.get('autoplay') !== '0' : SAMPLE_PROJECTS[sampleId].autoStart;
      this.setState({
        autoStart: autoStart,
        projectUrl: requestedProjectUrl,
        selectedSample: sampleId
      }, function () {
        if (requestedProjectUrl) {
          _this2.handleRemoteLoad();
        } else {
          _this2.loadProjectBuffer(SAMPLE_PROJECTS[sampleId].buffer, SAMPLE_PROJECTS[sampleId].label, autoStart);
        }
      });
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      cancelAnimationFrame(this.statsFrame);
      document.removeEventListener('mousemove', this.handleMouseMove);
      document.removeEventListener('mouseup', this.handleMouseUp);
      document.removeEventListener('touchmove', this.handleMouseMove);
      document.removeEventListener('touchend', this.handleMouseUp);
      document.removeEventListener('keydown', this.handleKeyDown);
      document.removeEventListener('keyup', this.handleKeyUp);
      window.removeEventListener('resize', this.handleCanvasResize);
      window.removeEventListener('dragover', this.handleDrop);
      window.removeEventListener('drop', this.handleDrop);
      this.canvas.removeEventListener('mousedown', this.handleMouseDown);
      this.canvas.removeEventListener('touchstart', this.handleMouseDown);
      this.canvas.removeEventListener('wheel', this.handleWheel);
      this.canvas.removeEventListener('contextmenu', this.handleContextMenu);
      this.vm.stopAll();
      this.vm.quit();
    }
  }, {
    key: "setCanvasHost",
    value: function setCanvasHost(element) {
      if (this.canvasHost === element) {
        return;
      }
      this.canvasHost = element;
      if (element && this.canvas.parentNode !== element) {
        element.appendChild(this.canvas);
        this.canvas.addEventListener('mousedown', this.handleMouseDown);
        this.canvas.addEventListener('touchstart', this.handleMouseDown, {
          passive: false
        });
        this.canvas.addEventListener('wheel', this.handleWheel, {
          passive: false
        });
        this.canvas.addEventListener('contextmenu', this.handleContextMenu);
        this.handleCanvasResize();
      }
    }
  }, {
    key: "getMouseData",
    value: function getMouseData(e) {
      this.rect = this.canvas.getBoundingClientRect();
      var _getEventXY = Object(_lib_touch_utils__WEBPACK_IMPORTED_MODULE_9__["getEventXY"])(e),
        x = _getEventXY.x,
        y = _getEventXY.y;
      return {
        x: x - this.rect.left,
        y: y - this.rect.top,
        canvasWidth: this.rect.width,
        canvasHeight: this.rect.height
      };
    }
  }, {
    key: "pushLog",
    value: function pushLog(type, message) {
      this.setState(function (prevState) {
        return {
          compilerLog: [createLogEntry(type, message)].concat(prevState.compilerLog).slice(0, 8)
        };
      });
    }
  }, {
    key: "applyCompilerOptions",
    value: function applyCompilerOptions() {
      this.vm.setTurboMode(this.state.turboEnabled);
      this.vm.setCompilerOptions({
        enabled: this.state.compilerEnabled,
        warpTimer: false,
        offload: {
          enabled: this.state.offloadEnabled,
          wasm: this.state.wasmEnabled,
          webgl: this.state.webglEnabled,
          minWasmIterations: 256,
          minWebGLElements: 4096,
          verifyGPU: 'conservative'
        }
      });
    }
  }, {
    key: "loadProjectBuffer",
    value: function () {
      var _loadProjectBuffer = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(buffer, label) {
        var autoStart,
          _args = arguments,
          _t;
        return _regenerator().w(function (_context) {
          while (1) switch (_context.n) {
            case 0:
              autoStart = _args.length > 2 && _args[2] !== undefined ? _args[2] : this.state.autoStart;
              this.setState({
                error: '',
                isLoading: true,
                projectLabel: label,
                question: null
              });
              this.vm.stopAll();
              this.vm.clear();
              this.handleMonitorUpdate(null);
              _context.p = 1;
              _context.n = 2;
              return this.vm.loadProject(buffer);
            case 2:
              this.pushLog('info', "Loaded ".concat(label));
              if (autoStart) {
                this.vm.greenFlag();
              }
              _context.n = 4;
              break;
            case 3:
              _context.p = 3;
              _t = _context.v;
              this.setState({
                error: "".concat(_t),
                isLoading: false
              });
              this.pushLog('error', "Load failed: ".concat(_t));
            case 4:
              return _context.a(2);
          }
        }, _callee, this, [[1, 3]]);
      }));
      function loadProjectBuffer(_x, _x2) {
        return _loadProjectBuffer.apply(this, arguments);
      }
      return loadProjectBuffer;
    }()
  }, {
    key: "loadRemoteProject",
    value: function () {
      var _loadRemoteProject = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(projectUrl) {
        var normalizedUrl, response, buffer, _t2;
        return _regenerator().w(function (_context2) {
          while (1) switch (_context2.n) {
            case 0:
              normalizedUrl = normalizeProjectUrl(projectUrl);
              this.setState({
                error: '',
                isLoading: true,
                projectUrl: normalizedUrl
              });
              _context2.p = 1;
              _context2.n = 2;
              return fetch(normalizedUrl);
            case 2:
              response = _context2.v;
              if (response.ok) {
                _context2.n = 3;
                break;
              }
              throw new Error("Request returned status ".concat(response.status));
            case 3:
              _context2.n = 4;
              return response.arrayBuffer();
            case 4:
              buffer = _context2.v;
              _context2.n = 5;
              return this.loadProjectBuffer(buffer, normalizedUrl);
            case 5:
              _context2.n = 7;
              break;
            case 6:
              _context2.p = 6;
              _t2 = _context2.v;
              this.setState({
                error: "".concat(_t2),
                isLoading: false
              });
              this.pushLog('error', "Remote load failed: ".concat(_t2));
            case 7:
              return _context2.a(2);
          }
        }, _callee2, this, [[1, 6]]);
      }));
      function loadRemoteProject(_x3) {
        return _loadRemoteProject.apply(this, arguments);
      }
      return loadRemoteProject;
    }()
  }, {
    key: "pollStats",
    value: function pollStats(timestamp) {
      if (this.lastFrameTime) {
        var instantaneousFps = 1000 / Math.max(1, timestamp - this.lastFrameTime);
        if (timestamp - this.lastStatsCommit > 240) {
          var nativeSize = this.renderer.getNativeSize();
          this.setState({
            fps: Math.round(instantaneousFps),
            rendererBackend: this.renderer && this.renderer._gl ? 'WebGL' : 'Canvas Fallback',
            stageSize: "".concat(nativeSize[0], "x").concat(nativeSize[1]),
            targetCount: this.vm.runtime.targets.length
          });
          this.lastStatsCommit = timestamp;
        }
      }
      this.lastFrameTime = timestamp;
      this.statsFrame = requestAnimationFrame(this.pollStats);
    }
  }, {
    key: "handleCanvasResize",
    value: function handleCanvasResize() {
      if (!this.canvasHost) {
        return;
      }
      var _this$canvasHost = this.canvasHost,
        clientWidth = _this$canvasHost.clientWidth,
        clientHeight = _this$canvasHost.clientHeight;
      if (clientWidth > 0 && clientHeight > 0) {
        this.renderer.resize(clientWidth, clientHeight);
        this.renderer.draw();
      }
    }
  }, {
    key: "handleCompileError",
    value: function handleCompileError(target, error) {
      this.setState(function (prevState) {
        return {
          compileErrors: prevState.compileErrors + 1
        };
      });
      this.pushLog('error', "".concat(target.getName(), ": ").concat(error));
    }
  }, {
    key: "handleContextMenu",
    value: function handleContextMenu(e) {
      e.preventDefault();
    }
  }, {
    key: "handleMonitorUpdate",
    value: function handleMonitorUpdate(monitors) {
      if (!monitors || typeof monitors.valueSeq !== 'function') {
        this.setState({
          monitors: []
        });
        return;
      }
      var renderedMonitors = monitors.valueSeq().toArray().filter(function (monitor) {
        return monitor.get('visible');
      }).slice(0, 6).map(function (monitor) {
        var params = monitor.get('params');
        var label = params ? typeof params.get === 'function' ? params.get('LABEL') : params.LABEL : null;
        return {
          id: monitor.get('id'),
          label: label || monitor.get('opcode'),
          value: "".concat(monitor.get('value'))
        };
      });
      this.setState({
        monitors: renderedMonitors
      });
    }
  }, {
    key: "handleRunStart",
    value: function handleRunStart() {
      this.setState({
        isRunning: true
      });
    }
  }, {
    key: "handleRunStop",
    value: function handleRunStop() {
      this.setState({
        isRunning: false
      });
    }
  }, {
    key: "handleRuntimeStart",
    value: function handleRuntimeStart() {
      this.setState({
        isRuntimeStarted: true
      });
    }
  }, {
    key: "handleRuntimeStop",
    value: function handleRuntimeStop() {
      this.setState({
        isRuntimeStarted: false
      });
    }
  }, {
    key: "handleGreenFlag",
    value: function handleGreenFlag() {
      this.vm.greenFlag();
      this.pushLog('info', 'Green flag started');
    }
  }, {
    key: "handleStopAll",
    value: function handleStopAll() {
      this.vm.stopAll();
      this.pushLog('info', 'Stopped all scripts');
    }
  }, {
    key: "handleTurboToggle",
    value: function handleTurboToggle() {
      var _this3 = this;
      this.setState(function (prevState) {
        return {
          turboEnabled: !prevState.turboEnabled
        };
      }, function () {
        _this3.vm.setTurboMode(_this3.state.turboEnabled);
        _this3.pushLog('info', "Turbo ".concat(_this3.state.turboEnabled ? 'enabled' : 'disabled'));
      });
    }
  }, {
    key: "handleCompilerToggle",
    value: function handleCompilerToggle() {
      var _this4 = this;
      this.setState(function (prevState) {
        return {
          compilerEnabled: !prevState.compilerEnabled
        };
      }, function () {
        _this4.applyCompilerOptions();
        _this4.pushLog('info', "Compiler ".concat(_this4.state.compilerEnabled ? 'enabled' : 'disabled'));
      });
    }
  }, {
    key: "handleToggleChange",
    value: function handleToggleChange(key) {
      var _this5 = this;
      this.setState(function (prevState) {
        return _defineProperty({}, key, !prevState[key]);
      }, function () {
        _this5.applyCompilerOptions();
        _this5.pushLog('info', "".concat(key, " => ").concat(_this5.state[key]));
      });
    }
  }, {
    key: "handleProjectUrlChange",
    value: function handleProjectUrlChange(e) {
      this.setState({
        projectUrl: e.target.value
      });
    }
  }, {
    key: "handleRemoteLoad",
    value: function handleRemoteLoad(e) {
      if (e) e.preventDefault();
      if (!this.state.projectUrl.trim()) {
        return;
      }
      this.loadRemoteProject(this.state.projectUrl.trim());
    }
  }, {
    key: "handleSampleChange",
    value: function handleSampleChange(e) {
      this.setState({
        selectedSample: e.target.value
      });
    }
  }, {
    key: "handleSampleLoad",
    value: function handleSampleLoad() {
      var sample = SAMPLE_PROJECTS[this.state.selectedSample];
      this.loadProjectBuffer(sample.buffer, sample.label, this.state.autoStart);
    }
  }, {
    key: "handleFilePicker",
    value: function handleFilePicker() {
      if (this.fileInputRef.current) {
        this.fileInputRef.current.click();
      }
    }
  }, {
    key: "handleFileChange",
    value: function handleFileChange(e) {
      var _this6 = this;
      var file = e.target.files && e.target.files[0];
      if (!file) {
        return;
      }
      var reader = new FileReader();
      reader.onload = function () {
        _this6.loadProjectBuffer(reader.result, file.name, _this6.state.autoStart);
      };
      reader.onerror = function () {
        _this6.pushLog('error', "Failed to read ".concat(file.name));
      };
      reader.readAsArrayBuffer(file);
      e.target.value = '';
    }
  }, {
    key: "handleDrop",
    value: function handleDrop(e) {
      var _this7 = this;
      if (e.type === 'dragover') {
        e.preventDefault();
        return;
      }
      if (e.type !== 'drop') {
        return;
      }
      var file = e.dataTransfer && e.dataTransfer.files && e.dataTransfer.files[0];
      if (!file || !/\.(sb|sb2|sb3)$/i.test(file.name)) {
        return;
      }
      e.preventDefault();
      var reader = new FileReader();
      reader.onload = function () {
        _this7.loadProjectBuffer(reader.result, file.name, _this7.state.autoStart);
      };
      reader.readAsArrayBuffer(file);
    }
  }, {
    key: "handleQuestionAnswer",
    value: function handleQuestionAnswer(e) {
      this.setState({
        questionAnswer: e.target.value
      });
    }
  }, {
    key: "handleQuestionSubmit",
    value: function handleQuestionSubmit(e) {
      e.preventDefault();
      this.vm.runtime.emit('ANSWER', this.state.questionAnswer);
      this.setState({
        question: null,
        questionAnswer: ''
      });
    }
  }, {
    key: "handleMouseMove",
    value: function handleMouseMove(e) {
      if (!this.canvasHost) {
        return;
      }
      if (e.type === 'touchmove') {
        e.preventDefault();
      }
      var coordinates = this.getMouseData(e);
      this.vm.postIOData('mouse', coordinates);
    }
  }, {
    key: "handleMouseDown",
    value: function handleMouseDown(e) {
      if (e.type === 'touchstart') {
        e.preventDefault();
      }
      var coordinates = this.getMouseData(e);
      this.vm.postIOData('mouse', _objectSpread({
        isDown: true,
        button: e.button || 0
      }, coordinates));
    }
  }, {
    key: "handleMouseUp",
    value: function handleMouseUp(e) {
      if (!this.canvasHost) {
        return;
      }
      var coordinates = this.getMouseData(e);
      this.vm.postIOData('mouse', _objectSpread({
        isDown: false,
        button: e.button || 0
      }, coordinates));
    }
  }, {
    key: "handleWheel",
    value: function handleWheel(e) {
      e.preventDefault();
      this.vm.postIOData('mouseWheel', {
        deltaY: e.deltaY
      });
    }
  }, {
    key: "handleKeyDown",
    value: function handleKeyDown(e) {
      if (isEditableElement(e.target)) {
        return;
      }
      var key = !e.key || e.key === 'Dead' ? e.keyCode : e.key;
      this.vm.postIOData('keyboard', {
        key: key,
        keyCode: e.keyCode,
        isDown: true
      });
      if (e.keyCode === 32 || e.keyCode >= 37 && e.keyCode <= 40 || e.keyCode === 8) {
        e.preventDefault();
      }
    }
  }, {
    key: "handleKeyUp",
    value: function handleKeyUp(e) {
      if (isEditableElement(e.target)) {
        return;
      }
      var key = !e.key || e.key === 'Dead' ? e.keyCode : e.key;
      this.vm.postIOData('keyboard', {
        key: key,
        keyCode: e.keyCode,
        isDown: false
      });
    }
  }, {
    key: "renderMonitorList",
    value: function renderMonitorList() {
      if (this.state.monitors.length === 0) {
        return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
          className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.emptyState
        }, "Visible monitors will appear here when the loaded project updates them.");
      }
      return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.monitorList
      }, this.state.monitors.map(function (monitor) {
        return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
          className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.monitorItem,
          key: monitor.id
        }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
          className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.monitorLabel
        }, monitor.label), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
          className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.monitorValue
        }, monitor.value));
      }));
    }
  }, {
    key: "renderLogList",
    value: function renderLogList() {
      if (this.state.compilerLog.length === 0) {
        return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
          className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.emptyState
        }, "Compile errors, load failures, and runtime actions are streamed here.");
      }
      return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.logList
      }, this.state.compilerLog.map(function (entry) {
        return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
          className: "".concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.logItem, " ").concat(entry.type === 'error' ? _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.logError : ''),
          key: entry.id
        }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
          className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.logMeta
        }, entry.time, " \xB7 ", entry.type), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", null, entry.message));
      }));
    }
  }, {
    key: "render",
    value: function render() {
      var _this8 = this;
      var offloadSummary = this.state.offloadEnabled ? "".concat(this.state.wasmEnabled ? 'WASM' : '') + "".concat(this.state.wasmEnabled && this.state.webglEnabled ? ' + ' : '') + "".concat(this.state.webglEnabled ? 'WebGL' : '') || 'Planner On' : 'JS only';
      return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.page
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.shell
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.stagePanel
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.stageHeader
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.eyebrow
      }, "TurboWasm VM Live Surface"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h1", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.title
      }, "Local VM, GUI renderer, WebGL stage."), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.subtitle
      }, "GUI \u5074\u306E stage \u521D\u671F\u5316\u30D1\u30BF\u30FC\u30F3\u3067 VM \u3092\u76F4\u63A5\u99C6\u52D5\u3057\u3001Scratch \u30D7\u30ED\u30B8\u30A7\u30AF\u30C8\u3092\u30EA\u30A2\u30EB\u30BF\u30A4\u30E0\u63CF\u753B\u3057\u307E\u3059\u3002 `.sb3` \u306E\u30C9\u30ED\u30C3\u30D7\u3001URL \u8AAD\u8FBC\u3001Turbo/Compiler/Offload \u5207\u66FF\u306B\u5BFE\u5FDC\u3057\u3066\u3044\u307E\u3059\u3002")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pills
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: "".concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pill, " ").concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pillActive)
      }, this.state.rendererBackend), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: "".concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pill, " ").concat(this.state.isRunning ? _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pillActive : '')
      }, this.state.isRunning ? 'Running' : 'Idle'), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pill
      }, offloadSummary))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.stageViewport
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.canvasHost,
        ref: this.setCanvasHost
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayTop
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayTitle
      }, "Project"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayText
      }, this.state.projectLabel)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayTitle
      }, "Input"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayText
      }, "Mouse, wheel, keyboard, ask blocks"))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayBottom
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayTitle
      }, "Drop Zone"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayText
      }, "Drop `.sb`, `.sb2`, or `.sb3` anywhere")), this.state.error ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayTitle
      }, "Error"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.overlayText
      }, this.state.error)) : null), this.state.question !== null ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.questionPanel
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.questionTitle
      }, "Question"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.questionText
      }, this.state.question || 'Enter your answer'), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("form", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.questionForm,
        onSubmit: this.handleQuestionSubmit
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("input", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.questionInput,
        onChange: this.handleQuestionAnswer,
        placeholder: "Type an answer for the project",
        ref: this.questionInputRef,
        value: this.state.questionAnswer
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.button,
        type: "submit"
      }, "Send Answer"))) : null)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("aside", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.controlPanel
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.section
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h2", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.sectionTitle
      }, "Transport"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.buttonRow
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.button,
        disabled: this.state.isLoading,
        onClick: this.handleGreenFlag
      }, "Green Flag"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: "".concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.button, " ").concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.dangerButton),
        onClick: this.handleStopAll
      }, "Stop"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.secondaryButton,
        onClick: this.handleTurboToggle
      }, "Turbo: ", this.state.turboEnabled ? 'On' : 'Off'))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.section
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h2", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.sectionTitle
      }, "Load Project"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.controlGrid
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.fieldRow
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("select", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.selectInput,
        onChange: this.handleSampleChange,
        value: this.state.selectedSample
      }, Object.keys(SAMPLE_PROJECTS).map(function (key) {
        return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("option", {
          key: key,
          value: key
        }, SAMPLE_PROJECTS[key].label);
      })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.secondaryButton,
        onClick: this.handleSampleLoad
      }, "Load Sample")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("form", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.fieldRow,
        onSubmit: this.handleRemoteLoad
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("input", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.textInput,
        onChange: this.handleProjectUrlChange,
        placeholder: "https://example.com/project.sb3",
        value: this.state.projectUrl
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.secondaryButton,
        type: "submit"
      }, "Load URL")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.buttonRow
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("button", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.secondaryButton,
        onClick: this.handleFilePicker
      }, "Open File")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("input", {
        accept: ".sb,.sb2,.sb3",
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.hiddenInput,
        onChange: this.handleFileChange,
        ref: this.fileInputRef,
        type: "file"
      }))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.section
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h2", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.sectionTitle
      }, "Compiler Surface"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.toggleList
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(Toggle, {
        checked: this.state.autoStart,
        hint: "Start the loaded project immediately after load.",
        label: "Autostart",
        onChange: function onChange() {
          return _this8.setState(function (prevState) {
            return {
              autoStart: !prevState.autoStart
            };
          });
        }
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(Toggle, {
        checked: this.state.compilerEnabled,
        hint: "Toggle the TurboWarp compiler path in the local VM.",
        label: "Compiler",
        onChange: this.handleCompilerToggle
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(Toggle, {
        checked: this.state.offloadEnabled,
        hint: "Enable loop offload planning for eligible code paths.",
        label: "Offload Planner",
        onChange: function onChange() {
          return _this8.handleToggleChange('offloadEnabled');
        }
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(Toggle, {
        checked: this.state.wasmEnabled,
        hint: "Allow eligible hot loops to target the WASM backend.",
        label: "WASM Backend",
        onChange: function onChange() {
          return _this8.handleToggleChange('wasmEnabled');
        }
      }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(Toggle, {
        checked: this.state.webglEnabled,
        hint: "Allow strict numeric kernels to target WebGL.",
        label: "WebGL Backend",
        onChange: function onChange() {
          return _this8.handleToggleChange('webglEnabled');
        }
      }))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.section
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h2", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.sectionTitle
      }, "Runtime Stats"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statsGrid
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statLabel
      }, "FPS"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statValue
      }, this.state.fps)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statLabel
      }, "Targets"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statValue
      }, this.state.targetCount)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statLabel
      }, "Compile Errors"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statValue
      }, this.state.compileErrors)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statCard
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statLabel
      }, "Stage"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("p", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.statValue
      }, this.state.stageSize))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("div", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.buttonRow
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: "".concat(_vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pill, " ").concat(this.state.isRuntimeStarted ? _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pillActive : '')
      }, "Runtime ", this.state.isRuntimeStarted ? 'Started' : 'Stopped'), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("span", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.pill
      }, this.state.isLoading ? 'Loading' : 'Ready'))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.section
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h2", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.sectionTitle
      }, "Visible Monitors"), this.renderMonitorList()), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("section", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.section
      }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement("h2", {
        className: _vm_live_css__WEBPACK_IMPORTED_MODULE_11___default.a.sectionTitle
      }, "Event Log"), this.renderLogList()))));
    }
  }]);
}(react__WEBPACK_IMPORTED_MODULE_2___default.a.Component);
Object(_app_target__WEBPACK_IMPORTED_MODULE_10__["default"])(/*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(VMLivePage, null));

/***/ }),

/***/ 0:
/*!*****************************************!*\
  !*** ../locale-data/index.js (ignored) ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* (ignored) */

/***/ }),

/***/ 1:
/*!*******************************!*\
  !*** ./lib/locales (ignored) ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* (ignored) */

/***/ }),

/***/ 2:
/*!*******************************!*\
  !*** ./lib/locales (ignored) ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* (ignored) */

/***/ }),

/***/ 3:
/*!*********************************!*\
  !*** readable-stream (ignored) ***!
  \*********************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* (ignored) */

/***/ }),

/***/ 4:
/*!********************************!*\
  !*** ./util.inspect (ignored) ***!
  \********************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* (ignored) */

/***/ })

/******/ });
//# sourceMappingURL=vm-live.js.map